# Proof Sketch Review

## Reviewed Sketch Identity

- Branch: `perspective_2/idea_2`
- Sketch attempt: 1
- Goal mode: exact-goal mode for the approved conditional theorem
- Reviewed roadmap: construct the pre-instance finite-dimensional mean-response space and its fixed coordinate map; compactify the closed convex response body in evaluation coordinates; obtain a seed-averaged exact-center correlation witness for every distribution-target pair; apply finite-dimensional minimax on each nonempty finite restriction; use the compact finite-intersection property in the same fixed body to cover an arbitrary domain; and transfer the resulting function to exact basis scores and the primitive polynomial rank bound.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch targets the formalized goal exactly. It proves one deterministic map fixed before every distribution, target, valid response policy, and realized seed, with pointwise margin \(1-2\varepsilon\) and dimension at most the primitive rank certificate. It consistently labels this as `conditional` progress, explicitly identifies `assump:mean-response-rank` as the added theorem-facing condition, and states that deriving this certificate from \(m,\tau\) alone and recovering the source's universal linear rate remain open. It therefore neither overclaims a full solution nor silently changes the deterministic exact-sign metric or arbitrary-domain scope.
- Dependency audit: The graph is acyclic and complete: `step_001` produces the common objects; `step_002` and `step_003` independently produce compactness and correlation; `step_004` consumes both; `step_005` consumes compactness and finite feasibility; and `step_006` consumes the fixed basis, the nonzero-rank boundary fact, and the global witness. Every dependency precedes its consumer, and every theorem-facing generated output has a recorded producer and final use.
- High-risk coverage: The sketch localizes all material obligations: completeness and public-transcript semantics of response rules, pre-instance quantifiers, evaluation-coordinate compactness, exact-center legality, seed-only correlation, Sion minimax orientation and attainment, arbitrary-family compact FIP, exact basis-score transfer, and all empty, zero-rank, zero-query, noiseless, and parameter-boundary branches. No generated membership, compactness, separator, or representation fact is promoted to a primitive assumption.
- Explicit-rate coverage: Objectives R1 and R2 expose \(r_A,B,k,m,\tau,\varepsilon\), specify deterministic probability mode, fixed-horizon mode, pointwise exact-sign/rank norm modes, fixed quantities, and the absence of hidden constants. `step_006` retains the exact chain \(\operatorname{dc}(\mathcal H)\le r_A\le B(1+m/\tau^2)^k\); no term, confidence conversion, domain-size dependence, or asymptotic constant is suppressed.
- Assumption and citation plausibility: Every technical assumption is cited by stable ID and used with the provenance declared in `setting.md`. The only theorem-critical external citation is Maurice Sion, *On General Minimax Theorems*, Pacific J. Math. 8(1) (1958), Theorem 3.4, DOI `10.2140/pjm.1958.8.171`. The sketch records the source objects, topology, semicontinuity and quasiconvexity/quasiconcavity hypotheses, max-min orientation, exact conclusion interface, and known non-outputs. The compact FIP and finite-dimensional evaluation facts are proved or restated directly in current notation rather than delegated to an unresolved citation.

## Early Obstruction Audit

- Limiting-case stress: If \(\mathcal X=\varnothing\), then \(\mathbb R^{\mathcal X}=\{0\}\), so \(V_A=K_A=\{0\}\), the basis and feature map are empty, and every pointwise sign requirement is vacuous; no probability law or simplex is invoked. If \(\mathcal H=\varnothing\), the target quantifier is empty and \(\operatorname{dc}(\mathcal H)=0\). If both are nonempty, `step_003` applies the correlation identity to a point mass and obtains a nonzero \(F_{R^0}\), excluding \(r_A=0\). For \(m=0\), the unique empty response rule directly produces the terminal predictor and no query-round argument is used. For every unrestricted \(\tau>0\), exact-center replies lie in \([-1,1]\subseteq[-1-\tau,1+\tau]\) and have zero validity defect. At \(\varepsilon=0\), every bridge preserves margin exactly \(1\). At \(B=k=1\), the final bound is exactly \(1+m/\tau^2\). Singleton finite sets reduce minimax to the point-mass statement; the empty finite set is handled only as the empty intersection \(K_A\).
- Theorem-critical bridge support: Every nontrivial bridge has a legal source. `step_001` takes the span over all complete deterministic rules before selecting an instance. `step_002` derives compactness from a finite evaluation-coordinate isomorphism and binary output bounds. `step_003` derives correlation from one exact-center rule covered by the universal premise. `step_004` uses a fully instantiated minimax equality. `step_005` uses closed subsets of the same compact body, not unrelated finite maps. `step_006` uses exact basis coordinates. None of these bridges requires a favorable seed, averaged oracle policy, response discretization, finite-domain encoding, representation-margin premise, or hidden compatibility condition.
- Exported-interface feasibility: The raw-control-to-output paths are exact. Complete public rules and finite rank export \(V_A\) and \(\varphi_A\); separating evaluations and \(|F_R(z_i)|\le1\) export compact \(K_A\); universal loss control exports correlation \(\rho\); correlation plus Sion exports a finite simultaneous witness; finite feasibility plus compact FIP exports \(f_h\); and basis equality plus the rank certificate exports the final theorem. The only quantitative defect is the explicit \(2\varepsilon\) in the loss-correlation identity. Tolerance, topology, minimax, FIP, and coordinate-transfer residuals are all zero.
- Theorem-critical mechanism witness gate: All six blocks have obstruction-level witnesses with the correct claim class, theorem role, source provenance, positive/control term, opposing defects, exact closure relation, boundary handling, and producer-consumer path. The lower-margin source is the universal accuracy guarantee through the signed loss identity, not merely boundedness or compactness. Compactness supplies attainment and arbitrary-family closure, but is not misused as the source of positive correlation. Every witness is compatible with the declared finite/static or arbitrary-family scope.
- Entry-state trace stress: The adaptive interaction is used only to define complete rules and verify exact-center legality. At \(m=0\) or immediate stopping, the empty/no-used-response rule produces \(g_{u,R}\) and the seed-only identity without a first update. At positive horizon, each reached exact-center reply has zero deviation independently, so no iterative error accumulates. The singleton point-mass state activates the positive-correlation source before minimax. For the arbitrary-family closure, the empty subfamily starts at nonempty \(K_A\), singleton constraints are feasible by `step_004`, and no generated global membership is consumed before `step_005` proves it.
- Obligation locality classification: Complete-rule construction and pre-instance independence (`step_001`), evaluation-coordinate compactness and continuity (`step_002`), exact-center legality/correlation and nonzero-rank boundary (`step_003`), finite minimax and attainment (`step_004`), arbitrary-domain FIP closure (`step_005`), and exact coordinates/rate assembly (`step_006`) are all `step-local`. Their sources and interfaces are already fixed under the unchanged theorem contract; later proof steps need only supply the stated derivations. There is no `sketch/interface defect` or `idea/theorem-contract defect`.
- Noncircular closure gate: \(K_A\) is produced from the primitive all-rule family and rank certificate before it is used. Correlation is produced from the primitive universal guarantee before minimax. Finite simultaneous feasibility is produced before the global FIP argument. Global \(f_h\) is produced before coordinates are taken. Neither compactness nor finite feasibility assumes a separator, and no admissibility condition restates the final representation.
- Mechanism-source and boundary stress: Evaluation upper bounds are used only for compactness, while the positive lower margin comes from \(1-2\mathbb E_U\mathcal L\ge\rho\); thus there is no source-to-claim mismatch. If this lower source vanished, the pointwise sign target could fail, but `assump:universal-adversarial-guarantee` and \(\varepsilon<1/4\) keep it strictly positive in every allowed nonempty regime. If finite rank vanished, pointwise boundedness would not yield the required compact topology, but `assump:mean-response-rank` explicitly excludes that regime in this conditional theorem.
- Generated-output flow: The table in the sketch gives a legal path for every generated output: primitive interface/rank -> `step_001` common objects -> `step_002` compact body and `step_003` exact-center correlation -> `step_004` finite witnesses -> `step_005` global witness -> `step_006` target weights and final theorem. The apparent extra use of the nonzero-rank fact by `step_006` is explicitly listed. No consumer precedes its producer or omits a dependency.
- Source-to-claim adequacy: The finite-dimensional evaluation argument supplies exactly compactness and continuity; it is not used as a positivity theorem. The signed identity uses the same binary predictor, target, distribution, and seed law as the setting and supplies exactly the lower correlation needed by minimax. Sion's theorem supplies only order equality for the same bilinear payoff on \(K_A\times\Delta(S)\); the sketch separately supplies compactness, per-\(p\) correlation, extreme-point reduction, attainment, and global closure. The direct FIP proof supplies arbitrary-cardinality intersection in the same topology, and the basis identity supplies exact pointwise scores.
- Residual-to-target adequacy: Every produced and consumed object is compared in the theorem's pointwise interface. Evaluation coordinates are an isomorphism of the same functions; \(F_{R^0}\) is the exact function consumed by minimax; finite and global constraints use the same evaluations and threshold; and \(f_h(x)-\langle w_h,\varphi_A(x)\rangle=0\). There is no transformed target, surrogate metric, wrong-scale error, persistent empirical term, or unbounded residual to dominate.
- Baseline invariance audit: The inherited exact/noiseless conclusions are preserved. At \(m=0\), the same deterministic exact-map conclusion holds and the rank bound specializes to \(r_A\le B\). At \(\varepsilon=0\), exact-center correlation, minimax, FIP, and basis transfer all preserve threshold \(1\), not a positive-margin surrogate. At \(B=k=1\), the exact displayed polynomial remains. Empty-domain/class specializations retain the original vacuous exact-sign conclusion rather than replacing it by an approximate or conditional statement.
- Scope-accumulation compatibility: There is no all-time or stochastic-recursion claim. Across at most \(m\) query rounds, the accumulated tolerance defect is the sum of zeros because every exact-center response is exactly centered. Across the arbitrary family of domain points, the controlled quantity is feasibility of \(\bigcap_{x\in S}C_{h,x}\); its forcing/residual is identically zero, every finite intersection is nonempty at the unchanged threshold, and compactness converts this exact FIP into the full intersection. This is a concrete scope-compatible relation, not a category-only compactness label.
- Scope and dependence consistency: The sketch preserves all formal quantifiers and modes: fixed learner before instances, all distributions and targets, every deterministic tolerance-valid complete response rule in the premise, expectation solely over learner seed, arbitrary domains, finite \(m\), every \(\tau>0\), deterministic exact signs, and fixed numerical \(B,k\). Only \(w_h\) depends on the target. The conditional rank premise and polynomial rather than source-linear rate are openly exposed.
- Generated-condition provenance: The common space, basis, compact body, exact-center membership, correlation, finite witnesses, global witness, and weights are all classified as derived and assigned to producer steps. The public theorem assumes only the four primitive conditions from `setting.md`. No event, local-validity condition, boundedness claim, or global membership fact is hidden in an admissibility package.
- Citation and tool applicability: Sion (1958), Theorem 3.4, DOI `10.2140/pjm.1958.8.171`, is mapped with \(M=K_A\), \(N=\Delta(S)\), and payoff \(L_h(f,p)=\sum_xp(x)h(x)f(x)\). Both sets are nonempty compact convex sets in the stated finite-dimensional conventions; \(L_h\) is continuous affine in each variable, hence upper-semicontinuous/quasiconcave in the maximizing variable and lower-semicontinuous/quasiconvex in the minimizing variable. Its max-min form (or the equivalent application to \(-L_h\)) yields exactly \(\max_f\min_pL_h=\min_p\max_fL_h\). The finite minimum is continuous in \(f\), so compactness of \(K_A\) supplies the selected maximizer. The sketch correctly records that Sion does not supply the fixed body, correlation, attainment bridge, or arbitrary-domain FIP.
- Same-setting repair plausibility: No repair is currently needed. Each hard obligation already has a source under the accepted conditional setting, an exact interface, and a lemma-sized target. A later local proof failure could still reveal a derivational error, but the obstruction-level audit exposes no theorem-contract contradiction or missing sketch bridge.
- Target-preserving bridge-repair gate: Passed without repair. The route keeps the formalized setting and goal unchanged and does not absorb a missing bridge by weakening exact signs, restricting the domain, adding policy averaging, or changing the rate. Consequently there is no basis for routing to sketch or idea revision.
- High-risk obligation classes: Structural nondegeneracy, finite-dimensional compactness, adaptive-response quantifiers, seed averaging, minimax mode conversion, arbitrary-cardinality closure, exact/noiseless specialization, explicit parameter dependence, and generated-output flow all pass. Approximation, sampling, probability-conversion, recursive-invariant, and persistent-defect classes are absent for specific reasons: every relevant transfer is an equality or a closed exact-threshold implication.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Complete public-rule mean family, finite \(V_A\), fixed basis, and pre-instance \(\varphi_A\) | Generated common object and target-independent representation interface | Direct construction from all deterministic rules assigning \(I_\tau\)-values to every admitted public query-bearing history, including histories unreachable for a particular seed/reply path; finite basis from the rank certificate | Primitive `assump:adaptive-sq-interface` and `assump:mean-response-rank`, then `step_001` | Exhaustion over all complete rules includes every later exact-center rule; the claim concerns seed-averaged \(F_R\), not individual terminal predictors | Same public queries/replies, hidden-seed convention, reply range, binary outputs, and seed law as `setting.md`; a rule sees the seed only through the public transcript | \((A,\mu_A,m,\tau,\text{interface})\mapsto\mathfrak R_A^{\mathrm{all}}\mapsto\{F_R\}\mapsto V_A\mapsto(\psi_j,\varphi_A)\), with \(f(x)=\langle w,\varphi_A(x)\rangle\) for every \(f=\sum_jw_j\psi_j\) | Constant-zero rule for \(m>0\) and unique empty rule for \(m=0\) give nonemptiness; finite rank exports the basis before \(\mathcal D,h,R,U\); consumers are `step_002`, `step_003`, and `step_006` | Same function and pointwise score; coordinate residual is identically zero | Static construction; unreachable histories and at most \(m\) adaptive rounds create no accumulated defect because the rule is complete at once | Primitive interface/rank -> `step_001`; no separator or target representation is assumed | Producer `step_001`; consumers `step_002`, `step_003`, `step_006`, and final map use | At \(m=0\) the empty rule immediately produces \(F_R\); if \(\mathcal X=\varnothing\), all functions are zero; if \(\mathcal H=\varnothing\), the target use is vacuous | `step-local` | None |
| Compact convex \(K_A\) and continuity of every evaluation | Structural boundedness/compactness and topology interface for minimax/FIP | Evaluation functionals separate \(V_A\); finite dimension makes their span all of \(V_A^*\), so select \(r_A\) evaluations forming an injective coordinate map; apply finite-dimensional Heine--Borel | Derived `step_001` plus binary-output range from `assump:adaptive-sq-interface`; direct current-notation linear algebra in `step_002` | Supplies exactly compactness and point-evaluation continuity, not the later positive margin | Closure is in the unique finite-dimensional topology of the same function space; \(E_Z\) is a coordinate isomorphism, not a transformed target | \(\delta_x\) separate points \(\Rightarrow\operatorname{span}\{\delta_x\}=V_A^*\); choose \(Z\) with \(E_Z\) injective; \(E_Z(F_R)\in[-1,1]^{r_A}\); closed plus bounded gives compact \(K_A\); \(\delta_x\circ E_Z^{-1}\) is continuous | Raw controls are finite rank and \(\lvert F_R(z_i)\rvert\le1\); exported interface is one nonempty compact convex body with continuous evaluations; consumers are `step_004` and `step_005` | Coordinates and original functions are identical under \(E_Z^{-1}\); every point value transfers exactly, with zero residual | Topological-closure scope only; closedness retains limits and boundedness is uniform, so accumulated defect is zero | `step_001` -> evaluation isomorphism -> `step_002`; global feasibility is not assumed | Producer `step_002`; compactness consumed by `step_004`/`step_005`, continuity by `step_005` | For \(r_A=0\), use \(V_A=K_A=\{0\}\) and the empty evaluation map; no nonexistent point is selected when \(\mathcal X=\varnothing\) | `step-local` | None |
| Exact-center legality, seed-only correlation \(\ge\rho\), and nonzero-rank boundary | Positive lower/sign source and response-quantifier discharge | One deterministic complete exact-center rule, covered by the every-valid-rule premise, plus \(hg=1-2\mathbf1\{g\ne h\}\) | Primitive `assump:parameter-regime`, `assump:adaptive-sq-interface`, and `assump:universal-adversarial-guarantee`, then `step_003` | Exact loss control supplies the needed positive correlation claim; no upper-bound source is substituted for a lower bound | Same query, public history, target, distribution, terminal predictor, loss, and seed law as the setting; no response-policy or oracle randomness is averaged | Exact-center replies lie in \([-1,1]\subseteq I_\tau\), have deviation zero on every reached seed path, and satisfy \(\mathbb E_{\mathcal D}[hF_{R^0}]=1-2\mathbb E_U\mathcal L_{\mathcal D,h}(g_{U,R^0})\ge\rho\) | Public queries and population expectations export one legal rule in the all-rule family; universal accuracy controls \(2\varepsilon\); consumers are `step_004` and the rank-boundary branch of `step_006` | \(F_{R^0}\) is exactly the function consumed in \(K_A\); correlation identity residual and tolerance residual are zero | At most \(m\) validity checks, each with zero deviation; only defect is the explicit controlled \(2\varepsilon\), with no policy or seed accumulation | Primitive guarantee -> exact-center legality -> correlation; no separator or compactness conclusion is assumed | Producer `step_003`; finite-correlation consumer `step_004`, nonzero-rank consumer `step_006` | At \(m=0\), use the empty rule; for all \(\tau>0\), exact center remains legal; at \(\varepsilon=0\), correlation is \(1\); point masses on nonempty \(\mathcal X\) and nonempty \(\mathcal H\) force \(r_A\ge1\) | `step-local` | None |
| Finite simultaneous witness \(f_{h,S}\) at exact margin \(\rho\) | Minimax scope conversion and finite structural margin | Sion (1958), Theorem 3.4, DOI `10.2140/pjm.1958.8.171`, applied to compact convex \(K_A\), finite simplex \(\Delta(S)\), and continuous affine \(L_h\) | Cited theorem with exact label/stable locator plus derived outputs `step_002` and `step_003`, then `step_004` | Sion supplies exactly max-min/min-max equality; per-\(p\) lower bounds and compact attainment are supplied separately | Source maximizing variable is \(f\in K_A\) in the branch's finite-dimensional topology; minimizing variable is \(p\in\Delta(S)\); affine continuity gives the required upper/lower semicontinuity and quasi-concavity/convexity | \(\forall p,\max_fL_h(f,p)\ge\rho\Rightarrow\min_p\max_fL_h\ge\rho=\max_f\min_pL_h\), and \(\min_pL_h(f,p)=\min_{x\in S}h(x)f(x)\); compactness attains the maximum | Raw controls are compact \(K_A\), nonempty finite \(S\), and one exact-center witness for each finitely supported \(p\); exported interface is one \(f_{h,S}\) in the fixed body; consumer is `step_005` | Witness and pointwise scores use the same function, evaluations, and threshold; basis-score residual remains zero | Finite aggregation only; witness may vary with \(p\) inside a fixed \(K_A\), but body/map variation and approximation defects are zero | `step_002` + `step_003` -> `step_004`; finite feasibility is produced, not assumed | Producer `step_004`; consumer `step_005` and its finite intersections | Singleton \(S\) is exactly the point-mass case; empty \(S\) is excluded from minimax and handled as the empty intersection; \(\rho=1\) is preserved | `step-local` | None |
| Global \(f_h\) for every point of arbitrary \(\mathcal X\) | Arbitrary-family scope upgrade and exact global membership | Compact finite-intersection theorem, proved directly for closed \(C_{h,x}\subseteq K_A\) using open complements and a finite subcover | Derived compactness/continuity from `step_002` and finite feasibility from `step_004`, then direct current-notation proof in `step_005` | Exact FIP in one fixed compact body supplies precisely the arbitrary-cardinality intersection; no generic finite-restriction representation claim is used | Every \(C_{h,x}\) uses the same real body, topology, point evaluation, target, and fixed threshold as finite minimax | \(\forall\) finite \(S,\ \bigcap_{x\in S}C_{h,x}\ne\varnothing\); if the total intersection were empty, open complements would have a finite subcover, contradicting that finite feasibility | Raw controls are compact \(K_A\), continuous evaluations, and all finite witnesses; exported interface is one \(f_h\in\bigcap_xC_{h,x}\); consumer is `step_006` | Finite and global constraints concern the identical function object and threshold; no limit-map, basis, or margin residual | Arbitrary-family scope; accumulated forcing/residual is identically zero, and compact FIP controls the whole family rather than only a sequence | `step_002` + `step_004` -> `step_005`; no global separator or compatibility premise appears upstream | Producer `step_005`; consumer `step_006` | Empty subfamily intersects to nonempty \(K_A\); singleton feasibility comes from `step_004`; if \(\mathcal X=\varnothing\), the total intersection is \(K_A\); threshold \(1\) is preserved at \(\varepsilon=0\) | `step-local` | None |
| Exact weights, strict signs, polynomial dimension, and baseline specializations | Exact target transfer, quantitative specialization, and final theorem closure | Exact basis-coordinate identity from `step_001`, global margin from `step_005`, nonzero-rank boundary from `step_003`, and primitive rank inequality | Derived producer steps plus `assump:parameter-regime` and `assump:mean-response-rank`, then `step_006` | Exact equality supplies the same pointwise score claim; positive \(\rho\) supplies strict signs; primitive rank supplies the displayed structural bound | Same fixed basis and feature map selected before instances; only coordinates \(w_h\) depend on \(h\); no source/target metric change | \(f_h=\sum_j(w_h)_j\psi_j\Rightarrow\langle w_h,\varphi_A(x)\rangle=f_h(x)\), hence \(h(x)\langle w_h,\varphi_A(x)\rangle\ge\rho>0\) and \(\operatorname{dc}(\mathcal H)\le r_A\le B(1+m/\tau^2)^k\) | Raw controls are the fixed basis, global witness, \(\rho>1/2\), and rank certificate; exported interface is the complete formalized theorem with no hidden constants; final assembly is the consumer | Exact same-target residual \(f_h(x)-\langle w_h,\varphi_A(x)\rangle=0\) at every point | Static assembly; all domain accumulation closed upstream; no term is dropped, absorbed, randomized, or made asymptotic | `step_001` + `step_003` + `step_005` -> `step_006`; rank assumption bounds the response space and does not assume \(\operatorname{dc}(\mathcal H)\) | Producer `step_006`; consumer final theorem | Empty \(\mathcal X\)/\(\mathcal H\) give \(\operatorname{dc}=0\); nonempty branches exclude \(r_A=0\); \(m=0\) gives \(r_A\le B\); \(\varepsilon=0\) gives margin \(1\); \(B=k=1\) gives \(1+m/\tau^2\); all \(\tau>0\) remain covered | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is ready for theorem-level diagnostic and step-local proof work. Its central quantifier conversion is supported by a fixed pre-instance body: exact-center witnesses may vary with the finite-support distribution, but the response space, compact body, basis, and feature map never vary. Sion's theorem then gives the finite simultaneous margin in the correct orientation, compact FIP globalizes that exact closed threshold over an arbitrary domain, and basis coordinates transfer it without residual. All hard obligations have source-adequate witnesses, complete generated-output flow, noncircular dependencies, and successful boundary traces.

The result is also labeled at the right research level. It is an exact proof target for the approved conditional setting and material partial progress toward the source question, not a claimed resolution of the unconditional linear implication. Since every unresolved derivation is genuinely `step-local` under the unchanged setting and no blocker or interface repair remains, `ACCEPTED` with score 9, retry target `None`, and retry mode `none` is the aligned outcome.
