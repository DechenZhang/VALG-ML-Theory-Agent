# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Reviewed setting SHA-256: `a77db70b5e184b062eabd7b32b21c1f0010c2e7122cd6c4ed4c2d277ff0e7587`
- Reviewed sketch SHA-256: `ec48a538bbde2f7a738402d2625a54eecfbb48693d25bed97f9abea9f61450da`
- Reviewed technical-survey SHA-256: `1988f8d836070f687d21033d38124a6675a5fd4208deab941ef2174f8c9069ce`

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The branch is in exact-goal mode for the conditional theorem in `setting.md`. The sketch proves the same pre-instance canonical-policy catalog theorem, the same pointwise margin \(1-2\varepsilon\), and the same deterministic rate \(\operatorname{dc}(\mathcal H)\le L\le B(1+m/\tau^2)^k\). It neither strengthens the catalog to other valid policies nor weakens exact signs, arbitrary-domain scope, or the adversarial-SQ premise. Its `conditional` progress label is faithful: removing the catalog certificate and recovering the unconditional linear rate remain explicitly open.
- Dependency audit: The graph `step_001 -> step_002 -> step_003 -> step_004 -> step_005`, with the additional legal edge `step_001 -> step_005`, is acyclic and every consumer uses an earlier producer. Each step has a stable ID, exact claim, named assumptions, proof tool, output target, rate objective, and review status. Generated validity, correlation, finite separators, and the global separator are produced before use.
- High-risk coverage: The sketch localizes all material obligations: legality of reached canonical executions, catalog membership only on those executions, finite-range extraction without an output-index selector, exact risk-correlation conversion, the minimizing-distribution/maximizing-mixture minimax orientation, arbitrary-family compactness in one fixed simplex, and exact coordinate-sign closure. The requested null and boundary regimes are addressed explicitly.
- Explicit-rate coverage: R1 and R2 expose \(m,\tau,\varepsilon,L,B,k\), declare no hidden constants, identify fixed protocol objects, and state deterministic probability mode, fixed finite horizon mode, and pointwise/exact-sign norm mode. The direct rate chain has no absorption or specialization gap. The cases \(m=0\), \(\varepsilon=0\), \(L=1\), \(B=1\), \(k=1\), and arbitrary \(\tau>0\) are retained.
- Assumption and citation plausibility: All theorem-facing assumptions are the three stable setting assumptions. Every additional property is derived. Sion (1958), Theorem 3.4, DOI 10.2140/pjm.1958.8.171, is identified by source and label, instantiated on the two finite compact convex simplices with continuous bilinear payoff, and used only for the exact min-max equality. The simplex extreme-point identities and the Heine--Borel/FIP argument are stated in current notation, including the latter's finite-subcover proof.

## Early Obstruction Audit

- Limiting-case stress: The mechanism remains valid for \(m=0\) (the actual no-query execution), \(\varepsilon=0\) (finite-range scalar loss yields an attained zero-risk value and margin \(1\)), \(L=1\), repeated catalog functions, every finite \(\tau>0\), nonatomic tapes, and empty, finite, countably infinite, or uncountable domains. Empty \(\mathcal X\) and empty \(\mathcal H\) give the stated vacuous sign conclusion and dimension \(0\). No allowed boundary makes a required conclusion false while its source is inactive.
- Theorem-critical bridge support: Every bridge is present: rounded centers produce actual valid executions; actual loss plus the canonical catalog produces per-distribution correlation; finite-support instantiations plus minimax produce simultaneous finite separators; closed-set FIP produces a global separator; exact coordinates and the primitive catalog bound produce signs and the rate. No bridge assumes its own output.
- Exported-interface feasibility: `step_001` exports response error at most \(1/K\le\tau\) and exact terminal-function membership; `step_002` exports risk at most \(\varepsilon\) and exact correlation at least \(1-2\varepsilon\); `step_003` exports the same pointwise payoff and threshold on each finite set; `step_004` exports the same constraints in the same \(\Delta_L\); `step_005` exports the identical mixture as a Euclidean score. Controlled and deliberately unused classes are separated: invalid synthetic branches and noncanonical valid-policy outputs are uncontrolled but have no consumer.
- Theorem-critical mechanism witness gate: Passed for all five steps. Each witness names a mechanism source, the controlling relation, opposing defects, boundary behavior, and producer-consumer path. Grid covering radius supports validity; finite attained scalar-loss range supports correlation; Sion's equality supports quantifier reversal; fixed-simplex closed-set FIP supports arbitrary-cardinality globalization; positive exact margin plus the primitive size inequality supports final closure.
- Entry-state trace stress: The only adaptive trajectory obligation is `step_001`. At \(m=0\) its empty execution is already actual and catalog-covered. If a first query is reached, its center lies in \([-1,1]\), and rounding is tolerance-valid before the risk premise is consumed; the same local check applies at every subsequent reached query without an accumulated-error premise. The later steps are static. At \(\varepsilon=0\), the scalar-loss argument activates directly and needs no probabilistic selector.
- Obligation locality classification: Canonical validity/membership (`step_001`), finite-range risk-to-correlation (`step_002`), finite minimax (`step_003`), fixed-simplex FIP globalization (`step_004`), and exact sign/rate closure (`step_005`) are all `step-local`. No theorem-critical obligation is a `sketch/interface defect` or `idea/theorem-contract defect`.
- Noncircular closure gate: Passed. The producer path is primitive rounding and catalog assumptions -> actual execution -> measurable scalar loss -> correlation -> finite game value -> finite feasibility -> global FIP intersection -> exact sign/rate. Neither global feasibility nor any generated condition appears as an assumption, and the same property is never consumed before production.
- Mechanism-source and boundary stress: The positive sources have the correct claim classes. The upper expected-risk bound is paired with finite attained range before being used for an existence claim; minimax, rather than a varying best-index selector, supplies the simultaneous lower bound; compactness acts on closed constraints at the unchanged positive threshold. If canonical validity, finite catalog range, minimax equality, fixed compactness, or positive margin were removed, the corresponding bridge could fail, but each source is present under the binding contract.
- Generated-output flow: Passed. `step_001` feeds `step_002` and supplies fixed catalog data to `step_005`; `step_002` feeds every finite-support game in `step_003`; `step_003` supplies every finite intersection to `step_004`; `step_004` supplies \(w_h\) to `step_005`; `step_005` supplies the theorem. There is no consumer-before-producer edge.
- Source-to-claim adequacy: Direct derivations use the exact branch convention: bounded queries, additive tolerance, actual execution, binary risk, and exact catalog-function equality. The finite-range argument integrates only the primitive measurable scalar loss and uses no measurable selector. Sion's cited result is mapped hypothesis by hypothesis and supplies exactly the needed order equality, not catalog existence or compactness. The standard FIP fact is proved in current notation. The produced mixture function and the consumed linear score are the same object, so no object-target convention bridge is missing.
- Residual-to-target adequacy: The only nonzero residual is canonical response rounding, bounded in the consumed absolute-error metric by \(1/K\le\tau\). Every later transfer has zero residual: actual output equals a catalog function, binary risk converts exactly to correlation, matrix payoff equals pointwise signed score, finite and global constraints are identical, and the final mixture equals the linear score coordinate by coordinate. Thus all residuals are controlled at the required target scales.
- Baseline invariance audit: The inherited conditional theorem's baseline conclusions are preserved. At \(m=0\), the no-query run remains actual and the rate becomes \(L\le B\). At \(\varepsilon=0\), the proof gives pointwise margin exactly \(1\), not a remainder. At \(L=1\), the singleton simplex is used without a dimension or margin loss. Empty-domain/class, duplicate-coordinate, arbitrary-\(\tau\), infinite-domain, and nonatomic-tape cases remain within the same statement. No weaker stopped, average, or probabilistic surrogate replaces an exact conclusion.
- Scope-accumulation compatibility: Reached-query validity has no additive forcing term because the SQ contract is per query: \(|a_t-v_t|\le\tau\) independently at each of at most \(m\) reached queries. The finite games use exact finite sums. Globalization has identically zero residual and uses the arbitrary-family FIP implication in one fixed compact simplex, rather than a sequential limit or a changing-space argument. Hence there is no persistent, adversarial-sign, or uncontrolled accumulated defect.
- Scope and dependence consistency: The proof retains one learner for every distribution, target, and valid adversarial policy, then instantiates that premise at one proved-valid canonical policy. Finite-support distributions are only proof instances of the every-distribution premise. The final map is fixed before the current instance, and only \(w_h\) depends on the target. No dependence on \(|\mathcal X|\), distribution, policy, replies, tape, or catalog multiplicity is introduced.
- Generated-condition provenance: Canonical validity, actual membership instantiation, low-risk catalog response, finite separator, and global separator are all derived by named steps. The sketch assumes no measurable index selector, finite tape support, random feature-map law, all-valid-policy catalog, stability event, recurrence invariant, or mean-response rank.
- Citation and tool applicability: Sion's source identity, exact theorem label, stable DOI, role, current objects, finite-dimensional compact-convex hypotheses, bilinear continuity, orientation, and non-output boundary are recorded. The exact use is \(\max_w\min_p p^TAw=\min_p\max_w p^TAw\), followed by simplex extreme points to obtain \(\min_p\max_i=\max_w\min_x\). The compactness source is standard and is also proved by a finite-subcover contradiction, so no unresolved theorem-critical citation applicability remains.
- Same-setting repair plausibility: No repair is required. All hard obligations have a source and complete sketch-level interface under the present setting. The review does not infer anything about removing the primitive catalog certificate, which would be an idea/theorem-contract change rather than unfinished local work.
- Target-preserving bridge-repair gate: Passed without a repair obligation. The existing roadmap already gives target-preserving, zero-residual bridges for every theorem-facing output. It does not weaken the conditional exact theorem or its baseline cases.
- High-risk obligation classes: Structural positive-sign claims, approximation error, adaptive execution validity, quantifier/mode conversion, arbitrary-family closure, explicit dependence, baseline specialization, and hidden generated assumptions were all scanned. No recursive stability, stochastic concentration, public-rate simplification, transformed entry object, or surrogate target appears.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Actual canonical-policy validity and catalog membership | Generated local-validity/membership certificate enabling the only learner execution used in the proof | Nearest-grid covering radius plus primitive canonical catalog implication | `assump:sq-parameter-regime`, bounded-query convention in `assump:universal-adversarial-sq`, and `assump:canonical-rounded-output-catalog` | A per-query absolute-error source directly proves tolerance validity; actual validity activates exact terminal-function membership | Exact branch conventions: centers and replies in \([-1,1]\), additive tolerance \(\tau\), fixed tie rule, reached adaptive queries only | \(K=\lceil1/\tau\rceil\), so \(|\rho(v)-v|\le1/K\le\tau\) at every reached query; membership then holds as equality of functions | Raw bounded center, fixed grid, at most \(m\) reached queries, and primitive actual-run catalog clause export a valid execution and exact member | Response residual is at most \(\tau\) in absolute error; terminal-object residual is zero | No summed forcing: validity is checked locally per reached query; adaptive query changes are not response defects; invalid and noncanonical branches are unused | Primitive parameter/query facts -> `step_001` validity -> primitive membership -> `step_002`; no risk or separator premise is used | Producer `step_001`; consumers `step_002` and catalog/map use in `step_005` | \(m=0\) is the actual empty execution; first reached query is valid before consumption; \(\tau\ge1\), ties, early stop, and nonatomic tapes pass | `step-local` | None |
| Low-risk actual catalog value and per-distribution correlation | Positive lower-bound certificate supplying each finite game's best response | Universal expected actual-run loss plus finite set of occurring catalog-risk values; exact binary risk-correlation identity | `assump:universal-adversarial-sq`, catalog membership from `step_001`, and direct current-notation algebra | If every occurring finite-range value exceeded \(\varepsilon\), their minimum would exceed \(\varepsilon\), contradicting \(\mathbb EZ\le\varepsilon\); this yields the required same-function correlation | Same \(\mathcal D,h\), binary loss, and actual canonical execution as the branch; only scalar \(Z\) needs measurability, with no output-index convention | \(\min \operatorname{range}(Z)\le\varepsilon\) by contradiction and \(\mathbb E[hg_i]=1-2\mathcal L(g_i)\ge1-2\varepsilon\) | Raw measurable \(Z\in[0,1]\), its expectation bound, and finite actual range export an occurring \(g_i\) with the exact correlation interface | Zero transfer residual: risk and correlation concern the same function and exact identity | Static argument; nonatomic tapes, nonmeasurable index events, and zero-mass outputs create no accumulated or integration defect because no selector/event probability is used | `step_001` -> actual \(Z\) and finite range -> scalar contradiction in `step_002` -> `step_003` | Producer `step_002`; consumer `step_003` for each finitely supported \(\mathcal D_p\) | \(m=0\) unchanged; \(\varepsilon=0\) forces an occurring zero-risk value; \(L=1\) and duplicate functions pass | `step-local` | None |
| Finite simultaneous separator on each finite \(F\) | Finite minimax/scope-conversion certificate changing \(\forall p\exists i\) into \(\exists w\forall x\) | Sion (1958), Theorem 3.4, specialized to the bilinear finite game, plus simplex extreme-point identities | Maurice Sion, *On General Minimax Theorems*, Pacific J. Math. 8 (1958), Theorem 3.4, DOI 10.2140/pjm.1958.8.171; derived correlation from `step_002` | The theorem gives exactly max-min equals min-max; finite-support instantiation gives the left game's lower bound | Current objects are nonempty finite-dimensional compact convex \(\Delta_L,\Delta_F\); \(f(w,p)=p^TAw\) is real, continuous, affine in each variable; \(A_{xi}=h(x)g_i(x)\) | \(\min_p\max_i p^TAe_i=\max_w\min_x(Aw)_x\ge\rho_\varepsilon\), with the maximum attained | Raw per-\(p\) correlation bounds and finite point values export \(w_{h,F}\in\Delta_L\) satisfying every point constraint on \(F\) | Zero residual: \((Aw)_x=h(x)\sum_iw_ig_i(x)\) is the consumed signed score at the same threshold | Best index may vary with \(p\), but minimax controls it exactly; finite sums have no measurability or approximation defect; no error accumulates across \(F\) | `step_002` for all \(\mathcal D_p\) -> game lower bound -> cited equality -> `step_003` output -> `step_004` | Producer `step_003`; consumer `step_004` | Singleton \(F\), \(L=1\), duplicate columns, zero weights, and \(\varepsilon=0\) pass; empty \(F\) is separately vacuous because \(\Delta_L\ne\varnothing\) | `step-local` | None |
| Global separator on arbitrary \(\mathcal X\) | Arbitrary-family theorem-closure certificate preserving all pointwise margins | Compactness of fixed \(\Delta_L\) and closed-set finite-intersection theorem, with direct open-cover proof | Heine--Borel plus the compact-space FIP fact stated and proved in current notation; finite feasibility from `step_003` | Exact finite feasibility for the same closed halfspaces is precisely the FIP premise needed for a total intersection | One fixed finite-dimensional simplex and the same linear point-evaluation convention are used for every finite set and for the full family | \(\forall F\subseteq\mathcal X\text{ finite}:\bigcap_{x\in F}C_{h,x}\ne\varnothing\Rightarrow\bigcap_{x\in\mathcal X}C_{h,x}\ne\varnothing\) | Raw \(L\ge1\), compact \(\Delta_L\), closed \(C_{h,x}\), and finite feasibility export one \(w_h\) for all points | Zero residual: the finite and global inequalities use identical weights, coordinates, and threshold | Arbitrary cardinality and incompatible finite witnesses are handled by FIP; accumulated residual/forcing is identically zero; no sequence is used | Primitive finite \(L\) -> fixed compact simplex; `step_003` -> every finite intersection; FIP -> `step_004`; global feasibility is not assumed | Producer `step_004`; consumer `step_005` | Empty domain gives total intersection \(\Delta_L\); finite, countable, uncountable domains, \(L=1\), duplicates, and threshold \(1\) pass | `step-local` | None |
| Exact pointwise sign representation (R1) | Structural positive-margin closure yielding one common deterministic feature map | Exact catalog-coordinate identity and global weight from `step_004` | `step_004`, pre-instance catalog data from `step_001`, and direct definition-level algebra | The produced mixture is exactly the consumed linear score; \(1-2\varepsilon>1/2>0\) supplies strict sign rather than an upper-bound surrogate | Same \(g_i\) functions, coordinate order, original domain, target, and Euclidean inner product as in the goal | \(h(x)\langle w_h,\phi(x)\rangle=h(x)\sum_iw_{h,i}g_i(x)\ge1-2\varepsilon>0\) | Raw fixed map and global pointwise certificate export exact signs; only \(w_h\) depends on \(h\) | Mixture-to-score residual is identically zero, dominated by the strict margin | Static assembly; no probability, limit, transformed object, or persistent defect | `step_001` fixes coordinates; `step_004` produces \(w_h\); `step_005` applies exact identity | Producer `step_005`; consumer is the formalized theorem and dimension definition | \(\varepsilon=0\) gives margin \(1\); \(L=1\), duplicates, empty \(\mathcal X/\mathcal H\), infinite domains, and nonatomic tapes pass | `step-local` | None |
| Explicit deterministic dimension rate and baseline specializations (R2) | Quantitative closure and baseline invariance obligation | Definition of \(\operatorname{dc}\), strict sign certificate, and primitive catalog-size bound | `assump:canonical-rounded-output-catalog`, `assump:sq-parameter-regime`, and `step_005` exact sign output | A common \(L\)-coordinate exact sign map directly implies \(\operatorname{dc}\le L\); the primitive certificate gives the requested displayed polynomial bound | Exact deterministic dimension metric, fixed finite horizon, no hidden constants, and no domain-size or probabilistic convention change | \(\operatorname{dc}(\mathcal H)\le L\le B(1+m/\tau^2)^k\) by direct substitution, with no dropped term | Raw pre-instance map, strict margin, and exact catalog bound export the full rate in \(m,\tau,\varepsilon,L,B,k\) (with \(\varepsilon\) visible in the companion margin) | Zero residual and no simplification loss; no hidden-constant dependence | No accumulation or probability conversion; catalog multiplicity and domain cardinality do not enter the bound | Primitive size certificate plus the derived sign map -> `step_005` -> final rate; representation is not assumed by the catalog | Producer `step_005`; consumer is final theorem assembly | \(m=0\) gives \(L\le B\); \(\varepsilon=0\) retains margin \(1\); \(B=1,k=1,L=1\), arbitrary \(\tau>0\), and empty-domain/class dimension \(0\) pass | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is ready for step-level proof work. It proves the binding conditional exact theorem through five acyclic, lemma-sized interfaces; every theorem-critical mechanism has an already available source with exact convention matching, zero or explicitly bounded residual, legal generated-output flow, and complete boundary handling. The citation-dependent minimax step is stated with the correct minimizing-distribution/maximizing-mixture orientation and a full hypothesis map, while the arbitrary-domain step uses the finite-intersection property in one fixed compact simplex. Because every hard obligation is `step-local` and no theorem-contract or sketch-interface repair is needed, `ACCEPTED` is the aligned status.
