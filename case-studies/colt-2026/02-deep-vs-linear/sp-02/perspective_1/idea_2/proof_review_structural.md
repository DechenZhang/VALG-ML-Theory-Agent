# Structural Proof Review

## Reviewer Verdict

PASS

## Blocking Issues

None

## Nonblocking Concerns

1. An independent `tectonic --keep-logs --keep-intermediates` build completed successfully with BibTeX and resolved references/citations, but its console reported one `5.2267pt` overfull box in `5_appendix.tex` lines 846--853. This is a small typesetting issue in the simultaneous-certificate statement, not a mathematical, dependency, self-containment, or compilation blocker. It also means the `assembly_report.md` note claiming no box warnings is not literally reproduced by the independent build.

## Suggested Repair Depth

None

## Smallest Candidate Repair Target

None

## Structural Audit

- Goal alignment: Exact-goal mode passes. The main theorem preserves the two authoritative primitive assumptions, the pre-instance quantifier on the single law \(\mathsf P_A\), simultaneous universality over every \((\mathcal D,h)\), the pointwise infimum inside the map expectation, the full comparison chain, and the exact conclusion \(\operatorname{dc}^{\mathrm{tie}}_\varepsilon(\mathcal H)\le(\lceil1/\tau\rceil+1)^m\). It does not claim deterministic exactification, finite-domain structure, a favorable oracle, or polynomial response-tree compression.
- Dependency closure: The accepted graph is acyclic and closed: `step_001` attempt 1 has no step dependency; `step_002` attempt 2 consumes the accepted `step_001` interface; `step_003` attempt 1 consumes the current accepted `step_001` and `step_002` pairs; and `step_004` attempt 1 consumes all three current accepted pairs. Every reviewed proof digest matches its current accepted review, including `step_002` proof SHA-256 `04991f3a26b765f4f6903fad47c91a8ec96f76a5772deeaf5982be234f1d1f83`. No stale attempt-1 `step_002` artifact or proof-history artifact is used.
- Sketch-step coverage: All four required sketch rows are present once in dependency order and are used by the theorem: canonical-oracle validity and path (`step_001`), total response-tree map/law/count (`step_002`), replay/one-hot/tie bridge (`step_003`), and expectation/risk/dimension closure (`step_004`). The accepted global diagnostic's blocks B1--B6 map to these same public results, and its previously repaired pre-instance/per-instance ordering and replay recurrence remain visible in the appendix.
- Assembly discipline: Every non-atomic producer unit is represented by a named public lemma or proposition with a substantive proof. The step-local invariant is correctly translated as Lemma~`lem:step-003-prefix-synchronization`; no public claim, hypothesis, or invariant environment is used. The four concluding `Completion of ...` proof blocks translate the four target-step assemblies. Final theorem assembly only reconciles notation and composes accepted public results; it introduces no independent lemma, new inequality, strengthened assumption, mode upgrade, or altered step claim.
- LaTeX bundle structural synchronization: The five required content files, `main.tex`, `arxiv.sty`, `ims.bst`, and `reference.bib` exist. `main.tex` inputs all five content files and runs the bibliography. The required section/appendix shape is present, including one mathematically named subsection per sketch step and a final `Proof of the Main Theorem` subsection. The frozen `assembly_report.md` and appendix hashes match `ba8f04e2b02e9c08af7e7da00cf45e63cdff078df7e10099238881b369b21e53` and `ebf1d03ff26770e834544562b18615ad34d08e029707474d898aedc723859e7e`. An independent build produced a PDF and BibTeX output; the sole nonblocking box warning is recorded above.
- Main theorem self-containedness: After the setup and preliminaries, every symbol in Theorem~`thm:main` is defined: the losses and dimension functional, SQ/oracle/tape conventions, \(K,G,N,\rho\), response-tree predictors and maps, \(\mathsf P_A\), canonical oracle/path, and the linear-score abbreviation. The theorem refers to no appendix-local result, label, helper constant, proof-step ID, or local unit ID. Its two numbered assumptions contain primitive conditions only; oracle validity, path existence, map-law measurability/independence, replay synchronization, and the tie-free bridge are derived in named appendix results.
- Theorem-style statement shape: Every public lemma and proposition states numbered assumptions and/or named prior theorem-style inputs before its local conditions and conclusion. Visible names are mathematical rather than workflow labels. The appendix contains no forbidden audit scaffolding, branch paths, accepted-step narration, copied sketch fields, placeholders, malformed command fragments, or mechanical translation debris.
- Explicit-rate and bridge structure: The public and appendix statements expose exactly \(m,\tau,\varepsilon,K,N\), no hidden constants, expectation only over the learner tape/map pushforward, fixed finite horizon, and the tie/binary loss modes. The technical and public bounds are the same exact bounds, so no Rate Specialization Bridge is needed. No term is dropped or absorbed. The exact baselines \(m=0\Rightarrow N=1\), \(\varepsilon=0\Rightarrow\) zero expected tie loss without rounding remainder, \(\tau\ge1\Rightarrow N=2^m\), and \(L_{\mathrm{tie}}=L_{\mathrm{bin}}\) on the selected binary score are preserved.
- Derivation-over-notation: Public preliminaries contain only the reusable linear-score abbreviation, response-string coordinate convention, basis notation, and empty-product convention needed to read the theorem. Tape-space, sigma-algebra, tie-pattern, section-measure, and selected-basis objects remain appendix- or proof-local and are accompanied by their defining derivations. No helper dictionary replaces a boundedness, measurability, rate, or quantifier argument.
- Appendix local-unit coverage: Exhaustive source-order comparison passes. For `step_001`, all three invoked local-result interfaces, Local Derivation units 001--003 (grid inversion/spacing/covering and boundary cases; adaptive querywise validity and nonaccumulation; deterministic recursion, unread padding, and \(m=0\)), and the Target-Step Assembly appear in appendix lines 7--151 in the same mathematical order. For `step_002` attempt 2, the Dynkin pi-lambda application and accepted-dependency discharges, units 001--004 (total replay/suffix induction; distinct-grid and finite-product induction; arbitrary-domain feature-map construction; complete-tape measurable typing, section/Dynkin argument, simple-function parameter integration, final sigma algebra, pushforward identity, factorization criterion, coordinate/fixed-weight measurability, finite exact tie-pattern reduction, canonical-path measurability, selected-basis finite sum, tape-wise infimum comparison, duplicate fibers, and boundary cases), and the Target-Step Assembly all appear in appendix lines 156--544 in source order. For `step_003`, all accepted-result invocations, units 001--004 (finite prefix induction, terminal replay, one-hot binary selection, event/loss equality and tape-side measurability), and the Target-Step Assembly appear in lines 548--737, with the invariant converted to an allowed lemma. For `step_004`, all accepted-result invocations, units 001--003 (pointwise infimum/pushforward, valid-oracle risk closure, and simultaneous dimension certificate), their complete displayed chains and boundary reductions, and the Target-Step Assembly appear in lines 741--945. No required definition, inequality chain, induction transition, cited-result check, assumption discharge, boundary case, or substantive target-assembly implication is replaced by a summary or citation-only shortcut.
- Internal label/ref structure: Static parsing found 79 unique labels, 43 distinct internal reference targets, one external citation key, no duplicate labels, no unresolved references, no unresolved citations, and no bare malformed `ref`/`cite` commands. The Kallenberg citation backs the Dynkin pi-lambda invocation and has a matching BibTeX entry. Mathematical dependencies use `Assumption`, `Lemma`, `Proposition`, or `Theorem` references rather than subsection, step, or unit identifiers.

Verdict alignment: `PASS` requires `Blocking Issues = None`, `Suggested Repair Depth = None`, and `Smallest Candidate Repair Target = None`. `BLOCKING` requires at least one numbered blocking issue, non-`None` repair depth, and an aligned repair target.
