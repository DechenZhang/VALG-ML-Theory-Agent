# Adversarial Proof Review

## Reviewer Verdict

BLOCKING

## Blocking Issues

1. The exact-learner-to-VC bottleneck is broken in the public appendix. In `latex_template/5_appendix.tex:168`, the update map is defined as
   \[
   U(\theta,x,y):=\theta-eta\nabla^{\mathrm{src}}_\theta\ell(yf_\theta(x)),
   \]
   whereas `latex_template/1_intro.tex:51` and the accepted `proof_steps/step_002/proof.md:88` use the source update with the declared step size, `\theta-\eta\nabla^{\mathrm{src}}_\theta\ell(yf_\theta(x))`. Read literally, bare `eta` is not the symbol `\eta`; it is an undeclared product of ordinary math letters and does not instantiate the source recursion for any arbitrary allowed `\eta>0`. Consequently, the identity at appendix line 178 does not show that the exact SGD trajectory is generated by the displayed map `U`, so the sampled-label measurability lemma does not currently apply to the learner whose expected risk is assumed. This is a verified assembly transcription break: the accepted step proof already contains the correct update, and changing only `eta` to `\eta` in the appendix restores its interface without revising a step claim.

## Nonblocking Concerns

None

## Suggested Repair Depth

assembly

## Smallest Candidate Repair Target

/proof-assembly

## Adversarial Audit

- Weakest theorem-level claim: The most vulnerable implication is universal expected success of the exact fixed-horizon learner implying `VC(H) < 2T`; every later count and amplification bound consumes that certificate. The accepted step proof establishes it, but the assembled appendix currently changes the defining update at precisely this interface.
- Standalone theorem attack: The theorem remains explicitly conditional on one target-independent confident-map law and does not claim that SGD supplies that law or a polynomial bound on `d`. Its quantifiers permit the tuple to depend on the fixed class and law while requiring the tuple, but not the separator, to be common across targets. No unconditional-source claim, hidden target-dependent law, or stronger representation mode was found.
- Weakest step-level claim: `step_002`, `unit_001` (sampled-label measurability) is the critical local mechanism. Conditioning on the test index, ordered sampled indices, sampled labels, and initialization does leave an unseen random label fair; repeated samples expose no additional coordinate, and the fixed tie label makes a zero-score prediction a fixed binary value. These attacks are neutralized in the accepted proof. The appendix's malformed update prevents that accepted reasoning from being faithfully assembled.
- Candidate counterexamples or stress cases: The literal bare-`eta` update is a verified break because it contains no occurrence of the arbitrary theorem parameter `\eta`. The cases `H = empty`, `d = 0`, `L = 1`, `T = 1`, `VC(H) = 0`, `|H| = r = 1`, repeated training points, success probability exactly `1/2`, and zero representation or aggregate scores were also tested. They are all closed by definitions, the strict inequality `epsilon < 1/4`, or exact displayed identities; none produces an additional counterexample.
- Explicit-rate stress cases: For `VC(H) = 0`, nonemptiness gives `|H| = r = 1`. For `VC(H) >= 1`, the appendix exposes the finite-domain growth bound, the `v = N` endpoint, `log_2 e < 3/2`, the ceiling charge `2 <= 2TS`, and `n <= S`, yielding `r <= 7TS` even at `T = n = S = 1`. The final multiplication by `d` and the separately conditional substitution `d <= p(S,T)` have no hidden probability, horizon, norm, `n`, or `eta` loss.
- Appendix self-contained attack surface: The bundle otherwise includes all 17 accepted local units, their substantive derivations, the five step conclusions, and a final theorem proof, with unique resolved internal labels and no external theorem dependency. Successful compilation does not neutralize the blocker: TeX accepts bare `eta` as ordinary math letters, so the semantic source-recursion mismatch survives compilation.
- Scope or mode upgrade attacks: The random-target average is reduced to one fixed target before universal success is invoked. Per-target probability under one common law is converted to a common deterministic tuple through an `r`-fold product and a finite union bound with failure at most `1/2`; no confidence claim remains. Direct-sum score equality is pointwise and preserves ties. No unsupported expectation-to-high-probability, local-to-uniform, finite-to-asymptotic, or conditional-to-unconditional upgrade was found.
- Remaining risk: After the appendix restores the exact `\eta` update and is freshly compiled, no further adversarial obstruction was found in the accepted proof chain or theorem scope. The repaired assembly should still undergo the complete specialized-review and aggregate-review cycle.
