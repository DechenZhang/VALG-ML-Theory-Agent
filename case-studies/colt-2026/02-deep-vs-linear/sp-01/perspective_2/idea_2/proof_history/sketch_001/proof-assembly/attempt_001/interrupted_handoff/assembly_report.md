# Assembly Report

## Status

COMPLETE

## Attempted Claim

Under `assump:source-regime`, `assump:universal-sgd-success`, and the separately explicit conditional premise `assump:tie-resolved-confident-map`,
\[
\operatorname{dc}(\mathcal H)\le 7TSd.
\]
Separately, if an external result supplies an explicit polynomial bound \(d\le p(S,T)\) with no hidden \(n\)- or \(\eta\)-dependence, then \(\operatorname{dc}(\mathcal H)\le7TSp(S,T)\); the bundle does not assert that this premise follows from SGD.

## Blockers

None

## Notes

All five sketch steps have matching current `ACCEPTED` reviews for sketch attempt 1 and unit attempt 1. The appendix translates every local derivation and target-step composition, including the empty, \(d=0\), \(T=1\), VC-dimension-zero, singleton-class, ceiling, common-law, and zero-score branches. No external citation is used.
