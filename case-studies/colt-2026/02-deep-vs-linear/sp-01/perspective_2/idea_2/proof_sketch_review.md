# Proof Sketch Review

## Reviewed Sketch Identity

- Branch: `perspective_2/idea_2`
- Sketch attempt: 1
- Reviewed artifact: `perspective_2/idea_2/proof_sketch.md`
- Goal mode: exact-goal mode for the explicitly conditional theorem in `setting.md`
- Roadmap: boundary reduction and (S\ge n); a random-label unseen-point proof of (\operatorname{VC}(\mathcal H)<2T); Sauer--Shelah counting and the explicit budget (r\le7TS); iid common-law amplification; and exact block concatenation, followed by the stated polynomial specialization.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch targets exactly the conditional claim (\operatorname{dc}(\mathcal H)\le7TSd), preserves the fixed source SGD and tie conventions, and explicitly leaves open both the unconditional polynomial implication and the unconditional (O(TS)) target. This is consistent with the branch's `conditional` progress type and stated materiality.
- Dependency audit: The graph `step_001` -> `step_002` -> `step_003` -> `step_004` -> `step_005`, with the additional listed earlier dependencies, is acyclic. Every consumed derived output has an earlier producer, and final assembly is correctly split between the null branches and the nondegenerate branch.
- High-risk coverage: The sketch localizes the random-target quantifier argument, the exact (T=1) threshold, the (v=0) Sauer--Shelah branch, elimination of (n) through (S\ge n), common-law iid amplification, exact tie-preserving concatenation, and the empty-class and zero-dimensional cases. No theorem-critical obligation is hidden in a generic future bridge.
- Explicit-rate coverage: The exposed variables, fixed-horizon and probability modes, exact representation metric, universal constant (7), prohibited hidden dependence, ceiling loss, and optional (d\le p(S,T)) specialization are all explicit. The inequalities in `step_003` account for every additive and logarithmic term.
- Assumption and citation plausibility: All theorem assumptions use stable `assump:*` ids. The confident map is honestly a primitive condition of this conditional theorem, while VC, cardinality, repetition budget, simultaneous coverage, and the common map are derived. The parent papers provide lineage only; the theorem-critical bridge uses self-contained current-notation arguments, so no unresolved cited theorem or wrapper interface remains.

## Early Obstruction Audit

- Limiting-case stress: The sketch explicitly survives (\mathcal H=\varnothing), (d=0), (T=1), (v=0), (n=1), (S=n), (\lvert\mathcal H\rvert=1), (r=1), repeated training inputs, randomized initialization, and zero aggregate or block scores. In particular, at (T=1) the unseen probability is exactly (1/2), so the averaged error lower bound is exactly (1/4>\varepsilon); and at (d=0) the unique score is zero, so the fixed tie rule forces every admissible target to be the constant (\tau) classifier.
- Theorem-critical bridge support: For a shattered set (Z) of size (2T), the sketch fixes the uniform distribution on (Z), samples a uniform labeling (b), chooses one realizing (h_b\in\mathcal H), and averages the exact learner's risk. Conditional on the test point being unseen, the full ordered sample inputs, their observed labels, initialization, and fixed-gradient trajectory reveal no information about (b(X)). Thus the exact returned binary predictor, including ties, mismatches (b(X)) with conditional probability (1/2). Finite averaging then selects one fixed target (h_b), so the universal target quantifier is not replaced by an average-target premise.
- Exported-interface feasibility: `step_001` exports exact null-case conclusions and (S\ge n); `step_002` exports (v<2T) in the same risk and probability interface as the premise; `step_003` exports the integer (r\le7TS); `step_004` exports the exact full-domain covering tuple; and `step_005` exports the precise map consumed by (\operatorname{dc}). Each output has raw controls, an exact or explicitly bounded transfer relation, and listed consumers.
- Theorem-critical mechanism witness gate: All five blocks have nonvacuous sources already present under the theorem contract. The sources are respectively definitions and positive widths, unseen-label independence, finite-class combinatorics, the explicitly conditional common law plus iid sampling, and exact direct-sum algebra. The opposing defects and boundary regimes are identified, and none of the witnesses is merely a later-step label.
- Entry-state trace stress: The only iterative learner is used through its information interface, not through an unproved convergence claim. At the (T=1) entry, after the sole update the state still depends only on the sampled target bit, so on the unseen event the test bit is fair conditional on the entire learner state. For amplification, at (r=1) the first draw covers a singleton class with probability at least (1/2). At (d=0), no update or draw is needed because the exact theorem conclusion already holds.
- Obligation locality classification: Exact null handling and (S\ge n) (`step_001`), the universal-SGD-to-VC bridge (`step_002`), the Sauer--Shelah and constant calculation (`step_003`), common-law iid amplification (`step_004`), exact concatenation (`step_005`), and the (d\le p(S,T)) scalar specialization (`step_005`) are all `step-local`. Their mechanism sources and interfaces are fixed in the sketch; later proof work only supplies local derivations under unchanged assumptions, scope, mode, metric, dependence, and conclusion.
- Noncircular closure gate: The sketch assumes none of the derived VC, cardinality, coverage, or common-map conclusions. Each is produced before its first consumer along the stated dependency graph. There is no all-time invariant, basin, stability, boundedness, or recurrence premise.
- Mechanism-source and boundary stress: If universal success did not cover the fixed uniform witness distribution, or if the learner accessed unsampled labels, the VC bridge would fail; both are excluded by the binding premise and exact update. If the feature law depended on the target, simultaneous amplification would have no common producer; the binding quantifier order explicitly excludes this. If a target had no covered block, concatenation would fail; `step_004` produces coverage before use.
- Generated-output flow: Every theorem-facing derived output appears in `## Generated Output Flow` with a legal producer, consumers, final use, dependency path, and provenance class. In particular, the deterministic tuple is produced by `step_004` from one primitive law and the budget from `step_003`, then consumed only by `step_005`.
- Source-to-claim adequacy: The VC claim is supported by a lower bound of the correct type, (\frac12(1-1/(2T))^T\), rather than by a generic capacity heuristic. Sauer--Shelah is to be proved in current notation on the exact finite domain (N=2^n), including (v=0). The amplification event is exactly the event in `assump:tie-resolved-confident-map`, and direct-sum score equality is exactly the sign interface in the definition of (\operatorname{dc}). No source convention, object, metric, or algorithm is substituted.
- Residual-to-target adequacy: The random-label average transfers to one fixed (h_b) with no risk or probability-mode residual. The count-to-budget transfer has only the explicit ceiling residual (2\le2TS). The sampled tuple uses the identical exact representation event. The concatenated score minus the selected block score is identically zero pointwise, including at ties. The polynomial specialization introduces only the supplied nonnegative scalar slack (p(S,T)-d), not a representation residual.
- Baseline invariance audit: The inherited exact conclusions are preserved for (\mathcal H=\varnothing), (d=0), the source predictor and expectation in the VC argument, deterministic existence after amplification, exact tie-resolved representation after concatenation, and the polynomial specialization. No baseline is replaced by an approximate, stopped, finite-confidence, or positive-dimensional surrogate.
- Scope-accumulation compatibility: The SGD information argument has exactly (T) iid draws and the finite-horizon avoidance factor ((1-1/(2T))^T); it contains no additive trajectory defect. The amplification has exactly (r) iid draws and multiplicative failure at most (2^{-r}), followed by the finite union cost (\lvert\mathcal H\rvert). Direct-sum cross-block contributions are exactly zero. No persistent, adversarial-sign, or uncontrolled accumulated term remains.
- Scope and dependence consistency: Architecture, (\eta), and (T) are fixed before the witness distribution and targets are averaged, as required. The conclusion is deterministic, finite-horizon, exact-sign, and uniform over the class. The final dimension contains only (S,T,d), with neither (n), (\eta), (\varepsilon), nor a failure parameter hidden in the constant.
- Generated-condition provenance: `assump:tie-resolved-confident-map` is a permitted theorem-wide conditional hypothesis because the formalized goal is explicitly conditional. All other target-facing certificates are derived by named earlier steps; no trajectory event, stability property, recurrence, boundedness fact, or local-validity fact is assumed.
- Citation and tool applicability: Feldman--Kamath--Srebro (PMLR 336, 2026) and Kamath--Montasser--Srebro (COLT 2020; arXiv:2003.04180) are used only for source and perspective lineage. The proof does not import a theorem-critical statement from either. The no-free-lunch calculation, Sauer--Shelah recursion and binomial estimate, union bound, and direct-sum identity are fixed as self-contained current-notation derivations with their exact inputs and output interfaces.
- Same-setting repair plausibility: No material obstruction was found. Any local algebraic or conditioning detail can be completed within the listed step under the unchanged setting. Removing the confident-map premise or deriving polynomial (d) from SGD would instead change the branch theorem contract, but neither is claimed by this sketch.
- Target-preserving bridge-repair gate: No missing theorem-critical bridge requires repair. The remaining source-level gap is explicitly outside the conditional target and is not mislabeled as step-local work.
- High-risk obligation classes: Structural counting, exact boundary behavior, finite iterative information exposure, confidence-to-determinism mode conversion, generated-certificate flow, explicit-rate simplification, and tie preservation were all tested. Approximation, perturbation, stochastic-error accumulation, all-time recurrence, and hidden invariant classes do not occur in the claimed route.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Empty-class branch | Exact baseline theorem closure | Definition (\operatorname{dc}(\varnothing)=0) | Binding setting definition | Directly supplies the claimed dimension in this branch | Same (\operatorname{dc}), domain, and fixed tie convention as the goal | (0=\operatorname{dc}(\varnothing)\le7TSd) | Raw control is emptiness; exported interface is the exact final inequality | Produced and consumed dimension are identical; residual zero | No generated process or accumulation | Setting definition -> `step_001` -> final assembly | `step_001` produces the closed branch; final assembly consumes it | Empty class makes the conclusion already true; no mechanism activation is needed | `step-local` | None |
| Zero-dimensional branch | Exact baseline and tie-boundary closure | Unique map and separator in (\mathbb R^0), fixed (\tau), and `assump:tie-resolved-confident-map` | Primitive conditional assumption plus binding definitions | A deterministic event of probability at least (1/2) must hold, forcing every (h(x)=\tau) | The producer and target both use score zero and the same (\operatorname{sgn}_{\tau}) | (d=0\Rightarrow h\equiv\tau\Rightarrow\operatorname{dc}(\mathcal H)=0=7TSd) | Raw controls are (d=0), exact success, and the tie rule; exported interface is one common zero map | Pointwise score residual is exactly zero | No draw-dependent defect; the only event is deterministic | Primitive condition -> `step_001` -> final assembly | `step_001` produces exact closure; downstream nondegenerate steps are bypassed | At the null-dimensional entry the theorem is already true, including zero scores | `step-local` | None |
| Architecture bound (S\ge n) | Structural rate input | First-layer parameter count and positive width | `assump:source-regime` | Supplies the required lower bound on the parameter count | Exact branch architecture, including (L=1), is used | (S\ge n_1n\ge n) since (n_1\ge1) | Raw widths and definition of (S) export (n\le S) to the count bridge | Exact inequality; no surrogate object or residual | Finite sum with no accumulated defect | Primitive architecture -> `step_001` -> `step_003` | `step_001` produces; `step_003` consumes | Valid at (n=1), (S=n), and (L=1) | `step-local` | None |
| (\operatorname{VC}(\mathcal H)<2T) | Structural capacity bound and learner-to-count bridge | Random labeling on a shattered (2T)-point set and unseen-test-point independence | Direct current-notation derivation from `assump:source-regime` and `assump:universal-sgd-success` | Gives an error lower bound of the same claim type and metric needed to contradict universal success | Exact Gaussian initialization, ordered one-sample updates, fixed kink rule, latter-half classifier, tie label, distribution, and expected risk are unchanged | (\Pr(E)=(1-1/(2T))^T\ge1/2), hence averaged error (\ge\frac12\Pr(E)\ge1/4>\varepsilon) | Raw controls are universal expected error, label-only sample access, and independent initialization; exported interface is (v<2T) | Finite target averaging selects one exact (h_b\in\mathcal H); distribution, metric, horizon, and probability residuals are zero | Repeated inputs are exactly included in the multiplicative avoidance probability; no additive trajectory error | Primitive learner premise -> `step_002` -> `step_003` | `step_002` produces (v<2T); `step_003` consumes it | At (T=1), error lower bound is exactly (1/4); ties still give a fixed binary prediction independent of the unseen fair bit | `step-local` | None |
| Sauer--Shelah count and (r\le7TS) | Quantitative specialization and amplification budget | Current-notation Sauer--Shelah recursion, binomial-sum estimate, (v<2T), and (S\ge n) | Direct combinatorial derivation from outputs of `step_001` and `step_002` | Bounds the exact finite class cardinality and the exact integer repetition count | Objects are precisely restrictions of (\mathcal H) to its (N=2^n) point domain; no representation convention enters | For (v\ge1), (\log_2M\le2T(n+\log_2e)) and (r\le\log_2M+2\le5Tn+2\le7TS); for (v=0), (M=r=1) | Raw controls are (N=2^n), (v<2T), (n\le S), (S,T\ge1); exported interface is integer (r\le7TS) | Only residual is the ceiling allowance (2\le2TS), explicitly dominated at target scale | Finite binomial sum and ceiling; no repeated defect | `step_001` + `step_002` -> `step_003` -> `step_004`,`step_005` | `step_003` produces count and budget; `step_004` and `step_005` consume them | (v=0), (v=N), (M=1), (n=1), (T=1), and (S=n) are covered | `step-local` | None |
| Deterministic tuple covering all targets | Probability-mode conversion and generated coverage certificate | One common law from `assump:tie-resolved-confident-map`, iid product sampling, and a finite union bound | Primitive explicitly conditional source plus derived (r) from `step_003` | Per-target exact failure multiplies across independent blocks and the union covers the same finite target class | The law precedes (\mathcal D,h); every block uses the same full domain, dimension, and fixed tie convention | For each (h), failure (\le2^{-r}); total failure (\le M2^{-r}\le1/2<1) | Raw controls are common (\mathcal P), success (\ge1/2), iid blocks, and exact (r); exported interface is one fixed tuple with a successful block for every (h) | Produced and consumed success events are identical; no error, score, margin, or distribution residual | Failure is multiplicative over exactly (r) draws and paid by the finite union; no persistent additive defect | Primitive common law + `step_003` -> `step_004` -> `step_005` | `step_004` produces the tuple; `step_005` consumes it | At (M=1,r=1), success probability remains at least (1/2); exact zero scores remain valid under (\tau) | `step-local` | None |
| Common (rd)-dimensional map and final bound | Exact theorem closure | Direct-sum feature map and zero-padded target separators | Direct linear-algebra derivation from outputs of `step_003` and `step_004` | Supplies exactly one common map and target-specific linear separators required by (\operatorname{dc}) | Same coordinates, inner product, target domain, and (\operatorname{sgn}_{\tau}); no transform or surrogate metric | (\langle u_h,\Phi(x)\rangle=\langle w_{i(h),h},\phi_{i(h)}(x)\rangle), so (\operatorname{dc}(\mathcal H)\le rd\le7TSd) | Raw controls are a covered block per target and (r\le7TS); exported interface is the exact common feature map | Score difference is identically zero on every input, including ties | All inactive blocks contribute exactly zero; no error accumulates with (r) | `step_003` + `step_004` -> `step_005` -> final assembly | `step_005` produces the map and bound; final assembly consumes them | At (r=1) the map is the successful block itself; zero block scores retain the fixed tie label | `step-local` | None |
| Specialization under explicit (d\le p(S,T)) | Public polynomial rate specialization | Main bound plus the separately established scalar inequality | Derived `step_005` output plus an explicitly supplied bound with no hidden (n,\eta) dependence | Monotone scalar substitution has the exact dependence and theorem mode required | Same deterministic dimension target; no algorithm, norm, probability, or horizon convention changes | (\operatorname{dc}(\mathcal H)\le7TSd\le7TSp(S,T)) | Raw controls are the accepted main bound and explicit (d\le p(S,T)); exported interface is the stated polynomial endpoint | Only scalar slack (p(S,T)-d\ge0); representation residual is zero | No repeated scope, dropped term, or probability conversion | Separate explicit (d)-bound + `step_005` -> final specialization | `step_005` produces the conditional specialization; final theorem prose consumes it only when the inequality is available | If (d=p(S,T)=0), `step_001` already gives exact closure; otherwise monotonicity applies | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is structurally complete and goal-faithful, and every theorem-critical obligation has a source-adequate, noncircular, boundary-tested interface under the unchanged conditional theorem contract. The hardest point, `step_002`, is genuinely local: the sketch already fixes the random-target experiment, conditioning sigma-field in substance, exact unseen probability, average-to-fixed-target conversion, (T=1) boundary, and exact source learner interface. The remaining steps are likewise local finite combinatorics, probability, and linear algebra with explicit constants and no unsupported mode upgrade. The branch is therefore ready for global-proof diagnostics and subsequent step-level proof work.
