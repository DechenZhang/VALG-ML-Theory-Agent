# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Branch: `perspective_3/idea_2`
- Reviewed artifact: `perspective_3/idea_2/proof_sketch.md`
- Goal mode: exact-goal mode
- Roadmap: exact depth-two ReLU antisymmetrization, tie-aware antipodal error transfer, distribution-wise approximate homogeneous separation, a self-contained finite convex infeasibility witness, strict high-accuracy exactification, and identity-map dimension closure.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch targets the exact theorem in `setting.md`. It proves exact identity-map representation, then the probability-one point-mass confident representation and the chain \(\operatorname{dc}^{1/2}(\mathcal H)\le \operatorname{dc}(\mathcal H)\le n\le S\le TS\). It preserves the material-partial label and expressly leaves the unrestricted-depth, non-odd, unrestricted-accuracy source target open.
- Dependency audit: The seven-step graph is acyclic. `step_001` and `step_004` are independent primitive branches; every other dependency points backward, and the two branches first meet legally in `step_006`. No generated trajectory score, certificate distribution, separator, or common feature map is consumed before its producer.
- High-risk coverage: The sketch localizes every theorem-critical issue: exact latter-half antisymmetrization, both tie conventions and all zero-score cases, symmetrized-distribution quantifiers, expectation-to-existence, strict-system equivalence, the current-notation convex alternative, support at most \(n+1\), uniform witness counting, the strict \(2\varepsilon\) gap, the empty class, and the final point-mass quantifier order.
- Explicit-rate coverage: The exposed variables, fixed quantities, zero hidden-constant dependence, expectation and probability-one modes, fixed finite horizon, exact loss/sign metric, factor \(2\), support threshold \(n+1\), and admissibility condition \(2\varepsilon<1/(n+1)\) are all declared. `step_006` is an explicit quantitative-specialization bridge and does not suppress equality or any residual term.
- Assumption and citation plausibility: Only the three stable setting assumptions are theorem-facing. All trajectory, error-transfer, convex-certificate, and separator facts are derived. The proof consumes no theorem-critical external wrapper: the ReLU and tie arguments are direct in the branch convention, while the Gordan/Caratheodory interface is supplied by a self-contained minimum-norm and affine-dependence derivation in the same Euclidean coordinates.

## Early Obstruction Audit

- Limiting-case stress: For \(T=1\), the latter-half index set is exactly the one stated in `setting.md`, and the per-iterate algebra still sums correctly. At ReLU kinks, zero weights, zero output weights, or \(v_\omega=0\), `step_001` remains an exact function identity. At \(\varepsilon=0\), `step_003` gives a zero-error realization on each invoked distribution and `step_006` exactifies immediately. At \(2\varepsilon=1/(n+1)\), the contradiction can fail, and the sketch correctly relies on the primitive strict inequality. For \(n=1\), the support bound and parameter chain remain valid. For \(\mathcal H=\varnothing\), the target quantifiers are vacuous and final assembly is instructed to take that branch before fixing a target.
- Theorem-critical bridge support: Every nontrivial bridge has a named producer and exact interface. The score identity is produced in `step_001`; the pairwise error inequality in `step_002`; the distribution-wise \(2\varepsilon\)-accurate vector in `step_003`; the strict feasibility system in `step_004`; the finite witness distribution and lower bound in `step_005`; strict feasibility in `step_006`; and the fixed feature law in `step_007`.
- Exported-interface feasibility: The raw controls and consumers are complete. In particular, \(A_\omega\) is not a surrogate for the linear score but equals \(x\mapsto\langle v_\omega,x\rangle\) pointwise; the two loss terms integrate exactly to twice the loss under \(\mathcal D^{\mathrm{sym}}\); the upper and lower bounds in `step_006` use the same \((h,\mathcal D_h^\star)\), score class, and tie-resolved metric; and the final feature object is exactly \(\varphi_{\mathrm{id}}\), not a trajectory-dependent transform.
- Theorem-critical mechanism witness gate: Passed for all seven steps. `step_001` uses \(\sigma(r)-\sigma(-r)=r\) with zero residual; `step_002` uses strict opposite-label sign forcing; `step_003` uses the universal premise on a legal symmetrized distribution; `step_004` uses representatives whose label is exactly \(-s_0\); `step_005` uses Euclidean closest-point separation and finite affine pruning; `step_006` uses the positive gap \(1/(n+1)-2\varepsilon\); and `step_007` uses one deterministic identity map plus the target-wise separators. None is only a future-step label.
- Entry-state trace stress: Although the parameters arise from a recursive SGD process, the proof needs no dynamical invariant. At initialization and after every update, including an update landing at a ReLU kink, the pointwise depth-two identity already holds for the produced parameters. Thus the source used by `step_001` is active before any generated score is consumed. The later steps are finite algebraic or existential arguments. At \(w=0\), the witness mechanism remains active because every witness label is \(-s_0\), so every tie is an error. For the point-mass law, the exact representation event already holds and no feature-map sampling transition is needed.
- Obligation locality classification: `step_001` exact antisymmetrization, `step_002` tie-aware transfer, `step_003` mode transfer, `step_004` strict-system equivalence, both parts of `step_005`, `step_006` exactification, and `step_007` feature-law/dimension closure are all `step-local`. Their sources and interfaces are already fixed by primitive assumptions or earlier outputs; local proofs only instantiate the displayed derivations without changing assumptions, scope, mode, metric, dependence, or conclusion.
- Noncircular closure gate: Passed. No stability, boundedness, support, basin, recurrence, or local-validity premise is assumed. The approximate separator is produced without exact representability; the convex certificate is produced conditional on infeasibility; and these independently produced interfaces are compared only afterward. The final common-map conclusion consumes separators already produced for all targets.
- Mechanism-source and boundary stress: For `step_002`, exhaustive simultaneous-correctness cases are: if \(s_0=+1\), then for target \(y=+1\) one has \(G(x)\ge0>G(-x)\), while for \(y=-1\) one has \(G(x)<0\le G(-x)\); if \(s_0=-1\), then for \(y=+1\) one has \(G(x)>0\ge G(-x)\), while for \(y=-1\) one has \(G(x)\le0<G(-x)\). Hence \(y(G(x)-G(-x))>0\) in all four cases, including every allowed zero source score. Its contrapositive proves the indicator inequality even when \(A(x)=0\). For `step_005`, \(\langle w,h(q)q\rangle\le0\) is an error whether it is negative or equal to zero because \(h(q)=-s_0\).
- Generated-output flow: Passed. Every derived object has a legal producer, all consumers and final uses are listed, and each consumer has a dependency path. In particular, the trajectory-dependent vector \(v_\omega\) is consumed only to establish the distribution-wise approximate interface; it is never substituted for the final feature map.
- Source-to-claim adequacy: The depth-two algebra supplies equality rather than upper control; oddness plus the fixed tie convention supplies strict signed content rather than generic regularity; the universal premise supplies exactly the needed expectation bound for every symmetrized distribution; the direct convex-hull argument supplies both a nonnegative zero certificate and its support bound; and the strict accuracy assumption supplies a genuinely positive numerical gap. The exact branch conventions for network, indexing, Euclidean inner product, sign, loss, distribution, and feature coordinates match every consumed interface.
- Residual-to-target adequacy: All object bridges are target-side exact except for the explicit factor \(2\) in error transfer. Per-iterate and aggregate antisymmetrization residuals are zero. The change-of-variables relation has no distribution residual. The signed witness inner product is exactly the target-score product. The contradiction compares identical target objects and metrics. The final produced and consumed feature maps are the same identity map. There are no persistent, empirical, event-level, approximation, or wrong-scale residuals.
- Baseline invariance audit: The inherited source protocol, tie convention, expectation mode, fixed horizon, and exact returned aggregate are unchanged. The exact/noiseless specialization \(\varepsilon=0\) preserves the same exact-representation conclusion, rather than merely making an error remainder vanish. The exact target is not replaced by the approximate \(2\varepsilon\) endpoint. The empty-class baseline is vacuous, and the point-mass law still may be fixed independently. No initialized or transformed surrogate object replaces the identity target.
- Scope-accumulation compatibility: No all-time, limiting, or iterative closure is claimed. The only time accumulation is the finite latter-half sum, whose one-term residual is exactly zero and hence whose accumulated residual is zero. The only convex accumulation has nonnegative coefficients summing to one and is reduced to at most \(n+1\) terms. Loss integration preserves a pointwise nonnegative inequality with the displayed factor \(2\). No adversarial-sign persistent defect remains.
- Scope and dependence consistency: The proof fixes \((n,m,T,\eta)\) and the architecture before universally choosing \((\mathcal D,h)\), invokes the learner separately on \((\mathcal D^{\mathrm{sym}},h)\), and extracts a vector that may depend on \((\mathcal D,h)\), which is allowed at the approximate intermediate stage. Exactification then gives \(w_h\) depending only on \(h\), while the identity feature map and its law depend on neither \(\mathcal D\) nor \(h\). No simultaneous trajectory realization across distributions or targets is claimed.
- Generated-condition provenance: Every generated fact is labeled derived and produced before use. The sketch introduces no good event, gate-stability condition, trajectory bound, recurrence applicability condition, or hidden invariant as a theorem assumption.
- Citation and tool applicability: The cited literature in `technical_survey.md` is contextual, not a theorem-critical proof authority. The sketch commits to direct current-notation proofs of the ReLU identity, finite closest-point alternative, and affine support pruning. The objects, hypotheses, conclusion shapes, tie convention, and Euclidean coordinates for those direct proofs are already fixed, so no unresolved source label, source version, wrapper output, or convention translation is deferred to a proof step.
- Same-setting repair plausibility: No material repair is presently required. Any failure of the depth-two identity, antipodal label transfer, or strict numerical gap after removing the corresponding primitive restriction would require a theorem-contract change, but those regimes are explicitly outside this branch rather than unresolved obligations inside it.
- Target-preserving bridge-repair gate: Passed without repair. The roadmap already contains each bridge needed to preserve the exact formalized goal, including the exactification bridge that prevents replacement by the weaker approximate endpoint.
- High-risk obligation classes: Present and passed are structural equality, signed/tie-aware lower claims, a distribution and expectation mode conversion, finite-support convex geometry, a quantitative strict-gap specialization, an exact baseline specialization, and common-feature quantifier closure. Approximation, perturbation, stochastic concentration, recursive invariant maintenance, basin closure, and all-time upgrades are not used.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `step_001`: \(A_\omega=\langle v_\omega,\cdot\rangle\) | Exact structural linearization; supplies every later homogeneous candidate | Direct identity \(\sigma(r)-\sigma(-r)=r\) on each realized bias-free depth-two iterate | Primitive architecture plus direct current-notation derivation | Exact equality has the same score class required downstream | Same bias-free network, ReLU, parameters, latter-half indices, and Euclidean pairing as `setting.md`; kink-gradient choice does not affect the function identity | \(f_t(x)-f_t(-x)=\langle (W^{(t)})^\top a^{(t)},x\rangle\), summed over exactly \(t=\lceil T/2\rceil,\ldots,T\) | Raw realized \((a^{(t)},W^{(t)})\) export the exact score and vector consumed by `step_002` and `step_003` | Pointwise residual is zero for every iterate and after summation | Finite accumulation of identically zero residuals; no forcing term | Primitive architecture produces the identity before either consumer; no linear representability is assumed | Producer `step_001`; consumers `step_002`, `step_003`; final use is the approximate identity-coordinate interface | Holds at initialization, after every update, at kinks, with zero parameters, \(v_\omega=0\), and \(T=1\) | `step-local` | None |
| `step_002`: tie-aware pairwise indicator and loss transfer | Structural signed comparison; moves source error to antisymmetric error | Oddness and exhaustive direct tie-sign analysis | Primitive `assump:antipodal-oddness`, fixed source tie rule, and `step_001` score | Simultaneous correctness forces the exact strict sign needed for correctness of \(A\) | Uses the identical \(s_0\), strict source error, labels \(h(x),h(-x)\), and scores \(G(x),G(-x)\) | \(e_A(x)\le e_G(x)+e_G(-x)\), then \(\mathbb E_{x\sim\mathcal D}e_G(-x)=\mathcal L_{\mathcal D^-,h}(G)\) | Raw oddness and two source losses export \(\mathcal L_{\mathcal D,h}(A)\le2\mathcal L_{\mathcal D^{\mathrm{sym}},h}(G)\) to `step_003` | Same target and loss; no score residual, only the explicit factor \(2\) | Pointwise nonnegative inequality integrates exactly; no omitted error class | Primitive oddness and `step_001` precede the comparison and its consumer | Producer `step_002`; consumer `step_003`; final use is distribution-wise approximate separation | All four \((s_0,y)\) cases pass; source-score zeros and \(A(x)=0\) are included | `step-local` | None |
| `step_003`: for every \((\mathcal D,h)\), some \(v\) has error at most \(2\varepsilon\) | Expectation-to-existence and distribution-mode transfer; approximate separator producer | Universal success invoked on \(\mathcal D^{\mathrm{sym}}\), then `step_001` and `step_002` | Primitive `assump:universal-sgd-success` plus accepted earlier derived interfaces | The premise applies to every distribution and the result is an identity-coordinate vector in the required error metric | Same learner, target, initialization, updates, aggregate, expectation law \(\mathbb Q_{\mathcal D^{\mathrm{sym}},h}\), and fixed horizon | \(\mathbb E_\omega\mathcal L_{\mathcal D,h}(\langle v_\omega,\cdot\rangle)\le2\varepsilon\), hence one realization is at most \(2\varepsilon\) | Raw universal expectation bound and exact transfer export a vector for the original \(\mathcal D\); the vector may legally depend on \((\mathcal D,h)\) | Same score and original-distribution target loss; factor \(2\) is retained, with no probability or feature residual | One-shot expectation conversion; no union, uniform-in-trajectory, or time accumulation | Universal premise and earlier algebra produce the approximate result before `step_006`; exact separation is not assumed | Producer `step_003`; consumer `step_006`; final use is contradiction on \(\mathcal D_h^\star\) | Nonsymmetric and point-mass \(\mathcal D\), \(v_\omega=0\), \(T=1\), and \(\varepsilon=0\) are legal; no simultaneous \(\omega\) is claimed | `step-local` | None |
| `step_004`: strict-system equivalence on \(Q_h\) | Exact support/sign interface; converts representation into strict feasibility | Select \(Q_h=\{q:h(q)=-s_0\}\), then use oddness and score homogeneity | Primitive oddness and fixed tie rule; direct derivation | Exactly one representative per antipodal pair is selected, and strict signed score is equivalent to exact prediction on the pair | Same identity coordinates, homogeneous score, target, and tie rule as the theorem | \(h(q)\langle w,q\rangle>0\) on \(Q_h\) iff exact prediction holds on \(Q_h\cup(-Q_h)=\mathcal X\) | Raw labels and antipodal geometry export the strict system to `step_005` and `step_006` | No transform or metric change; antipodal score residual is zero | Finite conjunction only; tie ambiguity is removed by the \(-s_0\) representative choice | \(Q_h\) is defined from the primitive target before infeasibility is tested; no separator is presumed | Producer `step_004`; consumers `step_005`, `step_006`; final use is exact identity representation | For either \(s_0\), equality on every selected representative is an error; \(n=1\) also selects one per pair | `step-local` | None |
| `step_005` certificate obligation: zero convex certificate with support \(k\le n+1\) | Structural infeasibility certificate and finite-support bound | Closest point to zero in \(\operatorname{conv}(Z_h)\), followed by minimal-support affine pruning | `step_004` infeasibility plus direct current-notation Euclidean derivation | Failure of a strictly positive common inner product implies \(0\in\operatorname{conv}(Z_h)\), and ambient dimension gives the needed support scale | Same signed vectors \(z=h(q)q\), Euclidean inner product, and strict inequality as the branch system; no cited wrapper | If closest \(p\ne0\), then \(\langle p,z\rangle\ge\lVert p\rVert^2>0\), contradiction; minimal positive convex support has \(k\le n+1\) | Raw finite set \(Z_h\) and infeasibility export distinct representatives and positive coefficients summing to one | Certificate is in the exact target coordinates; no surrogate residual | Convex coefficients have controlled nonnegative sign and unit total; affine dependence eliminates support without residual | Conditional infeasibility from `step_004` directly produces the certificate before any lower bound is consumed | Producer `step_005`; the uniform-witness sub-obligation and then `step_006` consume it; final use is the witness distribution | \(p=0\) is the desired branch; \(z\ne0\), support is nonempty, and the \(k=n+1\) boundary is allowed | `step-local` | None |
| `step_005` uniform-witness obligation: error at least \(1/k\ge1/(n+1)\) | Structural lower bound; defeats every homogeneous halfspace on one distribution | Nonnegative zero certificate plus labels \(h(q_i)=-s_0\) | Earlier certificate sub-obligation within `step_005` and the `step_004` representative convention | Weighted sum zero forces a nonpositive signed score, which is exactly a tie-resolved classification error | Same target, score, selected representatives, uniform distribution, and fixed tie convention | \(\sum_i\alpha_i h(q_i)\langle w,q_i\rangle=0\) implies some term is at most zero; one of \(k\) uniform atoms errs | Raw positive coefficients, support size, and selected labels export a universal lower bound to `step_006` | Signed inner product equals target-score product exactly; equality is an error because \(h(q_i)=-s_0\) | Finite unit coefficient budget; uniform counting loses exactly \(1/k\), with no uncontrolled coefficient-size requirement | The certificate is established first within `step_005`, before the distribution and lower bound are exported; exact representation is not assumed | Producer `step_005`; consumer `step_006`; final use is the contradiction threshold | At \(w=0\), all atoms are ties and error is one; negative and equality cases both count; \(k\le n+1\) gives the correct direction | `step-local` | None |
| `step_006`: strict high-accuracy exactification | Quantitative specialization and theorem closure for each target | `step_003` upper bound, `step_005` lower bound, and primitive strict threshold | Accepted derived interfaces plus `assump:high-accuracy` | Both bounds concern precisely the same target, witness distribution, homogeneous score class, and loss | Identical tie, distribution, norm/loss, and quantifier convention on both sides | \(\inf_w L\le2\varepsilon<1/(n+1)\le\inf_w L\) contradicts infeasibility | Raw approximate interface, witness lower bound, and positive gap export strict feasibility and \(w_h\) | Zero object, distribution, metric, and probability residual; all numerical losses are exposed | One finite contradiction per target; no repeated or asymptotic forcing | Independently produced upper and conditional lower interfaces meet only here; feasibility is the conclusion | Producer `step_006`; consumers `step_007` and final assembly | At \(\varepsilon=0\) the gap is immediate; at equality the mechanism would fail, and equality is excluded; \(v=0\) cannot evade the witness | `step-local` | None |
| `step_007`: common identity map, point-mass law, and dimension chain | Exact common-feature and probability/quantifier closure | Separators from `step_006`, deterministic \(\varphi_{\mathrm{id}}\), complexity definitions, and \(S=m(n+1)\) | Accepted derived separators and primitive integer setup | Target-specific weights with one target-independent feature map give exactly both complexity interfaces | Produced and consumed feature map is literally \(x\mapsto x\); no trajectory, distribution, or target changes it | \(\forall h\,\exists w_h\,\forall x\) exactness implies the point-mass event for every \(\mathcal D,h\); \(n\le m(n+1)=S\le TS\) | Raw separators and fixed identity map export probability-one confident success and the full dimension chain | Every feature/score residual is zero; probability is exactly one, not a high-probability surrogate | No accumulation; point-mass sampling is stationary and exact | `step_006` produces all separators before the common-map conclusion is used | Producer `step_007`; consumer final theorem assembly | For \(\mathcal H=\varnothing\), target quantifiers are vacuous; \(m=T=1\), \(n=1\), and either tie label preserve the chain | `step-local` | None |
| Exact/noiseless and empty-class baseline invariance | Baseline recovery; prevents weakening of the exact target | `step_001`-`step_007` exact interfaces, with a vacuous branch before fixing \(h\) | Primitive setting and derived exact bridges | At \(\varepsilon=0\), the same mechanism proves exact identity representation; for empty \(\mathcal H\), all target claims hold vacuously | Source learner, aggregate, tie rule, horizon, and identity target are unchanged | Zero expected error gives a zero-error intermediate vector; strict witness lower bound rules out infeasibility; empty-class assembly invokes no target-dependent construction | Raw zero-error premise or vacuous target set exports the original exact conclusion, not a remainder-only claim | No surrogate object and no residual; exact conclusion is preserved | No repeated process; finite exact identities and one contradiction, or no target branch | Baseline premise precedes all consumers; neither exactness nor nonemptiness is assumed | Producers are primitive specialization plus steps; consumers are `step_006`, `step_007`, and final assembly | At \(\varepsilon=0\) the mechanism remains active; at \(\mathcal H=\varnothing\) no update or separator is needed because the conclusion is already true | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is ready for theorem-level diagnostic and step proof work. Its exact specialized target is source-aligned as `material_partial`, the dependency graph closes without hidden generated assumptions, and every hard theorem interface has a concrete mechanism source under the unchanged setting. The tie, null, equality, empty-class, and quantifier-order stresses all preserve the claimed conclusion. The remaining work is genuinely local derivation and audit of the seven stated steps, so `ACCEPTED` with score 9 is the aligned outcome.
