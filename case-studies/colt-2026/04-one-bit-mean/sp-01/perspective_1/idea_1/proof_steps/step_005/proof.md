# Proof Step

## Step Identity

- Sketch attempt: 1
- Step ID: `step_005`
- Unit attempt: 1

## Target Step Claim

- Intended claim: Prove digit range, query measurability/precommitment, and exact dither first/second moment identities.
- Depends on: `step_004`.
- Assumptions used: `assump:iid-independent-randomness` for seed timing.
- Technical challenge: Exact square identity/endpoints.
- Intended proof tool or cited result: Floor bounds and direct integration.
- Output target: Bounded-dither interface.
- Rate objective: None.

## Step Proof Status

COMPLETE

## Allowed Assumptions And Dependencies

- Primitive conditions: Assumption~\ref{assump:iid-independent-randomness}. It is used only to certify that every refinement seed \((L_i,A_i,B_i,U_i)\) is drawn before any response bit and that, conditional on the localization output, the refinement sample, and all non-dither refinement seeds, \(U_i\) still has law \({\rm Unif}[-1,2]\). The deterministic floor and integration identities do not use a distributional assumption on \(X\).
- Accepted dependency: `perspective_1/idea_1/proof_steps/step_004/proof.md` (SHA-256 `5a3e58510ac0aae757c434ebc510adc63833b9e2f26fd8d1ad27f4bb23c81153`) together with its `ACCEPTED` review `perspective_1/idea_1/proof_steps/step_004/review.md`. The dependency supplies the unique Borel maps \(c\mapsto a_j(c)\), the well-defined selected quantizers \(Q_j^c\), and the exact selected-cell margins.
- Derived invariants supplied by the accepted dependency: For every real \(c\) and \(0\leq j\leq J\), \(a_j(c)\) is uniquely defined and Borel, and \(c\) lies strictly inside the selected half-open cell, at left distance in \([3h_j/8,5h_j/8)\) and right distance in \((3h_j/8,5h_j/8]\). These facts justify the Borel decoder-side selection and rule out a selected-grid boundary ambiguity at \(c\); no cell-margin conclusion is reproved or exported here.
- Local conditional hypotheses: None. Fixing \(j,a,b,x,c\) in the deterministic local results is quantification, not a generated-object assumption.

The setting definitions provide \(h_{j+1}=2h_j\), the finite seed spaces for \(L_i,A_i,B_i\), and \(U_i\sim{\rm Unif}[-1,2]\). Positivity of the setting scales was already discharged in the accepted dependency. No localization event, moment bound, or digit-inactivity property is used.

## Local Lemma Map

| Local Unit ID | Unit type | Statement | Contribution to target step |
| ------------- | --------- | --------- | --------------------------- |
| `unit_001` | lemma | For every setting scale \(h_j>0\), \(0\leq j<J\), \(a,b\in\mathcal S\), and \(x\in\mathbb R\), the half-open floor cells give \(0\leq x-Q_{j,a}(x)<h_j\), \(0\leq x-Q_{j+1,b}(x)<2h_j\), and hence \(-h_j<F_{j,a,b}(x)<2h_j\), with neither endpoint attainable. | Proves the exact bounded digit range and audits both floor endpoints. |
| `unit_002` | proposition | Under Assumption~\ref{assump:iid-independent-randomness}, Lemma~\ref{lem:step-004-four-arc-selector}, Proposition~\ref{prop:step-004-cell-margin}, and Lemma~\ref{lem:step-005-digit-range}, the threshold set generated by every realized refinement seed is Borel, its membership map is jointly Borel in the sample and seed, and it is fixed before any response; subsequent decoder selection is measurable and does not change the query. | Proves query measurability and exact precommitment. |
| `unit_003` | proposition | Under Assumption~\ref{assump:iid-independent-randomness}, Lemma~\ref{lem:step-004-four-arc-selector}, and Lemma~\ref{lem:step-005-digit-range}, uniform dithering gives, for every \(j,a,b,x,c\), the exact first moment \(F(x)-F(c)\), the exact second moment \(3h_j|F(x)-F(c)|\), and pathwise zero when \(x=c\); the same statements hold for the selected digit \(D_j^c\). | Exports the exact bounded-dither mean and square interface used downstream. |

Atomic step = no. Strict digit range, Borel precommitment, and the absolute-difference square identity are independent subclaims and are separated into three units.

## Cited Result Applications

No external paper result or standard theorem is used. In particular, the bounded-dither construction's literature lineage is not invoked as proof authority; the expectations are integrated directly below.

The accepted dependency and local results used later are restated in current notation:

1. **Accepted stable-selector and cell-margin certificate.** Lemma~\ref{lem:step-004-four-arc-selector} gives, for every real \(c\) and setting scale, a unique Borel selector \(a_j(c)\in\mathcal S\). Proposition~\ref{prop:step-004-cell-margin} gives the selected-cell distance intervals
   \[
   c-Q_j^c(c)\in[3h_j/8,5h_j/8),\qquad
   Q_j^c(c)+h_j-c\in(3h_j/8,5h_j/8].
   \]
   Its assumption basis and scale positivity were discharged in the accepted `step_004` proof/review pair. The conclusions used here are Borel selection of the exact pair \((a_j(c),a_{j+1}(c))\) and absence of a selected-grid boundary at \(c\).
2. **Exact floor-residual and digit-range lemma (Lemma~\ref{lem:step-005-digit-range}).** A shifted floor quantizer at width \(s>0\) has residual in \([0,s)\), including zero at every left endpoint and resetting to zero at every excluded right endpoint. At widths \(h_j\) and \(2h_j\), subtracting the two quantizers therefore places \(F_{j,a,b}\) strictly in \((-h_j,2h_j)\).
3. **Borel precommitted-query proposition (Proposition~\ref{prop:step-005-query-precommitment}).** The threshold preimage of every fixed digit and seed is a countable union of intersections of half-open floor cells. Assumption~\ref{assump:iid-independent-randomness} supplies the timing and independence needed to classify this Borel set as a precommitted query.
4. **Exact uniform-dither proposition (Proposition~\ref{prop:step-005-dither-identities}).** For two normalized digit values in \((-1,2)\), the difference of their threshold indicators has expectation equal to one third of their signed difference, while its square is the indicator of the half-open interval between the two thresholds and has expectation equal to one third of their absolute difference.

## Local Derivation

### unit_001: lemma

**Lemma (Exact floor residuals and bounded dyadic digits).** \(\label{lem:step-005-digit-range}\)
For every setting scale \(h_j>0\), every \(0\leq j<J\), every \(a,b\in\mathcal S\), and every \(x\in\mathbb R\),

\[
0\leq x-Q_{j,a}(x)<h_j,
\qquad
0\leq x-Q_{j+1,b}(x)<h_{j+1}=2h_j,
\]

and consequently

\[
-h_j<F_{j,a,b}(x)<2h_j.
\]

The two digit-range endpoints are never attained, including when \(x\) is a boundary of either shifted grid.

**Proof / justification.**
For a width \(s>0\) and shift \(\alpha\in\mathbb R\), put

\[
n=\left\lfloor\frac{x-\alpha s}{s}\right\rfloor.
\]

The defining floor inequalities give

\[
n\leq\frac{x-\alpha s}{s}<n+1,
\]

so

\[
(\alpha+n)s\leq x<(\alpha+n+1)s.
\]

Thus the shifted floor quantizer \((\alpha+n)s\) is the included left endpoint of the unique half-open cell containing \(x\), and

\[
0\leq x-(\alpha+n)s<s.
\]

At an included left endpoint \(x=(\alpha+n)s\), the residual is exactly zero. The point \((\alpha+n+1)s\) is excluded from that cell and included as the left endpoint of the next cell, where the floor index is \(n+1\) and the residual again equals zero. Hence the residual can never equal \(s\); this explicitly covers positive, zero, and negative cell indices.

Apply this calculation first with \((s,\alpha)=(h_j,a)\) and then with \((s,\alpha)=(h_{j+1},b)=(2h_j,b)\). Writing the two residuals only within this proof as

\[
r_j=x-Q_{j,a}(x)\in[0,h_j),
\qquad
r_{j+1}=x-Q_{j+1,b}(x)\in[0,2h_j),
\]

one obtains

\[
F_{j,a,b}(x)
=Q_{j,a}(x)-Q_{j+1,b}(x)
=r_{j+1}-r_j.
\]

Because \(r_{j+1}\geq0\) and \(r_j<h_j\), the difference is strictly greater than \(-h_j\). Because \(r_{j+1}<2h_j\) and \(r_j\geq0\), it is strictly less than \(2h_j\). Equality at \(-h_j\) would require the impossible value \(r_j=h_j\), and equality at \(2h_j\) would require the impossible value \(r_{j+1}=2h_j\). \(\square\)

### unit_002: proposition

**Proposition (Borel and precommitted refinement queries).** \(\label{prop:step-005-query-precommitment}\)
Under Assumption~\ref{assump:iid-independent-randomness}, Lemma~\ref{lem:step-004-four-arc-selector}, Proposition~\ref{prop:step-004-cell-margin}, and Lemma~\ref{lem:step-005-digit-range}, for every realized refinement seed \((L_i,A_i,B_i,U_i)=(j,a,b,u)\), the setting-defined query

\[
\mathcal A_i=\left\{x\in\mathbb R:
\frac{F_{j,a,b}(x)}{h_j}\geq u\right\}
\]

is Borel. The membership map is jointly Borel in \((x,j,a,b,u)\) on
\(\mathbb R\times\{0,\ldots,J-1\}\times\mathcal S^2\times[-1,2]\), and \(\mathcal A_i\) is fixed before any response bit is observed. Moreover, Lemma~\ref{lem:step-004-four-arc-selector} makes the decoder-side selected pair and centering indicator Borel in \(c\), but neither \(c\) nor any response bit changes \(\mathcal A_i\).

**Proof / justification.**
For fixed \(j,a,b\), the two quantizers are constant on the half-open cells

\[
C_{n}=[(a+n)h_j,(a+n+1)h_j),
\qquad
K_{m}=[(b+m)h_{j+1},(b+m+1)h_{j+1}),
\]

where \(n,m\in\mathbb Z\). On \(C_n\cap K_m\),

\[
F_{j,a,b}(x)=(a+n)h_j-(b+m)h_{j+1}.
\]

Consequently,

\[
\mathcal A_i
=\bigcup_{\substack{n,m\in\mathbb Z:\\
((a+n)h_j-(b+m)h_{j+1})/h_j\geq u}}
(C_n\cap K_m).
\]

Each intersection is empty or a half-open interval, and the union is countable, proving that \(\mathcal A_i\) is Borel. Equivalently, the floor map is Borel, so \(F_{j,a,b}\) is Borel and \(\mathcal A_i\) is its inverse image of the closed ray \([u,\infty)\) after division by \(h_j>0\).

The map \((x,u)\mapsto F_{j,a,b}(x)/h_j-u\) is Borel. Since the level and offset spaces are finite, adjoining \((j,a,b)\) proves joint Borel measurability of the membership indicator. Lemma~\ref{lem:step-005-digit-range} also audits the dither-support endpoints: if \(u=-1\), then \(F_{j,a,b}(x)/h_j>-1\) for every \(x\), so \(\mathcal A_i=\mathbb R\); if \(u=2\), then \(F_{j,a,b}(x)/h_j<2\), so \(\mathcal A_i=\varnothing\). Thus neither endpoint creates a range-overflow or measurability exception.

By Assumption~\ref{assump:iid-independent-randomness}, \(L_i,A_i,B_i,U_i\) are all generated before any response bit. The displayed set depends only on those seeds and the known, deterministic scale family, and contains no occurrence of the decoded center \(c\). It is therefore a precommitted measurable query.

For completeness on the decoder side, Lemma~\ref{lem:step-004-four-arc-selector} gives Borel maps \(c\mapsto a_j(c)\) and \(c\mapsto a_{j+1}(c)\). Because their ranges are finite and every \(F_{j,a,b}\) is Borel, both

\[
c\longmapsto
F_{j,a_j(c),a_{j+1}(c)}(c)=D_j^c(c)
\]

and the corresponding threshold indicator are Borel. Proposition~\ref{prop:step-004-cell-margin} additionally places \(c\) at positive distance from the boundaries of both selected cells, so the selected floor values have no spatial boundary ambiguity. These decoder operations only select and center an already observed, precommitted bit; they do not alter its query set. \(\square\)

### unit_003: proposition

**Proposition (Exact bounded-uniform-dither moments).** \(\label{prop:step-005-dither-identities}\)
Under Assumption~\ref{assump:iid-independent-randomness}, Lemma~\ref{lem:step-004-four-arc-selector}, and Lemma~\ref{lem:step-005-digit-range}, fix \(0\leq j<J\), \(a,b\in\mathcal S\), and \(x,c\in\mathbb R\). If \(U\sim{\rm Unif}[-1,2]\), then

\[
\mathbb E_U\!\left[3h_j\left(
\mathbf 1\!\left\{\frac{F_{j,a,b}(x)}{h_j}\geq U\right\}
-\mathbf 1\!\left\{\frac{F_{j,a,b}(c)}{h_j}\geq U\right\}
\right)\right]
=F_{j,a,b}(x)-F_{j,a,b}(c),
\]

and

\[
\mathbb E_U\!\left[\left(3h_j\left(
\mathbf 1\!\left\{\frac{F_{j,a,b}(x)}{h_j}\geq U\right\}
-\mathbf 1\!\left\{\frac{F_{j,a,b}(c)}{h_j}\geq U\right\}
\right)\right)^2\right]
=3h_j\left|F_{j,a,b}(x)-F_{j,a,b}(c)\right|.
\]

These identities remain valid in the actual refinement protocol after conditioning on \((c,X_i,L_i,A_i,B_i)\), which is independent of \(U_i\), and before conditioning on the response bit \(Y_i\). In particular, for the Borel selected pair \((a_j(c),a_{j+1}(c))\) supplied by Lemma~\ref{lem:step-004-four-arc-selector},

\[
\mathbb E_U\!\left[3h_j\left(
\mathbf 1\!\left\{\frac{D_j^c(x)}{h_j}\geq U\right\}
-\mathbf 1\!\left\{\frac{D_j^c(c)}{h_j}\geq U\right\}
\right)\right]
=D_j^c(x)-D_j^c(c),
\]

\[
\mathbb E_U\!\left[\left(3h_j\left(
\mathbf 1\!\left\{\frac{D_j^c(x)}{h_j}\geq U\right\}
-\mathbf 1\!\left\{\frac{D_j^c(c)}{h_j}\geq U\right\}
\right)\right)^2\right]
=3h_j\left|D_j^c(x)-D_j^c(c)\right|.
\]

At \(x=c\), the centered indicator difference and both displayed quantities are zero for every \(U\), not merely almost surely.

**Proof / justification.**
Use the proof-local abbreviations

\[
f_x=\frac{F_{j,a,b}(x)}{h_j},
\qquad
f_c=\frac{F_{j,a,b}(c)}{h_j}.
\]

Lemma~\ref{lem:step-005-digit-range} gives \(f_x,f_c\in(-1,2)\). For any \(t\in(-1,2)\), the threshold convention in the query yields

\[
\mathbb E_U\mathbf 1\{t\geq U\}
=\Pr\{U\leq t\}
=\frac{t+1}{3}.
\]

Subtracting this equality at \(t=f_x\) and \(t=f_c\), then multiplying by \(3h_j\), proves the first identity.

For the square, define only inside this proof

\[
\Delta_U=\mathbf 1\{f_x\geq U\}-\mathbf 1\{f_c\geq U\}.
\]

If \(f_x>f_c\), then \(\Delta_U=1\) exactly on \((f_c,f_x]\) and is zero elsewhere. If \(f_c>f_x\), then \(\Delta_U=-1\) exactly on \((f_x,f_c]\) and is zero elsewhere. If \(f_x=f_c\), it is identically zero. Hence in all cases

\[
\Delta_U^2
=\mathbf 1\{\min(f_x,f_c)<U\leq\max(f_x,f_c)\}.
\]

The interval lies inside \([-1,2]\) and has length \(|f_x-f_c|\). Since the uniform density is \(1/3\),

\[
\mathbb E_U\Delta_U^2=\frac{|f_x-f_c|}{3}.
\]

Multiplication by \(9h_j^2\) gives

\[
\mathbb E_U(3h_j\Delta_U)^2
=3h_j^2|f_x-f_c|
=3h_j|F_{j,a,b}(x)-F_{j,a,b}(c)|,
\]

which is the exact square identity. The half-open interval above audits threshold equality: at the smaller threshold both indicators equal one, whereas at the larger threshold only the larger-threshold indicator equals one. Such ties are \(U\)-null, but the pointwise convention is nevertheless consistent. At the support endpoints \(U=-1\) and \(U=2\), both indicator differences vanish because \(f_x,f_c\in(-1,2)\); including those endpoints in the dither support therefore leaves both identities unchanged.

Assumption~\ref{assump:iid-independent-randomness} makes each \(U_i\) independent of the localization output \(c\), the refinement sample \(X_i\), and \((L_i,A_i,B_i)\). Therefore, conditional on any realized values of those non-dither quantities, \(U_i\) retains the same uniform law and the two calculations apply verbatim. Finally, choosing \(a=a_j(c)\) and \(b=a_{j+1}(c)\), whose existence and Borel measurability follow from Lemma~\ref{lem:step-004-four-arc-selector}, turns \(F_{j,a,b}\) into the exact setting-defined selected digit \(D_j^c\). When \(x=c\), the two threshold indicators are identical for every \(U\), proving the exact zero-displacement specialization. \(\square\)

## Target-Step Assembly

Lemma~\ref{lem:step-005-digit-range} derives from the half-open floor convention the strict digit range

\[
-h_j<F_{j,a,b}(x)<2h_j
\]

for every bank entry and every real input, and it explicitly excludes both apparent endpoint values even at either grid's boundaries. Proposition~\ref{prop:step-005-query-precommitment} converts the same cell description into a Borel threshold set for every realized seed, proves joint membership measurability, and uses Assumption~\ref{assump:iid-independent-randomness} to establish that the query is fixed before all responses. Lemma~\ref{lem:step-004-four-arc-selector} supplies the Borel decoder-selected pair, and Proposition~\ref{prop:step-004-cell-margin} confirms that decoder centering at \(c\) has no selected-grid boundary ambiguity, while leaving the precommitted query unchanged.

Proposition~\ref{prop:step-005-dither-identities} then integrates the exact uniform threshold rule. Its first calculation gives the signed digit difference, and its half-open interval calculation gives the exact square

\[
3h_j|F_{j,a,b}(x)-F_{j,a,b}(c)|
\]

without an inequality or remainder. Instantiating the Borel pair from Lemma~\ref{lem:step-004-four-arc-selector} gives the identical interface for \(D_j^c(x)-D_j^c(c)\), and \(x=c\) gives pathwise zero for every dither value. These three named results jointly prove the exact sketch-row claim and export only the bounded digit range, Borel precommitted-query certificate, and exact dither mean/square identities needed by the downstream conditional-moment step.

## Explicit Rate Audit

None. This step is an exact one-level identity and is not rate-bearing.

## Blockers

None.

## Notation And Assumption Notes

- **Setting-defined, public-facing objects:** \(h_j\), \(Q_{j,a}\), \(F_{j,a,b}\), \(\mathcal A_i\), \(a_j(c)\), \(Q_j^c\), and \(D_j^c\) are all defined in `setting.md`. The minimal public-facing export is only the strict range \((-h_j,2h_j)\), Borel/precommitted threshold queries, and the two exact dither identities, including their selected-digit specialization.
- **Accepted dependency objects:** The selector \(a_j(c)\), selected quantizer \(Q_j^c\), and \(3h_j/8\) cell margin are derived outputs of the current accepted `step_004` pair. The margin is used only to audit selected-center boundary behavior and is not reintroduced as a primitive condition or re-exported by this step.
- **Proof-local objects:** The generic width and shift \((s,\alpha)\), floor index \(n\), residuals \(r_j,r_{j+1}\), cells \(C_n,K_m\), normalized values \(f_x,f_c\), and indicator difference \(\Delta_U\) are direct expressions in setting notation used only inside the relevant derivation. None is exported.
- **Appendix-local objects:** There are no new appendix-local constants or dictionaries. The countable cell union is a proof representation of the setting-defined query, not a new query object.
- **Constant and endpoint provenance:** The factor \(3\) and support endpoints \(-1,2\) come directly from the setting-defined dither law of interval length three. The residual intervals \([0,h_j)\) and \([0,2h_j)\) are proved in Lemma~\ref{lem:step-005-digit-range}; the strict digit endpoints and exact square coefficient are derived there and in Proposition~\ref{prop:step-005-dither-identities}. No hidden or free constant is introduced.
- **Assumption provenance:** Seed timing and conditional uniformity are primitive consequences of `assump:iid-independent-randomness`. Borel selector and cell-margin facts are accepted derived outputs of `step_004`. Digit boundedness, query measurability, and both moment identities are proved in this step. No generated event, localization success, stability event, boundedness hypothesis, or local conditional hypothesis is assumed.
- **Probability, scope, and boundary modes:** The deterministic claims hold for every \(0\leq j<J\), every \(a,b\in\mathcal S\), and every \(x,c\in\mathbb R\). Expectations are over one public dither variable, and the conditional version holds after fixing all independent non-dither quantities. There is no limit, horizon upgrade, norm conversion, or rate simplification. Floor-cell boundaries, dither-support endpoints, threshold ties, and the exact \(x=c\) baseline are all handled pointwise.
- **Diagnostic boundary:** `global_proof.md` was not read and supplied no evidence, cited result, assumption, or claim modification.
