# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Reviewed artifact: `perspective_3/idea_1/proof_sketch.md`
- Goal mode: exact-goal mode; unconditional full three-regime theorem

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch preserves the full source target: every fixed (k>1), the unrestricted class \(\mathcal D(k,\lambda,\sigma)\), all stated \(\lambda,\sigma,\epsilon,\delta\) quantifiers, zero adaptive transitions, one bit per sample, unconditional probability over samples and protocol randomness, and the complete piecewise rate including the single \(k=2\) logarithm. No generated success event is promoted to a theorem assumption.
- Dependency audit: `step_001` through `step_008` form an acyclic producer-consumer chain. Every dependency is earlier, every theorem-facing generated output has a producer before use, and the locally conditional refinement statement is discharged only in `step_008`.
- High-risk coverage: The sketch localizes the source wrapper, exact half-open ring geometry, target and alias cancellation, all-alias square control, activation, conditional variance, outer bias, confidence conversion, public-rate specialization, baseline identities, and protocol precommitment into separate auditable steps. Independent endpoint and algebra checks found no missing target or same-color alias branch.
- Explicit-rate coverage: Exposed variables, fixed quantities, hidden-constant dependence, probability, horizon, and norm modes are stated. The geometric evaluation of \(Z_S\), block and group ceilings, localization-confidence absorption, and all three public substitutions are assigned to `step_006` and `step_008` with concrete inequalities.
- Assumption and citation plausibility: The only theorem-critical citation was checked against the cached arXiv:2604.07796v2 source. Theorem 16 (`thm: alternative localization`) is deterministic and non-adaptive, assumes \(\mathbb E|X-\mu|\leq\sigma\), returns an interval containing \(\mu\), and has the stated logarithmic cost; Appendix `appendix: two-stage` gives \(|I|\leq100\sigma\). Lyapunov, midpoint, and nearest-grid wrappers discharge the remaining interface without changing the source protocol.

## Early Obstruction Audit

- Limiting-case stress: The construction remains well typed for negative grid indices, both child bits, all half-open endpoints, \(S=1\), \(\lambda=\sigma\), \(\epsilon=c_k\sigma\), \(k=2\), zero variance, laws supported in the selected \(J_0\), a point mass at \(m_0\), and localization failure. Floor division gives \(j_{s-1}=2j_s+b_s\) with \(b_s\in\{0,1\}\) even for negative indices. The estimator remains defined on localization failure and that event is paid in the final probability bound.
- Theorem-critical bridge support: The source interval is bridged as \(I\mapsto c=\operatorname{mid}(I)\mapsto m_0\mapsto M_k(c)\), with \(|c-\mu|\leq50\sigma\), \(|c-m_0|\leq h_0/2\), and \(|m_0-\mu|\leq3h_0/4\) after \(a_k\geq200\). The ring statistic is bridged to the target by the exact decomposition \(\mu-m_0=\theta(c)+\mathbb E[(X-m_0){\bf1}_{J_S^c}]\); `step_006` controls the only residual.
- Exported-interface feasibility: Each export has raw controls and a downstream-compatible interface. In particular, the activation budget uses the same \(r=|x-m_0|\) whose \(k\)-moment `step_001` controls; the variance is for the exact \(W_i(c)\) consumed by median-of-means; and the tail residual is in the final absolute-error metric at the required \(\epsilon/4\) scale. No threshold is used to hide a fixed, empirical, or event-level residual.
- Theorem-critical mechanism witness gate: Passed for every hard block. Localization has a checked cited source and scalar wrapper; geometry has exact endpoint identities; cancellation has the two dither identities and mask self/cross moments; activation has child-padding exclusion and four-color separation; variance has \(h_s^2/p_s=Z_Sh_s^k\); tail control has the moment-tail inequality; confidence has a strict group-success margin; and final closure has explicit triangle, union, and ceiling inequalities.
- Entry-state trace stress: At \(s=0\), \(R_0=J_0\); at the first higher scale, \(J_0\subset J_1\) and \(R_1=J_1\setminus J_0\). At \(x=m_0\), the base amplitude and \(\Delta Y\) vanish and every retained higher ring is inactive, so the correction is exactly zero. In the source trivial branch, \(I=[-\lambda,\lambda]\) and \(c=0\) are already defined without an update. No mechanism is inactive while its theorem-facing conclusion remains false.
- Obligation locality classification: The localization/core wrapper (`step_001`), ring/query geometry (`step_002`), exact mean and alias cancellation (`step_003`), activation ledger (`step_004`), all-alias variance and \(k=2\) accounting (`step_005`), tail/rate bridge (`step_006`), conditional median amplification (`step_007`), and unconditional protocol/rate closure (`step_008`) are all `step-local`. Their mechanism sources and interfaces are already fixed; later proofs need only instantiate the displayed derivations.
- Noncircular closure gate: Passed. Localization success is produced by the cited first block, geometry is deterministic for every decoder output, the exact mean precedes tail and concentration, activation precedes variance, variance and bias precede median concentration, and only the final step integrates the conditional event. No local-validity, boundedness, selected-cell, or success conclusion is assumed in order to prove itself.
- Mechanism-source and boundary stress: Exact endpoint arithmetic gives
  \[
  J_{s-1,2j}=[(j-\tfrac12)h_s,(j+1)h_s),\qquad
  J_{s-1,2j+1}=[jh_s,(j+\tfrac32)h_s),
  \]
  both contained in \(J_{s,j}=[(j-1)h_s,(j+2)h_s)\). Same-color paddings with indices differing by four have a full-cell gap. The target child padding contains the radius-\(h_s/2\) neighborhood of \(m_0\), while every non-target same-color padding is at distance greater than \(2h_s\) from the path cell. Thus target activity gives \(h_s\leq2r\), aliases give the stronger \(h_s\leq r/2\), and both boundary branches are covered.
- Generated-output flow: Every generated output in the sketch table has a legal producer, consumers, final use, and dependency path. The public theorem consumes only `step_001`-`step_007` outputs and primitive protocol conditions; no closure or assembly label acts as an unproved producer.
- Source-to-claim adequacy: For level zero,
  \[
  p_0\frac14\frac{16h_0}{p_0}\frac{x-m_0}{4h_0}=x-m_0.
  \]
  For \(s\geq1\), the level/color/branch/type probabilities and the displayed weights give \(x-m_s\) for the coordinate type and \(d_s\) for the mass type, hence \(x-m_0\). On an alias \(j\neq j_s\), \(\mathbb E[\rho_{j_s}\rho_j]=0\). The direct sources therefore have exactly the signed, lower/support, and cancellation content claimed, including every retained branch.
- Residual-to-target adequacy: Mask aliases and ring gaps leave zero mean residual. The sole transfer residual is \(R_{\rm tail}=\mathbb E[(X-m_0){\bf1}_{J_S^c}]\), and \([m_0-H,m_0+H]\subset J_S\) gives \(|R_{\rm tail}|\leq M_k(c)/H^{k-1}\leq C_k\sigma^k/H^{k-1}\). Choosing \(b_k^{k-1}\geq4C_k\) dominates it at \(\epsilon/4\). The estimator and target are then compared in the same absolute-value norm.
- Baseline invariance audit: If the law is supported in the selected \(J_0\), every retained higher target ring is excluded by nesting and every same-color alias padding is disjoint from the target \(J_s\); hence higher-level \(W_i\) terms are exactly zero, not merely mean zero. The outer residual is also exactly zero. If \(X=m_0\) almost surely, \(F=0\), \(\Delta Y=0\), and \(\widehat\mu=m_0=\mu\) on localization success. The inherited baseline conclusions are preserved without a surrogate remainder.
- Scope-accumulation compatibility: Ring telescoping is exact over finite \(S\). Nonnegative alias-square forcing satisfies \(\sum_{s\geq1}h_s^k{\bf1}_{\mathcal A_s}\leq C_kr^k\), so it has a finite geometric budget independent of \(S\). At \(k=2\), \(p_s=1/(S+1)\) and summing the pointwise charges before expectation gives \((S+1)\sum_s h_s^2{\bf1}_{\mathcal A_s}\leq C(S+1)r^2\): exactly one factor \(Z_S=S+1\), not \(S^2\). Median amplification accumulates only independent failure indicators at a fixed finite horizon.
- Scope and dependence consistency: The proof claims no all-time or fixed-seed guarantee. It conditions only on the independent localization transcript and averages over the independent per-sample refinement seeds, exactly as permitted by the goal's probability over protocol randomness. Freezing all public masks would generally destroy the common conditional mean, but that stronger conditional guarantee is neither claimed nor used. Conditional on localization alone, the \(W_i(c)\) remain iid under the joint sample-and-protocol law, so Chebyshev and Hoeffding apply without a cell union bound.
- Generated-condition provenance: \(\mathcal E_{\rm loc}\), core membership, recentered moment, path partition, exact mean, activation, variance, tail bound, and refinement-success event are all derived outputs with explicit producers. The final theorem retains only the four primitive assumptions from `setting.md`.
- Citation and tool applicability: arXiv:2604.07796v2, Theorem 16 and Appendix `appendix: two-stage`, match the current independent-sample, deterministic precommitted one-bit convention. The source interval/scalar mismatch and its non-output of any refinement statistic are explicit and repaired by current-notation wrappers. Dither, geometry, moment, Chebyshev, and Hoeffding claims are direct derivations, not unverified external wrappers.
- Same-setting repair plausibility: No material obstruction requiring repair was found. The remaining work consists of the declared local derivations under the unchanged setting and goal.
- Target-preserving bridge-repair gate: Passed without a repair route; every needed bridge is already exposed at sketch level, and no baseline, mode, metric, scope, or rate dependence is weakened.
- High-risk obligation classes: Structural support/nesting, countable Borel queries, approximation by outer truncation, randomized cancellation, all-alias second moments, conditional-to-unconditional probability conversion, exact-rate specialization, ceilings, and precommitment all have source-compatible, boundary-tested witnesses.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Localization interval, scalar center, core, recentered moment, and cost | Cited localization and same-target entry bridge | Lau-Scarlett v2 Theorem 16 plus midpoint, nearest-center, and Lyapunov wrappers | Checked theorem `thm: alternative localization` and Appendix `appendix: two-stage`; primitive assumptions `assump:parameter-domain`, `assump:moment-class`, `assump:independent-samples`, `assump:precommitted-protocol` | Interval containment and \(\lvert I\rvert\leq100\sigma\) give a scalar radius, then grid geometry gives the core and triangle-power gives the recentered moment | Same real observations, independent one-bit samples, deterministic precommitted measurable queries, absolute metric, and confidence; interval-only non-output is explicit | \(\lvert c-\mu\rvert\leq50\sigma\), \(\lvert c-m_0\rvert\leq h_0/2\), \(a_k\geq200\), and \(M_k(c)\leq2^{k-1}(\sigma^k+(3h_0/4)^k)\) | Raw first moment, source interval, interval length, and source count export \(\mathcal E_{\rm loc}\), \(m_0\), \(M_k(c)\), and \(N_{\rm loc}\) | Both interval-to-point and grid residuals are dominated at core radius \(3h_0/4\); failure remains explicit | One finite localization block; source Hamming defects are covered by \(\delta/4\), with no repeated forcing | Primitive assumptions -> cited source -> wrappers -> all consumers | `step_001` produces; `step_002`, `step_005`, `step_006`, `step_007`, and `step_008` consume | Trivial range, \(\lambda=\sigma\), source ties, grid ties, point mass, and failure output remain defined | step-local | None |
| Nested path, disjoint rings, outer containment, and bounded Borel queries | Structural support, exact finite telescope, and protocol legality | Direct half-open dyadic endpoint calculation and four-color spacing | Primitive grid definitions and `assump:precommitted-protocol` | Child paddings are subsets of parents; one-child differences partition \(J_S\); same-color supports are disjoint; amplitudes are at most \(3/4\) or \(1\) | Exact branch convention uses floor division and half-open real intervals for all integers; no external convention | \(J_s=J_{s-1}\mathbin{\dot\cup}R_s\), \(m_0\in P_{s,j_s}\), \(\lvert d_s\rvert\leq h_s/2\), and \([m_0-H,m_0+H]\subset J_S\) | Countable disjoint Borel pieces make each realized \(F_i\) a Borel function with at most one nonzero summand and range in \([-1,1]\) | Produced and consumed path sets are identical, and outer containment has zero geometric residual | Half-open endpoints remove overlap, four-color spacing removes aliases, amplitude bounds remove dither overflow, and the finite telescope has zero accumulated forcing | Definitions -> endpoint derivation in `step_002` -> mean, activation, variance, tail, and final legality | `step_002` produces; `step_003`-`step_006` and `step_008` consume | Negative ancestors, both children, every half-open endpoint, \(S=1\), and \(x=m_0\) pass | step-local | None |
| Exact conditional mean, alias cancellation, and target telescope | Signed cancellation and generated mean for refinement | Uniform dither identities, target-mask self-correlation, cross-mask orthogonality, and coordinate-plus-mass algebra | Direct derivation from `step_002` and primitive independent refinement seeds | Exact selection-probability inversion yields \(x-m_s\) and \(d_s\); distinct alias masks have zero cross moment | The law conditions on localization but includes independent per-sample protocol randomness, matching the theorem probability mode | \(\mathbb E_U\Delta=f/2\), \(\mathbb E[\rho_{j_s}\rho_j]={\bf1}\{j=j_s\}\), and \((x-m_s)+d_s=x-m_0\) | Raw levels, colors, branches, types, masks, dithers, and exact weights export \(\theta(c)=\mathbb E[(X-m_0){\bf1}_{J_S}]\) | Exact decomposition leaves only \(\mathbb E[(X-m_0){\bf1}_{J_S^c}]\), routed to `step_006`; alias and fine-scale residuals are zero | Alias means cancel within each query expectation and finite target rings telescope exactly | `step_002` -> `step_003` -> `step_006`, `step_007`, `step_008` | `step_003` produces \(\theta(c)\); later tail, concentration, and assembly steps consume | \(f=-1,0,1\), target, alias, outside-support, \(d_s=0\), endpoints, and \(x=m_0\) pass | step-local | None |
| All-retained target and alias activation ledger | Structural support and finite geometric budget for alias squares | Child-padding exclusion for target rings and four-color spatial separation for aliases | Direct deterministic geometry from `step_002` | The source supplies lower distance content, not merely boundedness: target activity forces \(r\geq h_s/2\), alias activity forces \(r>2h_s\) | Same half-open grid, floor ancestor, branch, and absolute-distance convention as the consumed moment | \(\sum_{s\geq1}h_s^k{\bf1}_{\mathcal A_s(x,c)}\leq\sum_{h_s\leq2r}h_s^k\leq C_kr^k\) | Raw path cell and all same-color branch-matched rings export a pointwise charge in the exact \(r=\lvert x-m_0\rvert\) metric | No surrogate distance or omitted alias class; every retained target and alias square is covered | Nonnegative forcing has a finite dyadic budget uniformly in \(S\), even if every eligible scale activates | `step_002` -> `step_004` -> `step_005` | `step_004` produces; `step_005` is the sole variance consumer | \(r=0\), first scale, equality at child endpoints, \(q=\pm1\), negative ancestors, both branch values, and \(S=1\) pass | step-local | None |
| Conditional variance and single-log \(k=2\) interface | All-alias second moment and concentration scale | Dither square identity, exact importance weights, activation ledger, and recentered moment | Direct derivation from `step_001`-`step_004` | Coordinate and mass squares are each \(O(h_s^2/p_s)\); target and alias masks square to one and are all charged | Exact statistic \(W_i(c)\), joint conditional law, and absolute scalar interface used by `step_007` | \(h_s^2/p_s=Z_Sh_s^k\) and \(\mathbb E W_i^2\leq CZ_S(h_0^k+\mathbb E r^k)\leq C_k\sigma^kZ_S\) | Raw amplitude, \(\lvert d_s\rvert\), dither square, level law, and all-alias activity export the variance of the consumed statistic | No target-only omission; level zero is separately \(O(Z_Sh_0^k)\); variance uses the same conditional law | Nonnegative charges are summed pointwise before expectation. At \(k=2\), one external \(Z_S=S+1\) multiplies a geometric \(r^2\) budget, so no second level factor occurs | `step_001` + `step_002` + `step_003` + `step_004` -> `step_005` -> `step_007` | `step_005` produces; `step_007` and `step_008` consume | Zero variance, alias-only activity, target-only activity, mass amplitude one, \(d_s=0\), \(S=1\), and \(k=2\) pass | step-local | None |
| Tail residual, dyadic rounding, \(Z_S\), and three public refinement regimes | Residual-to-target bridge and quantitative specialization | Moment-tail inequality and finite geometric sums | Direct derivation from recentering, outer containment, and parameter domain | Outer complement implies \(r>H\); the same \(k\)-moment controls bias, while the sign of \(2-k\) selects first, flat, or last term of \(Z_S\) | Same absolute metric, fixed horizon, exact dyadic scales, and natural-log public rate | \(\lvert R_{\rm tail}\rvert\leq C_k\sigma^k/H^{k-1}\leq C_kb_k^{1-k}\epsilon\), \(H_\star\leq H<2H_\star\), and explicit geometric-series bounds | Raw \(M_k(c)\), \(H_\star\), \(H\), \(h_s\), and \(Z_S\) export bias \(\epsilon/4\) and \(A_k=\sigma^kZ_S/\epsilon^2\) | The only target residual is dominated at \(\epsilon/4\); rounding costs a factor below two and creates no wrong-scale term | One tail residual and one finite nonnegative geometric sum; no repeated or adversarial forcing | `step_001` + `step_002` + `step_003` -> `step_006` -> `step_007`, `step_008` | `step_006` produces tail and rate interfaces; confidence and final assembly consume | Zero outer mass, support in \(J_0\), atoms on \(J_S\) endpoints, \(H_\star/h_0=2\), \(k=2\), and \(\epsilon=c_k\sigma\) pass | step-local | None |
| Conditional median-of-means event under public randomized queries | Fixed-horizon concentration and confidence amplification | Conditional Chebyshev plus independent-group Hoeffding | Direct derivation from primitive block independence and `step_003`, `step_005`, `step_006` | Variance controls the exact common joint-law mean \(\theta(c)\); a per-group failure at most \(1/4\) gives a strict majority margin | Conditioning is on the localization transcript only. Independent public masks, levels, and dithers remain part of each iid \(W_i\), exactly matching the theorem's probability over protocol randomness | \(4V_k/(B_{\rm ref}\epsilon^2)\leq1/4\) and \(\Pr\{S_G\geq G/2\mid c\}\leq e^{-G/8}\leq\delta/2\) | Raw mean, variance, independent per-sample seeds, block size, and odd group count export the conditional \(\epsilon/2\) event | Group failures are finite independent Bernoulli charges; median selects rather than sums group errors | Mean and variance are produced before failure indicators; no final PAC event is assumed | `step_003` + `step_005` + `step_006` -> `step_007` -> `step_008` | `step_007` produces; `step_008` consumes | Frozen localization boundary value, zero variance, minimal odd group count, ties, and \(\delta\uparrow1/2\) pass | step-local | None |
| Unconditional PAC bound, sample ceilings, and precommitment | Final theorem closure and public Rate Specialization Bridge | Primitive protocol rules plus all earlier accepted interfaces | Direct assembly; source cost from checked localization theorem | Triangle and union bounds preserve claim type; geometric rate substitutions and \(A_k\gtrsim_k1\) preserve all exposed dependence | Same one-bit arbitrary-query model, fixed horizon, absolute norm, and joint sample/protocol probability as the goal | Error \(\leq\epsilon/2+\epsilon/4\); failure \(\leq\delta/4+\delta/2\); \(B_{\rm ref}\lesssim_kA_k\), \(G_\delta\lesssim\log(1/\delta)\), and localization confidence is absorbed by \(A_k\log(1/\delta)\) | Borel query certificates, pre-drawn seeds, all generated statistical interfaces, and exact block sizes export the complete theorem | The two estimator-to-target residuals are controlled at \(\epsilon/2\) and \(\epsilon/4\); no residual is omitted or transferred in a different norm | Two finite error terms, two failure events, and two block costs are accumulated; every ceiling has scale slack | All statistical outputs precede `step_008`; query legality follows directly from pre-drawn seeds and `step_002` | `step_001`-`step_007` produce; `step_008` exports the final theorem | First query precommitted, source trivial branch, localization failure, \(\lambda=\sigma\), \(k=2\), and all ceilings pass | step-local | None |
| Supported-in-\(J_0\) and point-mass baseline invariance | Exact recovery/reduction obligation | Ring nesting, alias separation, zero dither amplitude, and exact tail decomposition | Derived jointly by `step_002`, `step_003`, `step_005`, `step_006`, and `step_008` | The mechanism proves exact inactivity and zero residual, not merely a small upper bound | Produced level-zero statistic and consumed \(\mu-m_0\) use the same target and absolute metric | For \(x\in J_0\), \({\bf1}_{R_s}(x)=0\) for \(s\geq1\) and no retained alias contains \(x\); at \(x=m_0\), \(F=\Delta Y=W=0\) | Raw support inclusion and point-mass specialization export exact level-zero reduction and \(\widehat\mu=\mu\) on localization success | Higher-ring, tail, alias, and artificial remainder residuals are exactly zero | All baseline defect and forcing terms vanish identically, so there is no accumulated baseline loss | Geometry produces inactivity before mean, variance, tail, and final baseline uses | `step_002`/`step_003` produce; `step_005`-`step_008` preserve | First higher scale, ring endpoints, any finite \(S\), support throughout \(J_0\), and \(X=m_0\) pass | step-local | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is exact-goal aligned, structurally complete, source-preflighted, and ready for local proof work. The novel ring multiplexer survives the theorem-level obstruction tests: half-open geometry is exact for negative as well as positive indices, target and alias means and squares are treated separately, the public-coin averaging matches the stated probability mode, all accumulated second-moment forcing has a finite pointwise budget, the \(k=2\) accounting has one logarithm, and the sole target residual is explicitly dominated. Every theorem-critical hard obligation has a noncircular, source-adequate, downstream-compatible `step-local` witness, so no sketch or theorem-contract revision is required.
