# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Goal mode: exact-goal mode
- Run mode: interactive
- Reviewed roadmap: deterministic midpoint localization in the additive intercept coordinate; conditional-density disintegration using the averaged, possibly unbounded slice cap; random-intercept and fixed-width verification; exact heteroscedastic support and cap calculation; full affine-hull plus within-hull-volume exclusion from the cited affine-latent baseline; exact final assembly and bounded-joint-density baseline reduction.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch targets the exact theorem in `setting.md`. For fixed `d >= 1`, `R > 0`, compact `Theta`, and every law `mu` in the nonempty class `D`, it proves the stated bound simultaneously for every positive-length interval, then takes exactly the two suprema defining `C_D`. It separately preserves the `d >= 3`, `R > 0`, `q in (0,1)` quantifiers of the explicit witness. It neither claims a converse nor hides a part-(b) polynomial claim. This is correctly labeled `material_partial`, and the unresolved necessary direction and indexed polynomial characterization are stated.
- Dependency audit: The seven stable steps form an acyclic graph. `step_001` produces the measurable slab before `step_002` integrates it; `step_003` consumes the general theorem and produces the random-intercept certificate; `step_004` discharges the explicit witness interface before `step_005` and `step_006`; `step_007` consumes only earlier outputs. Each step states its claim, dependencies, stable assumption IDs, challenge, tool, output, rate objective, and pending review status.
- High-risk coverage: The sketch localizes all high-risk obligations: arbitrary interval endpoint conventions and event measurability; conditioning with random unbounded slice caps; conditional-density versions and null fibers; exact support of the heteroscedastic law; the singular cap integral; every affine latent dimension; within-affine-hull volume; and the joint-density recovery comparison. None is hidden in final assembly.
- Explicit-rate coverage: R1, R2, and R3 expose every relevant structural and distributional quantity, state that there are no hidden constants, fix the probability, horizon, and norm modes, and identify the exact factors `1`, `1/2`, and `1/[R(1-q)]`. The sketch explicitly disclaims uniform polynomial dependence in `(d,R)` and records the exact baseline specialization when `B=1`.
- Assumption and citation plausibility: `assump:averaged-intercept-density` and `assump:random-intercept-witness` are primitive coefficient-side conditions with stable IDs; all support, cap, and geometry facts are assigned to producer steps. The only theorem-critical external source is the opening affine generalization in Balcan--Dick--Pegden, *Semi-bandit Optimization in the Dispersed Setting*, arXiv:1904.09014, cached in `dispersionToolAppendix.tex`, lines 4-17 and 30-101. Its full-column-rank volume convention, latent Lebesgue density, induced within-image density, and non-forced-root boundary are identified accurately.

## Early Obstruction Audit

- Limiting-case stress: For `d=1`, the higher tuple is a point and `L=1`, so the same one-dimensional density argument applies. Open, closed, and half-open intervals all contain their midpoint; an admitted root and the midpoint are joined by a segment contained in the interval. Tangent and multiple roots cause no failure because only an upper derivative envelope is used. For the witness, `Z=0` is the only zero-width fiber and has probability zero; `q=1` is correctly excluded because the cap integral diverges, while every `q in (0,1)` has finite exact integral.
- Theorem-critical bridge support: Writing `c_I(beta)=m_I^d+sum_{k=1}^{d-1} beta_k m_I^k`, `step_001` exports the measurable interval `J_I(beta)=[-c_I(beta)-L|I|/2,-c_I(beta)+L|I|/2]`. Root hitting implies `alpha_0 in J_I(beta)` by the mean-value theorem. `step_002` then bounds the actual conditional probability of this interval and integrates it. This is a complete primitive-to-target bridge, not a root-statistic assumption.
- Exported-interface feasibility: Every exported interface has raw controls, an exact conclusion, a defect split, a dominance relation, a threshold or margin source where relevant, and named consumers. In particular, the general interface is `Pr_mu(root in I) <= L|I| integral K_mu d pi_mu`; the random-intercept interface is the actual conditional law with cap `1/(2 rho)`; and the witness interface is the actual singleton averaged cap `1/[R(1-q)]`. No transformed or surrogate object is substituted for a theorem target.
- Theorem-critical mechanism witness gate: All seven witnesses name the claim class, theorem role, source, source-to-claim relation, controlling term, opposing terms, closure relation, boundary regime, and producer-consumer path. The key sources match their claims: an upper derivative envelope supports an upper slab width; an averaged conditional density supports probability mass; reciprocal-width integrability supports the random-intercept cap; `q < 1` supports the singular integral; and affine dimension plus within-hull absolute continuity supports affine-baseline exclusion.
- Entry-state trace stress: No recursive algorithm, trajectory, initialization, contraction, or all-time invariant appears, so no dynamic first update exists. The analogous static boundary traces are explicit: `d=1`; endpoint-near and multiple roots; `rho=0`; `Z=0`; `q` approaching `0` or `1`; minimal witness degree `d=3`; and all possible latent dimensions. In each consumed almost-everywhere interface the mechanism is active, or the exceptional state is null or explicitly outside the theorem range.
- Obligation locality classification: Midpoint/event measurability (`step_001`), unbounded-cap disintegration (`step_002`), random-intercept density (`step_003`), witness support and conditioning (`step_004`), singular cap integration (`step_005`), affine-latent exclusion (`step_006`), and baseline/final assembly (`step_007`) are all `step-local`. Each already has a primitive, earlier-derived, direct, standard-tool, or source-audited mechanism under the unchanged theorem contract.
- Noncircular closure gate: The density envelope is defined entirely from the coefficient law before roots are generated. Support and conditional-law facts are produced from the explicit latent variables before their cap or geometry is consumed. The affine comparison source is independent of the new theorem. No all-law or all-interval conclusion is reused as an assumption; the class and interval suprema occur only after the per-law, per-interval bound.
- Mechanism-source and boundary stress: The root event is Borel: every real interval is a countable union of compact subintervals, and for each compact piece the event is the zero set of the continuous map `alpha -> min_theta |phi_alpha(theta)|`. The slab indicator is jointly measurable because its center is polynomial in `beta`. These facts close the event and conditional-integration interfaces without measurable root selection.
- Generated-output flow: Every derived output has a legal producer before use. The paths are basic setting to `step_001` to `step_002`; primitive random-intercept data plus `step_002` to `step_003`; explicit law plus `step_003` to `step_004`; `step_004` plus `step_002` to `step_005`; `step_004` plus the cached source to `step_006`; and all completed outputs to `step_007` and final assembly.
- Source-to-claim adequacy: Mean value, disintegration/Tonelli, conditional affine change of variables, monotone convergence, and Lipschitz-image null-volume are each invoked in the same coordinates, measure convention, and target norm as the branch. For the cited affine theorem, the cached proof uses `sqrt(det(A^T A)) > 0`, identifies a `k`-dimensional affine image, and gives the pushforward a `k`-dimensional density. The sketch uses only this input-class convention for nonmembership, not the theorem's root bound or a converse. The source's additional no-forced-root condition is a known non-output boundary and is irrelevant once affine-law representability has already failed.
- Residual-to-target adequacy: The midpoint residual is exactly `phi(m_I)-phi(theta_*)` and is at most `L|I|/2`; the density bridge leaves only the exact interval length `L|I|`; the random-intercept conditional object is the actual intercept law; `beta_q` identifies `Z` through its first coordinate; the cap cutoff remainder is proportional to a positive power `delta^(1-q)` and vanishes; and the affine comparison is equality-in-law, dimension, and measure based. There is no uncontrolled empirical, event-level, persistent, fixed, or wrong-scale residual.
- Baseline invariance audit: Under the additional bounded joint density premise, with marginal density `g`, the current-notation identity `f=h/g` on `{g>0}` gives `gK <= kappa_joint`; integration over `[-R,R]^(d-1)` yields `bar_kappa <= kappa_joint(2R)^(d-1)`. When `B=1`, the derivative envelope gives exactly `d+R d(d-1)/2`, recovering the stated monic ambient-volume scale. The affine-latent theorem remains unchanged on its own class; the witness is proved outside that class rather than treated as a failed baseline case.
- Scope-accumulation compatibility: There is no recursive or all-time accumulation. In `step_002`, the nonnegative slice charge `L|I|K_mu(beta)` is accumulated once and has the primitive finite budget `integral K_mu d pi_mu`. In `step_005`, the only limiting accumulation is `integral_0^1 t^{-q}dt`; its sign is nonnegative, its primitive is `t^(1-q)/(1-q)`, and the central cutoff contribution vanishes for `q<1`. Suprema introduce no additive defect.
- Scope and dependence consistency: The general claim is simultaneous over all `mu in D` and all positive-length `I subseteq Theta` for the fixed setting. The class supremum is taken only after the per-law integral; no essential supremum over `beta` is inserted. The witness is asserted exactly for `d>=3`, `R>0`, and `q in (0,1)`. Dependence on `Theta` remains explicit through `B` and `L`.
- Generated-condition provenance: Every nonprimitive static fact, including the slab, Borel event, conditional law, support, positivity, cap, cap integral, affine hull, zero within-hull volume, and baseline reduction, is produced by a named earlier step. No good event, stability condition, support membership, or boundedness fact is smuggled into theorem-facing assumptions.
- Citation and tool applicability: The cached primary source statement and proof provide the exact affine-map, latent-density, rank/volume, and induced-density conventions needed by `step_006`. The branch maps the latent image to the full coefficient vector with the fixed monic coordinate, which preserves affine dimension. The source does not claim a representation converse, and the sketch does not use one. Standard tools are assigned to direct current-notation proofs with their branch and boundary conditions exposed.
- Same-setting repair plausibility: No material obstruction requiring repair was found. The remaining work consists of local derivations under already fixed interfaces and sources.
- Target-preserving bridge-repair gate: No missing bridge or target-changing loss was found. The exact all-law/all-interval conclusion, constructive clauses, and baseline recovery are all preserved.
- High-risk obligation classes: Structural support and dimension, unbounded conditional caps, null conditional fibers, a singular improper integral, event measurability, conditional-to-unconditional mode transfer, class/interval uniformity, explicit dependence, baseline specialization, and citation conventions were all tested and passed at obstruction-level granularity.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Midpoint slab and root-event measurability (`step_001`) | Deterministic localization; converts a continuum root event to an intercept interval | Monicity, additive `alpha_0`, compact `Theta`, finite derivative envelope, mean-value theorem, compact exhaustion | Primitive setup plus direct current-notation derivation | A root `theta_*` gives `abs(phi(m_I)) <= L len(I)/2`; compact-piece zero events are closed and their union is Borel | Exact branch polynomial, ordinary absolute derivative, and the same interval convention; no external object | `{root in I} subset {abs(alpha_0+c_I(beta)) <= L len(I)/2}` | Raw derivative bound and polynomial center export measurable `J_I(beta)` of length `L len(I)` to `step_002` | Exact evaluation difference is bounded at the consumed slab scale; no leftover term | One static segment; endpoint and multiplicity defects add nothing | Basic setup -> `step_001` before any probability bound | Producer `step_001`; consumers `step_002`, transitively `step_007` and final theorem | `d=1`, every endpoint type, endpoint-near, tangent, and multiple roots pass | `step-local` | None |
| Averaged-cap disintegration and R1 (`step_002`) | Conditional-to-unconditional probability bound and class-uniform theorem closure | `assump:averaged-intercept-density`, `step_001`, Tonelli/tower property | Primitive stable-ID assumption plus earlier derived slab and standard measure theory | A density essential supremum bounds mass of the exact random interval; integrability, not a pointwise slice cap, closes the law | Conditional density, Lebesgue essential supremum, and `pi_mu` are exactly those in `setting.md` | `Pr(root in I) <= L len(I) integral K_mu d pi_mu <= L len(I) bar_kappa_D` | Jointly measurable `f`, measurable `K` and slab, finite averaged budget; exports R1 to all later consumers | Actual-law disintegration at exact width; null-set infinities contribute zero and no surrogate remains | Nonnegative charge is integrated once; budget `integral K d pi <= bar_kappa`; no iterative forcing | Coefficient-side assumption + `step_001` -> `step_002`; root bound is not assumed | Producer `step_002`; consumers `step_003`, `step_005`, `step_007`, final theorem | `K` unbounded and null-set infinite, singular/discrete `pi`, and `d=1` pass; positive-mass nonintegrability is excluded by the primitive | `step-local` | None |
| Random-intercept and fixed-width certificate (`step_003`) | Constructive density, cap, support, and averaged-envelope interface | Independent uniform `U`, measurable `G,rho`, a.s. `rho>0`, support inequality, reciprocal-width budget | `assump:random-intercept-witness` plus direct conditional change of variables | Conditional affine uniform law gives exactly `f=1{abs(a_0-G)<=rho}/(2rho)` and `K=1/(2rho)` | Same conditioned variable `beta`, actual intercept, Lebesgue density, and cube support as the theorem | `bar_kappa_RI=(1/2)sup_mu integral rho^-1 d pi <= M_RI/2`; fixed width gives `1/(2r_mu)` | Raw independence, positivity, and reciprocal budget export the exact R1 input and fixed-width specialization | Produced law is the consumed actual conditional law; no transformed object or residual | Small widths are charged by the one-shot reciprocal integral; singular/dependent `beta` creates no additional term | Primitive RI structure -> `step_003`, with R1 already produced by `step_002` | Producer `step_003`; consumers `step_004`, `step_007`, final theorem | `rho=0` on positive mass would fail, but a.s. positivity and finite reciprocal budget exclude it; null versions are harmless | `step-local` | None |
| Exact heteroscedastic support and conditional object (`step_004`) | Support, a.s. positivity, and same-target conditioning certificate | Explicit uniform `Z,U`, continuous `rho_q`, injective first higher coordinate | Direct primitive-source derivation plus the current-notation uniform calculation | Coordinate bounds give cube support; continuous image of the full-support compact latent rectangle gives exact sheet support; `beta_1=Z` fixes the conditional fiber | Actual coefficient ordering and actual `beta`; no inverse, population, or metric mismatch | `rho_q(z)=0 iff z=0`; for `z!=0`, `K(beta_q(z))=1/[R(2 abs(z)/R)^q]` | Explicit `R,q,Z,U,beta_q` export support, positivity, and cap to `step_005`/`step_006` | Conditioning on `beta_q(Z)` equals conditioning on `Z`; zero residual | Only collapsed fiber is `Z=0`, of probability zero; support includes it without using it in an a.e. density claim | Explicit law -> `step_004` before quantitative or geometry consumers | Producer `step_004`; consumers `step_005`, `step_006`, `step_007`, final theorem | `Z=0`, `abs(Z)=R/2`, `d=3`, all `R>0`, and all `q in (0,1)` pass | `step-local` | None |
| Infinite pointwise cap, finite exact mean, singleton bound (`step_005`) | Structural separation and exact quantitative witness | `step_004`, uniform density of `Z`, `q<1`, R1 | Earlier derived actual cap plus direct integration and monotone convergence | Positive-mass neighborhoods prove essential unboundedness; the power integral supplies exactly the averaged R1 input | Same `Z` normalization, conditional cap, and singleton-law convention as the branch | `E K=(1/R) integral_0^1 t^-q dt=1/[R(1-q)]`; hence `C_{mu_q}<=L/[R(1-q)]` | Raw cap formula and uniform `Z` export exact average and root bound | Cutoff remainder is a constant multiple of `delta^(1-q)` and vanishes at the required target scale | Nonnegative singular forcing; explicit primitive gives finite total mass for `q<1`; no sign issue | `step_004` -> direct integral -> R1 from `step_002` -> `step_005` | Producer `step_005`; consumers `step_007`, final theorem | Every finite cap level is exceeded on positive mass; `q=1` divergence and `q=0` loss of unboundedness are correctly outside the stated open range | `step-local` | None |
| Affine-hull and within-hull-volume exclusion (`step_006`) | Structural support/nonmembership claim needed for materiality comparison | Exact sheet, projected noncollinearity, intercept direction, Lipschitz-image null volume; cached affine source rank and density convention | `step_004` plus Balcan--Dick--Pegden arXiv:1904.09014 Appendix opening theorem/proof, cached lines 4-17 and 30-101 | The sheet has affine hull dimension `3` but zero `H^3`; a full-rank affine pushforward of a latent Lebesgue density has matching affine dimension and is absolutely continuous there | Fixed monic coordinate preserves dimension; source uses full-column-rank `A` via `det(A^T A)>0` and induced `k`-density; only source input-class conventions are consumed | `dim aff(supp mu_q)=3`, `H^3(supp mu_q)=0`; equality in law forces `k=3` and then contradicts within-image absolute continuity | Raw support points and fibers plus source rank/density export exact nonmembership; source no-forced-root condition is an additional boundary, not needed for this prior representability failure | Equality-in-law target, exact support, dimension, and measure; no approximation or metric residual | Static global geometry; curvature controls `k=2`, zero `3`-volume controls `k=3`, affine-hull mismatch controls every other `k` | `step_004` + independent cached source -> `step_006` before comparison assembly | Producer `step_006`; consumer `step_007` and final comparison | Minimal `d=3`, collapsed `z=0` fiber, and every latent dimension pass; without full rank/latent absolute continuity the claim would not be made | `step-local` | None |
| Bounded-joint-density baseline invariance (`step_007`) | Recovery/specialization obligation and source comparison | Additional comparison premise `h<=kappa_joint`, marginalization, conditional density ratio, exact derivative sum | Direct current-notation derivation; source baseline stated in `RESEARCH_BRIEF.md` | `gK<=kappa_joint` is the right conditional-cap implication; at `B=1`, the derivative formula is exactly the source monic bracket | Same lower-coefficient cube, Lebesgue density, monic ordering, law/interval quantifiers, and normalization | `bar_kappa<=kappa_joint(2R)^(d-1)` and `L<=d+R d(d-1)/2` | Raw joint cap exports the exact known ambient-volume scale only in the comparison clause | No residual; integration domain volume and finite sum are exact | One static marginal integral; no persistent defect or mode conversion | Optional baseline premise -> `step_007`; it is not inserted into the new theorem assumptions | Producer `step_007`; consumer final comparison | `d=1` gives `(2R)^0`; `B=1` recovers the stated scale; affine baseline remains untouched on its class | `step-local` | None |
| Exact final theorem and scope closure (`step_007`) | Quantifier-preserving theorem assembly and material-partial source alignment | Completed outputs `step_002` through `step_006` and explicit limitation clauses | Earlier derived outputs only | Every clause of `setting.md` has a producer; no converse, necessity, or indexed polynomial statement is inferred | Same objects, scopes, probability mode, interval normalization, and explicit dependence throughout | Direct conjunction and substitution; both suprema are taken only after the per-law/all-interval inequality | R1, R2, R3, support, affine exclusion, and baseline reduction are all available before export | All objects are actual theorem targets; no uncontrolled or wrong-scale final remainder | Each output is consumed once in a finite DAG; no recursive accumulation | Earlier producers -> `step_007` -> final theorem; no assembly-produced premise | Producer `step_007`; consumer final theorem and comparison prose | General `d=1`, witness `d=3`, all endpoint types, singular `beta`, and unbounded slices have already passed producer-level stress | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is exact-goal aligned, structurally complete, source-faithful, and ready for local proof work. Every theorem-critical obligation has a noncircular and source-adequate mechanism under the unchanged setting, an exact raw-control-to-exported-interface path, target-scale residual control, legal generated-output flow, and a `step-local` verdict. The requested boundary, measurability, singular-integration, affine-source, baseline, and quantifier audits reveal no theorem-level obstruction or missing sketch interface.
