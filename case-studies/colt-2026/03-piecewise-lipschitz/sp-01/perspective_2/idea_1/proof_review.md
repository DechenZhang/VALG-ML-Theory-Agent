# Proof Review

## Formalized Setting

- Basic setup:
  Fix the source parameter domain \(\Theta\subseteq\mathbb R\), and let
  \[
  \mathcal I(\Theta)
  :=\{I\subseteq\Theta:I\text{ is an interval and }0<|I|<\infty\},
  \]
  where \(|I|\) is Lebesgue length and arbitrary endpoint conventions are
  allowed. Fix once and for all
  \(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\), with both
  entries finite. For an integer \(d\geq1\), a radius \(R\geq1\), and
  \(\alpha=(\alpha_0,\ldots,\alpha_{d-1})\in[-R,R]^d\), define the monic
  polynomial
  \[
  \phi_\alpha(\theta)
  :=\theta^d+\sum_{j=0}^{d-1}\alpha_j\theta^j
  \]
  and the interval root-hitting event
  \[
  H_{d,I}:=\{\alpha\in[-R,R]^d:\text{ there exists }\theta\in I
  \text{ such that }\phi_\alpha(\theta)=0\}.
  \]

  For a Borel probability law \(\mu\) supported on \([-R,R]^d\), whenever
  the indicated regular conditional laws have Lebesgue densities, write
  \[
  \begin{aligned}
  K_0^\mu
  &:=\left\|f^\mu_{\alpha_0\mid\alpha_{1:d-1}}
      (\,\cdot\mid\alpha_{1:d-1})\right\|_{L^\infty(\mathbb R)},\\
  K_\infty^\mu
  &:=\left\|f^\mu_{\alpha_{d-1}\mid\alpha_{0:d-2}}
      (\,\cdot\mid\alpha_{0:d-2})\right\|_{L^\infty(\mathbb R)}.
  \end{aligned}
  \]
  These are random essential-supremum slice caps, evaluated at the random
  conditioning coordinates. For \(d=1\), both conditioning tuples are empty
  and both quantities mean the \(L^\infty\)-norm of the density of the sole
  coefficient \(\alpha_0\).

  Let \(\mathcal P(\mathbb R^d)\) denote the Borel probability laws on
  \(\mathbb R^d\). Define, exactly and without additional membership conditions,
  \[
  \begin{aligned}
  \mathcal D_{d,R,\eta}:=\{\mu\in\mathcal P(\mathbb R^d):\;&
  \mu([-R,R]^d)=1;\\
  &\mu(\alpha_0\in\cdot\mid\alpha_{1:d-1})
    \text{ and }
    \mu(\alpha_{d-1}\in\cdot\mid\alpha_{0:d-2})
    \text{ admit Lebesgue densities a.s.};\\
  &\mathbb E_\mu K_0^\mu\leq\bar\kappa_0,
    \quad
    \mathbb E_\mu K_\infty^\mu\leq\bar\kappa_\infty\}.
  \end{aligned}
  \]
  Thus membership imposes both mean endpoint conditional-density caps, but
  no almost-sure uniform slice cap, ambient joint density, independence,
  affine-latent representation, root event, root intensity, interval
  statistic, or preassigned value of \(C_{\mathcal D}\).

  The class-uniform root-hitting constant is
  \[
  C_{\mathcal D_{d,R,\eta}}
  :=\sup_{\mu\in\mathcal D_{d,R,\eta}}
    \sup_{I\in\mathcal I(\Theta)}
    \frac{\mu(H_{d,I})}{|I|},
  \]
  with value \(0\) when either indexing set of the displayed suprema is empty.

  No learner or online algorithm is specialized. The analytic protocol is the
  following deterministic two-chart construction: split each
  \(I\in\mathcal I(\Theta)\) into the disjoint pieces
  \[
  I_0:=I\cap[-1,1],\qquad
  I_+:=I\cap(1,\infty),\qquad
  I_-:=I\cap(-\infty,-1),
  \]
  so \(|I_0|+|I_+|+|I_-|=|I|\). The coefficient sweeps are
  \[
  s_0(\theta;\alpha_{1:d-1})
  :=-\theta^d-\sum_{j=1}^{d-1}\alpha_j\theta^j
  \quad (|\theta|\leq1)
  \]
  and, after dividing by \(\theta^{d-1}\),
  \[
  s_\infty(\theta;\alpha_{0:d-2})
  :=-\theta-\sum_{j=0}^{d-2}\alpha_j\theta^{j-d+1}
  \quad (|\theta|\geq1).
  \]
  Empty sums are zero. A root in the first chart is equivalent to
  \(\alpha_0=s_0(\theta;\alpha_{1:d-1})\); a root in either outer chart is
  equivalent to
  \(\alpha_{d-1}=s_\infty(\theta;\alpha_{0:d-2})\). Define
  \[
  \begin{aligned}
  B_0(d,R)&:=d+\frac{R d(d-1)}2,\\
  B_\infty(d,R)&:=1+\frac{R d(d-1)}2,\\
  M_\eta(d,R)&:=\max\{\bar\kappa_0 B_0(d,R),
                            \bar\kappa_\infty B_\infty(d,R)\},\\
  \bar\kappa_*&:=\max\{\bar\kappa_0,\bar\kappa_\infty\},\\
  P_\eta(d,R)&:=\bar\kappa_*d+\frac{\bar\kappa_*}{2}R d^2.
  \end{aligned}
  \]
  The deterministic bridge to the probability bound is the pair of
  image-length estimates generated by
  \[
  \sup_{|\theta|\leq1}|\partial_\theta s_0(\theta;\alpha_{1:d-1})|
  \leq B_0(d,R),
  \qquad
  \sup_{|\theta|\geq1}|\partial_\theta s_\infty(\theta;\alpha_{0:d-2})|
  \leq B_\infty(d,R).
  \]

  For the nonemptiness assertion only, define a witness law
  \(\mu^{\mathrm{wit}}_{d,R}\) as follows. If \(d=1\), take
  \(\alpha_0\sim\mathrm{Unif}[-R,R]\). If \(d=2\), take
  \(\alpha_0,\alpha_1\) independently uniform on \([-R,R]\). If \(d\geq3\),
  take \(U_0,U_\infty\sim\mathrm{Unif}[-R,R]\) and a Rademacher variable
  \(S\), all mutually independent, and set
  \[
  \alpha_0=U_0,\qquad \alpha_{d-1}=U_\infty,
  \qquad \alpha_j=RS\quad(1\leq j\leq d-2).
  \]
  Every witness is supported on \([-R,R]^d\). For \(d\geq3\) the witness is
  ambiently singular, and for \(d\geq4\) its middle coordinates are dependent.

- Technical assumptions:
  - `assump:compact-parameter-domain` (Compact source parameter domain):
    \(\Theta\subseteq\mathbb R\) is the fixed compact parameter domain from
    the source problem. The theorem is uniform over every
    \(I\in\mathcal I(\Theta)\), and its constant has no dependence on
    \(I\) or \(\Theta\).
  - `assump:indexed-regime` (Indexed degree, radius, and fixed cap parameters):
    \(d\in\mathbb N\) with \(d\geq1\), \(R\geq1\), and
    \(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\) is finite and
    fixed independently of \(d\) and \(R\). No lower bound such as
    \(\bar\kappa_0,\bar\kappa_\infty\geq1/2\) is assumed for the root-hitting
    bound; that threshold is used only in the separate nonemptiness clause.
  - `assump:compact-cube-support` (Compact coefficient support and monicity):
    Each theorem-quantified law \(\mu\) satisfies
    \(\mu([-R,R]^d)=1\), and the associated polynomial is exactly the monic
    \(\phi_\alpha\) defined above. This is only a support condition and does
    not impose an ambient density.
  - `assump:mean-endpoint-conditional-caps` (Two mean conditional-density
    caps): The two endpoint regular conditional laws admit Lebesgue densities
    almost surely, and their random essential-supremum norms satisfy exactly
    \[
    \mathbb E_\mu K_0^\mu\leq\bar\kappa_0,
    \qquad
    \mathbb E_\mu K_\infty^\mu\leq\bar\kappa_\infty.
    \]
    These are mean bounds only. Together with
    `assump:compact-cube-support`, they are exhaustive of the law-membership
    requirements defining \(\mathcal D_{d,R,\eta}\).

## Formalized Goal

- Goal statement:
  For the single fixed \(\eta\) in `assump:indexed-regime`, prove that for
  every integer \(d\geq1\), every \(R\geq1\), every
  \(\mu\in\mathcal D_{d,R,\eta}\), and every positive-length interval
  \(I\in\mathcal I(\Theta)\),
  \[
  \begin{aligned}
  \mu(H_{d,I})
  &\leq
  \bar\kappa_0 B_0(d,R)|I_0|
  +\bar\kappa_\infty B_\infty(d,R)(|I_+|+|I_-|)\\
  &\leq M_\eta(d,R)|I|.
  \end{aligned}
  \]
  Consequently, simultaneously over the entire law class and all such
  intervals,
  \[
  \boxed{
  C_{\mathcal D_{d,R,\eta}}
  \leq
  \max\!\left\{
  \bar\kappa_0\!\left(d+\frac{R d(d-1)}2\right),
  \bar\kappa_\infty\!\left(1+\frac{R d(d-1)}2\right)
  \right\}
  \leq
  P_\eta(d,R)
  =\bar\kappa_*d+\frac{\bar\kappa_*}{2}R d^2.}
  \]
  The middle quantity is the exact two-chart maximum, not the sum of the
  chart constants; \(P_\eta\) is the explicitly displayed degree-three
  polynomial whose degree and coefficients are uniform in \((d,R)\), whose
  coefficients depend only on the fixed \(\eta\), and which has no hidden
  constants. In the same theorem, but only under the additional condition
  \(\bar\kappa_0,\bar\kappa_\infty\geq1/2\), prove that for every claimed
  \((d,R)\), the law \(\mu^{\mathrm{wit}}_{d,R}\) belongs to
  \(\mathcal D_{d,R,\eta}\): both of its mean slice caps equal
  \(1/(2R)\leq1/2\). Thus the indexed class is nonempty throughout the
  claimed regime under that threshold, without making the threshold an
  assumption of the bound.

## Reviewed Roadmap

The accepted sketch has seven required steps. Step 001 proves the inner sweep
derivative and image-length bounds. Step 002 proves the positive and negative
outer pivot certificates. Step 003 constructs measurable, version-independent
conditional caps, proves Borel section identities, and establishes matching
disintegration. Step 004 integrates the three chart bounds. Step 005 proves the
three-piece event decomposition, weighted union bound, and exact maximum.
Step 006 passes to the class supremum and proves the fixed-\(\eta\) polynomial
specialization. Step 007 computes the witness kernels in all degree regimes,
then proves support, singularity, middle-coordinate dependence, the isolated
cap threshold, and membership. The accepted local-unit counts are
\((2,2,3,3,3,4,6)\), totaling 23, and all seven target-step assemblies feed the
final theorem. This aggregate review was performed under reviewer handle
`cli:p2-review-aggregate:a6-20260729T152234`.

## Attempted Claim

Fix one finite
\(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\), independently of
\((d,R)\). For every \(d\geq1\), \(R\geq1\),
\(\mu\in\mathcal D_{d,R,\eta}\), and \(I\in\mathcal I(\Theta)\), the assembled
bundle proves
\[
\mu(H_{d,I})
\leq \bar\kappa_0B_0(d,R)|I_0|
 +\bar\kappa_\infty B_\infty(d,R)(|I_+|+|I_-|)
\leq M_\eta(d,R)|I|.
\]
Consequently, with the stated zero convention for empty supremum index sets,
\[
C_{\mathcal D_{d,R,\eta}}
\leq M_\eta(d,R)
\leq P_\eta(d,R)
=\bar\kappa_*d+\frac{\bar\kappa_*}{2}Rd^2,
\qquad
\bar\kappa_*:=\max\{\bar\kappa_0,\bar\kappa_\infty\}.
\]
Here
\[
M_\eta(d,R)
=\max\left\{
\bar\kappa_0\left(d+\frac{Rd(d-1)}2\right),
\bar\kappa_\infty\left(1+\frac{Rd(d-1)}2\right)
\right\},
\]
so the exact middle bound is the maximum of the two chart coefficients, not
their sum.

Separately, only when
\(\bar\kappa_0,\bar\kappa_\infty\geq1/2\), the bundle proves for every
\(d\geq1\) and \(R\geq1\) that
\[
\mu^{\mathrm{wit}}_{d,R}\in\mathcal D_{d,R,\eta},
\qquad
K_0^{\mu^{\mathrm{wit}}_{d,R}}
=K_\infty^{\mu^{\mathrm{wit}}_{d,R}}
=\frac1{2R}\quad\text{almost surely}.
\]
The result retains the `material_partial` scope: it gives a sufficient
condition for \(R\geq1\), without claiming necessity, treating laws lacking
either endpoint mean cap, or treating \(0<R<1\).

## Review Mode

split

## Review Verdict

- Score: 10
- Failure Type: ACCEPTED

## Audit Summary

- Goal coverage: PASS. The bundle proves the exact fixed-\(\eta\), all-\(d\),
  all-\(R\geq1\), all-law, all-positive-length-interval claim, including the
  weighted chart bound, exact maximum, class supremum, explicit polynomial,
  empty-index convention, and separately thresholded witness membership. It
  is correctly labeled `material_partial` and does not claim to solve the full
  source open problem.
- Roadmap or step coverage: PASS. All seven accepted steps and their
  \((2,2,3,3,3,4,6)\) local-unit counts are present. The four specialized
  reviews are contract-valid PASS diagnostics with no blocking or nonblocking
  concerns, no suggested repair depth, and no candidate repair target; there
  is no reviewer disagreement.
- Proof-details audit: PASS. The deterministic inner and signed outer sweep
  bounds, measurable conditional-cap construction, joint-law and fiber
  identification, Borel-section arguments, both disintegrations, measurable
  majorant integration, exact event union, exact scalar maximum, supremum
  closure, polynomial specialization, and all witness degree cases are
  derivationally complete.
- Adversarial stress test: PASS. The proof survives \(d=1,2,3,4\), \(R=1\),
  zero caps, empty sums, either empty supremum index set, arbitrary endpoint
  conventions, conditioning values off support, alternative regular
  conditional-law and density versions, singular witness laws, and dependent
  middle coordinates. No lower \(1/2\) cap threshold leaks into the general
  bound.
- LaTeX export structure/provenance gate: PASS. The five required content
  files, style, bibliography style, and database are present in canonical
  order. The four stable setting assumptions are numbered in Theoretical
  Setup; Preliminaries, Main Theorem, Proof Sketch, all seven step
  subsections, and the final proof subsection are present. No workflow
  provenance or unreviewed mathematical material appears in the public TeX.
- Main theorem self-containedness: PASS. Its notation is defined in the setup
  and preliminaries, its assumption basis is explicit, and its statement has
  no appendix-local or proof-step reference. The separate witness condition
  and the deterministic probability, horizon, and norm modes are readable
  from the statement itself.
- Assumption provenance gate: PASS. Compactness, indexed regime, cube support,
  monicity, and mean endpoint conditional caps retain their setting
  provenance. Measurability, support, kernel versions, witness membership,
  singularity, and dependence are proved rather than silently assumed.
- Redundant public prose gate: PASS. No audit scaffolding, copied workflow
  fields, placeholders, mechanical translation debris, or forbidden public
  claim, hypothesis, or invariant dependency remains.
- Proof-step notation surface gate: PASS. Public quantities are exactly the
  setting-derived chart pieces, sweeps, chart coefficients, maximum,
  polynomial, class constant, and witness. Kernel representatives,
  generators, support sets, and integration helpers remain proof-local or
  appendix-local and do not leak into the main theorem.
- Theorem readability and notation economy: PASS. The main theorem exposes
  the sharp weighted estimate, exact maximum, explicit polynomial, and witness
  clause without a technical admissibility dictionary; the short fixed-cap
  corollary gives the reader-facing rate.
- Explicit-rate contract: PASS. The public statement exposes \(d,R,\eta\) and
  interval lengths, declares no hidden constants, fixes \(\eta\) and
  \(\Theta\), and states deterministic probability mode, static horizon mode,
  conditional-\(L^\infty\) norm mode, Lebesgue metric, admissibility, and the
  absence of auxiliary tolerances or probability conversion.
- Rate Specialization Bridge: PASS. The named appendix proposition explicitly
  sets \(\bar\kappa_*=\max\{\bar\kappa_0,\bar\kappa_\infty\}\), checks
  nonnegativity and \(d,R\geq1\), displays \(1\leq d\) and
  \(d(d-1)\leq d^2\), dominates both chart coefficients term by term, then
  applies maximum monotonicity. It states the final coefficients and confirms
  that no probability, horizon, or norm conversion is hidden.
- Baseline-reduction check: PASS. No inherited theorem-facing recovery claim
  is weakened. At the relevant \(d=1\) boundary the exact public maximum is
  \(M_\eta(1,R)=\bar\kappa_*\); the later polynomial bound is openly
  conservative and does not replace the exact result.
- LaTeX appendix full-derivation gate: PASS. The comparison was exhaustive,
  not sampled: every one of the 23 accepted local units, every invoked
  cited-result application, and all seven target-step assemblies was compared
  source-to-appendix. All definitions, equations, inequalities, version
  arguments, assumption discharges, boundary cases, and assembly implications
  are preserved. The appendix has 24 named results because the Step 007
  assembly is packaged as the reusable witness-membership proposition.
- LaTeX export citation/reference gate: PASS. All 15 citation commands resolve
  to the three BibTeX entries `Folland1999`, `Kallenberg2002`, and
  `Munkres2000`; their applications and object mappings are valid. Labels
  are unique, all internal references resolve, and no malformed command
  fragment is present. A fresh isolated cached
  `<TECTONIC_BIN>` build ran BibTeX with `ims.bst`,
  stabilized all cross-references, and produced a 23-page PDF with no
  TeX/BibTeX error or unresolved citation/reference. The only diagnostics were
  nonblocking bundled-font substitutions.
- LaTeX export synchronization: PASS. `assembly_report.md` is COMPLETE with
  no blockers and matches the attempted claim. All 29 base inputs, the frozen
  manifest, and the four specialized reviews matched their frozen SHA-256
  identities before aggregation; the final immutability recheck also passed.

## Critical Issues

None

## Failure-Type Rationale

The formalized goal is proved under the exact authoritative assumptions and
quantifiers. All specialized diagnostics and every independent aggregate
contract gate pass, including exhaustive appendix preservation and a fresh
BibTeX-aware compile. The appropriate failure type is therefore `ACCEPTED`.
This accepts only the stated `material_partial` result and does not assert a
solution of the full source open problem.

## Suggested Next Action

None

## Retry Mode

none
