# Proof Review

## Formalized Setting

- Basic setup:
  Fix the source parameter domain \(\Theta\subseteq\mathbb R\), and let
  \[
  \mathcal I(\Theta)
  :=\{I\subseteq\Theta:I\text{ is an interval and }0<|I|<\infty\},
  \]
  where \(|I|\) is Lebesgue length and arbitrary endpoint conventions are
  allowed. Fix once and for all
  \(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\), with both
  entries finite. For an integer \(d\geq1\), a radius \(R\geq1\), and
  \(\alpha=(\alpha_0,\ldots,\alpha_{d-1})\in[-R,R]^d\), define the monic
  polynomial
  \[
  \phi_\alpha(\theta)
  :=\theta^d+\sum_{j=0}^{d-1}\alpha_j\theta^j
  \]
  and the interval root-hitting event
  \[
  H_{d,I}:=\{\alpha\in[-R,R]^d:\text{ there exists }\theta\in I
  \text{ such that }\phi_\alpha(\theta)=0\}.
  \]

  For a Borel probability law \(\mu\) supported on \([-R,R]^d\), whenever
  the indicated regular conditional laws have Lebesgue densities, write
  \[
  \begin{aligned}
  K_0^\mu
  &:=\left\|f^\mu_{\alpha_0\mid\alpha_{1:d-1}}
      (\,\cdot\mid\alpha_{1:d-1})\right\|_{L^\infty(\mathbb R)},\\
  K_\infty^\mu
  &:=\left\|f^\mu_{\alpha_{d-1}\mid\alpha_{0:d-2}}
      (\,\cdot\mid\alpha_{0:d-2})\right\|_{L^\infty(\mathbb R)}.
  \end{aligned}
  \]
  These are random essential-supremum slice caps, evaluated at the random
  conditioning coordinates. For \(d=1\), both conditioning tuples are empty
  and both quantities mean the \(L^\infty\)-norm of the density of the sole
  coefficient \(\alpha_0\).

  Let \(\mathcal P(\mathbb R^d)\) denote the Borel probability laws on
  \(\mathbb R^d\). Define, exactly and without additional membership conditions,
  \[
  \begin{aligned}
  \mathcal D_{d,R,\eta}:=\{\mu\in\mathcal P(\mathbb R^d):\;&
  \mu([-R,R]^d)=1;\\
  &\mu(\alpha_0\in\cdot\mid\alpha_{1:d-1})
    \text{ and }
    \mu(\alpha_{d-1}\in\cdot\mid\alpha_{0:d-2})
    \text{ admit Lebesgue densities a.s.};\\
  &\mathbb E_\mu K_0^\mu\leq\bar\kappa_0,
    \quad
    \mathbb E_\mu K_\infty^\mu\leq\bar\kappa_\infty\}.
  \end{aligned}
  \]
  Thus membership imposes both mean endpoint conditional-density caps, but
  no almost-sure uniform slice cap, ambient joint density, independence,
  affine-latent representation, root event, root intensity, interval
  statistic, or preassigned value of \(C_{\mathcal D}\).

  The class-uniform root-hitting constant is
  \[
  C_{\mathcal D_{d,R,\eta}}
  :=\sup_{\mu\in\mathcal D_{d,R,\eta}}
    \sup_{I\in\mathcal I(\Theta)}
    \frac{\mu(H_{d,I})}{|I|},
  \]
  with value \(0\) when either indexing set of the displayed suprema is empty.

  No learner or online algorithm is specialized. The analytic protocol is the
  following deterministic two-chart construction: split each
  \(I\in\mathcal I(\Theta)\) into the disjoint pieces
  \[
  I_0:=I\cap[-1,1],\qquad
  I_+:=I\cap(1,\infty),\qquad
  I_-:=I\cap(-\infty,-1),
  \]
  so \(|I_0|+|I_+|+|I_-|=|I|\). The coefficient sweeps are
  \[
  s_0(\theta;\alpha_{1:d-1})
  :=-\theta^d-\sum_{j=1}^{d-1}\alpha_j\theta^j
  \quad (|\theta|\leq1)
  \]
  and, after dividing by \(\theta^{d-1}\),
  \[
  s_\infty(\theta;\alpha_{0:d-2})
  :=-\theta-\sum_{j=0}^{d-2}\alpha_j\theta^{j-d+1}
  \quad (|\theta|\geq1).
  \]
  Empty sums are zero. A root in the first chart is equivalent to
  \(\alpha_0=s_0(\theta;\alpha_{1:d-1})\); a root in either outer chart is
  equivalent to
  \(\alpha_{d-1}=s_\infty(\theta;\alpha_{0:d-2})\). Define
  \[
  \begin{aligned}
  B_0(d,R)&:=d+\frac{R d(d-1)}2,\\
  B_\infty(d,R)&:=1+\frac{R d(d-1)}2,\\
  M_\eta(d,R)&:=\max\{\bar\kappa_0 B_0(d,R),
                            \bar\kappa_\infty B_\infty(d,R)\},\\
  \bar\kappa_*&:=\max\{\bar\kappa_0,\bar\kappa_\infty\},\\
  P_\eta(d,R)&:=\bar\kappa_*d+\frac{\bar\kappa_*}{2}R d^2.
  \end{aligned}
  \]
  The deterministic bridge to the probability bound is the pair of
  image-length estimates generated by
  \[
  \sup_{|\theta|\leq1}|\partial_\theta s_0(\theta;\alpha_{1:d-1})|
  \leq B_0(d,R),
  \qquad
  \sup_{|\theta|\geq1}|\partial_\theta s_\infty(\theta;\alpha_{0:d-2})|
  \leq B_\infty(d,R).
  \]

  For the nonemptiness assertion only, define a witness law
  \(\mu^{\mathrm{wit}}_{d,R}\) as follows. If \(d=1\), take
  \(\alpha_0\sim\mathrm{Unif}[-R,R]\). If \(d=2\), take
  \(\alpha_0,\alpha_1\) independently uniform on \([-R,R]\). If \(d\geq3\),
  take \(U_0,U_\infty\sim\mathrm{Unif}[-R,R]\) and a Rademacher variable
  \(S\), all mutually independent, and set
  \[
  \alpha_0=U_0,\qquad \alpha_{d-1}=U_\infty,
  \qquad \alpha_j=RS\quad(1\leq j\leq d-2).
  \]
  Every witness is supported on \([-R,R]^d\). For \(d\geq3\) the witness is
  ambiently singular, and for \(d\geq4\) its middle coordinates are dependent.

- Technical assumptions:
  - `assump:compact-parameter-domain` (Compact source parameter domain):
    \(\Theta\subseteq\mathbb R\) is the fixed compact parameter domain from
    the source problem. The theorem is uniform over every
    \(I\in\mathcal I(\Theta)\), and its constant has no dependence on
    \(I\) or \(\Theta\).
  - `assump:indexed-regime` (Indexed degree, radius, and fixed cap parameters):
    \(d\in\mathbb N\) with \(d\geq1\), \(R\geq1\), and
    \(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\) is finite and
    fixed independently of \(d\) and \(R\). No lower bound such as
    \(\bar\kappa_0,\bar\kappa_\infty\geq1/2\) is assumed for the root-hitting
    bound; that threshold is used only in the separate nonemptiness clause.
  - `assump:compact-cube-support` (Compact coefficient support and monicity):
    Each theorem-quantified law \(\mu\) satisfies
    \(\mu([-R,R]^d)=1\), and the associated polynomial is exactly the monic
    \(\phi_\alpha\) defined above. This is only a support condition and does
    not impose an ambient density.
  - `assump:mean-endpoint-conditional-caps` (Two mean conditional-density
    caps): The two endpoint regular conditional laws admit Lebesgue densities
    almost surely, and their random essential-supremum norms satisfy exactly
    \[
    \mathbb E_\mu K_0^\mu\leq\bar\kappa_0,
    \qquad
    \mathbb E_\mu K_\infty^\mu\leq\bar\kappa_\infty.
    \]
    These are mean bounds only. Together with
    `assump:compact-cube-support`, they are exhaustive of the law-membership
    requirements defining \(\mathcal D_{d,R,\eta}\).

## Formalized Goal

- Goal statement:
  For the single fixed \(\eta\) in `assump:indexed-regime`, prove that for
  every integer \(d\geq1\), every \(R\geq1\), every
  \(\mu\in\mathcal D_{d,R,\eta}\), and every positive-length interval
  \(I\in\mathcal I(\Theta)\),
  \[
  \begin{aligned}
  \mu(H_{d,I})
  &\leq
  \bar\kappa_0 B_0(d,R)|I_0|
  +\bar\kappa_\infty B_\infty(d,R)(|I_+|+|I_-|)\\
  &\leq M_\eta(d,R)|I|.
  \end{aligned}
  \]
  Consequently, simultaneously over the entire law class and all such
  intervals,
  \[
  \boxed{
  C_{\mathcal D_{d,R,\eta}}
  \leq
  \max\!\left\{
  \bar\kappa_0\!\left(d+\frac{R d(d-1)}2\right),
  \bar\kappa_\infty\!\left(1+\frac{R d(d-1)}2\right)
  \right\}
  \leq
  P_\eta(d,R)
  =\bar\kappa_*d+\frac{\bar\kappa_*}{2}R d^2.}
  \]
  The middle quantity is the exact two-chart maximum, not the sum of the
  chart constants; \(P_\eta\) is the explicitly displayed degree-three
  polynomial whose degree and coefficients are uniform in \((d,R)\), whose
  coefficients depend only on the fixed \(\eta\), and which has no hidden
  constants. In the same theorem, but only under the additional condition
  \(\bar\kappa_0,\bar\kappa_\infty\geq1/2\), prove that for every claimed
  \((d,R)\), the law \(\mu^{\mathrm{wit}}_{d,R}\) belongs to
  \(\mathcal D_{d,R,\eta}\): both of its mean slice caps equal
  \(1/(2R)\leq1/2\). Thus the indexed class is nonempty throughout the
  claimed regime under that threshold, without making the threshold an
  assumption of the bound.

## Reviewed Roadmap

The accepted sketch uses two coefficient-pivot sweeps with conditional
disintegration. Steps `step_001` and `step_002` prove the deterministic inner
and signed outer chart controls; `step_003` constructs measurable endpoint
caps, Borel root-event sections, and matching disintegrations; `step_004`
derives the three chart probability estimates; `step_005` forms the exact
three-piece union and maximum; and `step_006` takes the class supremum and
proves the fixed-\(\eta\) polynomial specialization. Independently,
`step_007` proves the degree-split witness kernels, support, singularity,
dependence, threshold comparison, and membership certificate. The bound path
is `(step_001, step_002, step_003) -> step_004 -> step_005 -> step_006`, and
the witness path is `step_007` alone.

## Attempted Claim

Fix one finite
\(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\), independently of
\((d,R)\). For every \(d\geq1\), \(R\geq1\),
\(\mu\in\mathcal D_{d,R,\eta}\), and \(I\in\mathcal I(\Theta)\), the assembled
bundle proves
\[
\mu(H_{d,I})
\leq \bar\kappa_0B_0(d,R)|I_0|
 +\bar\kappa_\infty B_\infty(d,R)(|I_+|+|I_-|)
\leq M_\eta(d,R)|I|.
\]
Consequently, with the stated zero convention for empty supremum index sets,
\[
C_{\mathcal D_{d,R,\eta}}
\leq M_\eta(d,R)
\leq P_\eta(d,R)
=\bar\kappa_*d+\frac{\bar\kappa_*}{2}Rd^2,
\qquad
\bar\kappa_*=\max\{\bar\kappa_0,\bar\kappa_\infty\}.
\]
The exact middle bound is the maximum of the two chart coefficients, not
their sum, and every coefficient is displayed.

Separately, only when
\(\bar\kappa_0,\bar\kappa_\infty\geq1/2\), the bundle proves for every
\(d\geq1\) and \(R\geq1\) that
\[
\mu^{\mathrm{wit}}_{d,R}\in\mathcal D_{d,R,\eta},
\qquad
K_0^{\mu^{\mathrm{wit}}_{d,R}}
=K_\infty^{\mu^{\mathrm{wit}}_{d,R}}
=\frac1{2R}\quad\text{almost surely}.
\]
The result retains the material_partial scope: it is a sufficient-condition
theorem for \(R\geq1\), without a necessity claim, coverage of laws lacking
both endpoint mean caps, or coverage of \(0<R<1\).

## Review Mode

split

## Review Verdict

- Score: 6
- Failure Type: PROOF_ASSEMBLY_FLAW

## Audit Summary

- Goal coverage: The attempted claim matches the authoritative quantifiers,
  weighted three-piece bound, exact chart maximum, class supremum, explicit
  polynomial, and separately thresholded witness clause. It consistently
  retains the `material_partial` label and states the unresolved necessity,
  missing-cap-law, and \(0<R<1\) regimes.
- Roadmap or step coverage: Sketch attempt 1 and the global diagnostic are
  accepted for downstream use, and all seven current step proofs have current
  `ACCEPTED` reviews whose recorded proof identities match their SHA-256
  hashes. The full acyclic bound and witness paths are represented in the
  appendix.
- Proof-details audit: The accepted source proofs establish the attempted
  claim. Specialized and global reviews were used only as diagnostics. All
  four specialized artifacts are independently contract-valid `BLOCKING`
  reviews with numbered issues, raw repair depth `assembly`, and raw target
  `/proof-assembly`; there is no reviewer disagreement.
- Adversarial stress test: The source proof survives the audited cases
  \(d=1\), \(R=1\), zero cap parameters, empty index sets, empty or singleton
  chart pieces, arbitrary endpoint conventions, roots at \(\pm1\), both outer
  signs, null conditioning fibers, tangencies, multiple roots, and all witness
  degree regimes. The public bundle nevertheless fails the standalone-proof
  test because malformed control sequences change literal formulas.
- LaTeX export structure/provenance gate: All five content files and all four
  support/entry files exist. Required sections, four stable assumption labels,
  seven step subsections, and the final main-theorem proof are present. No
  forbidden workflow provenance, public claim/hypothesis/invariant authority,
  audit scaffolding, or template placeholder was found. Compilation from
  inside `latex_template/` succeeded to a 21-page PDF in `/tmp`; after rerun,
  the log has no unresolved explicit references or citations, only nonblocking
  style/font warnings. Compilation silently accepts the malformed bare math
  tokens and therefore does not clear them.
- Main theorem self-containedness: Passed. The theorem uses only setup and
  preliminary notation, numbered assumptions, and constants defined before
  use; it does not depend on appendix-local labels.
- Assumption provenance gate: Passed. The theorem-facing conditions are the
  primitive class and indexed-regime conditions, while witness membership and
  the cap identities are proved before final use. The witness threshold is
  isolated from the general bound.
- Redundant public prose gate: Passed. No copied sketch-row fields, workflow
  narration, prohibited audit phrases, or mechanical placeholder prose was
  found.
- Proof-step notation surface gate: Passed apart from the identified
  transcription defects. Helper kernels, measurable caps, sections, and
  scalar aliases remain appendix- or proof-local; no nonpublic dictionary
  leaks into the main theorem.
- Theorem readability and notation economy: Passed. The main theorem exposes
  the exact claim directly, and the fixed-cap corollary provides a concise
  reader-facing polynomial rate. Preliminary notation is setting-derived and
  used by the theorem.
- Explicit-rate contract: Passed. The public theorem declares \(d,R,\eta\)
  and interval-length dependence, no hidden constants, fixed \(\eta\) and
  \(\Theta\), deterministic probability mode, static interval-uniform scope,
  and conditional \(L^\infty\)/Lebesgue-length norm inputs.
- Rate Specialization Bridge: Passed. The named bridge states
  \(\bar\kappa_i\leq\bar\kappa_*\), \(1\leq d\), and
  \(d(d-1)\leq d^2\), performs the termwise domination, and records that no
  probability conversion occurs.
- Baseline-reduction check: No inherited recovery or exact-limit theorem is
  part of this branch's target. The exact \(d=1\) specialization is preserved.
  The intro's separate comparison to an affine-latent/bounded-joint-density
  baseline is currently unsupported by a citation and is blocking as public
  related-result prose.
- LaTeX appendix full-derivation gate: Every used accepted step, not a sample,
  was compared from its `Local Derivation`, invoked `Cited Result
  Applications`, and `Target-Step Assembly` to the matching appendix
  subsection. All substantive definitions, equations, inequalities, boundary
  checks, standard-result discharges, and target assemblies are present.
  `step_003`, `step_004`, and `step_005` fail faithful public translation only
  at the five malformed tokens identified below; no other source obligation is
  unaccounted for or replaced by a summary.
- LaTeX export citation/reference gate: The 49 explicit labels are unique and
  all 142 explicit `\ref`/`\eqref` targets exist. The gate still fails because
  the external baseline prose has no BibTeX-backed citation and the appendix
  uses an unlabelled hard-coded `Section~2` locator.
- LaTeX export synchronization: Failed. `assembly_report.md` is structurally
  complete but its `COMPLETE`/`Blockers = None` disposition and clean-bundle
  claims conflict with the current public TeX. The accepted source formulas
  contain the missing backslashes, so the discrepancy is confined to assembly.

## Critical Issues

1. Sources: structural, rigor, citation, and adversarial reviewers plus the
   aggregate synchronization gate. Location:
   `latex_template/5_appendix.tex:362,700,723,743,912`. Defect: two `\qquad`,
   one `\lambda`, and two `\leq` control sequences lost their backslashes and
   are parsed as products of letter variables. Downstream effect: the public
   cap identity and scalar definitions are corrupted, the inner-chart
   integrand no longer denotes Lebesgue measure, and the positive-chart
   proposition and its key image bound do not state inequalities. Selected
   repair depth: `assembly`. Target: `/proof-assembly`.
2. Source: citation reviewer and aggregate citation/reference gate. Location:
   `latex_template/1_intro.tex:133-140` and
   `latex_template/reference.bib`. Defect: the public text asserts known
   affine-latent coverage and a specific bounded-joint-density baseline factor
   without a citation command or corresponding BibTeX source. Downstream
   effect: the external result, regime, cap convention, and quantitative
   comparison cannot be verified in a paper-ready bundle. Selected repair
   depth: `assembly`. Target: `/proof-assembly`.
3. Sources: structural and citation reviewers plus the aggregate internal
   reference gate. Location: `latex_template/5_appendix.tex:830`. Defect: the
   three-piece lemma imports \(I_0,I_+,I_-\) using hard-coded `Section~2`, with
   no stable section label and `\ref` pair and without restating the
   definitions inline. Downstream effect: this theorem-style dependency is not
   paper-ready or robustly traceable. Selected repair depth: `assembly`.
   Target: `/proof-assembly`.

## Failure-Type Rationale

The deepest required repair is assembly. Every affected formula is correct in
its current accepted source proof, all seven source-to-appendix derivation
audits otherwise close, and neither the theorem statement nor its roadmap
requires a semantic change. The citation and internal-navigation defects also
belong solely to the public bundle. `/proof-assembly` is therefore the
smallest controller-supported retry; rerunning a proof step, the sketch, or the
idea would alter accepted producer artifacts without addressing the actual
failure surface.

## Suggested Next Action

/proof-assembly

## Retry Mode

repair_assembly
