# Proof Review

## Formalized Setting

- Basic setup:
  Fix the source parameter domain \(\Theta\subseteq\mathbb R\), and let
  \[
  \mathcal I(\Theta)
  :=\{I\subseteq\Theta:I\text{ is an interval and }0<|I|<\infty\},
  \]
  where \(|I|\) is Lebesgue length and arbitrary endpoint conventions are
  allowed. Fix once and for all
  \(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\), with both
  entries finite. For an integer \(d\geq1\), a radius \(R\geq1\), and
  \(\alpha=(\alpha_0,\ldots,\alpha_{d-1})\in[-R,R]^d\), define the monic
  polynomial
  \[
  \phi_\alpha(\theta)
  :=\theta^d+\sum_{j=0}^{d-1}\alpha_j\theta^j
  \]
  and the interval root-hitting event
  \[
  H_{d,I}:=\{\alpha\in[-R,R]^d:\text{ there exists }\theta\in I
  \text{ such that }\phi_\alpha(\theta)=0\}.
  \]

  For a Borel probability law \(\mu\) supported on \([-R,R]^d\), whenever
  the indicated regular conditional laws have Lebesgue densities, write
  \[
  \begin{aligned}
  K_0^\mu
  &:=\left\|f^\mu_{\alpha_0\mid\alpha_{1:d-1}}
      (\,\cdot\mid\alpha_{1:d-1})\right\|_{L^\infty(\mathbb R)},\\
  K_\infty^\mu
  &:=\left\|f^\mu_{\alpha_{d-1}\mid\alpha_{0:d-2}}
      (\,\cdot\mid\alpha_{0:d-2})\right\|_{L^\infty(\mathbb R)}.
  \end{aligned}
  \]
  These are random essential-supremum slice caps, evaluated at the random
  conditioning coordinates. For \(d=1\), both conditioning tuples are empty
  and both quantities mean the \(L^\infty\)-norm of the density of the sole
  coefficient \(\alpha_0\).

  Let \(\mathcal P(\mathbb R^d)\) denote the Borel probability laws on
  \(\mathbb R^d\). Define, exactly and without additional membership conditions,
  \[
  \begin{aligned}
  \mathcal D_{d,R,\eta}:=\{\mu\in\mathcal P(\mathbb R^d):\;&
  \mu([-R,R]^d)=1;\\
  &\mu(\alpha_0\in\cdot\mid\alpha_{1:d-1})
    \text{ and }
    \mu(\alpha_{d-1}\in\cdot\mid\alpha_{0:d-2})
    \text{ admit Lebesgue densities a.s.};\\
  &\mathbb E_\mu K_0^\mu\leq\bar\kappa_0,
    \quad
    \mathbb E_\mu K_\infty^\mu\leq\bar\kappa_\infty\}.
  \end{aligned}
  \]
  Thus membership imposes both mean endpoint conditional-density caps, but
  no almost-sure uniform slice cap, ambient joint density, independence,
  affine-latent representation, root event, root intensity, interval
  statistic, or preassigned value of \(C_{\mathcal D}\).

  The class-uniform root-hitting constant is
  \[
  C_{\mathcal D_{d,R,\eta}}
  :=\sup_{\mu\in\mathcal D_{d,R,\eta}}
    \sup_{I\in\mathcal I(\Theta)}
    \frac{\mu(H_{d,I})}{|I|},
  \]
  with value \(0\) when either indexing set of the displayed suprema is empty.

  No learner or online algorithm is specialized. The analytic protocol is the
  following deterministic two-chart construction: split each
  \(I\in\mathcal I(\Theta)\) into the disjoint pieces
  \[
  I_0:=I\cap[-1,1],\qquad
  I_+:=I\cap(1,\infty),\qquad
  I_-:=I\cap(-\infty,-1),
  \]
  so \(|I_0|+|I_+|+|I_-|=|I|\). The coefficient sweeps are
  \[
  s_0(\theta;\alpha_{1:d-1})
  :=-\theta^d-\sum_{j=1}^{d-1}\alpha_j\theta^j
  \quad (|\theta|\leq1)
  \]
  and, after dividing by \(\theta^{d-1}\),
  \[
  s_\infty(\theta;\alpha_{0:d-2})
  :=-\theta-\sum_{j=0}^{d-2}\alpha_j\theta^{j-d+1}
  \quad (|\theta|\geq1).
  \]
  Empty sums are zero. A root in the first chart is equivalent to
  \(\alpha_0=s_0(\theta;\alpha_{1:d-1})\); a root in either outer chart is
  equivalent to
  \(\alpha_{d-1}=s_\infty(\theta;\alpha_{0:d-2})\). Define
  \[
  \begin{aligned}
  B_0(d,R)&:=d+\frac{R d(d-1)}2,\\
  B_\infty(d,R)&:=1+\frac{R d(d-1)}2,\\
  M_\eta(d,R)&:=\max\{\bar\kappa_0 B_0(d,R),
                            \bar\kappa_\infty B_\infty(d,R)\},\\
  \bar\kappa_*&:=\max\{\bar\kappa_0,\bar\kappa_\infty\},\\
  P_\eta(d,R)&:=\bar\kappa_*d+\frac{\bar\kappa_*}{2}R d^2.
  \end{aligned}
  \]
  The deterministic bridge to the probability bound is the pair of
  image-length estimates generated by
  \[
  \sup_{|\theta|\leq1}|\partial_\theta s_0(\theta;\alpha_{1:d-1})|
  \leq B_0(d,R),
  \qquad
  \sup_{|\theta|\geq1}|\partial_\theta s_\infty(\theta;\alpha_{0:d-2})|
  \leq B_\infty(d,R).
  \]

  For the nonemptiness assertion only, define a witness law
  \(\mu^{\mathrm{wit}}_{d,R}\) as follows. If \(d=1\), take
  \(\alpha_0\sim\mathrm{Unif}[-R,R]\). If \(d=2\), take
  \(\alpha_0,\alpha_1\) independently uniform on \([-R,R]\). If \(d\geq3\),
  take \(U_0,U_\infty\sim\mathrm{Unif}[-R,R]\) and a Rademacher variable
  \(S\), all mutually independent, and set
  \[
  \alpha_0=U_0,\qquad \alpha_{d-1}=U_\infty,
  \qquad \alpha_j=RS\quad(1\leq j\leq d-2).
  \]
  Every witness is supported on \([-R,R]^d\). For \(d\geq3\) the witness is
  ambiently singular, and for \(d\geq4\) its middle coordinates are dependent.

- Technical assumptions:
  - `assump:compact-parameter-domain` (Compact source parameter domain):
    \(\Theta\subseteq\mathbb R\) is the fixed compact parameter domain from
    the source problem. The theorem is uniform over every
    \(I\in\mathcal I(\Theta)\), and its constant has no dependence on
    \(I\) or \(\Theta\).
  - `assump:indexed-regime` (Indexed degree, radius, and fixed cap parameters):
    \(d\in\mathbb N\) with \(d\geq1\), \(R\geq1\), and
    \(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\) is finite and
    fixed independently of \(d\) and \(R\). No lower bound such as
    \(\bar\kappa_0,\bar\kappa_\infty\geq1/2\) is assumed for the root-hitting
    bound; that threshold is used only in the separate nonemptiness clause.
  - `assump:compact-cube-support` (Compact coefficient support and monicity):
    Each theorem-quantified law \(\mu\) satisfies
    \(\mu([-R,R]^d)=1\), and the associated polynomial is exactly the monic
    \(\phi_\alpha\) defined above. This is only a support condition and does
    not impose an ambient density.
  - `assump:mean-endpoint-conditional-caps` (Two mean conditional-density
    caps): The two endpoint regular conditional laws admit Lebesgue densities
    almost surely, and their random essential-supremum norms satisfy exactly
    \[
    \mathbb E_\mu K_0^\mu\leq\bar\kappa_0,
    \qquad
    \mathbb E_\mu K_\infty^\mu\leq\bar\kappa_\infty.
    \]
    These are mean bounds only. Together with
    `assump:compact-cube-support`, they are exhaustive of the law-membership
    requirements defining \(\mathcal D_{d,R,\eta}\).

## Formalized Goal

- Goal statement:
  For the single fixed \(\eta\) in `assump:indexed-regime`, prove that for
  every integer \(d\geq1\), every \(R\geq1\), every
  \(\mu\in\mathcal D_{d,R,\eta}\), and every positive-length interval
  \(I\in\mathcal I(\Theta)\),
  \[
  \begin{aligned}
  \mu(H_{d,I})
  &\leq
  \bar\kappa_0 B_0(d,R)|I_0|
  +\bar\kappa_\infty B_\infty(d,R)(|I_+|+|I_-|)\\
  &\leq M_\eta(d,R)|I|.
  \end{aligned}
  \]
  Consequently, simultaneously over the entire law class and all such
  intervals,
  \[
  \boxed{
  C_{\mathcal D_{d,R,\eta}}
  \leq
  \max\!\left\{
  \bar\kappa_0\!\left(d+\frac{R d(d-1)}2\right),
  \bar\kappa_\infty\!\left(1+\frac{R d(d-1)}2\right)
  \right\}
  \leq
  P_\eta(d,R)
  =\bar\kappa_*d+\frac{\bar\kappa_*}{2}R d^2.}
  \]
  The middle quantity is the exact two-chart maximum, not the sum of the
  chart constants; \(P_\eta\) is the explicitly displayed degree-three
  polynomial whose degree and coefficients are uniform in \((d,R)\), whose
  coefficients depend only on the fixed \(\eta\), and which has no hidden
  constants. In the same theorem, but only under the additional condition
  \(\bar\kappa_0,\bar\kappa_\infty\geq1/2\), prove that for every claimed
  \((d,R)\), the law \(\mu^{\mathrm{wit}}_{d,R}\) belongs to
  \(\mathcal D_{d,R,\eta}\): both of its mean slice caps equal
  \(1/(2R)\leq1/2\). Thus the indexed class is nonempty throughout the
  claimed regime under that threshold, without making the threshold an
  assumption of the bound.

## Reviewed Roadmap

The accepted sketch uses two coefficient-pivot sweeps with conditional
disintegration. Steps `step_001` and `step_002` derive the inner and two
outer derivative/image-length certificates. Step `step_003` supplies the
measurable conditional-density caps, Borel root events, and matching
disintegration interface. Step `step_004` converts those inputs into the three
chart probability estimates using the corresponding endpoint kernels and
mean caps. Step `step_005` performs the exact three-piece event union and
weighted-maximum reduction. Step `step_006` takes the class/interval suprema and
proves the displayed polynomial specialization. Independently, step
`step_007` proves the indexed witness membership in all degree regimes under
the separate cap threshold.

The dependency graph
\[
(\text{step_001},\text{step_002},\text{step_003})
\longrightarrow\text{step_004}
\longrightarrow\text{step_005}
\longrightarrow\text{step_006}
\]
is closed and acyclic, while \(\text{step_007}\) feeds only the separate
nonemptiness clause. All seven current proof/review pairs are accepted and
identity-matched. The diagnostic global proof is paired with its accepted
review. The aggregate audit compared every one of the 23 used accepted local
units and all seven target-step assemblies against the appendix, without
sampling, and found their mathematical content and final theorem assembly
preserved.

This roadmap establishes only the declared `material_partial@@ result: a
sufficient-condition theorem for \(R\geq1\) with explicit indexed rate. It
does not solve the full source open problem; necessity, laws lacking both
endpoint mean caps, and the regime \(0<R<1\) remain open.

## Attempted Claim

Fix one finite
\(\eta=(\bar\kappa_0,\bar\kappa_\infty)\in[0,\infty)^2\), independently of
\((d,R)\). For every \(d\geq1\), \(R\geq1\),
\(\mu\in\mathcal D_{d,R,\eta}\), and \(I\in\mathcal I(\Theta)\), the assembled
bundle proves
\[
\mu(H_{d,I})
\leq \bar\kappa_0B_0(d,R)|I_0|
 +\bar\kappa_\infty B_\infty(d,R)(|I_+|+|I_-|)
\leq M_\eta(d,R)|I|.
\]
Consequently, with the stated zero convention for empty supremum index sets,
\[
C_{\mathcal D_{d,R,\eta}}
\leq M_\eta(d,R)
\leq P_\eta(d,R)
=\bar\kappa_*d+\frac{\bar\kappa_*}{2}Rd^2,
\qquad
\bar\kappa_*=\max\{\bar\kappa_0,\bar\kappa_\infty\}.
\]
The exact middle bound is the maximum of the two chart coefficients, not
their sum, and every coefficient is displayed.

Separately, only when
\(\bar\kappa_0,\bar\kappa_\infty\geq1/2\), the bundle proves for every
\(d\geq1\) and \(R\geq1\) that
\[
\mu^{\mathrm{wit}}_{d,R}\in\mathcal D_{d,R,\eta},
\qquad
K_0^{\mu^{\mathrm{wit}}_{d,R}}
=K_\infty^{\mu^{\mathrm{wit}}_{d,R}}
=\frac1{2R}\quad\text{almost surely}.
\]
The result retains the material_partial scope: it is a sufficient-condition
theorem for \(R\geq1\), without a necessity claim, coverage of laws lacking
both endpoint mean caps, or coverage of \(0<R<1\).

## Review Mode

split

## Review Verdict

- Score: 6
- Failure Type: PROOF_ASSEMBLY_FLAW

## Audit Summary

- Goal coverage: PASS at the exact declared `material_partial@@ scope. The
  public theorem preserves the setting's quantifier order, weighted
  three-piece estimate, exact maximum coefficient, class supremum,
  polynomial specialization, and separately thresholded witness clause. It
  does not purport to settle necessity, laws without both endpoint caps, or
  \(0<R<1\), and therefore does not present itself as a solution of the full
  source open problem.
- Roadmap or step coverage: PASS. All seven accepted sketch steps have
  distinct appendix producers and theorem-level consumers; all accepted
  proof/review identities match, the dependency graph closes, and no stale or
  diagnostic claim is used as proof authority.
- Proof-details audit: PASS for the accepted mathematics and substantive
  public derivations. Exhaustive comparison of all 23 used local units and
  all seven target-step assemblies preserved definitions, displayed
  calculations, inequalities, case splits, assumptions, constants, and
  conclusions. Acceptance is nevertheless blocked by the public
  citation/source mappings listed below. The specialized verdicts are
  structural `PASS`, rigor `PASS`, adversarial `PASS`, and citation
  `BLOCKING`; under the deepest-issue rule, the contract-valid citation
  blocker controls rather than the majority of diagnostic verdicts.
- Adversarial stress test: PASS. Degree and radius boundaries, zero caps,
  empty/singleton chart pieces, endpoint allocation, tangencies, multiple
  roots, conditioning-null fibers, singular witness laws, and dependent
  middle coordinates do not refute the claim. The unstated measurability of
  \(y\mapsto\lambda(s_i(J;y))\) is nonblocking because it follows from a
  countable dense-set representation, or can be avoided by integrating the
  displayed measurable majorant.
- LaTeX export structure/provenance gate: BLOCKING only at assembly-level
  external-result provenance. The nine-file structure, theorem ownership,
  internal dependency provenance, and BibTeX wiring are sound, but the public
  appendix does not correctly source and instantiate every non-atomic
  measure-theoretic fact.
- Main theorem self-containedness: PASS. All theorem-facing events, classes,
  caps, interval pieces, coefficients, maxima, polynomial terms, assumptions,
  regimes, and the separate witness threshold are defined before use.
- Assumption provenance gate: PASS. The four primitive assumptions are stable
  and exhaustive; no generated condition or proof-local wrapper is promoted
  into the theorem assumptions, and the witness threshold does not enter the
  general probability bound.
- Redundant public prose gate: PASS. The public files contain no internal
  workflow, audit, controller, attempt, acceptance, artifact-contract, or
  template prose.
- Proof-step notation surface gate: PASS. Proof-local kernels, measurable
  representatives, support sets, and scalar aliases stay in their derivations
  rather than becoming opaque theorem-facing packages.
- Theorem readability and notation economy: PASS. The main statement exposes
  the exact weighted estimate and maximum with stable preliminary notation;
  appendix results are paper-facing and theorem-styled without forbidden
  claim, hypothesis, or invariant environments.
- Explicit-rate contract: PASS. The bundle displays all dependence on
  \(d,R,\bar\kappa_0,\bar\kappa_\infty\), interval lengths, probability mode,
  supremum mode, norm, metric, and admissible regime, with fixed finite
  \(\eta\) and no hidden constants, auxiliary tolerances, or asymptotic
  conversion.
- Rate Specialization Bridge: PASS. The named fixed-\(\eta\) specialization
  proves the scalar dominations \(\bar\kappa_i\leq\bar\kappa_*\),
  \(1\leq d\), and \(d(d-1)\leq d^2\) used to obtain
  \(M_\eta(d,R)\leq P_\eta(d,R)\).
- Baseline-reduction check: PASS for the applicable boundary obligation. At
  \(d=1\), both pivot descriptions reduce to the sole coefficient and
  \(B_0=B_\infty=1\); the theorem makes no unsupported recovery,
  zero-defect, or full-open-problem claim.
- LaTeX appendix full-derivation gate: PASS. The appendix retains substantive
  paper-facing proofs for all \(2+2+3+3+3+4+6=23\) accepted local units, all
  seven target-step assemblies, and the final theorem proof; no sampled
  comparison, proof-by-reference to internal artifacts, or derivation-erasing
  compression was used.
- LaTeX export citation/reference gate: BLOCKING. The internal label/reference
  graph and both BibTeX keys resolve, but the appendix invokes
  Radon--Nikodym before establishing kernel-product measurehood, cites an
  unrelated Folland proposition for integral continuity, and gives false
  Kallenberg locators while omitting the whole-space/equal-mass discharge for
  finite-measure uniqueness.
- LaTeX export synchronization: PASS mechanically and by frozen identity.
  The exact nine input files compile in a fresh isolated BibTeX-aware Tectonic
  build with a second stabilization invocation; there are no TeX errors,
  unresolved references/citations, duplicate labels, or overfull/underfull
  boxes. The only diagnostics are nonblocking bundled-font substitutions and
  cached-package UTF-8 warnings. Mechanical synchronization does not cure the
  citation-fidelity blocker.

## Critical Issues

1. In `latex_template/5_appendix.tex:383-398`, the appendix defines
   \(\gamma_i(C)=\int Q_y^i(C_y)\,\nu_i(\mathrm d y)\) and invokes
   Radon--Nikodym without first proving that \(\gamma_i\) is a measure.
   Kernel-section measurability and countable additivity must be established
   before Folland's Theorem 3.8 is applicable. The assembly should map an
   exact kernel-product result, such as Kallenberg Lemma 1.41(iii), to the
   conditioning marginal and probability kernel, or supply both derivations
   directly. Source: citation specialized review. Downstream effect: the
   public measurable-cap construction lacks the measure premise needed for
   density existence and is not paper-ready. Selected repair depth and
   target: assembly; `/proof-assembly`.
2. In `latex_template/5_appendix.tex:433-435`, Folland Proposition
   2.23 is cited for absolute continuity of the integral under
   rational-endpoint approximation, but that proposition does not state the
   property used. The assembly must provide an inline argument or a correct
   exact source and map the finite measure
   \(E\mapsto\int_E|q|\,\mathrm d\lambda\) and its hypotheses. Source:
   citation specialized review. Downstream effect: the strict rational-
   interval comparison rests on a false public locator, so the cap-equality
   derivation is not source-traceable. Selected repair depth and target:
   assembly; `/proof-assembly`.
3. The citations at `latex_template/5_appendix.tex:407`,
   `latex_template/5_appendix.tex:626`, and
   `latex_template/5_appendix.tex:637` incorrectly name Kallenberg
   "Lemma 1.1." The pi--lambda result is Theorem 1.1, while the
   bounded-measure uniqueness result is Lemma 1.17. The latter also requires
   a generating pi-system containing the whole space; the repaired assembly
   must expose that condition, or equivalently the equal-total-mass
   discharge, before concluding equality on all Borel sets. Source: citation
   specialized review. Downstream effect: the all-Borel fiber identity and
   the later kernel equalities lack valid public theorem locators and a
   required assumption discharge. Selected repair depth and target:
   assembly; `/proof-assembly`.

## Failure-Type Rationale

The blocker is confined to the paper-facing assembly. The accepted
`step_003` source interface already states the standard kernel,
Radon--Nikodym, and measurable-cap facts needed by downstream steps, and the
exhaustive comparison found no changed or missing accepted mathematical unit.
The public translation instead omits one source-to-object premise and assigns
false locators to two other standard facts. Correcting those citations,
instantiations, and assumption discharges requires no theorem-contract,
proof-sketch, local-step, or idea change. Therefore the smallest aligned
failure type is `PROOF_ASSEMBLY_FLAW`, not
`PROOF_STEP_FLAW`, `PROOF_SKETCH_FLAW`, or
`IDEA_FAIL`. This contract-valid assembly issue is the deepest remaining
blocker: accepting without a repair is insufficient, while routing deeper to
a producer step, sketch, or idea would exceed the evidence and required
change.

## Suggested Next Action

/proof-assembly

## Retry Mode

repair_assembly
