# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Branch: `perspective_2/idea_1`
- Goal mode: exact-goal mode. The binding target is the per-law, per-interval weighted two-chart estimate, its exact weighted maximum, the class supremum and explicit polynomial specialization, and the separately thresholded indexed nonemptiness clause in `setting.md`.
- Reviewed roadmap: `step_001` and `step_002` prove the inner and outer deterministic sweep bounds; `step_003` supplies measurable conditional caps and Borel event fibers; `step_004` disintegrates chart events; `step_005` forms the three-piece union and exact maximum; `step_006` takes suprema and specializes to the displayed polynomial; `step_007` proves witness membership.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch preserves every binding quantifier: one finite `eta` fixed independently of `d,R`; every integer `d >= 1`; every `R >= 1`; every `mu` in the exactly defined class; and every positive-length interval in the fixed compact source domain with arbitrary endpoint convention. It proves the exact weighted estimate before the maximum, takes both required suprema, uses the stated empty-index convention, and keeps the `1/2` hypothesis out of the bound.
- Source alignment: The branch is consistently labeled `material_partial`. It gives a noncircular coefficient-side sufficient condition and a nonempty indexed family, openly restricts to `R >= 1`, claims no necessity or new law-coverage result over the affine-latent baseline, and records the remaining gaps: necessity, laws without both endpoint caps, and `0 < R < 1`. This is faithful to the brief's permitted material-partial target.
- Dependency audit: The graph is acyclic. The independent producers `step_001`, `step_002`, and `step_003` feed `step_004`, followed by `step_005` and `step_006`; `step_007` is independent and is consumed only by the nonemptiness clause. Every generated output has a producer before each consumer, and the threshold in `step_007` has no path into `step_001` through `step_006`.
- High-risk coverage: The sketch localizes the exact inner and outer derivatives, both outer signs, measurable cap versions, Borel root-event sections, conditional disintegration, the weighted maximum rather than a sum, the class supremum, empty classes, polynomial specialization, all `d = 1` branches, and witness membership. Tangencies and multiple roots require no transversality because the fiber identities are exact.
- Explicit-rate coverage: The exposed variables, fixed quantities, deterministic probability mode, static interval-uniform mode, norm mode, and forbidden hidden dependencies are explicit. The bridge uses displayed termwise inequalities only; `P_eta(d,R)` has coefficients depending solely on fixed `eta`, with no asymptotic absorption or concealed constant.
- Assumption and citation plausibility: Every theorem-facing assumption is primitive and cited by a stable `assump:*` id. The 2020 sweep is provenance only, not a black-box theorem. The theorem-critical measure-theory facts in `step_003` are assigned to a current-notation proof with the exact standard-Borel and Lebesgue conventions, raw assumptions, and exported conclusions fixed; no unresolved external theorem label or mismatched source object is consumed.

## Early Obstruction Audit

- Limiting-case stress: The sketch passes `d = 1`, `R = 1`, `bar kappa_* = 0`, empty law classes, empty and singleton chart pieces, arbitrary open/closed/half-open interval endpoints, roots at `theta = +/-1`, approach to the outer endpoints, null conditioning fibers, tangencies, and multiple roots. At `d = 1`, both sums are empty, both pivots are the same sole coefficient, both derivative constants equal one, and the witness has the required sole-coefficient density.
- Theorem-critical bridge support: The inner identity has derivative
  \[
  s_0'(\theta)=-d\theta^{d-1}-\sum_{j=1}^{d-1}j\alpha_j\theta^{j-1},
  \]
  and the outer identity, after legal division by `theta^(d-1)` on both outer components, has derivative
  \[
  s_\infty'(\theta)=-1+\sum_{j=0}^{d-2}(d-1-j)\alpha_j\theta^{j-d}.
  \]
  Primitive cube support yields the exact `B_0` and `B_infty` image-length interfaces. The cap/event bridge, tower bridge, union bridge, supremum bridge, polynomial bridge, and witness bridge are each assigned to a stable step with exact inputs and outputs.
- Exported-interface feasibility: Each image-length output is for the exact pivot-image set consumed by disintegration. The measurable cap output is for the exact conditional kernel consumed by `step_004`. The three probability controls export the exact weighted terms consumed by `step_005`; `step_005` exports `M_eta |I|`; `step_006` exports the same root-hitting ratio under `P_eta`; and `step_007` exports membership of the exact witness law. Every raw-control-to-output relation and consumer path is present.
- Theorem-critical mechanism witness gate: All hard blocks have nonvacuous sources of the correct claim class. Upper derivative controls come directly from monicity and cube support; density domination comes from the primitive conditional absolute continuity and measurable cap; the exact maximum comes from length weights; the polynomial comes from pointwise scalar dominance; and nonemptiness comes from independent uniform endpoint innovations. No lower, sign, support, or closure claim is inferred from a merely upper-bound source.
- Entry-state trace stress: No recursive algorithm, trajectory, update, convergence claim, or maintained invariant occurs, so no entry-state trace is applicable. The corresponding static boundary states are nevertheless traced explicitly: `d = 1`, `R = 1`, `theta = +/-1`, empty/singleton pieces, null conditioning fibers, and the three witness degree regimes all leave the relevant mechanism active or make the event null for the stated source.
- Obligation locality classification: Inner sweep, outer sweeps, measurable cap/Borel disintegration, conditional chart estimates, weighted maximum, class supremum and empty-class handling, polynomial specialization, degree-one specialization, and witness membership are all `step-local`. Each has a primitive or earlier-derived source and a fixed current-notation interface; none needs a new assumption, changed scope, changed metric, changed dependence, weakened conclusion, or new roadmap bridge.
- Noncircular closure gate: No all-time, invariant, stability, recurrence, basin, support-maintenance, boundedness-maintenance, or generated-condition closure is used. The cap is produced from a primitive conditional kernel before consumption, and every later output follows an acyclic producer path. The class supremum consumes a per-law estimate and never assumes finiteness of `C_D`.
- Mechanism-source and boundary stress: Without cube support the two derivative bounds would be unsupported, and without conditional absolute continuity an atom could charge a zero-length image; both sources are primitive in the binding setting. Negative outer coordinates, inverse powers, endpoint allocation, and singular middle coordinates are explicitly handled. No allowed null regime makes a claimed source vanish while leaving its theorem-facing conclusion unsupported.
- Generated-output flow: The legal chain is `(step_001, step_002, step_003) -> step_004 -> step_005 -> step_006 -> final bound`; independently, `step_007 -> final nonemptiness clause`. All consumers and final uses are listed, and no closure, specialization, or assembly label substitutes for a producer.
- Source-to-claim adequacy: The direct derivative calculations use exactly the branch's monic coefficient ordering and Euclidean absolute value. The kernel argument uses standard-Borel conditioning spaces and Lebesgue measure on the same pivot coordinate as the theorem. The rational-interval formula, Lebesgue differentiation, and sigma-compact zero-set projection are stated in current notation with the exact raw assumptions and conclusion interface. No transformed population, reference, weighted, or surrogate object is introduced.
- Residual-to-target adequacy: On every chart, the fiber root event is exactly pivot membership in the corresponding image, so the event-transfer residual is zero. The measurable cap differs across kernel or density versions only on conditioning-null fibers, which has zero integrated contribution. `M_eta` and `P_eta` control the same scalar root-hitting ratio by an explicit pointwise inequality, and the produced witness is the exact law consumed by the membership clause. No persistent, empirical, event-level, irreducible, or wrong-scale residual remains.
- Baseline invariance audit: No recovery or zero-defect theorem from the 2020 baseline is included in the binding target, so there is no inherited baseline conclusion to preserve. The relevant exact boundary specialization is `d = 1`; it is preserved rather than replaced by a remainder: `s_0 = s_infty = -theta`, `B_0 = B_infty = 1`, both conditional caps concern the sole coefficient, and the same probability proof applies. The public polynomial is a conservative same-target upper bound, not a substitute for an inherited recovery claim.
- Scope-accumulation compatibility: The proof is static. Each chart is integrated once, the outer signs are two finite parallel cases, and the three events are combined by one finite union bound. There is no repeated, recursive, limiting, or all-time defect, forcing term, leakage, potential, or budget to accumulate. Uniformity over indexed `d,R` follows from one pointwise inequality at every index with fixed `eta`.
- Scope and dependence consistency: The sketch makes no upgrade beyond the setting's deterministic, per-law, all-interval statement. The restriction `R >= 1`, fixed `eta`, source-domain independence, conditional mean-cap mode, and all displayed `d,R` dependence are unchanged. The exact weighted maximum is
  \[
  A|I_0|+C(|I_+|+|I_-|)\leq\max\{A,C\}|I|,
  \]
  not `A + C`, and the polynomial specialization uses `1 <= d` and `d(d-1) <= d^2` without an unstated threshold.
- Generated-condition provenance: The only potentially version-dependent generated object is the measurable cap
  \[
  \widehat K(y)=\sup_{J\in\mathcal J_{\mathbb Q}}Q_y(J)/|J|.
  \]
  Countability gives measurability, and on every density fiber differentiation identifies it with the essential supremum. Bounded arbitrary intervals are sigma-compact; compact zero sets have compact coefficient projections, so the root events and their sections are Borel. Thus disintegration is legal and no event or realized membership fact is hidden as a primitive premise.
- Citation and tool applicability: arXiv:1904.09014, Appendix Theorem 18, supplies historical mechanism provenance only; none of its density hypotheses or constants is imported. The open-problem source fixes the model and comparison baseline. The standard kernel, differentiation, mean-value, connected-image, compact-projection, and tower facts are used in their native current-notation conventions with their hypotheses exposed, so no cited wrapper has an unknown conclusion shape, non-output boundary, or source-convention mismatch.
- Same-setting repair plausibility: No material obstruction was found. The local proofs still must establish the stated standard facts and calculations, but all required sources and interfaces already exist under the unchanged setting.
- Target-preserving bridge-repair gate: No missing bridge requires repair. In particular, there is no need to strengthen the cap assumptions, add joint density or independence to the general theorem, change the `R >= 1` scope, weaken the maximum, or import the witness threshold into the bound.
- High-risk obligation classes: Structural derivative controls, conditional-density versions, event measurability, event-fiber disintegration, arbitrary endpoints, finite-union loss, explicit-rate specialization, empty classes, the degree-one boundary, singular/dependent witness coordinates, and threshold isolation have all been stress-tested. There is no recursive, perturbative, stochastic-error, mode-upgrade, or accumulated-control obligation beyond these static blocks.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Inner sweep certificate | Deterministic structural upper bound feeding the inner chart probability | Direct differentiation of `s_0` plus mean-value and connected-image facts | `assump:indexed-regime`; `assump:compact-cube-support`; current-notation standard calculus | Cube support controls every coefficient derivative term and gives exactly `B_0` | Same monic ordering, pivot coefficient, Euclidean coordinate, normalization, and interval length as the binding setting | `abs(s_0') <= d + R sum_{j=1}^{d-1} j = B_0`, hence `lambda(s_0(J)) <= B_0 length(J)` | Raw controls are `abs(alpha_j) <= R` and `abs(theta) <= 1`; export is the exact inner pivot-image length consumed by `step_004` | Fiber event is exactly `alpha_0 in s_0(J;y)`; zero transfer residual | Finite coefficient sum, used once; no forcing or accumulated defect | Primitive assumptions -> `step_001` -> `step_004`; no target property is assumed | Producer `step_001`; consumer `step_004`, then `step_005`, `step_006`, final theorem | Empty/singleton `J`, arbitrary endpoints, `theta = +/-1`, and `d = 1` all pass; the empty sum gives derivative `-1` | step-local | None |
| Positive and negative outer sweep certificates | Deterministic structural upper bounds feeding both outer chart probabilities | Exact pivot division and direct differentiation of `s_infty` | `assump:indexed-regime`; `assump:compact-cube-support`; current-notation algebra and calculus | Nonzero outer `theta`, inverse-power bounds, and cube support give exactly `B_infty` on each connected side | Same monic polynomial, coefficient pivot, normalization, Euclidean metric, and two outer components as the setting | `s_infty' = -1 + sum_{j=0}^{d-2}(d-1-j) alpha_j theta^{j-d}` and `abs(s_infty') <= 1 + R d(d-1)/2` | Raw controls are monicity, `abs(alpha_j) <= R`, `abs(theta) >= 1`, and separate signs; exports are the two exact image-length bounds | Each outer fiber event equals membership in its same-side pivot image; zero residual | Two finite parallel uses, not accumulation; all inverse-power terms are controlled | Primitive assumptions -> `step_002` -> `step_004`; no outer event estimate is presumed | Producer `step_002`; consumer `step_004`, then `step_005`, `step_006`, final theorem | Negative `theta`, limits at `+/-1`, excluded boundary points, empty/singleton pieces, and `d = 1` pass; division is by `theta^0 = 1` at `d = 1` | step-local | None |
| Measurable cap, Borel event, and disintegration interface | Generated measurable wrapper needed to integrate conditional density caps and take event sections | Standard-Borel regular conditional kernels; countable rational-interval cap; Lebesgue differentiation; sigma-compact exhaustion and compact projection | Primitive a.s. conditional absolute continuity from `assump:mean-endpoint-conditional-caps` plus current-notation standard measure theory | These sources give measurability, a.s. equality to the exact conditional `L^infty` cap, density domination for every Borel pivot set, and Borel chart events | Conditioning spaces are Borel Euclidean spaces and the pivot reference is Lebesgue measure, exactly matching the setting; kernel-version changes occur only on marginal-null fibers | `Khat(y)=sup_{J in J_Q} Q_y(J)/length(J)=Linf(q_y)` on density fibers and `Q_y(A) <= Khat(y) lambda(A)` | Raw control is a regular conditional kernel absolutely continuous a.s.; exports are a measurable cap and the exact Borel event sections consumed by `step_004` | Null-fiber/version discrepancy has zero integrated mass; no approximation to the consumed conditional event | One static conditional integral; no repeated residual or adversarial forcing | Primitive kernel -> `step_003` -> `step_004`; cap is produced before use and does not assume a root estimate | Producer `step_003`; consumer `step_004` and all later bound steps | Null fibers, open/closed/half-open intervals, empty/singleton pieces, tangencies, and multiple roots pass; sigma-compact exhaustion covers arbitrary endpoint conventions | step-local | None |
| Three conditional chart probability controls | Derived event bounds supplying the exact weighted estimate | Exact chart fibers from `step_001`/`step_002`, measurable domination from `step_003`, and primitive mean caps | Earlier derived outputs plus `assump:mean-endpoint-conditional-caps` | Matching each chart to its own conditioning kernel yields the required mean-cap coefficient without an a.s. cap | Inner and outer conditioning sigma-fields remain distinct; each kernel acts on its own theorem pivot and event section | `Q_y(H_y) <= K_i(y) lambda(s_i(J;y))`, then expectation and `E K_i <= bar kappa_i` | Raw controls are the image lengths and mean cap bounds; exports are `bar kappa_0 B_0 length(I_0)` and `bar kappa_infty B_infty length(I_+/-)` | Root fibers and pivot images are identical, so event residual is zero | Random caps may be unbounded pointwise but are integrated once; their means control the entire defect | `step_001` + `step_002` + `step_003` -> `step_004` -> `step_005` | Producer `step_004`; consumer `step_005`, then public bound | At `d = 1` both conditioning tuples are empty and refer to the same density; zero-length pieces have zero probability | step-local | None |
| Exact finite union and weighted maximum | Theorem-closure block producing the per-law, per-interval maximum | Exact three-piece domain partition, finite subadditivity, and elementary weighted algebra | `step_004`; `assump:compact-parameter-domain`; `assump:indexed-regime` | The sources control event overlap while retaining piece lengths, which is exactly what is needed for the maximum rather than a sum | Same interval, endpoint convention, chart allocation, and Lebesgue length as the setting | `H_I = H_{I_0} union H_{I_+} union H_{I_-}` and `Ax+C(y+z) <= max{A,C}(x+y+z)` | Raw controls are the three chart probabilities and exact length identity; export is `mu(H_I) <= M_eta length(I)` | Event identity and length partition are exact; finite subadditivity is the sole stated loss | Exactly one three-event union; no repeated or persistent defect | `step_004` -> `step_005` -> `step_006`; no circular use of the final root constant | Producer `step_005`; consumers `step_006` and final theorem | Roots at `+/-1` are assigned only to `I_0`; empty/singleton pieces and arbitrary endpoints pass; event overlap is allowed | step-local | None |
| Class supremum and empty-index convention | Scope bridge from per-law estimates to the class-uniform root-hitting constant | Division by positive `length(I)`, supremum monotonicity, and the binding empty-supremum convention | `step_005` and the exact definition in `setting.md` | A uniform bound independent of `mu,I` passes directly through both suprema | Same law class, interval family, probability mode, and root-hitting ratio as the target | `mu(H_I)/length(I) <= M_eta` for every indexed pair, hence `C_D <= M_eta`; if an index set is empty, `C_D=0` | Raw control is the exact per-law bound; exported interface is the exact class constant, not a surrogate capacity | Zero residual; the same scalar ratio is consumed | No accumulation across indices; one pointwise uniform inequality applies to each | `step_005` -> `step_006` -> final theorem; finiteness of `C_D` is never assumed | Producer `step_006`; consumer final root-hitting theorem | Empty law class and empty positive-length interval family pass by the stated value-zero convention; nonempty cases use `length(I) > 0` | step-local | None |
| No-hidden-constant polynomial specialization | Quantitative specialization for the indexed material-partial result | Fixed `eta`, `bar kappa_i <= bar kappa_*`, `1 <= d`, and `d(d-1) <= d^2` | `assump:indexed-regime` and `step_005`/`step_006` | Pointwise scalar domination has the exact claim type and retains every exposed parameter | Same `d,R,eta`, deterministic mode, interval-length norm, and class target; no probability or horizon conversion | `M_eta <= bar kappa_* d + (bar kappa_*/2) R d^2 = P_eta(d,R)` | Raw controls are the two exact chart constants; export is the displayed polynomial with coefficients depending only on fixed `eta` | Same-target scalar inequality; zero residual and no dropped term | One pointwise specialization at each index; no asymptotic, limiting, or accumulated error | Primitive fixed-parameter convention + `step_005` -> `step_006` -> final theorem | Producer `step_006`; consumer final polynomial claim | `d = 1`, `R = 1`, and `bar kappa_* = 0` pass; no nonemptiness threshold enters | step-local | None |
| Degree-one boundary specialization | Boundary/baseline-specialization audit ensuring all identities and interfaces survive `d = 1` | Empty-sum conventions and the sole-coefficient conditional density | Setting definitions plus `step_001`, `step_002`, `step_004`, `step_005`, `step_006`, and `step_007` | Both chart sources remain active and prove the same root event; no higher-degree term is silently required | Both pivots are exactly `alpha_0`; both conditioning tuples are empty; polynomial is `theta + alpha_0` in the same metric | `s_0=s_infty=-theta`, `B_0=B_infty=1`, and both caps equal the sole density norm | Raw control is the sole density and exact linear pivot; exports are the weighted/max bound, class bound, and uniform witness membership | Produced and consumed coefficient/root objects are identical; no surrogate or remainder | No degree-recursive argument and no vanishing mechanism defect | Primitive degree-one model -> the same numbered steps -> both final clauses | All chart producers and consumers remain legal; witness producer is `step_007` | Arbitrary endpoint pieces, `theta=+/-1`, empty outer/inner pieces, and threshold equality pass | step-local | None |
| Indexed witness membership and threshold isolation | Static membership certificate proving nonvacuity only under the separate clause | Independent `Unif[-R,R]` endpoint innovations and the explicit Rademacher middle block | Exact witness construction in `setting.md`; `assump:indexed-regime`; separate clause hypothesis only | Independence leaves each pivot conditionally uniform; this directly supplies both required density caps and cube support | Produced law is exactly the law consumed by the class-membership claim; no affine, transformed, population, or reference surrogate | `K_0=K_infty=1/(2R) <= 1/2 <= bar kappa_i` | Raw controls are endpoint independence, uniform densities, support, and `R >= 1`; export is exact membership for every indexed `d,R` | Zero object-target residual; singular/dependent middle coordinates do not alter endpoint kernels | Static construction at each index; no membership defect accumulates | Explicit random variables -> `step_007` -> final nonemptiness clause; no path to the general bound chain | Producer `step_007`; consumer only the final nonemptiness clause | Separate checks for `d = 1`, `d = 2`, `d = 3`, `d >= 4`, `R = 1`, and cap equality pass; singularity/dependence is harmless | step-local | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is exact-goal aligned, acyclic, and complete at obstruction-level granularity. Every theorem-critical output has a source-adequate current-notation mechanism, a feasible exported interface, zero or explicitly harmless residual, a legal producer-consumer path, and the required boundary audit. All hard obligations are local derivations under the unchanged theorem contract, so the sketch is ready for theorem-level diagnostic and subsequent step proof work.
