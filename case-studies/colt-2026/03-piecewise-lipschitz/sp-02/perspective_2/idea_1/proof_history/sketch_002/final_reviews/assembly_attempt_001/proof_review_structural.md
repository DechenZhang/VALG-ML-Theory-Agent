# Structural Proof Review

## Reviewer Verdict

PASS

## Blocking Issues

None

## Nonblocking Concerns

1. In the `step_012` appendix proposition, the phrase `exact area/multiplicity multiplicity` repeats the final noun across a line break. This is editorial only: the complete area-formula, image-multiplicity, density-domination, exhaustion, and root-coverage derivations are present earlier and are cited precisely in the proposition proof.

## Suggested Repair Depth

None

## Smallest Candidate Repair Target

None

## Structural Audit

- Goal alignment: PASS. This is exact-goal mode. The public theorem preserves the five inseparable clauses in `setting.md`: the anchored matrix/projective certificate, arbitrary-partition affine chart and coordinate-free bounds, the sharper homogeneous bound, the original-coordinate deterministic-leading-coefficient monic specialization, and the Counter-example 1 scale audit. It also preserves the fixed-presentation-before-law-and-interval quantifier order, arbitrary full-joint coefficient correlation, ordinary-probability and interval-before-law supremum modes, literal Euclidean norm and measure conventions, all endpoint/null/dimension branches, and the boundary that no unrestricted raw Pfaffian presentation is claimed to admit the supplied derivative-closure certificate or polynomial format control. No assumption, regime, target, coefficient dimension, or quantifier is strengthened or narrowed.
- Dependency closure: PASS. All 12 current proof/review pairs bind sketch attempt 2, their listed step and unit attempts, their live proof digests, and raw `ACCEPTED` status. The dependency graph is acyclic: `step_001`; `step_002 <- step_001`; `step_003 <- step_001,step_002`; `step_004 <- step_003`; `step_005`; `step_006 <- step_001`; `step_007 <- step_004,step_005,step_006`; `step_008 <- step_001,step_004,step_005`; `step_009`; `step_010 <- step_003,step_009`; `step_011 <- step_001,step_008`; and `step_012 <- step_001,step_003,step_007,step_008,step_010,step_011`. No final block consumes a downstream, stale, unavailable, or merely transitive interface. In particular, the complete monic package reaches the theorem through the named `step_010` wrapper rather than bare `step_009` ancestry.
- Sketch-step coverage: PASS. The appendix maps all accepted interfaces one-to-one to mathematical subsections: `step_001` Static Certificate and Projective Closure (5 units), `step_002` Persistent-Root Nullity (3), `step_003` Measurable Affine Pivot Sweep (5), `step_004` Coordinate-Free Section Parametrization (4), `step_005` Translated Cube Sections (3), `step_006` Affine Root-Section Velocity (3), `step_007` General Affine Rate (3), `step_008` Homogeneous Projective Rate (5), `step_009` Exact Monic Presentation (7), `step_010` Affine-Monic Probability Recovery (4), `step_011` Counter-example 1 Scale Calculation (6), and `step_012` Anchored Coefficient-Sweep Theorem (1). Every subsection carries its Target-Step Assembly as concluding proof text. The theorem-block map is exact: `step_001` supplies clause 1; `step_003` and `step_007` supply clause 2; `step_008` supplies clause 3; `step_010` supplies clause 4; `step_011` supplies clause 5; and `step_012` conjoins those outputs and their dependence/mode boundary.
- Assembly discipline: PASS. The `step_012` proposition names the six direct producers and, inside those interfaces, every consumed theorem-style conclusion, including the `step_007` zero-certificate branch, `step_008` actual-section radial cancellation, and `step_010` complete monic wrapper. Its proof performs only exact conjunction and object-identical restatement. The final theorem proof then applies that proposition to the identical presentation and matches its six conclusion groups to the public clauses. The additional identity `sqrt(2) widehat Lambda_{B,T}=sqrt(2)/delta` is direct scalar substitution from the accepted `1/delta` certificate and is explicitly separated from both probability coefficients. Assembly introduces no lemma, inequality, cited theorem, assumption discharge, mode conversion, hidden constant, changed step statement, or stronger conclusion.
- LaTeX bundle structural synchronization: PASS. The public surface is exactly the nine required files: the five ordered content files, `main.tex`, `reference.bib`, `arxiv.sty`, and `ims.bst`. `main.tex` loads the style, inputs all five content files, and invokes the bibliography. The required setup, preliminaries, main-theorem, proof-sketch, appendix/proof-details, 12 step subsections, and final main-theorem proof appear in order. A fresh temporary-copy Tectonic build converged to a 399 KiB PDF; the final log and bibliography log contain no unresolved citation/reference, multiply-defined label, or substantive warning. Public-source scans found no workflow paths, audit scaffolding, template placeholders, forbidden claim/hypothesis/invariant environments, malformed reference commands, or prose-only asymptotic/absorption language.
- Main theorem self-containedness: PASS. Relative only to the four numbered primitive assumptions and the compact preliminary notation, the theorem defines every theorem-local object and specialization before use. Its five substantive clauses and final dependence paragraph expose the law class, interval scope, probability mode, supremum order, Euclidean norms and measures, literal constants, zero-certificate/stationary branches, and nonoutput boundary. Although necessarily long because the formalized goal makes all five clauses inseparable, it is organized by reader-facing mathematical clause labels and does not depend on an admissibility dictionary, appendix-local constant, proof-step ID, or appendix theorem reference. A separate simplified corollary is therefore not needed to make the interface readable.
- Theorem-style statement shape: PASS. Each appendix result states numbered assumptions or named prior theorem-style results, then local conditions, then its conclusion. No mathematical argument treats a subsection, step ID, local unit ID, review decision, or workflow artifact as proof authority. The sole source `claim` in `step_009` unit 007 is rendered as a public `lemma` with the identical statement and proof, and all downstream references are consistently updated; this is the required public-environment conversion, not a new result. The final theorem itself names no appendix-local result or constant.
- Explicit-rate and bridge structure: PASS. Every displayed coefficient is literal, with no hidden constant: the affine coefficient is `A(1+NR^2) widehat Lambda_{B,T}/(sqrt(2)R)`, the homogeneous coefficient is `A sqrt(N/2) Gamma_proj(F)` and then `A sqrt(N/2) widehat Lambda_{B,T}`, the monic coefficient is `kappa(2R)^(d-1)(d+Rd(d-1)/2)`, and the Counter-example scales remain `1/(4delta)`, `1/delta`, and the separate geometric `sqrt(2)/delta`. The proof supplies the actual-section velocity and cube-section bridges, the literal coefficient algebra, the interval and law supremum order, the zero-certificate branch, and the original-coordinate monic specialization directly from the common chart theorem. The monic bridge retains beta volume, disjoint partition lengths, negative-power control, `d=1`, singleton/empty intervals, deterministic leading coefficient, and arbitrary correlation. No term is silently dropped or absorbed, and both baseline-invariance obligations are exact.
- Derivation-over-notation: PASS. Preliminaries contain only setting-derived notation needed to read the theorem; exhaustion levels, pivot-local maps, component radii, velocity aliases, and finite-domain bookkeeping stay in their appendix proofs. Every public constant and bounded quantity is defined from primitive setting data or proved in a named result. The appendix displays the pivot-exhaustion/area chain, root-section Jacobian and Hausdorff geometry, homogeneous radial cancellation, derivative-shift calculation, original-coordinate monic pivots, Counter-example wedges, and final producer conjunction rather than replacing these derivations with helper dictionaries or provenance prose.
- Appendix local-unit coverage: PASS. I compared the actual source statement and derivation of all 49 accepted local units, every used cited-result application, and all 12 Target-Step Assembly sections against the corresponding appendix bodies, in the source counts `5,3,5,4,3,3,3,5,7,4,6,1`. The `step_003` comparison includes measurable finite exhaustion, exact chart determinant, Federer area formula mapping, multiplicity, joint-density domination, monotone convergence, persistent-root removal, tangent/multiple roots, endpoints, and `N=1`; `step_004` includes the fixed-section change of variables and exact normal-velocity identity; `step_005` includes Brunn-Minkowski central maximality, Ball's cube-section theorem, scaling, empty/support endpoints, and dimension one; `step_008` includes actual-root-section radial cancellation, stationary and zero-certificate branches, and literal coefficient algebra; `step_009` and `step_010` include the augmented monomial tuple, constant shift, deterministic leader, original coefficient order, both pivot charts and insertion maps, negative-power chain, beta volume, interval-location and `d=1` branches, and the complete wrapper; `step_011` includes both closed sign wedges, axes, square and interval endpoints, exact area/probability, and the distinct scale chain; `step_012` includes the six-producer conjunction. The Balcan convention citation, Federer area formula, Brunn-Minkowski theorem, and Ball cube-slicing theorem are each restated or mapped with their used hypotheses discharged and backed by one of the four bibliography entries. Apparent compression removes workflow metadata, dependency inventories, or duplicated setup only. Repeated mathematics is abbreviated only after a complete displayed instance with an exact result/equation reference and stated substitution. No substantive definition, equation chain, case, cited-result discharge, or target assembly is missing.
- Internal label/ref structure: PASS. The public sources contain 540 unique labels, including 462 equation labels; all 84 distinct `ref`/`eqref` targets resolve, and all four distinct citation keys have BibTeX entries. Begin/end environments are balanced, theorem-style labels are unique, and mathematical dependencies name their result types. The clean compile confirms the static scan. The public reference discipline therefore closes internally without bare references, citation-only proof authority, duplicate labels, or an empty bibliography.
