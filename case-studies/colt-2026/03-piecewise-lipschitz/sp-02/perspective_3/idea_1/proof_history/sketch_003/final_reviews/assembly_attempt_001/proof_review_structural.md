# Structural Proof Review

## Reviewer Verdict

BLOCKING

## Blocking Issues

1. The public main-theorem source is malformed at `latex_template/3_main.tex:27`. The intended inner-supremum qualifier `I\ {\rm an\ interval}` is stored as `I\ {<0x0D>m an\ interval}`: a literal carriage-return byte replaces the backslash before `rm`. TeX accepts that byte as an end-of-line and therefore does not parse `\rm`; the compiled qualifier is mathematical `m an interval`, not the stated reader-facing phrase `an interval`. This corrupts an exact quantifier qualifier in (T2), violates public-source/control-byte hygiene, and is not cured by successful compilation. The smallest repair is to replace that single malformed byte sequence in the bundle, rerun the canonical build and reference checks, and synchronize the assembly report; no accepted sketch or step proof needs to change.

## Nonblocking Concerns

1. `assembly_report.md` says that Tectonic completed "without a warning," whereas the independent controller build of the same canonical nine-file bundle produced two nonfatal `wasysym` font-substitution warnings. The 26-page build otherwise completed with resolved references and citations, and these font substitutions do not change a theorem, proof dependency, constant, or quantifier, so the warnings themselves are nonblocking. The report/source-log discrepancy should be stated accurately when `/proof-assembly` is rerun.

## Suggested Repair Depth

assembly

## Smallest Candidate Repair Target

/proof-assembly

## Structural Audit

- Goal alignment: Apart from the malformed interval qualifier in (T2), the bundle states the exact unconditional `material_partial` goal in `setting.md`. It proves fixed-family finiteness of `Gamma_piv`, the pairwise arbitrary-correlated-full-joint-density bound, the ordered interval-then-law suprema with literal empty-index semantics, the exact Counter-example 1 scale certificate, and the affine-monic specialization. The introduction, theorem, monic bridge, and final proof explicitly preserve the source boundary: no polynomial general-instance or Pfaffian-presentation-format control of `Gamma_piv` is claimed.
- Dependency closure: All six review identities were checked before their proofs were consumed. Each review records sketch attempt 3, its correct `step_001` through `step_006` ID, unit attempt 1, the current proof SHA-256, raw `ACCEPTED` status, `Blocking Issues = None`, and `Smallest Retry Target = None`. The graph is exactly `step_001`; `step_002 <- step_001`; `step_003 <- step_001,step_002`; `step_004 <- step_001,step_003`; `step_005 <- step_001`; `step_006 <- step_001,step_004`. It is acyclic and earlier-only. In particular, strict finiteness flows directly from `step_001` to `step_004`; `step_003` exports only event measurability and coefficient volume. No history artifact or stale final review was used as proof authority. The accepted global pair and assembly report were used only as diagnostic context.
- Sketch-step coverage: The accepted sketch has exactly six rows, and all six are used. The appendix has one mathematically named subsection per row, in dependency order, and contains all 24 accepted local units plus concluding proof text for every Target-Step Assembly. The six additional subsection-closing propositions only compose the accepted named results; they add no independent lemma, assumption, inequality, or mode upgrade.
- Assembly discipline: No substantive unreviewed proof obligation was introduced. The final theorem proof cites public propositions in the accepted dependency order and keeps the pairwise `step_004` result distinct from its ordered-supremum semantics. The sole blocking assembly defect is the carriage-return corruption in the public theorem source. The warning-free claim in the report is a separate nonblocking reporting defect.
- LaTeX bundle structural synchronization: Exactly the required nine files are present. `main.tex` loads the style, inputs the five content files in order, and invokes `ims.bst` and `reference.bib`. The controller's canonical Tectonic build produced 26 pages with resolved references and citations. The source scan found no placeholders, workflow paths, accepted-step narration, forbidden audit scaffolding, public `claim`/`hypothesis`/`invariant` environments, or workflow-style theorem titles. The one disallowed control byte is the blocker identified above.
- Main theorem self-containedness: `1_intro.tex` defines the primitive objects, full joint law class, density cap, `A`, Pfaffian convention, and three numbered primitive assumptions. `2_preliminary.tex` defines only theorem-facing `K_R`, `V_j`, `Gamma_piv`, the scale family, and the monic family. `3_main.tex` exposes all variables, fixed-family scope, ordinary-probability mode, interval scope, norm/measure modes, arbitrary correlation, deterministic monic leader, and absence of hidden constants. It is readable and not dominated by a helper dictionary. Its mathematical content matches the setting, but (T2)'s malformed source qualifier prevents structural acceptance.
- Theorem-style statement shape: Every appendix dependency is a lemma or proposition with an explicit assumption basis or named prior results, followed by local conditions and a conclusion. The public theorem does not depend on appendix-local labels in its statement. Generated facts such as pivot availability, event measurability, volume control, law-class nonemptiness, interval-class nonemptiness, and hyperplane nullity are proved before use rather than promoted to assumptions.
- Explicit-rate and bridge structure: The affine constant is exactly `kappa(2R)^(N-1) Gamma_piv |I| = A Gamma_piv |I|/(2R)`. The finite-capacity branch is exactly `A >= 1` and `|Theta| > 0`; every other index-empty branch retains `-infinity` without being called a capacity. The scale specialization is exactly `Gamma_piv = 1/delta`, including `theta=0` and `delta=1`. The monic bridge keeps only the `d` lower coefficients random, proves `Gamma_piv <= d + R d(d-1)/2`, and gives exactly `kappa(2R)^(d-1)(d + R d(d-1)/2)|I|`; setting `d=6` yields the direct fixed degree-six instance `Gamma_piv <= 6+15R` and coefficient `kappa(2R)^5(6+15R)`. There is no absorption, hidden constant, confidence conversion, horizon upgrade, independence premise, or conservative baseline loss.
- Derivation-over-notation: The appendix retains the displayed feasibility inequality, compact positive-pivot margin, quotient-rule bounds, extended-real selector formula, weak denominator exhaustion, original-coordinate graph identity, exact velocity, analytic/completion construction, determinant calculation, Lipschitz localization, extended-multiplicity area formula, Tonelli and monotone limits, disjoint-cell length identity, density integration, nonemptiness equivalences, specialized pivot calculations, compact localization, and affine-hyperplane nullity. Proof-local controls remain local and do not replace these derivations.
- Appendix local-unit coverage: `step_001` preserves all three local derivations and both feasible-set branches. `step_002` preserves all five units, including ties, selected/unselected zeros, endpoints, empty `K_R`, and `N=1`. `step_003` preserves all four units and the invoked Kechris 14.4, Kechris 21.10, Federer 3.2.3, localization/extension, extended multiplicity, exact coverage, compactness/exhaustion, and coefficient-volume arguments. `step_004` preserves all four units, full-density completion, exact constants, law and interval nonemptiness proofs, ordered suprema, and every vacuous branch. `step_005` preserves all three exact scale-stress units and its Target-Step Assembly. `step_006` preserves all five monic units, deterministic leading coefficient, original coefficient space, both pivot regions, `d=1`, `d=2`, the all-`d` result (hence `d=6`), positive-length localization, empty/singleton intervals, arbitrary correlation, and its Target-Step Assembly. No substantive source derivation or boundary regime was replaced by summary prose.
- Internal label/ref structure: The public content has 33 unique labels, 33 distinct resolved internal references, two citation keys, and two matching BibTeX entries. There are no duplicate labels, missing internal targets, missing citation entries, bare malformed `ref`/`cite` fragments, or unresolved compile references/citations. The only ASCII/control-byte failure is the single `0x0D` at `3_main.tex:27`.
