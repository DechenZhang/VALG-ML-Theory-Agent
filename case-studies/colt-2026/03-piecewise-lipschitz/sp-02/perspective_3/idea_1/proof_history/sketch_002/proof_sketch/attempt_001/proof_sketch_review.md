# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 2
- Attempt type: Target-preserving same-setting sketch repair after aggregate `PROOF_SKETCH_FLAW`
- Target mode: Exact-goal, unconditional, `material_partial`
- Reviewed sketch SHA-256: `aabcf6f6302d5cfab51cc1495d683cfa353390df7bf0fa81c3beecfcb076d108`
- Reviewed roadmap: `step_001` derives root feasibility, a uniform available-pivot margin on \(K_R\), and finiteness of \(\Gamma_{\rm piv}\); `step_002` constructs the Borel least-minimizer partition, exhaustion, graph maps, and exact derivative interface; `step_003` proves analytic/Lebesgue measurability, graph-image coverage, and the coefficient-volume bound; `step_004` converts volume to probability under the full joint-density cap; `step_005` proves exactly \(\Gamma_{\rm piv}(b_\delta,F_\delta;1)=1/\delta\); and `step_006` derives the exact affine-monic specialization from the general theorem.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch restores the exact theorem in `setting.md`, including \(\Gamma_{\rm piv}<\infty\), the law- and interval-uniform affine bound with no hidden constant, only the static equality \(\Gamma_{\rm piv}(b_\delta,F_\delta;1)=1/\delta\), and literal recovery of \(\kappa(2R)^{d-1}(d+Rd(d-1)/2)|I|\). The former extra scale-family probability target is absent from the formal goal, roadmap, R2, witnesses, interfaces, flow, `step_005`, dependencies, and assembly. The branch preserves the deterministic monic coefficient outside the random law, remains `material_partial`, and expressly leaves polynomial general-instance control of \(\Gamma_{\rm piv}\) open.
- Dependency audit: The graph `step_001`; `step_002<-step_001`; `step_003<-step_001,step_002`; `step_004<-step_003`; `step_005<-step_001`; `step_006<-step_001,step_004` is acyclic, earlier-only, and synchronized across all producer-consumer tables. Attempt-1 global, step, assembly, and final-review artifacts are declared stale. Fresh attempt-2 step dependencies must carry current 64-hex identities, so no stale structural digest is accepted as current evidence.
- High-risk coverage: The sketch localizes compact pivot finiteness, Borel selector and exhaustion measurability, analytic-set measurability, graph-image coverage, multiplicity-safe area control, the no-chart-count-loss identity, arbitrary correlated-density conversion, the exact static scale certificate, compact localization, and exact affine-monic recovery into lemma-sized steps. Tangencies, multiple or infinite fibers, pivot zeros, \(K_R=\varnothing\), \(N=1\), \(d=1\), \(\theta=0\), and \(|\theta|=1\) are all assigned explicit handling.
- Explicit-rate coverage: R1--R3 state all exposed variables, absence of hidden constants, fixed quantities, probability mode, horizon mode, norm mode, and specialization obligations. No term is discarded asymptotically: the conversions are the exact cube volume, \(\kappa(2R)^{N-1}=A/(2R)\), the exact \(1/\delta\) conditioning value, and literal substitution of the monic \(\Gamma_{\rm piv}\) bound.
- Assumption and citation plausibility: The theorem-facing assumptions are exactly the primitive stable IDs `assump:shared-pfaffian-chain`, `assump:no-forced-root`, and `assump:joint-density-cap`; all other controls have legal producer steps. Source preflight confirms Kechris, first edition (1995), Section 14, Proposition 14.4 (p. 86), for Borel images of analytic sets; Kechris, Section 21, Theorem 21.10 (p. 155), for universal measurability of analytic sets; and Federer (1969), Section 3.2.3 (p. 243), for the Lipschitz area formula on Lebesgue-measurable restrictions with multiplicity. The sketch maps the branch objects and conventions to these statements and records their nonoutputs.

## Early Obstruction Audit

- Limiting-case stress: The \(K_R=\varnothing\) branch gives an empty supported-root event and \(\Gamma_{\rm piv}=0\). When \(K_R\ne\varnothing\), a simultaneous zero of \(F\) would force \(b=0\), so `assump:no-forced-root` excludes the only null-source state that would invalidate a finite linear interval ratio. The \(N=1\) beta fiber has volume one; tangent and multiple roots remain covered by image measure; and \(\delta=1\), \(d=1\), \(\theta=0\), and \(|\theta|=1\) give the stated formulas. No allowed boundary regime breaks the target.
- Theorem-critical bridge support: Every bridge is present before consumption: cube support gives root feasibility; compact nonvanishing gives \(\rho>0\); quotient bounds give finite \(\Gamma_{\rm piv}\); finite Borel minimization gives the cells; exact differentiation gives the Jacobian control; the area formula gives coefficient volume; the joint density cap gives probability; and direct specialized algebra gives the scale and monic clauses.
- Exported-interface feasibility: For `step_001`, the raw controls \(K_R,B_0,B_1,\rho\) export finite \(\Gamma_{\rm piv}\) through the displayed quotient bound. For `step_002`, the raw controls are the finite extended \(V_j\), least-index rule, and \(|\beta_i|\le R\), exporting the exact original-coordinate graph and derivative inequality. For `step_003`, the raw graph maps and area formula export \(\lambda_N(S_I)\le(2R)^{N-1}\Gamma_{\rm piv}|I|\) through exact event coverage, monotone exhaustion, and the disjoint-cell length identity. `step_004` uses the full joint density directly, and `step_005`--`step_006` use exact specialized objects. Every controlled and boundary defect class has a source; no unrelated residual is assigned to a parameter threshold.
- Theorem-critical mechanism witness gate: All six step witnesses identify the claim class, theorem role, mechanism source, source-to-claim match, control term, opposing defects, concrete dominance or limiting relation, boundary stress, and producer-consumer path. Compactness supplies a positive pivot margin rather than an upper-bound-only surrogate; the area formula supplies image measure rather than expected multiplicity only; and direct polynomial identities supply the baseline rather than a separate baseline citation. All witnesses are source-adequate and scope-compatible.
- Entry-state trace stress: No recursive algorithm, dynamical process, or generated trajectory occurs, so the Entry-State / Activation Trace Gate is not triggered. The only limiting construction is the static nested exhaustion \(E_{j,m}\uparrow E_j\); each root with selected \(F_j\ne0\) enters some finite level, the graph map is already active there, and continuity from below plus monotone convergence gives the limiting image and integral. Static exact and baseline specializations are separately checked below.
- Obligation locality classification: Pivot finiteness (`step_001`), Borel selection and exhaustion (`step_002`), graph-image area control (`step_003`), density conversion and uniform suprema (`step_004`), the \(1/\delta\) audit (`step_005`), and exact affine-monic recovery (`step_006`) are each `step-local`. Their mechanism sources and interfaces are fixed in the sketch; none requires a changed assumption, dependency, scope, metric, probability mode, exposed dependence, or conclusion. There is no `sketch/interface defect` or `idea/theorem-contract defect`.
- Noncircular closure gate: The paths are primitive assumptions \(\to\) `step_001` pivot controls \(\to\) `step_002` chart controls \(\to\) `step_003` volume control \(\to\) `step_004` probability control, followed by independent specialized calculations. Neither pivot availability, event volume, nor the probability theorem is assumed in an admissibility package. The exhaustion limit is proved from nested domains and nonnegative Jacobian integrals, not assumed as event closure.
- Mechanism-source and boundary stress: On \(K_R\), \(\rho=\min_{K_R}\max_j|F_j|>0\) supplies the needed lower/nondegeneracy source. At pivot ties, the least index is Borel and unique; at a selected-pivot zero, \(V_j=+\infty\), so that coordinate cannot win against an available finite pivot. Tangent maps have zero Jacobian and hence null image contribution under the area formula. The constant pivot activates at \(\theta=0\), and the top monomial pivot is used only for \(|\theta|>1\), where it is nonzero.
- Generated-output flow: Each theorem-facing derived output has a legal producer, consumers, final use, and dependency path. In particular, no chart is consumed before `step_002`, the coefficient-volume certificate is consumed only after `step_003`, the affine probability theorem is produced by `step_004` before the monic specialization, and final assembly consumes only the outputs of `step_004`--`step_006`.
- Source-to-claim adequacy: `assump:no-forced-root` plus compactness supplies a genuine positive maximum-coordinate lower bound on \(K_R\), which is the right claim type for denominator control. Kechris 14.4 applies to the Borel domains/incidence set as analytic subsets of Polish ambient spaces and to the Borel chart maps/continuous projection; Kechris 21.10 then gives universal and hence Lebesgue measurability in Euclidean coefficient space. Federer 3.2.3 has the same equal-dimensional Euclidean domain, target, metric, and normalized Hausdorff/Lebesgue convention as the branch, and \(J_N\Psi_j=|\partial_\theta T_j|\); countable Borel Lipschitz localization inside \(\{F_j\ne0\}\) discharges its global-Lipschitz hypothesis. The density cap is on the full joint density, and the monic coordinate ordering differs from the source display only by a coordinate permutation.
- Residual-to-target adequacy: Every bridge is same-target. \(T_j\) solves the original root equation and \(\Psi_j\) inserts it into the original coefficient vector; hence \(S_I=\bigcup_{j,m}\Psi_j(D_{j,m})\) exactly. Volume and probability concern the same measurable event. The scale certificate produces the conditioning functional that the goal consumes, with no distributional bridge. Finally \(b_d+\langle\alpha,F_d\rangle=p_\alpha\) identically in the same interval and probability space. All residuals are zero.
- Baseline invariance audit: The inherited conclusion is the stated monic Theorem 2 bound for only the \(d\) random lower coefficients. The tested specialization is \(b_d=\theta^d\), \(F_d=(1,\theta,\ldots,\theta^{d-1})\), including \(d=1\), \(\theta=0\), \(|\theta|=1\), and arbitrary bounded \(I\subset\mathbb R\). Constant and top pivots give the global pointwise speed bound, compact localization invokes `step_004`, and exact object equality yields precisely \(\kappa(2R)^{d-1}(d+Rd(d-1)/2)|I|\). The original conclusion, distribution dimension, correlation allowance, and constants are preserved with no surrogate or remainder.
- Scope-accumulation compatibility: There is no iterative or all-time state. The only accumulated term is the nonnegative Jacobian integrand under \(m\uparrow\infty\); its sign is controlled, the domains and images are nested, and continuity from below and monotone convergence yield the exact limiting integral. The finite chart budget is \(\sum_j|I\cap E_j|=|I\cap K_R|\le|I|\), so no persistent chart-count defect accumulates. Suprema over laws and intervals are taken only after a pairwise uniform inequality and add no error.
- Scope and dependence consistency: The theorem remains ordinary-probability, law-uniform, interval-uniform, and exact-constant. It preserves \(N,R,\kappa,A,\Gamma_{\rm piv},|I|\), records \(q,M,\Delta_{\rm rnd},\Delta_{\rm aff}\) as fixed descriptors, and makes no hidden polynomial-complexity claim. The affine extension and the `material_partial` label are authorized by the source and selected Gap 3 perspective.
- Generated-condition provenance: Root feasibility, pivot availability, the Borel partition, event-volume control, and specialization certificates are all derived outputs with named producers. No realized event, stability condition, local-validity condition, invariant, or theorem conclusion is moved into the primitive assumptions.
- Citation and tool applicability: Kechris 14.4 states that a Borel image of an analytic subset of a Polish space is analytic, matching both chart images and the incidence-set projection; Kechris 21.10 states that every analytic subset of a standard Borel space is universally measurable, yielding the required Lebesgue-measurable events. Federer 3.2.3 states \(\int_A J_m f\,d\mathcal L^m=\int N(f|A,y)\,d\mathcal H^m(y)\) for Lipschitz \(f:\mathbb R^m\to\mathbb R^n\), \(m\le n\), and Lebesgue-measurable \(A\). At \(m=n=N\), multiplicity at least one on the image gives \(\lambda_N(f(A))\le\int_A|\det Df|\) without injectivity, finite fibers, regular values, transversality, or simple roots. Kechris supplies no volume estimate, and Federer supplies no event coverage, chart selection, derivative bound, density conversion, or conditioning control; the sketch supplies those interfaces separately.
- Same-setting repair plausibility: No material obstruction requiring repair was found. Routine local details such as handling endpoints of a compact interval in the area-formula localization and verifying analytic-image measurability are already assigned within `step_003`/`step_004` and use the stated \(C^1\), Borel, and Euclidean-measure interfaces without changing the theorem contract.
- Target-preserving bridge-repair gate: All required target-preserving bridges are already explicit. No weakening, new primitive assumption, new mechanism source, changed metric or probability mode, or conservative loss is needed.
- High-risk obligation classes: Structural lower/nondegeneracy control passes through \(\rho>0\); approximation and perturbation residuals are absent; static limiting closure passes by monotone convergence; the volume-to-probability and pairwise-to-uniform scope transitions are exact; explicit dependence and baseline specialization are complete; and no hidden generated condition remains.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Root feasibility, positive available-pivot margin, and finite \(\Gamma_{\rm piv}\) | Structural nondegeneracy and finite theorem constant | Cube support, compact \(K_R\), `assump:no-forced-root`, and \(C^1\) bounds | Primitive assumptions; direct extreme-value and quotient-rule derivation in `step_001` | \(F\ne0\) on \(K_R\) gives the lower source needed for denominators, not merely an upper bound | Same scalar \(\theta\), same \(b,F\), same \(\ell_1\) feasibility convention; Pfaffian data are used only to supply \(C^1\) regularity | \(\rho=\min_{K_R}\max_j\lvert F_j\rvert>0\) and \(\min_jV_j\le2B_0B_1\rho^{-2}(1+R(N-1))\) | Raw \(K_R,B_0,B_1,\rho\) export root feasibility and finite \(\Gamma_{\rm piv}\); no polynomial instance-complexity export is claimed | Same \(b,F,K_R,V_j\); zero residual and no surrogate | Static compact-set control; individual small pivots are avoided pointwise; no accumulated defect | Primitive assumptions \(\to\) `step_001` before any chart or probability consumer | `step_001`; consumed by `step_002`--`step_006` and final finiteness clause | \(K_R=\varnothing\) gives zero event; simultaneous \(F=0\) on nonempty \(K_R\) is excluded; allowing a forced root would falsify the target | `step-local` | None |
| Borel least-minimizer cells, exhaustion, exact graph map, and Jacobian bound | Generated chart certificate and downstream area-formula interface | Borel extended \(V_j\), finite least-index minimization, `step_001` pivot availability, quotient differentiation, and \(\lvert\beta_i\rvert\le R\) | Direct standard Borel construction and current-notation calculus in `step_002` | A finite nonzero pivot supplies the exact graph; the quotient rule supplies the exact claim type \(\lvert\partial_\theta T_j\rvert\le V_j\) | Original coefficient coordinates and scalar derivative; no metric, representation, or target change | \(\partial_\theta T_j=-(b/F_j)'-\sum_{i\ne j}\beta_i(F_i/F_j)'\), hence \(\lvert\partial_\theta T_j\rvert\le V_j\le\Gamma_{\rm piv}\) | Raw finite \(V_j\), tie rule, cube bound, and \(E_{j,m}=E_j\cap\{\lvert F_j\rvert\ge1/m\}\) export disjoint Borel cells and an exact graph interface | \(T_j\) equals the original \(\alpha_j\) at a root; zero residual | Static finite partition; \(E_{j,m}\uparrow E_j\); no signed forcing or chart duplication | `step_001` pivot output \(\to\) `step_002` cells and derivative \(\to\) `step_003` | `step_002`; sole theorem-critical consumer is `step_003` | Ties use the least index; a zero pivot has \(V_j=+\infty\); every selected nonzero pivot enters some finite exhaustion level | `step-local` | None |
| Analytic/Lebesgue-measurable root-event coverage and coefficient-volume bound | Theorem-critical measurability, image measure, limiting closure, and no-chart-count-loss certificate | Kechris analytic-set results, Federer area formula, `step_002` derivative control, monotone convergence, and disjoint \(E_j\) | Kechris (1995), Section 14, Proposition 14.4 (p. 86), and Section 21, Theorem 21.10 (p. 155); Federer (1969), Section 3.2.3 (p. 243); direct measure theory in `step_003` | Kechris makes the Borel chart images and root-event projection analytic and then Lebesgue measurable; Federer controls image measure with extended multiplicity, not merely an expected count or injective change of variables | The source spaces are Polish/standard Borel; the chart/incidence domains and maps are Borel; both area-formula spaces are Euclidean \(N\)-spaces with normalized \(\mathcal H^N=\lambda_N\), and \(J_N\Psi_j=\lvert\partial_\theta T_j\rvert\) | Federer gives \(\lambda_N(\Psi_j(D_{j,m}))\le\int_{D_{j,m}}\lvert\partial_\theta T_j\rvert\); then \(\sum_j\int_{I\cap E_j}V_j\le\Gamma_{\rm piv}\lvert I\rvert\) | Raw Borel domains/maps, incidence projection, countable Lipschitz localizations, beta cube, and derivative bound export measurable \(S_I\) and \(\lambda_N(S_I)\le(2R)^{N-1}\Gamma_{\rm piv}\lvert I\rvert\) | Exact equality \(S_I=\bigcup_{j,m}\Psi_j(D_{j,m})\) in the original coefficient space; zero residual | Nonnegative Jacobian; nested exhaustion; monotone convergence; finite chart budget \(\lvert I\cap K_R\rvert\le\lvert I\rvert\); multiplicities may be infinite | `step_001` \(\to\) `step_002` \(\to\) `step_003`; measurability and volume close before `step_004` | `step_003`; consumed by `step_004` | \(N=1\) uses beta volume one; tangencies, multiple/infinite fibers, endpoints, and ties require no injectivity or transversality | `step-local` | None |
| Full joint-density conversion and uniform affine theorem | Unconditional probability, exact rate conversion, and law/interval scope closure | `assump:joint-density-cap` and `step_003` coefficient-volume certificate | Primitive full joint-density source plus direct Lebesgue integration in `step_004` | A cap on the full density is exactly the source needed for arbitrary correlated laws | The density and volume live on the same original coefficient cube; no marginal, conditioning, or independence convention is introduced | \(\Pr_\mu(S_I)=\int_{S_I}f_\mu\le\kappa\lambda_N(S_I)\) and \(\kappa(2R)^{N-1}=A/(2R)\) | Raw \(\kappa\) and event-volume bound export the pairwise inequality and hence both suprema; correlation is an allowed, controlled class | Identical event under \(\lambda_N\) and \(\mu\); zero residual probability term | No repeated accumulation; a pairwise uniform inequality permits exact suprema | `step_003` volume output plus primitive density cap \(\to\) `step_004` | `step_004`; consumed by `step_006` and final assembly | Empty root event gives zero; right sides above one remain valid; singular laws are correctly outside the primitive class | `step-local` | None |
| Exact \(\Gamma_{\rm piv}(b_\delta,F_\delta;1)=1/\delta\) certificate | Static quantitative conditioning and source-direction audit | Direct derivatives in the fixed deterministic scale-stress family | Specialized family definitions and direct derivation in `step_005`; no law or event theorem is invoked | The two pointwise pivot speeds directly produce the conditioning functional required by the binding goal and nothing stronger | Same scalar \(\theta\), same \(b_\delta,F_\delta\), same interval \([-1,1]\), and same pivot convention | \(V_1(\theta)=1/\delta\) everywhere, while \(V_2(\theta)=\delta/\theta^2\) for \(\theta\ne0\) and \(V_2(0)=+\infty\); hence \(\sup_{[-1,1]}\min(V_1,V_2)=1/\delta\) | Raw \(\delta>0\), exact features, and their derivatives export the exact target value; no distributional raw control or output exists | Produced and consumed objects are the identical conditioning functional; zero residual | Static pointwise calculation; no recurrence, probability defect, or accumulated term | Specialized definitions plus common `step_001` interface \(\to\) `step_005` \(\to\) final assembly | `step_005`; consumed only by the static scale certificate in final assembly | At \(\theta=0\), pivot 1 gives the finite minimum; \(\delta=1\) is valid and \(\delta=0\) is outside the family | `step-local` | None |
| Exact affine-monic \(\Gamma_{\rm piv}\) certificate and Theorem 2 rate | Baseline invariance, same-target bridge, and public quantitative specialization | Constant and top monomial pivots, exact affine identity, compact localization, and `step_004` | Direct polynomial differentiation in `step_006`; the general theorem is the internal probability producer | The two pivots control respectively the small-\(\lvert\theta\rvert\) and large-\(\lvert\theta\rvert\) defects and give exactly the source constant after substitution | Source order \((\theta^{d-1},\ldots,1)\) and branch order \((1,\theta,\ldots,\theta^{d-1})\) differ only by coordinate permutation; the deterministic leading term remains outside the \(d\)-dimensional law | \(V_{\rm const}\le d+Rd(d-1)/2\) on \(\lvert\theta\rvert\le1\), \(V_{\rm top}\le1+Rd(d-1)/2\le d+Rd(d-1)/2\) on \(\lvert\theta\rvert>1\) | Raw monomial ratios, \(N=d\), \(A=(2R)^d\kappa\), and `step_004` export the global \(\Gamma\) certificate and literal \(\kappa(2R)^{d-1}(d+Rd(d-1)/2)\lvert I\rvert\) bound | \(b_d+\langle\alpha,F_d\rangle=p_\alpha\) identically for the same lower-coefficient vector, interval, and law; zero residual | Static two-region split; pointwise global speed has no interval-length or chart-count defect | `step_004` plus direct monic calculation \(\to\) `step_006` \(\to\) final baseline conclusion | `step_006`; consumed by final assembly | \(d=1\) has sole pivot speed one; constant pivot covers zero and \(\lvert\theta\rvert=1\); top pivot is used only where nonzero; bounded intervals localize compactly | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is ready for theorem-level and step-level proof work. Its exact target is source-aligned as `material_partial`, every theorem-critical output has a noncircular and convention-compatible producer path, the only limiting operation has a concrete monotone control relation, and every hard obligation is genuinely `step-local`. The unresolved polynomial general-instance control of \(\Gamma_{\rm piv}\) is expressly outside the binding theorem and is not disguised as future local proof work, so it does not change the acceptance decision.
