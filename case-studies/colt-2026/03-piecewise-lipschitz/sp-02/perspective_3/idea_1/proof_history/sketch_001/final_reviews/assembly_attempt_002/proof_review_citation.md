# Citation Proof Review

## Reviewer Verdict

BLOCKING

## Blocking Issues

1. The accepted `step_003` proof does not identify a verifiable source or result locator for its analytic-set machinery. Its `Analytic-set measurability` application names only a "standard continuous-image and projection theorem for Polish spaces," while `latex_template/5_appendix.tex:530` and `latex_template/5_appendix.tex:541` cite the whole Kechris book without a theorem, proposition, section, or page locator. This machinery is theorem-critical: it supplies analyticity and Lebesgue measurability of both the exhausted chart images and the projected root event. Although the restated claims and current-object hypotheses are mathematically consistent, source fidelity cannot be independently checked from the accepted evidence under the fail-closed citation contract.

2. The accepted `step_003` proof gives only the book-level DOI `10.1002/9780470434642` for the equal-dimensional Lipschitz area formula and describes the convention as merely "supported by" Azais--Wschebor. `latex_template/5_appendix.tex:621` likewise cites the whole book with no result locator. The proof needs the exact multiplicity formula on Borel restrictions of locally Lipschitz maps, with extended-valued multiplicity and no injectivity, finite-fiber, regular-value, transversality, or simple-root hypothesis. The current source evidence does not locate or verify that exact statement, so the theorem-critical inequality
   \[
   \lambda_N(\Psi_j(D_{j,m}))
   \leq \int_{D_{j,m}}J_N\Psi_j
   \]
   is not citation-verified even though the subsequent object mapping, Jacobian calculation, localization, and inequality direction are correct. Repair must add a verifiable source identity and exact result locator, restate the located result faithfully, and preserve its hypotheses and nonoutputs in `step_003`; a fresh `/proof-step-review step_003` is then required before reassembly.

## Nonblocking Concerns

None

## Suggested Repair Depth

step

## Smallest Candidate Repair Target

/proof-step step_003

## Citation Audit

- Cited theorem statements: Public TeX contains exactly three external citation commands. The two `\citep{kechris1995}` uses support (a) Borel images under Borel maps from Euclidean standard Borel domains being analytic, (b) coordinate projections of Borel subsets of Euclidean products being analytic, and (c) analytic subsets of Euclidean space being universally, hence Lebesgue, measurable. The `\citep{azaisWschebor2009}` use supports the equal-dimensional Euclidean area formula with multiplicity and its image-measure consequence. No other external theorem is cited.
- Source fidelity: Both book identities and DOIs are bibliographically coherent, but neither public citation has a result-level locator. More seriously, the accepted `step_003` analytic-set application has no source identity at all, and its area-formula application has only a book DOI rather than a located theorem. The accepted `step_003` review asserts that these tools were checked but supplies no independent source statement or locator; that review cannot replace source verification. These are the two blocking failures above.
- Instantiation and object mapping: For the analytic-set results, `D_{j,m}` is Borel in `\mathbb R^N`, `\Psi_j` is Borel on that domain, and the coefficient-root set `Z_I` is Borel in a Euclidean product; the images and coefficient projection therefore match the restated analytic-set conclusions. For the area formula, the domain coordinates are `(\theta,\beta)\in\mathbb R^N`, the target is the original coefficient vector `\alpha\in\mathbb R^N`, `D_{j,m}` is Borel, and `\Psi_j` is locally Lipschitz on an ambient open set. The determinant calculation gives `J_N\Psi_j=|\partial_\theta T_j|`. Multiplicity is used only through `N(\Psi_j,D_{j,m},\alpha)\geq1` on the image; coverage, selector disjointness, and probability conversion are proved separately and are not attributed to the cited formula.
- Quantitative regime fidelity: The external results contribute no rate constant. The factor `(2R)^{N-1}` comes solely from the beta-cube volume, `\Gamma_{\rm piv}` from the accepted chart-speed bound, and `|I|` from the disjoint-cell length budget. The later density conversion contributes exactly `\kappa`, and `A/(2R)=\kappa(2R)^{N-1}` is algebraic. No cited-result dependence, confidence parameter, horizon condition, multiplicity factor, or hidden constant is dropped. The theorem remains strictly `material_partial`: only fixed-family `\Gamma_{\rm piv}` control is proved, and polynomial general-instance control from Pfaffian-format descriptors remains explicitly unresolved.
- Assumption discharge: The descriptive-set-theory applications discharge standard-Borel/Polish and Borel-map hypotheses using Euclidean domains, Borel intervals/cubes/cells, continuity of affine evaluation, and the ambient `C^1` chart. The area-formula application discharges equal domain/target dimension, Borel restriction, and Lipschitz regularity by a finite disjoint localization inside the nonzero-pivot open set; it computes the full Jacobian and permits multiplicity in `\mathbb N\cup\{+\infty\}`. The `N=1` domain is correctly identified with `\mathbb R\times\mathbb R^0` and uses `\lambda_0(\{()\})=1`. These checks establish a valid current-notation application conditional on a verified source statement; they do not cure the missing source locators.
- Main theorem internal references: `latex_template/3_main.tex` references only the three numbered primitive assumptions in its theorem statement and does not cite appendix-local results, step IDs, or workflow artifacts. Its three conclusions are consumed from the exact public propositions in the final appendix proof. The theorem and setup explicitly state the fixed-family, ordinary-probability, no-hidden-constant scope and the unresolved general Pfaffian-format boundary.
- Appendix cited-result traceability: The internal dependency chain is exact and acyclic. `step_001` supplies root feasibility, the positive available-pivot margin, and fixed-family finiteness; `step_002` supplies Borel selection, exhaustion, the same-object chart, and its speed; `step_003` supplies measurable images, the area bound, exact coverage, and coefficient volume; `step_004` converts that same event by the full joint-density cap; `step_005` proves the scale-stress equalities; and `step_006` proves the exact monic specialization using `step_001` and `step_004`. The appendix statements and uses preserve the accepted assumptions, objects, constants, boundary cases, and nonoutputs. The only failed traceability lies in the two external theorem families inherited from accepted `step_003`.
- External BibTeX entries: The three citation commands use exactly two unique keys, `kechris1995` and `azaisWschebor2009`; both have unique `@book` entries with authors, titles, publishers, years, DOI, and DOI URL. There are no missing or duplicate keys. The bundled `ims.bst` declares `url` and defines `write.url` but never invokes that function, and it does not declare `doi`; therefore a repaired public citation should place its exact result locator in citation prose or an optional citation note rather than relying on these fields to appear in the compiled bibliography.
- Internal label/ref pairs: Static parsing of all six public TeX files found 87 labels, all unique, and 259 reference commands targeting 65 unique labels, with no unresolved target. It found no bare `ref`, `eqref`, `cite`, `citep`, or `citet` fragment. The mathematical dependencies use named assumptions or theorem-style results; no public `claim`, `hypothesis`, `invariant`, step-artifact path, or reviewer decision is used as proof authority. No pre-existing LaTeX build log was available, so this conclusion is based on the complete static source audit.
- Unsupported standard facts: Quotient differentiation, compactness/extreme-value arguments, finite Borel comparisons, Tonelli, continuity from below, monotone convergence, density domination, affine-hyperplane nullity, and the planar wedge calculation are either derived inline or applied with their operative measurability/nonnegativity hypotheses displayed. They introduce no additional citation blocker. The unsupported theorem-level shortcuts are precisely the analytic-set and area-formula source applications identified above.
- Binding manifest: All 27 prompt-bound SHA-256 values were recomputed before review and matched exactly, including all six proof/review pairs and every LaTeX source/support file.
