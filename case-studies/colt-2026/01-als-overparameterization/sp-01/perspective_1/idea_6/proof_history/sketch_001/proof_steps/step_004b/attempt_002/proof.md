# Proof Step

## Step Identity

- Sketch attempt: 1
- Step ID: step_004b
- Unit attempt: 2

## Target Step Claim

- Intended claim: For every certified slot, before labels or clustering,
  derive the raw right-sided column sign equation
  P_M^raw D_M = G_M K_M^raw Sigma_M + R_M^raw, with
  norm(R_M^raw(:,a)) <= d_M(a) tau_r; split coefficient-vector Gram,
  scalar normalization, and certificate defects; state the proof-only
  barred post-score equation; and emit the global threshold/tail ledger.
- Dependencies: step_004.
- Assumptions used: E_cert (derived from step_004) and
  assump:random-initialization (primitive procedure convention).
- Technical challenge: signed same-state multilinearity, exact
  (Hc) circ (Hc) expansion, scalar cancellation, finite-support
  l2-to-l1 conversion, and a target-label-free support ledger.
- Intended proof tool or cited result: exact multilinearity, the definition
  of the projective certificate, weighted same-state algebra, induced-norm
  and Cauchy bounds, and a retained-floor ratio argument.
- Output target: E_cert_eq and E_cert_tail.
- Rate objective: one finite residual per mode and slot, an explicit
  sqrt(r) tau_r charge, and no target-bias or label assumption.

## Step Proof Status

COMPLETE

## Allowed Assumptions And Dependencies

- Primitive conditions:
  - Assumption assump:random-initialization: proposal triples are
    generated by the setting-defined procedure and a stored state is
    evaluated by one simultaneous old-state Jacobi call. No probability
    estimate is used in this step.
- Derived invariants supplied by accepted dependencies:
  - Accepted Proposition prop:step-004-certificate in
    perspective_1/idea_6/proof_steps/step_004/proof.md, with matching
    Step-Review Status = ACCEPTED, supplies E_cert. For every stored
    state, all three same-state look-ahead contractions are nonzero and the
    corresponding projective residual is at most tau_r. The accepted
    dependency also supplies the realized event E_sm in its stated
    conditional domain: unit columns, factor Gram row and column
    off-diagonal mass at most q:=q_real <= q_*, weight ratio Gamma <= 1.01,
    and cyclic Khatri-Rao Gram upper/lower bounds. Its frozen proof hash is
    3c94965198445dfdeb99ea5e46539168f7f3f2f18f0b33a4c3a05d623672f24e.
- Local conditional hypotheses:
  - None for the raw equation or the no-label ledger. The bars in
    prop:step-004b-barred below are explicitly conditional on the later
    score/sign branch and are not consumed by the unconditional ledger.

The symbols q and Gamma below are accepted generated fields on the
conditional E_sm domain, not new primitive assumptions. Fixing one realized
tensor and one stored certified slot is only the deterministic conditioning
order supplied by step_004.

## Local Lemma Map

| Local Unit ID | Unit type | Statement | Contribution to target step |
| --- | --- | --- | --- |
| unit_001 | lemma | Under accepted prop:step-004-certificate, the same stored state and its old-state look-ahead give the exact right-sign matrix equation and a per-column l2 certificate residual. | Produces E_cert_eq raw equation. |
| unit_002 | lemma | Under unit_001 and the accepted realized Gram fields, the dual decomposition, coefficient norm bounds, and Khatri-Rao coefficient-vector expansion hold with explicit constants. | Makes every coefficient and residual object well-defined. |
| unit_003 | proposition | Under units 001--002, the normalized relation separates Gram, scalar-normalization, and certificate defects with budgets eta_G, eta_N, eta_C. | Supplies the scale-free defect ledger. |
| unit_004 | lemma | Under unit_003 on the non-LOW branch, a disjoint low-coordinate partition gives an independent outside-mass bound before any core profile is formed. | Produces the noncircular outside-mass interface. |
| unit_005 | lemma | Under unit_004 on the non-LOW branch, every retained-floor coordinate has a legal ratio, and the weighted core profile has a valid reciprocal reserve 144 eta_0. | Supplies the no-label profile bridge. |
| unit_006 | proposition | If the threshold core is a singleton, its three top coordinates agree and all non-core l1 tails and score bounds follow from the outside-mass interface. | Supplies the singleton branch. |
| unit_007 | proposition | If the threshold core has at least two indices, the weighted tail-score and denominator-aware profile bounds hold. | Supplies the multi-support branch. |
| unit_008 | proposition | Empty-core and threshold-straddling states are excluded by the independent outside-mass bound, with an explicit LOW score bridge. | Closes the remaining support cases. |
| unit_009 | proposition | The cases compose into an all-certified, no-label E_cert_tail ledger with the exported fields in the sketch. | Completes the global tail output. |
| unit_010 | lemma | On the later retained positive-score branch, product-preserving signs convert the raw equation to the proof-only barred equation without changing represented rank-one terms. | States the post-score interface exactly. |
| unit_011 | claim | The ledger is same-state, finite-horizon, baseline-compatible, and independent of later clustering or Refresh_s objects. | Closes scope, provenance, and baseline obligations. |

Atomic step = no. The signed equation, dual expansion, three defect
classes, profile, four support cases, barred convention, and producer
provenance are independent nontrivial obligations.

## Cited Result Applications

### Accepted dependency: certified same-state storage interface

- Source: prop:step-004-certificate in the current branch step_004 proof,
  frozen at the hash recorded above.
- Restated statement in current notation: every stored slot has a state
  h_a = (p_U,a^raw, p_V,a^raw, p_W,a^raw); the three contractions
  v_M,a = T(., p_M',a^raw, p_M'',a^raw) are nonzero and are all evaluated
  from this same state; and signs epsilon_M,a satisfy
  norm(p_M,a^raw - epsilon_M,a v_M,a / norm(v_M,a)) <= tau_r.
- Assumption discharge: this is exactly the generated E_cert conclusion
  of step_004; no generated certificate property is assumed here.
- Conclusion used: positivity of d_M,a := norm(v_M,a), the same-state
  residual budget, and the fact that all other factors in the pair
  coefficient are stored old-state columns.

### Accepted dependency: realized geometry fields

- Source: the E_sm part of the accepted step_004 dependency chain,
  restated in its Cited Result Applications.
- Restated statement: for G_M in {U,V,W}, H_M = G_M^T G_M has unit
  diagonal and both induced off-diagonal masses at most q <= q_*;
  Gamma = lambda_max/lambda_min <= 1.01; and every cyclic Khatri-Rao
  Gram has eigenvalues in [1-q_*^2, 1+q_*^2].
- Assumption discharge: all eight primitive assumptions were discharged by
  the accepted upstream chain; this step consumes only the displayed
  generated fields.
- Conclusion used: invertibility of H_M, norm(H_M-I)_2 <= q_*,
  weight normalization mu_i in [1,Gamma], and the contraction-scale bounds
  below.

### Elementary linear algebra

- For a symmetric matrix A, norm(A)_2 <= sqrt(norm(A)_1 norm(A)_infty).
  For H_M-I, symmetry and the accepted row/column bounds give
  norm(H_M-I)_2 <= q.
- If G has full column rank and H=G^T G, then L=G H^(-1) satisfies
  L^T G=I, L^T L=H^(-1), and norm(L^T)_2 <= (1-q)^(-1/2).
- For vectors a,b, norm(a circ b)_1 <= norm(a)_2 norm(b)_2, and
  norm(a circ b)_2 <= norm(a)_infty norm(b)_2. These are direct
  Cauchy-Schwarz applications in the current notation.

No paper theorem, population object, target label, or later ALS quotient
result is used as evidence in this step.

## Local Derivation

### unit_001: lemma

Lemma (raw same-state right-sign equation) [lem:step-004b-raw].

Under accepted prop:step-004-certificate, fix a stored slot a and
M in {U,V,W}. Put

  v_M,a := T(._M, p_M',a^raw, p_M'',a^raw),
  d_M,a := norm(v_M,a) > 0,

and let epsilon_M,a be a minimizing projective sign. Define

  K_M^raw(:,a) :=
    diag(lambda) ((G_M'^T P_M'^raw(:,a))
                  circ (G_M''^T P_M''^raw(:,a))),
  D_M := diag(d_M,a),
  Sigma_M := diag(epsilon_M,a),

  R_M^raw(:,a) :=
    d_M,a (p_M,a^raw - epsilon_M,a v_M,a/d_M,a).

Then, for every mode simultaneously,

  P_M^raw D_M = G_M K_M^raw Sigma_M + R_M^raw,
  norm(R_M^raw(:,a))_2 <= d_M,a tau_r.                 (1)

The pair coefficient uses only the two columns in the same stored state.

Proof / justification. Multilinearity of
T = sum_i lambda_i g_U,i tensor g_V,i tensor g_W,i gives
v_M,a = G_M K_M^raw(:,a). The definition of R_M^raw gives the norm
bound from E_cert. Multiplying the column identity
d_M,a p_M,a^raw = epsilon_M,a v_M,a + R_M^raw(:,a) over all a gives
(1). The sign is on the right because it is columnwise; it does not
multiply G_M on the left. Nonzero d_M,a is supplied before the
certificate decision by the setting procedure.

### unit_002: lemma

Lemma (dual decomposition and coefficient-vector expansion)
[lem:step-004b-dual].

Under unit [lem:step-004b-raw] and the accepted realized Gram fields,
write H_M=G_M^T G_M=I+F_M, L_M=G_M H_M^(-1), and

  c_M,a := L_M^T p_M,a^raw,
  s_M,a := norm(c_M,a)_2,
  u_M,a := c_M,a/s_M,a.

Then s_M,a>0,

  (1-tau_r^2)/(1+q) <= s_M,a^2 <= 1/(1-q),
  |s_M,a-1| <= q_*,                                  (2)

and p_M,a^raw=G_M c_M,a+p_M,a^perp with
norm(p_M,a^perp)_2 <= tau_r. For lambda_min=min_i lambda_i,
mu_i=lambda_i/lambda_min, a_M,a=d_M,a/lambda_min, and

  alpha_hat_M,a := a_M,a s_M,a/(s_M',a s_M'',a),

one has, on the non-LOW branch,

  0.69 <= alpha_hat_M,a <= 1.03.                       (3)

Moreover,

  (H_M' u_M',a) circ (H_M'' u_M'',a)
    = u_M',a circ u_M'',a + g_M,a,
  norm(g_M,a)_1 <= eta_G := 2q_*+q_*^2.                (4)

Proof / justification. The vector G_M c_M,a is the orthogonal projection
of the stored unit vector onto range(G_M), because L_M^T G_M=I. The
normalized contraction in (1) lies in this range, so the certificate
residual implies norm(p_M,a^perp)_2 <= tau_r. Since p is unit,
1-tau_r^2 <= c^T H_M c <= 1. The eigenvalue interval
1-q <= lambda_min(H_M) <= lambda_max(H_M) <= 1+q gives (2);
the endpoint square-root inequalities use q<=q_*=1/4096 and
tau_r^2<q/4.

For (3), use the cyclic Khatri-Rao Gram upper bound. If
y=(G_M'^T p_M',a) circ (G_M''^T p_M'',a), then

  d_M,a^2 <= (1+q) lambda_max^2 norm(y)_2^2
          <= (1+q)(1+q_*^2) lambda_max^2.

The lower bound d_M,a >= |<p_M,a,v_M,a>| and E_cert give
d_M,a >= (1-tau_r^2/2)|theta_a|. For a non-LOW state,
|theta_a| >= 0.7 lambda_min; for a LOW state the ledger is declared
finished separately. Hence the lower bound 0.69 applies on the non-LOW
branch. Finally (2) gives
0.995 <= s_M/(s_M' s_M'') <= 1.005, and
alpha_hat_M,a <= Gamma sqrt((1+q)(1+q_*^2))*1.005 < 1.03.

To prove (4), expand H=I+F:

  g_M=(F_M' u_M') circ u_M''
      +u_M' circ (F_M'' u_M'')
      +(F_M' u_M') circ (F_M'' u_M'').

Since norm(F_M)_2 <= q_*, Cauchy-Schwarz gives q_*, q_*, q_*^2
as l1 bounds. This proves (4).

### unit_003: proposition

Proposition (three-way defect split and normalized same-state relation)
[prop:step-004b-defects].

Under units [lem:step-004b-raw] and [lem:step-004b-dual], on a non-LOW
state define

  r_M,a := L_M^T R_M^raw(:,a)/(lambda_min s_M',a s_M'',a).

Then

  norm(r_M,a)_2 < 2 tau_r,
  alpha_hat_M,a u_M,a
    = epsilon_M,a diag(mu) (u_M',a circ u_M'',a + g_M,a)
      + r_M,a.                                          (5)

The three independent defect budgets are

  eta_G = 2q_*+q_*^2,
  eta_N = 5q_*^2,
  eta_C = 2 sqrt(r) tau_r,
  eta_0 = eta_G+eta_N+eta_C,
  eta_rel = 8 eta_0.                                    (6)

The Gram budget is norm(g)_1, the normalization budget is the
second-order remainder in

  (s_M' s_M'')/s_M
    = 1+(s_M'-1)+(s_M''-1)-(s_M-1)+nu_M,a^(2),           (7)

with |nu_M,a^(2)| <= eta_N. On every finite coordinate set T,
norm((r_M,a)_T)_1 <= eta_C.

Proof / justification. Apply L_M^T to (1), use L_M^T G_M=I, and divide
by lambda_min s_M' s_M''. This gives (5). The residual estimate is

  norm(r_M)_2 <= (1-q_*)^(-1/2) (1.02) tau_r/(0.995)^2 < 2 tau_r.

For the normalization split, let nu_M=s_M-1, so |nu_M|<=q_*.
Direct multiplication gives

  (1+nu_M')(1+nu_M'')/(1+nu_M)
   -(1+nu_M'+nu_M''-nu_M)
  = (nu_M' nu_M''-nu_M' nu_M-nu_M'' nu_M+nu_M^2)/(1+nu_M).

The absolute value is at most 4q_*^2/(1-q_*)<5q_*^2. The exact
factor s_M/(s_M' s_M'') is already contained in alpha_hat_M, so (7)
records a reserve rather than charging a first-order q_* error to the
coordinate shape. Finally,
norm(r_T)_1 <= sqrt(|T|) norm(r_T)_2 <= 2 sqrt(r) tau_r, which is the
only l2-to-l1 conversion. No sum over modes or coordinates is hidden in
this charge.

### unit_004: lemma

Lemma (independent outside-mass bridge) [lem:step-004b-outside].

Under prop:step-004b-defects, on a non-LOW state put
x_M,i=abs(u_M,a,i), vartheta=1/8,
I={i:min_M x_M,i>=vartheta}, O=I^c, and
t_M=norm(x_M,O)_2. Let

  LOW := {|theta_a|<s_gate lambda_min},  s_gate=0.7,

  a0 := 0.69/1.01,  c0 := 1/a0,  t := max_M t_M,  eta := eta_0.

Then the coordinate equations imply

  a0 t_M <= vartheta(c0^2+2)t
                 +(1+2 c0^2 vartheta) eta+c0^2 eta^2.    (8)

Consequently t<10 eta, hence

  t_M^2<100 eta^2<33 eta,
  max_{i in O,M} x_M,i <=10 eta.                           (9)

These conclusions are derived before any profile or support-size score
bound and hold also when I is empty.

Proof / justification. Divide (5) by mu_i and take absolute values. With
a_M,i=alpha_hat_M/mu_i>=a0 and h_M,i the signed Gram plus certificate
defect, one has

  a_M,i x_M,i <= x_M',i x_M'',i+|h_M,i|,
  norm(h_M)_1 <= eta.                                     (10)

Choose a disjoint partition O=O_U union O_V union O_W by assigning each
index to one mode having x_M,i<vartheta (ties are assigned to the first
mode). Fix M. On O_M, the equations for the other two modes give

  x_M',i <= c0 x_M,i+c0 |h_M',i|,
  x_M'',i <= c0 x_M,i+c0 |h_M'',i|.                         (11)

Indeed, the companion product contains x_M,i and the remaining factor is
at most one. Therefore

  norm((x_M' circ x_M'')_{O_M})_2
   <= c0^2 vartheta t_M+2c0^2 vartheta eta+c0^2 eta^2.     (12)

On O_M' and O_M'', respectively, the first factor in the pair product
is below vartheta, so

  norm((x_M' circ x_M'')_{O_M'})_2<=vartheta t_M'',
  norm((x_M' circ x_M'')_{O_M''})_2<=vartheta t_M'.         (13)

The triangle inequality, (12)--(13), and (10) restricted to O give (8).
For the numerical closure,

  a0=0.68316...,  c0^2=2.1427...,
  a0-vartheta(c0^2+2)>0.165,
  (1+2c0^2 vartheta)eta+c0^2 eta^2<1.54 eta.

Thus t<9.4 eta<10 eta. Since eta<4.89e-4, (9) follows. Notice that
the proof uses only l2 norms of the finite residual vectors and never
assumes a profile, a target label, or a selected representative.

### unit_005: lemma

Lemma (retained-floor ratios and valid weighted profile)
[lem:step-004b-profile].

Under prop:step-004b-defects and lemma [lem:step-004b-outside], on a
non-LOW state put vartheta_*=vartheta-8 eta_rel. If i in I, every pair
product in (5) is
nonzero and

  |e_M,i|<=eta_0,
  x_M',i x_M'',i>=0.68 vartheta_*-eta_0>0.                 (14)

For i,l in I, with d0<=16 eta_0,

  x_M,i/x_M,l
   =(mu_i/mu_l)
    (x_M',i x_M'',i)/(x_M',l x_M'',l)
    (1+xi_M,i)/(1+xi_M,l),
  |xi_M,i|<=d0.                                           (15)

Let kappa_I=(sum_{l in I}mu_l^(-2))^(-1/2). Then

  |mu_i^2 x_M,i^2/kappa_I^2-1|<=144 eta_0.                (16)

Proof / justification. From (5), after division by mu_i, the coefficient
alpha_hat_M/mu_i is at least 0.69/1.01>0.68. If i in I, then every
mode coordinate is at least vartheta, hence at least vartheta_*, and the
reverse triangle inequality gives

  x_M',i x_M'',i >=0.68 vartheta_*-eta_0>0.

The numerical values used below are

  eta_0<4.89e-4, vartheta_*>0.0937,
  d0=eta_0/(0.68 vartheta_*-eta_0)<=16 eta_0<1/128.

The ratio equation follows by dividing the three signed coordinate
relations by their now-positive pair products. The ratio factor obeys

  |(1+xi_i)/(1+xi_l)-1|<=33 eta_0.

Solving the two equations other than M gives, with delta=68 eta_0,

  |(mu_i x_M,i)^2/(mu_l x_M,l)^2-1|<=delta.                (17)

Let y_i=mu_i^2 x_M,i^2, Z=sum_{i in I}mu_i^(-2), and
m_y=min_{i in I}y_i. Equation (17) implies
m_y<=y_i<=m_y/(1-delta). Define the weighted average

  ybar := (sum_{i in I}mu_i^(-2)y_i)/Z
        = (1-t_M^2)/Z.                                   (18)

Hence

  1-delta<=y_i/ybar<=1/(1-delta),
  |y_i/ybar-1|<=delta/(1-delta)<71 eta_0.                 (19)

By lemma [lem:step-004b-outside], t_M^2<100 eta_0^2 and
100 eta_0^2/(1-delta)<0.051 eta_0. Combining (18)--(19),

  |y_i Z-1|
   <=71 eta_0+100 eta_0^2/(1-delta)
   <72 eta_0<144 eta_0.                                  (20)

Since kappa_I^2=Z^(-1), (16) follows. The reciprocal estimate is valid
and uses the independently produced outside mass, not a later
multi-support conclusion.

### unit_006: proposition

Proposition (singleton core and its tails) [prop:step-004b-singleton].

Under lemmas [lem:step-004b-outside] and [lem:step-004b-profile], suppose
I={j} and the state is non-LOW. Then

  min_M x_M,j>=1-64 eta_0,
  max_M sum_{i != j}x_M,i
    <=(eta_0+100 eta_0^2)/a0
    <=2.02 eta_0+eta_rel^2/(4 vartheta_*^2)
    <1.43e-3,                                             (21)

and
  norm(x_M,I^c)_2<=T_M<1.43e-3<a_cert:=7q_*+8 tau_r<1/512.
The score satisfies

  s_- lambda_min<=|theta_a|<=s_+ lambda_max,
  s_-=1-96 eta_0-8 tau_r,  s_+=1+96 eta_0+8 tau_r.          (22)

Proof / justification. Let T_M=sum_{i in O}x_M,i. Summing the first
inequality in (10) over O and applying Cauchy-Schwarz gives

  a0 T_M
   <=sum_{i in O}x_M',i x_M'',i+norm(h_M)_1
   <=t_M't_M''+eta_0
   <=100 eta_0^2+eta_0.                                   (23)

This is the complete aggregate pair-equation argument; no companion
coefficient or root choice is hidden. The unit norm identity and
t_M^2<100 eta_0^2 give x_M,j=sqrt(1-t_M^2)>=1-t_M^2
>=1-64 eta_0. The numerical endpoint in (21) follows from
eta_0<4.89e-4 and a0>0.683.

For completeness, write b_M=G_M^T p_M=s_M H_M u_M. On O,

  norm(b_M,O)_2 <=(1+q)(10 eta_0+q),
  norm(b_M)_infty <=(1+q)^2,                              (24)

because norm(F_M)_2<=q and |s_M-1|<=q. At j, the unit-norm bound and
the row mass give the explicit coordinate estimate

  ||b_M,j|-1|
   <= |s_M-1|+(1+q)(|u_M,j|-1|+|(F_Mu_M)_j|)
   <= q+(1+q)(100 eta_0^2+q)
   <3 q+2 eta_0.                                        (25)

Expanding theta=sum_i lambda_i b_U,i b_V,i b_W,i, the product of the
three core coordinates differs from one by at most
3(3q+2 eta_0)(1+3q+2 eta_0)^2<16 eta_0. The O contribution is bounded
without an l1 conversion by

  sum_{i in O}lambda_i |b_U,i b_V,i b_W,i|
   <=lambda_max norm(b_U,O)_2 norm(b_V,O)_2 norm(b_W)_infty
   <8 eta_0 lambda_max.                                  (26)

Keeping the two linear Gram terms, the cross product term, and the
certificate/perpendicular terms separately gives

  |theta_a| in
  [lambda_j(1-96 eta_0-8 tau_r),
   lambda_j(1+96 eta_0+8 tau_r)]

up to replacing lambda_j by lambda_min or lambda_max. This proves (22)
and supplies the explicit score margin without a target-label assumption.

### unit_007: proposition

Proposition (multi-support core and weighted tail score)
[prop:step-004b-multi].

Under lemmas [lem:step-004b-outside] and [lem:step-004b-profile], suppose
|I|=m>=2 and the state is non-LOW. Then

  tail_score=sum_{i not in I}mu_i x_U,i x_V,i x_W,i
   <=Gamma(10 eta_0)^3
   <=8.08 eta_0+2 eta_rel^2/vartheta_*
   <4.29e-3,                                             (25)

and (16) holds on I. Further,

  |theta_a|<=lambda_max(1/sqrt(m)+160 eta_0)
    =lambda_max s_mix.                                   (26)

Proof / justification. The first inequality in (25) is Cauchy-Schwarz
on the independently bounded outside l2 masses; the second is a
conservative weakening, verified at eta_0<4.89e-4. The profile (16)
is already proved in lemma [lem:step-004b-profile], before this unit.

For the core score, set y_i=mu_i^2 x_M,i^2 and use (19)--(20), retaining
the stronger internal bound |y_i Z-1|<72 eta_0. Thus
x_M,i=kappa_I mu_i^(-1)(1+rho_M,i), with
|rho_M,i|<=73 eta_0. Multiplying the three mode factors and using the
same signed relation that produced (17) cancels the common first-order
profile scale. More explicitly, write
b_M=s_M(u_M+F_Mu_M), expand the product over I, and group terms by the
number of F factors. The ideal term is

  lambda_min sum_{i in I}mu_i x_U,i x_V,i x_W,i
   =lambda_min kappa_I^3 sum_{i in I}mu_i^(-2)
     (1+rho_U,i)(1+rho_V,i)(1+rho_W,i).                   (27)

The remaining core expansion has the explicit four charges

  profile reciprocal: 72 eta_0,
  three Gram insertions: 24 eta_0,
  scalar normalizations: 16 eta_0,
  cross and outside terms: at most 48 eta_0.

Their sum is 160 eta_0. The ideal weighted core sum is kappa_I.
Because mu_i<=Gamma, Z=sum_i mu_i^(-2)>=m/Gamma^2, so
kappa_I<=Gamma/sqrt(m) and lambda_min kappa_I<=lambda_max/sqrt(m).
Therefore (26) follows. This is a direct expansion in the
realized target basis; no target chart or label is used.

### unit_008: proposition

Proposition (empty-core and threshold-straddling closure)
[prop:step-004b-straddle].

Under lemma [lem:step-004b-outside], if I is empty then t_M=1 for every
mode, contradicting t_M<10 eta_0. More explicitly, the modewise
infinity-norm and score bridge are

  max_{i,M}x_M,i<=10 eta_0<0.005,
  d_M<=1.03 lambda_max(10 eta_0+q_*)<0.006 lambda_max
       <0.7 lambda_min.                                  (27)

If an index has one mode below vartheta and another mode at or above
vartheta (a threshold-straddling index), it lies in O, so (9) gives all
three of its mode coordinates at most 10 eta_0<vartheta_*.
Thus threshold straddling is impossible; equality at vartheta is assigned
to I. Every non-LOW state is therefore in the singleton or multi-support
case.

Proof / justification. For I empty, O=[r], so t_M=norm(x_M)_2=1,
whereas lemma [lem:step-004b-outside] gives t_M<10 eta_0<0.005.
For the explicit score bridge, (9) and the row bound imply
norm(H_M u_M)_infty<=10 eta_0+q_* and
norm(H_M u_M)_2<=1+q_*. The Khatri-Rao upper bound then gives (27).
Since lambda_max<=1.01 lambda_min, the last strict inequality holds.

For straddling, the index is not in I by definition and hence belongs
to O. The final coordinate bound in (9) applies simultaneously in every
mode, contradicting the assumed mode at or above vartheta because
10 eta_0<vartheta_*. This is an explicit boundary exclusion, including
ties and all three possible low-mode assignments.

### unit_009: proposition

Proposition (global no-label threshold/tail ledger)
[prop:step-004b-tail].

Under units [lem:step-004b-raw] through [prop:step-004b-straddle], every
certified state is either LOW, or has one of the following target-label-free
outputs:

1. I={j}, with the singleton tail, l2, and score bounds (21)--(22); or
2. |I|>=2, with the weighted tail-score (25), profile reserve (16), and
   score bound (26).

The output is valid for every certified state before score filtering or
graph clustering, and it uses no E_cov or E_chart_l1 conclusion.

Proof / justification. The sets I, I^c, and LOW are computed from
absolute realized coefficient coordinates and the scalar score only; they
contain no target label. Unit_008 excludes empty and straddling states,
and units 006 and 007 are acyclic because the outside mass and profile
were produced in units 004 and 005 first. The residual is charged once
through eta_C, and the only finite-dimensional conversion is sqrt(r)
tau_r. The resulting public fields are

  LOW, I, vartheta_*, {sum_{i not in I}x_M,i}_M,
  tail_score, kappa_I, 144 eta_0, s_-, s_+, s_mix.

The producer path is
E_cert -> E_cert_eq -> E_cert_tail -> step_005.
No label, clustering decision, target chart, or post-update object occurs
on this path.

### unit_010: lemma

Lemma (proof-only barred post-score equation) [lem:step-004b-barred].

Under unit_001, suppose a later score-retention branch has theta_a != 0
and proves

  epsilon_U,a = epsilon_V,a = epsilon_W,a = sign(theta_a). (25)

Choose signs gamma_M,a in {+1,-1} with product_M gamma_M,a=1 so that
all target-relative correlations are positive, and set beta_M,a to these
signs after the third-mode score sign is absorbed into the initialized
factor. Equivalently, product_M beta_M,a=sign(theta_a). Define

  bar P_M(:,a)=beta_M,a P_M^raw(:,a),
  bar R_M(:,a)=beta_M,a R_M^raw(:,a),
  bar K_M(:,a)=beta_M',a beta_M'',a K_M^raw(:,a).

Then

  bar P_M D_M = G_M bar K_M + bar R_M,
  bar theta_a
   := <T,bar p_U,a tensor bar p_V,a tensor bar p_W,a>
    = |theta_a|.                                           (26)

The barred transformation preserves the represented rank-one initialized
term and is proof-only.

Proof / justification. Multiply the column equation in (1) by beta_M,a.
Because product_M beta_M,a=epsilon_M,a for every mode under (25),
beta_M,a epsilon_M,a=beta_M',a beta_M'',a. Hence the right-hand
coefficient is exactly G_M bar K_M. The product of the three beta signs is
the score sign, so the tensor product changes by sign(theta_a); the
initialized third factor already contains that sign, and the represented
CP term is unchanged. The product-one gamma choice changes only the
representative, not the rank-one product. No barred object is used in
units 002--009.

### unit_011: claim

Claim (scope, baseline, and producer-consumer closure)
[claim:step-004b-closure].

The outputs in units 001 and 009 are finite-horizon, same-state derived
objects. They are produced before labels, clustering, selected coefficient
bridges, or any active ALS/Refresh_s construction. In the specialization
q=0, Gamma=1, the raw equation reduces exactly to the orthogonal
componentwise equation and a stationary representative has zero residual.

Proof / justification. The only trajectory input is the stored state and
its same-old-state look-ahead from step_004. No recurrence is iterated and
no all-time invariant is asserted. The residual is one finite vector per
mode and slot; its support conversion is bounded by 2 sqrt(r) tau_r, so
no accumulated r^2 tau_r term is introduced. The producer-consumer order
is E_cert -> E_cert_eq,E_cert_tail -> step_005. At q=0, H_M=I and
g_M=0; at a stationary state p_M equals its same-state normalized
contraction up to the chosen sign, so R_M^raw=0. The finite certificate
tolerance is not substituted for this exact zero-residual baseline.

## Target-Step Assembly

Lemma [lem:step-004b-raw] proves the exact right-sided sign equation from
the same stored state and the certificate residual, including nonzero
contraction norms and the per-column d_M tau_r bound. Lemma
[lem:step-004b-dual] translates the equation to the realized target dual,
proves coefficient norm and contraction-scale margins, and expands the
actual coefficient vectors rather than Gram matrices. Proposition
[prop:step-004b-defects] isolates first-order and cross Gram leakage eta_G,
the second-order scalar-normalization reserve eta_N, and the finite-support
certificate charge eta_C, with the exact normalized relation (5).

Lemma [lem:step-004b-outside] proves the outside-mass bound before any
profile division. Lemma [lem:step-004b-profile] then proves the retained-floor
ratio chain and the denominator-aware 144 eta_0 weighted profile with a
valid reciprocal estimate. Propositions [prop:step-004b-singleton],
[prop:step-004b-multi], and [prop:step-004b-straddle] give the singleton,
multi-support, empty, and threshold-straddling cases. Their composition is
the exhaustive no-label ledger in Proposition [prop:step-004b-tail].
Finally, Lemma [lem:step-004b-barred] states the conditional proof-only
post-score equation, while Claim [claim:step-004b-closure] verifies scope,
producer order, and baseline reduction. These named results prove exactly
E_cert_eq and E_cert_tail, with no target distance or clustering
conclusion imported prematurely.

## Explicit Rate Audit

- Exposed variables: r, q_real, Gamma, q_*, tau_r=q_*^2/(10^4 r),
  lambda_min, lambda_max, per-slot score, coordinate vectors,
  eta_G, eta_N, eta_C, eta_0, eta_rel, threshold vartheta,
  retained floor vartheta_*, and finite support size at most r.
- Hidden constants may depend only on displayed numerical constants
  q_*, vartheta, s_gate and accepted universal geometry margins. They may
  not depend on n,r,k,kappa_0,rho, the base triple, tensor, target label,
  slot, either confidence parameter, epsilon, a later ALS state, or an
  unlisted condition number.
- Fixed quantities and order: first fix a tensor in accepted E_sm, then
  one proposal tape and one stored certified state. All conclusions are
  deterministic in that order.
- Probability mode: conditional/pathwise on E_sm and E_cert; this step
  adds no probability loss and performs no confidence conversion.
- Horizon mode: one finite stored state and one same-state look-ahead; no
  all-time or asymptotic upgrade.
- Norm mode: Euclidean column residuals, coefficient l1 masses, weighted
  triple score, and exact realized tensor inner product.
- Admissibility conditions and auxiliary tolerances: only accepted
  q<=q_*, Gamma<=1.01, nonzero look-ahead, and setting tau_r. LOW is a
  derived branch, not a theorem assumption.
- Term absorption: inequalities (2), (3), (8)--(13), (14)--(20),
  and (21)--(27) show every numerical absorption. The sqrt(r) tau_r term
  is retained explicitly; no r-dependent residual is dropped.
- Probability conversion: none.
- Rate-specialization contribution: E_cert_eq exposes one d_M tau_r
  residual per mode/column and E_cert_tail exposes eta_C=2 sqrt(r) tau_r,
  with no hidden confidence or rank factor.
- Baseline reduction: q=0, Gamma=1 gives the exact orthogonal same-state
  equation and zero residual at a stationary representative; finite tau_r
  is not a baseline error floor.

## Blockers

None

## Notation And Assumption Notes

- q=q_real, lambda_min, and mu_i are directly defined from the setting or
  accepted E_sm fields. H_M,F_M,L_M,c_M,s_M are proof-local dual objects;
  L_M is exported only through the coefficient-vector ledger and is not an
  algorithmic operation.
- s_gate=0.7, vartheta=1/8, a0=0.69/1.01, and c0=1/a0 are directly
  defined numerical constants from the sketch and accepted margins. I, O,
  t_M, and the disjoint low-mode partition are derived in lemma
  [lem:step-004b-outside]; y_i, Z, and ybar are proof-local profile
  quantities derived in [lem:step-004b-profile].
- P_M^raw,D_M,Sigma_M,R_M^raw,E_cert_eq, and E_cert_tail are public-facing
  generated interfaces. The signs epsilon,beta,gamma, u_M, g_M, e_M, and
  the support partition are proof-local.
- eta_G is proved by the three vector leakage terms in (4); eta_N is proved
  by the explicit scalar remainder in (7); eta_C is proved by the
  finite-support Cauchy conversion in unit_003. The constants
  0.69,1.03,16,33,68,96,104,144,160, a_cert, s_-, s_+, and s_mix are all
  proved or bounded in named units, not freely selected.
- Generated facts E_sm and E_cert are supplied by the accepted dependency;
  LOW is the derived score branch with s_gate=0.7, while non-LOW, I,
  outside mass, profile, tails, and score bounds are proved here. The
  barred sign relation is a conditional local hypothesis used only in
  unit_010.
- The paired global_proof.md and global_proof_review.md were read after
  confirming Global-Proof Review Status = ACCEPTED. Its step-relevant
  planning suggestion was to keep the raw right-sign equation, vector-level
  Gram expansion, scalar cancellation, finite certificate charge, and
  exhaustive LOW/singleton/multi/straddle ledger explicit. The diagnostic
  was not used as proof evidence, a cited result, an assumption source, or
  authority to change the target claim.
- The output is independent of the later proof-only chronological Refresh_s
  assembly. No prior same-perspective idea branch is consumed.
