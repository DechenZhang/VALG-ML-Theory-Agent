# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 7
- Reviewed sketch: `perspective_1/idea_6/proof_sketch.md`
- Reviewed sketch SHA-256: `9663efc957e3895be7a92831077b36481eea97542513abf25f5648ffe2597d1b`
- Binding setting SHA-256: `a6991b0e308a8a33e782be75f9f081325404974335e5c00df85e6d8a80921cab`
- Triggering attempt-6 review SHA-256: `75c2c40b9fd68ccab6e77224ca7f3931479310b17a2e18fff74bfdf1744b2f22`
- Goal mode: exact-goal mode
- Progress type: `conditional`

## Sketch Viability Score

6

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: The sketch preserves the conditional subquadratic-rank theorem, nested probability modes, original relative-Frobenius success criterion, chronological quotient state, polynomial stopping target, and exact orthogonal equal-weight baseline. No idea-level target drift is needed for the repairs below.
- Dependency audit: The inter-step graph is acyclic and the range chronology is ordered correctly as `step_007 -> step_007c -> step_007b -> step_008`. The current defects are inside the `step_007c` producer contract and its `step_007b`/table consumers, not a future-to-past dependency.
- High-risk coverage: Attempt 7 correctly replaces the old pre-normalization equation by the actual unit-column identity with `F_N`, uses one definition `E_N=B_N-B_N^0`, writes a correct exact `A_M-A_M^0` resolvent identity, and retains baseline and first-update traces. It does not yet close the comparator source, the termwise two-orientation reserve, or the unified chronological perpendicular ledger.
- Explicit-rate coverage: The exposed rank, smoothing, confidence, horizon, norm, and runtime fields remain present. The first-sweep constants `3072/3456/3888/4096` are not attached to one consistent `beta`/`eta` interface, so the local quantitative specialization is incomplete.
- Assumption and citation plausibility: No cited theorem is used to supply the disputed carry estimates. They are direct-current-notation obligations. The setting sources remain plausible, and the defects can be repaired without adding a primitive assumption or changing the theorem contract.

## Early Obstruction Audit

- Limiting-case stress: At the exact orthogonal equal-weight stationary state, direct substitution gives `B=B^0=I`, `F=0`, `A=A^0=0`, `nu=nu^0=1`, and `s^+=0`; the baseline passes. At a carried predecessor, however, the actual range update changes the current residual Gram to `F_N=0`, while the sketch does not define a producer proving that its retained `B_N^0=I+C_N` is normalized against that same current `F_N`.
- Theorem-critical bridge support: The actual identity `diag(B_N^T H_N B_N+F_N)=1` is correct. The claimed comparator identity with the same `F_N` is only asserted: neither `pred(U)`/`pred(V)` nor the `step_007b` row defines `B_N^0=(I+A_N^{0,\mathrm{pred}})D_{\nu^0,N}^{\mathrm{pred}}` or an equivalent same-current-record normalization. Consequently the subtraction producing centered cancellation has no legal comparator producer.
- Exported-interface feasibility: `E_N:=B_N-B_N^0` and `epsilon_N=||E_N||_{rc}` are now coherent. Feasibility still fails because the raw comparator certificate is absent, the displayed termwise reserve uses an undefined `L_N`, and the detailed `step_007b`, exported-interface, flow, and step rows revert to the old fixed `beta_perp=b_perp(tau_r,tau_r)` interface.
- Theorem-critical mechanism witness gate: The exact solve, actual unit normalization, target pair floor, range containment, and finite chronology are adequate sources. The centered comparator and sharp carry-gain witnesses are shallow: the former lacks a producer, and the latter does not derive the claimed combined bound from the displayed resolvent terms.
- Entry-state trace stress: The selected initialization supplies U with `h_U=0`; after the exact U update, `N_U^+=0`; V is then evaluated with the produced U record, and W only after the V record. This chronology is noncircular. The quantitative records passed along those edges are inconsistent, so the trace is structurally valid but not yet numerically certified.
- Obligation locality classification: `E_sm`, window/coverage, chart/certification, classification, selected row/column closure, `E_best/E_quotient`, target-span contraction, stopping, restart, and baseline are `step-local`. The normalized comparator certificate, the two-orientation carry reserve, unified perpendicular ledger, and numerical first-sweep export are `sketch/interface defect`. No reviewed obligation is an `idea/theorem-contract defect`.
- Noncircular closure gate: Passed structurally. Neither `step_007c` nor the first U call assumes `E_parallel_entry`; exact range is consumed only after its own block update. Repairing the records does not require a circular basin premise.
- Mechanism-source and boundary stress: The actual post-normalization identity remains active even with `F_N!=0`, and after a generated exact range update it specializes to `F_N=0`. A comparator may support the same centered identity only if it is separately normalized with the same current perpendicular record. Retaining an earlier static `C_N` is not by itself such a source.
- Generated-output flow: Earlier flow is legal. `pred(U)` and `pred(V)` do not export the comparator construction required by `step_007c`. Moreover, the attempt-7 repair declares all consumers use `beta_perp(M,h_M)`, but the detailed producer summary, `step_007b`, Exported Interface Feasibility, Generated Output Flow, and Sketch Steps use a fixed initial `beta_perp` and fixed `eta` instead.
- Source-to-claim adequacy: The two exact `A_M-A_M^0` formulas are algebraically correct. The subsequent reserve is not established: each of the two displayed resolvent terms is bounded by `u_C+u_J/2`, yet their sum is later treated as only `x_M=u_C+u_J/2`; `u_C,u_J` are never separately defined. The asserted exact cancellation of the `Lambda` similarity by `R_in^{-2}D_lambda^{-2}` is also not shown after `A_M` has been scale-stripped. A direct paired row/column calculation, with any surviving `Gamma` factor charged, is required before `ell_0=17/512` is step-local.
- Residual-to-target adequacy: All produced and consumed objects use the same realized factors, quotient metric, and original Frobenius target, so there is no surrogate mismatch. The residual decomposition is incomplete at the comparator/carry boundary; therefore the V/W output residuals are not yet dominated at the advertised target scale.
- Baseline invariance audit: The inherited conclusion is exact stationary recovery and zero residual at the deterministic orthogonal equal-weight specialization. The direct map and first stationary update preserve it; the positive numerical `beta` envelope is correctly distinguished from actual zero contamination. Retry route for the unrelated carry defects is `/proof-sketch`.
- Scope-accumulation compatibility: Proposal forcing is finite-horizon, the entry sweep has three calls, later target-span forcing is exactly zero, noncovered tapes stop at the fixed cap, and restart costs add over finitely many tapes. The first-sweep defect is not an accumulation failure; it is an invalid one-call exported interface.
- Scope and dependence consistency: Probability, horizon, rank, and norm modes are consistent. Quantitative notation is not: the repair section uses `beta_*`, `eta_*`, and undefined `eta_M`; later sections use fixed `beta_perp`, `eta`, and both `3888` and `4096` as the W output constant.
- Generated-condition provenance: Generated range and basin facts are not promoted to assumptions. The missing comparator normalization and unified ledger must be added to the predecessor outputs rather than assumed in the conditional lemma.
- Citation and tool applicability: The Uschmajew citation is restricted to quotient interpretation and does not discharge the disputed identities. No source-identity issue changes the route.
- Same-setting repair plausibility: High. Explicitly export the normalized comparator, repair the termwise resolvent accounting, and synchronize one `beta_*`/`eta_*` chronology. These are roadmap/interface edits under the existing setting.
- Target-preserving bridge-repair gate: `/proof-sketch` is sufficient. No primitive assumption, algorithm/model/procedure, theorem scope/mode/metric, exposed dependence, success criterion, or baseline conclusion must change.
- High-risk obligation classes: Structural inverse floors, post-normalization cancellation, induced row/column and ambient-column transfer, common-product transfer, generated range membership, all-sweep contraction, stopping, probability conversion, and baseline invariance were audited. Only the comparator/carry/first-sweep interfaces block acceptance.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_sm` | Static geometry and pair floors | Gaussian normalization, Schur product, Gershgorin | primitive assumptions -> direct derivation | Separate perturbation classes match the claim | Same normalized realized factors | smoothing margins plus finite union | Exports `q_real<=q_*`, `Gamma<=1.01`, pair floor | Produced factors are consumed directly | One static union | assumptions -> `step_001` | `step_001` -> dynamic consumers | Orthogonal zero-smoothing boundary is exact | step-local | None |
| `E_win,E_cov` | Proposal activation and rank rate | Gaussian regression, product tails, coupon bound | `E_sm` plus primitive initialization | Window probability matches `k` coverage | Setting raw-coordinate convention | `p_win asymp r^(-5/3)(log r)^(-3/2)` and miss union | Raw windows export target-relative ratios | No surrogate | Fixed slots; misses union-bounded | `step_001 -> step_002 -> step_003` | `step_003` -> chart/restart | Initial denominator slack checked | step-local | None |
| `E_RS,E_chart,E_cert` | Recurrence, chart closure, certification | Exact old-state Jacobi map and self-map contraction | earlier derived controls | Denominator and displacement sources match | Same simultaneous old-state convention | displayed `R,S,a_t` recurrences and geometric `zeta` decay | Window controls export stored certificate | Same projective/ambient target | Persistent `q` forcing over fixed horizon | `E_win,E_sm -> step_004` | `step_004` -> certificate consumers | `q=0` squaring/fixed point passes | step-local | None |
| `E_cert_eq,E_cert_tail` | Same-state equation and no-label tail ledger | Multilinearity, certificate residual, coefficient-vector expansion | `E_cert,E_sm` -> direct derivation | Sign and finite-support charge match | Raw/barred conventions separated | `eta_G+eta_N+eta_C`, retained-floor/profile bounds | Every certified slot covered before labels | No premature target-distance claim | One finite residual; `sqrt(r)tau_r` once | `step_004 -> step_004b` | `step_004b` -> classifier/row bridge | Stationary residual zero; transient retained | step-local | None |
| `E_support_wt,E_cluster,E_gauge` | Classification and representative selection | Weighted pair equations and score separation | derived certificate ledger | Weighted support gap matches graph claim | Same realized scores/factors | explicit mixture/component and graph margins | Scores export one cluster per target | Graph metric matches target chart | Finite graph | `step_004b -> step_005` | `step_005` -> selected bridge | Unequal-weight/sign boundaries handled | step-local | None |
| `E_col,E_row_residual,E_row` | Selected row/column closure | Dual equation and spatial small gain | accepted selected certificate outputs | Per-entry source supports both sums | Same target dual basis | column root and row coefficient `<1/8` | Per-entry `2tau_r` separately sums in both orientations | Exact residual transfer | Finite unknown-sign `r tau_r`, absorbed once | classifier -> `step_006` | `step_006` -> `step_007` | Exact stationary state gives zero | step-local | None |
| `E_best,E_quotient` | Basin seed and entry decomposition | Tensor expansion, projector, product-one balance | selected row/column/gauge outputs | Direction/product errors match theorem target | Same realized tensor/projector | four-class expansion and `512omega` | Exports actual `B_M,N_M,t_M` records | Original Frobenius metric controlled | One finite entry transfer | `step_006 -> step_007` | `step_007` -> first-sweep blocks | Exact target maps to zero state | step-local | None |
| Normalized predecessor comparator | Centered carried-direction certificate | Actual unit-column identity plus proposed comparator subtraction | actual record from exact solve; comparator producer absent | Actual `diag(B^THB+F)=1` matches; same-`F` comparator not established | Actual post-normalization convention is correct; comparator convention unspecified | Desired `diag(2(B^0)^THE+E^THE)=0` | `E=B-B^0` is coherent, but `pred(U/V)` does not export `B^0=(I+A^0)D_{nu^0}` with current `F` | Centered residual cannot be transferred | Three calls only; source missing before V/W consumers | `step_007b pred(U/V)` -> `step_007c` | Initial `E=0` passes; carried current-`F` case unresolved | sketch/interface defect | /proof-sketch |
| Two-orientation carry reserve / `E_carry_resolvent` | Sharp normalized one-block transfer | Correct exact resolvent identity, centered normalization, diagonal similarity | intended direct current-notation derivation | Exact identity matches, quantitative implication does not | `Lambda`/held-scale pairing is not discharged in the scale-stripped convention | Two terms each bounded by `x_M` are later totaled as `x_M`; undefined `L_N,u_C,u_J` | No proved combined row/column/ambient/log bound yielding `ell_0 h_M` | Same target, but residual dominance is incomplete | Finite three-call carry; one-step reserve unsupported | `E_sm,E_quotient -> step_007c` | Exact stationary boundary passes; mixed carry unresolved | sketch/interface defect | /proof-sketch |
| Unified `E_perp_pair` ledger | Actual chronological perpendicular pair control | Orthogonal decomposition and actual held Grams | `E_quotient` plus predecessor records | Parameterized `beta_perp(M,h_M)` can match the claim | Same Euclidean projector | Top repair gives unified envelope; detailed consumers revert to fixed formula | `step_007b` and tables omit `(1+48omega+h_M)^2` and `beta_*` | Actual `F_M` is correct; carried envelope is not propagated | Finite forcing removed modewise | `step_007 -> step_007c -> step_007b` | Shared-perpendicular static stress passes; V/W record mismatch remains | sketch/interface defect | /proof-sketch |
| `E_pair_entry,E_parallel_entry` | Inverse floors and exact span entry | Pair ledger, carry resolver, exact unfolding range | dependencies plus chronological derivation | Range source adequate; constants depend on blocked interfaces | Exact literal ALS/realized span | U/V/W recurrence stated with inconsistent `eta` fields and `3888/4096` outputs | No single producer record proves every domain inequality | Correct target would be reached if interfaces are repaired | Perpendicular defects vanish modewise | `step_007c -> step_007b` | First U update active; V/W quantitative domains unresolved | sketch/interface defect | /proof-sketch |
| `E_basin_parallel` | Invariant quotient contraction | Exact range invariance, pair floor, scale-equivariant refresh | `E_parallel_entry,E_sm` -> direct derivation | Mechanism matches once entry is valid | Literal orbit/quotient bridge explicit | chronological output recurrence and `d_Q` contraction | Feasible after repaired entry | Quotient metric bridges to Frobenius | Zero additive forcing; geometric budget | entry -> `step_008` | `step_008` -> `step_009` | Exact point fixed; unrestricted tube excluded | step-local | /proof-sketch |
| `E_stop` | Relative-error stopping | Quotient contraction and residual equivalence | basin output | Geometric rate matches original residual | Original rank-k loss | explicit `m_epsilon` | Feasible after valid basin entry | Original Frobenius residual | Finite geometric horizon | `step_008 -> step_009` | `step_009` -> runtime/theorem | Exact baseline stops at zero | step-local | /proof-sketch |
| `E_run_cap,E_restart` | All-tape termination, cost, confidence | Fixed counter, Moore-Penrose branch, independent tapes | approved procedure wrapper plus primitive randomness | Counter/restart sources match | Observable dense algorithm | finite cap; costs add; failures multiply | No latent-factor oracle | Success uses original residual | Arbitrary defects stopped; finite `J` | cap -> `step_010`; success path separate | `step_010` -> theorem | Noncovered singular/nonhitting tape exits | step-local | None |
| Orthogonal equal-weight baseline | Exact stationary recovery and zero-error limit | Direct Jacobi/ALS substitution | deterministic specialization | Exact equality, not only vanishing envelopes | Produced and consumed tensor identical | exact squaring, `B=I,A=0,nu=1,s=0` | No surrogate/error floor | Exact target equality | Zero forcing and stationary trace | direct `step_011` bridge | `step_011` -> theorem | First and later stationary updates exact | step-local | None |

## Blocking Issues

1. **`Attempt-7 Carried-Interface Repair` and `step_007c`: the comparator half of the post-normalization certificate has no producer.** The actual identity with `F_N` is correct, and `E_N=B_N-B_N^0` is coherent, but `B_N^0=I+C_N` is merely called separately normalized. `pred(U)`/`pred(V)` export only the actual `A_N^pred,D_{nu,N}^pred`; they do not construct an `A_N^{0,pred},D_{nu^0,N}^{pred}` comparator or otherwise prove unit columns with the same current `F_N`. Downstream effect: centered diagonal cancellation and every sharp carried estimate remain conditional on an unproduced fact. Smallest repair: `/proof-sketch`.
2. **The carried resolvent reserve does not close algebraically to `ell_0`.** The exact `A-A^0` identity is repaired, but both displayed resolvent terms are individually bounded by `u_C+u_J/2` and their sum is later treated as that same quantity; `u_C,u_J`, and the termwise `L_N` field are undefined. The asserted cancellation of the `Lambda` similarity against factors outside the scale-stripped `A` interface is not shown. Downstream effect: row, column, ambient-column, product-log, and `ell_0=17/512` outputs are not step-local. Smallest repair: `/proof-sketch`.
3. **The unified `beta_perp(M,h_M)` ledger is not propagated.** The repair section defines `beta_*` and says every consumer uses it, but detailed `step_007c` export prose, `step_007b`, Exported Interface Feasibility, Generated Output Flow, and Sketch Steps revert to `(1+48omega)^2`, fixed `b_perp(tau_r,tau_r)`, fixed `beta_perp`, and fixed `eta`. Downstream effect: there is no single actual-record envelope for V/W carried fields. Smallest repair: `/proof-sketch`.
4. **The chronological numerical specialization is internally inconsistent.** The repair uses `eta_U`, undefined `eta_V/eta_W/eta_M`, and a global `eta_*`; later text uses `eta`, while the W output appears as both `3888eta_W` and `4096(omega+beta_perp)`. Downstream effect: `Delta_J`, `d_M`, positivity, inverse floors, `h_V/h_W`, and the final `<rho_ALS/3` claim are not all consequences of one exposed interface. Smallest repair: `/proof-sketch`.

## Required Repair Bundle

1. In `Attempt-7 Carried-Interface Repair`, `step_007c`, and `step_007b`, explicitly construct the same-current-record comparator. For example, export `B_N^0=(I+A_N^{0,pred})D_{nu^0,N}^{pred}`, prove `diag((B_N^0)^TH_NB_N^0+F_N)=1`, define `C_N=B_N^0-I`, `E_N=B_N-B_N^0`, and then subtract. At the initial call use `B_N^0=B_N,E_N=0`; after an exact range update use the current `F_N=0`. Affected output: normalized predecessor certificate. Target: `/proof-sketch`.
2. Define every term in the two-orientation ledger (`L_N,u_C,u_J` or replacements), write separate row and transposed-column sums for all six source classes, and state a valid combined bound for the full exact `A-A^0` expression. Charge any surviving `Gamma`, inverse, diagonal-similarity, ambient-column, normalization, and log factors before fixing `ell_0`; do not count two `x_M` terms as one. Affected ID: `step_007c`. Target: `/proof-sketch`.
3. Choose one perpendicular interface throughout. Prefer the already declared `beta_perp(M,h_M)`, `beta_*:=max_M beta_perp(M,h_M)`, and `eta_*:=omega+beta_*`; route the actual `t_N` after each range update and replace every fixed/static formula in `step_007b`, Exported Interface Feasibility, Generated Output Flow, Sketch Steps, and Blockers. Alternatively prove one fixed envelope dominates every parameterized record and use that fixed envelope everywhere. Target: `/proof-sketch`.
4. Rerun the U/V/W domain arithmetic with the chosen single envelope. Define all per-mode quantities, prove `Delta_J<1/32768`, `d_M<1/16384`, positive `nu`, and `h_M<=2rho_ALS/3` before each call, then derive one consistent `epsilon_U/epsilon_V/epsilon_W` sequence and final `<rho_ALS/3` bound in both induced orientations, ambient columns, and the common log register. Update all evidence/flow rows to those exact constants. Target: `/proof-sketch`.
5. Target-preserving repair check: all four repairs change only comparator construction, algebraic bookkeeping, producer-consumer records, and constant specialization. They require no new primitive assumption, algorithm/model/procedure change, theorem scope/mode/metric change, exposed-rate change, success-criterion change, unsupported theorem-facing source, or weakened baseline. Do not route to idea generation.

## Review Rationale

`REVISE_SKETCH` is the deepest necessary change. Attempt 7 repairs two important points: the actual post-normalization identity now contains `F_N`, and the exact resolvent difference is correct under one coherent carried-field definition. The remaining failures are nevertheless theorem-critical because the comparator source, the sharp two-orientation reserve, and the actual chronological perpendicular envelope are precisely the producers required for first-sweep entry. Each can be repaired within the same formalized setting and goal, so `IDEA_FAIL` is not justified.
