# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 6
- Reviewed sketch: `perspective_1/idea_6/proof_sketch.md`
- Reviewed sketch SHA-256: `9d1ab5197a301564f2b58b41d155ddc12b4f467dba811d46aabfd66e0c126d97`
- Binding setting SHA-256: `a6991b0e308a8a33e782be75f9f081325404974335e5c00df85e6d8a80921cab`
- Triggering attempt-5 review SHA-256: `e67175fe9629ff5f23b3a8143a217e8f3dea1028900a35cc55e7dcff9f6a8cf9`
- Goal mode: exact-goal mode
- Progress type: `conditional`

## Sketch Viability Score

6

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: The sketch preserves the binding conditional theorem, the `Theta(r^(5/3)(log r)^(5/2))` rank, nested probability modes, original relative-Frobenius target, chronological quotient metric, finite-run wrapper, and exact orthogonal equal-weight baseline. No idea-level target drift is present.
- Dependency audit: The step graph is acyclic in name and orders `step_007c` before `step_007b`. The carried producer-consumer path is nevertheless contract-invalid: two incompatible definitions of the carried field are used, and the carried perpendicular ledger introduced in the attempt-6 section is not propagated into the `step_007b`, Exported Interface Feasibility, or Generated Output Flow records.
- High-risk coverage: All theorem blocks are localized. Attempt 6 addresses the correct two high-risk classes, normalized predecessor output and carried perpendicular mass, but the stated exact predecessor and resolvent identities do not support the exported bounds. The first-sweep bridge and every later contraction block remain dependency-blocked.
- Explicit-rate coverage: Exposed variables, hidden-constant restrictions, probability/horizon/norm modes, finite stopping choice, and baseline reduction remain explicit. The numerical `ell_0`, `3072/3456/3888`, and `beta_*` rates are not yet derived from one coherent raw interface.
- Assumption and citation plausibility: Generated geometry, range entry, basin membership, contraction, and stopping remain derived rather than primitive assumptions. The blocking mechanism is direct current-notation algebra, not an unresolved citation. The Uschmajew citation remains confined to quotient interpretation.

## Early Obstruction Audit

- Limiting-case stress: The exact stationary orthogonal state still gives zero residual and zero common product register. A one-column diagonal normalization stress breaks the new predecessor certificate: with `H=I`, `A^pred=a e_1 e_1^T`, and `D_nu^pred=diag((1+a)^(-1),1,...)`, the normalized output is `B=I`, but the sketch's claimed identity `2diag(HA^pred)+diag((A^pred)^T H A^pred)=0` requires `2a+a^2=0`, which is false for a generic small `a`. Thus the alleged exact source is not a unit-column identity for a pre-normalization matrix.
- Theorem-critical bridge support: Earlier bridges remain source-backed. `E_carry_resolvent` is not: the Attempt-6 section defines `E_N=(I+A_N^pred)D_nu,N^pred-I` while simultaneously using `B_N=I+C_N+E_N`; the `step_007c` section instead asserts `I+C_N+E_N=(I+A_N^pred)D_nu,N^pred`. These are incompatible whenever the explicitly allowed `C_N` is nonzero.
- Exported-interface feasibility: The raw predecessor-output-to-carried-field map is ambiguous, so `epsilon_N` does not name one fixed residual. Moreover, the displayed difference formula for `A_M-A_M^0` is not the exact resolvent identity. For `A=Lambda(C-J)J^{-1}Lambda^{-1}`, a valid form contains either `-(C^0)(J^0)^{-1}(J-J^0)J^{-1}` after the `C-C^0` term, or contains both `-(J-J^0)J^{-1}` and the `(C^0-J^0)` inverse-difference term. The sketch's formula omits that contribution. Hence the claimed no-`kappa_res` paired bounds and ambient-column export do not follow from the displayed raw controls.
- Theorem-critical mechanism witness gate: PASS for smoothing, coverage, chart/certificate closure, classification, selected row/column closure, exact unfolding range, target-span contraction, stopping, timeout/restart, and baseline mechanisms. FAIL for the normalized predecessor/carry witness and the carried perpendicular witness. Both failures are same-setting sketch/interface defects.
- Entry-state trace stress: The selected state is traced through U, V, and W, and exact unfolding is a valid source for setting the updated mode's perpendicular part to zero. V/W quantitative legality is still conditional because the predecessor certificate and actual-record pair ledger are not coherently produced after U/V.
- Obligation locality classification: `E_sm`, `E_win/E_cov`, `E_RS/E_chart/E_cert`, `E_cert_eq/E_cert_tail`, `E_support_wt/E_cluster/E_gauge`, `E_col/E_row`, `E_best/E_quotient`, target-span invariance, target-span contraction, stopping, timeout/restart, and baseline reduction are `step-local`. The normalized predecessor certificate, the sharp two-orientation carry reserve, the chronological `E_perp_pair` ledger, and their `E_parallel_entry` export are `sketch/interface defect`. No obligation is an `idea/theorem-contract defect`.
- Noncircular closure gate: The named inter-step graph is acyclic and no future basin is assumed. The current failure is an invalid producer interface rather than circular range use. A repaired conditional `step_007c` lemma may still be instantiated chronologically inside `step_007b` after each exact range update.
- Mechanism-source and boundary stress: Unit-column normalization is an adequate source only when written for the actual normalized output. If a carried mode still has perpendicular residual `F_N=N_N^T N_N`, its correct relation is `diag(B_N^T H_N B_N-I)=-diag(F_N)`, not zero; after an exact predecessor range update, the producer must explicitly export `F_N=0`. The current sketch does neither consistently at the certificate boundary.
- Generated-output flow: All earlier flows are legal. `pred(U)` and `pred(V)` are named producers, but their output field is inconsistent across the repair and step sections. `E_perp_pair` is advertised as an initial `E_quotient` output while V/W require parameterized ledgers for newly carried coefficient matrices. The generated-output rows therefore do not describe the actual chronological consumer path.
- Source-to-claim adequacy: The static Neumann and centered-normalization sources remain plausible. For carried records, the false predecessor identity, inconsistent carried residual, and incorrect resolvent difference prevent the claimed `u_C`, `u_J`, `2u_C+u_J`, ambient-column, and product-log bounds from being classified as step-local.
- Residual-to-target adequacy: All objects use the same realized factors and quotient/Frobenius targets, so no surrogate-target mismatch is present. Adequacy fails at the residual decomposition: the static `C_N`, normalized predecessor output, carried increment, and carried-perpendicular terms are not separated by one exact identity, leaving unassigned residual terms at the V/W target scale.
- Baseline invariance audit: The inherited baseline is exact stationarity and zero residual at the orthogonal equal-weight deterministic specialization. Direct substitution gives `B=I`, `N=0`, `A=0`, `nu=1`, and `s=0`; this conclusion is preserved. The positive `beta_perp` envelope is correctly distinguished from actual zero contamination.
- Scope-accumulation compatibility: Proposal forcing is finite-horizon, first-sweep perpendicular forcing is intended to be removed mode by mode, target-span contraction has zero additive forcing, noncovered tapes are stopped at `m_cap`, and restart costs add over finitely many tapes. The three-call carry scope is finite, but that does not repair a missing one-call identity. Later geometric accumulation is valid only after a repaired `E_parallel_entry` producer.
- Scope and dependence consistency: No silent probability, rank, norm, or horizon upgrade is present. The attempt alternates between `beta_perp(M,h_M)`, a fixed initial `beta_perp`, and `eta_M`/`eta`; this prevents the advertised U/V/W constants from being tied to one exposed quantitative interface.
- Generated-condition provenance: The carried facts are not promoted to primitive assumptions, which is correct. Their intended predecessor producer is named, but the actual output certificate must be repaired before the conditional lemma is discharged.
- Citation and tool applicability: No cited theorem supplies the disputed numerical carry bounds. They must be proved directly. The quotient citation does not cure the predecessor, resolvent, or carried-pair defects.
- Same-setting repair plausibility: High. A single coherent post-normalization residual definition, the correct unit-column identity, the correct resolvent difference, and a parameterized actual-record perpendicular ledger can be inserted without changing assumptions, algorithm, theorem mode, metric, exposed rate, success criterion, or baseline.
- Target-preserving bridge-repair gate: The smallest sound route is `/proof-sketch`. Idea generation is not justified.
- High-risk obligation classes: Structural inverse floors, normalization, induced row/column transfer, ambient-column transfer, common-product-log transfer, generated range membership, all-sweep contraction, finite-time stopping, probability conversion, and baseline invariance were scanned. Only the first-sweep predecessor/carry interfaces fail.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_sm` | Static geometry and inverse floors | Gaussian normalization expansion, Schur product, Gershgorin | primitive assumptions -> direct derivation | Separate perturbation classes match the claim | Same normalized realized factors | smoothing margins plus finite union | Base slack exports `q_real<=q_*`, `Gamma<=1.01`, pair floor | Produced factors are consumed directly | One static union; no accumulation | assumptions -> `step_001` | `step_001` -> dynamic consumers | Orthogonal zero-smoothing limit is exact | step-local | None |
| `E_win`,`E_cov` | Proposal activation and rank rate | Gaussian regression, product tails, coupon bound | `E_sm` plus primitive initialization | Window rate and `k p_win` match coverage | Setting raw-coordinate convention | `p_win asymp r^(-5/3)(log r)^(-3/2)`; miss union | Raw windows export target-relative ratios | No surrogate | Fixed slots; misses union-bounded | `step_001 -> step_002 -> step_003` | `step_003` -> chart/restart | Initial denominator slack is explicit | step-local | None |
| `E_RS`,`E_chart`,`E_cert` | Recurrence, chart closure, certification | Exact old-state Jacobi map and self-map contraction | accepted earlier generated controls | Denominator/self-map/displacement sources match | Same simultaneous old-state convention | displayed `R,S,a_t` recurrences and `zeta` decay | Window controls export stored-state certificate | Same projective/ambient target | Persistent `q` forcing over fixed horizon | `E_win,E_sm -> step_004` | `step_004` -> certificate consumers | `q=0` exact squaring/fixed point passes | step-local | None |
| `E_cert_eq`,`E_cert_tail` | All-certified same-state equation and no-label tail ledger | Multilinearity, certificate residual, coefficient-vector expansion | `E_cert,E_sm` -> direct derivation | Right signs, scalar cancellation, finite-support charge match | Raw/barred conventions separated | `eta_G+eta_N+eta_C`, retained-floor/profile bounds | Every certified slot covered before labels | No premature target-distance claim | One finite residual; `sqrt(r)tau_r` once | `step_004 -> step_004b` | `step_004b` -> `step_005,step_006` | Stationary residual zero; transient retained | step-local | None |
| `E_support_wt`,`E_cluster`,`E_gauge` | No-label classification and representative selection | Weighted pair equations and score separation | derived certificate ledger | Weighted support gap matches graph claim | Same realized factors/scores | `s_mix*1.01<0.794<0.85s_-`; graph margins | Scores/correlations export one cluster per target | Graph metric matches target chart | Finite graph; strict margins | `step_004b -> step_005` | `step_005` -> selected bridge | Unequal-weight and sign boundaries handled | step-local | None |
| `E_col`,`E_row_residual`,`E_row` | Selected row/column closure | Dual equation and spatial small gain | accepted selected certificate outputs | Per-entry source supports both induced sums | Same target dual basis | column root and row self coefficient `<1/8` | Per-entry `2tau_r` separately sums in both orientations | Exact residual transfer | Finite unknown-sign `r tau_r`, absorbed once | classifier -> `step_006` | `step_006` -> `step_007` | Exact stationary state gives zero | step-local | None |
| `E_best`,`E_quotient` | Target-metric basin seed and entry decomposition | Tensor expansion, projector, product-one balance | selected row/column/gauge outputs | Direction/product errors match quotient/Frobenius target | Same realized tensor and target projector | four-class expansion and `512omega` bound | Exports actual `B_M,N_M,t_M` records | Original Frobenius metric controlled | One finite entry transfer | `step_006 -> step_007` | `step_007` -> first-sweep blocks | Exact target maps to zero state | step-local | None |
| Normalized predecessor certificate | Generated carried-direction identity for V/W | Exact range update plus column normalization | intended `step_007b` predecessor output | FAIL: pre-normalization `2diag(HA)+diag(A^THA)=0` is false, and `E_N` has incompatible definitions | Same realized basis, but pre/post-normalization conventions are conflated | Two mutually inconsistent equations for `B_N,C_N,E_N,A_N^pred,D_nu` | Raw output does not define one `epsilon_N`; `F_N=0` is not built into the certificate relation | Static comparator and carried residual cannot be transferred uniquely | Three calls only; one-call source invalid | `step_007b pred(U/V)` -> conditional `step_007c` invocation | Diagonal-scale normalization counterexample fails | sketch/interface defect | /proof-sketch |
| `E_perp_pair` | Chronological held-pair ledger | Orthogonal decomposition and actual held Gram expansion | initial `E_quotient`; carried records intended from predecessors | Initial static bound matches U, but V/W rows revert to `(1+48omega)^2` and omit `epsilon_N` | Same Euclidean projector | Attempt-6 parameterized bound conflicts with fixed `step_007b`/export rows; `beta_act=beta^0+beta^carry` is not defined as an exact split | No single actual-record envelope is routed through U/V/W | Carried-perpendicular class remains unassigned in consumer rows | Finite one-sweep forcing; intended modewise removal | `step_007 -> step_007c`; carried updates arise inside `step_007b` | Shared-perpendicular static stress passes; carried stress unresolved | sketch/interface defect | /proof-sketch |
| `E_carry_resolvent` | Sharp normalized one-block transfer | Exact solve, resolvent, centered normalization, diagonal similarity | direct current-notation derivation from held records | FAIL: displayed `A-A^0` identity omits a `Delta_J J^{-1}` contribution; predecessor centering source is false/ambiguous | Target/quotient convention otherwise matches | Desired `epsilon_M^+<=3072eta_M+ell_0 h_M` is stated, but `u_C,u_J,2u_C+u_J` do not follow | Raw controls do not export the claimed paired no-`kappa_res` inequalities in both orientations | Ambient, induced, and log residuals remain conditional | Three finite calls; unresolved one-step forcing | `E_quotient -> step_007c`; no future basin premise | Exact stationary boundary passes; mixed carried boundary fails | sketch/interface defect | /proof-sketch |
| `E_pair_entry`,`E_parallel_entry` | Ordinary inverses and exact target-span entry | Pair ledger, carry resolver, exact unfolding range | dependencies plus chronological direct derivation | Range source is adequate; quantitative margins depend on blocked interfaces | Exact literal ALS/realized span | U/V/W envelopes and `N_M^+=0` chronology | Inverse floors and `3072/3456/3888` lack one coherent `eta/beta` input | Correct target would be reached if repaired | Perpendicular defects intended to vanish modewise | `step_007c -> step_007b` | First U range update active; V/W margins unresolved | sketch/interface defect | /proof-sketch |
| `E_basin_parallel` | Invariant target-span quotient contraction | Exact range invariance, pair floor, scale-equivariant refresh | `E_parallel_entry,E_sm` -> direct derivation | Mechanism matches once valid entry exists; currently dependency-blocked | Literal orbit and quotient representative bridged | refreshed output recurrence and `d_Q` contraction | Target-span controls export full-sweep map only after repaired entry | Quotient metric bridges to Frobenius target | Zero additive forcing; geometric budget | entry -> `step_008` | `step_008` -> `step_009` | Exact point fixed; unrestricted tube excluded | step-local | /proof-sketch |
| `E_stop` | Relative-error stopping | Quotient contraction and residual equivalence | accepted basin output | Geometric rate matches original residual target; currently dependency-blocked | Original rank-k loss | explicit `m_epsilon` and residual bridge | Feasible after valid basin entry | Original Frobenius residual | Finite geometric horizon | `step_008 -> step_009` | `step_009` -> runtime/theorem | Exact baseline stops at zero | step-local | /proof-sketch |
| `E_run_cap`,`E_restart` | All-tape termination, cost, confidence | Pre-registered counter, Moore-Penrose branch, independent tapes | procedure fields plus `E_stop` on covered tapes | Counter and restart sources match claims; success branch is dependency-blocked | Observable dense algorithm | finite counter; costs add; failures multiply | No latent-factor oracle; timeout exports no success | Success uses original residual | Arbitrary defects stopped at `m_cap`; finite `J` | cap -> `step_010`; success path separate | `step_010` -> final theorem | Noncovered nonhitting/singular tape exits | step-local | /proof-sketch |
| Orthogonal equal-weight baseline | Exact stationary recovery and zero-error limit | Direct Jacobi/ALS substitution | deterministic specialization | Exact equality, not only vanishing bounds | Produced and consumed tensor identical | exact squaring, `B=I,A=0,nu=1,s=0` | No surrogate or positive error floor | Exact target equality | Zero forcing and stationary trace | direct baseline bridge | `step_011` -> theorem | First and later stationary updates exact | step-local | None |

## Blocking Issues

1. **Attempt-6 repair and `step_007c`: the normalized predecessor certificate is internally inconsistent and contains a false exact identity.** `E_N=(I+A_N^pred)D_nu,N^pred-I` together with `B_N=I+C_N+E_N` conflicts with the later identity `I+C_N+E_N=(I+A_N^pred)D_nu,N^pred` when `C_N` is nonzero. In addition, unit normalization of `(I+A_N^pred)D_nu,N^pred` does not imply `2diag(H_NA_N^pred)+diag((A_N^pred)^TH_NA_N^pred)=0`; the diagonal-scale stress above is a direct counterexample. Downstream effect: neither centered diagonal suppression nor `epsilon_N` provenance is valid. Smallest repair: `/proof-sketch`.
2. **Attempt-6 resolvent block: the displayed difference identity for `A_M-A_M^0` is algebraically incomplete.** It combines a `C-C^0` first term with a `(C^0-J^0)` inverse-difference term but omits the corresponding `-(J-J^0)J^{-1}` contribution. Downstream effect: the no-extra-resolvent-factor `u_C/u_J`, both induced orientations, ambient-column transfer, product-log transfer, and `ell_0=17/512` reserve remain assertions rather than consequences. Smallest repair: `/proof-sketch`.
3. **`E_perp_pair` and `step_007b`: the carried-aware ledger is not propagated to the consumer contract.** Attempt 6 introduces `beta_perp(M,h_M)` with `(1+48omega+epsilon_N)^2`, but `step_007b` still uses `||H_N^par||<=(1+q_*)(1+48omega)^2` and a fixed initial `beta_perp`; the exported-interface and flow rows repeat the static formula. The claimed exact split `beta_act=beta_perp^0+beta_perp^carry` also conflates actual contributions with numerical upper bounds. Downstream effect: V/W pair-Gram margins and the `3456/3888` chronology are unsupported. Smallest repair: `/proof-sketch`.
4. **Gate and flow rows do not expose the repaired raw controls.** The `E_carry_resolvent` row omits the alleged predecessor matrix/normalization identities, and the `E_perp_pair`, `E_pair_entry`, and `E_parallel_entry` rows alternate between fixed `beta_perp`, parameterized `beta_perp(M,h_M)`, `eta`, and `eta_M`. Downstream effect: no single producer-consumer path discharges every residual class and numerical margin. Smallest repair: `/proof-sketch`.

## Required Repair Bundle

1. At the Attempt-6 repair and `step_007c`, choose one carried-field convention and use it everywhere. Either define the total normalized error `E_N:=B_N-I` and set the static field to zero after that mode is overwritten, or define the incremental error `E_N:=B_N-(I+C_N)` and retain the exact identity `I+C_N+E_N=(I+A_N^pred)D_nu,N^pred`. State exactly which quantity `epsilon_N` bounds. Affected IDs: `step_007c`, `step_007b`, `E_carry_resolvent`. Smallest target: `/proof-sketch`.
2. Replace the false predecessor identity by the correct unit-column relation for the actual normalized output. If the predecessor range update gives `N_N=0`, state that in the producer certificate and use `diag(B_N^TH_NB_N)=1`; if a perpendicular field remains, use `diag(B_N^TH_NB_N-I)=-diag(F_N)`. When using an incremental comparator, subtract a separately valid comparator identity and retain every `C_N/E_N/F_N` cross term. Derive the centered row and transposed-column consequences explicitly. Smallest target: `/proof-sketch`.
3. Correct the exact resolvent difference. For example, use `A-A^0=Lambda[(Delta_C-Delta_J)J^{-1}-(C^0-J^0)(J^0)^{-1}Delta_JJ^{-1}]Lambda^{-1}`, or the algebraically equivalent `Lambda[Delta_CJ^{-1}-C^0(J^0)^{-1}Delta_JJ^{-1}]Lambda^{-1}`. Then redo every diagonal-similarity factor, both induced orientations, ambient-column bound, centered normalization difference, and common-product-log bound before reasserting `2u_C+u_J` or `ell_0`. Smallest target: `/proof-sketch`.
4. Define the perpendicular split as actual matrices first and upper-bound their norms second. Parameterize the conditional ledger by the actual chronological `B_N`, `epsilon_N`, and `t_N`; use the carried `(1+48omega+epsilon_N)^2` factors at V/W, exploit `t_U=0` and then `t_V=0` only after their exact range producers, and avoid equality between `beta_act` and quantities defined only as envelopes. Synchronize `step_007b`, Exported Interface Feasibility, Generated Output Flow, and the step table. Smallest target: `/proof-sketch`.
5. With the repaired identities, rerun the U/V/W domains using one declared envelope such as `beta_*` and one consistent `eta_*`. Verify `Delta_J`, `d_M`, positivity, `h_V/h_W`, and the exact `3072/3456/3888` implications in both orientations and for ambient columns/product log. Update every affected gate row and remove `Blockers = None` until those relations are present. Smallest target: `/proof-sketch`.
6. Target-preserving repair check: these changes alter only the sketch-level bridge, residual definitions, and dependency interfaces. They require no new primitive assumption, algorithm change, theorem scope/mode/metric change, exposed-rate change, success-criterion change, unsupported theorem-facing source, or weakened baseline. Routing to idea generation is not justified.

## Review Rationale

`REVISE_SKETCH` is the deepest necessary change. Attempt 6 correctly identifies normalized predecessor output and carried perpendicular mass as the missing mechanisms, and the setting still plausibly supports a chronological first-sweep bridge. However, the current artifact cannot be accepted for proof-step work because its central predecessor certificate uses incompatible object definitions and a false normalization identity, its resolvent difference is incomplete, and its carried pair ledger is not reflected by the consumer and gate rows. These are target-preserving producer/interface defects, not evidence that the formalized theorem or idea must change.
