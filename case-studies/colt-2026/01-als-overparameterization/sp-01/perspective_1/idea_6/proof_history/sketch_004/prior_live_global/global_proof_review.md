# Global Proof Review

## Global-Proof Review Identity

- Branch: perspective_1/idea_6.
- Sketch attempt: 2.
- Reviewed setting.md SHA-256:
  a6991b0e308a8a33e782be75f9f081325404974335e5c00df85e6d8a80921cab.
- Reviewed proof_sketch.md SHA-256:
  c6126ab9b81d7a7cbcae3b9b857923f3fe8e275ed1ba4376fd830270c5ec95e1.
- Reviewed proof_sketch_review.md SHA-256:
  524bc3e32fc6d334d4c6c78bf8e347888df2cd4bbbd21bf7c3dc1414c0c9b3a4;
  its controlled status is ACCEPTED and its smallest retry target is None.
- Reviewed global_proof.md: global unit attempt 1 under sketch attempt 2,
  SHA-256
  cc2840004ec9cc0f943dd6003c8c146301159403089bae3db51e24106f55ae98.
- Reviewed global proof status: COMPLETE_DRAFT.
- Reviewed suggested routing: None.
- Goal mode: exact-goal. Progress type: conditional.
- The sketch-attempt-1 global and review are archival and were not consumed
  as current proof evidence.

## Global-Proof Review Status

ACCEPTED

## Smallest Retry Target

None

## Blocking Issues

None.

## Required Repair Bundle

None.

## Contract And Status Audit

The reviewed diagnostic contains the required identity, controlled status,
attempted theorem claim, whole-proof draft, theorem-level block map,
sketch-step coverage, dependency and assumption audit, citation and tool
audit, quantitative audit, scope-and-closure certificate, exported-interface
audit, generated-output flow, early obstruction audit, gap classification,
diagnostic-boundary note, and suggested routing. COMPLETE_DRAFT with
Suggested Routing = None is controlled and aligned.

The diagnostic boundary is explicit: global_proof.md is not proof evidence,
an assumption source, a cited result, or authority to modify an accepted
step. The document does not consume archived step proofs. Every numerical
derivation remains assigned to fresh proof-step work under the frozen sketch.

The G10 residual display contains a punctuation comma between
8 kappa_0^2 and 4^{-(m-1)}. The surrounding sentence, G11 cap definition,
quantitative-specialization row, and accepted sketch all unambiguously fix
the relation as multiplication. This is a nonmaterial typesetting blemish,
not a changed interface or an ambiguity for downstream work.

## Claim And Sketch Fidelity Audit

The attempted claim preserves the accepted theorem domain and conclusion:
the asymmetric realized third-order CP tensor; the eight primitive assump:*
conditions; exact old-state Jacobi proposals; active cyclic Moore--Penrose
ALS; rank

  k = ceil(C_rank r^(5/3) (log r)^(5/2));

the original relative Frobenius success metric; the separate smoothing and
conditional initialization probabilities; polynomial runtime with the
listed dependence; and the exact orthogonal equal-weight baseline.

The diagnostic preserves the sole controller-approved semantic delta of
sketch attempt 2. Before active refinement, the run controller fixes the
observable cap

  m_cap = 1 + ceil(log(8 kappa_0^2/epsilon)/log 4).

The cap changes no proposal, filter, cluster rule, initialization, ALS block
solve, residual threshold, successful output, rank, or selector. It is
nonbinding on the produced E_cov success witness and only terminates a
nonhitting tape that was not assigned a convergence claim. The diagnostic
does not broaden that approved delta or infer basin membership from the
observable cluster gate.

The proof-only quotient repair is also faithful. A state has one common
represented-product register e=(C_U,C_V,C_W,s). The exact chronological
states are e_U, e_V, and e_W, each refresh overwrites the preceding s value,
and only e_W is the completed full-sweep state. No raw per-mode amplitude,
intermediate full-state contraction, projection, or additional algorithmic
operation is introduced. The rho=0 language remains a deterministic
algebraic specialization and not a probability assertion outside the
positive-rho setting.

## Theorem-Level Structure Audit

All accepted sketch rows have a coherent theorem-level role:

- G1 / step_001 produces realized norms, interference, weights, and cyclic
  Khatri--Rao floors.
- G2-G3 / step_002-step_003 produce the exact window rate, rank
  specialization, and one-run all-target coverage.
- G4 / step_004 produces denominator persistence, chart membership, and
  stored certification.
- G5 / step_004b produces the same-state equation and exhaustive no-label
  support ledger before classification.
- G6 / step_005 produces weighted filtering, clustering, signs, and the
  product-preserving gauge.
- G7 / step_006-step_007 closes selected row/column mass and transfers the
  observable initialization into the exact target metric.
- G8 / step_007b retains collective perpendicular forcing, proves every
  first-sweep inverse, and generates exact target-span entry.
- G9 / step_008 constructs the literal-orbit quotient chronology and the
  completed-state contraction.
- G10 / step_009 converts the produced recurrence to the original residual
  and a finite covered-tape hitting horizon.
- G11 / step_010 separately gives every tape a finite exit and dense cost,
  then performs restart amplification.
- G12 / step_011 preserves the exact orthogonal equal-weight conclusion.

The final assembly uses G1 for the outer instance event. Conditional on that
fixed tensor, E_cov follows the G4-G10 success path, while E_run_cap follows
an independent all-tape termination path. Those paths meet only in G11's
completed-run restart product and cost sum. This block order covers every
accepted Step ID and does not create a consumer-before-producer edge.

## Dependency And Assumption Audit

Exactly the eight stable assump:* entries are theorem-facing primitives.
Every other condition is classified as derived. The successful-tape chain is

  E_sm -> E_win -> E_cov -> E_RS/E_chart/E_cert
  -> E_cert_eq/E_cert_tail
  -> E_support_wt/E_cluster/E_gauge
  -> E_col/E_row_residual/E_row
  -> E_best/E_quotient
  -> E_pair_entry/E_parallel_entry
  -> E_basin_parallel -> E_stop.

Within the quotient block, the generated order is

  E_parallel_entry -> U_U^q -> Refresh_s^U/e_U
  -> U_V^q -> Refresh_s^V/e_V
  -> U_W^q -> Refresh_s^W/e_W
  -> Psi^q -> E_basin_parallel.

The independent termination chain is

  pre-register m_cap -> finite proposal exit or capped active phase
  -> original-residual hit or timeout -> E_run_cap
  -> completed tapes -> E_restart.

E_cov and E_stop supply the positive one-run success inclusion. E_run_cap
supplies completion and cost for all tapes. Neither output is substituted for
the other. Thus an unconditional public conclusion is not completed by
assuming a generated chart, basin, stopping event, inverse floor, or
convergence event.

## Citation And Tool Audit

The direct and standard tools are mapped to the exact branch objects.
Gaussian normalization and directional concentration use the setting's
N(0,rho^2 I/n) perturbations and finite row/union scope. Schur product and
Gershgorin use the realized unit-column Grams. Conditional Gaussian
regression and product tails use the raw proposal coordinates. Banach's
theorem is consumed only after step_004 proves the current chart self-map,
completeness, and derivative bound. Multilinearity, target-dual algebra,
Hadamard inequalities, resolvent identities, exact unfoldings, and
Moore--Penrose block identities all use the setting's cyclic order, active
factors, and frozen zero columns. Their convention checks, raw assumptions,
quantitative margins, boundary branches, and output interfaces are stated.

The only theorem-critical external source is Andre Uschmajew, Local
Convergence of the Alternating Least Squares Algorithm for Canonical Tensor
Approximation, DOI 10.1137/110843587, Assumption 1, Lemma 3.2, and Theorems
3.3 and 3.5. Its role is limited to the qualitative scaling quotient and
block-Gauss--Seidel interpretation. The mapped branch objects are the active
rank-r CP factors modulo positive componentwise product-one scaling in the
literal U/V/W block order. Target-span entry, active inverse floors, the
current quotient kernel, scale cancellation, refresh identity, radius,
constant ell, original-residual comparison, timeout, and dense cost are
explicit known non-outputs and remain direct local obligations. The citation
therefore supplies no unsupported numerical or generated-condition claim.

The dense cost is an observed-input operation count: multiplication of dense
unfoldings by current Khatri--Rao designs, factor and pair Grams, an ordinary
inverse or pseudoinverse, exact residual evaluation, and frozen-zero
bookkeeping. It never invokes latent generating matrices.

## Quantitative Dependence Audit

The diagnostic preserves all exposed variables and modes:

- Instance geometry exposes n,r,kappa_0,rho,delta_sm and the fixed regime
  exponents, with separate linear directional, centered quadratic, and
  normalization contributions.
- Proposal and certification expose r,k,q_real,Gamma,tau_r,L_burn,L_cert.
- Entry and refinement expose omega,beta_perp,rho_ALS,ell,kappa_0,epsilon.
- Timeout and restart expose m_cap and delta_init, without putting either
  confidence parameter into k.
- The probability mode remains Pr(E_sm)>=1-delta_sm followed, for every
  fixed tensor in E_sm, by conditional success at least 1-delta_init.
- The metric changes are explicit: Euclidean columns, induced row/column
  l1 mass, pair-Gram eigenvalues, d_Q, and finally the original Frobenius
  residual.

The numerical specialization is coherent. The window rate
Theta(r^(-5/3)(log r)^(-3/2)) times k gives order log r. With
tau_r=q_*^2/(10^4 r),

  omega=q_*^2+r tau_r<6.0e-8,
  beta_perp<10^(-8)omega,
  4096(omega+beta_perp)<rho_ALS/3.

For q_*=1/4096 and rho_ALS=1/1024,
ell=8q_*+32rho_ALS=0.033203125<1/16, so the post-entry geometric budget is
less than 16/15 and the public stopping calculation may conservatively use
1/4. The cap equals the covered-tape horizon, and
4^{-(m_cap-1)}<=epsilon/(8kappa_0^2). With p_0>=1/2 and completed independent
tapes, the chosen J gives (1-p_0)^J<=delta_init and total cost at most
J T_run. All costs are polynomial in the exact variables listed by the goal.

The baseline audit is also quantitative: finite q=0 certified transients keep
their certificate and first-sweep charges, while only the exact stationary
orthogonal equal-weight state sets those defects and every refreshed s value
to zero. The cap introduces no positive residual floor.

## Scope And Closure Review

The scope-and-closure certificate covers every theorem-critical generated
condition, exported interface, all-time recurrence, lower/sign/support claim,
quantitative specialization, timeout, restart output, and baseline
obligation. Each row identifies its claim class, positive source, defects,
concrete relation, producer path, residual target, boundary trace, and
step-local verdict.

The finite-scope rows are noncircular. G1 is a finite static union. G2-G3 are
finite slot and target unions. G4 exposes the exact R,S,a recurrences,
positive denominator, chart self-map, and finite certification horizon.
G5-G7 expose finite residual, threshold, row/column, root, and absorption
ledgers. G8 charges the mixed tau_r^2 and coherent r tau_r^4 terms during one
literal cyclic sweep and removes each perpendicular field by exact unfolding
range containment before G9 uses target-span membership.

The all-sweep G9 row passes the Noncircular Closure Gate. Its entry is an
output of G8. For held-direction discrepancy D_h, the solved-output relation
is

  max(c_M^out, ||Delta s_M^out||_infinity) <= ell D_h.

Chronological substitution gives U and V outputs at most ell D and the W
output at most ell^2 D. The completed state satisfies

  d_Q(Psi^q(e),Psi^q(e_tilde)) <= ell d_Q(e,e_tilde).

Exact span invariance makes perpendicular forcing zero at every later
subupdate, and the earlier product values are overwritten. Thus
||e^(t+1)||_Q<=ell||e^t||_Q has a concrete geometric budget and no hidden
additive, persistent, or adversarial-sign term.

The Entry-State / Activation Trace Gate passes. A covered proposal begins
with nonzero target coordinates and a denominator margin. The actual first
cyclic sweep is traced U, V, W with its three inverse floors and enters the
exact-span tube with norm below rho_ALS/3. The first contracted sweep then
runs the exact refresh chronology. At the exact target, every block and
refresh is stationary. A noncovered gate-passing tape is not assigned this
mechanism: its first update is a finite Moore--Penrose computation, and the
counter reaches a residual hit or the observable timeout boundary.

E_stop is restricted to the E_cov producer path and controls the original
Frobenius residual. E_run_cap controls only termination and dense cost; a
timeout exports no theorem target. E_restart combines the positive success
probability with finite completed tapes. This division makes the generated
flow and residual-to-target bridge legal and prevents a cap-to-convergence or
cluster-to-basin circularity.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E_sm | Static geometry and pair nondegeneracy | Base norm/Gram/weight slack; Gaussian concentration; Schur/Gershgorin | Five geometry primitives -> step_001 | Exact norm floor, q_real, Gamma, and cyclic pair floors | Same normalized realized factors and Euclidean Grams as setting | Three perturbation classes separately dominated; pair off-diagonal mass <=q_*^2 | Base margins, finite union factors, and thresholds exposed | Produced factors are exactly all later target factors | Finite static tails; no recurrence | Primitives -> G1 -> consumers | step_001 produces; G2-G4,G8-G9 consume | Orthogonal boundary exact; zero norm and singular pair excluded by margins | step-local | None |
| E_win,E_cov | Rare activation, rank rate, and all-target coverage | Gaussian window density, regression/product tails, independent slots | E_sm and proposal primitive -> step_002-step_003 | Raw window gives R_0,S_0; miss bound gives p_0 | Raw coordinates equal setting proposal coordinates and target ratios | p_win=Theta(r^-5/3(log r)^-3/2); miss <=exp(-kp_win); kp_win>=8log r | a_*,b_*,k, finite-small-r constants, no cross-target independence | Window and consumed realized-target entry coincide | Finite competitor tails and slot misses | G1 -> G2 -> G3 before chart use | step_002 produces E_win; step_003 produces E_cov,p_0; G4,G6,G11 consume | Miss is failure; covered entry has nonzero targets and R_0<=19/20 | step-local | None |
| E_RS,E_chart,E_cert | Denominator invariant, chart, and stored certificate | Exact old-state Jacobi expansion and Banach chart | E_sm,E_cov -> step_004 | Same old state drives all modes; stored state compared with own look-ahead | Projective signs, ratios, and Euclidean chart match procedure | Displayed R,S,a recurrences; denominator >=31/32; self-map; L_chart<1/4; zeta decay | Initial pair mass, q forcing, horizon, radii, norm floor exposed | Same realized target; no look-ahead surrogate exported | Persistent q forcing absorbed over finite O(log r) horizon | Coverage -> recurrence -> chart -> certificate | G4 produces; G5-G6 consume | First update activated; q=0 is exact pair map; zero contraction exits | step-local | None |
| E_cert_eq,E_cert_tail | Same-state equation and exhaustive no-label ledger | Multilinearity, vector Gram expansion, scalar cancellation | E_cert,E_sm -> step_004b | Raw right-sign and barred equations; LOW/singleton/multi/straddle exhaustive | Same stored state, diag(lambda), right signs, product-preserving bars | eta_G=2q_*+q_*^2; eta_N=5q_*^2; eta_C=2sqrt(r)tau_r; retained-floor/profile bounds | Positive d_M, all thresholds and finite residual classes exposed | Compares state to own contraction; target transfer deferred legally | One sign-unknown finite residual per mode/slot | Certificate -> ledger before labels or clustering | G5 produces; G6-G7 consume | Finite q=0 transient retains residual; stationary state zero; d_M=0 exits | step-local | None |
| E_support_wt,E_cluster,E_gauge | Weighted support, score, graph, and sign/gauge | Weighted ledger and strict score/correlation margins | G5 plus E_cov witness -> step_005 | Exact filter/graph rules and one representative per target | Realized weights/correlations and initialized sign convention retained | 1.01s_mix<0.794<0.85s_-; component ratio >0.901; graph gaps | LOW, multi, straddle, unequal weights, negative/zero score, ties exposed | Selected directions are actual factors; sign gauge preserves tensor | Finite pool and graph; no accumulation | Ledger/coverage -> classifier -> cluster/gauge | G6 produces; G7 consumes | Exact components survive; mixtures lose; theta=0 excluded | step-local | None |
| E_col,E_row_residual,E_row | Selected target-dual row/column small gain | Exact dual equation, per-entry transfer, small root, row absorption | G5-G6 -> step_006 | Exact induced interfaces needed by target transfer | Same realized dual basis and row/column norm convention | chi<=32omega; per entry <=2tau_r; row/column residual <=2r tau_r; self coefficient <1/8 | Denominator, both roots, separate index sums, forcing classes exposed | Ambient residual transferred through exact target dual | Unknown-sign forcing finite at r tau_r; one row absorption | Classifier/equation -> column root -> residual -> row | step_006 produces; step_007 consumes | Large root excluded by chart; exact state zero; no r^2 tau_r | step-local | None |
| E_best,E_quotient | Same-target balanced initialization and projector bridge | Direct tensor expansion, unit norms, exact projector, product-one balance | G6-G7 and E_sm -> step_007 | Exact quotient/Frobenius and B+N interfaces for entry | Same realized tensor, spans, signs, and product register | <=512omega; B-I row/column <=48omega; G_M^T N_M=0; columns N_M<=tau_r | Scalar, direction, pair/cubic, row/column, perpendicular classes exposed | Original Frobenius metric and exact target projector, no surrogate | One finite expansion | Selected controls -> G7 -> G8 | step_007 produces; step_007b consumes | Exact component B=I,N=0,s=0; zero theta excluded | step-local | None |
| E_pair_entry,E_parallel_entry | First-sweep pair floor and generated exact-span entry | Projector Gram identity, target floor, resolvent, exact unfolding range | E_sm,E_best,E_quotient -> step_007b | Exact held designs and literal ALS outputs | Same cyclic order, amplitudes, active factors, zero padding, target | beta_perp; deviations 96/192; floor 31/32; envelopes 3072/3456/4096; N_M^+=0 | Mixed tau_r^2, coherent r tau_r^4, coefficient/amplitude defects all exposed | Outputs are exact G_M Z_M states and same represented rank-k tensor | Unknown-sign defects charged once and removed mode by mode | G7 decomposition -> G8 inverse/range trace -> entry before G9 | G8 produces; G9-G10 consume | Shared-perpendicular case charged at actual tau_r; target stationary; singular boundary excluded | step-local | None |
| U_M^q,Refresh_s^M | Solved-output and literal-orbit chronology | Exact normal equations, scale equivariance, canonical balance | E_parallel_entry,E_sm -> internal step_008 chain | Output depends only on held directions; refresh gives exact intermediate quotient | Positive product-one action, one common s, U/V/W order match setting | gamma_out formula; output <=ell D_h; refresh overwrites common register | Held directions, inverse, product formula, chart, and consumers fixed | e_U,e_V,e_W represent exact literal intermediate tensors | Historical U/V product values overwritten, not accumulated | Entry -> U/refresh -> V/refresh -> W/refresh | step_008 produces internal states; next block/Psi consume | Target writes s=0; pure scaling distance zero; singular columns excluded | step-local | None |
| E_basin_parallel,Psi^q | Invariant tube and all-sweep contraction | Exact span invariance, pair positivity, held-input Lipschitz, quotient kernel | G8 entry plus E_sm -> step_008 | Completed e_W is exact interface consumed by stopping | Same spans, quotient action, d_Q, tensor, and original loss | U/V <=ell D; W <=ell^2D; completed <=ell D; recurrence <=ell | Entry <rho_ALS/3, pair floor, intermediate legality, zero forcing exposed | e_W is literal post-sweep quotient; chart comparison reaches original residual | No additive forcing; geometric budget <16/15 | G8 membership -> G9 preservation/recurrence -> G10 | step_008 produces; step_009 consumes | First contracted state is actual entry; target fixed; tube/singular boundary traced | step-local | None |
| E_stop | Original residual and covered-tape finite hit | Entry, quotient contraction, balanced same-target comparison | E_parallel_entry,E_basin_parallel -> step_009 | Converts produced state to exact procedure stopping rule | Same active/frozen-zero factors and original Frobenius loss | residual <=8kappa_0^2 4^(-(m-1)); m_epsilon displayed | Entry offset, conservative factor, metric constant, inactive columns exposed | Original tensor residual; no quotient surrogate at export | Finite geometric horizon only on covered path | Entry/basin produced before stop | step_009 produces; G11/final success consume | Exact target stops immediately; arbitrary gate tape excluded | step-local | None |
| E_run_cap | Every-tape termination and dense cost | Approved fixed cap, observable counter/residual, Moore--Penrose rule | Sketch-attempt-2 procedure clause -> step_010 | Source proves termination/cost only, not basin or convergence | Uses setting parameters, observed T/current factors, exact residual convention | t->t+1 until hit or t=m_cap; T_run <= proposal cost + m_cap dense-sweep cost | Cap fixed before refinement; all early exits, singular/nonhit branches exposed | Only residual hits export; timeout exports no tensor target | Arbitrary persistent defects allowed but stopped after finite budget | Cap -> finite proposal/capped phase -> hit/timeout -> E_run_cap | step_010 produces; restart/runtime consume | Singular noncovered nonhitting first tape times out; exact baseline hits | step-local | None |
| E_restart | Conditional confidence and total finite runtime | E_cov success inclusion, E_stop, E_run_cap, independent completed tapes | step_003,step_009,step_010 plus randomness primitive | Success and completion sources have distinct exact roles | Same fixed tensor; independent proposal tapes; original selector | (1-p_0)^J<=delta_init; T_total<=J T_run | p_0>=1/2, J, all failures/timeouts, per-run cost exposed | Selector returns only an original-residual hit | Finite product and sum over exactly J tapes | Success path plus independent cap path -> completed restarts | G11 produces; final assembly consumes | First tape may fail arbitrarily; J=1 valid; tensor not resampled | step-local | None |
| Orthogonal equal-weight baseline | Exact/noiseless invariance and zero floor | Direct Jacobi substitution and exact CP normal equations | Deterministic specialization -> step_011 | Exact ratio map, fixed point, refreshes, and zero residual | Same factors/procedure; rho=0 explicitly nonprobabilistic | (A+,B+,C+)=(BC,AC,AB); Psi^q(0)=0; every refresh s=0 | Identity pair Gram and exact stationary representation exposed | Produced and consumed tensors identical | Stationary zero defect; finite transients kept separate | Direct specialization after target-preserving interfaces | step_011 produces; final baseline consumes | First update/refresh stationary; finite certificate transient not called exact | step-local | None |
| Final theorem assembly | Nested probability, rank, accuracy, runtime, baseline | E_sm, E_cov->E_stop, E_run_cap->E_restart, frozen-zero identity, baseline | G1-G12 -> assembly | Exact quantifier order and public outputs | Same tensor, conditional tapes, SC-JEP-ALS selector, Frobenius metric | Outer >=1-delta_sm; conditional >=1-delta_init; residual <=epsilon; terms <=k | Every event, cost, hit, rank, and specialization produced before export | Returned tensor and baseline compared to exact realized target | Finite nesting; probabilities not merged; no timeout defect exported | Static event -> conditional generated chains -> assembly | Assembly consumes all prior outputs and exports theorem | Confidence modes separate; all-fail event allowed; baseline zero | step-local | None |

## Hard-Step Localization Audit

| Step ID | Unresolved theorem-critical obligation | Locality classification | Audit basis |
| --- | --- | --- | --- |
| step_001 | Three-scale normalized smoothing bounds and cyclic pair floors | step-local | Primitive margins, exact convention, union scope, positive sources, and consumers are fixed. |
| step_002 | Two-sided target window and competitor-product probability | step-local | Raw coordinates, rate, regression convention, pair event, and finite-small-r bridge are fixed. |
| step_003 | All-target miss bound and p_0 | step-local | Independent slots, target union, k specialization, and output scope are fixed. |
| step_004 | R/S/a recurrences, denominator invariant, chart derivative, and certificate horizon | step-local | Entry activation, concrete recurrences, self-map, finite scope, and boundary exits are fixed. |
| step_004b | Right-sign same-state equation and exhaustive tail ledger | step-local | Object identities, signs, defect classes, finite-support conversion, thresholds, and consumers are fixed. |
| step_005 | Weighted classifier, filter/graph gaps, signs, and unequal-weight branches | step-local | Exact profile, strict margins, no-label flow, and all score/support boundaries are exposed. |
| step_006 | Column small root, per-entry residual, and row absorption | step-local | Raw dual equation, both induced norms, forcing budget, root branch, and absorption relation are fixed. |
| step_007 | Best-scalar, tensor residual, and exact-projector transfer | step-local | Same-target decomposition, defect split, target norm, gauge, and entry outputs are fixed. |
| step_007b | Collective pair-Gram audit and first-sweep range entry | step-local | Mixed/coherent perpendicular terms, three inverse branches, scale chronology, zero padding, and range source are fixed. |
| step_008 | Quotient kernel, scale cancellation, refresh identities, held-input estimates, and completed-state recurrence | step-local | Coordinate codomains, source convention, one-register chronology, concrete recurrence, and original-target interface are fixed. |
| step_009 | Same-target residual comparison and covered-tape sweep count | step-local | Actual entry, legal full-sweep recurrence, one-sweep offset, metric bridge, and target threshold are fixed. |
| step_010 | All-tape counter, dense cost, success inclusion, restart product, and cost sum | step-local | Cap timing, observable branches, pseudoinverse computation, E_cov scope, completed-tape independence, and outputs are fixed. |
| step_011 | Exact stationary baseline and finite-transient distinction | step-local | Direct algebraic source, nonprobabilistic specialization, first-update trace, and zero-floor target are fixed. |
| Final assembly | Nested quantifiers, returned rank/residual, runtime, and baseline conjunction | step-local | Every input is produced earlier; quantifier order, metric, rank, selector, and dependence are fixed. |

No future worker is being asked to invent a theorem-facing mechanism source,
new bridge, changed dependency, changed output, or changed theorem contract.
In particular, step_008 receives the exact-span entry and a fully specified
common-register chronology, while step_010 receives both the covered success
horizon and a separate finite counter for arbitrary tapes. A future failure
of one of the displayed derivations must be reviewed on its evidence, but the
current global diagnostic has localized each obligation under the unchanged
accepted interfaces.

## Early Idea-Failure Screen

- Contract contradiction: none. The exact-goal mode, primitive conditions,
  algorithmic updates, rank, metric, probabilities, exposed dependence, and
  success conclusion remain aligned. The approved cap affects unsuccessful
  termination only and is nonbinding on the success witness.
- Missing mechanism: none at diagnostic granularity. Norm/Gram slack supplies
  static positivity; target coordinates supply proposal activation;
  component scores and pair floors supply classification and inverse margins;
  exact unfolding supplies span entry; held-direction normal equations supply
  quotient contraction; the integer budget supplies arbitrary-tape exit.
- Source-convention stress: no source object is whitened, transformed,
  population-only, or measured in a foreign metric. Canonical balancing is a
  product-preserving quotient representative, and each source-to-branch
  convention bridge is stated.
- Residual-to-target stress: certificate residual, selected dual residual,
  exact projector residual, quotient state, and returned residual have explicit
  bridges to the same realized tensor. A timeout has no target output and is
  not used as an approximate solution.
- Persistent-defect stress: finite proposal/certificate and first-sweep
  defects are charged once. Post-entry perpendicular forcing is exactly zero
  and the quotient recurrence is contractive. Arbitrary noncovered defects
  may persist but are stopped after m_cap and never cross runs.
- Limiting and boundary stress: zero contractions and zero scores exit;
  shared perpendicular components are charged at tau_r before removal;
  singular arbitrary-tape updates use Moore--Penrose and time out; exact
  orthogonal factors are stationary with zero common register and residual.
- Entry-state trace: E_cov activates denominators and the finite proposal
  chart; the literal first active sweep generates exact-span entry before
  contraction is consumed. A noncovered first tape is traced through a
  finite computation and counter decrement rather than assigned convergence.
- Generated-condition and flow stress: every chart, support, cluster, basin,
  stopping, timeout, and restart output has a legal producer and dependency
  path. Closure, specialization, and final assembly do not manufacture a
  missing event.
- Dependence and mode stress: confidence does not enter rank; the two
  probability modes remain nested; all horizons and norm transfers are
  exposed; no unlisted condition number or latent-factor oracle is used.
- Baseline invariance: finite q=0 transients are not confused with the exact
  stationary state. At stationarity every block, refresh, and residual is
  exact, so arbitrary accuracy and the zero-error limit are preserved.
- Same-setting repair cut: no repair is currently required. No visible
  obstruction requires changed primitive assumptions, procedure, theorem
  scope, metric, exposed dependence, success criterion, added theorem-facing
  mechanism source, or weakened conclusion.

The early screen finds no sketch/interface defect and no
idea/theorem-contract defect.

## Review Rationale

ACCEPTED is the smallest sound status because global_proof.md is complete,
faithful to the accepted sketch-attempt-2 interfaces, and detailed enough to
consume only as diagnostic context. The covered-tape
E_cov -> entry -> basin -> E_stop chain has a legal same-target producer path.
The independent E_run_cap counter gives every other tape a finite,
observed-input cost without exporting convergence or a tensor target.
Restart amplification therefore uses completed independent tapes and keeps
its probability and runtime modes valid. The first-sweep and quotient
certificates expose concrete noncircular relations, generated-output flow,
entry traces, accumulation budgets, source conventions, and baseline
specialization. Every unresolved derivation is step-local under unchanged
claims, dependencies, assumptions, outputs, and theorem contract, so no
/global-proof, /proof-sketch, or idea retry is justified at this gate.
