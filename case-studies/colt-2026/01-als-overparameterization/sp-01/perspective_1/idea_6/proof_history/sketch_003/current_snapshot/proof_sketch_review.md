# Proof Sketch Review

## Reviewed Sketch Identity

- Branch: `perspective_1/idea_6`
- Sketch attempt: `3`
- Retry mode in the sketch: `revise_sketch`
- Frozen reviewed sketch SHA-256: `21150504d753137ae38a019ac14a8b17bd3238d9239cc1086eea0c737f033bde`
- Reviewed setting SHA-256: `a6991b0e308a8a33e782be75f9f081325404974335e5c00df85e6d8a80921cab`
- Reviewed idea: `perspective_1/idea_6/idea.md` (unchanged from the accepted attempt-2 review)
- Triggering review: `perspective_1/idea_6/proof_history/sketch_002/proof_steps/step_007b/attempt_002/review.md`
- Triggering review SHA-256: `9acdab69d7c88328d2596737974dd9cedc084455daf1cf2657111c115e9289d0`
- Triggering proof SHA-256: `9d7d3a7a35d518ce0b7efeb8e72493a83247785cdfe518c199d79c603241b960`
- Preserved attempt-2 sketch SHA-256: `c6126ab9b81d7a7cbcae3b9b857923f3fe8e275ed1ba4376fd830270c5ec95e1`
- Progress type: `conditional`
- Target rank: `U(r)=Theta(r^(5/3)(log r)^(5/2))`
- Reviewed scope: the attempt-3 `step_007c -> step_007b` repair and all inherited sketch interfaces; no proof-step artifact was treated as current evidence.

## Sketch Viability Score

6

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: The exact-goal conditional theorem is preserved. The sketch keeps the asymmetric realized CP tensor, original rank-`k` CP least-squares objective, `U(r)=Theta(r^(5/3)(log r)^(5/2))`, separate instance and conditional initialization probabilities, arbitrary relative Frobenius accuracy, polynomial runtime, and the exact orthogonal equal-weight baseline. The proof-only chronological `Refresh_s` construction and the first-sweep repair do not change the algorithm, rank, metric, or success criterion.
- Dependency audit: The intended graph is acyclic and the chronology is correctly ordered as `step_007 -> step_007c -> step_007b(U -> range -> V -> range -> W) -> step_008`. `E_perp_pair` and `F_M=N_M^T N_M` are legally produced before any range claim, and target-span membership is not used for the first `U` solve. The graph is not yet an executable interface at the V/W calls because the producer's advertised raw hypotheses remain static while its own carried calculation adds `E_N` and `h_M` terms.
- High-risk coverage: Shared-perpendicular leakage, the exact `tau_r^2` and `r tau_r^4` charges, all three inverse branches, exact unfolding range containment, zero padding, the common quotient register, chronological refreshes, target-span invariance, contraction, stopping, restart, and baseline traces are all localized. The remaining high-risk gap is the exported carried-input interface needed to instantiate the normalized-resolvent lemma after `U` and `V`.
- Explicit-rate coverage: The sketch exposes `q_*`, `tau_r`, `omega`, `beta_perp`, `Gamma`, `rho_ALS`, `ell_0`, `ell`, the `2977 eta < 3072 eta` arithmetic, `3072/3456/3888/4096` envelopes, `m_epsilon`, `m_cap`, confidence modes, and runtime variables. The V/W carried terms are numerically named but their admissibility inequalities and total raw Gram bounds are not exported in the producer contract, so the rate-bearing first-sweep interface is not yet closed.
- Assumption and citation plausibility: The eight stable `assump:*` identifiers remain the only primitive theorem inputs. Generated geometry, coverage, certification, clustering, entry, range, basin, stopping, and restart outputs are not promoted to assumptions. The Uschmajew citation is restricted to the quotient/block-Gauss--Seidel interpretation; current-notation scale equivariance and all numerical constants are direct obligations. No unresolved citation identity blocks the sketch.

## Early Obstruction Audit

- Limiting-case stress: The shared-perpendicular adversary is handled correctly at the actual entry: `t_M<=tau_r` gives the explicit `beta_perp`, including `r tau_r^4`, and exact unfolding removes each perpendicular field in U/V/W order. The exact orthogonal stationary state has actual `beta_act=0`, `A=0`, `nu=1`, and `s=0`; the positive `beta_perp` is retained only as a finite-transient bookkeeping upper bound. The remaining stress failure is interface-level: after the U output, a carried direction has `B_U=I+C_U+E_U` and is not covered by the literal `||B_U-I||<=48 omega` hypothesis stated for every call.
- Theorem-critical bridge support: `step_001` supplies the realized pair floor; accepted `step_007` supplies `bar P_M=G_MB_M+N_M`, orthogonality, `48 omega` coefficient control, and the `tau_r` column budget; `step_007c` supplies the exact `F_M`/`beta_perp` ledger and the centered normalization/resolvent mechanism; `step_007b` supplies exact range entry; and `step_008` supplies the generated invariant quotient domain. These bridges are otherwise target-preserving. The carried producer must expose its initial-plus-carried decomposition before it can be consumed at V/W.
- Exported-interface feasibility: `E_perp_pair` has a feasible raw-control path from the accepted projector decomposition. The static part of `E_carry_resolvent` also has a source-adequate path: both induced orientations, the Neumann resolvent, centered normalization cancellation, `S_0<=290 eta`, and `8(17/16)(290 eta)+4(128 eta)=2977 eta<3072 eta` are explicit. The exported conditional record is incomplete, however: lines 1212--1221 advertise only static `B_N`, `Delta_C`, and `Delta_J` bounds, while lines 1340--1350 use the additional `E_N` and `h_M` controls. A downstream consumer cannot infer the augmented interface without silently repairing the sketch.
- Theorem-critical mechanism witness gate: The witness is substantively present. Its source is the exact active normal equation and target pair floor; the positive/control terms are the centered off-diagonal mass and inverse margin; opposing defects are amplitude, carried direction, perpendicular, and quadratic terms; and the closure relations are the `2977/3072` static bound and `ell_0=17/512<1/8` carried gain. The witness is same-setting repairable, but its current raw hypothesis list is scope-incomplete for repeated U/V/W use, so this gate is `REVISE_SKETCH` rather than accepted.
- Entry-state trace stress: At U, the selected entry gives `H_M=emptyset`, `epsilon_U<=3072 eta`, and the exact range identity gives `N_U^+=0` before V. At V and W, the intended trace uses `h_V=epsilon_U` and `h_W=epsilon_U+epsilon_V`, followed by exact range removal. The sketch states this chronology, but it does not export the corresponding total `Delta_C`, `Delta_J`, scale-positivity, and `h_M<=2 rho_ALS/3` inequalities required to activate the same lemma at those two states. The exact target and singular-pair boundaries are otherwise traced.
- Obligation locality classification: `E_sm`, window/coverage, chart/certification, support and sign, coefficient closure, `E_perp_pair`, quotient assembly, exact range, stopping, restart, and baseline are `step-local`. The carried normalized-resolvent interface is a `sketch/interface defect` because the needed source is already available but its producer/consumer contract is not exported for carried states. No obligation requires an idea/theorem-contract change.
- Noncircular closure gate: The intended producer path is noncircular: `E_quotient -> E_perp_pair,E_carry_resolvent -> step_007b(U) -> exact range -> step_007b(V) -> exact range -> step_007b(W) -> E_parallel_entry -> step_008`. No target-span or basin fact is used to produce U. The unresolved issue is not circularity; it is the missing augmented raw interface at the two later calls.
- Mechanism-source and boundary stress: The legal `F_M` producer, actual-versus-bookkeeping `beta` distinction, induced row/column cancellation, rank-free diagonal similarity, and inverse floor are all source-adequate at static entry. The carried source needs an explicit `C_N/E_N` split and total cross/pair bounds; otherwise the `48 omega` and `96/192 omega` labels are only static bounds and cannot serve as V/W hypotheses. The route is a sketch repair.
- Generated-output flow: The flow table correctly names producers, consumers, final uses, and dependencies for `E_perp_pair`, `E_carry_resolvent`, `E_pair_entry`, and `E_parallel_entry`. The exact flow is therefore structurally present, but the `E_carry_resolvent` row must be amended to export the augmented carried-input record and its per-call discharge; the current `None` missing-flow field is too strong.
- Source-to-claim adequacy: Direct current-notation algebra, not the cited ALS paper, is the right source for the centered cancellation and scale transfer. The exact realized-factor convention, Euclidean projector, induced row/column norms, and target pair floor match the setting. Static source-to-claim adequacy passes; carried source-to-claim adequacy is conditional on explicitly stating `B_N=I+C_N+E_N` and the corresponding raw Gram bounds.
- Residual-to-target adequacy: All produced objects are the same realized factors and exact ALS outputs consumed by later steps; `N_M` is an actual Euclidean target-span residual, not a surrogate. The static residual scales and `4096(omega+beta_perp)<rho_ALS/3` are target-adequate. The V/W transfer remains deferred because the residual decomposition does not yet state which `h_M` terms dominate the consumed `31/32` floor and `ell_0` scale at each call.
- Baseline invariance audit: The inherited exact orthogonal equal-weight conclusion is preserved. The sketch explicitly distinguishes a finite `q_real=0` certificate transient from the deterministic stationary specialization, keeps actual `beta_act=0` separate from positive `beta_perp`, gives `Psi^q(0)=0`, and retains `s_U^+=s_V^+=s_W^+=0` and the zero-residual limit. The repair must not replace this with a positive bookkeeping bound.
- Scope-accumulation compatibility: The first sweep is finite-budgeted by the explicit `beta_perp` ledger, and later target-span sweeps have zero perpendicular forcing with geometric budget `1/(1-ell)<16/15`; historical product values are overwritten. The carried one-step relation is stated as `epsilon_M^+<=3072 eta+ell_0 h_M`, but the corresponding raw defect accumulation and admissibility budget are not exported in the hypotheses. This is a same-setting interface repair, not an accumulation obstruction in the idea.
- Scope and dependence consistency: Rank, probability, metric, `kappa_0`, epsilon, confidence, and runtime contracts are unchanged. `beta_perp` remains rank-aware through `tau_r`, while the normalized transfer is intended to be rank-free. No new hidden condition number or latent-factor dependence is introduced; the missing carried margins are a roadmap/interface omission.
- Generated-condition provenance: `E_sm`, `E_cov`, `E_cert`, `E_cert_tail`, `E_cluster`, `E_best`, `E_quotient`, `E_perp_pair`, `E_carry_resolvent`, `E_pair_entry`, `E_parallel_entry`, `E_basin_parallel`, `E_stop`, `E_run_cap`, and `E_restart` all have named producers. None is placed directly in the primitive theorem assumptions. Only the carried-input specialization of `E_carry_resolvent` lacks a complete producer-side export.
- Citation and tool applicability: The cited source has a stable DOI and named qualitative role, with current-object mapping and non-output boundaries. It is not being used to certify the disputed constants. No citation repair is needed.
- Same-setting repair plausibility: A local rewrite of the `step_007c` interface can preserve the current setting and goal: expose the accepted static component `C_N`, the carried component `E_N`, the perpendicular budget, the augmented cross/pair defects, and the per-call floor. No algorithm, primitive assumption, theorem mode, rank rate, metric, confidence protocol, or success criterion must change.
- Target-preserving bridge-repair gate: Pass for a sketch repair. The repair only makes explicit controls already named in lines 1340--1350 and retains the exact `2977<3072`, `ell_0<1/8`, range, quotient, rate, and baseline claims. It does not weaken or replace the baseline obligation. `IDEA_FAIL` is not warranted.
- High-risk obligation classes: Structural nondegeneracy, generated range/membership, carried cancellation, recursive chronology, explicit constants, quotient-state identity, scope accumulation, generated-output flow, arbitrary-accuracy rates, and exact/noiseless baseline were all scanned. Only the carried producer interface is unresolved.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_sm` | Static geometry, conditioning, and pair nondegeneracy | Gaussian concentration; normalization; Schur/Gershgorin | Primitive `assump:*` -> `step_001` | Exact realized norm, Gram, weight, and Khatri--Rao claims | Same normalized realized factors and Euclidean norms | Separate linear, quadratic, normalization margins; floor `1-q_*^2` | Base scales, smoothing margin, union factors, and confidence exposed | Produced realized factors are the consumed target | Finite static union; no accumulation | Primitive assumptions -> `step_001` | `step_001` -> all dynamic consumers | Orthogonal limit exact; singular floor boundary excluded | step-local | None |
| `E_win` | Per-slot rare activation | Independent Gaussian coordinates and pair-tail bound | `E_sm` -> `step_002` | Exact window and competitor-product event | Raw proposal coordinates match setting ratios | `p_win=Theta(r^(-5/3)(log r)^(-3/2))` | Target window, competitor tail, and finite-small-r constants exposed | Window is in the realized target chart | Finite event; no accumulation | `E_sm` -> `step_002` | `step_002` -> `step_003` | Zero denominator is rejected before export | step-local | None |
| `E_cov` | All-target coverage and one-run success source | Independent slots and coupon bound | `E_win` -> `step_003` | Exact all-target coverage probability | Proof labels are not algorithm inputs | `k p_win>=8 log r`, one-run probability `p_0` | `k=U(r)` and rank dependence explicit | Coverage feeds the same target entry path | Finite slot misses only | `E_sm -> E_win -> E_cov` | `step_003` -> `step_004`, `step_005`, `step_010` | All-miss event is an unsuccessful run | step-local | None |
| `E_RS` | Simultaneous ratio recurrence and denominator invariant | Old-state Jacobi expansion | `E_sm,E_win` -> `step_004` | Exact old-state map and displayed `R,S` recurrences | Same realized Gram and weight convention | Invariant interval with positive `1-Gamma q_real R_t` | `R_0<=19/20`, `Gamma<=1.01`, `q_real<=q_*` | Ratios are against realized target factors | Persistent `q_real` forcing is bounded, not erased | `E_sm,E_win -> step_004` | `step_004` -> chart/certification | First update and `q=0` squaring tested | step-local | None |
| `E_chart` | Named projective chart and fixed point | Banach self-map and direct derivative | `E_RS` -> `step_004` | Chart, norm conversion, and fixed point match target | Same projective signs and dual basis | `a_{t+1}` bridge, `L_chart<1/4`, `zeta_t` decay | `a_chart`, `chi_chart`, and `L_cert` explicit | Stored state is compared to its own same-state map | Finite burn/cert horizon; `q` forcing bounded | `E_RS -> E_chart` | `step_004` -> `E_cert`, `E_chart_l1` | Exact target stationary; zero contraction rejected | step-local | None |
| `E_chart_l1` | Covered-trajectory dual mass | Dual-basis l1 recurrence | `step_004` | Exact l1 quantity, not an ambient proxy | Same realized dual convention | `a_{t+1}<=2(Gamma S_t+q)/(1-Gamma qR_t)` | Covered-slot tightening is explicitly scoped | Only covered slots consume this tightening | Finite horizon; no global all-certified assumption | `E_RS -> E_chart_l1` | `step_004` -> covered consumers | Boundary `a=1/512` checked | step-local | None |
| `E_cert` | Stored certified state | Projective displacement and finite chart contraction | `E_chart` -> `step_004` | `zeta<=tau_r` at stored state | Same-state look-ahead convention | `3(1/4)^t<=tau_r` | `tau_r` and certification horizon explicit | No target-distance claim is smuggled in | One finite residual per mode | `E_chart -> E_cert` | `step_004` -> `step_004b`, `step_005` | Zero contraction makes run unsuccessful | step-local | None |
| `E_cert_eq` | Raw/barred same-state equation | Multilinearity and certificate definition | `E_cert` -> `step_004b` | Exact right-sided sign equation and per-column l2 residual | Raw setting objects stay distinct from proof-only bars | `||R_M(:,a)||_2<=d_M tau_r` | Positive contraction norm and sign placement explicit | Same stored state versus its own contraction | Finite sign-unknown residual | `E_cert -> E_cert_eq` | `step_004b` -> `step_005`, `step_006` | Transient residual retained; stationary residual zero | step-local | None |
| `E_cert_tail` | Global no-label support/tail ledger | Vector Gram expansion and scalar cancellation | `E_cert_eq,E_sm` -> `step_004b` | LOW/core/mixture/straddle branches match weighted target score | Same realized basis; no labels or chart premise | `eta_G`, `eta_N`, `eta_C`, profile `144 eta_0`, tail bounds | Thresholds, finite `sqrt(r)tau_r` conversion, and score margins explicit | No surrogate target; later dual bridge is named | Finite additive residual budget, sign-agnostic | `E_cert_eq -> E_cert_tail` | `step_004b` -> `step_005` | Multi-support, LOW, and straddle boundaries tested | step-local | None |
| `E_support_wt` | Weighted support and score classifier | Weighted profile and exact stationary support score | Ledger -> `step_005` | Correct unequal-weight score class | `diag(lambda)` retained; same direction metric | `s_mix`, `s_-`, `s_+`, `tail_score<=16 eta_0` | Weight ratio, thresholds, and gap inequalities exposed | Selected directions remain same realized target objects | Finite graph/filter; no iterative accumulation | Ledger -> classifier | `step_005` -> cluster/gauge/coefficient bridge | Equal-weight and unequal-weight mixture boundaries tested | step-local | None |
| `E_gauge` | Sign, permutation, and positive scalar interface | Theta lower bound and product-preserving signs | `E_support_wt` -> `step_005` | Barred equation preserves initialized tensor | Right-side signs and proof-only bars match setting | `abs(theta)>0.8 lambda_min`; `bar theta=abs(theta)` | Raw theta and barred theta are separated | Product-preserving orientation is exact | Discrete finite sign choice | `E_support_wt -> E_gauge` | `step_005` -> `step_006`, `step_007` | Negative and zero-score branches handled | step-local | None |
| `E_cluster` | Observable one-cluster-per-target output | Score and graph separation | `E_support_wt` -> `step_005` | Exact observable graph interface | Same modewise correlation metric | Within-target and cross-target inequalities precede ties | Graph threshold and score filter explicit | Representatives are actual stored tensor factors | Finite graph; no accumulation | Classifier -> cluster | `step_005` -> `step_006` | Duplicate, tie, and cross-target boundaries tested | step-local | None |
| `E_col` | Selected dual column small gain | Dual basis and quadratic root | `E_cluster,E_cert_eq` -> `step_006` | Exact target-dual coefficient output | Same realized dual and Gram convention | `chi<=32 omega` from explicit small root | Denominator and root branch exposed | Directly controls consumed target coefficients | Finite selected-set sums | `step_005 -> step_006` | `step_006` -> `step_007` | Exact q-zero state and transient both covered | step-local | None |
| `E_row_residual` | Per-entry and induced residual masses | Dual row norm and certificate l2 budget | `E_cert_eq,E_sm` -> `step_006` | Per-entry `2 tau_r` and row/column `2r tau_r` | Same target-dual projection | Sum per-entry once over the relevant index | No `r^2 tau_r` conversion; floor explicit | Residual is in the consumed target basis | Finite unknown-sign forcing | `step_005 -> step_006` | `step_006` -> row closure and best scalar | Stationary residual zero; transient charged | step-local | None |
| `E_row` | Selected dual row closure | Hadamard small gain and absorption | `E_col,E_row_residual` -> `step_006` | Exact row interface needed downstream | Same induced row/column norms | Self coefficient below `1/8`; `eta_row<=128 omega` | Static, coefficient, and certificate classes separated | Same target coefficients, no surrogate | Finite `r tau_r` budget and one-step absorption | Column root -> residual -> row | `step_006` -> `step_007` | q-zero transient and exact state tested | step-local | None |
| `E_best` | Balanced best-scalar target transfer | Tensor expansion and product-one gauge | `E_col,E_row,E_gauge` -> `step_007` | Same target and original Frobenius objective | Exact realized target projector and gauge | Four-class residual decomposition; `C_best=512` | Direction, amplitude, Gram, and perpendicular terms exposed | Residual dominated at `rho_ALS/4` | One finite expansion | `step_006 -> step_007` | `step_007` -> `step_007b` | Exact component gives zero defects | step-local | None |
| `E_quotient` | Quotient state and entry decomposition | Canonical balance and target projection | `E_best` -> `step_007` | Same represented rank-one products and target metric | Positive product-one action matches setting | `bar P_M=G_MB_M+N_M`, `48 omega`, `tau_r`, one `s` field | Raw direction, product, and perpendicular controls exported | Exact target projector; no transformed target | One finite entry state | `step_006 -> step_007` | `step_007` -> `step_007c`, `step_007b` | Orthogonal stationary state has `B=I,N=0` | step-local | None |
| `E_perp_pair` | Collective perpendicular pair-Gram ledger | Orthogonal projector identity and Hadamard expansion | `E_quotient` -> `step_007c` | Exact `F_M=N_M^T N_M`, mixed terms, and coherent term | Same realized held factors and Euclidean norm | `b_perp(t',t'')`; `beta_perp=b_perp(tau_r,tau_r)` | `t_N<=tau_r`, `48 omega`, and `r tau_r^4` explicit | Actual pair Gram is the consumed ALS Gram | Finite first-sweep defect; actual sign irrelevant | `E_quotient -> F_M,b_perp -> E_perp_pair` | `step_007c` -> `step_007b` | Shared-perpendicular adversary and exact baseline tested | step-local | None |
| `E_carry_resolvent` | Carried normalized-resolvent and common-product producer | Exact solve, centered normalization cancellation, Neumann transfer | `E_sm,E_quotient` plus conditional held record -> `step_007c` | Static claim matches; carried claim is not yet exported for V/W | Same realized target, Euclidean projector, and induced norms | Static `S_0<=290 eta`, `2977 eta<3072 eta`; carried `ell_0=17/512<1/8` | **Blocker:** current hypotheses require only `B_N-I<=48 omega`, `Delta_C<=96 omega`, `Delta_J<=192 omega+2 beta`; they omit `B_N=I+C_N+E_N` and the `h_M`-augmented total bounds | Static residual is target-adequate; V/W transfer is deferred | Intended finite carried gain, but raw accumulated terms are not in the declared interface | Intended `E_quotient -> E_carry_resolvent -> step_007b`; no circular range premise | `step_007c` -> `step_007b` U/V/W | U and baseline pass; V/W cannot be legally instantiated from the stated hypotheses | sketch/interface defect | /proof-sketch |
| `E_pair_entry` | First-sweep pair controls and inverse branches | `E_perp_pair` plus normalized cross/pair expansion | `step_007b` after `step_007c` | Exact held designs and ordinary inverse target | Same cyclic order and amplitude convention | `96/192` deviations, `31/32` floor, `beta_perp` | Depends on the missing augmented V/W carried record | Same exact ALS Gram and target | One finite sweep; defects charged then removed | `E_perp_pair,E_carry_resolvent -> step_007b` | `step_007b` -> subupdates and entry | U is active; V/W margins require repair | step-local | /proof-sketch |
| `E_parallel_entry` | Reachable exact target-span entry | Exact unfolding range and chronological solves | `E_pair_entry,E_carry_resolvent` -> `step_007b` | Exact outputs `G_M Z_M`, zero padding, and `N_U^+=N_V^+=N_W^+=0` | Same literal ALS orbit and quotient gauge | `3072/3456/3888/4096`; final `<rho_ALS/3` | V/W envelope depends on repaired carried-input export | Produced factors are exactly consumed by `step_008` | Perpendicular defects vanish mode by mode; no repeated `r tau_r^4` | U -> range -> V -> range -> W -> entry | `step_007b` -> `step_008`, `step_009` | Shared-perpendicular and exact stationary boundaries tested | step-local | /proof-sketch |
| `U_M^q` | Solved direction/product coordinate output | Target-span normal equations and scale equivariance | `E_parallel_entry,E_sm` -> `step_008` | One newly solved direction and product, not a full-state claim | Canonical held factors match literal ALS | Output discrepancy bounded by `ell` times held directions | Incoming common scale cancels; inverse floor explicit | Exact quotient orbit of the original ALS state | No raw amplitude forcing | Entry -> `U_M^q` | Internal to `step_008` | Exact target and singular floor tested | step-local | None |
| `Refresh_s^U,V,W` | Chronological quotient-state interface | Product-preserving canonical rebalance | `U_M^q` outputs -> `step_008` | Exact post-U/post-V/post-W representatives | One shared `s` field and product-one action | `e_U`, `e_V`, `e_W` overwrite historical products | No extra algorithmic operation or projection | Each state represents the literal intermediate tensor | Overwrite prevents historical accumulation | `e -> U -> Refresh_U -> e_U -> V -> Refresh_V -> e_V -> W -> Refresh_W -> e_W` | `step_008` -> `E_basin_parallel` | Exact target gives all `s=0`; entry is in tube | step-local | None |
| `E_basin_parallel,Psi^q` | Invariant target-span quotient tube and contraction | Target-span invariance, pair floor, Hadamard resolvent | `E_parallel_entry` -> `step_008` | Full `d_Q` and final `e_W` match target | Same realized factors and original loss | U/V `<=ell D`, W `<=ell^2D`, final `<=ell D<1/16D` | Entry `<rho_ALS/3`, zero perpendicular forcing, one common register | Balanced chart transfers to original Frobenius metric | Geometric budget `1/(1-ell)<16/15` | Entry -> refresh chronology -> `Psi^q` | `step_008` -> `step_009` | Target, entry, singular-pair, and rescaling boundaries tested | step-local | None |
| `E_stop` | Arbitrary-accuracy successful-trajectory stopping | `Psi^q` contraction and residual equivalence | `E_parallel_entry,E_basin_parallel` -> `step_009` | Original rank-k residual and horizon | Same tensor and loss | `m_epsilon=1+ceil(log(8 kappa_0^2/epsilon)/log 4)` | Entry offset and `C_res` explicit | No quotient surrogate is exported | Finite geometric horizon on `E_cov` only | `E_cov -> entry -> basin -> E_stop` | `step_009` -> `step_010`, theorem | Exact baseline stops at zero | step-local | None |
| `E_run_cap` | All-tape finite termination and dense cost | Approved observable cap and Moore--Penrose branch | Procedure clause -> `step_010` | Exact early-failure, hit, or timeout interface | Counter uses only observable setting quantities and current factors | `t -> t+1` until hit or `t=m_cap` | `m_cap` pre-registered; dense observed-tensor unit; no latent factors | Timeout is not transferred as a theorem output | Persistent unknown defects are stopped, not accumulated | Pre-register cap -> finite run -> `E_run_cap` | `step_010` -> restart/runtime | Noncovered singular/nonhitting tape and cap boundary tested | step-local | None |
| `E_restart` | Conditional confidence and total runtime | One-run inclusion, independent completed tapes, finite cost | `E_cov,E_stop,E_run_cap` plus random initialization -> `step_010` | Original probability and runtime modes | Same tensor, proposal law, and residual selector | `(1-p_0)^J<=delta_init`, `T_total<=J T_run` | `J`, all per-run costs, and confidence dependence explicit | Only residual-hitting output is selected | Costs add; failures multiply over finite runs | `E_cov -> E_stop`; `E_run_cap -> completed tapes -> restart` | `step_010` -> final theorem | `J=1`, all-fail, and baseline cases tested | step-local | None |
| Orthogonal equal-weight baseline | Exact/noiseless invariance and zero-limit | Direct simultaneous map and exact CP normal equations | Deterministic limiting clause -> `step_011` | Exact fixed point and arbitrary-accuracy zero residual | No positive-rho probability claim is made | `(A+,B+,C+)=(BC,AC,AB)`, `Psi^q(0)=0`, all refreshes `s=0` | Same target factors, gauge, and original metric | Produced and consumed tensors coincide | Stationary; no accumulated defect | Direct specialization -> baseline bridge | `step_011` -> final theorem | Finite transient is distinguished from stationary state | step-local | None |

## Blocking Issues

1. **The `step_007c` held-input contract is static where the V/W consumers are carried (proof sketch lines 1212--1221, 1340--1373; Gate Evidence row `E_carry_resolvent`).** The declared record lists only `||B_N-I||_(row/col,1)<=48 omega`, `||Delta_C||<=96 omega`, and `||Delta_J||<=192 omega+2 beta_perp` as the hypotheses for every invocation. The later calculation instead writes `B_N=I+C_N+E_N`, with `||C_N||<=48 omega`, `||E_N||<=epsilon_N`, and adds `(2q_*+8 rho_ALS)h_M` and `(4q_*+16 rho_ALS)h_M` to the cross/pair defects. After the U solve, the held U direction has `E_U` of size `epsilon_U`, so the literal static hypotheses do not describe the V input; after V they likewise do not describe the W input. The statement that `step_007b` proves the old inequalities is not a legal producer-consumer discharge. Downstream, the `E_carry_resolvent` output and the `3072/3456/3888/4096` first-sweep chain are therefore not yet source-adequately exported.

2. **Per-call admissibility and scale margins are not exported (lines 1282--1304, 1352--1363, 1463--1493).** The producer uses `||Delta_J||<=1/64`, `d_M<1/8`, `h_M<=2 rho_ALS/3`, positive diagonal input scales, and the ordinary inverse branch. The sketch gives the intended envelope values but does not state the V/W inequalities that discharge these conditions from `epsilon_U<=3072 eta` and `epsilon_U+epsilon_V<=6528 eta`, nor the augmented total `Delta_C/Delta_J` bounds that imply the `1/64` floor. This is part of the same interface gap, but must be made explicit so the chronological calls are independently legal.

Both issues are `sketch/interface defect` findings. They do not show the target is false and do not require changing the algorithm, primitive assumptions, theorem scope, metric, rank rate, probability mode, exposed dependence, or baseline conclusion.

## Required Repair Bundle

1. **Export the initial-plus-carried held-state record.** In `step_007c`, replace the static-only hypotheses by an explicit current-notation interface such as `B_N=I+C_N+E_N`, `||C_N||_(row/col,1)<=48 omega`, `||E_N||_(row/col,1)<=epsilon_N`, `max_j||N_N(:,j)||_2<=t_N`, and `h_M=sum_{N in H_M}epsilon_N`. State the corresponding initial (`h_M=0`) and carried (`h_M>0`) branches without using target-span membership as an input. Affected outputs: `E_carry_resolvent`; affected consumer: `step_007b`.
2. **Export and discharge total cross/pair defects at every call.** State the augmented bounds (or an algebraically equivalent form) `||Delta_C||<=96 omega+(2q_*+8 rho_ALS)h_M` and `||Delta_J||<=192 omega+2 beta_act+(4q_*+16 rho_ALS)h_M`, together with `||Delta_J||<=1/64`, positive input-scale bounds, and `d_M<1/8`. Instantiate them in the exact order U, range, V, range, W; explicitly show `h_U=0`, `h_V=epsilon_U`, `h_W=epsilon_U+epsilon_V`, `epsilon_U<=3072 eta`, `epsilon_V<=3456 eta`, `epsilon_W<=3888 eta<4096 eta`, and the required `h_M<=2 rho_ALS/3` margins. Do not reuse the initial `|alpha|<=128 omega` bound for a carried scale without a stated product-cancellation or positivity bridge. Affected steps: `step_007c`, `step_007b`; affected outputs: `E_pair_entry`, `E_parallel_entry`.
3. **Synchronize the evidence tables and chronology.** Update the `E_carry_resolvent` row in `## Exported Interface Feasibility`, the `E_carry_resolvent` and `E_pair_entry` rows in `## Generated Output Flow`, the corresponding Gate Evidence row, and `## Dependency Notes` to identify the augmented producer interface and its U/V/W discharge. Preserve the legal `F_M`/`beta_act`/`beta_perp` distinction, the centered cancellation, `2977 eta<3072 eta`, `ell_0=17/512<1/8`, exact range chronology, quotient `e=(C_U,C_V,C_W,s)`, rates, and baseline wording.

Target-preserving repair check: these changes only expose and route controls already present in the sketch's lines 1340--1350 and do not alter `setting.md`, the literal ALS procedure, rank, probability protocol, metric, runtime target, or exact orthogonal baseline. The smallest retry is `/proof-sketch`; no idea revision is required.

## Review Rationale

`REVISE_SKETCH` is the deepest required change. The attempt-3 repair successfully addresses the triggering normalization/resolvent arithmetic at the level of mechanism: the centered identity cancels the first-order diagonal term in both induced orientations, the rank-free resolvent factor is explicit, the arithmetic `2977 eta<3072 eta` is correct, the carried gain is `ell_0=17/512<1/8`, `F_M` and `beta_perp` have legal producers, the range trace is noncircular, and the single common quotient register and exact baseline are preserved. However, the producer's advertised hypotheses still describe only the initial static state, while the V/W consumers require the carried `C_N+E_N` decomposition and augmented cross/pair defects that the sketch derives later but does not export. A same-setting interface rewrite and explicit per-call margin discharge can close this gap without changing the idea or theorem contract, so `IDEA_FAIL` would be too deep and `ACCEPTED` would be premature.
