# Adversarial Proof Review

## Reviewer Verdict

PASS

## Blocking Issues

None

## Nonblocking Concerns

1. A compiler result remains unavailable. The fresh cached-only Tectonic invocation failed before TeX source processing because its macOS system-configuration networking thread panicked (`Attempted to create a NULL object`, exit code 101). Independent static checks pass, but a clean compilation should still be obtained when the environment permits.

## Suggested Repair Depth

None

## Smallest Candidate Repair Target

None

## Adversarial Audit

- Weakest theorem-level claim: the event-to-coverage-to-landing composition remains the most exposed chain. The attempt-5 repair restores the exact Step 002 target-window interface `(21)` and competitor/event interface `(22)` at section level before all consuming lemmas and propositions; the later one-slot identity `(32)` remains present. Replacing each subsequent use by these exact public statements now closes the chain through coverage, certification, frozen reserves, joint landing, cyclic contraction, stopping, and restart amplification.
- Standalone theorem attack: after reading only the setup and preliminaries, the main theorem's objects, primitive assumptions, event definitions, rank, probability modes, horizon, norm, stopping rule, and hidden-constant dependence are defined without appendix-local bookkeeping. In particular, $Z_{ij}^{(M)}$, $W_{ij}$, $C_{ij}$, and $E_{{\rm win},ij}$ are public before the theorem. Removing access to workflow artifacts leaves the appendix self-contained: the repaired `(21)`, `(22)`, and `(32)` references resolve within the Step 002 subsection.
- Weakest step-level claim: the synchronized Step 009--Step 011 bridge remains the highest-risk mechanism. Direct inspection confirms that the positive pair-Gram congruence and normalizer floor are produced before any solve; all three Step 010 outputs use the same frozen seed and are committed jointly; exact target-span entry precedes the Step 011 chronological U/V/W recurrence; and the latter has no additive forcing. No circular reserve, predecessor comparator, or unproved off-event contraction is used.
- Candidate counterexamples or stress cases: the audit tested $r=3$, the Step 002 finite-certificate/analytic boundary at $r=99/100$, arbitrary dependence among target events within a slot, maximal allowed signed Gram leakage, singular frozen or cyclic designs off the generated event, zero contractions and normalizers, confidence parameters near zero or one, $\epsilon\downarrow0$, and the exact orthogonal equal-weight seed. The finite certificate, slot-only independence, absolute-value ledgers, Moore--Penrose/failure-cap split, clamped restart count, no-floor contraction, and separate deterministic baseline neutralize these attacks.
- Explicit-rate stress cases: the two-sided window mass and exact $r^{5/3}(\log r)^{5/2}$ rank retain universal constants and the full $r\ge3$ range. The public Rate Specialization Bridge states the auxiliary choices, verifies the ceiling and primitive conditions, displays every domination in `(B.7)`--`(B.8)`, converts the nested probabilities without hiding confidence in $k$, and states the allowed hidden-constant dependence. No exposed structural, horizon, accuracy, smoothing-envelope, or confidence dependence is silently moved into an unspecified constant.
- Appendix self-contained attack surface: all thirteen step subsections, the 57 public theorem-style blocks, all thirteen target-step compositions, the Rate Specialization Bridge, and the final theorem proof are present. Static scans find balanced theorem/proof environments, eight stable assumption labels, no duplicate or unresolved label/reference, a matching BibTeX key, no bare `qquad` or malformed reference/citation command, no public claim/hypothesis/invariant environment, and no workflow path, audit scaffold, or placeholder prose. The former missing-backslash and proof-local-window blockers remain repaired.
- Scope or mode upgrade attacks: the theorem stays conditional on the declared primitive base regime. The once-drawn smoothing event at level $1-\delta_{\rm sm}$ remains outside the conditional proposal/restart probability at level $1-\delta_{\rm init}$; target dependence is never replaced by target independence; arbitrary tapes receive only finite observable cap behavior; and the $\rho_{\rm sm}=0$ orthogonal equal-weight result is a separately scoped deterministic exact-certified-seed corollary with zero residual.
- Remaining risk: obtain an actual TeX compilation once the local Tectonic system-configuration failure is resolved. This review is bound to assembly-report SHA-256 `6df30193da02586c00006fa93f24a0900e89d1642421f70c6d060327821070b1` and appendix SHA-256 `ba9d3c15312857519134fcdf9dde718a048500fae78cee8bb24a723d2dc10c7f`.
