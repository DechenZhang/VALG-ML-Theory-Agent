# Proof Sketch Review

## Reviewed Sketch Identity

- Branch: `perspective_2/idea_10`.
- Sketch attempt: 2.
- Reviewed sketch: `perspective_2/idea_10/proof_sketch.md`.
- Reviewed sketch SHA-256: `fe23f8cb324dbd68b3ddce771c6940382142b9eef02b32004e89619b86089c90`.
- Binding setting: `perspective_2/idea_10/setting.md`.
- Binding setting SHA-256: `c901dbc848dbbdbc09856210be4928ef068fa14541cb719eff34cdffc12e3d19`.
- Binding idea: `perspective_2/idea_10/idea.md`.
- Binding idea SHA-256: `298e6b77cf8edd14ab27e3672ab9cd0ba12a43e6f97037e183f480266af8b013`.
- Goal mode: `target-spec`.
- Progress type: `material_partial`.
- Stable controller graph: twelve rows; the table at `proof_sketch.md:963-982` is treated as the only controller-facing step list. The prose below it is supporting mechanism evidence, not additional checkpoints.
- Evidence boundary: the three hash-pinned binding artifacts, the live attempt-2 sketch, and the branch technical survey. The archived attempt-1 artifacts are used only to identify repeated blockers.

## Sketch Viability Score

5

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: The sketch preserves the exact asymmetric objective, balanced full-space unprojected transactional procedure, observable `Lev` screen, rank (k=\lceil C_{\rm rank}r^{3/2}\log^2(2r)\rceil), separate instance and tape probabilities, polynomial discovery work independent of (\epsilon), arbitrary relative Frobenius accuracy, same-target terminal metric, and the exact orthogonal/noiseless no-floor baseline. It remains a narrowed `material_partial` result rather than claiming arbitrary bases. The binding setting nevertheless still requires the unconditional second inequality in `(J-row)`, while the sketch changes it to a branch-qualified statement. That is a theorem-facing contract mismatch, not a harmless notation choice.
- Dependency audit: The explicit twelve-row graph is acyclic: `001 -> 002 -> 003 -> 004 -> 005 -> 006 -> 007 -> 008 -> 009`, with `001 -> 010 -> 011 -> 012` and the stated joins into `011` and `012`. The conditional-prefix interpretation can be a valid induction schema, but it is not yet represented as a named producer: steps `002-006` consume a legal prefix that step `007` claims to produce later. The sketch must make that temporal local hypothesis and its induction application explicit in the proof-step interface.
- High-risk coverage: Attempt 2 adds root-free `Theta_act`, `epsilon_act`, dual/tangent/Hessian objects, `q_nor`, `Lambda_nor`, a three-branch ledger, and a full `Cert+Lev` output row. These address the shape of the attempt-1 blockers. The raw complement branch, the literal `(J-row)` contract, and the target-blind certificate reachability mechanism remain unproved at the exported interface. The merged rows also combine several independent hard obligations that are not yet lemma-sized for local proof review.
- Explicit-rate coverage: Objectives R1-R4 expose structural, numerical, horizon, probability, metric, rank, smoothing, chart, and (\epsilon) dependence. However the exponent order does not state concrete inequalities that imply `(Act-margin)` or `(RAW-margin)`, and the latter is asserted for every passing state without a source lower-bounding its observable margin. The proposed `m_obs` mixes quantities with different units and is undefined when (\|R_I\|_F=0).
- Assumption and citation plausibility: The nine stable `assump:*` IDs are listed and generated matching, spans, histories, row bounds, reachability, and chart membership are not promoted to primitive assumptions. The parent papers and survey are correctly treated as proof-pattern provenance only. The source-normal recurrence is called inherited but its coefficient equation, index set, and residual-norm bound are not fixed in current notation; no cited theorem can silently supply that missing interface.

## Early Obstruction Audit

- Limiting-case stress: The empty prefix has (A=D=0), zero protected projectors, and exact rollback. The exact (H=e=0) specialization has the exact target, zero accepted errors, zero charges, and a valid no-floor terminal trace. The sketch also names screen equality, the last unresolved label, mixed support, dense/block frames, the isolated (h=e^2) audit, repeated failures, and the complement branch with (\nu=0,e>0). In that last allowed regime `(J-z)` still pads every off-diagonal row by (C_{\rm num}e>0), so the binding `C'_z\nu^2` upper is impossible for any fixed positive (C'_z). The `m_obs` definition has no (R_I=0) convention and no target-scale lower bound at small capture.
- Theorem-critical bridge support: The exact residual split and the two observable screen clauses are an appropriate source for a relative matched-error bound and unresolved-mass routing. The new dual identities and normalized tangent family are the right current-frame objects for a landscape calculation. The bridge is not yet complete: `(Act-margin)` and the singleton/negative-curvature conclusion are stated after an unproved raw margin, and the full certificate part of `Cert+Lev` is not connected to a target-blind activation source.
- Exported-interface feasibility: Static event, exact target, H/J/K/S, transfer gaps, `Theta_I`, the raw `Lev` split, exact J expansion, and the same-target PL objects have recognizable producers. The exported branch margin is not feasible as written: `Cert` and `Lev` supply thresholds but do not imply `m_obs` dominates (\nu^2+r\nu^3+\sqrt r e+r^2e+\Theta_{\rm act}), and the proposed minimum is not dimensionally normalized. The `q_nor`/`Lambda_nor` row also omits the explicit recurrence and the prior bound that turns the displayed (\Lambda_{\rm nor}(I)) into a uniform (\|D_I\|_F) output.
- Theorem-critical mechanism witness gate: The static small-gain path, protected-span first-order factorization, exact J charge, rootwise diagonal sum, rollback, stopped tape, and balanced-Jacobian chart each name an appropriate mechanism. The unresolved landscape witness has formulas but no coefficient-level support enumeration or explicit nonzero-selector convention. The reachability witness is still category-level language (``direct flow'', ``target-blind seed window'') without a seed set, slack radius, first-update relation, or Haar measure bound. These are same-setting sketch/interface defects.
- Entry-state trace stress: The empty state, exact baseline, zero-score rollback, guard failure, line-search exhaustion, and tape exhaustion are named. The first transaction from a full-space Haar seed is not traced to a `Cert+Lev` neighborhood with a quantitative activation window; step 004 asserts that output and step 008 consumes/produces it without exposing the first successful update. At (\nu=0,e>0), the numerical row obstruction is active on the first nonempty prefix, so it cannot be deferred to accumulation.
- Obligation locality classification: Static event and transfer, conditional `Lev` raw inequalities, exact J algebra, rollback, tape amplification, PL geometry, and baseline specialization are plausibly step-local after their interfaces are fixed. `Theta_act`/normal recurrence, the raw complement margin, the landscape support alternative, the complete `Cert+Lev` reachability output, and the temporal prefix induction are sketch/interface obligations. Rows 002, 004, 005, 007, and 012 each bundle several independent theorem-critical claims; without a local-unit map or a revised decomposition they are not small enough for one `/proof-step` producer and one local review.
- Noncircular closure gate: A finite empty-prefix induction with one-time nonnegative J charges and exact rollback is a plausible noncircular source. The current dependency text relies on a future step 007 to discharge the local prefix supplied to steps 002-006; the repair must state a transition lemma under a named local prefix hypothesis and then show exactly how step 007 applies it. The `RAW-margin` assertion is currently circular if its lower bound on `m_obs` is obtained from the same Hessian exclusion that consumes it.
- Mechanism-source and boundary stress: The source/target conventions for normalized realized factors, actual protected spans, exact residual, and balanced target slots agree. The dual tangent selectors can be zero or undefined in singleton/orthogonal boundary cases; the sketch must explicitly skip zero selectors and prove that a nonzero witness exists for every non-singleton support pattern. Sign ties, (g=0), (R_I=0), and the final empty unresolved set need explicit branches in the local producer.
- Generated-output flow: The compact flow table gives producers for static outputs, root-free activation objects, `Lev`, the combined `Cert+Lev` neighborhood, matching/J outputs, and the terminal tuple. It has no separate producer-consumer row for `(Act-margin)`/`(LB-margin)`/`(RAW-margin)`, and step 004's `Cert+Lev` output is asserted rather than supplied by an explicit activation producer. The future closure step cannot retroactively legalize a consumer that used an unproved output.
- Source-to-claim adequacy: Direct current-notation differentiation, Schur/Cauchy-Schwarz, finite Neumann inversion, exact multilinear expansion, Haar small-ball, and balanced-Jacobian/Taylor tools are appropriate in principle. For a theorem-critical direct derivation or standard tool, the sketch still needs the exact recurrence convention, instantiated selector objects, nonzero boundary handling, raw-control-to-margin inequalities, and the target-blind seed-to-neighborhood map. The cited papers do not provide those outputs and therefore cannot be treated as wrappers.
- Residual-to-target adequacy: `Lev-split` uses the exact realized tensor and the actual scratch contraction, and step 011 correctly uses per-slot `(R-euc)` rather than `(R-acc)` as factor distance. The unresolved Hessian bridge has no displayed residual decomposition that dominates all (B_I^{(\ge2)}), source-normal, history, and numerical terms at the required (s_I), (g^3), and (P^{-D_{\rm gap}}) scales. The proposed `m_obs` does not supply that comparison and is undefined at zero residual.
- Baseline invariance audit: The inherited exact balanced representation, joint-zero unused slots, exact rollback, stationary target, and arbitrary-relative-accuracy endpoint are preserved in the (H=e=0) trace. The repair must retain the conservative padded screen in that branch and may not replace the baseline by a finite-error or conditional surrogate. The literal unconditional `(J-row)` display, however, is not baseline-compatible with the allowed (\nu=0,e>0) boundary.
- Scope-accumulation compatibility: Neumann paths are geometrically summable, accepted J forcing is a nonnegative one-step charge summed at most (r) times, failed probes add zero, and terminal descent has a finite contractive horizon. This finite-budget story is valid only after the raw branch interface is repaired; it cannot absorb a persistent numerical term into (\nu^2) on (\mathcal E_{\rm lb}^c) or justify the false unconditional row.
- Scope and dependence consistency: The sketch exposes (n,r,k,\kappa,\rho,\Gamma_\star,P,\nu,\eta_0,\bar\eta,\epsilon), proof exponents, screen budgets, tape horizon, and separate probability modes. No extra lower-tail event is silently conditioned on. The missing exponent inequalities and branch qualification are the remaining dependence defects.
- Generated-condition provenance: The realized event, matching, protected spans, residual split, histories, J profiles, rollback, tape event, chart entry, and PL membership have intended producers. The root-free normal/Hessian objects have definitions but not a complete recurrence source; the raw branch margin and target-blind `Cert+Lev` event have no legally complete producer path.
- Citation and tool applicability: The two parent papers and the survey are explicitly proof-pattern sources, not theorem wrappers. Gaussian concentration, direct tensor calculus, Haar anti-concentration, and PL/Armijo are acceptable standard/direct tools only after the current convention, hypothesis discharge, and exported interface are written. No external citation resolves the branch contract or activation gap.
- Same-setting repair plausibility: The `Lev` screen, exact objective, rank, runtime, probabilities, metric, and baseline can remain unchanged if the sketch adds a target-scale margin producer, an explicit certificate/activation bridge, and a legal temporal induction interface. The unconditional `C'_z\nu^2` statement cannot be repaired by proof ordering or constants when the binding setting retains (\nu=0,e>0); it requires an approved setting-level contract change or a new primitive lower-bound condition. This is why the controller should treat the repeated row mismatch as an escalation point even though the public idea remains target-preserving.
- Target-preserving bridge-repair gate: For the two interface gaps, the smallest repair is target-preserving: carry raw (e)-terms on the complement branch, use a dimensionless observed margin with an independent lower-bound lemma, and expose the target-blind activation window. Do not add a source-label query, matched-mixture premise, projection, or terminal error floor. If the literal binding `(J-row)` is non-negotiable, no target-preserving sketch repair exists and the controller must route to idea revision.
- High-risk obligation classes: Structural support and negative curvature, generated source-normal transfer, protected-span routing, explicit numerical dependence, rootwise J accumulation, finite-tape reachability, same-target basin entry, first-exit convergence, generated-output flow, scope accumulation, and baseline invariance were all scanned. The three blockers below are the deepest unresolved interfaces.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_inst`, exact `T`, `Q*`, norm and Gram reserves | Static event, representation, baseline input | Gaussian concentration, normalization, exact CP algebra | Primitive `assump:*` IDs -> step 001 | Exact realized factors and target are the consumed objects | Normalized Gram and Frobenius conventions agree | Finite tail allocation and pair/triple reserves | Base norms, smoothing, rank, and `P` are exposed; static outputs are feasible | Equality to the exact target, no surrogate | Once-drawn; no trajectory accumulation | Primitive assumptions -> step 001 | Step 001 -> all later modules and terminal geometry | Empty, exact (H=0), and `T=0` early return are named | step-local | None |
| `H/J/K`, leakage, `S`, `q_res`, `q_row` | Structural source and Neumann small gain | Columnwise Cauchy-Schwarz, Schur products, direct transfer algebra | Step 001 direct derivation | Upper/source claim matches nonnegative transfer | No absolute-Gram operator norm is used | Displayed transfer bounds and (q\le1/4) | Array domains, zero diagonal, and eta padding are named | Same target-frame coordinates | Geometric finite Neumann accumulation | Step 001 -> step 002 | Step 002 -> `Theta`, J, and ledger consumers | `H=0`, threshold (q=1/4), and dense/block cases named | step-local | None |
| `Theta_I`, zero-diagonal `z`, and `Theta_act` | Generated history envelope and preselection landscape input | Nonnegative Neumann series and root-free maximum | Step 002 plus a local legal-prefix record | Shape is appropriate, but the conditional prefix source is not a prior accepted output | Current target-frame arrays match | (1/(1-q)) and explicit numerical padding | `S`, `A_I`, and `e` are visible; no root-specific row before selection | Analysis arrays are not substituted for the target | Finite conditional profile; prior forcing is input | Empty base -> transition lemma -> induction step 007 | Step 002 -> steps 003-007 | Empty and exact branches pass; zero-prefix source must be named | sketch/interface defect | /proof-sketch |
| `q_nor`, `Lambda_nor`, `Lev-normal`, `Lev-split` | Source-normal bridge and exact residual decomposition | Guarded triangular recurrence, pair Khatri-Rao gap, projection identity | Setting recurrence is only described as inherited; attempt 2 supplies a partial formula | Claim class is appropriate, but recurrence indices and a uniform residual bound are missing | Source and actual residual use the same Frobenius convention | Need explicit (q_{\rm nor}\le1/4), (\|R_I\|\) bound, and (\|D_I\|\) output before use | Current `Lambda_nor` includes `R_I` without a producer bound | `D_I=(I-\Pi_*)R_I` is exact once the normal output exists | Finite triangular forcing with raw (e) term | Local prefix -> normal producer -> Lev consumers | Step 002 -> step 003 and landscape/ledger | Empty, first commit, tiny (\nu), and exact limit need explicit recurrence traces | sketch/interface defect | /proof-sketch |
| `(Lev-raw)` and `(Lev-route)` | Residual-to-screen bridge and unresolved-mass support | Exact rank-one telescoping plus actual protected-span leverage | Steps 002-003 direct current algebra | First/higher-order defect classes match the screen | Actual QR/SVD spans and exact scratch tensor agree | `B1 Lambda <= gamma s` and `B2 <= s/64` imply a relative matched bound | Raw normal and numerical terms are retained, but the subsequent unresolved score scale is not independently lower-bounded | Same (T), (R_I), and contraction; no transformed object | One current-prefix inequality; failed screens add zero | Legal prefix -> Lev producer -> landscape | Step 003 -> steps 004-007 | Screen equality, (\nu=0,e>0), empty and last prefix named | step-local conditional | /proof-sketch |
| `(Act-margin)`, normalized Hessian, singleton alternative | Structural support/negative-curvature and label precursor | Dual selectors, current-frame stationarity, direct product-sphere Hessian | Step 002 definitions; no coefficient-level support enumeration | Appropriate source in principle, but the positive lower bound is only asserted | Product-sphere metric and normalized Hessian conventions agree | Need (12\epsilon_{act}+3B2\le\mu_{act}) and (g^3\mu_{act}\ge8NP^{-Dgap}) with explicit exponent checks | `Theta_act`, `epsilon_act`, `B2`, and `N/t` are listed; no independent branch margin source | Projected-to-actual residual transfer is not quantified at target scale | Defects must be dominated before Hessian exclusion; otherwise circular | Step 002 -> step 005, conditional on a legal prefix | Step 005 -> matching and J consumers | Zero selectors, singleton, mixed support, and (g\downarrow0) need explicit branches | sketch/interface defect | /proof-sketch |
| Full `Cert+Lev` neighborhood and activation window | Generated certificate, basin, and reachability output | Radial root, guarded product-sphere flow, continuity, Haar activation | Step 004 assertion plus step 008 dynamics | Output shape matches the algorithm, but the producer is not quantitative | Full-space spheres, exact loss, and actual projectors agree | Need explicit seed set, slack radius, first-update/guard relation, (H_{tx}), and small-ball mass | Cert clauses, Lev half-slack, and target-blind flow are named but not linked | Reached state is actual; no label may enter the algorithm | Stopped finite trajectory; failures rollback | Step 003 -> step 004 -> step 008 -> step 009 | Step 004/008 -> tape probability and commit output | Empty, last root, zero score, guard, and exact branch named but first successful trace absent | sketch/interface defect | /proof-sketch |
| Matching, signs, separation, `(R-euc)`, `(R-dir)`, injection | Generated target membership and calibrated slot | Singleton support plus dual coordinates and radial calibration | Step 005 conditional on repaired landscape | Correct target claim if singleton producer exists | Exact realized atom and product-one sign orbit match | Isolation gap and sign normalization must give (O(\eta_0)) direction/radius error | Root-specific `Theta` is consumed only after label selection | Actual slot is compared directly to exact target | One new label per successful commit; failures add none | Step 005 -> step 006 -> induction | Step 005 -> J, chart entry, and tape consumers | First/last label, sign ties, and separation boundary require local cases | step-local conditional | /proof-sketch |
| J transition, `(R-tensor)`, `(J-charge)`, `(J-update)`, diagonal audit | Exact one-step history charge | Multilinear expansion with `J(p,p)=1` and zero-diagonal `z` | Step 006 direct algebra | Exact update and honest (h^4+eh+e^2) audit match the binding setting | Same target-frame contractions and sign convention | One nonnegative charge bounds each accepted increment | Raw radial, directional, and numerical terms are displayed | Exact accepted error, not factor-distance surrogate | One finite charge per transition | Matching -> step 006 -> ledger | `ell=p`, `ell!=p`, cyclic modes, and (h=e^2) are named | step-local conditional | None |
| `(J-row)` and `(J-global)` branch ledger | Generated row bound, global accumulation, and angular-isolation input | Rootwise l2 factorization and one-time J charge | Binding setting plus steps 006-007 | Raw first inequality is plausible; unconditional (C'_z\nu^2) is false at allowed (\nu=0,e>0) | Same `J-z` and `J-diag` convention makes the contradiction direct | No fixed constant can turn positive (e) padding into (C'_z\nu^2=0) | The branch-qualified specialization in the sketch changes the binding interface; raw (r^2e) budget remains feasible | Undominated numerical residual is at the consumed row scale | Persistent positive numerical padding on the complement branch | No legal producer for the literal second inequality | Step 007 -> all-prefix, transaction, and terminal consumers, but edge is blocked | First nonempty prefix with (\nu=0,e>0) fails; exact (e=0) alone is insufficient | sketch/interface defect with possible idea-level escalation | /proof-sketch |
| Three-branch ledger margins `(LB-margin)` / `(RAW-margin)` | Quantitative specialization and closure margin | Exponent ordering, observable certificate scales, J budget | Step 007 assertion | LB branch is structurally plausible; raw branch has no lower-bound source | Same parameter conventions, but `m_obs` is not dimensionless | Need explicit inequalities for every raw term and a target-scale lower bound for `m_obs` | Cert/Lev give upper/threshold tests, not the displayed dominance | Residual terms are not shown dominated at (s,g^3,P^{-Dgap}) scales | Complement terms remain raw and cannot be absorbed into (\nu^2) | Current assertion is consumed by step 005/007 before it is proved | Step 007 -> landscape, ledger, and public rate | (R=0), tiny capture, and (\nu=0,e>0) expose missing cases | sketch/interface defect | /proof-sketch |
| Protected-prefix invariant and generated-output flow | Noncircular all-prefix closure | Empty base, transition lemma, exact rollback, finite one-time charge | Step 007 future closure over steps 002-006 | Induction is plausible only with a named conditional transition result | Actual state, injection, residual, and ledger conventions agree | Base plus one-step preservation must export every next-prefix record | Current compact table does not separately expose all margin/flow producers | Actual state remains target-compatible if transition output exists | At most (r) commits; failed probes add zero | Empty base -> conditional transition -> step 007 induction | Step 007 -> transaction/tape and all later rows | Empty, repeated failure, and last commit named; dependency timing needs repair | sketch/interface defect | /proof-sketch |
| Transaction, rollback, tape, and discovery work | Generated success event, probability, and runtime | Exact derivatives, Armijo/negative curvature, Haar small-ball, independent tape | Step 008/009 plus `assump:random-tape` | Protocol and probability mode match | Full-space Haar and actual QR/SVD conventions agree | Need target-blind activation output, (p_{tx}\ge(nr)^{-c}), stopped tail, and step floor | Work variables and rollback are exposed; upstream neighborhood producer is missing | Success states are actual; failure states equal pre-state | Failed probes have zero forcing; finite stopped horizon | Legal prefix -> transaction -> tape amplification | Step 009 -> all-commit tuple and step 011 | Zero score, guard, exhaustion, and rollback named | step-local conditional | /proof-sketch |
| Exact-target PL chart and entry | Same-target coercivity, basin membership, and discovery-to-terminal bridge | Balanced target Jacobian, pair/triple Gram reserve, per-slot `(R-euc)` | Steps 001, 005, 007, 009-011 | Correct exact target and full-state Euclidean metric | No whitening, projection, or surrogate target | Jacobian floor -> chart radius/PL; per-slot errors -> strict entry | (\sqrt r), weight ratio, and numerical terms are exposed | Direct actual tuple to exact target orbit; `(R-acc)` not used as factor distance | One-time entry then contractive terminal horizon | Step 001 -> step 010 -> step 011, conditional on all commits | Step 011 -> step 012 and final theorem | Exact target and (H=e=0) pass; upstream all-commit flow blocked | step-local conditional | /proof-sketch |
| Terminal descent, public rate, and baseline | Convergence, explicit runtime, probability separation, and no-floor recovery | PL/Armijo, first-exit, direct rank/smoothing substitutions | Step 012 and accepted prior outputs | Target and endpoint match the setting | Exact loss and Frobenius residual conventions agree | Geometric loss decrease and (\log(1/\epsilon)) horizon | Public substitutions are listed but branch contract and exponent checks remain open | Final residual is exact (T-\Psi(Q)) | Finite contractive horizon; no additive forcing in exact branch | Steps 007, 010, 011 -> step 012 | Step 012 -> final theorem | Exact stationary target and (\epsilon\downarrow0) pass conditionally | step-local conditional | /proof-sketch |

## Blocking Issues

1. **The binding `(J-row)` interface is still inconsistent on the raw complement branch.** `setting.md:559-573` requires
   \[
   \|z_p\|_{{\rm root},2}\le C_z(\nu^2+r\nu^3+\sqrt r e)\le C'_z\nu^2
   \]
   as a generated output, while `setting.md:520-524` and the sketch explicitly allow
   \(\mathcal E_{\rm lb}^c\), including \(\nu=0,e=P^{-D_{\rm stat}}>0\). For any captured root, `(J-z)` adds (C_{\rm num}e) to every off-diagonal entry, so the left side can be positive while (C'_z\nu^2=0). The sketch's lines 349-377 silently changes this to an `E_lb`-only specialization. This repeats the attempt-1 blocker and cannot be fixed by constants, ordering, or a later proof step. The smallest same-setting route is an explicitly approved setting-level replacement by the raw row plus a branch-qualified specialization; if the literal binding inequality is retained, this is an idea/theorem-contract escalation rather than a proof-sketch repair.
2. **The `E_lb^c` `RAW-margin` has no legal producer or residual-to-target comparison.** At `proof_sketch.md:353-377`, `m_obs` is defined as a minimum of (s_I), a normalized capture ratio, (g^3/N_I(g)), and (P^{-D_{\rm gap}}). These quantities have incompatible units as written, the ratio is undefined at (\|R_I\|_F=0), and neither `Cert` nor `Lev` supplies a lower bound showing that `m_obs` dominates (\nu^2+r\nu^3+\sqrt r e+r^2e+\Theta_{\rm act}) for every passing state. The same unproved inequality is consumed by `(Act-margin)`/step 005 and by the ledger closure, so the proposed route is circular. The smallest repair is to define a dimensionless margin with a zero-residual branch and add an independent certificate-to-score/capture lemma before Hessian exclusion, or to remove `RAW-margin` and carry all raw terms through a branch-specific ledger with explicit exponent inequalities.
3. **The full `Cert+Lev` activation producer and compact-step interfaces remain too shallow for step-level proof.** Step 004 (`proof_sketch.md:767-788`) names a `Cert+Lev` neighborhood and a target-blind seed window but does not state its seed set/radius, half-slack inequalities along the guarded trajectory, first-update/entry relation, (H_{\rm tx}) bound, or Haar small-ball mass. Step 008 both consumes that neighborhood and claims to flow into it, while steps 002-006 use a legal prefix that step 007 only produces later. In addition, rows 002, 004, 005, 007, and 012 each combine several independent theorem-critical outputs without stable local-unit boundaries. The smallest repair is to expose a named conditional transition hypothesis, a lemma-sized certificate/activation producer with its target-blind measure and rollback trace, and local-unit maps (or split only these hard rows) while retaining the twelve-row controller graph if desired.

## Required Repair Bundle

1. **Resolve the binding row contract before any proof-step dispatch.** Affected interfaces: `setting.md:(J-row)`, `setting.md:(J-z)`, `proof_sketch.md:159-186,349-377`, and steps 006-007/012. Either obtain an approved semantic delta that changes the theorem-facing row to
   \(\|z_p\|\le C_z(\nu^2+r\nu^3+\sqrt r e)\) with `C'_z nu^2` only under an explicit `E_lb` inequality, or add a primitive condition that rules out (\nu=0,e>0). Do not hide the change in a later local proof. The public objective, procedure, rank, probabilities, runtime, target metric, and exact baseline can remain unchanged under the first option; if the binding setting cannot be changed, route this repeated contract failure to `/subagent-idea-generator`.
2. **Repair the complement-branch margin and rate ledger.** Affected interfaces: `(Act-margin)`, `(LB-margin)`, `(RAW-margin)`, `Theta_act-bound`, steps 002-007, and the R2/R4 rate objectives. Define a dimensionless observed margin with explicit (R_I=0) handling; identify its producer before landscape exclusion; list all exposed exponent inequalities (including (D_{\rm cap},D_{\rm gap},D_{\rm lb},D_{\rm nor},D_{\rm stat},D_{\rm pass})); and prove each raw numerical, source-normal, history, and structural term is dominated at the consumed scale. If no such lower-bound bridge follows from the unchanged `Cert+Lev` tests, remove the assertion and retain a raw branch throughout rather than adding an unannounced lower-tail assumption.
3. **Make reachability and compact proof-step locality explicit.** Affected interfaces: steps 002-005, 007-009, and the generated-output flow table. State the legal-prefix transition hypothesis and the exact induction application in step 007. Add a certificate producer that gives the actual radial/angular/Hessian/capture/separation and both screen margins, a target-blind seed window, first-update/sign/guard behavior, finite (H_{\rm tx}) work, and inverse-polynomial Haar measure before step 009. For the twelve-row compact graph, attach stable local-unit maps for the independent claims in rows 002, 004, 005, 007, and 012; otherwise split those rows into auditable substeps. Preserve the no-target-query, full-space unprojected procedure and exact rollback.

Target-preserving repair check: repairs 2 and 3 can preserve the current public `material_partial` objective, procedure, rank, probabilities, runtime, target metric, and no-floor baseline. Repair 1 can preserve them only if the binding auxiliary `(J-row)` contract is formally corrected; a literal unconditional row is impossible on the allowed complement branch. No matched-mixture exchange premise, source-label query, projection, or additive terminal floor should be reintroduced.

## Review Rationale

`REVISE_SKETCH` with score 5 is the deepest status supported by the current public idea: the observable screen and the exact J-aware/rootwise mechanism remain target-aligned, and two of the prior interface blockers have been materially addressed. The live artifact is not ready for global proof or step-level workers because the binding row contract still contradicts its own allowed small-ν regime, the complement-branch raw margin is asserted without a noncircular target-scale producer, and the certificate/reachability flow is not sufficiently explicit for the compact rows. These are repairable at the sketch/interface level only if the controller accepts the branch-qualified auxiliary contract; otherwise the repeated `(J-row)` contradiction requires `IDEA_FAIL`/`/subagent-idea-generator` under the status rules.
