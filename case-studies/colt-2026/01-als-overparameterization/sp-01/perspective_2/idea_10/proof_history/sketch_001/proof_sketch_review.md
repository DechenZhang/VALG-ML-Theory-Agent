# Proof Sketch Review

## Reviewed Sketch Identity

- Branch: perspective_2/idea_10.
- Sketch attempt: 1.
- Reviewed sketch: perspective_2/idea_10/proof_sketch.md.
- Reviewed sketch SHA-256: 42d08b90b4df4c701dcae5331542b2f31cbbc928be396273256f8ff77f92156.
- Binding setting: perspective_2/idea_10/setting.md.
- Binding setting SHA-256: c901dbc848dbbdbc09856210be4928ef068fa14541cb719eff34cdffc12e3d19.
- Binding idea: perspective_2/idea_10/idea.md.
- Binding idea SHA-256: 298e6b77cf8edd14ab27e3672ab9cd0ba12a43e6f97037e183f480266af8b013.
- Goal mode: target-spec.
- Progress type: material_partial.
- Evidence boundary: the three hash-pinned binding artifacts, the branch-local technical survey and tracker, and the live sketch only. No archived proof-history artifact is used as current proof evidence.

## Sketch Viability Score

6

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: Pass on the public target. The roadmap preserves the exact asymmetric objective, balanced full-space transactional procedure, rank k=ceil(C_rank r^(3/2)(log(2r))^2), separate instance and tape probabilities, polynomial discovery work independent of epsilon, arbitrary relative Frobenius accuracy, and the exact orthogonal/noiseless no-floor baseline. It explicitly keeps the raw E_lb^c terms rather than adding a lower-tail assumption. The setting's displayed C'_z nu^2 and static-nu^2 comparison are not yet reconciled with that raw branch.
- Dependency audit: The 20 table rows are ordered and every explicit table dependency points to an earlier row. The apparent reuse of steps 004--012 is a legitimate intended temporal induction only if prior-prefix records are treated as local conditional hypotheses and step 013 supplies the induction hypothesis before the next invocation. The current flow text explains this, but the missing landscape aggregates below prevent the full producer path from being legal.
- High-risk coverage: The sketch has dedicated witnesses for the realized event, Lev raw split and route, unresolved-neighborhood screen passage, unresolved-only landscape, J transition, rootwise diagonal charge, global ledger, rollback, tape amplification, PL entry, and baseline. The landscape witness is not interface-complete because it consumes undefined root-free and derivative controls. The screen removes the former protected matched-mixture exchange obligation.
- Explicit-rate coverage: R1--R4 expose dimensions, rank, smoothing, structural and numerical scales, horizons, confidence modes, norms, chart constants, and the public specialization step. The raw r^2 P^(-D_stat) branch is displayed, but the required branch-specific comparison to the landscape/ledger margin is not displayed on E_lb^c.
- Assumption and citation plausibility: The nine primitive assump:* IDs are identified at the sketch front and used explicitly in the static, terminal, and tape rows. Generated matching, spans, screen passage, histories, row bounds, and PL membership are not promoted to public assumptions. The two parent papers are correctly limited to proof-pattern provenance; no theorem-critical cited wrapper is used. Rows 013 and 020 use the shorthand "all nine primitive assumptions" and should enumerate stable IDs in the repair.

## Early Obstruction Audit

- Limiting-case stress: The empty prefix has A=D=0, zero protected projectors, B1=B2=0, and exact rollback. The exact H=e=0 branch has zero actual matching errors, zero z, zero J charges, exact target entry, and no terminal floor; conservative bar-eta padding is explicitly retained in the screen check. Equality in either Lev clause, the last unresolved label, dense/block frames, the isolated h=e^2 J audit, repeated failures, and E_lb^c with nu=0,e>0 are named. The unresolved-only landscape source is active at empty and last prefixes, but its quantitative root-free interface is not exported.
- Theorem-critical bridge support: Exact Lev-split plus the two observable screen clauses gives |s_M| <= (gamma_lev+1/64)s_I and positive unresolved absolute mass without any matched-mixture exchange. This is a valid source for an unresolved-only landscape in principle. The downstream Hessian bridge still needs the explicit current/dual equations, tangent family, and raw defect envelope that make the source nonvacuous.
- Exported-interface feasibility: Static H/J/K/S and transfer gaps, the exact residual split, both Lev raw inequalities, screen-passing neighborhoods, J charges, rootwise sums, rollback, tape events, and the exact-target chart have raw controls and same-target interfaces. Step 008 currently names Theta_act(I), epsilon_act, and a Hessian quadratic form without defining or producing them. Step 012 also lacks a branch-specific raw control relation: a numerical r^2 e term cannot be compared to nu^2 when nu is arbitrarily small on E_lb^c.
- Theorem-critical mechanism witness gate: Static small gain, Lev routing, screen passage, J diagonal/global charging, rollback, tape stopping, PL coercivity, and baseline witnesses are obstruction-level and source-appropriate. The step-008 witness is a sketch/interface defect: "root-free Theta_act" and "already named epsilon_act" are labels rather than a current-notation source, and no explicit Q or normalized tangent family is exported. This is repairable in the same setting; no protected matched-mixture witness is required.
- Entry-state trace stress: At I=empty, exact atoms have zero leverage and pass the padded screen; the first failed transaction returns exactly to that state. At E_lb^c, the score floor is the intended active source for raw B2 terms, but the first-update route from a Haar seed to a full Cert+Lev neighborhood is only asserted: step 007 proves Lev slack, while step 015 consumes a certificate neighborhood without a named producer for the certificate part. Add a local Cert-neighborhood/flow bridge or make step 015's deterministic basin output explicit. The exact target terminal entry is stationary and correctly needs no update.
- Obligation locality classification: Static event, transfer, Lev split/route, screen passage, J charge, rootwise/global ledger, rollback, tape amplification, PL, chart entry, and baseline obligations are step-local under the declared raw interfaces. The missing Theta_act/epsilon_act/Q producer, the missing raw-branch margin, and the screen-to-Cert reachability interface are sketch/interface defect obligations. None requires a changed primitive assumption, algorithm, metric, probability mode, or success criterion, so no idea/theorem-contract defect is identified.
- Noncircular closure gate: The finite prefix induction has a noncircular base I=empty,A=D=0, exact rollback, at most r successful extensions, and one-step J charges. Prior-prefix records are used only as local hypotheses and are produced by the preceding temporal induction instance. The missing root-free aggregate must be produced from the complete current Theta_I before support selection; it cannot be supplied by the selected root or by step 009 after the fact.
- Mechanism-source and boundary stress: The source conventions for normalized realized factors, actual protected projectors, exact residual, target-frame contractions, and balanced target slots agree. The unresolved weighted threshold and dual selectors are the right claim class for an unresolved support/curvature conclusion, but their source-to-claim path is incomplete at the named controls above. No stale idea-9 (Mat-rel), (Mat-hi), (Mat-margin-10), B_mat, or protected matched-mixture exchange output occurs in the live sketch; the only "structural exchange" language is the unresolved-mass tangent alternative.
- Generated-output flow: The flow table is legal for E_inst, H/J/K/S, Theta_I, the Lev interfaces, J budgets, rollback/tape outputs, and terminal chart outputs, modulo the temporal induction interpretation. It has no row for Theta_act, epsilon_act, the current Hessian quadratic form/tangent family, or the branch-specific raw margin. The rows currently say None for missing blockers, so they must be corrected rather than leaving a false all-clear flow.
- Source-to-claim adequacy: Gaussian concentration, Schur/Cauchy--Schwarz, direct multilinear expansion, finite Neumann inversion, exact rollback, Haar small-ball, and balanced-Jacobian/Taylor calculations are appropriate current-notation tools. The unresolved support claim needs an explicit direct dual-coordinate derivation and normalized tangent selectors; a future "landscape calculation" label alone is not a source. The J charge remains a direct exact expansion with J(p,p)=1, zero-diagonal z, off-diagonal J=H, and no external theorem wrapper.
- Residual-to-target adequacy: Lev uses the exact R_I, actual protected spans, and the same scratch tensor in the Frobenius contraction. J arrays contract the exact accepted error against the exact realized target frame, and R-acc is not used as factor distance. The unresolved Hessian bridge must state projected/current residual transfer and show every B2, source-normal, history, and numerical term at the declared s, g^3, and P^(-D_gap) scales. The PL entry bridge is otherwise target-adequate because it uses per-slot R-euc and the exact target orbit.
- Baseline invariance audit: Pass. The inherited conclusion is exact balanced representation and stationary exact target in the H=e=0 specialization, exact rollback and joint-zero unused slots, and arbitrary relative refinement with no additive floor. The first empty-prefix transaction and terminal stationary traces are present. The repair must preserve this conclusion and must not replace it by a finite-error or conditional surrogate.
- Scope-accumulation compatibility: Pass for the declared finite scopes once interfaces are repaired. Neumann transfers have geometric 1/(1-q) control; accepted J forcing is a nonnegative one-step charge summed once over at most r commits; failed probes add zero; the tape is stopped; terminal descent is contractive over a finite logarithmic horizon. The raw E_lb^c ledger needs a separate score/capture/Hessian margin relation rather than an unsupported nu^2 absorption.
- Scope and dependence consistency: Rank, smoothing, kappa, P, e=P^(-D_stat), h, screen budgets, p_tx, H_tx, N_tx, epsilon, chart radius, and separate probability statements remain exposed. No extra lower-tail event is silently conditioned on. The only inconsistency is the unqualified static-nu^2 final comparison alongside the stated raw small-nu branch.
- Generated-condition provenance: The realized event, matching, protected spans, Lev split, screen passage, J row/global budgets, injection, rollback, tape event, chart entry, and PL membership each have intended producers. The root-free landscape aggregate and branch-specific margin are not yet generated outputs, and step 015's full Cert+Lev neighborhood is not explicitly produced.
- Citation and tool applicability: Pass at the cited-source boundary. The survey papers are explicitly proof-pattern provenance and cannot discharge the new screen or landscape bridge. Standard Haar small-ball and direct current-notation calculus are acceptable only after the missing raw controls and output shapes are written into the sketch.
- Same-setting repair plausibility: The blockers can be repaired by adding a root-free Theta_I aggregate/defect envelope, defining normalized dual tangent/Hessian interface, adding the missing normal guard scale, and splitting the ledger comparison by E_lb, E_lb^c, and the exact limit. The observable screen, exact objective, rank, runtime, probabilities, metric, and baseline can remain unchanged.
- Target-preserving bridge-repair gate: Pass conditionally on those additions. The repair must export unresolved-mass support only from the two Lev clauses, retain all raw numerical terms on E_lb^c, and keep the exact target/no-floor endpoint. It must not reintroduce a matched-mixture premise or any source-label query.
- High-risk obligation classes: Structural support/negative curvature, generated source-normal transfer, protected-span screen routing, rootwise J accumulation, explicit numerical dependence, finite-tape reachability, same-target basin entry, first-exit convergence, generated-output flow, and baseline invariance were all scanned. Only the three interface issues above remain blocking.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E_inst, T, Qstar, norms and Gram reserves | static event, representation, baseline input | Gaussian concentration, normalization, exact CP algebra | primitive static IDs -> step 001 | exact realized factors and target are setting objects | normalized-Gram and Frobenius conventions are identical | finite tail allocation and pair/triple eigenvalue reserves | norm, weight, P, nu_star, and radius bounds are explicit | produced T,Qstar are consumed target objects | once-drawn, no trajectory accumulation | primitive assumptions -> step 001 | step 001 -> structural and terminal consumers | nonzero columns, T nonzero, exact limit pass | step-local | None |
| H/J/K, leakage, S, q_res, q_row | positive small gain and transfer source | columnwise Cauchy--Schwarz, Schur products, direct block norms | step 001 -> step 002 | absolute-entry controls match nonnegative transfer claim | no operator norm for abs(G-I) is used | displayed transfer bounds and q <= 1/4 | raw columns, weights, eta padding, and array domains are named | arrays remain analysis coordinates of exact target | geometric finite Neumann accumulation | step 001 -> step 002 -> step 003 | step 002 -> step 003 and ledger consumers | H=0, q=0, threshold q=1/4 | step-local | None |
| Theta_I and zero-diagonal z | generated history profile and J input | nonnegative Neumann series | step 002 plus local prior-prefix A_I | exact root/mode/label array and zero diagonal match J formulas | same target-frame contractions and rootwise norm | inverse 1/(1-q), numerical off-diagonal padding | S,A_I,e*1 are explicit; no row simplification is assumed | no transformed tensor is consumed | finite conditional profile; prior forcing is input | empty base -> conditional step 003 | step 003 -> steps 004,008--013 | empty prefix and exact H=e=0 pass | step-local | None |
| Root-free Theta_act, epsilon_act, Q, tangent family, Lambda_nor | support/curvature producer before label selection | direct dual/current-frame stationarity and normalized product-sphere Hessian | intended from steps 003--008, but definitions/producer absent | claim class is appropriate, exported controls are only names | target convention plausibly compatible, pending formulas | define max row, all defects, selectors, normalized Rayleigh form, and normal guard | no explicit raw bound or flow row currently exists | projected and actual residual transfer is asserted, not quantified at needed scale | one-prefix sign-unknown defects must be dominated by mu_act and P^(-D_gap) | step 003 must produce before step 008; current path incomplete | missing producer for step 008 and step 009 | empty, mixed support, straddle, one unresolved, exact limit named but not quantified | sketch/interface defect | /proof-sketch |
| Residual reserve, Lev-normal, Lev-split | generated source-normal bridge | post-fit stationarity, pair-Khatri--Rao gap, triangular inversion | prior R-euc records plus steps 001--003 | exact D_I split matches setting | source and actual residual share target and Frobenius norm | q_nor <= 1/4, finite forcing, reverse triangle | q_nor and Lambda_nor need explicit definitions/guard; other controls named | exact equality, not surrogate residual | finite triangular recurrence; rollback zero forcing | prior temporal records -> step 004 -> steps 005--008 | step 004 -> Lev consumers | empty/first/last prefix and nu=0,e>0 raw branch named | step-local after guard definition | /proof-sketch |
| First/higher-order Lev-raw and Lev-route | generated unresolved-mass support | exact rank-one telescoping, protected-span proximity, observable clauses | steps 004--005 plus current screen | first-order pair leverage and raw higher-order classes match | actual QR/SVD projectors are protocol projectors; no source query | B1 Lambda <= gamma s, B2 <= s/64 imply route | bar-eta,B1,B2,s,Lambda and both raw branches explicit | exact current residual and unresolved target share T | one current-prefix inequality; failed screens rollback | steps 004--006 -> steps 008--009,013,015 | step 006 -> landscape and reachability | screen equality, nu=0,e>0, empty, last label pass | step-local | None |
| Unresolved-neighborhood Lev passage | screen reachability interface | Gram row bound, projector formula, continuity | steps 001,004--006 and prior prefix records -> step 007 | exact unresolved atom and actual screen objects used | no projected/whitened neighborhood | explicit B2 and B1 Lambda slack plus Lipschitz reserve | raw budgets, lambda, Lambda, D_pass named | same factor/product-sphere metric | uniform finite prefixes and unresolved labels | step 007 -> step 015 | empty, dense/block, exact, tiny-nu pass at screen level | step-local | None |
| Full Cert+Lev neighborhood and one-seed reachability | generated certificate event and probability source | exact radial/angular flow, curvature escape, compact guard, Haar activation | steps 007,014,015 plus random-tape | protocol quantities match, but step 007 exports only Lev slack | full-space spheres and exact loss match algorithm | need Cert persistence/attraction and seed-to-neighborhood path within H_tx | p_tx,H_tx,small-ball,step floor stated; Cert producer deferred | successful state actual; rollback exact | stopped finite tape, failures zero forcing | step 014 flow + step 007 screen -> step 015 -> step 016; Cert producer missing | zero score, guard, curvature, exhaustion handled; first successful Cert trace incomplete | sketch/interface defect | /proof-sketch |
| Unresolved singleton, signs, separation, R-euc, R-dir, injection | generated matching and prefix transition | weighted unresolved threshold, dual selectors, radial root, Gram inversion | intended step 008 -> step 009 | appropriate if unresolved landscape source completed | actual factors compared directly with exact atom | singleton margin and C_led eta0 < 1/8 yield uniqueness | selected row, radial scale, numerical terms named | actual slot target-compatible | one new label per success, at most r | step 008 -> step 009 -> next prefix/J consumers | first/last labels, sign ties, separation pass conditionally | step-local conditional on step 008 | /proof-sketch |
| R-tensor, J charge/update, diagonal, O.1 audit | one-step signed history charge | exact accepted-error expansion, J(p,p)=1, zero-diagonal z | steps 003,009 -> step 010 | exact target-frame contraction matches A_I and Xi | J=I+H, off-diagonal J=H, product-one signs match | one charge bounds each update; diagonal retains eta h^2+hz+z^2 | radial/directional, Hz, z^2, eh,e^2 retained | accepted error and baseline same target | one charge per transition; failed probes zero | step 009 -> step 010 -> steps 011--013 | captured diagonal, cyclic modes, embedded block, h=e^2, exact limit pass | step-local | None |
| Raw rootwise row and Lev-Jdiag | generated per-root accumulation control | rootwise Neumann contraction, KJ source, Cauchy--Schwarz | steps 002--003,010 and prior budget -> step 011 | rootwise norm and diagonal sum match setting | same captured root and zero diagonal consumed | raw C(nu^2+r nu^3+sqrt(r)e), diagonal C(nu^3+r e) | raw branch explicit; C'_z nu^2 only branch-qualified in sketch and needs setting reconciliation | exact target-frame root contractions | finite rootwise sum, no tested-label multiplier | step 011 -> step 012 -> step 013 | nu=0,e>0, one root, dense/block, exact limit pass | step-local conditional on branch clarification | /proof-sketch |
| J-global, history/resolvent budgets, R-acc | finite recurrence and accepted-error accumulation | one-step charge, rootwise diagonal, off-diagonal KJ/Neumann | steps 010--011 -> step 012 | charge and budgets declared index set | same target-frame arrays and exact baseline cancellation | O(r nu^3+r^2e) displayed; raw-branch margin absent | structural/numerical classes separated, no E_lb^c target-scale relation | exact accepted errors, not factor-distance surrogate | finite at most r, nonnegative charges, failures zero | step 010 -> step 011 -> step 012 -> step 013 and next prefix | isolated O.1, dense/block, arbitrary order, tiny-nu named; static-nu^2 comparison unsupported | sketch/interface defect | /proof-sketch |
| Protected-prefix invariant and rollback | all-prefix generated condition | finite induction, monotone injection, exact rollback | empty base plus steps 003--012 | local conditional outputs listed in temporal order | actual protected state/residual tracked | at most r extensions and one-time budget; failures preserve equality | all interfaces named subject to step-008/margin repairs | same actual R_I after each transaction | finite scope, zero failure forcing | empty base -> conditional transition -> step 013 | step 013 -> transaction/tape and next prefix | empty, first/last, repeated failure, screen equality, exact limit pass | step-local conditional on repaired rows | /proof-sketch |
| Transaction map, p_tx, tape amplification, runtime | generated success event and finite work | exact derivatives, Armijo/eigenvector, Haar small-ball, independent tape | step 013/014 plus random-tape -> steps 015--016 | protocol/probability modes match setting | full-space Haar, exact loss, QR/SVD, rollback agree | step floor, p_tx >= (nr)^(-c_tx), stopped tail, N_tx | polynomial screen/QR/SVD cost and epsilon-independent discovery explicit; Cert path needs repair | successful/failed states actual pre/post states | stopped tape, failures zero forcing | step 013 -> step 014 -> step 015 -> step 016 | zero score, guard, exhaustion, rollback pass; reachability conditional | step-local conditional on reachability | /proof-sketch |
| Exact-target PL chart, entry, first-exit, public rate | coercivity, basin, convergence, specialization | balanced Jacobian/Taylor, per-slot R-euc, Armijo | steps 001,009,013,016--019 | exact target orbit, factor metric, Frobenius residual match | no transformed target | radius c g_min/r, mu c g_min^4, strict entry, geometric decay | sqrt(r) eta0, r^(3/2)e, rank/runtime and separate probabilities explicit | direct tuple-to-Qstar bridge; R-acc not factor distance | one-time entry then finite contractive horizon | step 017 -> step 018 -> step 019 -> step 020 | exact target, chart boundary, epsilon->0, no-floor baseline pass | step-local conditional on discovery | None |
| Exact orthogonal/noiseless baseline | recovery, zero-limit, stationary endpoint | direct substitution, exact rollback, exact target representation | specialization of same setting/protocol | exact conclusion, not merely vanishing defects | same balanced target and Frobenius endpoint | actual errors,z,charges,history,loss vanish | padded screen inequalities checked | produced and consumed target equal | no accumulated forcing | exact branch -> steps 001--020 baseline clause | empty/first transaction and terminal stationary traces pass | step-local | None |

## Blocking Issues

1. The unresolved-only landscape has an incomplete producer/interface. In proof_sketch.md:875-946 and the step-008 row, the proof consumes a root-free Theta_act(I), epsilon_act, a normalized Hessian quadratic form, and finite tangent selectors. The live setting defines only the full array Theta_I; it does not define Theta_act, epsilon_act, the tangent family or Q, or the normal guard scale Lambda_nor used at proof_sketch.md:584-609. The flow and gate tables consequently have no legal producer for these controls. Add the max-row definition, its raw bound from the complete current Theta_I and prior budget, the exact current-frame stationarity equations, normalized tangent/Hessian formulas, all defect terms, and the producer-consumer path before step 008 selects a label. This is a same-setting sketch/interface defect, not the deleted Mat route.
2. The raw E_lb^c ledger margin is not exported. The sketch correctly retains r^2 e, sqrt(r)e, bar-eta^2, and source-normal terms on the complement branch (proof_sketch.md:109-123, 706-791, 1113-1212). However, step 012 states only that R-acc lies below a static angular-isolation scale (proof_sketch.md:1202-1207), while the binding setting asks for comparison to the nu_real^2 scale (setting.md:689-753, 904-915). For allowed E_lb^c instances with nu arbitrarily small or nu=0,e>0, a positive r^2 e budget cannot be absorbed into nu^2. Add explicit inequalities: use nu^2 absorption only on E_lb, use the observable score/capture and normalized Hessian thresholds to dominate every raw numerical term on E_lb^c, and state exact-limit equality. Reconcile the sketch's branch-qualified C'_z nu^2 language with the binding setting's displayed unconditional chain. This is a target-preserving sketch/interface defect if the intended branch-qualified reading is made explicit; silently dropping the raw branch is forbidden.
3. Step 015 consumes a full Cert+Lev neighborhood, but step 007 exports only Lev passage. proof_sketch.md:793-873 proves screen clauses near an unresolved atom, while proof_sketch.md:1347-1419 and the generated-flow rows assert that deterministic Haar-seeded dynamics reach a certificate-and-screen state. Add a lemma-sized output in step 007 or step 014/015 giving the certificate (radial, angular, Hessian, capture, and separation) neighborhood, its target-blind seed event, and the H_tx path bound; then compose it with the Lev slack. This is a same-setting reachability/interface repair, not an idea-level change.

## Required Repair Bundle

1. Repair step 003/008's landscape interface. Define Theta_act(I) as a root-free maximum over all unresolved rows before support selection; define epsilon_act as a sum of static, history, source-normal, radial, angular, and numerical defects; define Q and the normalized pair/synchronized tangent families; and define/provide Lambda_nor and q_nor. State the exact raw inequalities against mu_act and P^(-D_gap). Update the mechanism witness, Gate Evidence Table, Generated Output Flow, Rate Objective R2, and step dependencies/assumption IDs. A root-specific row may be consumed only after step 009 selects the unresolved label.
2. Repair step 012 and the public specialization with a three-branch margin ledger. On E_lb, display fixed exponent inequalities that absorb r^2 e and sqrt(r)e into the structural nu^2 scale. On E_lb^c, retain those terms and compare them separately with observable B2, capture s, radial g^3, and normalized Hessian P^(-D_gap) thresholds; do not invoke a lower-tail bound. In the exact H=e=0 limit, show equality and zero forcing. Reconcile the branch-qualified J-row with the setting's literal displayed simplification or explicitly record the required setting-level contract correction for the next formalization.
3. Add the missing Cert-neighborhood producer and flow. Show that the unresolved atom/nearby stationary state satisfies all certificate clauses with inverse-polynomial slack, or state the deterministic attraction/escape interface that produces it before step 015's Haar probability calculation. Include the first-update trace, sign correction, guard, and screen composition, and keep all target labels analysis-only.
4. Replace the sketch's Blockers: None in the next sketch with these interface blockers until the repaired outputs and branch inequalities are available. Enumerate assump:* IDs explicitly in steps 013 and 020, and retain the no-Mat route, exact J h=e^2 audit, rootwise one-time sum, separate probabilities/runtime, and exact baseline unchanged.

Target-preserving repair check: all three repairs can keep the current exact objective, observable protected-span screen, rank, runtime, probability modes, target metric, and no-floor baseline. No new primitive assumption or protected matched-mixture conclusion is needed. If the setting insists on an unconditional nu^2 absorption on E_lb^c, that auxiliary contract is impossible at nu=0,e>0 and must be escalated explicitly rather than hidden in a later step; the public screen-based theorem can remain target-preserving with the branch-specific raw margin.

## Review Rationale

REVISE_SKETCH is the deepest required change. The leverage screen successfully removes the idea-9 matched-mixture producer: no Mat-rel, Mat-hi, Mat-margin-10, protected matched-mass assumption, target-label query, or stale exchange flow remains, and the exact J diagonal/rootwise ledger, rollback, tape, PL, probability, runtime, and baseline interfaces are otherwise well localized. The live artifact is not yet ready for global proof or proof-step workers because the sole unresolved-mass landscape producer uses undefined theorem-critical controls, the full certificate reachability output is only asserted, and the raw small-nu ledger has no valid branch-specific target-scale comparison. These are coherent same-setting sketch/interface repairs rather than an idea/theorem-contract failure; score 6 and retry /proof-sketch.
