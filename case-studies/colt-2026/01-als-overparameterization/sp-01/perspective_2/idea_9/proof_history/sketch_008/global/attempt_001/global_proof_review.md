# Global Proof Review

## Global-Proof Review Identity

- Branch: `perspective_2/idea_9`.
- Global unit attempt: 2 under accepted sketch attempt 8.
- Binding setting: `perspective_2/idea_9/setting.md`, SHA-256
  `27315acf705cd791f29d341efaa11aae66803d02b9c240fc613cce06cc57c4b2`,
  in target-spec `material_partial` mode.
- Accepted sketch: `perspective_2/idea_9/proof_sketch.md`, SHA-256
  `40782b585d3292d25d1a921425470ade621f1dc6180a2f52bb05b94c96375fe8`,
  identifying sketch attempt 8 and eighteen stable Step IDs.
- Accepted sketch review: `perspective_2/idea_9/proof_sketch_review.md`,
  SHA-256
  `2a5834040fccae1871b1346184eca7458d479d525f8cdcb8a2e4110baa20a4c7`,
  with `Sketch Review Status = ACCEPTED` and retry target `None`.
- Reviewed global diagnostic: `perspective_2/idea_9/global_proof.md`, SHA-256
  `e31d16a3adbb6be1cd5ccd6654e51afb6ae52e88ae62eec20125c40d0eefa80c`.
- Reviewed global status: `PARTIAL_BLOCKED`.
- Reviewed suggested routing: `None`.
- Diagnostic architecture: ten theorem-level blocks G1--G10 covering all
  eighteen accepted steps, including `step_003b` and the attempt-8 repair
  step `step_003c` before `step_004`.

## Global-Proof Review Status

ACCEPTED

## Smallest Retry Target

None

## Blocking Issues

None.

## Required Repair Bundle

None.

## Contract And Status Audit

The diagnostic contains the required identity, controlled `PARTIAL_BLOCKED`
status, attempted theorem claim, whole-proof draft, theorem-level block map,
eighteen-row sketch-step coverage, dependency and assumption audit, citation
and tool audit, quantitative dependence audit, scope-and-closure certificate,
exported-interface feasibility table, generated-output flow, early obstruction
screen, global gap list, diagnostic boundary note, and controlled suggested
routing.

`PARTIAL_BLOCKED` with `Suggested Routing = None` is valid here. The artifact
does not claim any displayed local inequality as proved. Instead, every
unresolved theorem-critical obligation is assigned to an unchanged accepted
Step ID with a concrete mechanism source, raw interface, boundary trace, and
forward producer path. The diagnostic boundary expressly prohibits using this
artifact as proof evidence, a citation, an assumption source, or authority to
change the accepted theorem contract.

## Claim And Sketch Fidelity Audit

The attempted theorem preserves the target-spec `material_partial` claim and
exactly the nine primitive assumptions in `setting.md`. Realized norm, Gram,
weight, lower-tail, source-normal, ledger, matching, transaction, chart, and
convergence conditions remain derived. The diagnostic does not promote
`E_lb`, `B_perp`, `Theta_I`, a label assignment, or chart membership to a
public assumption.

The objective, full-space unprojected transaction protocol, exact rollback,
balanced-product-weight source class, strictly subquadratic rank
`ceil(C_rank r^(3/2)(log(2r))^2)`, separate instance and tape probabilities,
full-state/Frobenius metrics, arbitrary relative accuracy, and no-floor exact
baseline all match the accepted sketch. `Pi_star` remains analysis-only, and
`R-acc` is not used as factor distance.

The attempt-8 semantic repair is represented faithfully. `step_003c` uses
current correlations `x=M^T u`, dual coefficients `alpha=G^(-1)x`, and exact
dual selectors. `step_004` only transfers the already-produced witness and
does not reintroduce the rejected orthonormal-frame support argument. No
`idea_10` artifact, new primitive condition, changed metric, or weakened
endpoint is consumed.

## Theorem-Level Structure Audit

The ten blocks cover the eighteen accepted steps without dropping or merging
away a theorem-facing output:

| Block | Covered Step IDs | Theorem role | Review result |
| --- | --- | --- | --- |
| G1 | `step_001` | Once-drawn geometry, exact target, and derived `E_lb` | Complete diagnostic role; local probability and normalization proof remains. |
| G2 | `step_002`, `step_003` | H/J/K source, two transfer norms, conditional `Theta`, local z | Correct producer order and array conventions. |
| G3 | `step_003b` | Full-history normal recurrence, residual reserve, same-target projection transfer | Concrete triangular mechanism and raw/branch interfaces exposed. |
| G4 | `step_003c`, `step_004`, `step_005` | Dual/current activation, actual-residual transfer, matching and calibration | Attempt-8 repair is explicit and precedes matching. |
| G5 | `step_006` | Exact accepted-error expansion and honest J transition | Local z precedes use; O.1 numerical terms retained. |
| G6 | `step_007`, `step_008`, `step_009` | Rootwise row, one-time global ledger, protected-state induction | Raw terms and temporal prefix order preserved. |
| G7 | `step_010`, `step_011`, `step_012` | Deterministic transactions, rollback, Haar reachability, tape | Concrete algorithmic and conditioning roles exposed. |
| G8 | `step_013` | Exact-target Jacobian, chart, PL, smoothness | Geometry is produced before entry. |
| G9 | `step_014` | Same-target full-state chart entry | Direct `R-euc` bridge in the consumed metric. |
| G10 | `step_015`, `step_016` | First-exit descent, rate, probabilities, exact baseline | Terminal contraction and specialization are forward-only. |

The temporal path is noncircular:
`prior step_005 audit and prior-prefix budget -> current step_003b -> current
step_003c -> step_004 -> current step_005 audit -> transition/ledger -> next
prefix`. At the empty prefix the audit set is empty, `A=0`, and `B_perp=0`.
`step_009` supplies prior records and raw budgets by induction; concrete
transaction outputs do not flow backward into it.

## Dependency And Assumption Audit

Every theorem-facing generated condition has a legal source. `step_001`
produces `E_inst`, the exact realized target, and `E_lb`; `step_002` produces
H/J/K/S and both transfer gaps; `step_003` produces all candidate-indexed
`Theta` rows and local z; prior `step_009` outputs supply the legal history
and raw prefix budget; `step_003b` produces `(Res)`, `B_perp`, `(SE-raw)`, and
branch-qualified `(SE-lb)` before the current activation call.

Before label selection, `step_003c` consumes only the complete candidate array
through the root-free maximum `Theta_act`; a selected row is first consumed
after `p` exists in `step_005`. The local z definition precedes `step_006`,
while its quantitative row bound is produced later by `step_007`. The
all-commit tuple and chart constants are independently produced before
`step_014`, and chart membership precedes descent.

`E_lb` is used only for `(SE-lb)` and optional numerical-to-structural
simplification. The unconditional raw source-normal, row, and global terms
remain visible. This preserves the quantifier and probability modes and
avoids treating a generated lower tail as a primitive condition.

## Citation And Tool Audit

No external paper is used as theorem authority. The two named tensor papers
are proof-pattern provenance for source-span/Khatri--Rao arguments under
different procedures; all branch outputs are assigned to direct
current-notation derivations.

The theorem-critical tools are convention-compatible: Gaussian concentration
and density bounds act on the exact smoothed columns; Cauchy--Schwarz, Schur
products, and Gershgorin use the signed normalized Grams; Neumann series use
the exact nonnegative transfer domains; the triangular solve acts on actual
post-fit residuals; dual selectors are built from the same realized factors;
product-sphere calculus acts on the full ambient spheres; J expansion acts on
the actual accepted tensor error; Haar and Armijo use the stated transaction;
and balanced Jacobian/Taylor calculus uses the exact target orbit and declared
full-state metric. No unresolved source label, theorem wrapper, normalization
translation, or object-target mismatch is deferred to a later citation.

## Quantitative Dependence Audit

The diagnostic retains all exposed structural variables, proof exponents,
probability modes, horizons, and norms. In particular:

- the raw row and global ledger scales are
  `nu^2+r nu^3+sqrt(r)e` and `r nu^3+r^2e`;
- `(SE-raw)` is unconditional, while `(SE-lb)` and finite-smoothed
  `nu^2` absorption use only the derived `E_lb` branch;
- capture and radial comparability precede `N_I(g)e/g^3` and the activation
  margins;
- the isolated O.1 scale remains `h^4+e h+e^2`, including at `h=e^2`;
- `varrho_PL=c_chart g_minus/r`, `mu_PL>=c_mu g_minus^4`, and
  `L_chart=C_L g_plus^4` remain exposed;
- full-state entry retains `g_plus sqrt(r)eta0` and the ratio
  `chi_tau r^(3/2)eta0`;
- instance and conditional tape probabilities remain separate; and
- discovery is finite and independent of `epsilon`, while terminal work is
  polynomial times `log(1/epsilon)`.

Hidden constants remain restricted to fixed class and protocol constants.
No instance, commit order, tape realization, horizon, or accuracy dependence
is silently hidden.

## Scope And Closure Review

The scope certificate passes. The source-normal row exposes the concrete
finite recurrence

```
a_(t,M)+sum_(q<t)c_(q,t,M)a_(q,M)=-b_(t,M),
||b_(t,M)||<=e Lambda_nor,
sum_(q<t)|c_(q,t,M)|<=q_nor<=1/4,
```

the unresolved-target reserve, triangular inverse, finite
`r e Lambda_nor` forcing budget, projector telescoping, and the exact scales
at which `B_perp` is dominated. This is a noncircular same-target mechanism,
not a future-step label.

The activation row likewise has a theorem-critical witness. Capture first
produces positive score and radial scale. Exact biorthogonality then gives
`Dual-stat` without an additive first-order Gram defect. The finite normalized
single-label and pair tangent families cover low score, common support,
one-/two-mode straddling, signs, threshold equality, all mode permutations,
and `|U_I|=1`. The shared relations
`12 eps_act<=mu_act`, `3 B_perp<=mu_act`, and
`g^3 mu_act>=4N_I(g)P^(-D_gap)` provide the local dominance interface.

The J ledger has a nonnegative one-step charge counted once over at most r
commits; failed probes have zero forcing. The protected invariant is a finite
induction. Transaction amplification is stopped and finite. Terminal descent
is contractive with a finite path reserve and no additive forcing. The exact
baseline is algebraic equality. These mechanisms are compatible with their
declared finite or logarithmic scopes.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported-interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_inst`, exact target, `E_lb` | Event, representation, optional absorption source | Gaussian tails/density, normalization, direct CP identities | Primitive source assumptions -> `step_001` | Exact realized norms, Grams, weights, T, Qstar, and lower-tail property | Same normalized-Gram and Frobenius conventions | Finite reserves; `P(E_lb^c)<=r^-20`, `nu>=P^-D_lb` on branch | Base reserves, variance, polynomial ranges, finite probability allocation | Produced T is exactly consumed T | Once-drawn; no trajectory accumulation | Primitives -> `step_001` -> static/prefix/terminal consumers | `step_001` feeds `step_002`, `step_003b`, `step_009`, `step_013`, `step_016` | Nonzero norm, tiny-nu complement, exact H=e=0 branch | step-local | None |
| H/J/K/S, gaps, `Theta`, local z | Positive small gain and conditional array source | Column Cauchy--Schwarz, Schur products, exact transfer, Neumann series | `step_001 -> step_002 -> step_003` | Exact array domains, roots, modes, labels, and zero diagonal | Entrywise absolute Gram is never given a false operator bound | Transfer norm gives `q_res,q_row<=1/4`; z is defined before its row bound | H columns, K/J rows, S, legal A, e, q gaps | Same target-frame coordinates consumed by activation and J charge | Geometric within-state sum; one conditional profile | Static event -> gaps -> full candidate Theta/local z | Theta feeds `step_003b`,`step_003c`,`step_005`; z feeds `step_006`,`step_007` | Empty A, simultaneous candidates, zero diagonal, exact branch | step-local | None |
| `(Res)`, `B_perp`, `(SE-perp)`, `(SE-raw)/(SE-lb)` | Source support and same-target residual transfer | Post-fit stationarity, pair-Khatri--Rao unit diagonal, triangular inversion, reverse triangle, projector telescoping | Static outputs plus prior `step_005` audits and prior-prefix invariant -> `step_003b` | Full tensor normal bound and residual reserve have exact consumed claim class | Actual residual and realized source-span projectors use current setting | Displayed normal recurrence, `q_nor<=1/4`, `B_perp=3r Lambda_nor e/(1-q_nor)` | Prior R-euc/post-fit records, restricted Gram reserve, exponents, `E_lb` | `R_I=Pi_star R_I+(Id-Pi_star)R_I`; target-frame contractions equal; normal residual dominated at score/Hessian scales | Finite sign-free `r e Lambda_nor`; `(SE-lb)` only on branch | Empty prefix -> current `step_003b` -> current activation -> stored next audit | `step_003b` feeds `step_003c`,`step_004`,`step_005`,`step_009`,`step_011`,`step_016` | Empty/first row, invisible completion, finite e/tiny nu, exact branch | step-local | None |
| `Theta_act`, `Dual-stat`, singleton/exchange | Support activation and strict-saddle alternative | Ordered capture/radial scale, exact dual selectors, Gram energy, normalized tangent family | `steps_001--003b` plus prior-prefix raw budget -> `step_003c` | Exact label selector and common `mu_act` interface consumed by `step_004` | `D=MG^-1`, `x=M^Tu`, `alpha=G^-1x` use exact realized frame | `Dual-stat`; low/common/straddle/sign/cardinality split; `Q>=4mu_act` or positive singleton | Complete candidate Theta, root-free maximum, Bperp, dual norm bounds, all margin inequalities | Projected and actual residual share exact T; Bperp is sole transfer residual | One finite prefix calculation; history/numerical defects in `eps_act` | Prior prefix -> ordered radial scale -> dual equations -> finite alternative -> transfer | `step_003c` produces singleton/exchange for `step_004`; singleton alone reaches `step_005` | Exact atom with one-mode correlation, all-mode products, low/common/straddling/sign/equality/`|U|=1`, H=e=0 | step-local | None |
| Actual-residual transfer, matching, calibration | Support, membership, injection, factor error | Product-sphere Hessian identity, completed activation witness, G3 transfer, radial/angular equations | `step_003c -> step_004 -> step_005` | Exports unique unresolved label, R-euc/R-dir, separation, post-fit audit | Full ambient spheres and exact realized atoms | Exchange margin contradicts Hessian certificate; singleton plus radial equations gives calibration | `mu_act`, Dgap, capture, separation, selected Theta row after selection | Actual accepted slot compared directly to exact target atom | One record per successful commit | Activation -> transfer -> matching/audit -> next prefix | `step_005` feeds next `step_003b`, `step_006`, `step_009`, `step_011`, `step_014` | Positive/zero/negative score, mixed support, separation boundary | step-local | None |
| J update, O.1, row/global ledger | Signed transition and finite accumulated invariant | Exact E expansion, same-J baseline cancellation, rootwise l2 factorization | Local z and matching -> `steps_006--009` | J-diag/update, raw row, J-global, history and R-acc match setting | J diagonal/off-diagonal and root-block conventions identical | `Delta A<=2Gamma Upsilon`; row and global raw bounds as displayed | H/J/z, calibration, q_row, strict prior prefix, finite count | E, A, Xi are exact target-frame ledger objects, not factor distance | Nonnegative one-time charges; at most r; failed probes zero | Empty base -> exact charge -> row -> global sum -> next prefix | `step_006` charge, `step_007` row, `step_008` global, `step_009` invariant | h=0, h<<e, h=e^2, cyclic/transitive, dense/block, exact limit | step-local | None |
| Protected invariant, transactions, tape | Finite closure, reachability, rollback, probability | Ordered induction, exact rollback, exact dynamics, Haar activation, independent tape | `step_009 -> steps_010--012` plus random-tape primitive | Actual success/failure states, ptx, tuple, and work claims | Full-space Haar and exact-loss transaction match setting | Valid prefix plus success extends; failure identity; `p_tx`, stopped tail | Prefix outputs, guard, step floor, Htx/Ntx, independence | Success is actual state; failure is exact prior state | At most r successes; failed probes have no forcing | Empty prefix -> abstract invariant -> dynamics -> reachability -> tape | `step_012` produces actual tuple for `step_014` | Empty scratch, sign flip, zero score, missing root, guard, exhaustion, rollback | step-local | None |
| Exact-target PL geometry and chart entry | Coercivity, basin membership, residual-to-target bridge | Balanced Jacobian floor, pair/triple gaps, Taylor bounds, R-euc cube-root comparison | `step_001 -> step_013`; discovery tuple/R-euc -> `step_014` | Exact full-state chart, PL, smoothness, and membership | Exact Qstar orbit, full-state Euclidean metric, Frobenius loss | Radius `g_minus/r` makes Taylor defects subcritical; distance `<=C g_plus sqrt(r)eta0` fits strict reserve | Weight/Gram gaps, chi, radius, Dstat, all 3r factor residuals | Actual tuple and exact orbit share metric; R-acc excluded | Local geometry plus one finite entry sum | Geometry and tuple independently produced -> chart entry | `step_013` feeds `step_014/015`; `step_014` feeds `step_015/016` | Exact target, minimum weight, block stress, maximum nu, H=e=0 equality | step-local | None |
| Terminal first exit, public rate, exact baseline | Contraction, arbitrary accuracy, no floor | PL, smoothness, strict entry reserve, exact Armijo, algebraic baseline | `steps_013--016` | Final Frobenius residual, work, probabilities, and exact reduction | Same target and balanced metric throughout | Geometric F recurrence and finite path prevent exit; defects vanish at H=e=0 | mu, Lchart, path coefficient, epsilon, rank/horizon substitutions | Final residual is exactly `T-Psi(Q)` | Contractive logarithmic horizon; no additive forcing | Entry -> descent -> specialization; exact branch -> stationary target | `step_016` assembles final theorem and baseline | Qstar stationary, epsilon -> 0, chart boundary, first rollback | step-local | None |

## Hard-Step Localization Audit

- `step_001`--`step_003` remain local static probability, array, and
  resolvent derivations under fixed objects and norms.
- `step_003b` is high risk but local: the unit-diagonal normal system,
  pair-Khatri--Rao gap, prior audit, residual reserve, and fixed exponent order
  already fix its source and exported interface.
- `step_003c` is highest risk but local: exact dual selectors, the projected
  score Hessian, root-free history control, normalized finite tangent family,
  and common margin are all fixed under the unchanged accepted step. A future
  proof failure may still trigger sketch revision, but the diagnostic itself
  exposes no missing interface.
- `step_004`--`step_005` are local same-target transfer and calibration work;
  they do not need a new support mechanism.
- `step_006`--`step_009` are local exact expansion, rootwise counting, and
  finite induction work with raw numerical terms retained.
- `step_010`--`step_012` are local guarded-dynamics and conditional-probability
  work under the existing full-space tape law.
- `step_013`--`step_014` are local exact-target calculus and direct full-state
  entry work; the coercive source and consumed radius are already fixed.
- `step_015`--`step_016` are local first-exit, stopping, substitution, and
  baseline assembly work.

No unresolved obligation currently requires a changed primitive assumption,
algorithm, theorem scope, metric, probability mode, exposed dependence,
success criterion, generated-output interface, or conclusion.

## Early Idea-Failure Screen

- Contract contradiction: none. The theorem remains inside the accepted
  material-partial class and algorithm.
- Exact-atom/source-convention stress: passed. With only mode A correlated and
  the scratch state equal to atom 1, `x_A=G_Ae_1` and `alpha_A=e_1`; the false
  off-label first-order term is absent. Correlation in the other modes enters
  only through the honest product source already present in the K/J budget.
- Activation nonvacuity: passed at diagnostic granularity. Low score,
  common support, one-/two-mode straddling, signs, equality, source-normal
  modes, and one remaining label are assigned normalized witnesses or the
  singleton branch under one positive margin.
- Entry-state trace: passed. Empty history has `A=B_perp=0`; capture precedes
  inverse radial scales; zero/negative score and guard failures are explicit;
  rollback is identity; Qstar is stationary with an active Jacobian floor.
- Residual-to-target stress: passed. G3 transfers the actual residual to its
  exact source-span component with an explicit normal residual; G9 separately
  transfers the actual tuple to the exact target orbit in the consumed metric.
- Generated-output flow: passed. The current activation call consumes G3 and
  the prior-prefix budget, the selected row is delayed until `step_005`, the
  current audit feeds only the next temporal call, and terminal producers
  precede entry and descent.
- Accumulation stress: passed. Normal forcing and J charges have finite
  budgets, failures contribute zero, and terminal descent has no additive
  forcing.
- Dependence and baseline stress: passed. Raw numerical terms precede branch
  simplification, probabilities remain separate, and exact representation,
  rollback, joint-zero slots, arbitrary epsilon, and no floor are preserved by
  equality in the exact branch.

The screen finds no sketch-level or theorem-contract obstruction hidden by
the `PARTIAL_BLOCKED` label. This conclusion accepts only the diagnostic
locality classification; it does not pre-accept the local dual/tangent,
ledger, reachability, or PL derivations.

## Review Rationale

The global diagnostic is faithful, detailed, and safe to pass as diagnostic
context to step-level workers. It covers all eighteen accepted steps, supplies
a legal temporal flow, and independently exposes the two high-risk mechanism
witnesses: the full-history source-normal triangular bridge and the repaired
current/dual-frame singleton-or-exchange alternative. The former is
same-target and branch-correct; the latter passes the exact-atom stress and
has normalized boundary witnesses at one common scale. The exact J transition,
finite ledger, transaction/tape scope, exact-target PL chart, direct
full-state entry, public rates, and no-floor baseline also have concrete
producer-consumer paths.

`ACCEPTED` with `Smallest Retry Target = None` is therefore the smallest sound
decision. Later proof-step failures must still be routed on their own evidence;
under the user-directed policy, a repeated activation derivation failure that
exposes a missing interface should route to `/proof-sketch`, not be treated as
pre-authorized idea failure.
