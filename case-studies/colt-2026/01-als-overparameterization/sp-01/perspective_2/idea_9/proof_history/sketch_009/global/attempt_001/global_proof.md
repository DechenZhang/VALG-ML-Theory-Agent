# Global Proof

## Reviewed Sketch Identity

- Branch: `perspective_2/idea_9`.
- Global unit attempt: 1 under the current accepted sketch (sketch attempt 9).
- Producer-attempt identity: this is the first global-proof producer dispatch
  for sketch attempt 9. Any sketch-8 global/step diagnostics are archival
  lineage only and are not current proof evidence.
- Binding idea: `perspective_2/idea_9/idea.md`, SHA-256
  `bc2eb7163a21b79d11fb5af6cd746eb70ea94141ca95501818f11f40a62894b6`.
- Binding setting: `perspective_2/idea_9/setting.md`, SHA-256
  `27315acf705cd791f29d341efaa11aae66803d02b9c240fc613cce06cc57c4b2`,
  in target-spec `material_partial` mode.
- Reviewed producer: `perspective_2/idea_9/proof_sketch.md`, SHA-256
  `ec03ae55ed75fb555f0aa2401c28a14a042d6195679afc41b331e92dbe782fb3`,
  which identifies sketch attempt 9 in `revise_sketch` mode and contains the
  nineteen stable Step IDs, including repair rows `step_003b`, `step_003c`,
  and the new all-label bridge `step_003d` before `step_004`.
- Reviewed gate: `perspective_2/idea_9/proof_sketch_review.md`, SHA-256
  `ed7df07285be0ad9f63cd1ee40d2db961982ee24ad93eb0e573c4dccf6c8f1c9`,
  which reviews attempt 9 and records `Sketch Review Status = ACCEPTED`,
  viability score 8, retry target `None`, and retry mode `none`.
- Prior sketch, global, and step diagnostics are archived lineage only. They
  are not used as current proof evidence. This attempt-9 diagnostic audits the
  repaired all-label matched-mass/exchange interface in addition to the
  weight-scaled radius, direct full-state `R-euc` bridge, and source-normal
  recurrence.
- Exposed algorithmic rank:
  `k=ceil(C_rank r^(3/2)(log(2r))^2)`, with `r<k<r^2`.
- Binding baseline: exact asymmetric CP least squares, full-space unprojected
  transactions, exact rollback, exact generally nonorthogonal target,
  joint-zero unused slots, separate instance and tape probabilities, and no
  additive terminal floor.

## Status

PARTIAL_BLOCKED

The accepted attempt-9 sketch composes at theorem-diagnostic granularity. Its
generated-output graph is acyclic in temporal as well as static order:
`steps_001--003` plus the prior accepted `step_005` audit feed the current
`step_003b`; that bridge feeds ordered `step_003c`, whose all-label equations
feed `step_003d`; only the finished singleton/exchange alternative then feeds
`step_004`, whose `step_005` audit is stored for the next temporal
`step_003b`. The empty prefix has no normal rows and zero source-normal
residual. `step_009` supplies these prior records by induction and consumes no
later concrete transaction output. Within one landscape call, `step_003`
produces all candidate rows, `step_003c` consumes only the root-free maximum
before support selection, `step_003d` consumes the generated matched-error
tensor and all labels (never a selected-label row), and `step_005` consumes a
root-specific row only after an unresolved label is selected. Separately,
`step_003` defines the local precommit z profile before `step_006` consumes it,
`step_007` later proves its quantitative bound, and `step_013` produces the
exact-target radius, smoothness scale, and path coefficient before entry or
descent.

The former full-space landscape mismatch is repaired in the actual residual
metric. With source-span projectors `P_M` and
`Pi_star=P_A tensor P_B tensor P_C`, prior post-fit stationarity gives the
triangular normal system

```
a_(t,M)+sum_(q<t)c_(q,t,M)a_(q,M)=-b_(t,M),
||b_(t,M)||_2<=e Lambda_nor,
sum_(q<t)|c_(q,t,M)|<=q_nor<=1/4.
```

Triangular inversion and rank-one projector telescoping therefore give

```
||(Id-Pi_star)R_I||_sigma
 <= ||(Id-Pi_star)R_I||_F
 <= B_perp:=3r Lambda_nor e/(1-q_nor).
```

The independent unresolved-target reserve
`||R_I||_F>=c_R tau_min` and the fixed exponent order yield the unconditional
capture/curvature comparisons `(SE-raw)`. Only the derived branch
`E_lb={nu>=P^(-D_lb)}` yields `(SE-lb)`, namely
`B_perp<=c_SE tau_min eta0`; no finite-e eta-scale absorption is claimed on
its complement. Target-frame contractions are unchanged exactly by
`Pi_star`, so this bridge controls the previously invisible outside-span
certificate without changing the algorithm or target.

The former terminal mismatch is repaired in the declared full-state metric.
The actual all-commit tuple satisfies

```
dist(Q_fin,Z_star) <= C_ent g_plus sqrt(3r) eta0,
```

while the consumed chart has radius
`varrho_PL=c_chart g_minus/r`. Their ratio is bounded by
`C_ent sqrt(3)chi_tau r^(3/2)eta0`, whose structural and numerical pieces are
`O(1/(sqrt(r)log^2 r))` and `O(r^(-1/2))`. Thus the direct factor bridge, not
`R-acc`, supplies entry with a fixed reserve after choosing the final
asymptotic threshold under the already fixed source margin. The exact
`H=e=0` branch has equality and a larger
constant-relative chart.

Substantial local work remains: Gaussian normalization and lower-tail
constants, the post-fit normal equations and triangular inversion, the
capture/radial ordering and exact current/dual-frame equations, the generated
matched-mass tensor and residual-curvature exchange margin in `step_003d`, the
actual-residual transfer, the exact J update, the rootwise one-time ledger,
history-uniform Haar reachability, balanced Jacobian/Taylor bounds, and
first-exit Armijo path control all require proofs.
Each unresolved theorem-critical obligation has an existing mechanism source,
an unchanged output interface, a same-target bridge where needed, and
`step-local` locality. No sketch/interface or theorem-contract defect is
diagnosed.

## Attempted Theorem Claim

Goal mode is `target-spec`; the intended progress type is `material_partial`
and the theorem-facing scope is exactly the narrowed smoothed,
balanced-product-weight class stated in `setting.md`. There are fixed choices of all proof and protocol constants and exponents such
that the following holds uniformly over every deterministic base triple
satisfying the nine primitive assumptions in `setting.md`.

1. With probability at least `1-r^(-10)` over the single Gaussian smoothing
   draw, all realized columns are nonzero and polynomially bounded, the
   realized product-weight ratio is at most `2Gamma_star`,
   `nu_real<=2nu_star`, the pair and triple Khatri--Rao Grams have the stated
   `1/16` gaps, the tensor norm has the stated two-sided comparison, and the
   exact balanced rank-r representation lies inside the guard. The same
   event includes a derived lower-tail branch
   `E_lb={nu_real>=P^(-D_lb)}` whose added failure allocation is at most
   `r^(-20)`.
2. Conditional on one such fixed instance, the H/J/K source, transfer gaps,
   conditional Neumann envelope, local zero-diagonal z profile, actual
   accepted-history forcing, unresolved-target reserve, exact current/dual
   activation alternative, matching, exact J update, quantitative row
   profile, and finite all-prefix budgets are derived rather than assumed.
   Before each current landscape call, prior accepted certificates and
   matching records produce `(Hist-F)`, the residual reserve `(Res)`, the
   source-normal bound `(SE-perp)`, unconditional `(SE-raw)`, and the
   `E_lb`-conditioned `(SE-lb)`. The raw bridge uses
   `B_perp=3r Lambda_nor P^(-D_stat)/(1-q_nor)` with `q_nor<=1/4` and keeps
   every target-frame contraction exact under `Pi_star`.
   The raw all-commit scale is
   `O(Gamma_star r nu_real^3+r^2P^(-D_stat))`; only `E_lb` or the exact
   `H=0,e=0` branch permits the displayed `nu_real^2` simplifications.
3. Every observable certificate first yields, through exact current
   correlations and dual coefficients, all-label coordinate equations. The
   generated protected-error tensor and an unresolved/matched mass split in
   `step_003d` then give an exhaustive alternative: an unresolved singleton,
   an unresolved structural exchange, or a matched-residual exchange with an
   explicit curvature margin. A matched singleton is rejected by `Sep`; only
   the unresolved singleton is passed to matching. `step_004` transfers the
   finished witness to the actual residual and, after a proof-only product-one
   sign choice, exports separation, `R-euc`, `R-dir`, and injection extension.
   Every accepted transition satisfies the exact symmetric J-aware history
   update and tensor influence bounds in all three modes. On the isolated O.1
   family the captured-label charge is
   `O(h^4+P^(-D_stat)h+P^(-2D_stat))`, includes the positive numerical terms,
   and dominates the exact `2h^4+O(h^6)` increment even at
   `h=P^(-2D_stat)`, without a lower bound on h.
4. A fresh full-space Haar transaction reaches a valid new certificate with
   history-uniform conditional probability at least `(nr)^(-c_tx)`. Every
   failed transaction rolls back exactly. A polynomial independent tape
   yields r distinct commits with conditional tape probability at least
   `1-r^(-10)` and discovery work polynomial in
   `n,r,k,kappa,rho^(-1)` and independent of `epsilon`.
5. Let `g_minus=tau_min^(1/3)` and `g_plus=tau_max^(1/3)`. The exact target
   orbit has a balanced full-state chart of radius
   `varrho_PL=c_chart g_minus/r`, a quadratic sandwich, gradient Lipschitz
   scale `L_chart=C_L g_plus^4`, and PL constant
   `mu_PL>=c_mu g_minus^4`, with both radius and PL constant at least
   `P^(-D_PL)`. The actual all-commit tuple enters this exact-target chart by
   the direct factor bridge above. Joint unprojected balanced Armijo descent
   remains inside and, for every `0<epsilon<1`, returns a rank-k tensor with
   residual at most `epsilon||T||_F` in polynomial work times
   `log(1/epsilon)`, without an additive floor.

The instance and tape probabilities remain separate. Hidden constants and
polynomial degrees may depend only on the fixed class and protocol constants
named in the setting, never on dimensions, the realized instance, the commit
order, or `epsilon`. The result is material-partial because it covers the
balanced-product-weight, modewise-near-orthogonal smoothed source class and
the specified sequential transactional algorithm, not arbitrary bases,
unbalanced weights, or simultaneous all-slot gradient descent.

The inherited exact orthogonal/noiseless specialization is part of the claim
as a deterministic exact-limit statement. With `H=0` and `e=0`, all
structural and numerical ledger terms vanish, the exact target plus joint-zero
unused slots has zero loss, failed probes preserve the represented tensor by
exact rollback, and the terminal module has no positive error floor.

## Whole-Proof Draft

### Block G1: once-drawn realized geometry and the derived lower-tail branch

Write `a_j=bar a_j+g_j` and cyclically, with
`g_j~N(0,rho^2 I_n/n)`. Gaussian norm concentration, the base norm reserve,
and the smoothing margin must first yield simultaneously over all `3r`
columns

```
(2kappa)^(-1) <= ||a_j||,||b_j||,||c_j|| <= 2kappa.
```

Expand each normalized Gram entry around its deterministic base value into a
linear Gaussian part, a centered quadratic part, and a normalization
remainder. An operator or epsilon-net bound on the signed matrix, with all
three modes and normalization events included in one finite union, must give

```
nu:=max_M ||G_M-I||_op
   <= nu_base+C_sm kappa rho sqrt(log(2r)) <= 2nu_star.
```

No operator bound is asserted for the entrywise absolute matrix. Relative
norm control transfers the primitive product balance to
`tau_max/tau_min<=2Gamma_star`.

For each pair of modes, columnwise Cauchy--Schwarz gives

```
sum_(l!=j) |(G_B-I)_(jl)(G_C-I)_(jl)| <= nu^2.
```

Gershgorin therefore yields the pair Khatri--Rao Gram gap. The triple
Hadamard Gram has no larger off-diagonal row sum in the declared regime.
Consequently

```
(15/16)||tau||_2^2 <= ||T||_F^2 <= (17/16)||tau||_2^2,
```

and the balanced target slots represent this exact T inside the radius guard.

The numerical-to-structural ledger comparison needs an independently derived
lower-tail source. Fix a label pair in one mode and condition on the second
realized column. On its norm event, the raw inner product with the first
Gaussian-perturbed column has arbitrary mean and conditional standard
deviation at least `c rho/(sqrt(n)kappa)`. The uniform Gaussian density bound
therefore controls the probability of a centered interval independently of
the mean. Upper norm bounds transfer a raw threshold to the exact normalized
Gram entry. Since `P=16nrk kappa rho^(-1)` and all reciprocal scales have the
declared polynomial bounds, one fixed `D_lb` gives

```
P(E_lb^c) <= r^(-20),   E_lb={nu>=P^(-D_lb)}.
```

Disjoint pairs may strengthen the allocation but are not a new theorem
assumption. Choosing `D_stat` after `D_lb`, with sufficient polynomial slack,
then gives on `E_lb`

```
r^2e <= c_num nu^2,   sqrt(r)e <= c_num nu^2,
e=P^(-D_stat).
```

Outside `E_lb` the raw terms remain displayed. The exact `H=e=0` branch is a
separate algebraic specialization rather than a positive-smoothing event.

### Block G2: static H/J/K source and two-norm resolvent

For `H_M=abs(G_M-I)`, taking entrywise absolute values preserves each
column's Euclidean norm. Thus

```
max_j ||H_M e_j||_2 <= nu,
||H_M||_1=||H_M||_infty <= sqrt(r)nu.
```

For `K_A=H_B circ H_C`, columnwise Cauchy--Schwarz gives

```
||K_A||_1=||K_A||_infty <= nu^2,
L_(A,j) <= tau_max nu^2 <= 2Gamma_star tau_j nu^2,
```

and cyclically. With `J_A=I+H_A`, the static first-chain source satisfies

```
||S||_1 <= 3r nu^2(1+sqrt(r)nu).
```

The identity part of J retains the direct path, its H part retains the first
three-label path, and later Neumann powers retain longer paths.

For one fixed unresolved root, the transfer is a three-mode off-diagonal
block map. Each diagonal multiplier is at most `nu+eta0`, each J spreading
block has norm at most `1+sqrt(r)nu`, and the three-mode adjacency has norm
two. Therefore both relevant induced norms obey

```
||Tcal|| <= 2(nu+eta0)(1+sqrt(r)nu),
q_res,q_row <= 1/4.
```

For any legal conditional history, the nonnegative Neumann series converges:

```
Theta_I=(Id-C0 Gamma_star Tcal)^(-1)
        C0 Gamma_star(S+A_I+e 1).
```

It gives separately

```
||Theta_I||_1
 <= C Gamma_star(||S||_1+||A_I||_1+r^2e),

||Theta_I||_(root,2)
 <= C Gamma_star(||S||_(root,2)
                 +||A_I||_(root,2)+sqrt(r)e).
```

At the empty prefix `A=0`. At later prefixes G6 supplies the conditional
history. For a candidate p, `step_003` now legally defines the local array

```
z_(p,M,l)=Theta_(I;p,M,l)+C_num e  for l!=p,
z_(p,M,p)=0.
```

This definition precedes its use by the exact charge and makes no premature
rowwise-smallness claim.

### Block G3: full-history source-normal recurrence and projection transfer

Let `P_M` be the orthogonal projector onto the full realized source span in
mode M and set

```
Pi_star=P_A tensor P_B tensor P_C,
Lambda_nor=R_bar+(2R_max)^3+2L_bar/tau_min,
q_nor=nu^2+4delta_led sqrt(r)nu+4r delta_led^2,
delta_led=C_led eta0.
```

At a protected prefix, write `U_I=[r]\pi_I(I)` and
`T_U=sum_(j in U_I)tau_j phi_j`. Restricted triple-Gram conditioning gives

```
||T_U||_F^2
 =tau_U^T(G_A[U] circ G_B[U] circ G_C[U])tau_U
 >=(15/16)||tau_U||_2^2
 >=(15/16)tau_min^2.
```

For each earlier accepted slot i matched to p, balanced rank-one telescoping
of its already produced `R-euc` record gives

```
||E_i||_F<=C_E tau_p eta0,
B_hist,F(I):=sum_(i in I)||E_i||_F
 <=C_E r tau_max eta0.
```

The upper Gram margin and the selected numerical exponent make this at most
one half of the unresolved-target reserve. Since
`R_I=T_U-sum_(i in I)E_i`, reverse triangle therefore yields, before any
capture argument,

```
||R_I||_F
 >=sqrt(15/16)tau_min-B_hist,F(I)
 >=c_R tau_min.                                      (Res)
```

This positive lower bound is unconditional on `E_lb`; it uses only the upper
Gram margin and the small structural/numerical accepted-factor errors.

The source-normal tensor is recovered from the post-fit equations, not
assumed as a prefix condition. Order accepted slots by commit time. For each
earlier slot t define, in mode A,

```
a_(t,A)=g_t^3 P_A^perp u_(t,A),
b_(t,A)=P_A^perp(R_(I_t) times_2 u_(t,B)^T
                          times_3 u_(t,C)^T),
c_(q,t,A)=<u_(q,B),u_(t,B)><u_(q,C),u_(t,C)>,
```

and cyclically in B and C. Because the exact target lies in the source
product span, it disappears after `P_M^perp`. The radial and angular
certificate at the same accepted slot, evaluated with its post-commit
residual, must give

```
a_(t,M)+sum_(q<t)c_(q,t,M)a_(q,M)=-b_(t,M),
||b_(t,M)||_2<=e Lambda_nor.                          (Nor)
```

Distinct generated labels and `R-euc` imply

```
sum_(q<t)|c_(q,t,M)|
 <=nu^2+4delta_led sqrt(r)nu+4rdelta_led^2
 =q_nor<=1/4.                                         (Nor-gap)
```

Thus finite triangular Neumann inversion gives

```
sum_(t,M)||a_(t,M)||_2
 <=3r Lambda_nor e/(1-q_nor)=:B_perp.
```

Telescoping each accepted rank-one tensor through the three mode projectors
turns this factor-normal sum into the actual same-target tensor bound

```
||(Id-Pi_star)R_I||_sigma
 <=||(Id-Pi_star)R_I||_F
 <=B_perp.                                            (SE-perp)
```

The exponent order is part of the interface. Primitive polynomial controls
give fixed `D_nor,D_tau` with
`r Lambda_nor<=P^D_nor` and `tau_min>=P^(-D_tau)`. After fixing
`D_lb,D_gap,D_cap`, choose

```
D_stat >= D_nor+D_tau
          +max{1,2D_lb,D_cap/2,D_gap+3D_tau}+10.
```

Combining this order with `(Res)` yields, for every certificate radius
`0<g<=2R_max`, the unconditional raw comparisons

```
B_perp<=c_SE min{
  tau_min/r,
  ||R_I||_F P^(-D_cap/2),
  N_I(g)P^(-D_gap)/g^3
}.                                                    (SE-raw)
```

Only on the derived event `E_lb`, where `nu>=P^(-D_lb)`, does the same
exponent order give

```
B_perp<=c_SE tau_min eta0.                            (SE-lb)
```

For finite `e>0` and `nu<nu_lb`, `(SE-lb)` is unavailable and no later block
may use it; `(SE-raw)` remains valid. In the exact `H=e=0` branch both
`B_perp` and `eta0` vanish.

Finally use the exact decomposition

```
R_I=Pi_star R_I+(Id-Pi_star)R_I.
```

Every target-frame contraction used by `A_I`, `Theta_I`, or the J ledger is
unchanged because its test tensor lies in the range of `Pi_star`. For an
arbitrary full-space scratch direction, the normal residual changes the score
by at most `B_perp`, the product-sphere gradient by at most
`sqrt(3)B_perp`, the Hessian quadratic form by at most
`3B_perp||xi||_2^2`, and the residual norm by at most `B_perp`. These are the
exact residual-to-target comparisons consumed in G4.

The temporal order is noncircular:

```
steps_001/002/003 + prior-iteration step_005 audit
 -> current step_003b
 -> current step_003c
 -> current step_004
 -> current step_005 audit
 -> next temporal step_003b.
```

`step_009` supplies the prior certificate, matching, `R-euc`, and post-fit
records. At the empty prefix there are no normal rows and `B_perp=0`. No
concrete transaction output flows backward into `step_009`. The remaining
work in this block is the step-local derivation of the exact post-fit bound,
the coefficient row sum, projector telescoping constants, and all raw/branch
dominance inequalities under the fixed exponent order.

### Block G4: exact current/dual activation, transfer, matching, and calibration

This block is split conceptually even though the accepted roadmap assigns the
activation mechanism to `step_003c` and the actual-residual comparison to
`step_004`--`step_005`.  G3 supplies, for a legal protected prefix,

```
R_I=R_I^parallel+R_I^perp,
R_I^parallel=Pi_star R_I,
||R_I^perp||_(sigma,F)<=B_perp,
```

with exact equality of every source-frame contraction and `(SE-raw)` transfer
at score, gradient, Hessian, capture, and residual-norm scales.  The
`(SE-lb)` comparison is used only on its named derived branch.  No outside-span
rank-one sector is left as an unstated premise.

#### G4a: ordered capture and radial scale (`step_003c`)

For `t=g^3`, `s=s_I(u,v,w)`, and `R_0=||R_I||_F`, the observable capture
identity and the relative certificate first give

```
s >= sqrt(2) R_0 P^(-D_cap/2),
R_0 e <= s/16,
(1-4e)s <= t <= (1+4e)s,
t >= (c_R tau_min/2)P^(-D_cap/2),
s_parallel >= 3s/4 > 0.
```

The last comparison uses `(SE-raw)` only after the first four inequalities.
Thus `N_I(g)e/t` and all activation scales are well-defined without a
circular lower bound on `g^3`, on `E_lb`, on its complement, and in the exact
branch.  Define, only now,

```
Theta_act(I)=max_(j in U_I,M)||Theta_(I;j,M,.)||_2,
eps_act=C_act tau_max[nu^2+rnu^3+sqrt(r)e+Theta_act(I)]
         +C_act[B_perp+N_I(g)e/t],
s_low=tau_min/(512 sqrt(r)), beta_act=1/(8 sqrt(r)),
delta_act=C_delta r^2 eps_act/tau_min,
mu_act=c_act tau_min/r^2.
```

The exponent and upper-margin checks supply
`delta_act<=1/256`, `12 eps_act<=mu_act`, `3B_perp<=mu_act`, and
`g^3 mu_act>=4N_I(g)P^(-D_gap)`.  The complete candidate-indexed `Theta_I`
array is already produced by `step_003`; the maximum above is root-free and
is the only history row consumed before a label is selected.

#### G4b: exact current/dual equations and atom stress

Let `M_M=[m_(1,M),...,m_(r,M)]`, `D_M=M_M G_M^(-1)`, and
`d_(j,M)=D_M e_j`.  Then

```
<d_(j,M),m_(ell,M)>=delta_(jell),
x_M=M_M^T u_M,
alpha_M=D_M^T u_M=G_M^(-1)x_M,
P_M u_M=M_M alpha_M,
||P_Mu_M||^2=x_M^T G_M^(-1)x_M.
```

The Gram reserve controls the dual norms multiplicatively, so first-order
Gram mixing is retained in `x=G alpha` rather than charged as an additive
`O(nu)` defect.  For
`v_M=R_I^parallel times_(N ne M) u_N^T`, angular stationarity gives

```
v_M-s_parallel u_M=zeta_M,
-s_parallel P_M^perp u_M=P_M^perp zeta_M,
tau_j x_(j,N)x_(j,L)-s_parallel alpha_(j,M)
  =h_(j,M)+<d_(j,M),zeta_M>.
```

Here `h` is the exact projected accepted-history contraction.  The current
prefix raw budget, dual norm bounds, and the Neumann equation bound all `h`
and `zeta` by `eps_act`; no root-specific `Theta` row is used.  This exact
equation is the source-compatible support selector consumed by the next
subblock.

The former one-mode-correlated atom is an exact boundary check.  With empty
history, modes B and C orthogonal, mode A having off-diagonal `h`, and
`u_M=m_(1,M)`, one has `x_A=G_Ae_1`, `alpha_A=e_1`, while
`x_B=x_C=alpha_B=alpha_C=e_1`.  For label 2 both sides of the displayed
equation are zero and for label 1 they equal `tau_1-s_parallel=0`; there is
no spurious `Theta(tau_1 h)` term.  If B or C is also correlated, the
remaining off-label term is the honest product of two cross-correlations and
is charged by the existing `K/J` source at `O(tau_max nu^2)`.

#### G4c: exhaustive normalized tangent alternative (`step_003c`)

Set `L_M={j:|alpha_(j,M)|>=beta_act}` with equality active and define
`z_(j,M)=P_(u_M^perp)d_(j,M)`.  The dual energy and source-normal equation
ensure every mode has an active coefficient, while the margin gives
`delta_act<=1/256`.  The exact projected Hessian form used for the case split
is

```
Q(xi)=2<R_I^parallel,
 xi_A otimes xi_B otimes w + xi_A otimes v otimes xi_C
 +u otimes xi_B otimes xi_C>
      -s_parallel sum_M||xi_M||^2.
```

Use normalized single-label two-mode tangents from `z_(j,M)` whenever the
two displayed norms exceed `1/4`.  For `p != q`, use the exactly tangent dual
pair family

```
z_M^(p,q)=alpha_(q,M)d_(p,M)-alpha_(p,M)d_(q,M),
<m_(ell,M),z_M^(p,q)>=alpha_(q,M)1_(ell=p)
                         -alpha_(p,M)1_(ell=q).
```

Normalize these vectors using the dual Gram bounds and enumerate all three
two-mode pairs and all four product-one synchronized three-mode sign choices.
The low-score case `s_parallel<s_low` receives a signed single-label tangent
or, when one residual dual norm is small, a pair tangent; direct substitution
leaves a margin at least `8mu_act` after `6eps_act`.  If two labels are active
in all modes, the four synchronized signs give the same lower bound on the
pair form.  If supports first straddle across one or two modes, the first
change supplies either the single-label alternative or a two-mode pair with
margin `c_2 tau_min beta_act^2-6eps_act>=8mu_act`, cyclically over AB, AC, BC.
Threshold equality is active.  When `|U_I|=1`, pair tangents are omitted and
the same low/singleton split applies.

The remaining case has a unique `p` active in all modes, with

```
|alpha_(p,M)|>=1-32delta_act,
sum_(j!=p)alpha_(j,M)^2<=64delta_act^2,
|R_oth|<=tau_p|x_(p,A)x_(p,B)x_(p,C)|/8+4eps_act.
```

If the dominant product sign is negative, a product-one signed tangent takes
the exchange branch.  Otherwise the singleton branch exports

```
s_parallel >= (3/4)tau_p|x_(p,A)x_(p,B)x_(p,C)|-5eps_act=:s_iso>0.
```

All mode permutations, signs, equality boundaries, normal modes, and the
cardinality-one case are covered.  Under the sketch's signed convention the
exchange form has the required normalized negative-curvature witness
(`Q>=4mu_act` in the displayed `Q` orientation), while the singleton form has
the positive isolation margin.  This is a finite one-prefix calculation, so
history accumulation is only through `Theta_act` and `B_perp`.

#### G4d: actual-residual transfer and matching (`step_004`--`step_005`)

`step_004` transfers the already-produced singleton/exchange witness through
`B_perp`, preserving the same `mu_act` scale, and compares the angular
Hessian threshold with `P^(-D_gap)`.  It does not redo support selection or
consume a root-specific row.  On the singleton branch, radial and angular
stationarity, the unit-vector identity, and the branch-qualified calibration
comparison yield

```
|delta_i|+max_M||e_(i,M)|| <= C_led eta0,
|<e_(i,M),m_(ell,M)>| <= C_led(Theta_(I;p,M,ell)+e), ell!=p,
<e_(i,M),m_(p,M)>=-||e_(i,M)||^2/2.
```

The separation certificate excludes protected labels, product-one signs are
proof-only, and the injection extends.  Only now does `step_005` consume the
selected root-specific `Theta_(I;p,M,.)` row, store `R-euc/R-dir`, and record
the post-fit normal audit for the next temporal `step_003b`.  Thus matching,
calibration, and source-normal provenance have a legal forward flow.

### Block G5: exact accepted-error expansion and honest J transition

For a commit matched to p, orient the product-one signs and write
`u_M=m_(p,M)+e_M`, `g^3=tau_p(1+delta)`. Then

```
E/tau_p
 = (1+delta)(m_(p,A)+e_A) tensor (m_(p,B)+e_B)
                  tensor (m_(p,C)+e_C)-phi_p.
```

Contracting two modes with one tested target and separating the exact target
product from every error-bearing term gives the Xi tensor-influence bound.

For a still-unresolved root j and tested A-label l, the tested target factor
is exactly `J_A(p,l)`: it is one for `l=p` and `H_A(p,l)` off diagonal. The
two root-mode factors are `H_B(p,j),H_C(p,j)` because `p!=j`. Subtract the
exact target scalar before applying the product majorant. This yields

```
A_(I union {i};j,A,l)-A_(I;j,A,l)
 <= 2Gamma_star Upsilon_tilde_(p;j,l)^A,
```

where

```
Upsilon_tilde_(p;j,l)^A
 =(1+eta0)(J_A(p,l)+z_(p,A,l))
   (H_B(p,j)+z_(p,B,j))(H_C(p,j)+z_(p,C,j))
  -J_A(p,l)H_B(p,j)H_C(p,j).
```

The local z used here was already produced by G2; its quantitative row bound
is not needed for this exact expansion. Cyclic modes are identical.

For `l=p`, zero diagonal z gives

```
Upsilon_tilde_(p;j,p)^A
 =eta0 H_B(p,j)H_C(p,j)
  +(1+eta0)[H_B(p,j)z_(p,C,j)
            +H_C(p,j)z_(p,B,j)
            +z_(p,B,j)z_(p,C,j)].
```

On the isolated O.1 family `nu=h`, `H_A(p,j)=0`, and
`H_B(p,j)=H_C(p,j)=h`. The relevant structural profile vanishes and the
positive numerical padding is order e. Hence

```
Upsilon_tilde_(p;j,p)^A=O(h^4+e h+e^2),
```

with matching positive structural and numerical terms. At `h=e^2` it still
dominates `2h^4+O(h^6)`. For `l!=p`, J equals H and preserves the direct,
transitive, and longer-path interface without a spurious diagonal term.

### Block G6: quantitative row profile, one-time ledger, and abstract prefix

After the symbolic transition is established, the already defined local z is
quantitatively bounded. The exact fixed-root resolvent line is

```
||z_p||_2
 <= C[nu^2(1+sqrt(r)nu)
      +||A_(I;p)||_2+sqrt(r)e].
```

The step-local proof must use the strict prior-prefix history and the exact
G5 transition rootwise, before taking a global l1 sum, to obtain

```
Z:=max_(p,M)||z_(p,M,.)||_2
 <= C(nu^2+rnu^3+sqrt(r)e).
```

On `E_lb`, this is at most `C'nu^2`; at `H=e=0` it is zero. Outside `E_lb`
the raw terms are retained.

For one captured p and one mode, expand the already baseline-subtracted
charge before summing. Put `L=1+sqrt(r)nu`. Dropping only nonnegative index
restrictions gives

```
sum_(j,l) Upsilon_tilde_(p;j,l)^A
 <= L[eta0 nu^2+(1+eta0)(2nu Z+Z^2)]
    +(1+eta0)sqrt(r)Z[nu^2+2nu Z+Z^2].
```

The first bracket includes the captured-label diagonal and the second the
tested-mode z terms. With
`eta0=O(Gamma_star nu^2+e)`, `sqrt(r)nu<=1/20`, and the displayed Z, every
structural and numerical monomial is at most `C(nu^3+r e)` per captured p
after the fixed constants are chosen. Summing each actual commit once gives

```
U_s <= C Gamma_star rnu^3+C r^2e.
```

The exact one-step relation then yields

```
||A_(I_s)||_1 <= 2Gamma_star U_s,
```

the same factorization for Xi yields `R-acc`, and substitution into the l1
resolvent yields `R-budget`. No failed probe enters these sums.

The abstract protected-state induction is now noncircular. The base is
`I_empty,A_empty=0`, with empty injection and the G2 resolvent. Given a valid
prefix, G3 first derives its current source-normal/residual interface from
prior audits; G4 matches an abstract successful certificate; G5 charges it;
and G6 produces the next raw and branch-qualified budgets plus the post-fit
audit used at the next temporal invocation of G3. An abstract failure that
restores the scratch slot leaves the prefix exactly unchanged. Thus
`step_009` proves the success/failure preservation interface without
consuming any later transaction output. Concrete transactions only consume
this invariant in G7.

### Block G7: deterministic transaction dynamics and finite tape

At fixed directions,

```
partial_g f_I=-3g^2(s_I-g^3),
```

so a positive score has the unique positive radial root `g^3=s_I`.
Safeguarded bracketing and dyadic bisection must reach the relative radial
tolerance within the guard. At a radial bracket, exact product-sphere angular
gradient steps give Armijo decrease. If the normalized gradient is small but
the angular Hessian has eigenvalue below `-P^(-D_gap)`, an approximate
minimum-eigenvector and the better of its two signs must give polynomial
decrease after radial re-bracketing. Compact guard smoothness supplies the
accepted-step floor `P^(-D_ls)`. Every other branch is unsuccessful and maps
the scratch slot exactly to joint zero.

G4, after consuming G3 and the exact current/dual `step_003c` alternative,
provides an inverse-polynomial activation window
around each unresolved
atom. Three full-space Haar small-ball estimates, combined with the
deterministic flow, must give uniformly over every legal protected history

```
P_tape(next transaction commits | T,history)
    >= p_tx=(nr)^(-c_tx).
```

Separation and injection make every success a new label. A stopped
negative-binomial or stagewise tail with
`N_tx=C_tape r p_tx^(-1)log(2r)` gives all r commits with conditional failure
at most `r^(-10)`. Failed probes contribute zero forcing and successes are
finite, so the ledger's declared scope is respected.

### Block G8: exact-target balanced Jacobian, quadratic chart, and PL

Restrict to the r active balanced slots and minimize only over the finite
permutation/product-one sign orbit. At the exact target, the CP synthesis
Jacobian maps a balanced tangent perturbation to the sum of its three one-mode
replacements and radial component. Pair and triple Khatri--Rao Gram gaps and
the removal of continuous scaling by balance must give

```
c_J g_minus^2 ||Delta||_2
 <= ||D Psi(Qstar)Delta||_F
 <= C_J g_plus^2 ||Delta||_2.
```

This is the coercive source. Slotwise multilinearity and Cauchy--Schwarz give
the diagnostic perturbation interfaces

```
||D Psi(Q)-D Psi(Qstar)||_op
    <= C_T r g_plus dist(Q,Z_star),

||residual Hessian term||_op
    <= C_T r g_plus^3 dist(Q,Z_star).
```

At

```
varrho_PL=c_chart g_minus/r,
```

choosing `c_chart` below fixed multiples of
`c_J/chi_tau` and `c_J^2/chi_tau^3` makes both perturbations smaller than a
quarter of the exact-root floor. Finite orbit representatives remain
separated and the guard retains slack. In aligned normal coordinates this
gives

```
c_F g_minus^4 dist(Q,Z_star)^2
 <= F(Q)
 <= C_F g_plus^4 dist(Q,Z_star)^2,

||grad_bal F(Q)||_F^2 >= 2c_mu g_minus^4 F(Q).
```

The same direct derivative bounds produce
`L_chart=C_L g_plus^4`, with fixed dimensionless `C_L`. From
`c_F,C_F,c_mu,C_L,chi_tau` and the fixed Armijo parameters, define the fixed
path coefficient `C_path_star` before entry and descent are analyzed.

The realized norm and weight bounds give
`g_minus>=(2kappa)^(-1)` and bounded `chi_tau`, so
`varrho_PL>=c/(kappa r)` and `mu_PL>=c/kappa^4`. Because P dominates r and
kappa, one fixed `D_PL` makes both at least `P^(-D_PL)`. In the exact
orthogonal branch the block-diagonal Jacobian permits the larger
`varrho_orth=c_orth g_minus`.

### Block G9: direct full-state entry at the weight-scaled radius

The all-commit injection is a bijection. Align each active slot with its
target label and product-one signs. If `g_i^3=tau_p(1+delta_i)`, the cube-root
mean-value bound and `R-euc` give, for every mode,

```
||sigma_(i,M)g_i u_(i,M)-g_p^star m_(p,M)||_2
 <= C_ent g_p^star(|delta_i|+||e_(i,M)||_2).
```

Summing exactly the `3r` active factor blocks and noting that inactive slots
agree at joint zero yields the same-target bridge

```
dist(Q_fin,Z_star) <= C_ent g_plus sqrt(3r)eta0.
```

This bridge uses the exact full-state Euclidean metric and no transformed
object. `R-acc` remains a tensor-contraction/influence budget and contributes
no factor-distance term.

Divide by `varrho_PL=c_chart g_minus/r`. Entry with reserve reduces to

```
C_ent sqrt(3)chi_tau r^(3/2)eta0
    <= theta_ent c_chart,
```

where

```
theta_ent <= min{1/2,1/(4C_path_star),
                 (1/4)sqrt(c_F/C_F)chi_tau^(-2)}.
```

The structural and numerical pieces satisfy

```
r^(3/2)Gamma_star nu^2
 <= 4Gamma_star/(C_nu^2 sqrt(r)log^2(2r)),

r^(3/2)e <= r^(-1/2)
```

because `nu<=2/(C_nu r log(2r))`, `P>=r`, and `D_stat>=2`. For the already
fixed source margin, choose the final `r_0` after
`c_chart,theta_ent,C_ent`. This respects the declared constant order and proves

```
dist(Q_fin,Z_star)<=theta_ent varrho_PL<=varrho_PL/2
```

without `E_lb`. Independent O.1 blocks, whose aggregate factor displacement
is `g_plus sqrt(r)h^2`, have ratio
`O(chi_tau/(sqrt(r)log^2 r))` and therefore pass the previously blocking
stress. At `H=e=0`, the aligned tuple equals Qstar exactly and consumes the
constant-relative orthogonal chart.

### Block G10: first-exit descent, quantitative assembly, and exact baseline

Inside the G8 chart, compact smoothness and exact-loss Armijo give an
inverse-polynomial accepted-step floor and

```
F_(t+1) <= (1-c_arm mu_PL/L_chart)F_t
```

up to the fixed trust-region/backtracking constants. The quadratic sandwich
and the fixed condition ratio imply the total factor path bound

```
sum_t dist(Q_(t+1),Q_t)
 <= C_path_star dist(Q_fin,Z_star).
```

The choice of `theta_ent` makes this path shorter than the distance to the
chart boundary. A first-exit contradiction therefore proves chart invariance.
There is no additive forcing after discovery: inactive slots remain zero and
the r committed slots are updated jointly. Geometric loss decrease reaches

```
sqrt(2F_t)=||T-Psi(Q_t)||_F <= epsilon||T||_F
```

in `poly(P)log(1/epsilon)` terminal work.

The public specialization substitutes the rank formula, the polynomial
ranges of kappa and `rho^(-1)`, all transaction and line-search horizons, and
the exponent choices. It keeps the raw `rnu^3+r^2e` ledger visible before
the `E_lb` branch comparison, allocates `E_lb^c` inside the once-drawn
instance failure budget, and states the tape probability conditionally on the
fixed good instance. Hidden constants retain exactly the allowed dependence.

#### Exact baseline specialization

In the deterministic orthogonal/noiseless and exact-stationarity limit,
`H=K=S=0`, `e=0`, `A_empty=0`, and the Neumann envelope and every local z
profile are zero. The J diagonal is one, but both root-mode factors vanish, so
the first accepted transition has zero charge. Exact target slots plus
joint-zero unused slots represent T, every failed transaction restores the
identical represented tensor, and Qstar is stationary at zero loss. The
constant-relative orthogonal chart has zero entry residual, so terminal
descent preserves arbitrary epsilon and no floor. This is equality in the
same factor and tensor metrics, not a finite-e surrogate.

### Assembly

G1--G2 establish the only static sources. G3 derives the full-history
source-normal and residual-reserve bridge from prior accepted records before
each current landscape call. G4 first derives the exact current/dual
activation alternative, then transfers it and turns any abstract successful
certificate into a unique label; G5--G6 complete the exact transition and next valid finite
prefix without circular transaction input. G7 realizes those abstract
transitions on the finite tape and produces the actual tuple. G8 independently
produces exact-target coercivity and all terminal constants. G9 transfers the
actual tuple to that exact chart at the correct weight and full-state scale.
G10 proves no-floor descent, work, separate probability statements, and the
inherited exact specialization. Every transition has an existing mechanism
source and a legal forward dependency, but the local derivations remain to be
proved and reviewed.

## Theorem-Level Block Map

| Block | Role in attempted theorem | Source Step ID(s) or assembly role | Dependency inputs | Theorem-facing assumptions | Unresolved obligations |
| --- | --- | --- | --- | --- | --- |
| G1 | Once-drawn geometry, exact target, and derived lower tail | `step_001` | Six source primitives | Stable assumption IDs from the setting only | Prove normalization/operator tails, small-ball constants, and finite probability allocation. |
| G2 | H/J/K source, transfer gaps, conditional Theta, and local z definition | `step_002`, `step_003` | G1 and a conditional legal A | No realized or history condition promoted publicly | Prove both induced norms, Neumann bounds, and exact local array convention. |
| G3 | Full-history normal recurrence, residual reserve, and projection transfer | `step_003b` in its temporal induction role | G1--G2 plus prior accepted `step_005` records supplied by `step_009` | No source-normal condition or residual lower bound is assumed | Prove post-fit equations, `q_nor<=1/4`, triangular inversion, projector telescoping, `(Res)`, `(SE-raw)`, and branch `(SE-lb)`. |
| G4 | Exact current/dual activation, actual-residual transfer, unique label, calibration, and injection | `step_003c`, `step_004`, `step_005` | G1--G3, ordered capture/radial scale, complete candidate-indexed `Theta_I`, and G3 transfer | Primitive balance only through G1; `E_lb` remains derived; no target label is an input | Prove `(Act-cap)`--`(Act-margin)`, exact dual stationarity, atom stress, all low/common/straddling/sign/cardinality-one cases, normalized exchange margin, transfer, `R-euc/R-dir`, separation, and the stored post-fit audit. |
| G5 | Exact E expansion, tensor influence, J update, and honest O.1 audit | `step_006` | G2 local z and G4 calibration | No lower bound on h | Prove scalar/tensor expansions, cyclic forms, constants, and exact-increment domination. |
| G6 | Quantitative z row, global budgets, and abstract temporal prefix invariant | `step_007`, `step_008`, `step_009` | G1--G5 and strict prior-prefix inputs | `E_lb` permits only named simplifications | Prove rootwise history control, per-p factorization, one-time counting, raw budgets, and the noncircular G3-to-next-G3 induction. |
| G7 | Transaction dynamics, rollback, Haar reachability, and tape amplification | `step_010`, `step_011`, `step_012` | G4/G6 invariant, which already includes G3 | `assump:random-tape` | Prove accepted-step floor, activation window, history-uniform probability, stopped tail, and work. |
| G8 | Exact-target quadratic chart, PL, smoothness, and path coefficient | `step_013` | G1 exact target and pair/triple gaps | No basin membership premise | Prove balanced tangent floor, Taylor defects, `C_L`, and inverse-polynomial bounds. |
| G9 | Same-target full-state chart entry | `step_014` | G4 calibration, G7 tuple, G8 radius | No factor interpretation of R-acc | Prove orbit alignment, cube-root bound, one Cauchy--Schwarz sum, and raw `r^(3/2)eta0` dominance. |
| G10 | First-exit descent, public rate, probability assembly, and exact baseline | `step_015`, `step_016`, direct specialization | G1,G3,G6--G9 and exact setting identities | `assump:accuracy-range`, rank/tape primitives; deterministic exact-limit branch | Prove path control, contraction, stopping, work/rate specialization, branch probability accounting, and first-transition/stationary zero-loss trace. |

## Sketch-Step Coverage

| Step ID | Covered block or assembly use | Dependency use | Local proof obligations | Notes |
| --- | --- | --- | --- | --- |
| `step_001` | G1 | Produces `E_inst,E_lb,Qstar` | Norm retention, normalized Gram concentration, weights, Schur gaps, tensor norm, small-ball normalization | Static and step-local. |
| `step_002` | G2 | Consumes G1; produces H/J/K/S and both gaps | Column l2, K l1, leakage, S, l1/rootwise block maps | No operator bound on entrywise absolute Gram. |
| `step_003` | G2 | Consumes gaps and conditional A; produces Theta and local z | Two Neumann norms, empty case, zero diagonal, padding convention | Local z precedes charge; no row bound yet. |
| `step_003b` | G3 | Consumes G1--G2 plus prior temporal `step_005` records; produces the current residual bridge | Hist-F/Res, post-fit b bound, triangular coefficient row, inversion, projector telescoping, raw/branch dominance, differential transfer | Empty prefix has no rows; `SE-lb` is `E_lb`-conditioned. |
| `step_003c` | G4 | Consumes G1--G3, observable certificate, realized Grams, and root-free `Theta_act` | Ordered capture/radial scale, exact current/dual equations, exact-atom stress, normalized single/pair tangents, exhaustive support partition, `mu_act` margin | Sole producer of `Dual-singleton` or `Dual-exchange`; no root-specific Theta row is consumed before selection. |
| `step_004` | G4 | Consumes the finished `step_003c` witness and G3 transfer | Same-target score/gradient/Hessian transfer, unchanged `mu_act` comparison, exchange exclusion, certificate dichotomy | Does not redo support selection; high-risk step-local transfer. |
| `step_005` | G4 | Consumes singleton dichotomy; produces matching/calibration and next audit | Product-one signs, radial/diagonal calibration, R-euc/R-dir, separation, post-fit b record and selected Theta row | Exact target factors; later G3 consumes the audit. |
| `step_006` | G5 | Consumes local z and calibration; produces exact transition | E/R-tensor, same-J subtraction, J-diag, O.1 upper/lower, cyclic cases | Does not consume later z norm. |
| `step_007` | G6 | Consumes local z, exact transition, prior prefix | Fixed-root history decomposition, raw row bound, E_lb/exact branch | High-risk finite-prefix estimate. |
| `step_008` | G6 | Consumes G5/G6 row outputs | Per-p factorization, diagonal/off-diagonal counting, J-global, R-acc | Keep `r^2e` raw. |
| `step_009` | G6 | Applies all conditional producers from empty prefix, including current G3 and `step_003c` before transfer/matching | Temporal producer order, success/failure abstraction, branch-qualified row, stored next audit | No concrete transaction output consumed. |
| `step_010` | G7 | Consumes invariant; produces certificate or rollback | Bracketing, Armijo, curvature escape, guard and work floor | Exact failure state. |
| `step_011` | G7 | Consumes deterministic flow and invariant | Haar activation and history-uniform p_tx | Target labels remain proof-only. |
| `step_012` | G7 | Consumes p_tx/injection/rollback | Stopped tail, all r commits, separate tape probability and work | Produces actual tuple. |
| `step_013` | G8 | Consumes exact target/gaps; produces all chart constants | Balanced Jacobian, Taylor defects, quadratic sandwich, PL, `C_L`, path coefficient | Geometry precedes entry. |
| `step_014` | G9 | Consumes tuple, R-euc, weights, G8 radius | Orbit alignment, cube-root transfer, full-state sum, raw dominance | R-acc explicitly excluded. |
| `step_015` | G10 | Consumes chart constants and strict entry | Armijo decrease, path length, first exit, floor-free stopping | Finite logarithmic horizon. |
| `step_016` | G10 | Consumes raw/branch rates, tape, entry, descent | Hidden dependence, work/rank substitution, separate probabilities, exact baseline | Public Rate Specialization Bridge. |

## Dependency And Assumption Audit

The attempted public theorem uses exactly the nine primitive assumptions in
the binding setting. All event, trajectory, ledger, matching, transaction,
and basin conditions below are derived or explicitly local conditional
hypotheses.

| Theorem-facing condition | Provenance class | Producer | First theorem-critical consumer | Audit result |
| --- | --- | --- | --- | --- |
| Realized norms, Grams, weights, gaps, exact T/Qstar | derived static event | `step_001` | `step_002`, `step_013` | Legal forward dependency. |
| `E_lb` | derived static event | `step_001` | `step_003b`, then branch-aware `step_004`, `step_007`, `step_008`, `step_009`, `step_016` | Used for `(SE-lb)` and optional ledger absorption only; raw interfaces remain outside it. |
| H/J/K, S, leakage, q gaps | derived static controls | `step_002` | `step_003` and ledger blocks | Exact array conventions. |
| Conditional Theta and local z | explicitly conditional generated controls | `step_003` | `step_003b`, then `step_004`--`step_007` | Theta supplies target-frame controls; local z exists before charge and its row bound remains later. |
| `B_hist,F`, `(Res)`, `B_perp`, `(SE-perp)`, `(SE-raw)`, branch `(SE-lb)` | derived temporal prefix and residual-transfer controls | current `step_003b` from static outputs plus prior `step_005` records supplied by `step_009` | current `step_003c`, `step_004`, `step_005`, `step_009`, `step_011`, `step_016` | Empty prefix is the base; target-frame contractions are exact; no normal/reserve premise is assumed. |
| `Theta_act`, `Dual-singleton`, `Dual-exchange`, `mu_act` | derived activation/support controls | `step_003c` from G1--G3 and the observable certificate | `step_004`; singleton branch then `step_005` | Exact current/dual selectors, ordered radial scale, normalized tangent family, and all boundary cases are supplied before support selection; no target label is assumed. |
| Matching, signs, R-euc/R-dir, injection, post-fit audit | derived per-success controls | `step_004`, `step_005` after the `step_003c` witness | next temporal `step_003b`, `step_006`, `step_009`, `step_011`, `step_014` | Labels/signs are proof-only; the post-fit record is consumed only at a later prefix. |
| E, Xi, Upsilon, J update, O.1 audit | derived transition controls | `step_006` | `step_007`--`step_009` | Exact idea-9 interface. |
| Quantitative z, global/history/R-acc budgets | derived finite-prefix controls | `step_007`, `step_008` | `step_009`, later prefix consumers, rate audit | Raw and branch-specialized forms distinguished. |
| Abstract protected invariant | derived finite-horizon invariant | `step_009` | next temporal `step_003b` and `step_010`--`step_012` | Supplies prior audits but consumes no later concrete transaction output. |
| Concrete transaction/tape outcomes and tuple | derived algorithmic events | `step_010`--`step_012` | `step_014` | Conditional probability path is forward-only. |
| PL radius, quadratic sandwich, `L_chart,C_path_star` | derived exact-target geometry | `step_013` | `step_014`, `step_015` | No entry premise is used to prove geometry. |
| Full-state chart membership | derived basin certificate | `step_014` | `step_015` | Direct R-euc bridge in exact metric; no R-acc transfer. |
| Terminal contraction and public rate | derived finite-horizon outputs | `step_015`, `step_016` | final theorem | Legal after strict entry reserve. |
| Exact baseline | derived deterministic specialization | direct identities and `step_016` | final baseline clause | Same-target equality, not a finite-e theorem premise. |

No generated invariant is inserted into the public assumptions, and no
missing producer-consumer edge is visible in the accepted roadmap.

## Citation And Tool Audit

| Source or tool | Current-branch objects and role | Assumptions or mapping to discharge | Needed conclusion and convention compatibility | Affected block / Step ID | Diagnostic disposition |
| --- | --- | --- | --- | --- | --- |
| Gaussian norm and normalized-Gram concentration | Independent smoothed columns around nonidentical bases | Base norms, normalization remainder, three modes, finite union, signed-Gram convention | G1 norm and `nu<=2nu_star` event with displayed dependence | G1 / `step_001` | Standard/current-notation tool; constants remain step-local. |
| Uniform Gaussian density/small-ball | One raw pair inner product conditional on partner | Partner norm event, arbitrary mean, normalization, probability allocation | `E_lb`, fixed `D_lb`, failure at most `r^-20` | G1 / `step_001` | Valid source path; full proof pending. |
| Cauchy--Schwarz, Schur product, Gershgorin | H columns, K kernels, pair/triple Grams | Unit diagonals and exact normalized factors | R-src, K/leakage/S and spectral gaps | G1/G2 / `step_001`,`step_002` | Direct compatible derivations. |
| Nonnegative Neumann series | Exact l1 and rootwise transfer maps | q gaps and zero-diagonal array domain | Conditional Theta and local z source | G2 / `step_003` | Standard fact with exact norm mapping. |
| Orthogonal projection, finite triangular inversion, rank-one telescoping | Actual prior accepted factors, post-fit residuals, realized source spans | Prior temporal `step_005` audits, distinct labels, `R-euc`, pair-Khatri--Rao row bound, exact asymmetric residual convention | `(Hist-F)`, `(Res)`, `(Nor)`, `(SE-perp)`, raw/branch transfer in the exact Frobenius/product-spectral metrics | G3 / `step_003b` | Direct current-notation derivation. The unit-diagonal system and every defect class are exposed; constants remain step-local. |
| Exact current/dual Gram coordinates and finite tangent algebra | Realized normalized factors, `G_M`, `D_M=M_M G_M^{-1}`, current correlations, projected residual | Modewise Gram reserve, exact biorthogonality, ordered capture/radial scale, complete root-free `Theta_act`; no orthonormal-frame substitution | `Dual-stat`, source-normal exclusion, exact-atom boundary, low/common/straddling/sign/cardinality-one alternatives, normalized exchange or singleton margin | G4 / `step_003c` | Direct current-notation derivation; source conventions match the realized frame, and no external strict-saddle wrapper is invoked. |
| Product-sphere differential calculus and same-target transfer | Exact residual score, angular gradient/Hessian | Full spheres, exact radial re-bracketing, no algorithmic protected projection; G3 transfer is applied afterward and preserves `mu_act` | Landscape dichotomy and calibration for the actual residual | G4 / `step_004`,`step_005` | Direct current-notation proof; no cited strict-saddle theorem assumed. |
| Multilinear expansion and rootwise Cauchy--Schwarz | Actual E, target contractions, J charge, z/history arrays | Product-one sign gauge, weights, same J baseline, strict prefix | J update, O.1 audit, quantitative row/global budgets | G5/G6 / `step_006`--`step_009` | Direct algebra; constants remain local. |
| Dyadic Armijo and approximate minimum eigenvector | Exact guarded transaction loss | Guard smoothness, radial root, tangent Hessian-vector access | Accepted-step floor or exact rollback | G7 / `step_010` | Standard algorithmic tool with direct interfaces. |
| Haar spherical-cap/small-ball estimates | Three independent full-space seeds | Exact dimension and activation windows, history-uniform geometry | `p_tx>=(nr)^(-c_tx)` | G7 / `step_011` | Step-local; no label is queried. |
| Stopped Bernoulli/negative-binomial tail | Independent unused tape entries under adaptive history | Uniform conditional p_tx, at most r successes, rollback | All-commit probability and discovery work | G7 / `step_012` | Standard probability tool with compatible conditioning. |
| Balanced Jacobian and Taylor calculus | Exact active CP map at Qstar | Balanced tangent/gauge, pair/triple gaps, weight scales, full-state metric | Jacobian floor, radius, quadratic sandwich, PL and smoothness | G8 / `step_013` | Direct current-notation derivation; no external wrapper. |
| Cube-root mean value and Euclidean Cauchy--Schwarz | Matched amplitudes/directions and exact orbit | Delta range, R-euc, bijection/signs, weight ratio | Full-state entry at the consumed radius | G9 / `step_014` | Exact same-target transfer; all residual factors exposed. |
| Local PL plus Armijo first-exit | Exact objective in the produced chart | Strict entry reserve, L_chart, quadratic sandwich, step floor | Geometric loss, bounded path, no-floor endpoint | G10 / `step_015` | Step-local after G8/G9. |
| *Guarantees for Alternating Least Squares in Overparameterized Tensor Decompositions* (NeurIPS 2025 official proceedings) and *Beyond Lazy Training for Over-parameterized Tensor Decomposition* (arXiv:2010.11356) | Source-span/Khatri--Rao projection proof-pattern provenance | Their algorithms, objectives, initialization, and output contracts differ; no theorem statement is mapped into this branch | Motivation for the G3 proof pattern only; all branch conclusions are rederived in current notation | G3 lineage | Not consumed as proof authority or cited wrapper. |

No theorem-critical citation has an unresolved identity, object mapping, or
conclusion mismatch. Every direct/standard tool uses the setting's exact
normalization, target, procedure, and metric.

## Quantitative Dependence Audit

| Category | Exposed contract | Diagnostic audit |
| --- | --- | --- |
| Structural variables | `n,r,k,kappa,rho,Gamma_star,P,nu,eta0`, realized weights and H/J/K/S | Realized values are derived; hidden constants use only fixed class/protocol constants. |
| Hidden constants | Fixed class/protocol dependence only | Constants may depend on `c_0,c_rho,C_dim,C_sm,C_nu,C_rank,Gamma_star` and fixed protocol/Armijo choices, but not on dimensions, instance, order, tape, or `epsilon`. |
| Fixed quantities | Deterministic base triple, class constants, fixed proof exponents, protocol constants, and a prefix length `s<=r` when a conditional ledger row is evaluated | Limits and public substitutions hold these quantities fixed; `D_stat` is selected only after `D_lb,D_gap,D_cap` and the polynomial `Lambda_nor/tau_min` bounds. |
| Rank | `k=ceil(C_rank r^(3/2)log^2(2r))`, `r<k<r^2` | Exact strictly subquadratic substitution; unused slots remain zero. |
| Numerical scales | `e=P^(-D_stat)`, h, `D_lb,D_stat,D_gap,D_cap,D_ls,D_PL` | Exponent order is fixed; local `e h,e^2` remain visible. |
| Source-normal bridge | `Lambda_nor,q_nor,B_perp,SE-raw,SE-lb`, `D_nor,D_tau` | `B_perp=3r Lambda_nor e/(1-q_nor)`; `(SE-raw)` is unconditional, while `(SE-lb)` and any `nu^2` absorption require `E_lb` (or exact `H=e=0`). |
| Activation bridge | `s,t,s_parallel,Theta_act,eps_act,s_low,beta_act,delta_act,mu_act,D_cap,D_gap` | Capture and radial comparability precede every inverse radial scale; raw history/numerical/source-normal defects remain separate, `12eps_act<=mu_act`, `3B_perp<=mu_act`, and the exchange/singleton margin is checked on `E_lb`, its complement, and the exact branch. |
| Ledger rate | Raw `nu^2+rnu^3+sqrt(r)e` row and `rnu^3+r^2e` global | E_lb is the only finite-smoothed source of optional nu-squared absorption. |
| Local O.1 rate | `h^4+e h+e^2` on isolated `nu=h` family | Correct at h=0, h much smaller than e, and h=e^2; larger embeddings use nu. |
| Weight/chart scales | `g_minus,g_plus,chi_tau,varrho_PL,mu_PL,L_chart` | `varrho_PL=c_chart g_minus/r`, `mu_PL>=c_mu g_minus^4`, `L_chart=C_L g_plus^4`; all dependencies exposed. |
| Entry rate | `C_ent g_plus sqrt(3r)eta0` and ratio `C_ent sqrt(3)chi_tau r^(3/2)eta0` | Structural and numerical terms vanish as `1/(sqrt(r)log^2r)` and `r^-1/2`; no E_lb or R-acc is used. |
| Probability | Instance at least `1-r^-10`; conditional tape at least `1-r^-10` | Separate statements; E_lb failure allocated only inside instance event. |
| Horizon | Once-drawn event; finite at most r commits; finite tape; terminal `poly(P)log(1/epsilon)` | Ledger finite-budgeted, rollback zero forcing, terminal contraction no additive forcing. |
| Norms/metrics | Column Euclidean, signed-Gram operator, l1/rootwise l2 arrays, full-state Euclidean orbit distance, balanced gradient, Frobenius residual | Every transfer is same-object/same-target; tensor influence is not factor distance. |
| Auxiliary tolerances and public bridge | `e=P^(-D_stat)`, `D_gap,D_cap,D_ls,D_PL`, `theta_ent`, `C_path_star`, and `epsilon` | Every dropped or absorbed term is tied to `(SE-raw)`, the named `E_lb` branch, the chart-entry inequality, or the PL/Armijo recurrence; the public specialization must restate these choices and probability conversions. |
| Accuracy | Every `0<epsilon<1`, no additive remainder | Terminal iterations depend logarithmically on epsilon after entry. |
| Runtime | Polynomial discovery independent of epsilon; polynomial terminal prefactor times `log(1/epsilon)` | Final step must count bracketing, curvature, failed probes, tape, and descent before substitutions. |
| Baseline | Exact representation, rollback, stationary target, joint-zero unused slots, no floor | Preserved by direct H=e=0 equality and constant-relative orthogonal chart. |

## Scope And Closure Certificate

| Target condition or control | Claim class / theorem role | Declared theorem scope | Local interface or recurrence | Key positive/control term or structural source | Defect/forcing terms | Accumulation behavior / scope compatibility | Closure mechanism | Mechanism source / boundary exclusion | Source-to-claim adequacy | Residual-to-target adequacy | Obligation locality classification | Noncircular closure status | Entry-state / first-update stress result | Closure/dominance/absorption relation | Primitive controls | Generated controls | Boundary stress result | Failure mode if mechanism absent |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_inst` and exact target | Static event, conditioning, baseline source | Once-drawn | Finite intersection of norm, Gram, weight, gap, tensor, guard events | Base norm/Gram reserve and Gaussian tails | Linear, quadratic, normalization, union errors | No trajectory accumulation | Structural lower/upper comparison | Six source primitives; zero tensor has early return | Exact conditioning content for realized source | Produced A/B/C/T are exact consumed objects | step-local | Primitives -> `step_001` -> consumers | Empty state and Qstar immediately valid | Each deviation stays below reserve | Six source IDs | Realized event outputs | Minimum norm, exact target, Gram boundary | Without reserves later geometry can fail. |
| `E_lb` | Derived optional absorption source | Once-drawn finite-smoothed event | Failure at most `r^-20`, `nu>=P^-D_lb` | Conditional Gaussian variance of raw pair inner product | Small-ball and normalization | No trajectory accumulation | Source exclusion/structural comparison | Gaussian smoothing and partner norm event; exact branch separate | Supplies only the lower-scale claim needed | Same exact normalized Gram defines nu | step-local | Gaussian draw -> `step_001` -> branch consumers | First profile sees E_lb before absorption | D_stat slack yields `r^2e,sqrt(r)e<=cnu^2` | Smoothing, polynomial ranges | E_lb | Tiny-nu complement raw; H=e=0 equality | Without E_lb finite e cannot be absorbed into tiny nu. |
| `B_hist,F`, `(Res)`, `B_perp`, `(SE-perp)`, `(SE-raw)`, `(SE-lb)` | Source support, residual transfer, capture/curvature specialization | Every finite protected prefix before its current landscape call | `(Nor)` triangular system with unit diagonal; `norm(b_t)<=e Lambda_nor`, coefficient row sum `q_nor<=1/4`; `norm(R_I)>=sqrt(15/16)tau_min-B_hist,F>=c_R tau_min`; `B_perp=3r Lambda_nor e/(1-q_nor)` | Unit diagonal and pair-Khatri--Rao gap drive the normal solve; restricted triple-Gram reserve drives `(Res)` | Numerical forcing is `e Lambda_nor`; accepted-factor reserve is `C_E r tau_max eta0`; no structural normal forcing is hidden | Finite triangular recurrence; forcing has finite `r e Lambda_nor` budget, `(SE-raw)` is valid on every finite branch and `(SE-lb)` only on `E_lb` | Triangular Neumann inversion, projector/nonexpansive transfer, reverse-triangle reserve, structural lower/upper comparison | Primitive `E_inst` gaps plus earlier accepted `step_005` matching/R-euc/post-fit records; empty prefix and exact branch exclude boundary failure; no finite-e eta absorption on `E_lb^c` | Pair-Khatri--Rao unit diagonal is adequate for source-normal recurrence; restricted triple Gram is adequate for positive residual reserve | Produced `R_I` and consumed `Pi_star R_I` have the same target and ambient metric; target-frame contractions are equal; `B_perp` dominates capture/curvature scales in `(SE-raw)` and tau-min eta scale only in `(SE-lb)` | step-local | Empty prefix -> prior accepted records -> current `step_003b` -> current `step_003c` -> current `step_004`/audit -> next temporal `step_003b`; no circular use of current output | Empty prefix has no normal rows and `B_perp=0`; first update has `a_1=-b_1`; finite `e>0,nu=0` is raw-only; exact `H=e=0` gives equality | `q_nor<=1/4` gives finite inverse; exponent order `D_stat>=D_nor+D_tau+max{1,2D_lb,D_cap/2,D_gap+3D_tau}+10` gives raw and branch comparisons | `assump:modewise-gram-margin`, base/product balance, norm event, fixed protocol bounds | prior matching, `R-euc`, post-fit `b_t`, `E_lb`, `Theta`, `D_stat` | Outside-span rank-one witness is excluded; tiny-nu complement is not overclaimed; exact limit passes | Without the normal source or positive reserve an invisible certificate can pass and capture/curvature transfer fails. |
| H/J/K and q gaps | Static source and positive small gain | Every finite prefix after one event | Transfer norm `<=2(nu+eta0)(1+sqrt(r)nu)`, q at most 1/4 | H column l2 and K l1 | eta and J row padding | Geometric within-state paths | Summable control | G1 and exact transfer; q=1 excluded | Upper/small-gain source matches boundedness | Exact array domain and diagonal | step-local | G1 -> `step_002` -> `step_003` | Empty A gives first resolvent | Fixed slack makes q at most 1/4 | Gram margin, balance | H/J/K/S | H=0 gives q=0; chains retained | Without gap Neumann closure fails. |
| Conditional Theta and local z | Generated boundedness and exact charge input | One legal precommit state | Neumann Theta; z equals Theta plus e off diagonal and zero diagonal | q gap and static/history source | A and numerical padding | Conditional one-state; no row accumulation claimed | Algebraic coupling/summable control | G2 and legal prior A | Source matches array boundedness/definition | Same coordinates consumed by score/J charge | step-local | `step_002` -> `step_003` -> later consumers | Empty A and zero diagonal traced | Series converges; definition precedes use | Static source | A,Theta,z | Tiny e and exact H=e=0 | Without local z charge input is undefined. |
| `Theta_act`, `Dual-stat`, `Dual-singleton`/`Dual-exchange` | Structural support, signed isolation, strict-saddle alternative | One certificate state before label selection | Ordered `(Act-cap)` -> `(Act-rad)` -> projected stationarity; finite normalized tangent case split with `mu_act` | Exact biorthogonality `D_M=M_MG_M^{-1}`, dual energy, positive capture/radial scale, and product-sphere Hessian form | History `Theta_act`, numerical `e`, source-normal `B_perp`, and residual transfer | One finite calculation per prefix; no new recurrence; root-free maximum is computed before any selected root row | Algebraic coupling, structural lower/upper comparison, and explicitly conditional singleton/exchange target | Realized Gram reserve and exact current frame; empty/atom, low/common/straddling/sign/equality/`|U_I|=1` boundaries handled; no outside-span singular-vector assumption | Dual vectors select exact labels (`<d_j,m_l>=delta_jl`), so the source has the required support/sign content | `R_I^parallel` and actual `R_I` share the same target; `B_perp` is dominated by the capture/curvature margin and no transformed object is consumed | step-local | `step_001--003` -> `step_003b` -> `step_003c` -> `step_004`; singleton -> `step_005` | Exact one-mode-correlated atom gives zero false off-label term; empty prefix and exact branch pass | `12eps_act<=mu_act`, `3B_perp<=mu_act`, `g^3mu_act>=4N_I(g)P^-D_gap`; all tangent defects absorbed at the same margin | Gram margin, certificate capture/Hessian tests, protocol exponents | `Theta_I`, `Theta_act`, `B_perp`, dual norms, `eps_act` | Tiny score, missing dual tangent, mixed support, threshold equality, and one-mode atom are explicitly assigned | Without exact dual selectors or normalized tangent family, activation can falsely report a singleton or miss mixed-support curvature. |
| Landscape and matching | Support, sign, membership, calibration | Each legal protected state | G3 projection transfer plus coupled d inequality, singleton/mixed Hessian dichotomy, R-euc/R-dir | Positive score/radial root/angular gap | Static leakage, A, e, mixed support, ambiguity, and `B_perp` | Conditional; cross-prefix use waits for G6 | Algebraic coupling plus structural comparison | Exact score/gradient/Hessian with `(SE-raw)`; `(SE-lb)` only on named branch | Score/curvature have sign/support content and G3 supplies same-target bridge | Scratch compared directly to exact atom; projected target-frame tests are exact | step-local | Prior prefix -> G3 -> G4; G6 supplies prefix | Empty A, positive/zero/negative score, invisible witness, separation boundary | Singleton gap dominates all named defects after raw/branch transfer | Balance/event | Theta, certificate, B_perp | Mixed support and outside-span sectors | Without G3 support transfer label assignment can fail. |
| J update and O.1 charge | Signed transition and local numerical clause | Every abstract success; all h in isolated audit | `Delta A<=2Gamma Upsilon`, honest `h^4+eh+e^2` | Same-J exact baseline and positive eta/hz/zz | Calibration and finite e | One nonnegative charge, later finite sum | Signed cancellation before reserve/ledger | Exact E expansion, local z, no h lower bound | J diagonal supplies required exact identity | Actual E and exact target contraction coincide | step-local | G2/G4 -> `step_006` -> G6 | Empty first transition and h=e^2 pass | Positive charge dominates exact increment | Balance | z,calibration | h=0, h much less than e, cyclic/transitive | Omitting J/e terms recreates prior failures. |
| Quantitative z and global ledger | Generated recurrence/invariant budget | Every order, prefix `s<=r` | Fixed-root z bound; per-p charge `<=C(nu^3+re)`; A update | Rootwise Neumann and exact J cancellation | Structural/numerical absolute terms | Nonnegative finite-budgeted, one count per p/root/test/mode; failures zero | Reserve/ledger under finite scope | G2,G3--G5 and prior prefix | Source matches row/global upper claims | A/Xi are exact history/influence arrays, not factor norm | step-local | Empty base -> z -> charge -> next budget | First diagonal charge included | Sum at most r per-p bounds; E_lb optional | Static event/balance | z,Upsilon,A,Xi | Dense/block, every order, exact limit | Entrywise counting adds invalid multiplicity. |
| Abstract protected invariant | All-prefix generated closure | At most r successes/failures | Valid prefix + G3 bridge + abstract success -> next; abstract rollback -> same | Empty base and G3--G6 producers | Accepted error and normal forcing | Finite induction; failures identity; next G3 consumes only prior audit | Algebraic coupling and exact rollback preservation | Earlier steps; no concrete transaction output | Producers cover every invariant clause and temporal audit | Next transaction consumes exact R_I; projection is analysis-only | step-local | `I=A=empty` -> G3 -> G4 -> G5 -> G6 -> next prefix | First/last commit and failure traced | Each success extends injection/budget and stores next audit | Nine primitives | All prefix outputs | Empty, failure, last root | Circular transaction dependence would invalidate flow. |
| Transaction/tape | Reachability, rollback, probability | Finite `N_tx,H_tx` | Per-seed p_tx, stopped tail to r successes | Radial root, angular descent/curvature, Haar, tape | Guard/zero-score/exhaustion | Failures zero; successes finite | Stopping/conditioning argument | Protocol and random-tape primitive | Sources match algorithm/probability claims | Success actual state; failure exact prior state | step-local | G6 -> steps 010--012 | Empty scratch, sign flip, zero score | `N_tx=C r p^-1 log r` | Random tape | Invariant/certificate | Missing root, guard, rollback | Without activation/rollback theorem fails. |
| Exact-target PL geometry | Coercivity, radius, quadratic growth | Local deterministic full-state chart | Jacobian floor, Taylor defects, PL, L_chart | Pair/triple gaps and positive weights | Gauge, cross terms, residual Hessian | No trajectory accumulation | Structural lower/upper comparison | G1 and direct balanced calculus | Source has lower-conditioning content | Exact Qstar/full-state/Frobenius objects | step-local | G1 -> `step_013` before entry | Qstar stationary with active floor | Radius makes both defects subcritical | Event/balance | Chart constants | Minimum weight, orbit/chart boundary | Without floor PL can vanish. |
| Same-target entry | Basin membership/discovery bridge | One all-commit tuple | Distance `<=C g_plus sqrt(3r)eta0`, ratio `r^(3/2)eta0` | R-euc, bijection/signs, weight-scaled radius | sqrt(r), g_plus, structural/numerical eta | One-time finite sum | Structural upper comparison | G4/G7 tuple and G8 radius; E_lb not needed | R-euc has exact factor claim class | Actual tuple and exact orbit, same metric; every residual dominated | step-local | Tuple and chart produced independently -> `step_014` | Independent blocks and H=e=0 traced | Gram upper margin/Dstat make ratio below theta | Column/weight/Gram/rank primitives | R-euc,tuple,radius | Max nu, min weight, pair blocks | Without radius/R-euc basin entry is unsupported. |
| First-exit terminal descent | Contraction, no-floor accuracy | Finite logarithmic horizon | Geometric F recurrence and total path bound | PL, L_chart, strict entry reserve | Retraction/Taylor/line-search defects | Contractive, zero additive forcing | Self-contraction and stopping argument | G8/G9 and exact Armijo | PL supplies signed descent/coercivity | Exact F residual is final target | step-local | Entry -> `step_015` -> specialization | Exact target stationary; first nonzero step active | Path reserve prevents exit; F reaches epsilon | Accuracy primitive | Chart/entry | epsilon to zero, chart boundary | Without reserve local PL cannot be consumed. |
| Exact baseline | Exact recovery/no floor | Deterministic H=e=0 branch | z=charge=A=Xi=0; Qstar fixed | Exact CP identities and rollback | None | No accumulation | Algebraic equality | Exact target setting | Equality has exact support/sign content | Produced and consumed target identical | step-local | Exact branch -> entry/descent/rate | First failed rollback and stationary target | All defects vanish identically | Exact identities | None | Empty/first/stationary states | Finite-e surrogate would violate baseline. |

Every unresolved row is `step-local` and has a concrete mechanism-specific
control relation. The certificate therefore supports `PARTIAL_BLOCKED`; it
does not constitute proof evidence.

## Exported Interface Feasibility

| Exported interface or output target | Producer block/step/source | Raw controls available before export | Defect terms and controlled/uncontrolled classes | Residual-to-target adequacy | Dominance, transfer, simplification, or absorption relation | Margin, threshold, or slack source | Consumers | Feasibility status or blocker |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_inst,E_lb,Qstar` | G1 / `step_001` | Six source IDs, Gaussian tails/small ball | Norm/Gram/normalization errors controlled; lower-tail failure charged | Exact realized target, no surrogate | Errors fit reserves; E_lb only supports later e absorption | Norm floor, nu_star, gaps, probability allocation | G2,G5,G7,G9 | Valid diagnostic interface; proof constants pending. |
| H/J/K/S and transfer gaps | G2 / `step_002` | Signed-Gram columns, Cauchy--Schwarz, exact map | Absolute rows, eta and J padding explicit | Exact array domain/diagonal | Displayed transfer norm gives q at most 1/4 | Primitive Gram slack | G2--G5,G7 | Valid. |
| Conditional Theta/local z | G2 / `step_003` | S, legal A, e, q gaps | Static/history/numerical entries separated; no row bound yet | Exact score/J-charge coordinates | Neumann factor plus explicit zero-diagonal definition | q margin and empty base | G3,G4,G5 | Valid conditional interface. |
| Full-history source-normal bridge and residual reserve | G3 / `step_003b` | Source spans, pair/triple gaps, prior matching/R-euc/post-fit records, `Theta`, `E_lb`, `D_stat` | `e Lambda_nor` normal forcing, finite accepted-history reserve, and `B_perp` are separated; no structural normal term is treated as forcing | `R_I` and `Pi_star R_I` share the exact target; target-frame contractions are equal and `(SE-raw)` dominates capture/curvature scales | Triangular inverse with `q_nor<=1/4`; `(SE-raw)` unconditional; `(SE-lb)` only on `E_lb`; exact H=e=0 equality | Unit diagonal, reverse-triangle reserve, fixed exponent order, `D_gap,D_cap` thresholds | G4 current call, G4/G6 audit path, G7, G10 | Valid diagnostic interface; post-fit normal algebra and projector telescoping remain step-local. |
| Dual/current-frame activation alternative | G4 / `step_003c` | Certificate capture/radial scale, realized `G_M`, exact dual selectors, complete `Theta_I` and root-free `Theta_act`, `(SE-raw)` | History/numerical/source-normal defects are isolated in `eps_act`; first-order Gram mixing remains exact in `x=Galpha` | Produced singleton/exchange witness and consumed actual residual use the same realized target; `B_perp` is the only transfer residual | Ordered radial scale, exact `Dual-stat`, normalized tangent enumeration, and `12eps_act,3B_perp,g^3mu_act` margins | `s_low,beta_act,delta_act,mu_act,D_gap` and upper Gram reserve | `step_004`; singleton branch -> `step_005` | Valid diagnostic interface; local dual/tangent inequalities remain step-local. |
| Matching/calibration | G4 / steps 004--005 | Finished dual/current-frame witness, radial/capture/separation, and G3 transfer | Mixed support is discharged upstream; history, e, guard, and normal residual remain explicit | Actual slot to exact atom in factor metric | Singleton/curvature gap plus `(SE-raw)` yields R-euc/R-dir | eta0, gap/capture, separation, branch flag | G3 next audit, G5,G6,G7,G9 | Valid; difficult direct proof pending. |
| J update/O.1 audit | G5 / `step_006` | Exact E, local z, calibration, weights, J/H | Radial/directional and positive e terms retained | Actual E to exact target contraction | Same-J cancellation; honest local scale | J diagonal and eta structural term | G6/local theorem clause | Valid; prior local blocker repaired. |
| Quantitative z/global budgets | G6 / steps 007--009 | Prior prefix, q_row, exact update, rootwise factorization | Structural/numerical classes separate | Exact A/Xi consumers; no factor claim | Per-p `nu^3+re`, at most r; E_lb optional | q_row and finite prefix | Later prefixes,G7,rate audit | Valid interface; high-risk proof pending. |
| Transaction/tape outputs | G7 / steps 010--012 | Exact derivatives, guards, Haar, tape | Failure branches rollback; tail events charged | Actual committed tuple/prior state | p_tx and stopped tail | H_tx,N_tx,independence | G9,G10 | Valid conditional interface. |
| Exact-target chart constants | G8 / `step_013` | Gaps, weights, balanced Jacobian/Taylor | Gauge, cross and residual-Hessian terms | Exact target/full-state/Frobenius conventions | Radius makes defects subcritical; produces L/path coefficient | Jacobian floor, chi_tau, guard/orbit slack | G9,G10 | Valid diagnostic interface. |
| Full-state chart entry | G9 / `step_014` | Tuple, R-euc, weights, nu upper, Dstat, radius | sqrt(r), g_plus, structural and e pieces explicit; R-acc excluded | Exact tuple to Qstar orbit in declared metric | Divide by g_minus/r; both `r^(3/2)` pieces vanish | theta_ent, c_chart, already fixed source margin, final r0 | G10 | Valid and target-scale adequate. |
| Floor-free public rate | G10 / steps 015--016 | PL/quadratic chart, L/path, epsilon, probabilities | Taylor/line-search/substitution distinct from ledger | Final residual exactly against T | Geometric contraction, path reserve, explicit substitutions | mu, step floor, entry reserve | Final theorem | Valid conditional interface; local proof pending. |
| Exact baseline | G10 / direct specialization | H=e=0 exact identities | No finite residual | Equality in exact metrics | All generated defects vanish | Exact representation/rollback | Final clause | Valid and target-preserving. |

## Generated Output Flow

| Generated output or control | Producer block/step/source | Consumer block/step/final use | Dependency path | Provenance class | Flow status |
| --- | --- | --- | --- | --- | --- |
| `E_inst,T,Qstar` | G1 / `step_001` | G2,G3,G4,G6,G8,G9,G10 and theorem event | Primitives -> G1 -> consumers | derived | Valid. |
| `E_lb` | G1 / `step_001` | `step_003b`, steps 004,007--009,016 | Small ball -> G1 -> branch-aware bridge/ledger/rate; raw paths remain outside branch | derived | Valid; only named optional absorption. |
| H/J/K/S/gaps | G2 / `step_002` | steps 003--009 and 013 | G1 -> G2 -> consumers | derived | Valid. |
| Conditional Theta | G2 / `step_003` | `step_003b`, steps 004,005,007 | Static source + legal A -> Theta -> source-normal/landscape/ledger consumers | explicitly conditional | Valid. |
| Local zero-diagonal z | G2 / `step_003` | steps 006,007 | Theta -> exact local definition -> charge/bound | explicitly conditional | Valid; precedes charge. |
| Full-history source-normal bridge and residual reserve | G3 / `step_003b` | current `step_004`, current `step_005` audit, `step_009`, `step_011`, `step_016`, next temporal `step_003b` | static source + prior accepted audit -> triangular normal solve -> projection transfer | derived | Valid; `(SE-raw)` unconditional and `(SE-lb)` branch-qualified. |
| Root-free `Theta_act` and exact dual/current equations | G4 / `step_003c` | `step_004`; only singleton branch reaches `step_005` | `step_002 -> full candidate-indexed Theta -> root-free maximum -> ordered capture/radial scale -> dual stationarity -> finite tangent alternative` | derived | Valid; no selected-label row or hidden support premise is consumed. |
| `Dual-singleton` / `Dual-exchange` activation alternative | G4 / `step_003c` | `step_004` | `step_001/002/003 -> step_003b -> step_003c -> step_004` | derived | Valid; exact atom stress and all support/sign/cardinality boundaries are included. |
| Matching/calibration/injection | G4 / steps 004--005 | next temporal `step_003b`, steps 006,009,011,014 | G3 -> step_003c -> transfer -> matching/audit | derived after prefix bridge | Valid. |
| E/Xi/J charge/update/audit | G5 / `step_006` | steps 007--009 | Local z + matching -> exact expansion | derived | Valid. |
| Quantitative z/J-row | G6 / `step_007` | steps 008,009 | Local z + prior prefix + update -> row bound | derived | Valid. |
| Global/history/R-acc budgets and prefix | G6 / steps 008--009 | later prefixes,G7,rate audit | Empty base -> G3 -> transition -> finite ledger -> invariant -> next G3 | derived | Valid. |
| Concrete transaction outcome | G7 / `step_010` | `step_011` | Abstract invariant -> dynamics -> outcome | derived | Valid; no backward edge to step 009. |
| One-seed probability | G7 / `step_011` | `step_012` | Invariant/outcome -> Haar reachability | derived | Valid. |
| All-commit tuple | G7 / `step_012` | G9 | p_tx/injection/tape -> tuple | derived | Valid. |
| Radius, PL, L_chart, C_path_star | G8 / `step_013` | G9,G10 | Exact event/Qstar -> calculus -> constants | derived | Valid; precedes entry/descent. |
| Full-state chart entry | G9 / `step_014` | G10 | Tuple + R-euc + exact radius -> membership | derived | Valid; R-acc separate. |
| Terminal iterate/public rate and exact baseline | G10 / steps 015--016 | final theorem and baseline clause | Entry -> first exit -> contraction -> specialization; exact H=e=0 equality | derived | Valid interface. |

## Early Obstruction And Repair Plausibility

- **Contract contradiction screen:** None found. The material-partial class,
  exact objective, algorithm, rank, probability modes, full-state/Frobenius
  metrics, runtime, endpoint, and baseline match the setting.
- **Local J screen:** The idea-8 first-transition obstruction remains removed.
  The local profile exists before the charge, and the honest
  `h^4+e h+e^2` scale is retained at `h=e^2` and `h` much smaller than e.
- **Generated-flow screen:** The former z/charge and prefix/transaction cycles
  are absent. The temporal path is `prior step_005 audit -> step_003b ->
  step_003c -> step_004 -> step_005 audit`; within a call, capture/radial
  ordering and root-free `Theta_act` precede the dual support alternative,
  while a selected root row is consumed only by `step_005`. Definition, exact
  expansion, quantitative bound, global sum, abstract invariant, concrete
  dynamics, and tape amplification occur after that path.
- **Source-normal screen:** The new G3 bridge has a unit-diagonal triangular
  normal system, explicit forcing `e Lambda_nor`, coefficient budget
  `q_nor<=1/4`, and projector telescoping. Its reverse-triangle reserve is
  established before capture, and `(SE-raw)` is unconditional. The only
  stronger `(SE-lb)` comparison is explicitly conditioned on the derived
  `E_lb`; finite `e>0,nu=0` is not silently absorbed. The empty prefix and
  first update are traced, so the invisible outside-span witness is excluded
  without a new assumption or changed algorithm.
- **Current-frame activation screen:** The attempt-7 first-order frame defect
  is removed at the accepted sketch interface.  The exact dual identity
  `D_M=M_MG_M^{-1}`, `x=Galpha` passes the one-mode-correlated atom test,
  capture and radial comparability precede `N_I(g)e/t`, and the simultaneous
  root-free `Theta_act` maximum precedes support selection.  The finite
  normalized tangent family covers low score, common support, one- and
  two-mode straddling, signs, threshold equality, all mode permutations, and
  `|U_I|=1`; the remaining work is local algebra under unchanged claims.
- **Ledger screen:** The per-p factorization exposes the exact monomials and
  one-time count needed for `O(rnu^3+r^2e)`. Proving the fixed-root prior
  history estimate is difficult, but the source and unchanged target are
  explicit, so it remains step-local.
- **Chart residual screen:** Passed. The produced tuple and target orbit are
  compared in the same metric. All `3r` blocks, `g_plus`, `g_minus`, and the
  `sqrt(r)` factor are present. The weight-scaled radius turns the old block
  stress ratio into `O(1/(sqrt(r)log^2r))`; R-acc is not misused.
- **PL/source screen:** The exact balanced Jacobian floor has the required
  coercive content. Radius, Taylor defects, quadratic sandwich, PL,
  `L_chart`, and `C_path_star` are produced before their consumers. No cited
  result is asked to provide an unstated radius or entry bridge.
- **Entry/activation screen:** Empty A and local z are defined; positive score
  activates the radial root; `step_003c` first derives capture/radial
  comparability and then uses exact current/dual coordinates, not an
  orthonormal surrogate. The one-mode-correlated atom has zero false
  off-label equation, and low/common/straddling/sign/equality/`|U_I|=1`
  cases have normalized tangents at the same `mu_act` scale. Zero score and
  all guard/exhaustion failures rollback. Qstar is stationary, and nonzero
  terminal entry has active PL unless already accurate.
- **Scope screen:** Ledger accumulation is finite and sign-controlled;
  transaction failures have zero forcing; terminal motion is contractive with
  a finite path budget. No persistent additive defect is hidden.
- **Probability/rate screen:** E_lb costs only instance probability and is not
  needed for chart entry. Tape probability remains conditional and separate.
  Raw terms precede every simplification, and all chart scales are exposed.
- **Baseline screen:** H=e=0 gives zero local z, charge, history, and entry
  residual; rollback and stationary descent preserve the original exact/no-
  floor conclusion rather than a conservative remainder.
- **Same-setting repair plausibility:** All current unresolved work can be
  proved or rejected within its assigned step. No new primitive condition,
  changed dependency/output, new mechanism source, altered metric, or weaker
  conclusion is presently required.

## Global Gaps And Hard Steps

1. **`step_001` (`step-local`): static smoothing and lower-tail event.**
   Required units are norm retention, normalized signed-Gram concentration,
   product-weight transfer, pair/triple Schur gaps, tensor norm/guard, and a
   conditional Gaussian small-ball normalization lemma with full probability
   allocation.
2. **`steps_002--003` (`step-local`): static source and legal local z.**
   Required units are H/K/leakage/S bounds, exact l1 and rootwise transfer
   maps, two Neumann estimates, empty-state specialization, and the precise
   zero-diagonal local array definition.
3. **`step_003b` (`step-local`, high priority): source-normal and residual
   transfer.** Required units are the post-fit radial/angular normal equations,
   the `B_hist,F` reverse-triangle reserve, the `q_nor` row-sum bound,
   triangular inversion, projector telescoping, target-frame equality, and
   the unconditional `(SE-raw)` versus branch-qualified `(SE-lb)` comparisons.
   The temporal proof unit must consume only prior `step_005` audits supplied
   by `step_009`, with the empty prefix as its zero-row base case.
4. **`step_003c` (`step-local`, highest priority): current/dual activation.**
   Required units are the capture-before-denominator ordering, exact
   biorthogonal/current correlation identities, projected source-normal
   exclusion, root-free `Theta_act`, the exact-atom stress, low/common/
   straddling/sign/mode/equality/`|U_I|=1` partition, normalized tangent
   norms, and the common `mu_act` exchange/singleton margin.
5. **`steps_004--005` (`step-local`, high priority): transfer and matching.**
   Required units are same-target score/gradient/Hessian transfer, unchanged
   `mu_act` versus `P^(-D_gap)`, exchange exclusion, singleton isolation,
   radial/diagonal calibration, product-one signs, separation, and injection
   extension with the post-fit audit.
6. **`step_006` (`step-local`, high priority): exact J transition.** Required
   units are E/R-tensor expansions, same-J target subtraction, product
   majorization, J-diag, isolated O.1 two-sided audit at all boundary scales,
   exact increment comparison, larger-embedding convention, and cyclic
   assembly.
7. **`steps_007--009` (`step-local`, high priority): row profile and finite
   ledger.** Required units are a fixed-root prior-history estimate, raw z
   bound, exact per-p charge factorization, diagonal/off-diagonal and numerical
   counting, Xi/history/Theta budgets, E_lb specialization, and strict abstract
   prefix induction.
8. **`steps_010--012` (`step-local`, high priority): transactions and tape.**
   Required units are radial bracketing, guard smoothness, angular Armijo and
   two-sign curvature decrease, work/step floor, Haar activation window,
   history-uniform success, stopped tail, separate probability, and complete
   discovery operation count.
9. **`step_013` (`step-local`, high priority): exact-target local geometry.**
   Required units are the balanced tangent description, Jacobian kernel and
   two-sided singular bounds, finite-orbit alignment, two Taylor-defect
   estimates at `g_minus/r`, quadratic sandwich, PL, compact gradient
   Lipschitz constant, and explicit path coefficient.
10. **`step_014` (`step-local`): full-state entry.** Required units are the
   bijection/sign alignment, cube-root mean-value bound, exact `3r` Euclidean
   sum, weight ratio, structural and numerical `r^(3/2)eta0` comparisons, and
   exact orthogonal equality branch.
11. **`steps_015--016` (`step-local`): terminal and public closure.** Required
   units are exact Armijo decrease, geometric path bound, first-exit
   contradiction, Frobenius stopping, exponent/rank/work substitutions,
   separate instance/tape conversion, hidden-constant declaration, and the
   exact first-transition/stationary baseline proposition.

These are local proof bottlenecks, not missing theorem-level mechanisms. A
future step failure must still be routed at its actual smallest repair level;
this diagnostic does not pre-accept any local lemma.

## Diagnostic Boundary Note

This `global_proof.md` is diagnostic only. It cannot be consumed as proof
evidence, a cited result, an assumption source, or authority to change the
accepted sketch-step claims, dependencies, assumptions, generated-output
interfaces, rates, procedure, metric, or conclusion. Every displayed local
inequality and source application must be independently proved and reviewed
in its assigned `proof_steps/<Step ID>/proof.md` before assembly.

## Suggested Routing

None

Continue to step-level proof work under accepted sketch attempt 8 in dependency
order. Prioritize `step_001`, then `step_002`--`step_003b`, the repaired
`step_003c` activation bridge, and the transfer/matching chain; within the
ledger prioritize `step_006`--`step_009`, and before terminal assembly
independently stress `step_013` and `step_014`, because they carry the exact
coercivity and repaired same-target entry interfaces. The controller may
proceed without an additional checkpoint after sketch review; all unresolved
rows here are `step-local`.
