# Global Proof Review

## Global-Proof Review Identity

- Branch: `perspective_2/idea_9`.
- Sketch attempt: 9.
- Global unit attempt: 1 under accepted sketch attempt 9.
- Binding setting: `perspective_2/idea_9/setting.md`, SHA-256
  `27315acf705cd791f29d341efaa11aae66803d02b9c240fc613cce06cc57c4b2`.
- Reviewed sketch: `perspective_2/idea_9/proof_sketch.md`, SHA-256
  `ec03ae55ed75fb555f0aa2401c28a14a042d6195679afc41b331e92dbe782fb3`.
- Accepted sketch review: `perspective_2/idea_9/proof_sketch_review.md`,
  SHA-256
  `ed7df07285be0ad9f63cd1ee40d2db961982ee24ad93eb0e573c4dccf6c8f1c9`,
  with `Sketch Review Status = ACCEPTED` and retry target `None`.
- Reviewed global diagnostic: `perspective_2/idea_9/global_proof.md`,
  SHA-256
  `7d25278e034c9e033a43cd4d09566408bfe4a04fc9d55aa617166f2f671adfb9`.
- Reviewed global status: `PARTIAL_BLOCKED`.
- Reviewed suggested routing token: `None`.
- Triggering attempt-8 activation review is archival diagnostic lineage only.
  No sketch-8 proof artifact and no `idea_10` artifact is consumed here.

## Global-Proof Review Status

REVISE_GLOBAL

## Smallest Retry Target

/global-proof

## Blocking Issues

1. The diagnostic's identity and attempted claim name sketch attempt 9 and
   `step_003d`, but the whole-proof draft is still the superseded attempt-8
   activation proof. G4c assigns the unresolved singleton/exchange
   alternative to `step_003c`, and G4d jumps directly to `step_004`. It never
   derives or even states the sketch-9 protected tensor `E_I^mat`, the
   unresolved/matched mass split, `(Mat-ex)`, `(Mat-margin)`, `B_mat`,
   `epsilon_all`, or the matched-singleton rejection by `Sep`.
2. The theorem-level block map, sketch-step coverage, dependency audit,
   citation/tool audit, scope certificate, exported-interface table, generated
   output flow, obstruction screen, and hard-step list all repeat the stale
   producer path `step_003c -> step_004`. The coverage table has eighteen
   rows, omits `step_003d`, calls `step_003c` the sole producer of
   `Dual-singleton`/`Dual-exchange`, and gives `step_004` no `step_003d`
   dependency. Thus the diagnostic has no legal sketch-9 producer-consumer
   path for the theorem-critical matched-mass output.
3. The scope-and-closure certificate classifies the old unresolved-only
   activation row as `step-local` without auditing the actual sketch-9 hard
   obligation. In particular, it gives no source-to-claim or
   raw-control-to-exported-interface evidence for the positive term in
   `(Mat-ex)`, no residual decomposition at the `N_I(g)P^(-D_gap)` target
   scale, no raw `E_lb^c` dominance check for `(Mat-margin)`, and no entry
   trace for the protected-mixture state with `W_U=0`. These are diagnostic
   omissions, not proof-step obligations that can be accepted unseen.
4. The final routing prose says to continue under accepted sketch attempt 8,
   contradicting the binding identity, the accepted attempt-9 gate, and the
   controlled `Suggested Routing = None` token.

## Required Repair Bundle

1. Affected artifact: `global_proof.md`, G4 and Assembly. Smallest target:
   `/global-proof`. Preserve accepted sketch attempt 9. Rewrite G4 so
   `step_003c` produces only ordered radial scale, exact dual/current-frame
   equations, all-label stationarity, and normalized tangent identities;
   `step_003d` then produces the protected-error/mass split, unresolved
   structural alternative, matched exchange `(Mat-ex)`, matched-singleton
   rejection, and `(Mat-margin)`; only then may `step_004` transfer the
   finished witness and `step_005` consume an unresolved singleton.
2. Affected sections: theorem-level block map, all nineteen sketch-step rows,
   dependency/assumption audit, citation/tool audit, exported interfaces,
   generated-output flow, global gaps, and Assembly. Smallest target:
   `/global-proof`. Preserve the accepted sketch. Add `step_003d` everywhere
   it is required and replace every stale
   `step_003c -> step_004` edge by
   `step_003c -> step_003d -> step_004`. Update downstream temporal consumers,
   especially `step_009` and `step_011`.
3. Affected sections: quantitative audit, scope-and-closure certificate,
   exported-interface feasibility, obstruction screen, and hard-step
   localization. Smallest target: `/global-proof`. Preserve the accepted
   sketch. Expose the exact setting convention, `E_I^mat` producer, all-label
   energy, `s_U/s_M/W_U` split, tangent family, positive curvature source,
   every defect in `B_mat` and `epsilon_all`, the raw and `E_lb` branches, the
   capture-to-gap exponent comparison, same-target transfer, consumers, and
   the `W_U=0`, matched-singleton, threshold-equality, `|U_I|=1`, and
   `H=e=0` stresses. This must be an obstruction-level feasibility audit, not
   a promise that `step_003d` will prove it later.
4. Affected section: Suggested Routing. Smallest target: `/global-proof`.
   Preserve accepted sketch attempt 9 and replace the stale attempt-8
   instruction. If the rewritten diagnostic exposes a genuine repeated
   `(Mat-ex)`/`(Mat-margin)` activation obstruction, the user-directed route
   is `/proof-sketch`, never a new idea or `idea_10`.

## Contract And Status Audit

The input gate is valid: the supplied hashes match, and the sketch review
accepts the exact attempt-9 sketch. The global diagnostic contains the usual
major sections, a valid controlled status token `PARTIAL_BLOCKED`, a valid
controlled routing token `None`, and a diagnostic-boundary note.

It is nevertheless not contract-valid for acceptance. Its body, tables, and
closing instruction contradict its attempt-9 identity and omit one of the
nineteen accepted steps. `PARTIAL_BLOCKED` with `None` is not justified while
the theorem-critical generated-output path is missing. The diagnostic note
is otherwise correct: none of its displayed relations is proof evidence.

## Claim And Sketch Fidelity Audit

The public theorem paragraph preserves the setting's target-spec
`material_partial` mode, nine primitive assumptions, asymmetric objective,
full-space transactional procedure, strictly subquadratic rank, separate
instance/tape probabilities, full-state and Frobenius metrics, arbitrary
relative accuracy, and exact/noiseless no-floor baseline. The rate and
baseline claims are not weakened.

Fidelity fails in the proof roadmap. Attempt 9 changed the accepted output
targets and dependencies: `step_003c` no longer exports unresolved support,
and `step_003d` is the sole producer of the all-label matched-mass alternative.
The diagnostic silently restores the superseded `step_003c` target and drops
`step_003d`. This can be repaired without changing the setting, goal, sketch,
algorithm, metric, probability mode, dependence, or conclusion.

## Theorem-Level Structure Audit

| Step ID(s) | Diagnostic coverage verdict |
| --- | --- |
| `step_001`, `step_002`, `step_003`, `step_003b` | Present in G1--G3 with the accepted static, resolvent, residual-reserve, and source-normal roles. |
| `step_003c` | Present but assigned the obsolete attempt-8 support/exchange output instead of the accepted all-label coordinate/tangent output. |
| `step_003d` | Missing from G4, block map, coverage, certificate, flow, and hard-step list. |
| `step_004`, `step_005` | Present, but consume a stale `step_003c` witness rather than the required `step_003d` output. |
| `step_006`, `step_007`, `step_008` | Present with the accepted J-transition, row-profile, and one-time-ledger roles. |
| `step_009` | Present, but its induction omits the accepted matched-mass producer and therefore does not close the sketch-9 prefix state. |
| `step_010` | Present with the accepted transaction/rollback role. |
| `step_011` | Present, but its activation input is the stale G4 alternative rather than the completed `step_003d -> step_004` certificate path. |
| `step_012`, `step_013`, `step_014`, `step_015`, `step_016` | Present with the accepted tape, chart, entry, descent, and public-specialization roles. |

Ten named blocks can still support the theorem after a G4 rewrite, but the
current composition covers only eighteen accepted rows and cannot authorize
fresh step work under sketch attempt 9.

## Dependency And Assumption Audit

The static event, lower-tail branch, H/J/K source, conditional `Theta`, local
`z`, source-normal recurrence, ledgers, tape, chart, entry, and terminal
conditions remain derived and are not promoted to primitive assumptions.
`E_lb` remains branch-qualified, and the unconditional raw terms are retained.

The activation path fails provenance. The accepted producer order is

`step_003b -> step_003c -> step_003d -> step_004 -> step_005`,

with prior `step_005` records producing the next `E_I^mat`. The diagnostic
instead exports support from `step_003c`, so `step_004`, `step_005`,
`step_009`, and `step_011` consume an output that is not an accepted
attempt-9 `step_003c` conclusion. This is a global-document mismatch; the
accepted sketch already fixes the required dependency path.

## Citation And Tool Audit

No external paper is used as theorem authority, and the named papers remain
proof-pattern provenance only. The Gaussian, Schur/Gershgorin, Neumann,
triangular-solve, product-sphere, Haar, balanced-Jacobian, and Armijo tools are
mapped to current objects at diagnostic granularity.

The missing theorem-critical tool audit is the direct current-notation
residual-Hessian calculation for `step_003d`. The diagnostic must check that
the exact ambient product-sphere convention and the realized target produce
the stated `(Mat-ex)` interface, including normalized pair/three-mode
tangents, sign maximization, controlled and uncontrolled defect classes, and
the `step_004` conclusion interface. Calling the old `step_003c` tangent
enumeration sufficient does not discharge this distinct matched-residual
calculation.

## Quantitative Dependence Audit

The rank `ceil(C_rank r^(3/2)(log(2r))^2)`, raw row/global rates,
`sqrt(r)e`, `r^2e`, chart radius, PL/smoothness scales, entry ratio,
probabilities, horizons, and `log(1/epsilon)` terminal factor are preserved.
The exact/noiseless specialization remains stated without a floor.

The activation quantitative audit is stale. It records only
`epsilon_act`, `delta_act`, and `mu_act`; it does not expose the accepted
`B_mat`, `epsilon_all`, `mu_mat`, the quadratic scale
`|s_M|^2/(r tau_max)`, or the exact exponent comparison needed to turn
capture into `(Mat-margin)` on `E_lb^c` while using `E_lb` only for its named
simplification. Until those quantities are audited, the claimed polynomial
rate and observable Hessian threshold are not globally closed.

## Scope And Closure Review

The static event, finite triangular source-normal recurrence, finite
one-time ledger, exact rollback, chart entry, and first-exit terminal
contraction have concrete noncircular sources and scope-compatible finite
budgets in the diagnostic. Their entry and exact-baseline traces are adequate
at diagnostic granularity.

The attempt-9 activation certificate does not pass review because it is not
present. The protected tensor must be produced from prior accepted records;
all-label stationarity must precede the finite mass split; `(Mat-ex)` must
have a same-target positive-curvature source and explicit opposing defects;
and `(Mat-margin)` must dominate the actual observable threshold under the
declared raw/branch scope. The protected-mixture entry state `W_U=0` is the
critical first-use trace: the unresolved mechanism is inactive there, so the
matched-residual exchange mechanism must remain nonvacuous. The accepted
sketch already states these interfaces, making this a `/global-proof`
omission rather than evidence of a sketch or theorem-contract failure.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_inst`, `E_lb`, exact target | static event, optional branch, baseline source | Gaussian/current-source identities | primitives -> `step_001` | Exact realized objects and branch role are stated | Same normalization and target | finite event reserves and lower-tail exclusion | norm/Gram/weight/gap controls exposed | exact T, no surrogate | once-drawn, no accumulation | primitives -> `step_001` -> consumers | norm, tiny-nu, exact branches traced | step-local | None |
| H/J/K, `Theta`, local `z` | static/conditional boundedness producers | Schur, Neumann, exact array definition | `steps_001--003` | Correct upper-control interfaces | exact signed-Gram/array conventions | q gaps and two Neumann norms | static/history/numerical terms separated | exact target-frame arrays | one conditional state | forward to ledger/activation consumers | empty and q=0 states traced | step-local | None |
| `(Res)`, `B_perp`, `(SE-raw)/(SE-lb)` | support reserve and same-target transfer | triangular normal solve and projector telescoping | prior audits -> `step_003b` | Positive reserve and normal control match consumers | actual residual/source-span convention | `q_nor<=1/4`, finite inverse, exponent order | normal forcing and branch split exposed | exact decomposition of `R_I` | finite r-row forcing | prior audit -> current bridge -> next audit | empty, finite-e tiny-nu, exact states traced | step-local | None |
| All-label coordinates/tangents | activation input for matched bridge | exact biorthogonality and Gram energy | accepted `step_003c` | Accepted source exists, but diagnostic assigns a different output | same realized factors in sketch; misstated in diagnostic flow | `(Act-rad)`, `(All-stat)`, normalized selectors required | `Theta_act`, raw history, radial scale available in sketch | projected and actual residual split already produced | one finite prefix | `step_003b -> step_003c -> step_003d` required | exact atom and all-label nonempty stress omitted from current G4 allocation | global-document defect | /global-proof |
| `E_I^mat`, mass split, `(Mat-ex)` | matched support/curvature producer | prior protected errors, all-label energy, ambient residual Hessian | accepted `step_003b/003c -> step_003d` | Entire source-to-claim audit is absent from diagnostic | sketch fixes exact realized target/product-sphere convention; diagnostic does not audit it | `s_parallel=s_U+s_M` and curvature lower bound required | `B_mat`, `epsilon_all`, tangent norms and signs omitted | same-target residual bridge stated only in sketch | one finite pair enumeration; no recurrence | prior records -> `E_I^mat` -> `step_003d` -> `step_004` | decisive `W_U=0` protected mixture not traced | global-document defect | /global-proof |
| `(Mat-margin)` and matched-singleton rejection | observable threshold and unresolved-output gate | capture, exponent dominance, `Sep` | accepted `step_003d` | Missing from diagnostic | same metric/interface fixed by sketch | curvature must dominate `8N_I(g)P^-D_gap`; matched p must violate `Sep` | raw `E_lb^c` and branch-qualified terms omitted | exchange must transfer to actual residual before certificate use | finite per-prefix; no hidden matched-mass assumption | `step_003d -> step_004`; unresolved singleton only -> `step_005` | matched singleton, equality, `|U_I|=1`, exact branch omitted | global-document defect | /global-proof |
| Matching, prefix induction, transaction reachability | membership and finite generated closure | transferred attempt-9 alternative, exact rollback | `steps_004--012` | Downstream claims are plausible but currently consume the wrong producer | exact factors/full-space protocol | finite success/failure induction and stopped tape | matching/audit/ledger interfaces otherwise exposed | exact atom/full-state outputs | at most r commits; failures zero forcing | must include `step_003d` before every current `step_004` | empty, rollback, last-root states otherwise traced | global-document defect | /global-proof |
| Exact chart, entry, terminal descent | coercivity, basin, convergence | balanced Jacobian, direct `R-euc`, PL/Armijo | `steps_013--016` | Correct theorem roles and target scale stated | same full-state/Frobenius objects | radius, entry ratio, contraction/path reserve | all weight/r/numerical factors exposed | exact target orbit and residual | finite entry plus contractive horizon | chart -> entry -> descent -> rate | stationary exact target and boundary traced | step-local | None |
| Exact/noiseless baseline | baseline invariance | algebraic equality and rollback | exact branch -> `step_016` | Final equality is stated, but attempt-9 activation trace must be restored | same target and metric | all defects, including `E_I^mat`, must vanish | no finite remainder permitted | exact produced/consumed T | no accumulation | exact branch -> final clause | empty, first-use, stationary states require sketch-9 flow | global-document defect | /global-proof |

## Hard-Step Localization Audit

The G1--G3, G5--G10 local derivations remain plausibly `step-local` under
their unchanged accepted interfaces. This review does not pre-accept them as
proofs. The diagnostic's former `step_003c` support case split is not a valid
attempt-9 hard-step allocation. The all-label coordinate output belongs to
`step_003c`; the protected-error exchange and margin belong to `step_003d`;
transfer belongs to `step_004`; unresolved matching belongs to `step_005`.

Because the accepted sketch already supplies those claims, dependencies,
mechanism sources, raw controls, consumers, and boundary cases, the current
failure is classified as a `global-document defect`. A rewritten diagnostic
must independently establish obstruction-level feasibility before it may
classify `(Mat-ex)` and `(Mat-margin)` as `step-local`.

## Early Idea-Failure Screen

- Contract contradiction: none established; the claimed setting, procedure,
  metric, rate, probability modes, endpoint, and baseline remain unchanged.
- Missing mechanism: the diagnostic omits the accepted matched-residual
  mechanism, but the sketch identifies a same-setting direct source and
  interface. This supports `/global-proof`, not idea failure.
- Source-convention/residual stress: the sketch requires the exact realized
  target and ambient product-sphere Hessian, while the diagnostic never audits
  that calculation. A rewrite must do so before step work.
- Entry-state trace: in the allowed protected-mixture state `W_U=0`, the
  unresolved activation source is inactive. The diagnostic's attempt-8 G4
  therefore does not exclude a false certificate; the missing sketch-9
  matched exchange is precisely the required trace.
- Persistent-defect and scope: no new recurrence is introduced by
  `step_003d`; `B_mat` and `epsilon_all` are finite per-prefix defects, but
  their target-scale dominance is currently unaudited.
- Generated condition: the matched alternative has a legal producer in the
  accepted sketch but none in the diagnostic's flow.
- Dependence/mode and baseline: no need to expose new dependence, change a
  mode, weaken the target, or replace the exact baseline has been shown.
- Same-setting repair plausibility: a faithful rewrite under the accepted
  sketch is sufficient at the current evidence level. If that rewrite
  exposes failure of `(Mat-ex)` or `(Mat-margin)`, the next route is the
  user-directed `/proof-sketch`, never idea generation or `idea_10`.

## Review Rationale

`REVISE_GLOBAL` is the smallest sound status. The exact setting and accepted
sketch are internally bound by valid hashes, and the sketch already contains
the new producer, interface, dependency, quantitative branch split, and
boundary stresses. The reviewed diagnostic merely declares attempt-9
coverage while retaining attempt-8 G4 content and eighteen-step bookkeeping.
Rerunning `/global-proof` can repair every identified issue without changing
the theorem contract or accepted sketch. Step-level proof work must wait for
that faithful diagnostic and a fresh global review.
