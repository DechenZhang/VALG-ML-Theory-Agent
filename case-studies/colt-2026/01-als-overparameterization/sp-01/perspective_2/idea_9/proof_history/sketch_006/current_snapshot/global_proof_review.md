# Global Proof Review

## Global-Proof Review Identity

- Branch: `perspective_2/idea_9`.
- Global unit attempt: 1 under accepted sketch attempt 6.
- Binding setting: `perspective_2/idea_9/setting.md`, SHA-256
  `27315acf705cd791f29d341efaa11aae66803d02b9c240fc613cce06cc57c4b2`,
  target-spec `material_partial` mode.
- Accepted sketch: `perspective_2/idea_9/proof_sketch.md`, SHA-256
  `9d74e0e0e33ce44cc9a48c4dfcbc10c25a89d08f6f1e91012f3b0b809be7c4ef`.
- Accepted sketch review: `perspective_2/idea_9/proof_sketch_review.md`,
  SHA-256
  `0333e1da5da4014de163f34435be48d00e539de71efc317ffd5bc63aaa88af0f`,
  with `Sketch Review Status = ACCEPTED`.
- Reviewed global diagnostic: `perspective_2/idea_9/global_proof.md`,
  SHA-256
  `2afe59ac64816efaf736ac52d4d70f984e31d642f346febc4ad08a233214619e`.
- Reviewed global status: `PARTIAL_BLOCKED`.
- Reviewed suggested routing: `None`.
- Diagnostic architecture: ten theorem-level blocks G1--G10 covering all
  seventeen sketch steps, including `step_003b` between the conditional
  resolvent and the full-space landscape.

## Global-Proof Review Status

ACCEPTED

## Smallest Retry Target

None

## Blocking Issues

None.

## Required Repair Bundle

None.

## Contract And Status Audit

The diagnostic contains the required reviewed-sketch identity, controlled
`PARTIAL_BLOCKED` status, attempted theorem claim, substantive whole-proof
draft, theorem-level block map, complete seventeen-row sketch-step coverage,
dependency and assumption audit, citation and tool audit, quantitative
dependence audit, scope-and-closure certificate, exported-interface table,
generated-output flow, early obstruction and repair-plausibility audit, global
gap list, diagnostic boundary note, and suggested routing.

The status vocabulary is valid. `PARTIAL_BLOCKED` is appropriate for a
diagnostic that leaves substantial local derivations to proof-step workers,
and `Suggested Routing = None` is aligned because every listed unresolved
obligation is classified `step-local`. The scope certificate has 19 fields,
the exported-interface table has 9 fields, and the generated-flow table has 6
fields on every row. The diagnostic explicitly states that it is not proof
evidence, a cited result, an assumption source, or authority to alter the
accepted sketch. No sketch, setting, tracker, or proof artifact is changed by
this review.

## Claim And Sketch Fidelity Audit

The attempted claim preserves the accepted target-spec and its
`material_partial` scope. It quantifies over the same deterministic base
class and keeps exactly the nine primitive assumptions. Realized norms,
Grams, weights, `E_inst`, `E_lb`, histories, labels, profiles, transaction
outcomes, chart membership, and rates remain derived outputs rather than
public assumptions.

The diagnostic retains the exact asymmetric CP least-squares objective,
full-space unprojected transactional discovery, exact rollback, balanced
product-weight source class, rank
`ceil(C_rank r^(3/2)(log(2r))^2)`, separate instance and tape probabilities,
full-state factor and Frobenius metrics, arbitrary relative epsilon, and the
no-floor exact baseline. It does not use `R-acc` as a factor-distance source,
does not turn `Pi_star` into an algorithmic projection, and does not narrow
the theorem beyond the accepted smoothed near-orthogonal class.

The attempt-6 semantic delta is represented faithfully. G3 is the temporal
`step_003b` source-normal and residual-transfer bridge; G4 consumes it before
the current landscape call; the current G4 audit is stored before the next
G3 invocation. The local z definition precedes G5, its norm bound is deferred
to G6, and G8 produces the exact-target chart before G9 entry and G10
descent. The inherited exact orthogonal/noiseless specialization remains an
equality branch.

## Theorem-Level Structure Audit

G1 produces the once-drawn geometry, exact target, and lower-tail branch.
G2 derives H/J/K, both transfer gaps, conditional `Theta`, and local z. G3
derives `(Hist-F)`, `(Res)`, the post-fit normal recurrence, `(SE-perp)`,
unconditional `(SE-raw)`, and branch-qualified `(SE-lb)`. G4 supplies the
conditional landscape, matching, calibration, signs, separation, and audit.
G5 supplies the exact transition and honest J charge. G6 supplies the
rootwise row, one-time global budgets, and abstract finite-prefix invariant.
G7 realizes the invariant with exact rollback and finite tape. G8 derives
exact-target Jacobian coercivity, chart radius, PL, smoothness, and path
constants. G9 proves direct same-target full-state entry. G10 proves
first-exit descent, public rate specialization, probabilities, and baseline.

All seventeen accepted sketch rows appear exactly once in the coverage table.
The only apparent temporal edge is guarded by finite induction: prior
accepted `step_005` records feed the current `step_003b`, while `step_009`
proves that those records exist from the empty prefix and does not consume a
later concrete transaction output. Static and terminal branches remain
forward-only. No block silently drops the repaired `step_003b` interface.

## Dependency And Assumption Audit

The diagnostic classifies the six event-producing assumptions used in G1 and
the remaining three protocol or accuracy assumptions exactly as they appear
in `setting.md`. `E_inst`, `E_lb`, H/J/K/S, gaps, `Theta`, z, matching,
post-fit audits, ledgers, the protected invariant, transaction events, chart
membership, and convergence are all assigned to explicit producers or to
local conditional hypotheses.

The important temporal path is legal:

`step_001/002/003 + prior step_005 audit -> step_003b -> step_004 -> step_005 audit -> next step_003b`.

The empty prefix has no normal rows and `B_perp=0`. `E_lb` is consumed only
for `(SE-lb)` and optional numerical absorption; raw interfaces remain valid
on its complement. `step_009` supplies prior records by induction, and
concrete steps 010--012 consume the completed abstract invariant rather than
feeding their outcomes backward. No generated condition is inserted into an
unconditional theorem assumption.

## Citation And Tool Audit

No external paper is used as a theorem wrapper or proof authority. The two
named tensor papers are explicitly limited to source-span and Khatri-Rao
proof-pattern provenance, with different algorithms and objectives. Their
conclusions are not imported.

The direct and standard tools have current-object mappings and stated
interfaces: Gaussian norm and normalized-Gram concentration act on the exact
smoothed columns; conditional Gaussian density gives the normalized-Gram
lower-tail event; Cauchy--Schwarz, Schur products, Gershgorin, and Neumann
series use the declared signed-Gram and zero-diagonal conventions; projection,
triangular inversion, and rank-one telescoping act on the actual residual;
product-sphere calculus acts on full ambient spheres; multilinear expansion
acts on the exact accepted error; Armijo and approximate curvature act on the
observable exact loss; Haar activation and stopped tails act on the stated
independent tape; and balanced Jacobian/Taylor calculus acts on the exact
target orbit.

For the high-risk Haar row, the diagnostic identifies the full ambient
dimension, the history-uniform activation window, the exact-loss deterministic
flow, and the required polynomial `p_tx` interface. The cap or flow constants
remain a local step-011 calculation, not an unstated theorem assumption. For
the G3 normal row, prior `R-euc` calibration and the `tau_min` term in
`Lambda_nor` provide the scale needed when converting normalized angular
stationarity to the post-fit `b` bound; the exact conversion is correctly
left to step-local algebra. No unresolved source identity, convention
translation, object-target mismatch, or cited-wrapper discharge blocks the
diagnostic.

## Quantitative Dependence Audit

The diagnostic preserves all exposed categories and modes:

- Structural and data variables: `n,r,k,kappa,rho,Gamma_star,P`, realized
  weights, `nu`, `eta0`, and H/J/K/S arrays.
- Numerical and auxiliary scales: `e=P^(-D_stat)`, `h`, `D_lb`, `D_stat`,
  `D_gap`, `D_cap`, `D_ls`, `D_PL`, `theta_ent`, and `C_path_star`.
- Chart scales: `g_minus`, `g_plus`, `chi_tau`, `varrho_PL`, `mu_PL`, and
  `L_chart`, with `C_L` fixed before the path constant.
- Probability and horizon: once-drawn instance probability at least
  `1-r^(-10)`, conditional tape probability at least `1-r^(-10)`, at most r
  successful commits, finite tape, and terminal work logarithmic in
  `1/epsilon`.
- Norm and metric modes: column Euclidean, signed-Gram operator, array l1
  and rootwise l2, full-state Euclidean orbit distance, balanced gradient,
  and tensor Frobenius residual.

The raw row and global ledger terms remain
`nu^2+r nu^3+sqrt(r)e` and `r nu^3+r^2e`. The `E_lb` lower-tail event is the
only finite-smoothed source for optional `nu^2` absorption; the exact branch
uses equality. The public bridge names its auxiliary choices, term
comparisons, probability separation, rank/runtime substitutions, and hidden
constant dependence. No epsilon, order, instance, horizon, or generated
radius is hidden.

## Scope And Closure Review

The global scope certificate is complete and passes independent review. Its
rows cover the static event, lower-tail branch, H/J/K small gain, conditional
Theta/z, G3 source-normal and residual bridge, landscape/matching, J update,
quantitative ledger, abstract invariant, transaction/tape, PL geometry,
same-target entry, first-exit rate, exact baseline, and generated-output
specialization.

- G3 has a concrete triangular recurrence with unit diagonal, forcing
  `e Lambda_nor`, coefficient row sum `q_nor<=1/4`, finite budget
  `r e Lambda_nor`, reverse-triangle reserve `(Res)`, and projector transfer.
  Its `B_perp` comparison is unconditional at capture and curvature scales and
  is `E_lb`-conditioned only at the `tau_min eta0` scale.
- G5/G6 name the nonnegative one-step J charge, rootwise l2 factorization,
  accumulated `r nu^3+r^2e` budget, zero forcing on rollback, and finite
  at-most-r scope. No persistent additive term is hidden.
- G7 names the history-uniform activation source, exact rollback, stopped
  tape tail, and separate conditioning. The activation neighborhood and
  accepted-step floor are concrete outputs for step 011, not assumptions.
- G8/G9 name the balanced Jacobian lower source, Taylor defects, same-target
  residual decomposition, weight-scaled radius, and strict entry reserve.
  G10 supplies a contractive first-exit path and no-floor endpoint.
- The exact baseline row traces empty state, first failure, exact target, and
  stationary terminal behavior. It preserves equality rather than only
  vanishing defects.

Every row is classified `step-local`. The certificate has noncircular
producer paths, source-adequate positive or support mechanisms, explicit
boundary handling, and target-side residual comparisons. The diagnostic does
not claim these local statements are already proved.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E_inst` and exact target | Static event, conditioning, representation, baseline | Gaussian concentration, normalization, direct CP identities | six source assumptions -> G1 / `step_001` | exports the exact realized norms, Grams, weights, `T`, and `Qstar` | normalized-Gram, factor, and Frobenius conventions match the setting | finite union reserves deviations below `nu_star`, pair/triple gaps, and guard margins | norm tails, Gram tails, dimension, smoothing, weight balance, and probability allocation are named | produced `A,B,C,T` are the consumed target objects with no surrogate | once-drawn finite intersection; no trajectory accumulation | primitives -> G1 -> every static, prefix, chart, and rate consumer | G1 / `step_001` -> G2,G3,G4,G6,G8,G9,G10 | minimum norm, exact target, Gram boundary, and zero-tensor early return pass | step-local | None |
| `E_lb` | Derived lower-tail source and optional absorption support | conditional Gaussian density and normalization | smoothing plus norm event -> G1 / `step_001` | supplies only `nu>=P^(-D_lb)` and its stated failure budget | raw pair inner product normalizes to the same `nu_real` used downstream | `P(E_lb^c)<=r^(-20)`; `D_stat` then controls `r^2e`, `sqrt(r)e`, and G3 eta-scale comparison on this branch | variance, arbitrary mean, polynomial parameter ranges, and branch consumers are explicit | exact instance property; no object transfer | once-drawn branch; complement retains raw terms; exact H=e=0 is separate | Gaussian source -> G1 -> G3 and branch-aware G4,G6,G10 | G1 produces `E_lb`; G3 consumes it; G4/G6/G10 consume qualified interfaces | tiny-nu complement is raw-only; exact branch equality passes | step-local | None |
| H/J/K/S and transfer gaps | Static positive small gain and resolvent support | column l2 Cauchy-Schwarz, Schur products, exact transfer map | G1 -> G2 / steps 001--002 | produces R-src, leakage, S, and both q gaps in the exact array domain | entrywise absolute values are used only entrywise; zero diagonal and signed source conventions match | `2(nu+eta0)(1+sqrt(r)nu)<=1/4` gives summable Neumann maps | H columns, K l1, J rows, weights, eta, and Gram reserves are available before export | same root/mode/label arrays are consumed; no target transform | within-state paths are geometrically summable | G1 -> G2 -> G3/G4/G5/G6 | G2 produces controls for `step_003`, `step_003b`, steps 004--009, and 013 | H=0, transitive chain, and q boundary are tested | step-local | None |
| Conditional `Theta` and local z | Generated conditional interface and exact charge input | nonnegative Neumann series plus zero-diagonal definition | G2 and legal conditional A -> `step_003` | exact target-frame `Theta` and local z coordinates are exported before charge | protected residual, root/mode/label order, and J convention match | inverse small gain defines Theta; z equals Theta plus explicit e padding off diagonal and is zero on diagonal | S, A, q gaps, e, and padding convention are named | analysis arrays contract the same target frame; no surrogate target | one conditional state; quantitative row accumulation is deferred | G2 -> `step_003` -> G3/G4/G5/G6 | `step_003` feeds `step_003b`, steps 004--007 | empty A, zero diagonal, tiny e, exact H=e=0 pass | step-local | None |
| G3 source-normal bridge, `(Res)`, `(SE-perp)`, `(SE-raw)`, `(SE-lb)` | Full-space support, residual transfer, capture and curvature interface | post-fit stationarity, pair-Khatri-Rao unit diagonal, matching/R-euc, reverse triangle, projector telescoping | prior accepted G4 audit supplied by G6/`step_009`, plus G1--G2 | exact triangular recurrence and product-spectral/Frobenius source bound match the consumed claim | realized source projectors, asymmetric residual, commit order, and product-sphere metric match; papers are provenance only | `a_t+sum c_qt a_q=-b_t`, `norm(b_t)<=e Lambda_nor`, row sum `q_nor<=1/4`, finite inverse, `(Res)`, then raw versus E_lb branch comparisons | prior R-euc/post-fit records, pair rows, `Lambda_nor`, exponents, `Theta`, and residual reserve are named before export | `R_I=Pi_star R_I+(Id-Pi_star)R_I`; target-frame contractions are exact and normal residual is dominated at every consumed scale | sign-free forcing `r e Lambda_nor`; accepted-error reserve finite; failed probes add no row | empty prefix -> current G3 -> G4 -> audit -> next G3; no current-output self-use | G3 feeds current G4, G6, G7, G10 and next temporal G3 | empty, first commit, finite e with tiny nu, invisible witness, and exact branch pass | step-local | None |
| G4 landscape, matching, calibration, separation | Structural support, label membership, factor error | exact score/radial/angular equations plus G3 transfer and singleton/mixed curvature | G2/G3 -> steps 004--005 | exports unique unresolved label, signs, R-euc/R-dir, separation, and post-fit audit | full ambient spheres, actual residual, balanced factors, and realized target atoms coincide | coupled direction inequality and Hessian dichotomy dominate leakage, history, e, normal, and ambiguity defects | score, capture, Hessian gap, Theta, B_perp, eta, separation, and branch flag are available | accepted slot is compared directly to exact realized atom in factor Euclidean norm | one output per successful prefix; cross-prefix use waits for G6 | G3 -> G4 -> G6 and next G3 | G4 produces matching, calibration, injection, and post-fit audit for G6 and next G3; G4 current outputs feed G5 | empty/positive/zero/negative score, mixed support, separation boundary, and outside-span witness pass | step-local | None |
| G5 J transition and O.1 audit | Signed recurrence and honest numerical local clause | exact multilinear expansion with same-J subtraction and local z | G2/G4 -> `step_006` | exact J update, J diagonal, cyclic forms, and honest `h^4+e h+e^2` output | J(p,p)=1, off-diagonal J=H, zero-diagonal z, and target contractions match | `Delta A<=2 Gamma_star Upsilon`; positive terms dominate exact increment at `h=e^2` | H,J,z,calibration,weights, and e terms are split before export | actual E and target-frame contraction are the consumed objects | nonnegative one-step charge; no later row bound is assumed | G2/G4 -> G5 -> G6 | G5 produces E, Xi, Upsilon, J update, and audit for G6 | h=0, h<<e, h=e^2, cyclic, diagonal, and transitive cases pass | step-local | None |
| G6 quantitative row and global ledger | Finite recurrence, invariant budget, tensor influence | fixed-root Neumann estimate, rootwise l2 factorization, one-time counting | G2--G5 plus strict prior prefix -> steps 007--009 | raw row, J-global, history, R-budget, and R-acc match the setting | same arrays, root blocks, zero diagonal, and commit-order scope are used; no factor conversion | per-p charge `<=C(nu^3+r e)`, row `nu^2+rnu^3+sqrt(r)e`, global `rnu^3+r^2e`; E_lb only simplifies | q_row, local z, exact update, prior budget, finite prefix, and branch flag are explicit | A/Xi/R-acc are exact ledger objects in the consumed frame | nonnegative finite budget; each p/root/test/mode counted once; rollback zero | empty base -> charge -> row -> global -> next prefix | G6 steps 007--009 feed later prefix, transaction, and rate consumers | diagonal charge, dense/block, all orders, last root, tiny-nu raw branch, exact limit pass | step-local | None |
| G6 abstract protected invariant | All-prefix generated closure | finite induction and exact rollback preservation | empty state plus G3--G6 -> `step_009` | carries every theorem-facing protected-state clause | actual protected residual, frozen slots, and joint-zero failures match protocol | valid prefix plus abstract success extends; abstract failure is identity; next G3 consumes prior audit only | all raw and branch-qualified outputs are available before each landscape call | next transaction consumes actual `R_I`; projection is analysis-only | at most r commits; finite normal forcing and ledger; no failed forcing | empty prefix -> ordered G3/G4/G5/G6 -> next prefix | `step_009` produces invariant for G7 and prior records for next G3 | first success, failure, last root, E_lb complement, exact branch pass | step-local | None |
| G7 transaction dynamics, Haar reachability, tape | Reachability, rollback, conditional probability | exact radial/Angular dynamics, guard smoothness, Haar activation, stopped tail | G6 invariant plus `assump:random-tape` -> steps 010--012 | actual certificate, rollback, p_tx, all-commit tuple, and work claims match | full-space Haar, exact loss, balanced slots, and conditional tape mode match | positive root, accepted-step floor, history-uniform `p_tx`, and stopped tail produce r distinct successes | score window, roots, guards, H_tx, N_tx, independence, and injection are named | success is actual state; failure is exact prior state | failures zero; finite successful commits; stopping controls accumulation | G6 -> G7 step_010 -> step_011 -> step_012 | G7 outputs outcome, probability, and tuple for G9/G10 | empty scratch, sign flip, zero score, missing root, guard, exhaustion, rollback pass | step-local | None |
| G8 exact-target PL geometry | Coercivity, radius, quadratic growth, smoothness | balanced Jacobian lower bound and direct Taylor/derivative calculus | G1 exact target and gaps -> `step_013` | lower singular value and PL have the exact target claim class | balanced tangent, finite target orbit, full-state factor metric, and Frobenius loss match | radius `c_chart g_minus/r` makes Jacobian and residual-Hessian defects subcritical; derive `L_chart` and path constant | gaps, weights, chi, orbit/guard slack, and fixed derivative constants are explicit | produced chart is centered on exact Qstar, no entry premise or surrogate | local deterministic geometry, no trajectory accumulation | G1 -> G8 -> G9/G10 | G8 produces radius, PL, smoothness, path constants for G9/G10 | Qstar stationary, minimum weight, orbit, chart boundary, exact branch pass | step-local | None |
| G9 same-target full-state entry | Basin membership and discovery-to-terminal bridge | bijection/signs, R-euc, cube-root comparison, weight-scaled chart | G4/G7 tuple plus G8 radius -> `step_014` | exact factor-distance membership claim is exported | actual tuple and Qstar orbit share full-state metric; unused slots agree | distance `<=C_ent g_plus sqrt(3r)eta`; ratio to radius exposes `r^(3/2)eta` and strict reserve | tuple, R-euc, weights, nu upper, Dstat, theta, radius, and every 3r residual are named | direct same-target residual decomposition dominates consumed chart scale; R-acc excluded | one-time finite sum, no terminal forcing yet | G9 produces exact chart membership for G10 from the G7 tuple, G4 R-euc records, and G8 radius | tuple and chart paths -> G9 -> G10 | independent block stress, max nu, min weight, and H=e=0 equality pass | step-local | None |
| G10 terminal descent and public rate | Contraction, arbitrary epsilon, no floor, runtime/probability | PL, smoothness, strict entry reserve, exact Armijo, direct specialization | G8/G9 and protocol/accuracy primitives -> steps 015--016 | final Frobenius residual and work/probability claims match goal | same exact target and active-slot metric persist | geometric loss recurrence plus path bound gives first-exit invariance and epsilon stopping; substitutions preserve raw branches | mu, L, path, entry, epsilon, rank, horizons, events, and constants are exposed | final residual is exactly `T-Psi(Q)`; no additive or surrogate term | finite logarithmic horizon; no persistent discovery forcing | entry -> G10 -> final theorem | G10 produces iterate, public rate, probabilities, and baseline | exact target stationary, first exit, epsilon -> 0, chart boundary pass | step-local | None |
| Exact baseline and generated-output flow | Exact recovery/no-floor specialization and dependency closure | direct H=e=0 identities, exact rollback, ordered producer tables | setting baseline plus G1--G10 flow | inherited exact representation, stationarity, rollback, and no-floor conclusion preserved | exact target, factor/tensor metrics, joint-zero slots, and branch conventions coincide | all generated defects vanish; every output has a producer before its consumers | event, bridge, ledger, tuple, chart, entry, rate, and baseline paths are explicit | produced and consumed baseline object is identical; no surrogate transfer | exact equality; finite branch retains raw or qualified terms | exact branch -> entry -> stationary descent -> rate; flow is forward-only | all generated outputs and consumers listed, including next G3 audit | empty, first failure, exact target, stationary, epsilon range pass | step-local | None |

## Hard-Step Localization Audit

- G1 and `step_001` are `step-local`: primitive Gaussian tails,
  normalization, pair/triple gaps, exact representation, and the lower-tail
  event provide the required source interfaces.
- G2 and `steps_002--003` are `step-local`: the exact H/J/K arrays, two
  induced norms, Neumann maps, empty state, and zero-diagonal z definition are
  fixed before their consumers.
- G3 and `step_003b` are `step-local` but highest priority. Prior accepted
  matching/R-euc/post-fit records, the pair-Khatri-Rao unit diagonal, the
  restricted triple-Gram reserve, and the explicit exponent order provide a
  source-adequate normal, reserve, and projection interface. The conversion
  from normalized angular stationarity to `b_t` uses the prior calibration
  scale in `Lambda_nor`; it is a local algebra check, not a new assumption.
- G4 and `steps_004--005` are `step-local` and high risk: exact score,
  radial, angular, and Hessian equations plus G3 transfer provide the support,
  matching, and calibration mechanism.
- G5 and `step_006` are `step-local`: same-J cancellation, diagonal identity,
  positive numerical terms, exact increment comparison, and cyclic forms are
  fixed accepted interfaces.
- G6 and `steps_007--009` are `step-local` and high risk: rootwise l2
  factorization, finite one-time charge, E_lb split, rollback identity, and
  abstract induction provide the closure mechanism.
- G7 and `steps_010--012` are `step-local`: exact guarded dynamics, Haar
  activation source, independent tape, stopped tail, and rollback equality
  support the conditional reachability claim. The activation window and
  accepted-step constants remain to be calculated locally, but no new
  initialization or probability mode is required.
- G8 and `step_013` are `step-local` and high risk: the exact balanced
  Jacobian floor, Taylor defects, radius, PL, smoothness, and path coefficient
  have direct current-notation sources.
- G9 and `step_014` are `step-local`: the all-commit tuple, R-euc, exact orbit,
  cube-root comparison, full-state metric, and weight-scaled dominance give a
  target-preserving entry bridge.
- G10 and `steps_015--016` are `step-local`: PL and Armijo supply signed
  descent, the entry reserve supplies first-exit closure, and direct
  substitutions supply public work, probability, and baseline statements.

Every unresolved theorem-critical obligation in the scope certificate is
classified `step-local`. No diagnostic row is a `sketch/interface defect` or
an `idea/theorem-contract defect`; no changed dependency, assumption,
generated-output interface, metric, mode, exposed dependence, success
criterion, or conclusion is needed before step work.

## Early Idea-Failure Screen

- Contract contradiction: None. The global claim remains within the accepted
  material-partial setting, procedure, rank, modes, metrics, probability
  statements, and endpoint.
- Missing-mechanism check: None at theorem-diagnostic granularity. G3 has a
  unit-diagonal normal source and pair-Khatri-Rao gap; G5 has exact same-J
  cancellation; G6 has a finite nonnegative ledger; G8 has a genuine
  Jacobian lower source; G7 has exact dynamics and an independent tape; G10
  has PL and Armijo.
- Source-convention and object-target stress: Passed. Direct tools use the
  exact normalized factors, actual residual, full ambient spheres, balanced
  target orbit, full-state factor metric, and Frobenius tensor metric. The
  external papers are not used to transfer a differently normalized theorem.
- Residual-to-target stress: Passed. G3 decomposes the actual residual into
  the exact target-span projection and `B_perp` normal remainder; target-frame
  contractions are equal. G9 separately compares the actual tuple with the
  exact orbit and retains all radial, direction, weight, and sqrt(r) terms.
- Persistent-defect and accumulation stress: Passed. Normal forcing is finite
  over at most r accepted slots, J charges are nonnegative and counted once,
  failures are exact identity transitions, and terminal descent has no
  additive forcing.
- Limiting and boundary stress: Passed for the empty prefix, first commit,
  finite `e>0,nu=0` raw branch, `E_lb` complement, tiny positive nu, O.1
  `h=e^2`, dense and block frames, transitive paths, zero score, guard and
  tape failures, minimum weights, chart boundary, epsilon tending to zero,
  and exact `H=e=0` behavior.
- Entry-state trace: Passed. Empty z and A are legal, positive score enables
  radial activation, unsuccessful branches rollback, the first normal row is
  numerical only, and Qstar is stationary with an active PL source unless
  the target is already accurate.
- Generated-condition and flow stress: Passed. Every event, profile,
  invariant, transaction outcome, tuple, chart constant, entry certificate,
  rate, and baseline output has a producer, later consumers, and a dependency
  path. The prior `step_003b` consumer-list omission is not repeated.
- Dependence and mode stress: Passed. Raw numerical terms precede branch
  simplification, instance and tape probabilities remain separate, discovery
  is finite and epsilon-free, terminal work is logarithmic in epsilon, and
  all public metric and hidden-constant declarations are retained.
- Baseline invariance: Passed. Exact representation, rollback, stationarity,
  joint-zero unused slots, arbitrary relative accuracy, and no floor are
  preserved by equality, not replaced by a weaker remainder.
- Same-setting repair plausibility: No repair is currently required. The
  remaining work can be proved or falsified inside the fixed step interfaces;
  supplying it does not require changing primitive assumptions, algorithm,
  theorem scope, metric, exposed dependence, success criterion, or baseline.

The early screen therefore finds no hidden sketch-level or theorem-contract
obstruction behind the diagnostic's `PARTIAL_BLOCKED` status.

## Review Rationale

The global diagnostic is safe to pass to step-level workers. It is complete,
faithful to the accepted sketch and setting, and independently exposes the
repaired source-normal and residual-transfer interface. In particular, G3
establishes the temporal producer path, the positive `(Res)` reserve,
unconditional `(SE-raw)`, and `E_lb`-only `(SE-lb)`; G5/G6 preserve the honest
J numerical terms and finite raw ledger; and G8/G9 use the exact target orbit
and full-state metric for PL entry. The generated-output flow and all 17
sketch-step mappings are legal, and every scope-certificate row has a
concrete source, accumulation relation, boundary trace, and `step-local`
classification.

`ACCEPTED` with `Smallest Retry Target = None` is the smallest sound route.
This accepts only the diagnostic boundary and its `PARTIAL_BLOCKED`
classification; it does not treat any displayed inequality as proof evidence
or preclude a later proof-step review from routing a failed local derivation.
