# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Reviewed artifact: `perspective_1/idea_2/proof_sketch.md`
- Goal mode: exact-goal
- Reviewed roadmap: fixed one-mode conservation, conditional Haar projection margin, exact orthogonal-complement witness, separate cALS and cGD scalar-limit arguments, explicit constrained-only scope boundary, and joint probability/limit assembly.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch targets the exact theorem in `setting.md`, including the quantifiers over arbitrary deterministic bases, the window \(r<k\le r^{5/4}\), \(n\ge 8r^{5/4}\), the shared smoothed tensor, independent method initializations, joint probability \(1/4\), and objective fraction \(3/8\). It consistently labels the result `material_partial` and expressly leaves unconstrained ALS/GD open.
- Dependency audit: The seven-step graph is acyclic and all table dependencies point backward. `step_001` produces the Haar/fixed-span interfaces; `step_002` and `step_003` separately produce the probability margin and deterministic objective floor; `step_004` and `step_005` independently produce the two scalar limits; `step_006` certifies the scope boundary; and `step_007` consumes the positive theorem-facing outputs. The cGD all-time membership statement is correctly conditional on defined iterates, with `step_005` co-consumed at assembly to supply existence of every finite iterate.
- High-risk coverage: The sketch localizes and supplies obstruction-level witnesses for full-rank/Haar initialization, exact fixed-span preservation, the conditional projection event, the exact fixed-witness bridge, singular-design cALS descent, pointwise-smooth Armijo termination, all-time-to-limit conversion, and the non-transfer to unconstrained dynamics. Boundary cases are explicitly traced.
- Explicit-rate coverage: Objectives A and B expose \(r,n,k,t\), the constants \(2,1/2,3/4,3/8\), probability and horizon modes, the Frobenius/objective norms, fixed quantities, and allowed hidden dependence. The only simplification is the displayed implication \(k/n\le1/8\Rightarrow1-2k/n\ge3/4\); no term or probability loss is hidden.
- Assumption and citation plausibility: Every theorem-facing technical assumption is cited by a stable `assump:<slug>` id where used. Haar/full-rank and trajectory membership are derived outputs rather than public assumptions. No theorem-critical external citation is invoked; the Gaussian-rank, Haar-isotropy, Moore-Penrose, orthogonal-projection, Taylor, Markov, and monotone-sequence facts are stated as direct current-notation derivations with compatible Euclidean/Frobenius conventions.

## Early Obstruction Audit

- Limiting-case stress: The argument survives \(T=0\), zero orthogonal residual, exact/noiseless specialization, the boundary \(k/n=1/8\), singular or zero Khatri-Rao designs, nonunique block minimizers, zero block drop, zero cGD gradient, zero objective, and arbitrarily large but finite current factors. The normalized witness is omitted when its denominator is zero, while the unnormalized Pythagorean floor remains valid.
- Theorem-critical bridge support: `step_002` exports the residual margin in the same projector and Frobenius norm consumed by `step_003`; `step_003` uses the exact identity
  \[
  T-S_t^M=(I-P_{\mathcal H_M})T+(P_{\mathcal H_M}T-S_t^M)
  \]
  with orthogonal summands; `step_004` and `step_005` export actual-objective scalar limits; and `step_007` passes the all-time floor to those limits without changing object, metric, scope, or probability mode.
- Exported-interface feasibility: All raw-control-to-output paths are explicit. Haar trace \(k\), independence, and Markov export the event; exact \(Q_M\)-parameterization exports zero-leakage membership; Pythagoras exports the actual-objective floor; exact block minimization and accepted Armijo inequalities export monotone objectives; and the tower property exports the unconditional joint probability. Controlled and uncontrolled defect classes are separated, and no threshold is used to absorb an unrelated residual.
- Theorem-critical mechanism witness gate: Passed for every positive theorem-facing block. The key sources are Haar codimension, exact one-mode algebraic conservation, orthogonal decomposition, exact sequential least-squares minimization, and pointwise polynomial smoothness. Each witness identifies its opposing term, control relation, boundary behavior, and producer-consumer path. `step_006` correctly records that the corresponding conservation source is absent outside the constrained theorem contract.
- Entry-state trace stress: At \(t=0\), \(X_0^M\in\mathcal S_M\); the first cALS update is explicitly \(Q_MD_1\), and the first cGD update changes coefficients while retaining \(X_1=Q_MC_1\). A singular first ALS design still has a Moore-Penrose minimizer. A zero first cGD gradient accepts the first trial and gives stationary objective behavior; otherwise a sufficiently small dyadic step gives Armijo descent. Thus every consumed mechanism is active, or the required conclusion is already true, from the first transition.
- Obligation locality classification: Full-rank/Haar identification, exact fixed-span preservation, projection expectation/probability conversion, fixed-witness floor, cALS well-definedness and limit, cGD well-definedness and limit, baseline preservation, constrained-only scope certification, and final joint assembly are all `step-local`. No theorem-critical obligation requires a changed assumption, dependency interface, algorithm, metric, scope, exposed dependence, or success criterion.
- Noncircular closure gate: Passed. Fixed-span membership is produced algebraically before use; projection events are static initialization events; objective monotonicity is derived from the displayed updates rather than assumed; and limit existence uses only nonnegativity plus monotonicity, not the desired positive limit. No generated event contains factor boundedness, convergence, or the theorem conclusion.
- Mechanism-source and boundary stress: The lower-bound source is genuine codimension plus exact orthogonality, not an upper bound masquerading as coercivity. If fixed membership vanished, an outside-span represented component could cancel the witness; `step_006` exhibits this at the first unconstrained update and therefore prevents an invalid scope upgrade.
- Generated-output flow: Every theorem-facing generated output has a legal producer, listed consumers, a final use, and an earlier-to-later dependency path. In particular, `step_001` produces membership for every defined iterate, `step_005` produces existence of all finite cGD iterates, and `step_007` co-consumes both interfaces rather than using either circularly.
- Source-to-claim adequacy: For fixed \(T\), \(\mathbb E P_{\mathcal S_M}=(k/n)I\) gives exactly the tensor projection expectation after mode-1 matricization. The Moore-Penrose formulas solve the exact current block problems even at rank deficiency. The compact-segment Hessian bound applies to the exact coefficient objective \(f_Q=F(QC,Y,Z)\). All direct sources use the branch's Euclidean coordinates, Frobenius norm, sequential cALS convention, and dyadic Armijo convention; there is no transformed or surrogate target.
- Residual-to-target adequacy: The produced residual \(R_M=(I-P_{\mathcal H_M})T\) and consumed residual \(T-S_t^M\) satisfy an exact same-target decomposition in the theorem norm. The remaining in-subspace residual is orthogonal and nonnegative after squaring. The cALS matricized residual and cGD coefficient objective equal the actual tensor objective, so no empirical, population, reference-operator, transformed-object, or wrong-scale residual remains.
- Baseline invariance audit: The inherited exact/noiseless and zero-defect conclusions are preserved. The projection and witness identities are deterministic in the realized \(T\); at \(T=0\) the target is zero and holds surely; at zero objective, exact block minimization preserves zero and differentiable nonnegativity forces zero cGD gradient. The sketch does not replace these cases by a stopped, finite-scope, or remainder-only surrogate.
- Scope-accumulation compatibility: Fixed-span leakage is identically zero at every update. The all-time objective floor is re-established by the same static identity and has no accumulated defect. cALS sweep drops and accepted cGD drops telescope against the finite potential \(F_M(0)\), while nonnegativity supplies the lower budget. Conditional projection errors are static and do not accumulate over time.
- Scope and dependence consistency: The proof remains uniform over deterministic bases and smoothing realizations after conditioning on \(T\). It makes no unsupported pointwise-to-uniform, conditional-to-unconditional, finite-horizon-to-all-time, or constrained-to-unconstrained upgrade. Taking \(r_0=1\) is compatible with the exact goal, with empty small-\(r\) integer rank windows vacuous.
- Generated-condition provenance: Full rank, Haar law, span membership, projection events, objective monotonicity, line-search termination, and scalar limits are assigned to producer steps. None is moved into a theorem-facing primitive assumption.
- Citation and tool applicability: No theorem-critical cited result needs external statement preflight. Each standard tool is instantiated in current notation with its hypotheses visible: \(k<n\) for Gaussian full rank/Haar dimension, nonnegativity for Markov and monotone convergence, orthogonality for Pythagoras, finite-dimensional Moore-Penrose least squares for cALS, and continuous Hessian on a compact current search segment for Armijo termination.
- Same-setting repair plausibility: No repair is required for the constrained theorem. The only unsupported interface is the unconstrained extension, which is explicitly outside the formalized goal rather than hidden as future local proof work.
- Target-preserving bridge-repair gate: Passed. All bridges needed by the current target are already present. Extending the conclusion to unconstrained ALS/GD would change the algorithmic theorem contract and is correctly not routed as a repair of this sketch.
- High-risk obligation classes: Structural lower bounds, generated invariant maintenance, singular-design least squares, local smoothness without global boundedness, conditional probability conversion, all-time/asymptotic mode conversion, baseline specialization, and constrained/unconstrained scope separation were all tested and passed for the stated theorem.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Almost-sure full-rank Haar spans independent of \(T\) | Static nondegeneracy and random-projector source | iid Gaussian \(G_x^M\), \(k<n\), orthogonal invariance, independent starts | `assump:dimension`, `assump:rank_window`, `assump:joint_initialization`; direct derivation in `step_001` | Full rank gives a \(k\)-plane; invariance gives Haar law; joint independence gives independence from \(T\) and across methods | Exact Euclidean subspace and projector used by the theorem; no whitening, weighting, or surrogate | \(\operatorname{rank}G_x^M=k\) a.s. and \(U\mathcal S_M\stackrel d=\mathcal S_M\) for orthogonal \(U\) | Raw Gaussian law exports \(P_{\mathcal S_M}\) with trace \(k\), Haar law, and required independence | Produced and consumed projector are identical; residual zero | Static probability-one certificate; no iterative defect | Primitive Gaussian draw -> `step_001` before any projector consumer | Producer `step_001`; consumers `step_002`, `step_003`, `step_006`, `step_007` | Dimension boundary still has \(k/n\le1/8<1\); Gaussian rank failure is null | step-local | None |
| All-time fixed-span membership \(S_t^M\in\mathcal H_M\) | Generated invariant enabling the fixed witness | Exact cALS left factor \(Q_MD_{t+1}\) and cGD parameterization \(Q_MC_t\) | Formal algorithm plus `step_001`; cGD iterate existence from `step_005` at final use | Exact algebraic inclusion supplies membership, not merely boundedness or smallness | Same \(Q_M\), tensor subspace, and Frobenius geometry as the witness | \(X_t^M=Q_MD_t\Rightarrow S_t^M\in\mathcal S_M\otimes\mathbb R^n\otimes\mathbb R^n\) | Initialization and every update have the required \(Q_M\)-factor; exported leakage is exactly zero | Produced and consumed subspace are identical; no transfer residual | Zero leakage per update, hence zero accumulated leakage | Initial membership and update formula precede `step_003`; defined-iterate scope is co-closed by `step_005` for cGD | Producer `step_001`; consumers `step_003`, `step_007`; comparison consumer `step_006` | Holds at \(t=0\), first update, singular ALS designs, zero gradient, and \(T=0\) | step-local | None |
| Conditional projection events and joint residual margin | Structural projection bound, confidence conversion, positive codimension source | Haar projector expectation, Markov inequality, conditional independence, tower property | `step_001` plus `assump:dimension`, `assump:rank_window`, `assump:joint_initialization`; direct `step_002` calculation | \(\mathbb E\|P_{\mathcal H_M}T\|_F^2=(k/n)\|T\|_F^2\) directly controls the event threshold needed for the complement margin | Exact mode-1 matricization, Euclidean projector, shared realized \(T\), and Frobenius norm | \(E_M:\|P_HT\|^2\le2(k/n)\|T\|^2\); then \(\|P_{H^\perp}T\|^2\ge(1-2k/n)\|T\|^2\) and conditional joint probability at least \(1/4\) | Trace \(k\), Haar independence, and \(k/n\le1/8\) export the event and \(3/4\) margin; no trajectory condition is needed | Exact complement identity; no omitted residual | Static projected-energy defect, controlled on the event; no accumulation | `step_001` -> `step_002` before `step_007`; event does not assume the target limit | Producer `step_002`; consumer `step_007` | \(T=0\) handled surely without division; equality \(k/n=1/8\) gives exactly \(3/4\) | step-local | None |
| Exact fixed-witness actual-objective floor | Structural lower bound and residual-to-target bridge | Fixed-span membership plus Hilbert-space orthogonal decomposition | Derived `step_001` membership; direct `step_003` Pythagorean calculation | Orthogonality supplies an equality and a positive lower term of the required claim class | Same tensor, projector, subspace, and Frobenius norm; no source/target mismatch | \(\|T-S_t\|^2=\|(I-P_H)T\|^2+\|P_HT-S_t\|^2\) | Zero-leakage membership exports \(F_M(t)\ge\frac12\|(I-P_H)T\|^2\) for every defined \(t\) | Exact decomposition; in-subspace residual is nonnegative after squaring and cannot cancel | No forcing or recurrence; same identity applies at every time | `step_001` -> `step_003` before floor use in `step_007` | Producer `step_003`; consumer `step_007`; boundary comparison `step_006` | At zero orthogonal residual or \(T=0\), normalized witness is omitted and the zero floor remains valid | step-local | None |
| cALS well-definedness, monotonicity, and scalar limit | Exact block minimization, signed descent, asymptotic scalar closure | Moore-Penrose minimum-norm least squares in sequential block order | Formal cALS definition; direct `step_004` derivation | Each displayed formula is an exact minimizer of the current actual block objective, including rank-deficient designs | Matricization preserves the exact tensor Frobenius objective; constrained \(X=QC\) uses orthonormal \(Q\) | Three within-sweep inequalities give \(F_{t+1}\le F_t\); \(\sum(F_t-F_{t+1})\le F_0\); \(F_t\ge0\) | Raw block formulas export a finite chosen minimizer and actual-objective descent; no Gram inverse or conditioning margin is required | No surrogate residual; matrix and tensor objectives coincide | Nonnegative drops telescope against finite \(F_0\); no factor-bound forcing term | Formal update -> `step_004` before limit consumption in `step_007` | Producer `step_004`; consumer `step_007` | Zero/singular Khatri-Rao matrix, nonunique minimizer, zero drop, and zero objective all preserve the claim | step-local | None |
| cGD finite Armijo search, monotonicity, and scalar limit | Iterative well-definedness, signed descent, asymptotic scalar closure | Continuous Hessian of finite-dimensional polynomial \(f_Q\) on each current search segment | Formal cGD definition; direct `step_005` Taylor derivation | Local Hessian control gives exactly the stated Armijo inequality; no global smoothness is claimed | Exact coefficient-space Euclidean gradient and product Frobenius norm specified in the algorithm; \(f_Q=F(QC,Y,Z)\) | For \(\eta\le\min\{1,L_t^{-1}\}\), \(f(u-\eta g)\le f(u)-\eta\|g\|^2/2\); dyadic search reaches such \(\eta\); accepted drops telescope | Finite current iterate, finite gradient, compact segment, and polynomial Hessian export a finite next iterate and actual-objective decrease | Coefficient objective equals consumed tensor objective exactly; no transfer residual | Local Taylor remainder is absorbed one step at a time; total accepted drops are bounded by \(F_0\) | Formal initialization -> `step_005` inductively before `step_007`; no bounded-trajectory assumption | Producer `step_005`; consumers `step_007` and cGD all-time-scope co-use with `step_001` | Zero gradient accepts \(\eta=1\) and is stationary; arbitrarily large finite iterates still have finite local \(L_t\) | step-local | None |
| Exact/noiseless and zero-target baseline preservation | Baseline invariance obligation | Deterministic projection identity, exact update rules, nonnegative squared loss | `step_001` through `step_005`, assembled in `step_007` | The same theorem conclusion, not a weaker remainder, holds under the specialization | Same realized/baseline tensor and theorem metric; no changed reference object | At \(T=0\), target lower bound is zero; at zero objective, cALS preserves the zero block minimum and cGD has zero gradient | Existing raw identities and update rules export the original zero/baseline conclusion without added conditions | No residual at \(T=0\); exact/noiseless nonzero \(T\) retains the same fixed-witness relation | Zero leakage and monotone potential remain valid under specialization | Earlier step producers -> `step_007`; baseline is not assumed as an event | Producers `step_001`-`step_005`; consumer final theorem | First update is stationary at zero objective; normalized witness is not required at zero residual | step-local | None |
| Constrained-only scope and non-transfer certificate | Source-direction fidelity and scope-boundary obligation | Exact first unconstrained \(X\)-update and outside-span residual formulas | Direct `step_006` algebra using constrained interfaces from `step_001`, `step_003` | Correctly shows absence of the fixed conservation source; does not claim generic unconstrained failure | Same initialization projector and actual residual geometry used in the comparison | \((I-P_0)X_1=\eta(I-P_0)T_{(1)}K_0\) for GD and the analogous pseudoinverse expression for ALS; outside-span residual has an uncontrolled subtraction | Raw unconstrained update exports only a non-transfer certificate, not a positive-limit theorem | Transfer to the unconstrained target is explicitly inadequate because \((I-P_H)S_t\) is uncontrolled on the witness scale | Generated outside-span term may be persistent with unknown sign; no accumulation claim is made | `step_001`,`step_003` -> `step_006` before final scope statement | Producer `step_006`; consumer `step_007` scope label | First update can activate outside-span motion; accidental vanishing is not promoted to an invariant | step-local for the stated scope certificate | None |
| Joint probability and two positive scalar-limit bounds | Final theorem closure, probability conversion, and all-time-to-limit specialization | Outputs of `step_002`, `step_003`, `step_004`, `step_005`; scope label from `step_006` | Earlier accepted-target step interfaces under all five stable assumptions | Event margin, actual-objective floor, and existing scalar limits match every conjunct of the exact goal | Same \(T\), methods, objective, Frobenius norm, conditional law, and asymptotic mode as `setting.md` | Joint event implies \(F_M(t)\ge3\|T\|^2/8\) for all \(t\); taking limits preserves it; conditional probability \(\ge1/4\) averages unchanged | Raw margin \(1-2k/n\), half-squared-loss factor, deterministic limits, and conditional product export all constants and quantifiers | No remaining residual, leakage, smoothing-transfer, or probability defect | Static event and zero-defect floor; method drops separately telescope | `step_002`,`step_003`,`step_004`,`step_005`,`step_006` -> `step_007`; no conclusion is consumed before production | Producer `step_007`; consumer final theorem | \(T=0\), equality \(k/n=1/8\), zero drops, zero gradient, and vacuous small-\(r\) windows all remain valid | step-local | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is ready for step-level proof work. Its central obstruction is static and exact, its probability calculation is conditional on the correct shared object, and its two method-specific convergence claims concern only monotone scalar objectives, avoiding unsupported factor boundedness or parameter convergence. Every theorem-critical generated output has a source-adequate, noncircular, boundary-tested producer-consumer path under the unchanged exact-goal contract. The constrained scope is labeled consistently as material partial, and the explicit non-transfer step prevents the result from being mistaken for a solution of the unconstrained source target. Accordingly, no sketch or idea revision is required.
