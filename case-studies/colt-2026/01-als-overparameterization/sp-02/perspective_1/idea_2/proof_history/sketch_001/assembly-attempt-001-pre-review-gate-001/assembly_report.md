# Assembly Report

## Status

COMPLETE

## Attempted Claim

With $r_0=1$, $L(r)=r^{5/4}$, and $\alpha=1/4$, for every admissible $r,n,k$ and every unrestricted deterministic base triple, the joint Gaussian smoothing-and-independent-initialization law for the shared tensor $T$ gives probability at least $1/4$ that both one-mode fixed-span constrained methods have finite scalar objective limits at least $(3/8)\|T\|_F^2$. On the same event, each fixed orthogonal residual has squared energy at least $(3/4)\|T\|_F^2$ and the objective floor holds at every finite iterate. No conclusion is claimed for ordinary unconstrained ALS or full-variable gradient descent.

## Blockers

None

## Notes

All seven current unit-attempt-1 proofs and their matching accepted reviews were verified against sketch attempt 1. The appendix was assembled source-by-source from every substantive local derivation, invoked elementary-result application, boundary case, constant and probability conversion, assumption discharge, and concluding inference. Audit metadata and exact duplicate setup were omitted, but no mathematical proof obligation was replaced by a summary. No external citation is used, so the public bundle retains the canonical bibliography support files while suppressing an empty bibliography section. Tectonic compiled `main.tex` successfully to `<TEMP_ROOT>/p1-assembly-compile/main.pdf`; the final log has no unresolved references, citations, duplicate labels, overfull boxes, or underfull boxes.
