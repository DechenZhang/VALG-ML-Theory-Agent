# Proof Sketch Review

## Reviewed Sketch Identity

- Branch: `perspective_2/idea_1`
- Sketch attempt: 1
- Reviewed artifact: `perspective_2/idea_1/proof_sketch.md`
- Reviewed proof status: `PARTIAL_BLOCKED`
- Goal mode: exact conditional goal
- Roadmap reviewed: candidate `step_001` (initial coefficient margin), candidate `step_002` (certificate-to-span-floor transfer), candidate `step_003` (objective convergence and coefficient-to-ambient transfer), and diagnostic candidate `step_004` (certificate-probability upgrade).

## Sketch Viability Score

3

## Sketch Review Status

IDEA_FAIL

## Smallest Retry Target

/subagent-idea-generator

## Retry Mode

new_idea

## Audit Summary

- Goal alignment: The sketch faithfully targets the formalized conditional event inclusion and correctly keeps the positive-probability certificate claim outside that target. It also preserves the declared `conditional` progress type and the remaining source-level gap. The target is nevertheless quantitatively mis-scoped: the event gives only the fixed absolute margin \(\Delta_0\geq\delta\), whereas the conclusion requires a residual squared norm of order \(\|T\|_F^2\asymp r\) in the exact well-conditioned baseline.
- Dependency audit: The intended graph `step_001 -> step_002 -> step_003` is acyclic, and diagnostic `step_004` is correctly independent. It is not executable: `step_001` cannot export a theorem-scale margin, `step_002` has no source-adequate accumulated span-motion interface, and `step_003` has no source-adequate convergence interface. The sketch correctly declines to mislabel these candidates as dispatchable proof steps, but consequently it has no valid stable-step graph satisfying the sketch artifact gate.
- High-risk coverage: The sketch identifies the right high-risk classes: normalization, adaptive-span rotation, cancellation under the represented-sum map, infinite-horizon accumulation, objective convergence, smoothing residual transfer, exact/noiseless baseline preservation, and conditional-versus-unconditional probability scope. Three theorem-critical outputs have no legal producer under the current contract.
- Explicit-rate coverage: The rate objective lists the exposed variables, hidden-constant restrictions, probability mode, horizon mode, and norms. The proposed transfer \(\delta-C_1(k/r^2)L-C_2\rho\) is at the wrong scale for a relative loss, and a deterministic \(C_2\rho\) control is unsupported for the unbounded Gaussian smoothing realization on the literal event \(\mathsf C\).
- Assumption and citation plausibility: The three clauses of \(\mathsf C\) are legal explicitly conditional hypotheses, not hidden primitive assumptions. Kolda--Bader supports only CP/ALS conventions, and the cited ALS paper supplies regime context; neither supplies the required deterministic transfer or convergence theorem for this half-relaxed simultaneous map. The idea's generic Kurdyka--Lojasiewicz appeal has no identified theorem, descent hypothesis discharge, relative-error interface, or map-specific conclusion and cannot support `step_003`.

## Early Obstruction Audit

- Limiting-case stress: In the coordinate-orthonormal exact/noiseless specialization, \(Q=I\), \(E_\rho=0\), \(T=D_r\), and \(\|T\|_F^2=r\). The requested coefficient estimate yields at best an \(r\)-independent positive norm margin, hence only an absolute \(O(1)\) squared-residual certificate. It cannot be converted into the required \(\epsilon r\) lower bound. This violates the inherited relative-floor baseline obligation before any iterative argument is used.
- Theorem-critical bridge support: The static identity \(Q(T-\widehat T_t)=D_r+E_\rho-P_t\), with \(P_t\in\mathcal S_t\), is valid and target-compatible. The dynamic producer is missing: the certificate controls squared motion of the represented sum, while the consumed object is the adaptive span of its individual coefficient rank-one terms.
- Exported-interface feasibility: The raw controls \(\Delta_0\geq\delta\), conditioned pair Grams, and \(\sum_t\ell_t\leq L\) do not export (i) a \(\sqrt r\)-scale initial margin, (ii) finite first-order component or projector path length, (iii) a convergent objective, or (iv) a coefficient residual dominating both \(E_\rho\) and \(\|T\|_F\). These are distinct defect classes and no single threshold in the present certificate controls them.
- Theorem-critical mechanism witness gate: Failed. For span preservation, the positive source is only a quadratic represented-sum energy budget, while the opposing defect is first-order span rotation plus cancellation directions in the sum map. For convergence, square-summable increments give asymptotic regularity but no signed descent, finite variation, or verified KL descent/relative-error pair. For the relative ambient floor, the valid static coordinate map has neither the required normalized margin nor a realization-level smoothing bound. The first and third failures require a changed theorem-facing conditional interface; the convergence defect would at minimum require a new map-specific sketch interface and otherwise a changed certificate.
- Entry-state trace stress: At a balanced, Gram-one entry, a rank-one coefficient feature can undergo an infinitesimal angular change for which span-distance loss is first order in the angle and represented-tensor squared motion is second order. The current certificate has no source active before that first transition that absorbs the linear term. At a stationary or slowly rotating bounded represented-tensor path, square-summable increments remain compatible with a nonconvergent distance to the target. At the exact/noiseless entry, the fixed-margin scale defect is already present.
- Obligation locality classification: The normalized initial-margin export and smoothing-to-relative-target bridge are `idea/theorem-contract defect` because repair must change the theorem-facing conditional scope/exposed dependence or weaken the conclusion. The squared-energy-to-span and square-energy-to-convergence interfaces are at least `sketch/interface defect` because no current map-specific control relation is stated; if no exact relaxed-ALS relation supplies them, their repair also requires adding unsupported theorem-facing mechanism clauses and is therefore idea-level. The literal consumption of the three clauses of \(\mathsf C\) is legal and step-local, but those clauses are inadequate for the outputs demanded from them.
- Noncircular closure gate: Failed for the all-time coefficient floor and objective limit. Assuming persistent coefficient deficit, finite projector path length, sufficient decrease, finite variation, or convergence would insert precisely the generated outputs that must be proved. The all-time Gram and energy clauses themselves may be assumed only because the formal goal is explicitly conditional; their positive-probability production is not part of this theorem.
- Mechanism-source and boundary stress: For a unit rank-one feature \(v(\theta)\), the sketch's computation gives a linear change of \(\operatorname{dist}_F(D_r,\operatorname{span}\{v(\theta)\})\) but \(\|v(\theta)-v(0)\|_F^2=\theta^2+O(\theta^4)\). Splitting a fixed rotation into many increments makes total quadratic charge vanish while preserving finite first-order rotation. This path is not itself shown to be an exact ALS orbit, so it does not alone disprove every possible map-specific identity; it does prove that the energy clause is not an adequate mechanism source, and the sketch supplies no normal-equation identity that removes the defect or the cancellation kernel.
- Generated-output flow: Conditional membership in \(\mathsf C\) legally feeds its literal clauses to the candidates. The theorem-scale margin, all-time coefficient floor, objective limit, and relative ambient floor have no legal producer-consumer path. Diagnostic certificate probability has no consumer in the reviewed conditional theorem and is correctly left outside the graph.
- Source-to-claim adequacy: Blockwise least-squares normal equations provide residual orthogonality for one old Khatri--Rao design; they do not, as stated, control simultaneous cross-mode span rotation or first-order accumulated projector motion. Bounded balanced factors and pair-Gram conditioning provide compactness, not convergence. A generic analytic/KL label is inadequate without a source identity and the sufficient-decrease and relative-error hypotheses for this exact pseudoinverse Jacobi map. The coordinate convention is compatible for the static bridge, but its quantitative source-to-relative-claim implication fails.
- Residual-to-target adequacy: The produced control is \(\operatorname{dist}_F(D_r,\mathcal S_t)\); the consumed interface is \(\|T-\widehat T_t\|_F/\|T\|_F\). Under \(Q=\Lambda_A\otimes\Lambda_B\otimes\Lambda_C\),
  \[
  \|T-\widehat T_t\|_F\geq \|Q\|_{\mathrm{op}}^{-1}\bigl(\operatorname{dist}_F(D_r,\mathcal S_t)-\|E_\rho\|_F\bigr)_+.
  \]
  Base conditioning bounds \(\|Q\|_{\mathrm{op}}\), so this is a valid same-target, not surrogate, bridge. It is quantitatively inadequate: the available margin is order one, the required residual norm is order \(\sqrt r\) in the baseline, and neither \(\|E_\rho\|_F\) nor the realization-dependent scale \(\|T\|_F\) is controlled on \(\mathsf C\). Base-range-orthogonal residuals do not invalidate the lower bridge, but they do not repair its missing relative normalization.
- Baseline invariance audit: The inherited conclusion is a fixed positive fraction of the loss scale in the exact/noiseless, coordinate-orthonormal case. The produced object and consumed target coincide there, and the first-update issue is unnecessary for seeing the break: \(\|D_r\|_F=\sqrt r\), while the certificate only guarantees a fixed \(\delta\). The original relative conclusion is not preserved by the displayed absolute bridge. Repair requires a normalized deficit clause or a weaker absolute conclusion; either changes the theorem contract, so the route is `/subagent-idea-generator`.
- Scope-accumulation compatibility: For span preservation, the accumulated defect is first-order projector rotation with uncontrolled sign, while the only budget is \(\sum_t\|\widehat T_{t+1}-\widehat T_t\|_F^2\); no one-step absorption, telescoping potential, component-path budget, or stopping relation controls it. For objective convergence, the variation contains \(-2\langle R_t,d_t\rangle\), linear in \(\sqrt{\ell_t}\) with uncontrolled sign; \(\sum_t\ell_t<\infty\) does not make this term summable. Both accumulated closures are unsupported.
- Scope and dependence consistency: The sketch correctly declares deterministic conditional, all-time, and asymptotic modes, but it attempts an unjustified mode transfer from square-summability to convergence and an unjustified scale transfer from a fixed coefficient gap to a relative ambient loss. It also uses an \(O(\rho)\) smoothing residual as though it were realization-uniform despite the unbounded Gaussian law and no smoothing-good-event clause.
- Generated-condition provenance: The all-time Gram and finite-energy conditions are explicitly conditional and therefore legally consumable without a primitive-to-event proof. Persistent span deficit, objective convergence, and the ambient relative floor are derived outputs and may not be folded into \(\mathsf C\) without changing the conditional theorem. The certificate's positive probability is an unresolved source-level generated-event obligation, not a hidden premise of the reviewed theorem.
- Citation and tool applicability: Kolda--Bader's CP matricization and pseudoinverse conventions are compatible but do not output the theorem-critical dynamic interfaces. The ALS source is not cited with a theorem label supplying half-relaxed simultaneous descent, span stability, finite length, or the certificate event, and the sketch expressly says it supplies only context. No theorem-critical citation has a valid hypothesis-to-conclusion discharge for the missing outputs.
- Same-setting repair plausibility: A revised sketch could test an exact map-specific descent or component-motion identity, but it cannot repair the fixed-deficit normalization and uncontrolled smoothing-to-relative-target transfer while keeping the same formalized event and conclusion. Those repairs require changing the theorem-facing conditional interface, probability scope, exposed normalization, or success criterion. Therefore `REVISE_SKETCH` is not the deepest sound route.
- Target-preserving bridge-repair gate: Adding a static bridge step is insufficient because the existing static bridge is already exact. The missing inputs are theorem-scale margin and target-scale smoothing control. Adding \(\Delta_0\geq\delta\sqrt r\), a normalized version of that clause, or a smoothing-good-event condition changes \(\mathsf C\); changing the conclusion to an absolute floor weakens the success criterion. No roadmap-only repair preserves both the current setting and goal.
- High-risk obligation classes: Structural lower-bound scale, adaptive-span stability, cancellation/identifiability of component motion, simultaneous-update perturbation terms, all-time accumulation, convergence mode, unbounded smoothing residual, relative normalization, exact/noiseless baseline invariance, and certificate probability were all audited. The probability gap is nonblocking only because it is explicitly outside the conditional theorem.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Theorem-scale initial coefficient margin | Structural lower bound and quantitative specialization feeding the relative floor | Literal conditional clause \(\Delta_0\geq\delta\) | Explicitly conditional target | Matches an absolute gap but not the required \(\Theta(\sqrt r)\) residual norm | Same \(D_r\), \(\mathcal S_0\), and coefficient Frobenius convention | Only \(\Delta_0\geq\delta\); no normalization relation | Raw fixed \(\delta\) cannot export \(\Delta_0/\|D_r\|_F\geq c\) | No object mismatch, but the residual is wrong-scale because \(\|D_r\|_F=\sqrt r\) | Entry-only; scale defect precedes accumulation | Event clause -> candidate `step_001`; noncircular but inadequate | \(\mathsf C\) -> `step_001` -> blocked `step_002` and `step_003` | Exact/noiseless coordinate baseline gives only fixed absolute margin against target norm \(\sqrt r\) | `idea/theorem-contract defect` | `/subagent-idea-generator` |
| All-time adaptive coefficient-span floor | Structural lower-bound preservation, recurrence, and horizon-uniform closure | Gram clauses, squared represented-sum energy, and block normal equations | Explicitly conditional controls plus direct derivation candidate | Pair-Gram conditioning controls block designs; squared sum motion does not control first-order component-span rotation or cancellation kernel | Coefficient and ambient conventions are fixed, but the source controls a different dynamic object | Required relation would charge projector/span motion to \(\ell_t\); infinitesimal rotation is linear while \(\ell_t\) is quadratic | Raw Gram bounds and \(\sum_t\ell_t\leq L\) do not export finite projector path length | Produced sum-motion control has no residual decomposition controlling component reparameterization and cancellation directions | First-order, adversarial-sign span loss can accumulate while total squared charge is arbitrarily small | No producer independent of the desired persistent span barrier | Conditional controls -> blocked `step_002` -> `step_003` and final theorem | Balanced Gram-one infinitesimal rotation activates the defect at the first transition | `sketch/interface defect`; idea-level if no exact ALS-specific identity exists | `/proof-sketch`, but superseded by idea-level scale defects |
| Objective convergence | Convergence and asymptotic-mode export | Bounded gauged factors and \(\sum_t\ell_t<\infty\); generic KL suggestion | Explicit conditional control plus unsupported standard-tool appeal | Square-summability gives \(d_t\to0\), not convergence of \(\widehat T_t\) or \(\mathcal L_t\) | Same ambient tensor and objective convention; cited KL interface is unspecified | \(\mathcal L_{t+1}-\mathcal L_t=-2\langle R_t,d_t\rangle+\ell_t\); the linear term has no summable or signed control | Compactness and asymptotic regularity do not export a limit | Same target, but no finite-variation or sufficient-decrease residual control | Linear forcing has uncontrolled sign and may be nonsummable although \(\sum_t\ell_t<\infty\) | No noncircular descent, relative-error, or finite-length producer | Conditional energy -> blocked `step_003` -> final limit claim | A bounded unit-circle path with angular increments of order \(1/t\) has summable squared steps and nonconvergent target distance; exact map-specific exclusion is absent | `sketch/interface defect`; idea-level if convergence must be added to the certificate | `/proof-sketch`, but superseded by idea-level scale defects |
| Same-target coefficient-to-ambient relative floor | Public relative-loss bridge and theorem closure | Exact coordinate map \(Q\) and base conditioning | Direct current-notation derivation from primitive base maps | Adequate for an absolute ambient lower bound; inadequate for a relative floor without normalized margin and smoothing control | Compatible: \(Q(T-\widehat T_t)=D_r+E_\rho-P_t\) uses the branch's exact maps and target | \(\|R_t\|_F\geq\|Q\|_{op}^{-1}(\operatorname{dist}(D_r,\mathcal S_t)-\|E_\rho\|_F)_+\) | Raw base conditioning controls \(\|Q\|_{op}\), but no raw control exports a \(\sqrt r\) margin or bounds \(E_\rho\) and \(\|T\|_F\) on \(\mathsf C\) | Exact same-target bridge; every residual is not dominated at the required relative scale | Realization-dependent smoothing residual is fixed along time but uncontrolled on the conditional event | Static producer is noncircular; theorem-scale inputs are missing | `step_002` floor plus coordinate identity -> blocked `step_003` -> final theorem | At \(E_\rho=0\), order-one margin still fails relative scale; with Gaussian smoothing, unbounded \(E_\rho\) is not event-controlled | `idea/theorem-contract defect` | `/subagent-idea-generator` |
| Exact/noiseless baseline relative-floor preservation | Baseline invariance obligation | Coordinate-orthonormal specialization with \(Q=I\), \(E_\rho=0\) | Setting-level inherited baseline | Source preserves object identity but not theorem-scale margin | Exact equality of produced coefficient target and consumed ambient target | \(\|T\|_F^2=\|D_r\|_F^2=r\), while exported norm floor is only order one | Existing raw controls cannot export a constant-fraction baseline loss | No surrogate residual; direct scale mismatch | Entry-only and persistent under the requested bridge | Noncircular specialization exposes the failure | Baseline specialization -> `step_001`/`step_003` -> final theorem | Failure is present before the first update and at \(\rho=0\) | `idea/theorem-contract defect` | `/subagent-idea-generator` |
| Positive probability of \(\mathsf C\) | Source-level probability-mode upgrade only; not a premise or conclusion of reviewed conditional theorem | Gaussian smoothing and initialization support only prospective fixed-time concentration | Primitive joint law; no all-time trajectory theorem | Inadequate for horizon-uniform Gram conditioning and finite energy | Branch probability convention is explicit and compatible | No recurrence, stopping rule, summable failure budget, or invariant produces the all-time event | Fixed-time random controls do not export the complete trajectory certificate | Entire post-entry trajectory is an uncontrolled residual for this upgrade | Adaptive infinite-horizon defects have no probability accumulation control | No producer; assuming \(\mathsf C\) is legal only inside the conditional theorem | Primitive law -> missing certificate event -> source-level upgrade; no consumer in current theorem | Time-zero concentration may hold, but preservation after the first update has no source | N/A to conditional-theorem acceptance; `idea/theorem-contract defect` for a full upgrade | `/subagent-idea-generator` for the separate upgrade |

## Blocking Issues

1. **`setting.md` Goal and candidate `step_001`: fixed deficit is incompatible with the relative target scale.** The exact/noiseless baseline has \(\|T\|_F^2=r\), while \(\Delta_0\geq\delta\) and the mandated transfer produce only an absolute norm margin. Downstream effect: the static coordinate bridge cannot yield \(\epsilon\|T\|_F^2\). Smallest repair: change the theorem-facing certificate to a normalized or \(\sqrt r\)-scale deficit, or weaken the success criterion; both require `/subagent-idea-generator`.
2. **Candidate `step_002`: squared represented-sum energy has the wrong order and wrong object for adaptive-span control.** The defect is first-order projector rotation and component cancellation, whereas the charge is quadratic motion of the sum. The exact block normal equations stated in the sketch do not supply an accumulation-compatible bridge. Downstream effect: no all-time coefficient floor reaches `step_003`. Smallest repair: expose and preflight an exact relaxed-ALS component/span control relation under unchanged assumptions; if none exists, strengthen the conditional certificate through `/subagent-idea-generator`.
3. **Candidate `step_003`: square-summability does not imply objective convergence.** The objective variation contains a potentially nonsummable, uncontrolled-sign term linear in \(\sqrt{\ell_t}\). Compactness and a generic KL label do not close the exact simultaneous pseudoinverse map. Downstream effect: the theorem's limit-existence clause has no legal producer. Smallest repair: add a map-specific sufficient-decrease/relative-error or finite-length step with complete source preflight; if unavailable, revise the theorem-facing certificate through `/subagent-idea-generator`.
4. **Candidate `step_003`: the exact same-target bridge lacks target-scale residual dominance.** The event controls neither \(\|E_\rho\|_F\) nor the realization-dependent comparison with \(\|T\|_F\), and Gaussian smoothing is unbounded. Downstream effect: even a coefficient floor cannot be converted to the displayed realization-uniform relative loss. Smallest repair: add a smoothing-control event and normalized target comparison, or change the probability/conclusion mode, through `/subagent-idea-generator`.
5. **`proof_sketch.md` structure: no dispatchable stable steps exist.** Candidate IDs do not satisfy the required exact-claim/dependency/output interface because their theorem-critical sources fail. Downstream effect: global proof and local proof workers cannot legally consume this sketch. Smallest repair: after the theorem contract is repaired, regenerate the complete stable step graph with mechanism witnesses, exported interfaces, and generated-output flow.

The missing constant-probability lower bound for \(\mathsf C\) is not a blocker for the literal conditional theorem. It remains a material residual source-level gap and must not be counted as proved or folded into a hidden good-event assumption.

## Required Repair Bundle

1. **Normalized entry interface (Blocking Issue 1; `setting.md`, `step_001`).** Replace the fixed absolute deficit clause by a target-scale statement such as \(\Delta_0\geq\delta\|D_r\|_F\), or state another source-backed bridge that exports an order-\(\sqrt r\) margin uniformly in the declared regime. Record how the new clause relates to the Gaussian initialization law and the remaining certificate-probability gap. Affected interface: \(\mathsf C\), formalized goal, `step_001`, `step_002`, and the public relative-floor specialization. Smallest repair target: `/subagent-idea-generator`.
2. **Component/span-motion source (Blocking Issue 2; `step_002`).** Either supply an obstruction-level exact relaxed-Jacobi identity that bounds first-order coefficient-feature or projector path motion, including cancellation directions and simultaneous cross-mode terms, by a concrete finite all-time budget, or change \(\mathsf C\) to include a target-compatible finite path-length/projector-motion or fixed-witness control. State the one-step charge and why its accumulated budget is finite. Smallest repair target: `/proof-sketch` only if the identity follows from the current theorem contract; otherwise `/subagent-idea-generator`.
3. **Convergence source (Blocking Issue 3; `step_003`).** Preflight a theorem or direct derivation for the exact half-relaxed pseudoinverse Jacobi map giving sufficient decrease plus relative error, finite variation, or another noncircular convergence mechanism. Include source identity, hypotheses, pseudoinverse/Gram convention, cross-term control, and boundary behavior. If unavailable, add an explicit finite-length or objective-convergence clause to the conditional interface and preserve its conditional labeling. Smallest repair target: `/proof-sketch` only for a current-contract derivation; otherwise `/subagent-idea-generator`.
4. **Relative ambient and smoothing bridge (Blocking Issues 1 and 4; `step_003`).** Retain the exact \(Q\)-identity, but add raw controls that bound \(\|E_\rho\|_F\), compare \(\|T\|_F\) with \(\|D_r\|_F\), and dominate every residual at order \(\sqrt r\). State whether this is deterministic, conditional on an added smoothing event, or probabilistic, and expose the resulting hidden-constant dependence. Smallest repair target: `/subagent-idea-generator`.
5. **Regenerated executable graph (Blocking Issue 5).** After Items 1--4 are resolved at the theorem-contract level, generate actual stable steps for entry scaling, dynamic span control, convergence, same-target ambient transfer, and final conditional assembly. Each step must have an exact claim, dependencies, assumption ids, source-adequate mechanism witness, accumulation relation, output interface, and producer-consumer flow. Keep the certificate-probability theorem separate unless the revised idea explicitly adds it. Smallest repair target: `/proof-sketch` after idea re-formalization.
6. **Target-preserving repair check.** A roadmap-only repair cannot preserve the current formalized setting and goal because the literal event lacks both theorem-scale normalization and a smoothing-to-relative-target control. Adding those sources changes the theorem-facing conditional scope and exposed dependence; weakening to an absolute floor changes the success criterion. Thus the deepest required repair is idea/formalization revision rather than another sketch attempt under the unchanged contract.

## Review Rationale

`IDEA_FAIL` is required because at least two independent defects are in the theorem contract rather than the roadmap: the fixed coefficient deficit is not normalized to the relative ambient-loss scale, and the conditional event has no source controlling the smoothing residual and target norm at that scale. Repairing either defect requires changing the theorem-facing conditional interface, probability/scope declaration, exposed normalization, or success criterion. The span-motion and convergence gaps independently prevent dispatch to local proof work; an exact map-specific sketch repair could be tested for those two gaps, but it would not cure the scale and smoothing defects. The score is 3 because the sketch is candid, structurally diagnostic, and source-aligned, yet the exact formalized implication is not viable without idea-level changes and no accepted proof-step graph exists.
