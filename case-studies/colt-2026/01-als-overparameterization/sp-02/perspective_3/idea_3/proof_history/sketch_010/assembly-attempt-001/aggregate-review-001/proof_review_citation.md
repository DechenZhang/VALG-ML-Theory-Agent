# Citation Proof Review

## Reviewer Verdict

BLOCKING

## Blocking Issues

1. The public bundle relies on acceptance-critical standard results that are neither proved in-file nor supported by citation commands and matching BibTeX entries. The unsupported uses are the rectangular Gaussian operator-norm tail and Weyl singular-value perturbation inequality in the `step_001` translation; Gaussian chi-square concentration, the rectangular Gaussian operator-norm tail, and self-adjoint matrix Bernstein in the `step_003` translation; uniqueness of Haar measure in the `step_006` translation; and the bi-invariant compact-group curvature formulas and Bakry--Emery log-Sobolev criterion in the `step_007` translation. The auxiliary blocks merely say that these statements are being used, and the later proofs invoke them. `reference.bib` contains no source entry, no public TeX file contains a citation command, and `main.tex` emits no bibliography. The accepted step applications do fix the exact statements, current objects, constants, parameter regimes, and assumption discharges, so the repair is to add faithful public source identities, citation commands, matching BibTeX entries, and bibliography behavior without changing any accepted mathematical application. The selected sources must support the exact Gaussian-tail, matrix-Bernstein, Haar-uniqueness, curvature-normalization, and log-Sobolev constants actually used.

2. Several public theorem-style statements are not self-contained and do not expose a valid named dependency. Proposition~`prop:step-007-product-tail` asserts an undefined equation `(Haar)`; Proposition~`prop:step-008-raw-leverage` and the two `step_009` results use an undefined `(LEV)`; Proposition~`prop:step-014-positive-physical-loss` uses undefined `(T2)`--`(T3)` and its subsection conclusion uses undefined `(T1)`--`(T3)`; Proposition~`prop:step-015-conditional-accounting` uses undefined `(T)`. The same area also uses vague theorem-facing inputs such as "the fixed-target Haar estimate" and "the elliptic singular intervals" instead of the exact prior theorem-style references. The underlying accepted claims are present in the proofs, but the public statements make the reader reconstruct workflow-local target aliases. Replace every alias by the exact displayed conclusion or a named, labeled public result and state each proposition's complete assumption/dependency basis.

3. The appendix uses manual `\tag{...}` numbers and prose references such as "equation (1)" throughout instead of unique `\label{...}` and `\ref{...}` pairs. Numbers are reused across subsections, and the aliases in issue 2 have no target at all. Although the compiled log has no unresolved LaTeX `\ref`, this manual reference layer is not machine-resolvable and violates the public internal-reference contract. Give every cross-used equation a unique label and use `Equation~\ref{...}` or `\eqref{...}`; remove purely redundant equation aliases.

## Nonblocking Concerns

None

## Suggested Repair Depth

assembly

## Smallest Candidate Repair Target

/proof-assembly

## Citation Audit

- Cited theorem statements: The 45 public appendix theorem-style units match the 45 accepted local units in order and at the same mathematical strength. Their actual dependency chain is valid, but the `step_007`--`step_009`, `step_014`, and `step_015` statements named in blocking issue 2 are not self-contained because they retain undefined step-local target aliases. No theorem statement may use those aliases after repair.
- Source fidelity: Comparison with the accepted step artifacts shows that the Gaussian operator tail, Weyl inequality, chi-square bound, matrix Bernstein inequality, Haar uniqueness statement, compact-group curvature convention, and Bakry--Emery criterion are restated with the same conclusions used by the local derivations. The accepted reviews also correspond to the current producer attempts. Source fidelity cannot nevertheless be completed publicly because no external source identity or stable locator is supplied. `assembly_report.md`, `global_proof.md`, and `global_proof_review.md` were used only to locate likely gaps and were not treated as proof evidence or citation authority.
- Instantiation and object mapping: The full evidence-use map is below. Every accepted dependency uses the same branch objects; no normalized or surrogate target replaces `D_r`, `T`, or the physical Frobenius loss.

| Public block | Accepted evidence source | Conclusion used and current objects | Hypotheses and discharge | Downstream use |
| --- | --- | --- | --- | --- |
| Setup and `step_001` | `step_001` units 001--003 | Base bounds plus `Xi_M=M-bar M` give `E_cond`, singular interval `[kappa_1^{-1},kappa_1]`, and `||M^dagger||_op<=kappa_1` for `M=A,B,C` | `assump:base_conditioning`, `assump:dimension`, `assump:gaussian_smoothing`; Gaussian operator tail at variance `r^{-2q}/n`; Weyl on the same physical matrices | `step_002`, `step_008`, `step_012`, `step_014` |
| `step_002` | `step_001`; `step_002` units 001--003 | Compact-SVD map `H_M`, iid `z_i^M`, exact balancing scalars, `bar zeta_i^M=(s_i^m/sqrt(r))H_M z_i^M`, exact `S_0`, and unchanged `Dhat_0` | `assump:independent_initialization` and derived `E_cond`; Gaussian polar law is derived from the radial density; SVD and Gaussian linear-image calculations are carried out in current notation | `step_003`--`step_006` |
| `step_003` | `step_001`, `step_002`; `step_003` units 001--004 | Radius diagonals, isotropic `r^2 x k` Khatri--Rao Gram in `[1/2,3/2]`, elliptic/diagonal transfer, and exact `E_gram^norm` | `assump:dimension`, `assump:rank_window`, `assump:independent_initialization`; chi-square, Gaussian operator tail, and matrix Bernstein instantiated with `N=r`, matrix dimension `k`, explicit envelope, variance, and deviation `1/8` | `step_010` |
| `step_004` | `step_002`; `step_004` units 001--003 | Raw Gaussian radius tail and geometric-mean balancing imply exact `E_size` | `assump:dimension`, `assump:rank_window`, `assump:independent_initialization`; the Gaussian-square MGF and Markov/Chernoff calculation are proved directly | `step_010`, `step_011` |
| `step_005` | `step_002`; `step_005` units 001--004 | `G_raw=(r/n)^2G_norm`, raw/normalized synthesis ranges equal `S_0`, gauge quotient has the same image, and `Dhat_0 in S_0` | Setting definitions and exact nonzero scalar/product identities; all zero branches are treated algebraically | `step_006`, `step_009` |
| `step_006` | `step_002`, `step_005`; `step_006` units 001--004 | Measurable `Z_M=Q_MR_M`, independent product Haar `SO(r)^3`, shape-independent `E`, `dim(E)<=3kr`, and exact `S_0=LQE` | `assump:independent_initialization`, `assump:rank_window`; Gaussian full-rank and QR facts are checked, while Haar uniqueness is the unproved standard input | `step_007`, `step_008` |
| `step_007` | `step_006`; `step_007` units 001--003 | For fixed `E,X`, `E h=d/r^3`, `Lip(h)<=2sqrt(3)`, and tail `P(h-Eh>=t)<=exp(-rt^2/288)` | `r>=3` from `assump:dimension`; sequential twirling and Lipschitz bounds are proved directly; compact-group curvature and Bakry--Emery LSI are the unproved standard inputs | `step_008` |
| `step_008` | `step_001`, `step_006`, `step_007`; `step_008` units 001--004 | Exact oblique projector for `LQE`, elliptic loss `kappa_1^12`, and `||P_{S_0}D_r||_F^2<=r/2` with conditional failure at most `r^{-20}` | `assump:rank_window`, derived `E_cond`, fixed `X=L^T D_r`, `d<=3kr`, `tau_kappa=1/(4kappa_1^12)`, explicit exponential-to-polynomial conversion | `step_009` |
| `step_009` | `step_005`, `step_008`; `step_009` units 001--002 | `Dhat_0 in S_0`; `W_0=P_{S_0^perp}D_r/||P_{S_0^perp}D_r||` is a unit raw normal and generates `E_deficit` | Exact raw leverage and Pythagoras; `||D_r||_F=sqrt(r)` and `delta_0=1/8` | `step_010`, `step_013` |
| `step_010` | `step_001`, `step_003`, `step_004`, `step_009`; `step_010` units 001--002 | Exact intersection `E_init_norm` has probability at least `1-r^{-10}` | Five primitive statistical/structural assumptions through dependencies; tower property and a conditional union bound give `4r^{-20}<=r^{-10}` without independence | `step_011`, `step_015` |
| `step_011` | `step_004`, `step_010`; `step_011` units 001--003 | On `E_init_norm intersect C_path`, finite total variation gives a finite `d_bal` limit and all factor columns remain at most `3` | `assump:gd_step`, derived `E_size`, explicit conditional `C_path`; finite-dimensional completeness in the exact product Frobenius metric | `step_012`--`step_015` |
| `step_012` | `step_001`, `step_011`; `step_012` units 001--003 | Exact derivative and trilinear remainder for `Psi`, bounded at the endpoint by `delta_0/16` | Derived pseudoinverse bound, endpoint displacement at most `E_star`, factor radius `3`; rank-one Frobenius identity and finite Cauchy--Schwarz are proved directly | `step_013` |
| `step_013` | `step_009`, `step_011`, `step_012`; `step_013` units 001--002 | Tangent cancellation against `W_0` and raw endpoint margin at least `(15/16)delta_0 sqrt(r)` | Exact raw witness, exact endpoint, and one endpoint remainder; Frobenius Cauchy--Schwarz in the same coefficient space | `step_014` |
| `step_014` | `step_001`, `step_013`; `step_014` units 001--002 | `(P_A tensor P_B tensor P_C)(T-S(theta))=(A tensor B tensor C)(D_r-Psi(theta))`, physical residual floor, `||T||_F<=kappa_1^3sqrt(r)`, and relative loss `epsilon_0=((15/16)delta_0)^2kappa_1^{-12}` | Derived full column rank and raw margin; projector identity and tensor-product singular values are proved directly for the actual physical target | `step_015` |
| `step_015` and main theorem | `step_010`, `step_011`, `step_014`; `step_015` units 001--003 | Polynomial continuity, `lim_t F(theta_t)=F(theta_infinity)`, exact event inclusion, and `P(F_+)>=(1-r^{-10})P(C_path|E_init_norm)` | All six theorem assumptions through dependencies; the conditioning denominator is positive for `r>=2`; the conditional factor may be zero | Theorem~`thm:main` |

- Quantitative regime fidelity: The public theorem preserves exposed `n,r,k,kappa,q`, confidence `1-r^{-10}`, all-time/asymptotic horizon, `d_bal`, physical Frobenius loss, and the possibly zero conditional path probability. The standard-tool applications retain their exact quantitative regimes: smoothing variance `r^{-2q}/n`; rank window `r<k<=r^{5/4}`; Khatri--Rao aspect `k/r^2<=r^{-3/4}`; matrix-Bernstein dimension factor `2k`; Haar metric `-tr(KL)` with `r>=3`; `0<t<=1`; and elliptic loss `kappa_1^12`. Constants `r_0,C` depend only on `kappa,q`, `c_H=1/288` is universal, and `epsilon_0` depends only on `kappa`. No hidden dependence or mode upgrade was found.
- Assumption discharge: All six stable assumptions from `setting.md` occur exactly once as numbered public assumptions with matching labels. The main theorem cites the complete range `assump:base_conditioning` through `assump:gd_step`. `E_cond`, `E_gram^norm`, `E_deficit`, `E_size`, convergence, radius, margin, and positive loss are derived outputs, not assumptions. `C_path` is the sole conditional trajectory hypothesis, exactly as allowed by the formalized conditional goal.
- Main theorem internal references: `3_main.tex` cites only the six numbered assumptions and the setup section; it has no appendix-local reference, proof-step identifier, local-unit identifier, or workflow artifact citation. Its target, relative-loss constant, probability law, convergence mode, and conditional factor match `setting.md`.
- Appendix cited-result traceability: Named theorem-style dependencies generally map one-to-one to accepted units, and the final proof cites Proposition~`prop:step-010-public-confidence`, Lemma~`lem:step-011-finite-path-limit`, Propositions~`prop:step-014-positive-physical-loss`, `prop:step-015-event-inclusion`, and `prop:step-015-conditional-accounting` at their accepted strength. Traceability fails only at the public translation layer identified in blocking issues 2--3: undefined aliases, vague unnamed inputs, and manual equation references.
- External BibTeX entries: No external citation command appears. `reference.bib` contains only string macros and a placeholder comment, and `main.tex` intentionally has no bibliography command. The current PDF therefore does not expose an empty References section, but that does not discharge the standard-tool citation obligations in blocking issue 1.
- Internal label/ref pairs: The six assumption labels, theorem label, 15 appendix-subsection labels, and all 45 theorem-style labels are unique. The current compile log reports no undefined or multiply defined LaTeX references. This clean log does not cover the manually typed equation references and undefined aliases, which have no `\label`/`\ref` representation and remain blocking.
- Unsupported standard facts: Citation or a complete inline proof is required for the exact rectangular Gaussian operator-norm tail, Weyl singular-value perturbation inequality, Gaussian chi-square concentration bound, self-adjoint matrix Bernstein inequality, compact-group Haar uniqueness, bi-invariant compact-group curvature/Killing-form convention, and Bakry--Emery log-Sobolev criterion. The Kronecker/product singular-value comparisons should either receive the same linear-algebra source or be proved explicitly by the SVD argument already used later in `step_014`. Gaussian polar decomposition, Gaussian-square MGF, sequential twirling, the Lipschitz estimate, finite-dimensional completeness, Cauchy--Schwarz, polynomial continuity, and conditional probability accounting are sufficiently derived or elementary in-file.

Verdict alignment: `BLOCKING` is caused only by public assembly traceability and bibliography defects. The accepted step claims, object mappings, parameter regimes, assumption discharges, and exact physical target do not require revision.
