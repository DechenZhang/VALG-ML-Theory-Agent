# Citation Proof Review

## Reviewer Verdict

PASS

## Blocking Issues

None

## Nonblocking Concerns

None

## Suggested Repair Depth

None

## Smallest Candidate Repair Target

None

## Citation Audit

- Cited theorem statements: All seven external sources are identified by a
  stable public locator and are restated before use in current notation. The
  Davidson--Szarek Gaussian norm tail, Stewart--Sun singular-value
  perturbation inequality, Laurent--Massart chi-square deviations, Tropp
  self-adjoint matrix Bernstein inequality, Folland Haar-uniqueness theorem,
  Besse bi-invariant curvature formulas, and Bakry--Gentil--Ledoux
  Bakry--Emery criterion support exactly the conclusions used below.
- Source fidelity: The locator, statement shape, constants, and non-output
  boundaries of every cited source were checked independently against the
  accepted application. The detailed source audit below records each
  specialization. No source is used for a stronger conclusion than it
  supplies.
- Instantiation and object mapping: Every cited result is translated to the
  branch objects before use. In particular, the two Gaussian norm uses act on
  the exact smoothing and coefficient arrays; matrix Bernstein acts on the
  conditional truncated row matrices; Haar uniqueness acts on the finite
  measures generated by the QR shape events; and the geometric sources use
  the exact unnormalized Hilbert--Schmidt product metric on (SO(r)^3).
  Assembly attempt 3 also restores, before their relevant uses, the accepted
  raw/balanced/normalized coefficient declarations, the `Z_M` and `H_a/H_b/H_c`
  mode map, the fixed-target `h(Q;E,X)` definition and nonzero-target domain,
  and the remaining-initialization conditional law. The declaration audit
  below checks each restored object against its accepted source.
- Quantitative regime fidelity: The bundle preserves the exposed
  (n,r,k,\kappa,q), smoothing variance (r^{-2q}/n), rank window
  (r<k\le\lfloor r^{5/4}\rfloor), confidence (1-r^{-10}), universal
  (c_H=1/288), elliptic loss (\kappa_1^{12}), endpoint constant
  \(\epsilon_0=((15/16)\delta_0)^2\kappa_1^{-12}\), all-time/asymptotic
  horizon, and the possibly zero conditional path factor. The constants
  (r_0,C) depend only on (\kappa,q); no dependence on (n,r,k), the base
  triple, the internal shapes, the target direction, or time is hidden.
- Assumption discharge: The six stable assumptions in `setting.md` appear as
  the six matching numbered public assumptions. All generated events and
  controls have named producers. The only conditional theorem-facing input is
  the setting-defined finite-path event (\mathcal C_{\rm path}), exactly as
  allowed by the formalized conditional goal.
- Main theorem internal references: `3_main.tex` cites only numbered
  assumptions and setup notation; it contains no appendix-local theorem,
  step ID, local unit, or workflow reference. Its probability, horizon, norm,
  endpoint, and conditional modes match `setting.md` and `assembly_report.md`.
- Appendix cited-result traceability: All 45 accepted local units occur as 45
  theorem-style public results, in dependency order, and every one is cited by
  a later theorem-style result or subsection/main-theorem proof. All 45
  per-unit review verdicts are PASS, and all 15 current step reviews are
  ACCEPTED for sketch attempt 10. The full comparison below includes every
  local unit, every cited-result section, and every target-step assembly.
- External BibTeX entries: The nine citation occurrences use exactly seven
  keys, and all seven keys have complete matching entries in `reference.bib`.
  `main.tex` invokes `ims.bst` and `reference.bib`; `main.aux` contains all
  seven `\\bibcite` records; `main.bbl` contains seven nonempty entries; and
  `main.blg` reports no BibTeX warning.
- Internal label/ref pairs: Across the public TeX there are 252 unique labels
  and 476 `\\ref`/`\\eqref` occurrences. Every occurrence resolves, no label
  is duplicated, all labels are ASCII, all 46 theorem-style labels (the main
  theorem plus 45 local results) are referenced, and the current compile log
  has no undefined citation, undefined reference, or multiply-defined label.
  No manual `\\tag`, malformed command fragment, or legacy `(Haar)`, `(LEV)`,
  `(T)`, or `(T1)`--`(T3)` alias remains.
- Unsupported standard facts: None. The nontrivial external inputs are cited
  as above. Gaussian polar decomposition, Gaussian-square MGF, compact-SVD
  mapping, sequential twirling, the projection Lipschitz calculation,
  Kronecker/product singular comparisons, Euclidean completeness,
  Cauchy--Schwarz, polynomial continuity, and conditional event accounting
  are elementary or derived in current notation at the point of use.

## External Source Fidelity

| Source and locator | Public use | Exact current-notation interface and discharge | Fidelity result |
| --- | --- | --- | --- |
| Davidson--Szarek, Theorem II.13 | `step_001` and `step_003` | For an iid standard Gaussian (m\times p) matrix, (\mathbb P(\|G\|_{\rm op}>\sqrt m+\sqrt p+u)\le e^{-u^2/2}). The smoothing use sets (G=(\sqrt n/r^{-q})(M-\bar M)) and (u=\sqrt{2\log(3r^{20})}); the Khatri--Rao use sets (G=Z\in\mathbb R^{r\times k}) and (u=8\sqrt{\log r}). Both dimensions, variances, thresholds, and unions are shown. | Exact statement, object map, and tail constant preserved. |
| Stewart--Sun, Chapter IV | `step_001` | Weyl's bound (\sigma_{\min}(X+E)\ge\sigma_{\min}(X)-\|E\|_{\rm op}) is applied to (X=\bar M), (E=M-\bar M). The base singular margin and perturbation radius are supplied by the two preceding public lemmas. | Exact matrix objects, norm, and singular-value conclusion preserved. |
| Laurent--Massart, Lemma 1 | `step_003` | Substitution (x=d\delta^2/8) into the upper and lower chi-square deviations gives (\mathbb P(|\|g\|_2^2/d-1|>\delta)\le2e^{-d\delta^2/8}) for (0<\delta<1). The proof then uses ((d,\delta)=(n,1/2)) for all (3k) full radii and ((r,1/4)) for the (k) projected columns. | Derived corollary, constants, dimensions, and finite unions are valid. |
| Tropp, Theorem 6.1 | `step_003` | The standard one-sided self-adjoint Bernstein theorem, applied to both (X_a) and (-X_a), gives the displayed two-sided (2d\exp[-t^2/(2v+2Rt/3)]) form. Here (d=k), (t=1/8), (R=18L_r^2k/r^2), and (v\le79k/r^2). Conditioning on the coordinate-product truncation event preserves row independence and centering under the truncated law. | The two-sided conversion, dimension factor, envelope, variance proxy, and conditioning hypotheses are all discharged. |
| Folland, Chapter 2 | `step_006` | Uniqueness of normalized Haar measure implies that each finite left-invariant Borel measure (\mu_B(A)=\mathbb P(O_0\in A,R_0\in B)) on (O(r)) is (\mathbb P(R_0\in B)\operatorname{Haar}(A)). This yields both Haar orientation and independence; the same argument applies after the determinant bit is moved into the shape for (SO(r)). | Compact groups, measures, invariance, and independence conclusion match the cited theorem. |
| Besse, Chapter 7 | `step_007` | In the convention (\langle K,L\rangle=-\operatorname{tr}(KL)), the cited bi-invariant connection and curvature formulas are combined with the displayed basis calculation (\operatorname{tr}(\operatorname{ad}_K\operatorname{ad}_L)=(r-2)\operatorname{tr}(KL)), giving (\operatorname{Ric}_{SO(r)}=(r-2)g/4). Ricci adds under the squared-sum product metric. | Metric scale, curvature sign, Killing-form normalization, and product convention are explicit and correct. |
| Bakry--Gentil--Ledoux, Chapter 5 | `step_007` | On complete connected (SO(r)^3) with normalized product Haar and (\rho=(r-2)/4>0), the cited criterion gives (\operatorname{Ent}(f^2)\le(2/\rho)\int\|\nabla f\|^2). With (L=2\sqrt3), the displayed Herbst calculation gives (\exp[-(r-2)t^2/96]\le\exp[-rt^2/288]). | Manifold, measure, curvature, metric, Lipschitz, and exponential constants are fully matched. |

## Attempt-3 Declaration Audit

| Restored declaration | Accepted source and current mapping | First-use and scope audit | Result |
| --- | --- | --- | --- |
| Pre-balancing, balanced, and normalized coefficient vectors | The declarations reproduce the `step_002` source exactly: the columns of `X_0^raw,Y_0^raw,Z_0^raw` are `tilde x_i,tilde y_i,tilde z_i`; `(A,x,alpha)`, `(B,y,beta)`, `(C,z,gamma)` fix the mode correspondence; `tilde zeta_i^M=M^dagger tilde m_i`; `zeta_i^A=alpha_{i,0}` and analogously; and `bar zeta_i^M=sqrt(n/r) zeta_i^M`. | They precede the coefficient-Gaussianization statement and every use of these aliases. The subsequent `H_M z_i^M` identities derive their values; no declaration assumes the result. | Exact accepted notation and normalization restored. |
| Gaussian mode arrays and elliptic aliases | `Z_M=[z_1^M ... z_k^M]` uses the standard Gaussian vectors produced by `lem:step-002-coefficient-gaussianization`; `Z_A,Z_B,Z_C` map to modes `a,b,c`; and `H_a=H_A`, `H_b=H_B`, `H_c=H_C`. | The step-003 inline `Z_M` definition remains consistent, while the step-006 declaration precedes the product-shape and equivariance uses. The aliases introduce no new random object, law, or conditioning. | Exact producer-consumer object map restored. |
| Fixed-target projection energy | `h(Q;E,X)=||P_{QE}X||_F^2/||X||_F^2` is the exact `step_007` object for fixed `E`, `X != 0`, and product rotations `Q`; `d=0` gives `h=0`. In step 008, `X=L^T x` is nonzero whenever `x` is nonzero because `L` is invertible. | The definition precedes the mean, Lipschitz, tail, and elliptic-transfer uses. It excludes the zero denominator and handles the zero-subspace boundary without extending the accepted domain. | Definition, domain, and downstream instantiation are explicit. |
| Remaining-initialization conditional law | `P_init(. | A,B,C)` is declared as conditional probability over the independent raw Gaussian initialization after a realized smoothing triple in `E_cond` is fixed. This is the law supplied by `assump:independent_initialization`. | The declaration precedes both occurrences in the step-009 inherited tail calculation. It adds no independence beyond initialization-smoothing independence and agrees with the plain conditional law used by steps 003, 004, 008, and 010. | Probability object and conditioning mode restored exactly. |

## Full Evidence-Use Map

| Step | Accepted local units and public theorem-style translations | Evidence use and exact exported conclusion | Target-step assembly audit |
| --- | --- | --- | --- |
| `step_001` | `unit_001` -> `lem:step-001-base-conditioning`; `unit_002` -> `lem:step-001-gaussian-perturbation`; `unit_003` -> `prop:step-001-realized-conditioning` | Primitive base, dimension, and smoothing assumptions plus Davidson--Szarek and Stewart--Sun produce the exact (\mathcal E_{\rm cond}), singular interval, pseudoinverse bound, and (r^{-20}) budget. | All three units and no downstream premise are used. |
| `step_002` | `unit_001` -> `lem:step-002-balancing-scalars`; `unit_002` -> `lem:step-002-coefficient-gaussianization`; `unit_003` -> `prop:step-002-balancing-invariance` | The restored coefficient declarations fix every source object first. Derived conditioning and Gaussian initialization then give the exact (H_M,z_i^M,s_i^m) law; nonzero pair scalars preserve (\mathscr S_0), and (s_i^xs_i^ys_i^z=1) preserves (\widehat D_0). | Radius law, coefficient law, and raw invariance are all assembled at accepted strength. |
| `step_003` | `unit_001` -> `lem:step-003-radius-diagonals`; `unit_002` -> `lem:step-003-isotropic-khatri-rao`; `unit_003` -> `prop:step-003-elliptic-transfer`; `unit_004` -> `prop:step-003-normalized-gram-event` | Laurent--Massart, Davidson--Szarek, and Tropp are applied to the exact conditioned Gaussian arrays; the pointwise dependent balancing diagonals and elliptic maps then yield all three normalized Gram windows. | The four units preserve the conditional (r^{-20}) budget and do not claim scalar/Gaussian independence. |
| `step_004` | `unit_001` -> `lem:step-004-raw-radius-tail`; `unit_002` -> `prop:step-004-uniform-raw-size`; `unit_003` -> `prop:step-004-balanced-size-transfer` | A directly derived Gaussian-square MGF tail and finite union produce the raw event; the geometric-mean law is cited from `lem:step-002-balancing-scalars` and yields the exact balanced (\mathcal E_{\rm size}). | The repaired dependency is the balancing-scalar lemma, not an unrelated coefficient result. |
| `step_005` | `unit_001` -> `lem:step-005-pair-gram-scaling`; `unit_002` -> `prop:step-005-synthesis-scaling`; `unit_003` -> `lem:step-005-gauge-kernel`; `unit_004` -> `prop:step-005-quotient-range` | Direct scalar and kernel algebra gives (G_{\rm raw}=(r/n)^2G_{\rm norm}), equal synthesis ranges, exact gauge handling on every branch, and (\widehat D_0\in\mathscr S_0). | All four algebraic units preserve the raw target and metric. |
| `step_006` | `unit_001` -> `lem:step-006-gaussian-qr-shape`; `unit_002` -> `prop:step-006-reflection-absorption`; `unit_003` -> `prop:step-006-product-shape`; `unit_004` -> `prop:step-006-exact-equivariance` | The restored `Z_M`/mode/elliptic map identifies the accepted arrays and operators. Gaussian QR plus Folland's Haar uniqueness then produces independent (SO(r)^3) orientations and a measurable internal shape with (d\le3kr); accepted span algebra gives the exact (\mathscr S_0=LQE). | The complete shape, determinant bit, null branches, independence, dimension, and exact raw range are all included. |
| `step_007` | `unit_001` -> `lem:step-007-sequential-twirl`; `unit_002` -> `lem:step-007-lipschitz`; `unit_003` -> `prop:step-007-product-tail` | The restored definition fixes `h(Q;E,X)` on the accepted nonzero-target domain. Sequential current-notation twirling gives (d/r^3); direct differentiation gives (2\sqrt3); Besse and Bakry--Gentil--Ledoux give the metric-matched tail with (c_H=1/288). | The public proposition states the full tail directly; no undefined `(Haar)` alias remains. |
| `step_008` | `unit_001` -> `lem:step-008-oblique-projection`; `unit_002` -> `prop:step-008-elliptic-transfer`; `unit_003` -> `prop:step-008-raw-leverage`; `unit_004` -> `lem:step-008-tail-conversion` | The exact oblique projector, singular intervals, fixed target (L^{\mathsf T}D_r), rank window, and exponential-to-polynomial conversion yield (\|P_{\mathscr S_0}D_r\|_F^2\le r/2). | All thresholds and the (\kappa_1^{12}) loss are explicit; no undefined `(LEV)` alias remains. |
| `step_009` | `unit_001` -> `lem:step-009-normal-energy`; `unit_002` -> `prop:step-009-raw-witness` | Under the restored remaining-initialization conditional law, exact raw membership and leverage give positive normal energy; normalization constructs the canonical raw witness and proves (\mathcal E_{\rm deficit}) on the leverage event. | Both units, the zero-denominator check, inherited conditional failure, and conditioning mode are present. |
| `step_010` | `unit_001` -> `prop:step-010-conditional-union`; `unit_002` -> `prop:step-010-public-confidence` | The four exact constituent producers, tower property, and conditional union bound give (4r^{-20}\le r^{-10}) without an independence claim. | The exact setting event and joint probability law are preserved. |
| `step_011` | `unit_001` -> `lem:step-011-finite-path-limit`; `unit_002` -> `prop:step-011-path-radius`; `unit_003` -> `prop:step-011-uniform-factor-radius` | The explicit finite path budget gives the Cauchy limit and displacement; derived initial size gives every finite-time and endpoint column radius at most (3). | All-time convergence and radius are derived on (\mathcal C_{\rm path}), not assumed. |
| `step_012` | `unit_001` -> `lem:step-012-exact-expansion`; `unit_002` -> `prop:step-012-raw-remainder`; `unit_003` -> `prop:step-012-endpoint-remainder` | The exact trilinear expansion, left-inverse bounds, base radius, and endpoint displacement give (C_{\rm CP}(\kappa,3)E_\star^2\le\delta_0/16). Endpoint existence cites `lem:step-011-finite-path-limit`; displacement cites `prop:step-011-path-radius`. | All three units and the single-endpoint, raw-Frobenius scope are preserved. |
| `step_013` | `unit_001` -> `lem:step-013-tangent-cancellation`; `unit_002` -> `prop:step-013-preserved-raw-margin` | On every outcome in (\mathcal E_{\rm init\_norm}\cap\mathcal C_{\rm path}), (W_0) is selected directly from the existential definition of (\mathcal E_{\rm deficit}). Exact tangent cancellation and the endpoint remainder then give the (15/16) raw margin. Endpoint and zero-path claims cite `lem:step-011-finite-path-limit`; displacement cites `prop:step-011-path-radius`. | The canonical `step_009` witness is not misused as event-wide authority; both units preserve the exact raw objects. |
| `step_014` | `unit_001` -> `lem:step-014-mode-span-projection`; `unit_002` -> `prop:step-014-positive-physical-loss` | The exact physical projection identity, Pythagoras, tensor singular values, and preserved raw margin yield (F(\theta_\infty)\ge((15/16)\delta_0)^2\kappa_1^{-12}\|T\|_F^2>0). | The exact target, physical Frobenius norm, relative normalization direction, and zero-path baseline are preserved. |
| `step_015` | `unit_001` -> `prop:step-015-polynomial-continuity`; `unit_002` -> `prop:step-015-event-inclusion`; `unit_003` -> `prop:step-015-conditional-accounting` | Polynomial continuity gives the asymptotic physical loss, the exact event inclusion follows, and positivity of (\mathbb P(\mathcal E_{\rm init\_norm})) permits the exact conditioning identity. | The final factor (\mathbb P(\mathcal C_{\rm path}\mid\mathcal E_{\rm init\_norm})) is retained and may be zero; no unconditional positive-probability claim is introduced. |

Assembly attempt 3 was reviewed against the current `assembly_report.md` and
current public bundle. The accepted sketch and global diagnostic were used only to locate expected
interfaces. Their current hashes match the accepted review identities, and
the global review status is ACCEPTED, but neither diagnostic was used as proof
evidence or as a source of assumptions. The complete unit-level comparison
found no step or sketch interface defect, so the overload rule does not
trigger a sketch revision.
