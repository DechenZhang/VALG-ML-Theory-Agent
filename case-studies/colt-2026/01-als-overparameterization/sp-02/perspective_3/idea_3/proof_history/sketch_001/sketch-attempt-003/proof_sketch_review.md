# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 3
- Review target: `perspective_3/idea_3/proof_sketch.md`
- Trigger: sketch-attempt-2 review returned `REVISE_SKETCH` for the incomplete diagram export, missing topology source, and missing excess-profile charging interface.
- Goal mode: exact-goal mode
- Progress type: `conditional`

## Sketch Viability Score

7

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: The 15-step sketch preserves the exact formalized goal: the same smoothed CP objective, balanced simultaneous GD protocol, universal window (r<k\le r^{5/4}), normalized initialization event, conditional finite-path conclusion, positive relative limiting loss, and exact factor \(\Pr(\mathcal C_{\rm path}\mid\mathcal E_{\rm init\_norm})\). No theorem scope, probability mode, metric, rank threshold, or progress-type change is introduced.
- Dependency audit: All 15 IDs are present, and the displayed chain `step_001 -> ... -> step_015` is acyclic. The producer order `step_003 -> step_004 -> step_005 -> step_006 -> step_007 -> step_008 -> step_009` is now coherent. The later path/Taylor/physical steps consume only earlier outputs. The remaining issue is source feasibility inside steps 005--007, not an ordering defect.
- High-risk coverage: Step 004 now names the complete \(\Gamma(\mathcal D)\) datum, including (K,E,B,\lambda,v,\sigma,\tau,H_M,F_M,F\), empty modes, and roots. Step 005 isolates TOPrank from TOP; step 006 names a greedy inverse; step 007 names Charge; step 008 performs the multinomial sum; step 009 keeps TF/deficit assembly separate. This is a substantial repair. TOPrank and Charge still lack the exact source-interface details required to classify them as `step-local`.
- Explicit-rate coverage: The target rate \(\sqrt{k}/r+\sqrt{p_0/r}+p_0/r\), (p_0=\lceil31\log r\rceil), the no-log-leading requirement, Markov conversion, radial factor 16, and corrected (C_{\rm TF}=32e\sqrt{32/30}C_G) are retained. Constants (C_0,C_1,C_2,C_G) are not yet given explicit provenance or dependence bounds in the profile/Charge interface.
- Assumption and citation plausibility: Primitive assumptions and generated-event provenance remain correct. No external theorem is consumed, but the circuit-nullity identity \(\operatorname{rank}(R_M)=p-F_M\) and the proposed \(\mathbb F_2\) factorization are theorem-critical direct-derivation inputs. The sketch does not define the entries and dimensions of (L_M,N_M,R_M), or give a checked current-notation derivation. The greedy switch inverse and Charge are likewise proposed constructions rather than source-adequate interfaces.

## Early Obstruction Audit

- Limiting-case stress: The sketch explicitly lists (B\le2), degree-two vertices, (p=1), one-color words, empty (S_M), repeated labels, maximal collision, first cross-visit, maximal (k), and (p_0=\Theta(\log r)). These are the right boundary regimes, and no counterexample is supplied. Their inclusion is not itself a proof of TOPrank or Charge.
- Theorem-critical bridge support: The `step_004` schema is now substantially complete and can be a local bookkeeping producer if the stated loop/circuit identities are written as exact definitions. The remaining theorem-critical bridges are (i) TOPrank to TOP, (ii) the profile inverse, and (iii) Charge to the profile bound. Each is named with a target and a consumer, but the first source-to-claim derivation is still deferred.
- Exported-interface feasibility: `step_004` exports an exact diagram object to steps 005--007. `step_009` has a complete raw-control-to-TF/deficit path conditional on (21z). The TOPrank export is not yet feasible because (Q), (R_M), (L_M), and (N_M) are only described, not instantiated, and the circuit-nullity normalization is not discharged. The profile export is not yet feasible because “lexicographically first two-pair-switch sequence” does not specify the admissible switch set, termination, or reconstruction invariant. The Charge export is not yet feasible because the displayed inequality is the desired multiplicity/face-loss conclusion rather than a derived inequality with all exponents and constants accounted for.
- Theorem-critical mechanism witness gate: `step_001`--`step_004` and `step_009`--`step_015` have adequate mechanism witnesses. Step 005 has the correct proposed source (a joint incidence/interlace rank factorization) and consequence, but the source is incomplete at obstruction level. Step 006 has a plausible canonical encoding but no verified inverse. Step 007 identifies (e=(3p+2-v)-F), \(\sigma=B-v\), label/switch counts, and the nonleading-slot rule, but does not state the concrete exponent algebra that absorbs every multiplicity. These three witnesses remain shallow and route to sketch repair.
- Entry-state trace stress: The initialization is generated before the trajectory is used. On zero path length, step 011 yields the initial limit, step 012 has zero remainder, and steps 013--014 preserve the full raw deficit and physical floor. The conditional path source is not circular. This gate passes.
- Obligation locality classification: Steps 001--004 and 009--015 are `step-local` at the roadmap level. Step 005 TOPrank, step 006's profile inverse, step 007 Charge, and step 008's (ENUM)/(21z) are `sketch/interface defect` until their exact maps and inequalities are exposed. No current evidence requires an `idea/theorem-contract defect`.
- Noncircular closure gate: The path closure uses only the explicit finite variation certificate and passes. The diagram flow is acyclic. TOPrank and Charge are not logically circular in the dependency graph, but each currently names its desired consequence as the only quantitative control; an independent direct derivation and explicit count relation are still required.
- Mechanism-source and boundary stress: The (B\le2) branch makes TOP immediate once (F_M\le p) is recorded. For (B\ge3), the rank (B-2) and (p-F_M) claims need exact reduced-matrix definitions and a proof that deletion of the root/global relation preserves the stated ranks. For Charge, the cross-visit (b+c\ge1) rule is correctly stated, but it does not yet show how forest and switch multiplicities are dominated by the (\sqrt{p/r}) and (p/r) slots.
- Generated-output flow: Producer-consumer paths are legal: Gamma(D) -> TOPrank/profile -> Charge -> (ENUM)/(21z) -> TF/deficit -> initialization and final theorem. The flow table correctly records unresolved blockers. No output is consumed before its named producer.
- Source-to-claim adequacy: Direct quotient, Bernstein, Gaussianization, Wick, finite-path, Taylor, projection, and conditional-probability sources match their claim classes. TOPrank's unnamed incidence projections and circuit-nullity step do not yet pass direct-derivation feasibility. The profile inverse and Charge are novel direct derivations whose raw implications and constant dependence remain unstated.
- Residual-to-target adequacy: Normalized-to-raw scale, tangent-span equality, radial conjugation, raw projection, Taylor, and physical mode-span identities are exact. No surrogate-target or wrong-metric bridge appears. The open issue is whether the full trace moment is produced, not whether it transfers to the final target.
- Baseline invariance audit: Passed. (D_r,\widehat D_0,\mathscr S_0,W_0), the coefficient residual, and the physical loss remain raw. The zero-path specialization preserves the original positive-loss conclusion, rather than replacing it by a weaker remainder statement.
- Scope-accumulation compatibility: The moment is at fixed finite order (p_0); all diagram sums and event unions are finite. The path defect is a nonnegative increment series with a tail-sum control. Charge still needs a finite-order multiplicity budget; no temporal or horizon accumulation defect is hidden.
- Scope and dependence consistency: Quantifiers over (n,r,k), fixed (kappa,q), conditional initialization probability, and all-time conditional path mode agree with `setting.md`. The profile constants must be declared to depend only on (kappa), not (p,r,k,n), before the explicit-rate gate can pass.
- Generated-condition provenance: All conditioning, Gram, size, radial, TF, deficit, convergence, radius, and loss events are derived. Only (mathcal C_{\rm path}) is explicitly conditional. TOPrank, profiles, and Charge are not silently promoted to assumptions.
- Citation and tool applicability: The sketch correctly avoids treating generic chaos results or an unverified topology theorem as authority. The circuit-nullity identity must either be proved in current notation or cited with exact source/label and convention mapping. “Direct \(\mathbb F_2\) incidence/interlace factorization” is not enough without the actual matrices and hypotheses.
- Same-setting repair plausibility: The defects are repairable by expanding the TOPrank matrices and rank proof interface, specifying the canonical switch inverse and profile invariants, and deriving the Charge exponent/count inequalities. No counterexample or theorem-contract contradiction is known.
- Target-preserving bridge-repair gate: The repairs preserve (TOP), (ENUM), (21z), the no-log-leading TF rate, the corrected constant, raw (W_0), and every downstream theorem claim. Route to `/proof-sketch`, not idea revision.
- High-risk obligation classes: Structural topology, repeated-component multiplicity, explicit rate, generated-output flow, baseline invariance, finite-path closure, mode conversion, and same-target transfer were all audited. Only TOPrank/profile/Charge source feasibility blocks acceptance.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `step_001`: E_cond | Singular-value support for all later bridges | Gaussian perturbation plus Weyl | Primitive assumptions | Exact claim type | Realized Euclidean matrices | Perturbation below half margin | Exports factor bounds | Exact realized factors | Initialization-only | Primitive -> step_001 | Consumers 002,003,012,014 | Zero smoothing/lower margin pass | `step-local` | None |
| `step_002`: normalized Gram, raw scale, E_size, span | Gram nondegeneracy and entry control | Gaussianization, covariance/chi concentration | Step 001 plus primitive initialization | Exact normalized and raw claims | Exact \(G_{raw}=(r/n)^2G_{norm}\), same span | Constant normalized margin and size threshold | Exports exact scale and actual balanced columns | No target residual | Finite initialization union | 001 -> 002 | Consumers 003,009,010,011 | Arbitrary large n and maximal k pass | `step-local` | None |
| `step_003`: quotient/TC/radial/trace/local sector | Algebraic and fixed-sector moment reduction | Direct quotient algebra, Bernstein, Gaussian conjugation, Wick | Steps 001--002 and primitive rank/initialization | Matches listed reductions | Raw coefficient and trace-one Gaussian conventions | Quotient margin, TC, radial factor, loop weight, local sector | Exact outputs, excluding TOP/ENUM | Gaussian conjugation returns to raw operator | Fixed-order finite diagrams | 001,002 -> 003 | Isotropic, one-color, p=1, maximal rank are stated | `step-local` | None |
| `step_004`: Gamma(D) schema | Exact generated diagram bookkeeping | Finite half-edge/graph construction | Derived step 003 | Exact object and loop identities are intended | Same trace colors, labels, matchings, and raw convention | Exact (F_M)-to-loop identity, empty-mode/root rules | Gamma(D) includes all fields consumed downstream | N/A, no surrogate | Finite diagram datum | 003 -> 004 | Empty modes, one-color, repeated labels, p=1 specified | `step-local` | None |
| `step_005`: TOPrank/TOP | Topology face budget | Proposed \(\mathbb F_2\) incidence/interlace factorization | Novel direct derivation, not yet proved | Target consequence is correct, source identity incomplete | Q/R/L/N dimensions, entries, and root/global relation missing | Rank(Q)=B-2, rank(R_M)=p-F_M, then subadditivity | K and H_M are available, but factorization is not yet feasible | N/A, exact combinatorial object | High-degree/repeated-label defects | 004 -> 005 | Boundary cases listed, not discharged | `sketch/interface defect` | `/proof-sketch` |
| `step_006`: profile partition/inverse | Exact disjoint/exhaustive diagram classification | Lexicographic forest and two-pair-switch encoding | Novel direct derivation, inverse unproved | Profile target is plausible but canonical map incomplete | Exact admissible switch set and reconstruction convention missing | (a+b+c=2p), local sector ((2p,0,0)), cross-visit (b+c\ge1) | Gamma(D) is raw input; profile export not yet feasible | N/A, partition before weights | Repeated labels and nonlocal pairings | 004,005 -> 006 | First switch, one repeat, all-distinct, maximal collision listed | `sketch/interface defect` | `/proof-sketch` |
| `step_007`: Charge | Profile multiplicity/face-loss control | e, sigma, forest/switch counts, nonleading-slot rule | Novel direct derivation, inequality unproved | Desired inequality is restated, not derived | Same F, v, B conventions, but constants/profiles not linked algebraically | Charge must turn (r^{F-4p}k^v) and all counts into three profile factors | Exact moment intended, but raw count-to-output relation missing | Finite p multiplicity; no leading-log loss not shown | 003,005,006 -> 007 | First cross-visit and maximal k listed | `sketch/interface defect` | `/proof-sketch` |
| `step_008`: ENUM and 21z | Full Gaussian trace moment | Profiles plus Charge and multinomial theorem | Depends on unresolved 005--007 | Exact target rate and prefactor | Same Gaussian trace moment | Multinomial sum; no p factor on leading slot | Full trace moment only after Charge; C_G provenance missing | Exact target moment intended | Finite fixed-order sum | 003--007 -> 008 | p0, all-distinct, maximal collision listed | `sketch/interface defect` | `/proof-sketch` |
| `step_009`: TF, quotient coercivity, W0, deficit | Frame-to-deficit assembly | 21z, radial transfer, TC, Markov, projection | Derived 003/008 outputs | Correct conditional claim | Same quotient/raw target | Corrected C_TF, TF <= 1/2, projection energy <= r/2 | Complete once 21z exists | Exact raw projection identity | Finite event union | 003,008 -> 009 | Threshold and stationary cases pass conditionally | `step-local` | None |
| `step_010`: E_init_norm probability | Generated confidence closure | Constituent events and union/tower | Derived 001,002,009 | Exact event | Joint law | Failure <= r^(-10) | Exact intersection | No proxy | Finite union | 001,002,009 -> 010 | All allowed n,k | `step-local` | None |
| `step_011`: path convergence/radius | All-time conditional closure | E_size plus finite E_path | Explicit C_path and derived size | Exact trajectory claim | Actual d_bal and factors | Tail-sum Cauchy; radius <=3 | Exact limit and radius | No surrogate | Increments charged once | 002,010,C_path -> 011 | Zero update and max budget pass | `step-local` | None |
| `step_012`: Taylor remainder | Nonlinear residual control | Trilinearity and left-inverse bounds | Derived 001,011 | Exact raw coefficient map | Raw Frobenius norm | C_CP d^2, R=3 | Exact remainder | Same raw target | One endpoint term | 001,011 -> 012 | Zero displacement pass | `step-local` | None |
| `step_013`: coefficient margin | Preserve initial normal obstruction | W0 orthogonality plus Taylor absorption | Derived 009,011,012 | Exact margin | Raw tangent span/residual | C_CP E_star^2 <= delta0/16 | Exact residual floor | One endpoint defect | 009,011,012 -> 013 | Zero path preserves margin | `step-local` | None |
| `step_014`: physical loss | Same-target physical bridge | Projection identity and singular values | Derived 001,013 | Exact physical objective claim | Same realized T and Frobenius norm | Explicit epsilon0(kappa) | Exact mode-span equality | Orthogonal discard is nonnegative | One-time limit | 001,013 -> 014 | Lower singular boundary pass | `step-local` | None |
| `step_015`: final probability | Event inclusion and conditional theorem | Conditional identity | Derived 010,011,014 | Exact final statement | Joint event law | Retain P(C_path | E_init_norm) | Actual F_plus event | No proxy | All-time scope remains conditional | 010,011,014 -> 015 | Zero conditional factor handled | `step-local` | None |

## Blocking Issues

1. **TOPrank source/interface remains incomplete.** The sketch names (Q,L_M,R_M,N_M) and the relation (Q=\sum_M L_MR_MN_M), but does not define their dimensions, entries, projections, or the exact root/global-tour deletion. It also treats \(\operatorname{rank}(R_M)=p-F_M\) as a current-notation circuit-nullity identity without a source statement or derivation. The downstream TOP inequality therefore still has a first-source gap. Smallest repair: specify the matrices and prove or restate the exact rank identity and factorization, including all boundary branches.
2. **The profile inverse is still a plan, not an exported bijection.** “Lexicographically first two-pair-switch sequence” does not define the admissible switch set, minimality criterion, termination invariant, or reconstruction of the original component word and Wick matching. The profile statistics (a,b,c) and the claim (a+b+c=2p) need an exact inverse proof interface. Smallest repair: formalize the greedy algorithm and its inverse on every Gamma(D), including all-distinct, one-repeat, first-switch, and maximal-collision cases.
3. **Charge does not yet establish the no-log-leading moment bound.** The bounds (N_{labels}\le k^v\binom{2p}{a,b,c}), (N_{switches}\le(8p)^b(16p)^c), and the displayed Charge inequality are stated, but the exponent algebra linking (e,\sigma,F,B,v) to the three profile factors is absent. The constants (C_0,C_1,C_2,C_G) have no declared dependence or construction. Without this relation, a hidden (p) factor may still multiply the leading \(\sqrt{k}/r\) slots, so (ENUM) and (21z) are not feasible exports.
4. **Downstream consequence.** Until steps 005--008 are repaired, step 009 cannot export TF, quotient coercivity, (W_0), or \(\mathcal E_{\rm deficit}\) with the claimed (r^{-20}) failure budget. Steps 010--015 are otherwise viable and should remain unchanged.

## Required Repair Bundle

1. Complete the TOPrank interface: define (Q,R_M,L_M,N_M) in current notation with dimensions and entries; state the exact circuit-nullity/interlace identity; prove the factorization and rank subadditivity; and discharge degree-two, (B\le2), one-color, empty-mode, repeated-label, and high-degree cases. Preserve TOP exactly.
2. Complete the profile producer: define the canonical switch operation and admissible sequence, prove termination and uniqueness, state the reconstruction invariant, and prove the profile partition is disjoint and exhaustive with (a+b+c=2p) and (b+c\ge1) outside the local sector.
3. Complete Charge: derive every label, forest, and switch multiplicity bound from the profile statistics; expose the exact exponent inequalities using (e=(3p+2-v)-F) and \(\sigma=B-v\); declare (C_0,C_1,C_2,C_G\) as functions of (kappa) only; and prove that all (p)-dependent factors are attached to (b,c), never the leading (a)-slots. If this requires another split, revise the roadmap rather than treating Charge as a target assertion.
4. Keep step 008's multinomial specialization and step 009's corrected Markov/radial/TC assembly separate. Preserve (21z), the \(\sqrt{k}/r+\sqrt{\log r/r}\) TF rate, failure budgets, raw quotient, (W_0), and the exact physical-loss conclusion.
5. Keep generated-output flow and future-failure routing explicit: a TOPrank counterexample, failed inverse, or failed Charge/no-log sum routes to `/proof-sketch`; use idea revision only if evidence shows no same-setting interface can support (21z). Do not weaken the theorem contract.
6. Target-preserving repair check: all repairs are source-interface, decomposition, or count-derivation changes under the unchanged setting and goal. No new primitive assumption, algorithm change, rank-window reduction, probability weakening, metric change, or baseline loss is authorized.

## Review Rationale

Attempt 3 is a substantial and faithful repair of the previous sketch. The 15-step dependency graph, exact diagram output, profile target, separate Charge step, and downstream TF/deficit assembly are now visible. Nevertheless, TOPrank, the profile inverse, and Charge are still the first sources of theorem-critical topology and multiplicity control. Their current descriptions are useful proof plans, but the missing matrix definitions, inverse invariant, exponent/count relations, and constant provenance prevent classification as `step-local` under the source-to-claim and exported-interface gates. The theorem remains viable under same-setting repair, so `REVISE_SKETCH` with score 7 and `/proof-sketch` is the deepest required change; `IDEA_FAIL` is not justified.
