# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Reviewed artifact: `perspective_3/idea_3/proof_sketch.md`
- Goal mode: exact-goal mode
- Progress type: `conditional`
- Roadmap summary: prove a scale-correct high-probability normalized initialization certificate; produce a raw tangent-normal deficit through a gauge-fixed Terracini frame; use the explicitly conditional finite balanced path length to obtain a bounded factor limit; preserve the deficit through a one-shot raw CP Taylor expansion; transfer it by the exact mode-span identity to a relative physical loss floor; and retain the unresolved path-certificate probability exactly.

## Sketch Viability Score

8

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch targets the exact conditional theorem in `setting.md`, including every quantifier over `n,r,k`, the well-conditioned deterministic bases, the balanced simultaneous-GD protocol, the normalized initialization event, convergence on `C_path`, the relative positive physical-loss floor, and the exact unresolved factor `P(C_path | E_init_norm)`. It does not promote the branch to constant-probability or multi-method progress.
- Dependency audit: The nine-step graph is acyclic and complete. `step_001`--`step_004` produce the initialization event; `step_005` produces the actual factor limit and radius; `step_006`--`step_008` produce the raw remainder, limiting coefficient margin, and exact physical loss; `step_009` performs only event assembly. Every dependency points to an earlier step and every generated output has a legal producer-consumer path.
- High-risk coverage: The sketch exposes the exact normalized-to-raw Gram factor, the gauge quotient, the two tangent-frame estimates (TF)--(TC), the finite-path accumulation relation, the no-`k`-loss Taylor interface, the raw residual preservation inequality, and the same-target physical projection. The hard random-geometry calculation is localized to `step_003` with a concrete source and boundary scaling.
- Explicit-rate coverage: Exposed variables, hidden-constant dependence, probability mode, horizon mode, norm mode, fixed quantities, the `r^{-20}` constituent budgets, the `r^{-10}` public confidence, `E_star`, `C_CP`, and `epsilon_0(kappa)` are declared. The only unresolved probability is displayed rather than absorbed.
- Assumption and citation plausibility: All primitive assumptions are cited by stable IDs. The parent papers are contextual only; no theorem-critical external result is consumed. Every critical source is a current-notation direct derivation, standard finite-dimensional completeness fact, or exact algebraic identity with its convention and downstream interface fixed.

## Early Obstruction Audit

- Limiting-case stress: Arbitrarily large allowed `n` no longer causes dilution because `G_raw = (r/n)^2 G_norm` is theorem-facing. At `k=r^{5/4}`, the proposed Terracini off-block scale is `O_kappa(r^{-3/8}+sqrt(log(r)/r))=o(1)`, while (TC) gives `O_kappa(r^{1/4}+log r)=o(r)`. Zero path length, zero displacement, maximal permitted path budget, zero smoothing, and the realized singular-value boundary preserve the claims.
- Theorem-critical bridge support: Every nontrivial bridge is assigned and source-supported: base to realized conditioning (`step_001`), normalized initialization to exact raw scale and tangent span (`step_002`), gauge-fixed angular geometry to the raw deficit (`step_003`), finite path to convergence and radius (`step_005`), raw trilinearity to endpoint remainder (`step_006`), tangent cancellation to limiting raw margin (`step_007`), and coefficient residual to physical loss (`step_008`).
- Exported-interface feasibility: The normalized Gram export uses the exact multiplicative transfer and does not transform `D_r`, `Dhat_0`, or the loss metric. The frame export fixes the quotient Gram margin, off-block defect, target-correlation defect, and projection formula. The path export uses the actual balanced trajectory and metric. The Taylor and physical exports use the exact raw coefficient map and realized target. Controlled and uncontrolled defect classes are separated.
- Theorem-critical mechanism witness gate: Passed. For `step_003`, the claim class is a structural lower residual. The source is the centered off-component gauge-fixed Terracini frame under independent conditioned elliptic angular laws. The within-component quotient Gram has lower bound one; off-component blocks have variance scale `k/r^2`, yielding (TF), and `J*D_r` is controlled by Hadamard-product energies of total scale `k/r`, yielding (TC). These inequalities leave a fixed normal margin at the maximal-rank boundary.
- Entry-state trace stress: The initialization certificate and `W_0` are produced at the actual balanced `theta_0` before any update. A stationary first update has zero path charge and immediate convergence; a nonzero update is charged once to the same summable path series. At zero displacement the Taylor remainder is zero and the initial raw deficit transfers unchanged to physical loss.
- Obligation locality classification: Realized conditioning (`step_001`), normalized Gram and size (`step_002`), quotient-frame conditioning and target correlation (`step_003`), initialization probability (`step_004`), finite-path convergence and boundedness (`step_005`), raw Taylor remainder (`step_006`), limiting coefficient margin (`step_007`), physical bridge (`step_008`), and conditional probability assembly (`step_009`) are all `step-local`. No sketch/interface or theorem-contract defect remains.
- Noncircular closure gate: Passed. The only all-time source is the explicitly conditional numerical budget `E_path <= E_star`, which contains neither convergence nor boundedness. Tail sums independently produce the Cauchy property and radius bound before either is consumed. Initialization events are produced from primitive randomness before assembly, and no final-loss consequence constructs `W_0`.
- Mechanism-source and boundary stress: The base singular margin survives zero smoothing; normalized covariance stays constant-scale as `n` grows; the quotient Gram stays separated at `k=r^{5/4}`; the target projection remains `o(r)`; and the path/Taylor threshold leaves positive slack. If a source vanishes, the sketch identifies the downstream failure rather than treating an upper bound or generic smallness as a lower-margin source.
- Generated-output flow: Passed. Each generated event, frame certificate, limit, radius, remainder, coefficient margin, physical floor, and probability conclusion has a named producer, all consumers, a valid earlier-step dependency path, and the correct `derived` or `explicitly conditional` provenance.
- Source-to-claim adequacy: Direct perturbation supplies singular-value bounds; conditioned sphere/elliptic laws supply normalized Gram control; the gauge-fixed frame supplies a lower projection residual rather than only a dimension count; finite total variation supplies actual convergence; trilinearity supplies the exact second-order remainder; and the projection identity plus tensor-product singular values supplies the same-target physical lower bound. Coordinate, metric, normalization, and realized-factor conventions agree at every consumer.
- Residual-to-target adequacy: The normalized Gram transfers exactly by `(r/n)^2`; its only target-side use is span/frame conditioning. The tangent residual obeys `P_{S0-perp}(D_r-Dhat_0)=P_{S0-perp}D_r` exactly. The endpoint bridge leaves only the one-shot Taylor remainder, dominated by `C_CP E_star^2`. The physical bridge is an equality after mode-span projection, and the discarded orthogonal part only increases the full residual norm.
- Baseline invariance audit: Passed. The inherited `idea_2` baseline is that certificate normalization changes only Gram scale. Exact span equality, raw target, raw initial core, raw residual, and physical loss are preserved. The zero-path specialization gives `theta_inf=theta_0`, zero Taylor remainder, the full initial deficit, and the same physical lower-bound route.
- Scope-accumulation compatibility: Passed. Initialization concentration uses finite unions. Along the trajectory, the accumulated defect is the nonnegative increment series; each increment is charged once, every tail tends to zero, and the total is bounded by `E_star`. The Taylor defect is evaluated once at the endpoint and controlled by squared total displacement.
- Scope and dependence consistency: Estimates are uniform over the stated bases and every `n >= C(kappa,q) r^4 log r`. Initialization-geometry constants may depend only on `kappa,q`; the deterministic loss constant depends only on `kappa`. No `n`, `k`, horizon, or conditional-probability dependence is hidden.
- Generated-condition provenance: The four initialization constituents, all-time radius, convergence, tangent margin, and loss floor remain derived outputs. Only `C_path` is a conditional theorem hypothesis, exactly as authorized by the goal, and it contains none of its downstream consequences.
- Citation and tool applicability: No theorem-critical citation is invoked. In `step_003`, the local proof should establish the frame/correlation events under realized-factor conditioning and then intersect with the normalized Gram event; it need not condition the angular law on that generated event. This is local proof organization, not an interface defect.
- Same-setting repair plausibility: No repair is required. Failure of the displayed (TF) or (TC) estimate during local proof would route first to `/proof-step step_003` if the same interface remains valid, or to `/proof-sketch` only if the interface must change; no current boundary or convention check exposes such failure.
- Target-preserving bridge-repair gate: Passed. The prior raw-scale obstruction is repaired by an exact normalized certificate while preserving the raw theorem target. No further theorem-contract change is needed.
- High-risk obligation classes: Structural nondegeneracy, anisotropic random-frame conditioning, target correlation, confidence conversion, all-time finite-budget closure, nonlinear remainder control, asymptotic mode conversion, relative-rate specialization, baseline preservation, and conditional probability accounting all have explicit sources and interfaces.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Realized-factor conditioning `E_cond` | Structural singular-value support for coefficient and physical bridges | Gaussian perturbation of conditioned bases | Primitive `assump:base_conditioning`, `assump:dimension`, `assump:gaussian_smoothing`; direct Weyl argument | Supplies required upper and positive lower singular-value bounds | Exact realized matrices and Euclidean operator norm | `||Xi_M||_op <= (2 kappa^2)^{-1}` gives `sigma_min(M) >= (2 kappa^2)^{-1}` and `||M||_op <= 2 kappa^2` | Base margin and Gaussian tail export the event and `||M^dagger|| <= kappa_1` | Perturbation residual is `Xi_M` and is below half the base margin | Initialization-only | Primitive base plus smoothing -> `step_001` -> all consumers | Zero smoothing and lower-margin boundary preserve event | `step-local` | None |
| Normalized pair-Gram event and raw-scale identity | Structural Gram nondegeneracy; scale-correct initialization certificate | Balanced radius-direction decomposition and conditioned elliptic covariance | `assump:independent_initialization` plus `step_001`; direct concentration | Constant normalized covariance and `k/r^2=o(1)` support polynomial spectral window | Exact normalized coefficient and Euclidean Khatri-Rao convention | `K_raw=(r/n)K_norm`, `G_raw=(r/n)^2G_norm`, normalized fluctuation `o(1)` | Conditioned left-inverse bounds, chi radii, and independent directions export all three normalized Gram windows | Exact multiplicative transfer; no additive residual; `S0_raw=S0_norm` | Finite union; arbitrary large `n` handled exactly by normalization | `step_001 -> step_002 -> step_003/step_004` | Normalized scale remains constant as `n` grows; raw scale is reported exactly | `step-local` | None |
| Initial-size event `E_size` | Generated entry bound for all-time Taylor radius | Chi concentration for balanced common radius `g_i` | `assump:dimension`, `assump:rank_window`, `assump:independent_initialization` | Directly bounds actual balanced columns | Same balanced representatives used by `d_bal` and `Psi` | `max_i g_i <= 2` with failure at most `r^{-20}` | Gaussian radii export `max_{i,m} ||m_{i,0}|| <= 2` | Produced and consumed columns coincide | Initialization-only finite union | Primitive initialization -> `step_002` -> `step_005` | Maximal `k` is dominated by `n >= C r^4 log r` | `step-local` | None |
| Gauge-fixed Terracini conditioning (TF) | Structural coercivity modulo CP gauge; enables projection control | Within-component quotient Gram plus centered off-component angular blocks | `assump:rank_window`, `assump:independent_initialization`, `step_001`; direct decoupled frame calculation | Correct lower/coercive source, not merely an upper bound | Unit rescaling preserves raw tangent span and coefficient inner product | Quotient diagonal Gram has minimum at least 1; off-block norm is at most `C_kappa(sqrt(k/r^2)+sqrt(log r/r)) <= 1/2` | Independent conditioned elliptic directions and `k/r^2 <= r^{-3/4}` export `||(J*J)^dagger|| <= 2` | Same tangent operator is used by projection formula | Centered blocks have variance scale `k/r^2`; initialization-only failure budget `r^{-20}` | Primitive directions plus `step_001` -> `step_003` before `W_0` use | At maximal `k`, defect is `o(1)`; gauge kernel is removed exactly | `step-local` | None |
| Diagonal-core correlation (TC) and tangent deficit | Structural normal-margin lower bound; positive source for final loss | Hadamard-product correlation plus quotient pseudoinverse formula | Same direct `step_003` angular calculation | Upper tangent projection yields lower normal residual | `D_r`, `Dhat_0`, `S0`, and Frobenius pairing remain raw | `||J*D_r||^2 <= C_kappa(k/r+log r)` and `||P_S0 D_r||^2 <= 2 C_kappa(k/r+log r) <= r/2` | Independent mode directions, quotient margin, and rank window export unit `W_0` with margin at least `delta_0 sqrt(r)` | `Dhat_0 in S0` gives exact same-target projection equality | Initialization-only correlation sum of scale `k/r` | `step_001/step_002 -> step_003 -> step_004/step_007` | At maximal `k`, `k/r+log r=o(r)`; loss of quotient margin would block deficit | `step-local` | None |
| Combined initialization probability | Confidence-explicit generated-event closure | Earlier constituent estimates and tower/union bound | Derived `step_001`--`step_003` outputs | Produces exact event named by theorem | Joint law and factor-conditional estimates agree | Four failure terms total at most `4 r^{-20} <= r^{-10}` | Each constituent has a legal producer and budget; export is exactly `E_init_norm` | No enlarged event | Finite union only | `step_001,step_002,step_003 -> step_004 -> step_009` | Uniform at maximal `k`, minimal `n`, and arbitrary larger `n` | `step-local` | None |
| Finite-path convergence, displacement, and radius 3 | All-time boundedness and asymptotic convergence under explicit conditional source | Summable balanced increment lengths plus initial size | `C_path`, `step_002`, `assump:gd_step` | Finite total variation directly supplies convergence and norm control | Actual iterates and same `d_bal` metric | `d(theta_u,theta_t) <= sum_{s=t}^{u-1} d(theta_{s+1},theta_s) -> 0`; column norms at most `2+E_path <= 3` | `E_size` and `E_path <= E_star` export actual limit, displacement at most 1, and radius 3 | No surrogate limit or metric residual | Each nonnegative increment charged once; tail vanishes | Conditional budget -> `step_005` -> `step_006`--`step_009` | Zero first update converges immediately; maximal budget still gives radius 3 | `step-local` | None |
| Raw CP Taylor remainder | Second-order control of nonlinear endpoint motion | Exact trilinear expansion and left-inverse bounds | `step_001`, `step_005`; direct algebra | Exactly supplies quadratic/cubic defect consumed by margin step | Exact raw `Psi` and coefficient Frobenius norm | Three quadratic terms at most `3 kappa_1^3 R d^2`; cubic at most `kappa_1^3 d^3 <= kappa_1^3 d^2` | Radius 3, displacement at most 1, and left-inverse bounds export `C_CP d^2` with no `k` factor | Produced remainder is exact residual beyond derivative | One-shot endpoint defect | `step_001 + step_005 -> step_006 -> step_007` | Remainder vanishes at zero displacement; cubic conversion holds at `d=1` | `step-local` | None |
| Limiting raw coefficient margin | Structural lower residual after full path | Initial `W_0`, tangent orthogonality, endpoint displacement, Taylor bound | `step_003`, `step_005`, `step_006` | Positive source and nonlinear defect are in same norm | Same raw residual `D_r-Psi(theta_inf)` throughout | Margin at least `delta_0 sqrt(r)-C_CP E_star^2 >= (15/16) delta_0 sqrt(r)` | Initial deficit and exact remainder export limiting coefficient floor | Only residual is dominated Taylor remainder; derivative and `Dhat_0` cancel exactly | Whole path enters once through endpoint displacement | `step_003 + step_005 + step_006 -> step_007 -> step_008` | Zero path preserves full deficit; maximal `E_star` leaves slack | `step-local` | None |
| Exact physical bridge and relative loss floor | Same-target physical lower bound and quantitative specialization | Mode-span projection identity plus tensor-product singular values | `step_001`, `step_007`; exact identity | Coefficient lower bound transfers to physical claim type | Produced object is projection of residual for same realized `T` | `F(theta_inf) >= kappa_1^{-6}(15 delta_0/16)^2 r` and `||T||_F^2 <= kappa_1^6 r` | Coefficient margin and factor singular bounds export `epsilon_0(kappa)=kappa_1^{-12}(15 delta_0/16)^2` | Projection discards only nonnegative orthogonal component | One-time limit comparison | `step_001 + step_007 -> step_008 -> step_009/final theorem` | Lower singular boundary leaves positive constant; zero path uses same bridge | `step-local` | None |
| Final conditional probability interface | Event closure retaining sole source-level gap | Deterministic inclusion and conditional-probability identity | `step_004`, `step_005`, `step_008` | Supplies exactly conditional conclusion, not constant probability | Actual convergence and physical positive-loss event | `P(F_+) >= P(E cap C) = P(E)P(C|E) >= (1-r^{-10})P(C|E)` | Exact initialization and path events export the retained conditional factor | Uncontrolled `P(C|E)` remains explicit and multiplicative | All-time dependence stays inside authorized conditional event | `step_004 + step_005 + step_008 -> step_009 -> final theorem` | If `P(C|E)=0`, theorem correctly gives zero unconditional progress | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is ready for theorem-level and step-level proof work. The prior theorem-contract failure is repaired by an exact normalized-to-raw scale interface without changing the raw target or physical metric. The only unusually difficult obligation is `step_003`, but it is not an unexposed mechanism: the quotient convention, within-block coercivity, centered off-block variance scale, diagonal-core correlation scale, maximal-rank boundary, projection formula, probability budget, and consumers are fixed at obstruction-level granularity. Its remaining work is a genuine local derivation under the unchanged setting. Finite-path accumulation, baseline preservation, Taylor residual adequacy, same-target physical transfer, generated-output flow, and conditional probability scope all pass with explicit noncircular sources.
