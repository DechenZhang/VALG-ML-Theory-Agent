# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Reviewed artifact: `perspective_3/idea_2/proof_sketch.md`
- Goal mode: exact-goal mode
- Roadmap summary: derive a high-probability initial coefficient certificate, use the explicitly conditional finite balanced path length to obtain a factor limit, preserve an initial normal residual through a second-order CP Taylor bound, and transfer that residual through the exact mode-span projection to a relative physical loss floor.

## Sketch Viability Score

4

## Sketch Review Status

IDEA_FAIL

## Smallest Retry Target

`/subagent-idea-generator`

## Retry Mode

`new_idea`

## Audit Summary

- Goal alignment: The sketch targets the exact two-part goal in `setting.md` and accurately preserves its conditional progress label and remaining path-probability gap. Its deterministic finite-path-to-positive-loss part is aligned. The initialization part, however, proves that the exact theorem-facing raw-Gram certificate cannot have the required probability uniformly over the stated dimension range.
- Dependency audit: The conceptual order is acyclic: initial certificate -> finite-path limit -> Taylor residual -> physical loss -> probability accounting. The artifact deliberately stops before giving stable proof-step IDs because the first block refutes the exact goal. Consequently it is not ready for step dispatch even apart from the theorem-contract failure.
- High-risk coverage: The sketch correctly exposes the raw-versus-normalized coordinate scale, the finite-path accumulation mechanism, first-order tangent cancellation, quadratic remainder budget, and exact same-target projection bridge. The raw-Gram lower bound is a verified obstruction, not an unresolved hard lemma. The tangent-deficit producer remains only an intended direct random-geometry derivation and would need a concrete normalized interface in any repaired branch.
- Explicit-rate coverage: The sketch declares all relevant dependence and gives the decisive bound
  \[
    \mathbb P(\mathcal E_{\rm gram}\mid A,B,C)
    \le C_\kappa r^{22}/n^2
  \]
  on `\(\mathcal E_{\rm cond}\)`. Taking the allowed value `\(n=r^{12}\)` makes the probability at most `\(C_\kappa r^{-2}\)`, contradicting the requested lower bound `\(1-r^{-10}\)` for all large `\(r\)`.
- Assumption and citation plausibility: The six setting assumptions are cited by stable IDs and the contextual papers are not used as theorem-critical authorities. Realized-factor conditioning and the deterministic Taylor/projection blocks have plausible current-convention direct sources. No cited result supplies, or could override, the missing `\((r/n)^2\)` factor in the raw Gram.

## Early Obstruction Audit

- Limiting-case stress: The allowed regime `\(n=r^{12}\)` is fatal. Conditional on any realized `\(A,B,C\in\mathcal E_{\rm cond}\)`, balanced Gaussian initialization gives `\(\mathbb E\|B^\dagger u\|_2^2,\mathbb E\|C^\dagger u\|_2^2=O_\kappa(r/n)\)`, so the average raw Khatri--Rao Gram eigenvalue has expectation `\(O_\kappa(r^2/n^2)\)`. The fixed lower threshold `\(r^{-20}\)` therefore fails with probability tending to one in this permitted dimension regime.
- Theorem-critical bridge support: The finite-path, Taylor, and physical-loss bridges are source-adequate at sketch granularity. The bridge from normalized coefficient geometry to the exact raw event is not: it is exactly
  \[
    G_{\rm raw}=(r/n)^2G_{\rm normalized},
  \]
  and the target discards this theorem-critical scale factor.
- Exported-interface feasibility: `\(\mathcal E_{\rm gram}\)` must export a raw lower eigenvalue `\(r^{-20}\)` to `\(\mathcal E_{\rm init}\)` and the final high-probability statement. Available raw controls export only scale `\(O_\kappa((r/n)^2)\)`. No positive margin dominates the missing factor for all `\(n\ge C r^4\log r\)`. By contrast, the conditional path event exports finite total variation and bounded factors exactly to the convergence and Taylor consumers, and the mode-span projection exports the coefficient residual to the same physical residual target.
- Theorem-critical mechanism witness gate: It fails for the initial raw-Gram nondegeneracy claim. The direct Gaussian mechanism has the right qualitative nondegeneracy but the wrong quantitative scale, and dimension dilution is an opposing defect that cannot be absorbed under the declared unbounded-above dimension scope. This is an `idea/theorem-contract defect`. The finite-path convergence, Taylor residual, and projection witnesses pass at obstruction level and are reusable after a new setting is formalized.
- Entry-state trace stress: The Gram obstruction is already active at `\(t=0\)`, before any GD update, so dynamics cannot repair the required initial event. For the retained conditional trajectory mechanism, a zero first update yields a stationary convergent path, while a nonzero first update is charged to the explicitly finite series defining `\(E_{\rm path}\)`; neither case creates circular convergence. The Taylor mechanism consumes the initialized normal witness only after the finite-path limit is produced.
- Obligation locality classification: realized-factor conditioning is `step-local`; the exact raw-Gram high-probability event and hence the combined initial-certificate probability are `idea/theorem-contract defect`; the tangent-deficit random-geometry interface is presently a `sketch/interface defect` because its exact normalized Gram/projection control is not exposed; conditional finite-path convergence, Taylor preservation, the mode-span transfer, and the exact conditional-probability identity are `step-local` under their stated conditional sources.
- Noncircular closure gate: The path certificate is an explicitly conditional theorem hypothesis and does not contain convergence, positive loss, or a residual barrier. Its finite total variation noncircularly produces convergence. The initial Gram event has a noncircular direct producer calculation, but that calculation refutes rather than proves the demanded output.
- Mechanism-source and boundary stress: The only source for raw Gram nondegeneracy is Gaussian initialization passed through well-conditioned left inverses. It produces column norm scale `\(r/n\)` per coefficient pair and Gram scale `\((r/n)^2\)`. At the permitted boundary `\(n=r^{12}\)`, this source is too small; the theorem-critical high-probability conclusion remains false. Neither small step size nor later balancing changes this time-zero scale.
- Generated-output flow: `\(\mathcal E_{\rm cond}\)`, `\(\mathcal E_{\rm gram}\)`, and `\(\mathcal E_{\rm deficit}\)` are intended derived initialization outputs consumed by `\(\mathcal E_{\rm init}\)`. There is no legal producer for the stated raw `\(\mathcal E_{\rm gram}\)` output. The explicitly conditional `\(\mathcal C_{\rm path}\)` produces boundedness and total-variation controls consumed by the limit and Taylor blocks; the limit and residual margin then flow legally to the projection block and final positive-loss event.
- Source-to-claim adequacy: The direct current-notation Gram calculation uses exactly the setting's raw coordinates, Euclidean inner product, Gaussian variance `\(1/n\)`, and balance map, so its convention is compatible and its adverse conclusion is decisive. A normalized-coordinate standard fact would concern a different exported event and cannot prove the raw target without the displayed transfer factor. The direct finite-dimensional completeness argument, multilinear Taylor bound supplied in the setting, and orthogonal mode-span identity match their downstream claims. The tangent-deficit claim has no theorem-critical cited dependency, but its proposed random-subspace source is too schematic for `step-local` classification until a repaired sketch states the exact normalized generator Gram and projection inequality.
- Residual-to-target adequacy: For the failed bridge, the produced control is a normalized Gram with natural constant scale, the consumed interface is the raw Gram lower threshold `\(r^{-20}\)`, the theorem norm is spectral eigenvalue scale, and the exact residual/transfer is the multiplicative factor `\((r/n)^2\)`. It is not dominated at the required target scale for unrestricted large `\(n\)`. For the surviving loss bridge, the produced coefficient residual `\(D_r-\Psi(\theta_\infty)\)` and consumed physical residual `\(T-S(\theta_\infty)\)` satisfy the exact projection identity; singular-value bounds transfer the former without an uncontrolled residual.
- Baseline invariance audit: No recovery, exact-fit, noiseless-zero-loss, or other baseline conclusion is part of this lower-bound theorem contract, so no Baseline Invariance Obligation applies. The branch's conditional label and explicit unresolved probability interface are preserved; the review does not treat them as a full source result.
- Scope-accumulation compatibility: The raw-Gram claim is initialization-only, but its requested uniformity accumulates over an unbounded-above dimension scope; the dilution factor worsens monotonically and has no compensating margin. The trajectory block is explicitly conditional on the summable forcing budget `\(\sum_t d_{\rm bal}(\theta_{t+1},\theta_t)\le E_\star\)`: tail distances are bounded by tails of this nonnegative convergent series, and the total displacement charges once to `\(E_\star\)`. The Taylor remainder then charges once to `\(C_{\rm CP}E_\star^2\)`.
- Scope and dependence consistency: The contradiction respects all fixed quantities and hidden-constant rules. It uses an allowed `\(n\)`, every allowed `\(k\)` is irrelevant to the average-eigenvalue upper bound, and `\(C_\kappa\)` is independent of `\(n,r,k\)`. Repairing the event by normalization, imposing `\(n=O_\kappa(r^{11})\)`, or changing initialization variance respectively changes the theorem-facing certificate, dimension scope, or primitive procedure.
- Generated-condition provenance: The sketch correctly refuses to promote `\(\mathcal E_{\rm init}\)` to a primitive assumption. The all-time path clauses are legal because the formalized theorem is explicitly conditional on `\(\mathcal C_{\rm path}\)`. No positive-loss or convergence fact is hidden in that certificate.
- Citation and tool applicability: The NeurIPS 2020 and STOC 2014 papers are contextual provenance only; no theorem label from either is consumed. Thus no citation-identity defect can rescue or obscure the direct scale contradiction. All surviving mechanisms are framed as current-notation direct derivations.
- Same-setting repair plausibility: No same-setting proof-sketch repair can establish the false event probability. Replacing the event by normalized Grams, using raw thresholds `\((r/n)^2[r^{-20},r^{20}]\)`, or deleting an unused absolute-scale condition changes `\(\mathcal E_{\rm init}\)`, `\(\mathcal C_{\rm path}\)`, and the exact formalized probability target. Restricting `\(n\)` or rescaling initialization changes other primitive parts of the theorem contract.
- Target-preserving bridge-repair gate: The exact transfer factor has already been exposed and shows that target preservation is impossible. A bridge step cannot turn normalized constant-scale control into the fixed raw lower threshold uniformly in `\(n\)`. The smallest repair is a minimal-delta new idea/formalization that rescales the certificate while preserving the GD protocol, conditional path mechanism, rank window, and positive-loss conclusion.
- High-risk obligation classes: Structural nondegeneracy and confidence conversion fail at theorem-contract level; the tangent-normal deficit remains interface-level; recursive convergence, quadratic perturbation control, generated-output flow, and coefficient-to-physical target transfer pass at obstruction level under the explicit conditional path source.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Realized-factor conditioning `\(\mathcal E_{\rm cond}\)` | Structural singular-value bounds; constituent of initial certificate | Gaussian perturbation of uniformly conditioned deterministic bases | Primitive `assump:base_conditioning`, `assump:dimension`, `assump:gaussian_smoothing`; direct perturbation derivation | Adequate: small operator perturbation preserves full rank and the stated loose `\(2\kappa^2\)` bounds | Exact branch matrices, normalization, and Euclidean operator norm; no surrogate target | `\(\sigma_{\min}(\bar M+\Xi)\ge\sigma_{\min}(\bar M)-\|\Xi\|_{op}\)` and analogous upper bound | Raw Gaussian operator-norm control exports exactly the event consumed by `\(\mathcal E_{\rm init}\)` | No transformed target; perturbation residual is `\(\Xi\)` and is dominated for large `\(r\)` because `\(\rho=r^{-q}\)` | Initialization-only; no temporal accumulation | Primitive bases + smoothing -> perturbation bound -> `\(\mathcal E_{\rm cond}\)` | Direct producer; consumers are coefficient definitions, all later initialization blocks, and final certificate | Zero-smoothing limit recovers the conditioned base; small smoothing preserves the margin | `step-local` | `None` |
| Raw Khatri--Rao Gram event `\(\mathcal E_{\rm gram}\)` | Structural lower/upper spectral bound; required constituent of `\(\mathcal E_{\rm init}\)` | Balanced Gaussian directions passed through `\(B^\dagger,C^\dagger\)` and analogues | Primitive `assump:independent_initialization` plus derived `\(\mathcal E_{\rm cond}\)`; exact direct calculation | Failed: source scale is `\((r/n)^2\)`, not an `\(n\)`-uniform raw lower bound `\(r^{-20}\)` | Exact raw coefficient convention is used; normalized coordinates are a different interface with `\(G_{raw}=(r/n)^2G_{norm}\)` | `\(\mathbb E[k^{-1}\operatorname{tr}G_{raw}\mid A,B,C]\le C_\kappa r^2/n^2\)` and Markov gives `\(\Pr(\lambda_{min}\ge r^{-20})\le C_\kappa r^{22}/n^2\)` | Available controls cannot export the demanded raw threshold for every allowed `\(n\)`; no independent margin controls dimension dilution | Exact multiplicative transfer leaves no additive residual, but the factor `\((r/n)^2\)` is below the required scale for `\(n\gg r^{11}\)` | Initialization-only; dimension dilution is persistent and monotone in `\(n\)` | Noncircular direct producer calculation refutes the desired output | No legal producer; intended consumers are `\(\mathcal E_{\rm init}\)`, `\(\mathcal C_{\rm path}\)`, tangent geometry, and final probability bound | At allowed `\(n=r^{12}\)`, event probability is at most `\(C_\kappa r^{-2}\)` | `idea/theorem-contract defect` | `/subagent-idea-generator` |
| Tangent-deficit event `\(\mathcal E_{\rm deficit}\)` | Structural normal-margin lower bound; preserves a residual against first-order CP motion | Dimension deficit `\(3kr<r^3\)` plus random initialized coefficient directions and a generator-Gram/projection estimate | Primitive `assump:rank_window`, `assump:independent_initialization`, and derived factor conditioning | Plausible claim class, but the sketch does not expose the exact normalized generator Gram, projection inequality, or uniform anti-concentration margin needed for the fixed `\(D_r\)` target | Raw coefficient and Frobenius conventions are fixed; compatibility after a Gram rescaling must be restated in a repaired sketch | Intended relation is `\(\|P_{\mathscr S_0^\perp}D_r\|_F\ge\delta_0\sqrt r\)`; no obstruction-level probability inequality is supplied | Current raw-Gram interface is unusable; a repaired normalized Gram/projection interface may export the same deficit without changing `\(W_0\)` or the final loss metric | Projection residual to the fixed diagonal core is the target itself; adequacy is not yet demonstrated uniformly over conditioned anisotropic coordinates | Initialization-only; no temporal accumulation | Intended direct random-geometry producer precedes Taylor consumer, but its quantitative interface is incomplete | Producer would be the repaired initialization block; consumers are the Taylor cancellation and residual-margin blocks | Coordinate-orthogonal and conditioned anisotropic boundary regimes are named but not quantitatively traced | `sketch/interface defect` under a repaired theorem contract | `/proof-sketch` after idea/formalization repair |
| Combined initial certificate probability `\(\Pr(\mathcal E_{\rm init})\ge1-r^{-10}\)` | Confidence-explicit theorem closure for initialization | Intersection of the three generated initialization events plus a union/probability argument | Derived outputs of the conditioning, Gram, and deficit blocks | Failed because one required constituent has probability `\(O_\kappa(r^{-2})\)` in an allowed regime | Joint-law and conditional-on-realized-factor conventions match the setting | `\(\Pr(\mathcal E_{\rm init})\le\Pr(\mathcal E_{\rm cond}\cap\mathcal E_{\rm gram})\le C_\kappa r^{22}/n^2\)` | No raw controls can export the stated confidence uniformly over `\(n\)` | The undominated defect is dimension dilution in `\(\mathcal E_{\rm gram}\)`; no other event repairs it | Constituent-event producer path is noncircular but terminates in a refutation | Intended producers are the three initialization blocks; consumer is the public theorem and probability identity | `\(n=r^{12}\)` contradicts the requested confidence | `idea/theorem-contract defect` | `/subagent-idea-generator` |
| Conditional path certificate and factor convergence | Explicitly conditional all-time boundedness/finite-variation interface; produces finite factor limit | The theorem's explicit conditional hypothesis `\(\mathcal C_{\rm path}\)`, especially `\(E_{path}\le E_\star\)` | Explicitly conditional target authorized by `setting.md`; not claimed from primitive GD assumptions | Adequate for convergence and bounded-limit production, but not for a lower bound on the certificate's probability (which the goal expressly leaves open) | Same balanced factor representation and metric are produced and consumed | `\(d_{bal}(\theta_u,\theta_t)\le\sum_{s=t}^{u-1}d_{bal}(\theta_{s+1},\theta_s)\to0\)` | Finite total variation, positive raw norms, and `\(R\)` bound export exactly the limit and Taylor-domain interfaces | No surrogate: the balanced-factor limit is in the same metric; norm bound passes to the limit | Increment magnitudes are nonnegative and summable with finite budget `\(E_\star\)` | Conditional path event is stated before use and contains no convergence assertion | Conditional source -> convergence block -> Taylor block and final theorem | Zero first update gives immediate stationary convergence; nonzero first update is charged to the same series | `step-local` | `None` |
| Taylor preservation of the initial normal residual | Structural residual lower bound after all-time motion; key positive-loss mechanism | `\(W_0\perp\mathscr S_0\)`, finite displacement, and explicit multilinear remainder bound | Derived `\(\mathcal E_{\rm deficit}\)` plus explicitly conditional path output; direct current-notation Taylor derivation | Adequate once a legal initial deficit is produced: the derivative lies in `\(\mathscr S_0\)` and the quadratic error is below the initial margin | Exact coefficient CP map `\(\Psi_{A,B,C}\)` and Frobenius pairing used throughout | `\(\langle D_r-\Psi(\theta_\infty),W_0\rangle\ge\delta_0\sqrt r-C_{CP}E_\star^2\ge(15/16)\delta_0\sqrt r\)` | Raw controls are initial normal margin, same-target derivative cancellation, bounded factors, and total displacement; they export the coefficient residual margin | Remainder is the only residual and is dominated by the explicit `\(E_\star\)` threshold | All increments are summarized by total displacement; Taylor remainder is charged once quadratically, not accumulated per time | Deficit producer + convergence producer -> Taylor residual -> projection consumer | Stationary path leaves the full initial margin; maximal allowed displacement leaves at least the displayed margin | `step-local` after legal deficit production | `None` |
| Coefficient residual to physical relative-loss floor | Same-target bridge and quantitative specialization to public loss | Orthogonal mode-span projection and singular-value bounds on `\(A,B,C\)` | Derived `\(\mathcal E_{\rm cond}\)` and Taylor residual; direct tensor-product identity | Exact match: projection of physical residual equals image of coefficient residual | Produced and consumed objects use the same realized factors and physical Frobenius norm; no whitening or reference change | `\((P_A\otimes P_B\otimes P_C)(T-S)=(A\otimes B\otimes C)(D_r-\Psi)\)` with lower singular-value and `\(\|T\|_F^2\le\kappa_1^6r\)` comparisons | Coefficient residual and factor singular values export `\(F(\theta_\infty)\ge\kappa_1^{-12}(15\delta_0/16)^2\|T\|_F^2\)` | Projection discards only an orthogonal complement, which can reduce norm in the safe direction; no uncontrolled target residual is subtracted | One-time limit comparison; no temporal forcing | Taylor residual producer -> projection bridge -> final loss event | Nonzero coefficient margin and full column rank imply a strictly positive physical floor | `step-local` | `None` |
| Conditional probability identity and remaining source gap | Event-flow/public probability specialization | Elementary conditional probability applied to `\(\mathcal E_{\rm init}\)` and `\(\mathcal C_{\rm path}\)` | Standard fact in the exact joint probability space | Identity is adequate when `\(\Pr(\mathcal E_{\rm init})>0\)`; the advertised lower bound additionally needs the false current initial-certificate bound | Same joint law and event definitions as the setting | `\(\Pr(E\cap C)=\Pr(E)\Pr(C\mid E)\)` and `\(E\cap C\subseteq\mathcal F_+\)` | Exact event controls export the identity; no constant lower bound on `\(\Pr(C\mid E)\)` is claimed | No residual for the identity; the unresolved conditional probability is explicitly retained rather than absorbed | All-time event dependence is conditioned, not accumulated algebraically | Initialization and path events -> positive-loss event -> public probability statement | If `\(\Pr(C\mid E)=0\)`, the conditional theorem remains true but yields zero unconditional progress, as explicitly stated | `step-local` for identity; final lower bound blocked by initial certificate | `/subagent-idea-generator` for the failed initial factor |

## Blocking Issues

1. **Exact raw-Gram event and initial-certificate probability (`proof_sketch.md`, Proof Roadmap block 1).** Under the setting's raw coordinates and initialization, the average Gram eigenvalue is `\(O_\kappa(r^2/n^2)\)`. Markov's inequality gives `\(\Pr(\mathcal E_{\rm gram}\mid A,B,C)\le C_\kappa r^{22}/n^2\)`, so the allowed choice `\(n=r^{12}\)` contradicts `\(\Pr(\mathcal E_{\rm init})\ge1-r^{-10}\)`. The smallest repair is a new idea/formalization with a scale-correct certificate; no proof-sketch rewrite can prove the current exact goal.
2. **Initialization geometry interface (`proof_sketch.md`, Assumption Provenance Objectives and absent step decomposition).** Even after a theorem-contract repair, the tangent-deficit claim needs an explicit normalized generator-Gram/projection mechanism and probability bound, and all retained blocks need stable step IDs, exact claims, dependencies, assumption IDs, outputs, and review statuses. This is a sketch-level obligation only after the false certificate has first been repaired at idea/formalization level.

## Required Repair Bundle

1. **Minimal theorem-contract repair; affected item: `\(\mathcal E_{\rm gram}\)` in the idea and setting; target: `/subagent-idea-generator`.** Replace the fixed raw spectral window by a scale-correct theorem-facing certificate, for example a normalized-coordinate Gram window or the exactly equivalent raw window carrying the `\((r/n)^2\)` factor. Update every occurrence in `\(\mathcal E_{\rm init}\)`, `\(\mathcal C_{\rm path}\)`, the goal, and the probability accounting. An alternative dimension upper bound or initialization rescaling is a larger theorem-contract change and is not the preferred minimal delta.
2. **Preserve unaffected structure; affected items: algorithm, rank window, path certificate, conditional progress label, and positive-loss target; target: `/subagent-idea-generator`.** Keep the simultaneous balanced-GD protocol, `\(r<k\le r^{5/4}\)`, the explicitly conditional finite-path mechanism, the exact projection bridge, and the declared absence of a constant lower bound for `\(\Pr(\mathcal C_{\rm path}\mid\mathcal E_{\rm init})\)`. Do not weaken the positive-limit conclusion or hide the remaining source gap.
3. **Rebuild the initialization proof interface after formalization; affected blocks: factor conditioning, normalized Gram, and tangent deficit; eventual target: `/proof-sketch`.** Give stable steps and an obstruction-level direct derivation in the exact convention: raw-to-normalized mapping, eigenvalue/concentration window with all `\(n,r,k,\kappa\)` dependence, generator-Gram control for `\(\mathscr S_0\)`, projection/anti-concentration inequality for `\(D_r\)`, and a joint failure budget at most `\(r^{-10}\)`.
4. **Retain and step-localize the deterministic implication; affected blocks: convergence, Taylor residual, physical transfer, probability identity; eventual target: `/proof-sketch`.** State stable steps in dependency order and preserve the explicit relations already checked in this review, including the summable path-tail bound, `\(C_{\rm CP}E_\star^2\)` margin, exact mode-span projection identity, relative-loss constant, and conditional event flow.

Target-preserving repair check: normalization is a simple algebraic bridge for later deterministic arguments, but changing the event that appears in the exact formalized goal is not a target-preserving sketch repair. It changes the theorem-facing certificate and its exposed `\(n\)` dependence. Therefore the current exact setting and goal cannot be preserved, while the surrounding branch structure can be preserved by a minimal-delta new idea.

## Review Rationale

The deepest defect is not missing proof detail but falsity of a quantified part of the exact formalized goal. The direct calculation is convention-matched, noncircular, and produces an allowed-parameter counter-regime. A normalized-Gram bridge would be mathematically natural and would leave the deterministic positive-loss mechanism largely unchanged, but it necessarily changes the definition of the certified event and the theorem's exposed dependence. Under the Step-Locality And Theorem-Contract Gate, that is an `idea/theorem-contract defect`, so `IDEA_FAIL`, `/subagent-idea-generator`, and `new_idea` are the only aligned routing fields. The high score within the failure range reflects that a minimal certificate rescaling appears capable of preserving nearly all other branch structure.
