# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 2, `perspective_1/idea_2/proof_sketch.md`, retry mode `revise_sketch` (`SEARCH_MODE=OPEN`), reviewed against the exact-goal contract in `perspective_1/idea_2/setting.md`.
- Triggering failure: `proof_steps/step_002/review.md`, SHA-256 `375cb45b188861ffcbcefdd949c11a6562e8a000457ade261d1d000fde11a74c`, which rejected attempt 1's self-referential Yan confidence substitution and omitted outer logarithm.
- Frozen review inputs: `setting.md` SHA-256 `8cdb060c6de66a9bdbf6a2ac920a48e2850550488e4e7e0926e61bd2a09ffe3c`; `proof_sketch.md` SHA-256 `5edf677e82997f4761c3a6ba94df41bd2bb2a64b845c0559483658e642134413`; `technical_survey.md` SHA-256 `d3e48a73ba87fa41ec3aba9ea4143be06d19e28c87887d43d15b3f8cc19e56d1`.
- Primary-source audit: Yan, arXiv:2505.06581v2, source archive SHA-256 `ef2df94c47ad0ee4a0942348b72e8773aacedee0fe893ae1e7efc58b7738c43b`; ALMM, arXiv:1806.00949, source archive SHA-256 `90d1ad2757740a9cbdc8a5d3d9e84db2a8981258f15c42e6ebb5ce09a1ad29e6`.

## Sketch Viability Score

6

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: The sketch remains in exact-goal mode and preserves both clauses of `setting.md`: the arbitrary-distribution upper theorem for all (0<\delta<1), the candidate-wise unrestricted-improper lower theorem, the fixed PAC constants, replacement adjacency on every fixed-size dataset, the displayed squared-log quota, and the conditional (n_*\) specialization. It also preserves the branch's `conditional` source-alignment label and does not claim to settle higher-VC indecomposable factors.
- Dependency audit: The 15 formal step dependencies are earlier-only and acyclic. One generated-output record is inconsistent with them: `step_012` formally depends on `step_010`, but the hidden-factor flow and one exported-interface row use only `{step_009,step_011} -> step_012`; the duplicate `step_013` flow row should also be removed. More importantly, `step_003`, `step_005`, and the upper part of `step_015` consume a factor privacy/utility interface that `step_002` has not yet source-validly produced.
- High-risk coverage: Attempt 2 correctly localizes the old confidence fixed point and supplies a plausible fixed-confidence repair. The score side is (y_j\ge z), the comparison side is (y_j\le z), realizable true-path uniqueness supports positive-score identification, and one good comparison block is enough for Yan's final deterministic inclusion comparison. However, source inspection exposes two earlier obligations that the sketch treats as discharged: arbitrary-input replacement privacy of the choosing score and the per-block good-event bound for the generally improper core hypothesis.
- Explicit-rate coverage: Conditional on repairing those two interfaces, the displayed (t_i), choosing slack, two ceilings, fixed confidence ratios, Hoeffding threshold, prefix size (N_i), and quota comparison plausibly fit (q_i) with no (log(t_i+2)), log-log, third logarithm, or extra confidence factor. Any replacement-private choosing wrapper must nevertheless restate its exact privacy allocation, bounded-growth parameter, slack, and the resulting (N_i\le q_i) calculation rather than relying on the current source theorem unchanged.
- Assumption and citation plausibility: The four setting assumptions remain primitive, and the ALMM expected-risk wrapper is source-plausible: the active proof of `lem:reduction` uses only expected empirical loss at most (1/8) (ALMM source lines 716-748), while factor-nine resampling and delta/log changes are universal losses. Yan's cited privacy and block-accuracy interfaces are not adequate as currently mapped. Its deterministic-point definition and algorithm are not total on all inputs, its privacy proof only asserts one-coordinate/1-bounded behavior, and its VC theorem controls pairs of concepts in the class rather than the induced improper block core.

## Early Obstruction Audit

- Limiting-case stress: Three allowed cases break the current `step_002` interface. First, when the transformed target is (c_i=f_i), a realizable all-zero block can have no positive deterministic point, so (B_{ij}=\varnothing) and (y_{ij}=\max_{x\in B_{ij}}d(x)) is undefined unless an explicit sentinel convention is added. Second, an inconsistent arbitrary input can have empty version space, making the universal deterministic-point condition vacuous and potentially making (B_{ij}) all of (X_i), so the maximum, layer, and score range need not be defined. Third, even realizable replacement neighbors can change two score coordinates: for the VC-one class consisting of the empty concept and singleton point functions, replacing the sole positive record at (a) by a positive record at (b) changes the deterministic core from ({a}) to ({b}). These are theorem-facing because privacy is required on nonrealizable as well as realizable datasets.
- Theorem-critical bridge support: The lower branch and the upper arithmetic/occupancy bridges have legal sources. The Yan factor producer does not. `thm:privacy-OPTPrivateLearner` does not prove the setting's all-input replacement interface under the score transition above, and `thm:learn vc` does not directly prove (\Pr[Z_{ij}=1]\le b) for (h_{ij}=1_{B_{ij}}). Both are `sketch/interface defect`s, not obligations that may first be sourced inside later proof steps.
- Exported-interface feasibility: The raw upper controls are VC-one structure, a realizable i.i.d. block, the source tree/order lemmas, private median, choosing, and a fixed confidence (b). The desired export is one total ((\varepsilon_{\rm fac},\delta_{\rm fac}))-DP mechanism on every length-(q_i) dataset plus requested-confidence risk at most (1/64) on realizable data. The export is infeasible as written because the arbitrary-input score map and the block-good event lack valid source-to-claim paths. Once those are supplied, the median margin (t_i/6), choosing loss at most (t_i/12), bad-count threshold (t_i/12), and fixed-ratio quota comparison are adequate. Downstream occupancy and the lower direct sum have complete raw-control-to-output paths.
- Theorem-critical mechanism witness gate: Fails for `step_002`. The witness identifies the intended positive terms and confidence arithmetic, but its privacy source does not match replacement adjacency on arbitrary inputs and its utility source does not match the induced hypothesis class. The witness is otherwise adequate for the median orientation, positive-score uniqueness, one-good-comparison inference, and squared-log calibration. The defect is same-setting sketch-repairable by a current-notation wrapper and block-core lemma; no theorem-contract change is yet forced.
- Entry-state trace stress: No recursive or all-time process appears. For the finite block mechanism, the relevant entry traces fail at the empty-core and inconsistent-core states because the first computation of (y_{ij}) is not total. The symmetrized-prefix transition is otherwise valid, and the lower hidden-factor transition uses each supplied row at most once and becomes input-independent on overflow.
- Obligation locality classification: `step_001`, `step_004`, and `step_006` through `step_014` are mathematically step-local, subject to their declared dependencies. The Yan arbitrary-input privacy wrapper, block-core generalization lemma, and sentinel/measurability conventions inside `step_002` are `sketch/interface defect`s. `step_003`, `step_005`, and the upper closure in `step_015` are downstream `sketch/interface defect`s until that producer is repaired. The claim of `step_012` is step-local, but its recorded producer-consumer flow has a `sketch/interface defect` because it omits `step_010`. No current hard obligation is classified as an `idea/theorem-contract defect`.
- Noncircular closure gate: Counts, occupancy, ALMM priors, hard-factor membership, overflow, couplings, and lower risk have earlier noncircular producers. The proposed bad-block event is not circular, but it is unsupported: the sketch names `thm:learn vc` without first producing an in-class comparison hypothesis or proving an appropriate VC-one closure class. Thus Hoeffding cannot yet consume the indicators. The factor-DP output similarly has no total all-input producer.
- Mechanism-source and boundary stress: Yan's deterministic-point definition (source lines 452-454), construction of (B_i,y_i), and choosing score (lines 493-511), and privacy proof (lines 527-532) do not discharge the setting's replacement-adjacency boundary. The source definition of (k)-boundedness is an add-one monotonicity statement, while the branch needs fixed-size replacement privacy; the singleton example changes two candidate scores. The endpoint (d_i=1), prefix coupling, block orientation, and multiset routing are otherwise plausible. The ALMM, VC, occupancy, minimax, and overflow mechanisms remain active on all branches where the sketch consumes them.
- Generated-output flow: The upper flow is blocked at `step_002`, so `step_002 -> {step_003,step_005,step_006} -> step_015` is not yet a legal accepted-output path, although the quota-only path to `step_006` is quantitatively plausible. In the lower flow, `step_012` consumes the high-factor/subcriticality certificate from `step_010` to establish ALMM eligibility, so every hidden-factor path must read `{step_009,step_010,step_011} -> step_012 -> step_013`; the current table omits `step_010` and duplicates the `step_013` row.
- Source-to-claim adequacy: Yan v2 is correctly identified, but source interfaces are overread. `thm:learn vc` (source lines 233-237) bounds pairs (c,h\in\mathcal C); (h_{ij}=1_{B_{ij}}) need not belong to (C_{i,f_i}). The source's unlabeled block-good lemma (lines 554-563) does not close this gap: the comparison concept (h') is not fixed consistently through the argument, the claimed inclusion is not derived, and the risk comparison contains an apparent object typo. A current-notation path/LCA witness (g_{ij}\in C_{i,f_i}), or a proof that the entire core-hypothesis closure has VC dimension at most one with the needed empirical relation, is required. Likewise, the source privacy proof asserts rather than proves the required replacement score bound. ALMM's source identity, object mapping, expected-loss role, and known formal-corollary non-output are adequate at sketch granularity.
- Residual-to-target adequacy: If a valid block comparison (g_{ij}) is produced, the required relation is (R_{D_i}(h_{ij},c_i)\le R_{D_i}(g_{ij},c_i)\le\alpha_{\rm fac}) on the same target and risk, so there is no surrogate residual. That relation is currently missing. Yan's final one-witness comparison is target-compatible only after this bridge. The lower truncated/ideal experiment has the exact same ((c_i,D_i)) off overflow and an explicit bounded-loss residual (eta_0); weighting produces exact global risk and `step_014` dominates both (eta_0) and (w_L).
- Baseline invariance audit: The inherited (k=1) upper conclusion is not yet preserved because it still invokes the same underdefined and unverified Yan factor mechanism; setting (k=1) does not remove arbitrary-input DP or block-core utility obligations. The inherited (k=1) lower conclusion is preserved by the ordinary VC lower source for small (r_1) and the ALMM source for large (r_1), with zero overflow under (m_{n,1}\ge4n). The retry remains `/proof-sketch` because the upper baseline can plausibly be restored by a same-setting wrapper with only universal constant losses.
- Scope-accumulation compatibility: Conditional on (\Pr[Z_{ij}=1]\le b), the (t_i) disjoint block events are independent after conditioning on the data-independent partition, and Hoeffding controls the adversarial-sign bad count in one finite batch. The two private failures and concentration loss add to (b+b+\beta_{\rm fac}/2=\beta_{\rm fac}). A replacement-private choosing repair may add privacy composition or change the bounded-growth parameter; those finite losses must be included in the internal budget and slack before claiming (N_i\le q_i). All other accumulation is finite: at most two routed factors for privacy, one weighted upper-risk sum, and one weighted lower residual sum.
- Scope and dependence consistency: The sketch retains all exposed variables, arbitrary-(\delta) upper scope, candidate-wise lower scope, fixed-sample mode, pointwise privacy mode, and distributional-risk metric. The fixed-confidence calibration plausibly removes the old (log(t_i+2)) defect. No hidden factor-cardinality dependence is needed, but totalization and measurable selection must be uniform over possibly infinite factor classes and domains.
- Generated-condition provenance: The occupancy events, priors, hard set, overflow event, and lower couplings are derived. The block indicators (Z_{ij}) are named as generated outputs, but their marginal probability certificate has no valid producer. The total score map and candidate layer used by the choosing mechanism are also not defined on every allowed input. These conditions cannot be treated as source-supplied assumptions.
- Citation and tool applicability: The exact Yan labels and source version are traceable. The median and choosing utility statements, tree order, and final deterministic inclusion paragraph support the proposed confidence repair only after the missing privacy and core-generalization wrappers. Yan's `thm:privacy-OPTPrivateLearner` and unlabeled block-good lemma are known non-outputs for those wrappers. ALMM's active `lem:reduction` proof does use only expected empirical loss at most (1/8), and the stated factor-nine resampling, privacy, label, and logarithm conversions are compatible with the branch. Standard Hoeffding, Chernoff, finite minimax, composition, and PAC-to-expectation tools match their claimed roles.
- Same-setting repair plausibility: Plausible. A total sentinel-based definition, an exact replacement-private bounded-growth wrapper with recalibrated constant budget, and a direct VC-one path/core generalization lemma can all be added without changing the class assumptions, learner's theorem-facing form, PAC constants, risk metric, candidate scope, or displayed asymptotic dependence. The review does not certify that these lemmas are true; it records the smallest target at which they must be made explicit and source-valid.
- Target-preserving bridge-repair gate: Passes only as a routing decision. The required changes are internal interfaces for the same Yan factor procedure and one dependency-table correction. Universal constant-factor changes in (K,K_t), privacy allocation, or block size are allowed by the current quota contract, but an extra unbounded logarithm, finite-cardinality assumption, properness restriction, realizable-only privacy definition, weakened confidence, or altered upper theorem would not be target-preserving and would require renewed idea-level review.
- High-risk obligation classes: Audited classes are arbitrary-input replacement privacy, score bounded growth, empty/inconsistent version spaces, improper-core generalization, median-side orientation, generated-event concentration, exact quota specialization, routed composition, arbitrary-mass occupancy, ALMM expectation conversion, finite minimax, low-factor mass, overflow coupling, unrestricted direct-sum averaging, candidate quantifiers, and both one-factor baselines. Only the first four and the generated-flow record require sketch repair.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `step_001`: product VC, LD, and finite-cardinality identities | Exact structural input to both theorem clauses | Disjoint full Cartesian product; direct shattering and online arguments | Primitive `assump:canonical-product`, `assump:vc-one-factors` | Exact claim type and exact class object | Same binary representation, whole-domain partition, and factor conventions | (\operatorname{VC}(C)=k), (\operatorname{LD}(C)=\sum_i d_i), and finite products multiply cardinalities | Raw factor structure exports (d_i\ge1,s_i\ge2,k\le M/2,s_i\le r_i+2) | Exact objects; no surrogate residual | Finite algebraic aggregation; no stochastic forcing | Primitive assumptions -> `step_001` -> all consumers | Producer `step_001`; consumers `step_006`, `step_007`, `step_010`, `step_015` | (k=1,d_i=1), and infinite-cardinality non-use pass | `step-local` | None |
| `step_002`: total arbitrary-input Yan mechanism and factor privacy | Theorem-critical cited wrapper producing factor DP | Yan `alg:improperlearner`, private median, choosing, and `thm:privacy-OPTPrivateLearner` | Yan v2, deterministic-point definition lines 452-454, algorithm lines 493-511, privacy assertion lines 527-532 | Fails: source does not derive the setting's total replacement-private score interface | Source says one-entry replacement, but its choosing lemma uses add-one (k)-boundedness; empty version spaces and infinite layers are not handled | Required relation is a total score transition with exact replacement sensitivity/bounded growth and an internal privacy sum at most ((\varepsilon_{\rm fac},\delta_{\rm fac})); current sketch supplies neither | Raw VC-one tree and fixed input length do not yet export all-input factor DP | Privacy has no target residual; the exact DP interface itself is missing | One changed block may decrement one candidate and increment another; inconsistent blocks may change many or make maxima undefined | No legal producer before `step_003`; citation assertion is insufficient | Intended producer `step_002`; consumers `step_003`, `step_015` | Singleton replacement changes ({a}) to ({b}); inconsistent block and empty core fail | `sketch/interface defect` | `/proof-sketch` |
| `step_002`: block-core good event (\Pr[Z_{ij}=1]\le b) | Generated utility condition required by Hoeffding and final factor accuracy | Yan `thm:learn vc`, tree order lemmas, unlabeled block-good lemma | Yan v2 lines 233-237 and 554-563 | Fails: VC theorem quantifies over (c,h\in C), while (h_{ij}=1_{B_{ij}}) may be improper; source proof does not supply one fixed valid witness | Realizable i.i.d. convention matches, but hypothesis-class membership/closure does not | Needed relation: construct (g_{ij}\in C_{i,f_i}) with the empirical premise and (R(h_{ij},c_i)\le R(g_{ij},c_i)), or prove an appropriate VC-one core closure and apply a matching theorem | Raw sample consistency and path structure do not yet export the marginal bad probability | Missing same-target transfer (R(h_{ij},c_i)\le\alpha_{\rm fac}); no other residual is allowed | Once marginal bounds hold, independent blocks give a finite Bernoulli sum; presently the forcing indicators are unsupported | No valid producer for (Z_{ij}) before Hoeffding | Intended producer `step_002`; consumers `step_002` concentration, `step_005`, and upper `step_015` | Realizable blocks do not by themselves put the core indicator in (C_i); source inference at lines 558-560 is incomplete | `sketch/interface defect` | `/proof-sketch` |
| `step_002`: median orientation, choosing margin, concentration, and exact quota | Quantitative specialization producing requested confidence without the old fixed point | Yan private median/choosing/order/final comparison plus standard Hoeffding | Exact Yan v2 labels; raw ((t+2)b) corollary is a declared non-output | Matches conditionally on a total private score and valid block-good marginals | Score side (y_j\ge z); comparison side (y_j\le z); fixed (b) and replacement allocation must be restated after repair | At least (t_i/6) blocks on each side; (\Delta_i\le t_i/12); fewer than (t_i/12) bad blocks leaves one good comparison witness; (2b+\beta_{\rm fac}/2=\beta_{\rm fac}) | (s_i,L_i,t_i,m_i,N_i) plausibly export (N_i\le q_i) with universal constants and no (log(t_i+2)) | Same (D_i,c_i,R_{D_i}) after a valid witness; no transformed-target residual | One finite bad-count sum; Hoeffding absorbs it; two private failures are unioned once | Fixed (b) -> explicit (t_i) -> median/choosing/good-count event | Producer `step_002`; consumers `step_005`, `step_006`; privacy consumer `step_003` needs the separate repaired wrapper | (d_i=1), tiny (\delta), (\delta\uparrow1), and unused-prefix remainder pass conditionally | `step-local` after the two named producer interfaces are supplied | None |
| `step_003`: at-most-two routed replacement privacy | Global pointwise DP certificate | Multiset routing, symmetrized prefix, basic composition, postprocessing | Direct derivation from factor DP | Routing claim matches, but the factor-DP premise from `step_002` is unavailable | Fixed-size replacement and arbitrary padded inputs match only after total factor DP | At most two factor inputs change by one replacement; desired sum is (2(\varepsilon/2,\delta/2)=(\varepsilon,\delta)) | Routing controls can export global DP exactly once factor DP is legal | Exact postprocessing; no residual | Finite support two; no (k)-accumulation | Repaired `step_002` -> `step_003` -> `step_015` | Producer `step_003`; consumer `step_015` | Same-block, cross-block, padding, and (k=1) routing pass; factor privacy boundary does not | `sketch/interface defect` downstream of `step_002` | `/proof-sketch` |
| `step_004`: weighted shortage mass | Generated padded-mass control for upper utility | Binomial marginals and light/heavy split | Primitive i.i.d. sampling; direct Chernoff | Exact claim type | Same arbitrary block masses and zero-mass convention | (\mathbb E\sum_i\rho_i1\{N_i<q_i\}\le128Q/n+e^{-16}) | Raw (N_i\sim\mathrm{Bin}(n,\rho_i)), (q_i\ge1), (\sum_i\rho_i=1) export the exact padded mass | Exact risk weight; no surrogate | Finite nonnegative weighted sum; count dependence is irrelevant | Sample -> `step_004` -> `step_005` | Producer `step_004`; consumer `step_005` | (\rho_i=0), many light blocks, and one heavy block pass | `step-local` | None |
| `step_005`: weighted factor bad mass and upper PAC event | Global high-probability utility closure | Factor utility from `step_002`, occupancy from `step_004`, conditional exchangeability, Markov | Direct risk algebra plus repaired Yan wrapper | Occupancy matches; factor utility does not yet have a valid block-good source | Same factor distributions and global risk, conditional on unpadded blocks | Desired (R_D\le1/64+W), (\mathbb EW<3/1024), then Markov at margin (3/64) | Raw occupancy is feasible; raw factor-failure probability is not yet exported | Every padding/factor failure is weighted exactly; missing factor certificate is not an absorbable residual | One finite weighted sum; no union over factors | `{repaired step_002,step_004} -> step_005 -> step_015` | Producer `step_005`; consumer `step_015` | Zero-mass and arbitrarily dependent factor outputs pass; unsupported (Z_{ij}) blocks closure | `sketch/interface defect` downstream of `step_002` | `/proof-sketch` |
| `step_006`: public quota sum | Quantitative specialization to the displayed upper rate | Exact quota formula and product inequalities | `step_001`; explicit arithmetic from calibrated `step_002` | Matches, provided repaired privacy allocation changes only universal constants | Natural-log public convention and log-star source convention are separated | (Q\le C_{\rm quota}(M/\varepsilon)\log^2(eM/(\varepsilon\delta))), including ceilings and fixed ratios | (s_i\le M), (\sum_i s_i=M), (k\le M/2), (\varepsilon\le1/10) support the export | Exact sample threshold; no residual | Finite nonnegative sum; ceiling defect at most (k) | `{step_001,calibrated step_002} -> step_006 -> step_015` | Producer `step_006`; consumer `step_015` | (k=1,d_i=1,\delta\uparrow1), tiny positive (\delta) pass | `step-local`; recalibration belongs in repaired `step_002` | None |
| `step_007`: global VC lower certificate | Additive (k) lower source | Independent unseen labels on one shattered point per block | `step_001`; direct random-target argument | Exact unrestricted-output claim | Same global sample and risk; privacy unnecessary | If (n<k/2), expected unseen-label risk is at least (1/4>31/256) | Raw shattered set exports (n\ge a_{\rm VC}k) | Exact global risk; no residual | One finite expectation; collisions only help the lower bound | `step_001 -> step_007 -> {step_010,step_014}` | Producer `step_007`; consumers `step_010`, `step_014` | Small (k), point functions, and improper output pass | `step-local` | None |
| `step_008`: ALMM expected-risk wrapper | Cited unrestricted factor lower interface | ALMM `lem:bun`, `lem:reduction`, `thm:main`, `thm:shelah`, `thm:ADPimpliesLD` | ALMM arXiv:1806.00949; active reduction lines 716-748 | Expected loss at most (1/8) gives exactly the (7/8) expected empirical accuracy used by the proof | Replacement DP, unrestricted outputs, binary relabeling, and distributional loss match; factor nine and log shifts are universal | (m<a_L\log_2^*d) under the delta budget implies worst-case expected risk (>1/8) | Resampling and the source threshold embedding export the exact factor-risk floor | Uniform-on-database population risk equals empirical risk; no target residual | One fixed budget; no repeated accumulation | Cited proof -> `step_008 -> step_009` | Producer `step_008`; consumer `step_009` | (d=1) is inactive; (m\ge8), improper output, and smaller (\varepsilon) pass | `step-local` | None |
| `step_009`: finite hard priors | Learner-independent factor-task source | Finite ALMM restriction and finite zero-sum minimax | `step_008`; finite-dimensional direct argument | Exact expected-risk and arbitrary-output claim on the finite support | DP polytope includes nonrealizable finite databases under replacement adjacency | Strict worst-case floor yields a finite prior with prior-average floor (>1/8); point masses complete inactive factors | Finite tasks and output label vectors export all-factor priors before the common experiment | Exact (R_{D_i}); finite restriction is a subproblem, not a surrogate | One compact game per factor; finite product of priors | `step_008 -> step_009 -> {step_012,step_013}` | Producer `step_009`; consumers `step_012`, `step_013` | Infinite ambient classes and inactive low factors pass by finite restriction/completion | `step-local` | None |
| `step_010`: hard-factor membership, subcritical budgets, and low-mass bound | Structural bridge activating ALMM on almost all mixture mass | VC lower, ALMM constants, and explicit arithmetic | `step_007`, `step_008` | Exact weights and candidate budgets | Same (s_i,r_i,m_{n,i},\pi_i) as the theorem | Chosen (R_0,c) give (w_L<1/128) and (m_{n,i}<a_Lr_i) on (H) | Raw (n\ge a_{\rm VC}k), (n<cM), (s_i\le r_i+2) export the activation certificate | Exact weights/budgets; no residual | Finite deterministic partition; discarded mass is explicit | `{step_007,step_008} -> step_010 -> {step_012,step_013,step_014}` | Producer `step_010`; consumers include `step_012`, `step_013`, `step_014` | All-low, one-high, floor-eight, and ceiling transitions pass | `step-local` | None |
| `step_011`: binomial overflow certificate | Generated coupling-loss bound | Optimized binomial Chernoff | Direct from candidate definitions | Exact event and constant | Same (L_i\sim\mathrm{Bin}(n,\pi_i)) and integer threshold | (\Pr[O_i]\le\eta_0=e^7(2/9)^9<3/2048) | Raw mean, factor-four buffer, and floor eight export the event bound | Bounded risk loses at most (eta_0) | One local subtraction per marginal; no factor union | Block-index sequence -> `step_011 -> consumers` | Producer `step_011`; consumers `step_012`-`step_014` | (p=0,np\le2,np=2,np\ge2,n=1) pass | `step-local` | None |
| `step_012`: hidden-factor learner, one-use privacy, and ideal coupling | Reduction making global (A) eligible for a hard factor prior | Priors, hard-factor certificate, overflow, sequential one-use embedding | `step_009`, `step_010`, `step_011`; direct construction | Mathematical interface matches; recorded generated-flow path is incomplete | Exact replacement adjacency, full Cartesian target, same local risk | One factor-row replacement maps to at most one global-row replacement; off (O_i), simulated and ideal datasets coincide | Raw priors plus `step_010` eligibility and `step_011` capacity export (B_i) | Same ((c_i,D_i)) off overflow; residual at most (1_{O_i}) | One use and one call; overflow branch is input-independent | `{step_009,step_010,step_011} -> step_012 -> step_013` | Producer `step_012`; consumer `step_013`; current table omits `step_010` | Zero rows, used/unused replacement, overflow, (k=1), and improper (A) pass | `sketch/interface defect` in flow record; proof claim is step-local | `/proof-sketch` |
| `step_013`: independent-prior tensorization and weighted risk lower | Direct-sum expected-risk certificate | Product priors, same ideal experiment, exact risk identity | `step_009`-`step_012`; linearity/Tonelli | Exact global-risk claim | Full Cartesian targets and mixture distribution match the setting | (\mathbb ER_D>(1-w_L)(1/8-\eta_0)) | Hard priors, activation, and couplings export all local marginal bounds without selecting a factor after seeing (A) | Overflow weighted once; low mass explicit; exact targets unchanged | Finite weighted sum; joint output dependence is harmless | `{step_009,step_010,step_011,step_012} -> step_013 -> step_014` | Producer `step_013`; consumer `step_014` | One hard factor, correlated outputs, and zero-risk low factors pass | `step-local` | None |
| `step_014`: numerical contradiction and candidate witness | Candidate-wise lower theorem | PAC-to-expectation and `step_013` lower certificate | Direct arithmetic and logic | Exact probability/risk mode | Same global target, distribution, and candidate delta condition | ((127/128)(1/8-3/2048)=32131/262144>31/256) | Raw (w_L,eta_0) and PAC expectation ceiling export (n\ge c_{\rm low}M) | Every lower residual is present and dominated | One fixed-candidate comparison; no cross-candidate accumulation | `step_013 -> step_014 -> step_015` | Producer `step_014`; consumer `step_015` | (n=1), all-low, one-high, and equality in delta threshold pass | `step-local` | None |
| `step_015`: exact theorem assembly | Final two-sided closure and scope guard | Earlier upper and lower outputs | Direct logical assembly | Lower branch matches; upper producers are not yet legal | Arbitrary-(\delta) upper and candidate-wise lower remain distinct | Invoke upper from `step_003`,`step_005`,`step_006`; lower from `step_014`; specialize at (n_*) only under its condition | Raw lower controls are feasible; upper privacy/utility controls are blocked at `step_002` | No new residual; missing producer cannot be absorbed | Fixed-sample and candidate-wise only | `{step_003,step_005,step_006,step_014} -> step_015` after repair | Producer `step_015`; final theorem consumer | Failed upper (k=1) baseline; lower and delta-scope boundaries pass | `sketch/interface defect` downstream of `step_002` | `/proof-sketch` |
| One-factor upper baseline | Baseline invariance obligation | One total Yan factor call plus padding/routing | Intended `step_002`-`step_006` path | Fails until Yan privacy and block-good interfaces are repaired | Same (C_1,c_1,D_1,R_{D_1}); (k=1) does not remove nonrealizable privacy inputs | Desired factor DP and PAC control at the displayed squared-log rate | Quantitative quota is plausible; total DP and marginal block-good controls are missing | No allowed weaker surrogate; padding vanishes above quota | No factor aggregation; only internal (t_1) blocks | Repaired `step_002 -> {step_003,step_005,step_006} -> step_015` | Upper baseline consumed by final theorem | (c_1=f_1), inconsistent input, and singleton replacement expose the same gaps | `sketch/interface defect` | `/proof-sketch` |
| One-factor lower baseline | Baseline invariance obligation | VC nonconstancy plus ALMM log-star hardness | `step_007`-`step_014` | Exact lower claim up to universal constants | Same class, target, risk, improper output, and candidate privacy mode | Small (r_1) paid by VC; large (r_1) paid by ALMM; (m_{n,1}\ge4n) makes overflow zero | Raw two lower sources export (Omega(s_1)) under the candidate condition | No overflow or surrogate residual | One candidate, one factor | `step_007`,`step_008` -> `step_010`-`step_014` -> `step_015` | Lower baseline produced by `step_014`; final theorem consumes it | (r_1=0) and (r_1\ge R_0) branches close | `step-local` | None |

## Blocking Issues

1. **`step_002`, arbitrary-input replacement privacy.** Yan's deterministic-point definition, score construction, and `thm:privacy-OPTPrivateLearner` do not establish the factor interface required by `setting.md`. The source privacy proof merely asserts that one score coordinate changes and that the score is 1-bounded. Its (k)-bounded definition is add-one based, while the branch uses fixed-size replacement. In the VC-one class containing the empty concept and all singleton point functions, replacing a sole positive (a) by a sole positive (b) changes the core and score from the (a)-candidate to the (b)-candidate, so two layer coordinates change even on realizable neighbors. On inconsistent inputs, empty version space makes the deterministic set vacuous or underdefined and can cause much larger changes. Downstream effect: `step_003` cannot compose factor DP, and the upper clause of `step_015` has no pointwise privacy certificate. Smallest repair: add a total current-notation Yan wrapper and prove an exact replacement-private choosing interface, with explicit bounded-growth/sensitivity parameter, internal privacy allocation, and recalibrated choosing slack/quota.

2. **`step_002`, unsupported block-good marginals.** Yan's `thm:learn vc` controls two concepts in the source class, but the induced (h_{ij}=1_{B_{ij}}) may be improper. The source's unlabeled lemma at lines 554-563 does not supply a valid fixed comparison concept or closure argument. Therefore (\Pr[Z_{ij}=1]\le b) is not source-certified, and independence alone cannot justify the Hoeffding tail. Downstream effect: the one-good-comparison witness, factor utility, `step_005`, and the upper theorem remain unproved. Smallest repair: add a current-notation path/core generalization lemma that either constructs an in-class witness with the exact empirical and risk-transfer relations or proves that the relevant core closure has VC dimension at most one and that the stated uniform-convergence theorem applies.

3. **`step_002`, missing total boundary and measurability conventions.** The formal source algorithm does not define (y_{ij}) for an empty positive core, including the allowed realizable case (c_i=f_i); it also does not resolve empty version spaces, constant coordinates, duplicate points/concepts, infinite candidate layers, measurable candidate selection, or measurability of the upward-set output. The sketch's quotienting and hard-coding prose does not specify a total algorithm or prove these properties. Downstream effect: the mechanism is not a well-defined measurable kernel on the setting's entire data space, even before privacy is checked. Smallest repair: specify the sentinel/root layer, version-space convention, quotient/pullback, finite supported candidate range or measurable selection rule, and measurable output for every input, and show that these conventions preserve realizable utility and replacement adjacency.

4. **`step_012` generated-output flow.** The formal step depends on `step_010` to know that a factor is high and its budget is ALMM-subcritical, but `## Exported Interface Feasibility` and `## Generated Output Flow` record the hidden-factor path as `{step_009,step_011} -> step_012`; the flow table also duplicates the `step_013` output row. Downstream effect: the recorded provenance omits a theorem-critical eligibility producer even though the step table itself is acyclic. Smallest repair: change every `step_012` producer path to `{step_009,step_010,step_011} -> step_012 -> step_013`, list the hard-set certificate as a `step_012` input, and remove the duplicate flow row.

## Required Repair Bundle

1. **Total replacement-private Yan wrapper (issues 1 and 3; affects `step_002`, `step_003`, `step_015`; target `/proof-sketch`).** State one total factor algorithm on every length-(q_i) labeled sequence. Define version space on inconsistent data, the empty-core sentinel and its distance/layer/output, the data-independent reference (f_i), quotienting of indistinguishable points and duplicate concepts, treatment of constant coordinates, the actual finite or measurably selectable candidate range, and the pullback hypothesis. Prove that the mechanism is a measurable randomized kernel. Then prove privacy under the setting's fixed-size replacement adjacency: give the exact score transition, bounded-growth or sensitivity parameter, and theorem used for the choosing stage. If replacement is implemented through two add/remove transitions or a generalized (k)-bounded choosing lemma, allocate every internal ((\varepsilon,\delta)) cost explicitly so the whole factor remains ((\varepsilon_{\rm fac},\delta_{\rm fac}))-DP.

2. **Recalibrate the repaired privacy interface at the unchanged quota (issue 1; affects `step_002`, `step_006`; target `/proof-sketch`).** Recompute (\Delta_i), the private-median and choosing thresholds, (t_i), (m_i), and (N_i) using the exact bounded-growth parameter and internal privacy split from repair item 1. Prove with displayed inequalities that (\Delta_i\le t_i/12), all subroutine failure probabilities sum to at most (\beta_{\rm fac}), and (N_i\le q_i) with only universal changes to (K,K_t). The repair must not introduce (log(t_i+2)), another unbounded logarithm, a factor-cardinality term, or a realizable-only privacy claim.

3. **Block-core generalization producer (issue 2; affects `step_002`, `step_005`, upper `step_015`; target `/proof-sketch`).** Define the block hypothesis in the repaired sentinel convention. For every realizable block, prove one exact lemma that yields (\Pr[Z_{ij}=1]\le b): either construct a single (g_{ij}\in C_{i,f_i}) satisfying the empirical premise of Yan's `thm:learn vc` and a pointwise or risk relation (R_{D_i}(h_{ij},c_i)\le R_{D_i}(g_{ij},c_i)), or define a source-supported core-closure class, prove its VC dimension bound, and map all hypotheses of the chosen generalization theorem. Include the all-zero/empty-core branch. Only after this lemma may the sketch assert independence of (Z_{ij}), apply Hoeffding, and invoke the one-good comparison block.

4. **Retain and sharpen the valid confidence repair (issue 2; affects `step_002`; target `/proof-sketch`).** Preserve the correct orientation (y_j\ge z) for score blocks and (y_j\le z) for comparison blocks. State why realizability and the repaired sentinel convention give at most one positive-score true-path candidate at layer (z), why choosing positive score identifies it, and why fewer than (t_i/12) bad blocks leave a good member among at least (t_i/6) comparison blocks. Map that block to Yan's final deterministic inclusion (B_{ij}\subseteq I_{\rm good}) and the exact same-target risk comparison. Do not use the raw ((t_i+2)b) corollary as quantitative authority.

5. **Complete source-convention and generated-flow records (issues 1-4; affects mechanism witness, exported-interface table, generated-output table, `step_012`; target `/proof-sketch`).** Mark Yan's printed privacy theorem and unlabeled block-good lemma as non-outputs for the new wrappers; record exact source lines/labels, raw controls, controlled and uncontrolled defects, sentinel and measurability branches, consumers, and the target-preserving constant-loss route. Correct the lower path to `{step_009,step_010,step_011} -> step_012 -> step_013` everywhere and delete the duplicate `step_013` flow row.

6. **Target-preserving repair check (all issues; target `/proof-sketch`).** Keep all four primitive assumptions, the routed/padded learner, unrestricted improper outputs, arbitrary distributions, pointwise replacement DP on nonrealizable inputs, fixed PAC constants, arbitrary-(\delta) upper scope, candidate-wise lower scope, and the displayed squared-log dependence unchanged. The lower branch `step_007`-`step_014` needs no semantic change. If the total privacy or block-core lemma cannot be proved without finite cardinality, properness, an extra unbounded logarithm, a weaker confidence/privacy mode, or a different algorithm, the next review must reclassify that obstruction at idea/theorem-contract depth rather than silently weakening this target.

## Review Rationale

Attempt 2 plausibly repairs the triggering confidence fixed point: its two-sided median orientation, positive-score uniqueness, one-good comparison witness, Hoeffding aggregation, endpoint handling, and squared-log arithmetic are coherent once the needed block events and private score map exist. The lower direct-sum branch also remains source-plausible and quantitatively closed. The sketch is nevertheless not ready for `global-proof` or `/proof-step`, because its sole upper-factor producer relies on two source claims that do not match the consumed interfaces and is not total on allowed boundary inputs. Those defects invalidate upper privacy and utility, including the (k=1) baseline, but they can plausibly be repaired by current-notation wrappers and dependency/interface changes under the unchanged setting. Therefore the deepest justified route is `REVISE_SKETCH`, not `ACCEPTED` and not yet `IDEA_FAIL`.
