# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 3, perspective_1/idea_2/proof_sketch.md, RUN_MODE=interactive, reviewed in exact-goal mode against perspective_1/idea_2/setting.md.
- Frozen inputs: setting.md SHA-256 8cdb060c6de66a9bdbf6a2ac920a48e2850550488e4e7e0926e61bd2a09ffe3c; proof_sketch.md SHA-256 2fab93419bb930a1e0979822b1a09cf0b6cd556d5d8ea25add20ae199621fc42; technical_survey.md SHA-256 5e86f70c3b89c7db99f617a87bed1d2cb3a1fe830c0fb727d062e6a66b70c98d.
- Triggering review: perspective_1/idea_2/proof_history/sketch_002/sketch_attempt_002_revise_sketch/proof_sketch_review.md, SHA-256 c36897189f5f5b51e70aa06ecb79c9d5b512579494f3853a96983283c99b885f, status REVISE_SKETCH.
- Primary-source audit: Yan arXiv:2505.06581v2 frozen archive SHA-256 ef2df94c47ad0ee4a0942348b72e8773aacedee0fe893ae1e7efc58b7738c43b; BNSV arXiv:1504.07553 frozen local archive SHA-256 2ca6dee8460bc7c9d58b8f4c058aa60c263c85ff557f472c69302532964760de; ALMM arXiv:1806.00949 frozen archive SHA-256 90d1ad2757740a9cbdc8a5d3d9e84db2a8981258f15c42e6ebb5ce09a1ad29e6.

## Sketch Viability Score

7

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: Attempt 3 preserves the exact conditional two-sided goal, the arbitrary-\(\delta\) upper clause, candidate-wise lower clause, replacement adjacency on nonrealizable inputs, fixed PAC constants, unrestricted improper output, displayed squared-log quota, conditional \(n_*\) specialization, progress type conditional, and the remaining higher-VC indecomposable-factor gap.
- Dependency audit: All listed dependencies are earlier-numbered and the specifically repaired route \(\{\mathrm{step\_009},\mathrm{step\_010},\mathrm{step\_011}\}\to\mathrm{step\_012}\to\mathrm{step\_013}\) is now present in the step table, exported-interface table, generated-output table, and dependency notes. The full interface is still inconsistent at two other locations: step_010 consumes the \(s_i\le r_i+2\) structural output attributed to step_001 without listing step_001, and step_014 explicitly consumes step_011's numerical \(\eta_0<3/2048\) certificate without listing step_011 or making that certificate an exported conclusion of step_013. Several assumptions-used cells also use prose rather than stable ids.
- High-risk coverage: The exact BNSV choosing extension does support one leaving and one entering score coordinate at the unswollen \((\varepsilon_c,\delta_c)\) budget. The source proof is already directed over equal-size replacement neighbors: one leaving candidate is paid by the \(\delta_c\) exceptional mass, one entering candidate is paid in the normalizer comparison, common scores have sensitivity one, and swapping the neighbor order gives the second privacy direction. If one support is empty, the adjacent nonempty support has optimum at most one; the low-score noisy-test bound gives nonfailure probability at most \(\delta_c\), which proves both directions for the forced-root branch. The core-closure combinatorics, median orientations, one-good-comparison argument, occupancy, lower simulation, and candidate contradiction also pass. The remaining high-risk defect is the unsupported assertion that the quotient/core/path construction is a measurable kernel under the exact arbitrary-instance-space setting.
- Explicit-rate coverage: The fixed \(b=\beta_{\mathrm{fac}}/4\), implicit universal \(K_t\), choosing slack, Hoeffding threshold, two ceilings, and \(N_i\le 8K_tT_iA_\beta\le q_i\) preserve the exact squared logarithm and introduce no \(\log(t_i+2)\), factor-cardinality term, or other unbounded logarithm. One exact endpoint remains unstated: Yan's displayed private-median interface requires \(t_i>n_{PM}\), while the sketch derives only \(t_i\ge n_{PM}\); the universal choice must be made strictly large enough. The elementary \(m_i\le4A_\beta\) ceiling inequality should also be written with its \(+1\) term in the repaired certificate.
- Assumption and citation plausibility: Yan's printed privacy theorem, unlabeled core lemma, raw confidence corollary, and final rate theorem are correctly treated as non-outputs. BNSV and ALMM are source-compatible at obstruction level. In the formal step table, step_009 must cite assump:vc-one-factors for nonemptiness/nonconstancy, step_010 must cite all primitive factor structure it uses, step_013 must cite assump:canonical-product for the full-product target and exact mixture identity, and step_015 must spell out all four stable assumption ids instead of saying only "all four setting assumptions."

## Early Obstruction Audit

- Limiting-case stress: The score construction passes replacement \(u\to v\), \(\dagger\to v\), \(u\to\dagger\), empty support, inconsistent version space, empty core, \(z=0\), \(c_i=f_i\), \(d_i=1\), \(\delta\uparrow1\), and \(k=1\) as a pointwise set-theoretic mechanism. The upper utility proof passes the root case because a comparison block with \(y_j=0\) has the same empty positive set as the root output. The lower route passes zero block mass, all-low factors, one hard factor, overflow, unused input rows, and equality in the candidate delta condition. These checks do not establish measurable dependence on a random sample.
- Theorem-critical bridge support: The replacement-private choosing bridge and the improper-core VC bridge now have correct mathematical sources. The total measurable-kernel bridge does not: declaring evaluation cylinder sigma-fields and saying that fixed preprocessing is coordinatewise measurable does not prove measurability of \(S\mapsto V(S)\), the empty-version-space test, \(S\mapsto B(S)\), \(S\mapsto y(S)\), \(S\mapsto\ell_z(S)\), or the pulled-back path output.
- Exported-interface feasibility: Raw combinatorial controls do export a total set-valued block summary, factor DP, a VC-one bad-block marginal, fixed-confidence utility, and the exact quota. They do not yet export the claimed measurable learner. The first-occurrence enumeration makes the already-produced positive support finite; it does not make the preceding version-space/core selector measurable. This blocks step_002's advertised kernel and therefore the theorem-facing upper interface.
- Theorem-critical mechanism witness gate: The privacy, core-VC, concentration, occupancy, hard-prior, hard-set, overflow, hidden-factor, and tensorization witnesses pass. The step_002 measurability witness is shallow and is a same-setting sketch/interface defect. A repair must derive measurability from the exact setting, not add countability, finite cardinality, a standard-Borel hypothesis, or a finite-support distribution assumption.
- Entry-state trace stress: No iterative or all-time mechanism appears. Pointwise entry traces at inconsistent input, empty core, \(z=0\), padded input, zero requested hidden-factor rows, and overflow are total. The measurable-kernel defect occurs before these branches can be integrated over the i.i.d. sample.
- Obligation locality classification: step_001 and the mathematical claims of step_004 through step_013 are step-local. The set-theoretic replacement and core arguments within step_002 are step-local, but its measurable-kernel export is a sketch/interface defect; step_003, step_005, and the upper part of step_015 are downstream blocked by it. The step_009, step_010, step_013, and step_015 assumption cells and the step_010/step_014 dependency mismatches are sketch/interface defects. No mathematical lower obligation currently requires an idea/theorem-contract change.
- Noncircular closure gate: The core marginal is produced before Hoeffding; occupancy before weighted utility; priors, hard membership, and overflow before the hidden-factor learner; and the hidden-factor output before tensorization. No theorem-facing success event is assumed. The specifically requested step_010-to-step_012 and step_009/010/011-to-step_012-to-step_013 paths are noncircular.
- Mechanism-source and boundary stress: BNSV lines corresponding to alg:choosing and the proof of lem:CMprivacy use both support differences and therefore validate the two-coordinate replacement histogram with \(k=1\). The forced failure on empty support is private because the adjacent nonfailure mass is at most \(\delta_c\), and \(1\le e^{\varepsilon_c}(1-p)+\delta_c\) when \(p\le\delta_c\). Yan's order makes every realizable contributing summary at layer \(z\) the same target-path point. The unresolved boundary is measurable selection over an infinite quotient layer and arbitrary version spaces.
- Generated-output flow: The triggering step_012 flow defect is discharged exactly, and the duplicate step_013 row is gone. The remaining flow mismatches are step_001's structural output consumed in step_010 without a matching formal dependency and step_011's numerical overflow output consumed explicitly in step_014 without a matching formal dependency or re-export through step_013.
- Source-to-claim adequacy: Yan thm:learn vc applies to the fixed closure class once that class is a legal measurable concept class: singleton version sets contain \(H_i\), realizable block cores are empirically consistent, comparable pairs miss pattern \((1,0)\), and incomparable pairs miss \((1,1)\). BNSV supplies the exact replacement argument as above. Yan's private-median and order statements supply the two sides and final inclusion. ALMM's active reduction uses the expected empirical loss \(1/8\) interface. None of these citations supplies the missing measurable-selection conclusion.
- Residual-to-target adequacy: Quotienting and xor are label/risk isometries set-theoretically. For utility, \(B_{ij}\subseteq I_{p_z^*}\subseteq I(c_{i,f_i})\) gives the pointwise same-target inequality in the correct direction. The lower ideal/truncated experiments use the same \((c_i,D_i)\) off overflow and lose only \(\eta_0\). There is no numerical surrogate residual; the missing item is legality/measurability of the produced upper hypothesis and kernel.
- Baseline invariance audit: The one-factor lower baseline is preserved by the VC/ALMM split and has zero overflow. The one-factor upper algebra and privacy budget are preserved, including the all-zero/root branch and squared-log rate, but the same measurable-kernel defect remains at \(k=1\); a set-theoretic output is not yet the measurable learner required by the setting.
- Scope-accumulation compatibility: All accumulation is finite. Bad core indicators are handled by one Hoeffding bound; two factor subroutine failures are unioned once; routed privacy composes over at most two factors; occupancy and risk use finite nonnegative weighted sums; overflow is subtracted once per marginal before tensorization. No persistent or adversarial-sign term is hidden.
- Scope and dependence consistency: The sketch retains every exposed variable and every probability, privacy, horizon, and risk mode. The implicit \(K_t\) is universal because \(K_t/(1+\log K_t)\to\infty\). The strict private-median sample endpoint and exact ceiling line need repair, but the squared-log target itself remains feasible.
- Generated-condition provenance: Core events, bad-block counts, occupancy, priors, hard-set membership, overflow, couplings, and lower risk are derived. Measurability is not a primitive generated condition that can be asserted by choosing sigma-fields after the setting is fixed.
- Citation and tool applicability: Yan v2, BNSV, and ALMM have stable identities and matching object/risk/privacy conventions for the claims actually consumed. BNSV's finite-solution presentation is not overread: the sketch correctly proposes a direct finite-positive-support proof. Yan's source does not prove the measurable quotient/core wrapper, and the standard VC theorem needs a legal measurable class/event before its probability statement can be consumed.
- Same-setting repair plausibility: Plausible at sketch depth. A revised wrapper can try to prove that every principal path/core used here is a finite Boolean combination of original measurable concept supports and then prove the sample-to-summary and sample-to-cylinder-probability maps measurable. If that cannot be done under the setting's existing convention, the obstruction must be reclassified as idea/theorem-contract depth rather than repaired by adding a new measurable-space assumption.
- Target-preserving bridge-repair gate: Adding the missing measurability lemmas, exact dependency edges, stable assumption ids, and strict constant inequalities preserves the setting, learner, privacy mode, PAC constants, candidate scope, and rate. Adding countability, finite \(X_i\), finite \(C_i\), standard-Borel structure, measurable-selection assumptions, or finite-support \(D_i\) would change the theorem contract.
- High-risk obligation classes: Audited classes are arbitrary-input replacement privacy, finite/empty positive supports, total sentinel geometry, measurable selection, improper-core generalization, median orientation, fixed-confidence concentration, exact quota arithmetic, routing, arbitrary-mass occupancy, ALMM expected-risk transfer, minimax, hard-factor activation, overflow, one-use privacy, tensorization, candidate quantifiers, generated-output flow, and both one-factor baselines. Only measurability, formal dependency/provenance records, and the strict median endpoint remain blocking.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| step_001 product identities | Structural input to both clauses | Full disjoint Cartesian product | assump:canonical-product; assump:vc-one-factors | Direct shattering and online-product arguments give exact VC/LD identities | Same binary labels, blocks, and product | \(\operatorname{VC}(C)=k\), \(\operatorname{LD}(C)=\sum_i d_i\), \(s_i\le r_i+2\) | Exact outputs; no surrogate | Exact class quantities | Finite nonnegative algebra | Primitive assumptions -> step_001 | Consumers step_006, step_007, step_010, step_015 | \(k=1,d_i=1\), infinite cardinality pass | step-local | None |
| step_002 quotient/root totality and measurable kernel | Upper factor producer | Fixed reference, quotient, finite-height VC-one tree, root, finite support | Direct wrapper of Yan lines 329--525 | Set-theoretic totality matches; measurable-kernel implication is not proved | Evaluation-cylinder declaration does not discharge the setting's input/output measurability | Every block has one \(y\) and one \(\ell_z\); root handles empty/inconsistent branches | Finite first-occurrence support exists only after the unproved core/summary map | Label/risk pullback is exact if the produced hypotheses and map are measurable | No stochastic accumulation; unresolved measurability of arbitrary version-space tests and path pullback | Intended step_001 -> step_002 -> upper consumers | Producer step_002; consumers step_003, step_005, step_015 | Empty version, root, infinite width are total but not measurably certified | sketch/interface defect | /proof-sketch |
| step_002 replacement-private choosing | Factor DP source | One-summary histogram plus BNSV choosing proof | BNSV alg:choosing, lem:CMprivacy; Yan printed privacy theorem excluded | Exact: replacement has one leaving and one entering coordinate, each support difference at most one | Fixed-size replacement, sensitivity one, \(k=1\), \(\varepsilon_c<2\) match | BNSV pointwise/normalizer proof; empty support has adjacent nonfailure mass \(p\le\delta_c\) | Finite positive supports export \((\varepsilon_c,\delta_c)\)-DP with no cardinality term | Exact candidate and pulled-back output, conditional on legal measurability | Two coordinates change but are controlled inside one proof, not composed | Total summaries -> choosing -> median/choosing composition | Producer step_002; consumer step_003 | Both privacy directions, \(\dagger\leftrightarrow p\), and empty support pass | step-local conditional on total measurable summary map | /proof-sketch |
| step_002 core closure and bad-block marginal | Generated utility condition | VC-one closure \(\mathcal K_i\) and Yan thm:learn vc | Yan thm:learn vc and lem:must comparable; unlabeled lemma excluded | Combinatorics match: comparable misses 10, incomparable misses 11; core/target are consistent members | Same quotient target and \(D_i\), but measurability of all consumed closure hypotheses/events is not proved | Empirical disagreement zero plus \(m_i\) VC bound gives \(\Pr Z_{ij}\le b\) | Raw version-space core exports the marginal only after legal measurable-class status | Exact same-target factor risk | Independent finite blocks after marginal production | Core construction -> VC marginal -> Hoeffding | Producer step_002; consumers step_002 comparison, step_005, step_015 | All-zero and empty-core combinatorics pass | sketch/interface defect only for measurability | /proof-sketch |
| step_002 confidence and exact quota | Quantitative factor utility specialization | Private median, BNSV utility, Hoeffding, explicit constants | Yan private median/order; BNSV lem:CMutility | Correct orientations and fixed-confidence arithmetic match | Natural log, base-two log-star, fixed replacement mode match | \(\Delta_i\le t_i/12\), bad count below \(t_i/12\), \(N_i\le8K_tT_iA_\beta\le q_i\) | Raw \(s_i,L_i\) support the exact squared-log export | Pointwise nested-positive-set risk comparison has zero residual | One finite Hoeffding sum and two failure events | Core marginal -> count -> comparison -> factor utility | Producer step_002; consumers step_005, step_006, step_015 | \(z=0,d_i=1,\delta\uparrow1\) pass; source strict \(t_i>n_{PM}\) not yet written | sketch/interface defect for strict endpoint | /proof-sketch |
| step_003 routed global privacy | Global DP closure | Multiset routing, symmetrization, basic composition | Direct derivation plus standard composition | At most two factor inputs are one-replacement adjacent | Exact setting adjacency and padding | Two costs \((\varepsilon/2,\delta/2)\) sum to \((\varepsilon,\delta)\) | Feasible once step_002 is a measurable DP factor kernel | Exact piecewise postprocessing | Support size two, no \(k\)-accumulation | step_002 -> step_003 -> step_015 | Producer step_003; consumer step_015 | Same/cross-block and padding pass | sketch/interface defect downstream of step_002 | /proof-sketch |
| step_004 occupancy and step_005 weighted upper utility | Generated global PAC event | Binomial Chernoff, conditional factor certificate, weighted Markov | Direct standard tools | Exact arbitrary-mass claim | Same \(D_i,\rho_i,R_D\) | \(\mathbb EW<3/1024\), \(R_D\le1/64+W\), Markov at \(3/64\) | Raw counts and factor failure probabilities export the PAC event | All padding/failure residuals appear in \(W\) | Finite nonnegative weighted sum; no union over factors | step_002, step_004 -> step_005 -> step_015 | Producers step_004/005; consumer step_015 | Zero mass and many light blocks pass | step-local after repaired step_002 | /proof-sketch |
| step_006 public quota sum | Explicit-rate bridge | Exact quota, \(s_i\le M\), \(k\le M/2\) | step_001 and step_002 arithmetic | Exact public sample threshold | Same log and fixed constants | Pointwise log domination and ceiling charge \(k\) | Feasible with universal \(C_{\mathrm{quota}}\) | No residual | Finite sum | step_001, step_002 -> step_006 -> step_015 | Producer step_006; consumer step_015 | \(k=1,\delta\uparrow1\) pass | step-local after exact step_002 endpoint | /proof-sketch |
| step_007 global VC lower | Additive lower source | Unseen independent labels | step_001 direct argument | Exact unrestricted global risk claim | Same target/product/risk; privacy unused | \(n<k/2\) gives expected risk at least \(1/4>31/256\) | Raw shattered points export \(n\ge a_{\mathrm{VC}}k\) | Exact | One finite expectation | step_001 -> step_007 | Consumers step_010, step_014 | \(k=1\) and improper output pass | step-local | None |
| step_008 ALMM expected-risk wrapper | Factor hardness source | Resampling plus active ALMM reduction | ALMM lem:bun, lem:reduction, thm:main, thm:shelah | Expected empirical loss \(1/8\) is the exact active numerical input | Replacement DP, unrestricted outputs, labels, risk, and delta scaling match | \(m<a_{\mathrm L}\log_2^*d\) under the factor delta budget implies risk \(>1/8\) | Finite threshold witness exports the factor floor | Uniform database makes empirical and population risk equal | One fixed candidate budget | Source -> step_008 -> step_009 | Producer step_008; consumer step_009 | \(d=1\) inactive; \(m=8\) and smaller epsilon pass | step-local | None |
| step_009 finite hard priors | Learner-independent task producer | Finite restriction and finite minimax | step_008 plus direct compact game | Exact strict prior-average factor risk | Same factor task and unrestricted output restriction | Attained finite game value remains \(>1/8\) | Hard priors on active factors; point masses elsewhere | Exact factor risk | Finite game per factor | step_008 -> step_009 -> step_012/013 | Producer step_009; consumers step_012, step_013 | Infinite ambient class and inactive factors pass | sketch/interface defect in assumptions-used cell; mathematics step-local | /proof-sketch |
| step_010 hard-set certificate | Activation and low-mass bridge | VC lower, ALMM constant, log-star arithmetic | step_007, step_008, and structural \(s_i\le r_i+2\) | Exact \(w_L\) and \(m_{n,i}\) claims | Same candidate weights and budgets | \(w_L<1/128\), \(m_{n,i}<a_{\mathrm L}r_i\) | Raw \(n<cM\), \(n\ge a_{\mathrm{VC}}k\), structural shift/floor controls | Exact | Finite factor partition | Intended step_001/007/008 -> step_010 -> step_012/013/014 | Producer step_010; consumers step_012, step_013, step_014 | All-low and floor-eight pass | sketch/interface defect in formal dependency/assumption record; mathematics step-local | /proof-sketch |
| step_011 overflow certificate | Coupling residual producer | Optimized binomial Chernoff | Direct derivation | Exact event and constant | Same \(m_{n,i}\) and slot law | \(\Pr O_i\le\eta_0<3/2048\) | Factor-four buffer and floor eight support export | Risk residual at most \(\eta_0\) | One local event, no union | step_011 -> step_012/013 and intended step_014 | Producer step_011; consumers step_012, step_013, explicitly step_014 | \(p=0,np=2,k=1\) pass | step-local; flow-to-step_014 record defective | /proof-sketch |
| step_012 hidden-factor learner | One-use DP and same-target coupling | Priors, hard certificate, overflow, sequential embedding | step_009, step_010, step_011; direct construction | Exact factor learner eligibility | Same replacement adjacency, full-product target, and factor risk | One factor row changes at most one global row; overflow branch is input-independent | All three required inputs precede construction | Off overflow exact ideal sample; residual \(1_{O_i}\) | One use and one global call | step_009/010/011 -> step_012 -> step_013 | Producer step_012; consumer step_013 | Zero slots, unused row, overflow, improper A pass | step-local | None |
| step_013 tensorized expected lower risk | Direct-sum risk producer | Independent finite priors, hidden learners, exact risk identity | step_009 through step_012 | Exact global expected risk | Full Cartesian target and mixture distribution match, but stable id is omitted in row | \(\mathbb ER_D>(1-w_L)(1/8-\eta_0)\) | Local marginal floors sum by finite linearity | Exact same targets; only explicit overflow/low-mass residuals | Finite nonnegative sum; output dependence irrelevant | step_009/010/011/012 -> step_013 -> step_014 | Producer step_013; consumer step_014 | One hard factor and correlated outputs pass | sketch/interface defect in assumption record; mathematics step-local | /proof-sketch |
| step_014 candidate contradiction | Lower theorem closure | PAC expectation ceiling and lower certificate | step_007, step_010, step_013, plus explicitly used step_011 numeric bound | Exact candidate-wise PAC impossibility | Same privacy/risk/candidate modes | \((127/128)(1/8-3/2048)>31/256\) | Raw \(w_L,\eta_0\) support exact contradiction | All residuals dominated | One fixed candidate | Intended step_007/010/011/013 -> step_014 -> step_015 | Producer step_014; consumer step_015 | \(n=1\), all-low, equality delta pass | sketch/interface defect in dependency record; mathematics step-local | /proof-sketch |
| step_015 exact theorem assembly | Final closure | Upper and lower accepted outputs | step_003, step_005, step_006, step_014 | Exact goal and remaining source gap | Modes and quantifiers match; stable primitive ids are not fully listed | Invoke upper for all delta and lower only at an admissible candidate | Feasible after measurable upper producer and table repairs | No new residual | Fixed sample/candidate only | Upper/lower producers -> step_015 | Final theorem | Candidate failure of delta condition correctly disables lower claim | sketch/interface defect downstream and in assumption record | /proof-sketch |
| One-factor upper baseline | Baseline invariance obligation | Single repaired factor call | step_002 through step_006 | Set-theoretic privacy/rate/utility match; measurable learner not certified | Same \(C_1,c_1,D_1,R_{D_1}\) | No factor aggregation; padding vanishes above quota | Exact squared-log quota | No weaker surrogate allowed | Finite internal blocks | step_002 -> upper closure | Final theorem | All-zero, inconsistent, and root branches pass pointwise | sketch/interface defect | /proof-sketch |
| One-factor lower baseline | Baseline invariance obligation | VC nonconstancy plus ALMM | step_007 through step_014 | Exact lower scale | Same factor task and candidate condition | Small \(r_1\) by VC, large \(r_1\) by ALMM; overflow zero | Exact active sources | No residual at \(k=1\) | One candidate/factor | Lower sources -> step_014 -> step_015 | Final theorem | \(r_1=0\) and large \(r_1\) pass | step-local | None |

## Blocking Issues

1. **step_002 measurable-kernel export.** The sketch's cylinder-sigma-field sentence does not prove that the version-space/core/depth/summary maps are measurable, that every path/core indicator is a measurable hypothesis, or that the sample-to-output probabilities are measurable for cylinder events. The finite first-occurrence rule begins only after those objects have been produced. Downstream effect: the advertised factor kernel, the VC probability event, global upper learner, and \(k=1\) upper baseline are not legal under the exact setting. Smallest repair: add a current-notation measurable-geometry lemma under the existing setting convention, with no new countability, finite-cardinality, topological, support, or standard-Borel assumption.

2. **Formal dependency and assumption-provenance inconsistencies.** The special triggering route into step_012/013 is fixed, but step_010 consumes the structural \(s_i\le r_i+2\) output assigned to step_001 without a matching dependency; step_014 explicitly consumes step_011's \(\eta_0<3/2048\) output without a matching dependency or re-export; and the assumptions-used cells of step_009, step_010, step_013, and step_015 do not name every used setting assumption by stable assump:* id. Downstream effect: generated-output consumption and later proof-step authority are ambiguous. Smallest repair: add the missing earlier dependencies or change the producer/export interfaces consistently, and replace prose assumption references by exact stable ids everywhere.

3. **Exact private-median endpoint.** The source theorem is stated for \(t_i>n_{PM}\), but the calibration records only \(t_i\ge n_{PM}\). The \(m_i\le4A_\beta\) line also suppresses the ceiling \(+1\). Downstream effect: the claimed exact quota certificate has a small but real endpoint gap. Smallest repair: enlarge the universal fixed-point inequality so \(t_i>n_{PM}\) is explicit and display the fixed numerical inequality including both ceilings; retain the same \(N_i\le q_i\) and squared-log dependence.

## Required Repair Bundle

1. **Measurable quotient/core/path wrapper (blocking issue 1; affects step_002, step_003, step_005, step_015; target /proof-sketch).** State the exact measurable structures already present in the setting and prove: quotient and constant-cell maps are measurable; every principal path and every realized core is a measurable binary hypothesis; the closure class used by Yan's VC theorem is a legal measurable class/event; and, for every fixed output cylinder, the maps through \(V(S)\), empty support, \(B(S)\), \(y(S)\), \(\ell_z(S)\), finite choosing, and pullback are measurable. A finite-intersection representation derived from the finite-height VC-one tree is a plausible same-setting route, but it must be proved. If no such route exists without a new measurable-space assumption, reclassify at idea/theorem-contract depth.

2. **Dependency and stable-id normalization (blocking issue 2; affects step_009, step_010, step_013, step_014, step_015 and both flow tables; target /proof-sketch).** Add step_001 to step_010 or rederive and reclassify every structural input there; add step_011 to step_014 or make step_013 explicitly export the numerical overflow bound and update every consumer path. Cite assump:vc-one-factors in step_009, the exact structural ids in step_010, assump:canonical-product in step_013, and all four stable ids individually in step_015. Preserve the already-correct \(\{\mathrm{step\_009},\mathrm{step\_010},\mathrm{step\_011}\}\to\mathrm{step\_012}\to\mathrm{step\_013}\) path and the single step_013 flow row.

3. **Exact constant endpoint (blocking issue 3; affects step_002 and step_006; target /proof-sketch).** Replace \(K_t\ge C_1\) by an explicit universal inequality that gives \(t_i>n_{PM}\), and show the ceiling calculation \(m_i\le \text{raw block size}+1\le4A_\beta\). Recheck \(\Delta_i\le t_i/12\), Hoeffding, \(N_i\le8K_tT_iA_\beta\), \(K\ge8K_t\), and \(N_i\le q_i\). No confidence may depend on \(t_i\), and no extra unbounded logarithm may appear.

4. **Triggering-repair disposition and target preservation (all issues; target /proof-sketch).** Repair items for the exact replacement-private BNSV extension, both privacy directions, empty support, VC-one core closure, fixed confidence, median-side orientations, same-target risk comparison, squared-log quota shape, and step_010-to-step_012-to-step_013 lower flow are discharged. The measurability part of the total-wrapper/source-record repair is not discharged. Preserve all four primitive assumptions, the routed/padded learner, arbitrary distributions, unrestricted improper output, nonrealizable-input replacement privacy, fixed PAC constants, arbitrary-\(\delta\) upper scope, candidate-wise lower scope, and the stated remaining open-problem gap.

## Review Rationale

Attempt 3 resolves the substantive privacy and utility obstructions that caused attempt 2 to fail: the BNSV proof really does handle a one-leaving/one-entering replacement at \(k=1\) without doubling privacy, the empty-support branch is private in both directions, the core closure has VC dimension at most one, the median/comparison orientations and nested-risk inequality are correct, the fixed-confidence arithmetic retains the squared logarithm, and the required hidden-factor producer chain is restored. It is nevertheless not ready for proof-step work because its theorem-facing factor output is asserted, not shown, to be a measurable kernel under the exact arbitrary-instance-space contract, and its formal dependency/assumption table still authorizes several outputs inconsistently. These are target-preserving sketch/interface repairs, not evidence that the conditional mathematical idea or lower direct sum is false, so REVISE_SKETCH with score 7 is the deepest justified status.
