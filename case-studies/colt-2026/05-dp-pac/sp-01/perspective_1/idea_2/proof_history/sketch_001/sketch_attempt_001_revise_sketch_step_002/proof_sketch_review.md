# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1, `perspective_1/idea_2/proof_sketch.md`, initial mode (`SEARCH_MODE=OPEN`), reviewed against the exact-goal contract in `perspective_1/idea_2/setting.md`.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: Exact-goal mode passes. The roadmap preserves the arbitrary-distribution upper clause, the unrestricted-improper candidate-wise lower clause, both fixed PAC constants, replacement adjacency, the public quota rate, and the condition that the sample-complexity sandwich is inferred only when `assump:candidate-delta-budget` holds at the actual candidate \(n_*=\mathrm{SC}_{\varepsilon,\delta}(C)\). The branch remains labeled `conditional` and explicitly leaves higher-VC indecomposable factors open, so it does not overclaim the source problem.
- Dependency audit: All 15 stable steps have exact claims, earlier-only dependencies, stable assumption ids, proof tools, output targets, rate objectives, and review states. The graph is acyclic. Every generated count, event, prior, hard-factor certificate, coupling, expected-risk bound, and theorem clause has a legal producer before every consumer.
- High-risk coverage: The sketch localizes and gives obstruction-level witnesses for Yan's quota and order convention, two-factor routed privacy, arbitrary-mass occupancy, weighted PAC conversion, product identities, the ALMM expected-risk wrapper, finite minimax, high-factor arithmetic, overflow constants, one-use hidden-factor privacy, independent-prior tensorization, numerical contradiction, and candidate-wise quantifiers. Boundary checks cover \(k=1\), \(d_i=1\), \(\rho_i=0\), \(n=1\), padding, cross-block replacement, infinite factor cardinality, and improper outputs.
- Explicit-rate coverage: Both rate objectives declare exposed variables, fixed quantities, allowed and forbidden hidden-constant dependence, probability mode, fixed-sample/candidate-wise horizon mode, risk metric, and every simplification bridge. `step_006` retains ceilings and fixed logarithmic constants, while `step_010`, `step_011`, and `step_014` expose all lower-bound losses and thresholds.
- Assumption and citation plausibility: The four setting assumptions remain primitive and every data-dependent fact remains derived. Yan arXiv:2505.06581v2 has the cited `alg:improperlearner`, `cor: threshold dimension and littlestone dimension`, `thm:composition`, and `thm:improper sample complexity`; its DP convention is fixed-size replacement adjacency and its proof parameter is \(TD(C_{i,f})+1\). ALMM arXiv:1806.00949 has the cited `lem:bun`, `lem:reduction`, `thm:main`, `thm:shelah`, and `thm:ADPimpliesLD`. The source proof supports the stated \(1/8\) expected-risk wrapper as detailed below, rather than merely the formal PAC corollary.

## Early Obstruction Audit

- Limiting-case stress: No counterexample is exposed by the allowed boundaries. At \(\rho_i=0\), the factor is always padded but has zero risk weight. At \(k=1\), routing has one affected mechanism, overflow is impossible because \(m_{n,1}\ge 4n\), and the upper and lower arguments reduce to the advertised VC-one sources up to universal constants. At \(d_i=1\), \(r_i=0\) deactivates ALMM but \(s_i=2\) keeps Yan's quota positive and the VC lower pays the constant term. At a candidate outside the delta condition, the proof exports no lower claim.
- Theorem-critical bridge support: Every nontrivial primitive-to-theorem bridge is assigned to one of `step_001` through `step_015`. In particular, neither the source theorem names nor a future-step label is used as the first source of a theorem-critical property.
- Exported-interface feasibility: The sketch's dedicated interface table supplies raw controls, exact outputs, defect splits, target-side comparisons, thresholds, and consumers. The upper path exports exact factor risk, exact tuple privacy, exact weighted padded/bad mass, and the exact quota sum. The lower path exports exact factor risk, exact budgets \(m_{n,i}\), exact same-target ideal couplings, and the exact global risk identity. No threshold is asked to control an unrelated persistent or empirical defect.
- Theorem-critical mechanism witness gate: Passes for all 15 steps. Each witness identifies the claim class, theorem role, legal source, matching positive/control term, opposing defects, a concrete equality or dominance relation, boundary behavior, and producer-consumer provenance. No witness is only a category such as minimax, Chernoff, composition, or later proof.
- Entry-state trace stress: No iterative or all-time recursion is present. The relevant first-transition tests pass: a cross-block replacement activates exactly two factor comparisons; a zero-mass block remains risk-null; an overflow branch becomes input-independent before an unavailable row is read; and the one-factor lower simulation uses at most \(n\) of its \(m_{n,1}\ge4n\) rows. No mechanism is inactive while its claimed conclusion remains false.
- Obligation locality classification: `step_001` through `step_015`, including both cited wrappers and all quantitative bridges, are `step-local`. Their mechanism sources and interfaces already occur in the binding sketch; local proof work must instantiate them without changing assumptions, algorithm, scope, metric, dependence, or conclusion.
- Noncircular closure gate: Passes. Counts come from primitive i.i.d. sampling; factor utility comes from the verified Yan wrapper; priors come from the already-derived ALMM interface and finite minimax; the hard set comes from independent VC and ALMM constants; overflow comes from the block-index sequence; and the hidden-factor learner consumes only previously produced objects. No success event, hard-factor membership, coupling, or final lower conclusion is assumed to prove itself.
- Mechanism-source and boundary stress: The structural lower sources are full Cartesian freedom, unseen independent labels, ALMM's empirical probability jump, the factor-four occupancy buffer, and one-use embedding. They remain active on the branches where consumed. Small-\(r_i\) factors are not falsely assigned ALMM hardness; padding is not assigned Yan utility; and overflow is not hidden inside privacy or realizability.
- Generated-output flow: Passes row by row. The paths in `## Generated Output Flow` are acyclic and complete, including `step_008 -> step_009 -> step_012 -> step_013 -> step_014` for source hardness and `step_011 -> step_012 -> step_013 -> step_014` for coupling loss.
- Source-to-claim adequacy: Yan's source uses datasets that differ in one entry and a final substitution that turns the internal \((2\varepsilon,2\delta)\) mechanism into the theorem's requested budget. Its internal \(TD(C_{i,f})+1\), together with \(TD\le2^{d_i+1}\), gives a positive endpoint and a universal-constant bound by \(s_i\); an independent random-permutation wrapper converts routed multiset replacement into one source sequence replacement without changing i.i.d. utility. For ALMM, assume a factor learner has worst-case expected risk at most \(1/8\). On every fixed realizable database of size \(9m\), sample \(m\) rows with replacement and run it. Linearity makes the expected empirical loss at most \(1/8\), hence expected empirical accuracy at least \(7/8\), exactly the sole utility input used in the active proof of `lem:reduction`. Bun et al.'s secrecy-of-the-sample lemma with ratio \(9\) keeps privacy within \((0.1,\delta)\); shrinking \(c_\delta\) discharges ALMM's condition at the \(9m\) empirical size. `thm:main` and `thm:shelah` then yield the claimed universal \(a_{\mathrm L}\) after the fixed factor-nine and log-star convention losses. Thus both cited wrappers have current-notation conclusions, object maps, hypothesis discharge, known non-outputs, and matching unrestricted-output conventions.
- Residual-to-target adequacy: Yan's unpadded output is evaluated on the same \((c_i,D_i,R_{D_i})\); padding is a separately weighted bad-block term. Piecewise postprocessing leaves no privacy residual. The ALMM wrapper and finite priors use the same factor risk. The only transformed experiment is the truncated hidden-factor simulation; off \(O_i\) it equals the ideal i.i.d. experiment for the same \((c_i,D_i)\), and on \(O_i\) bounded loss gives the explicit residual \(\eta_0\). The weighted identity transfers these exact local risks to \(R_D\), and `step_014` dominates both \(\eta_0\) and \(w_L\) at the theorem scale.
- Baseline invariance audit: The inherited \(k=1\) upper conclusion is preserved by one symmetrized Yan call; the half privacy allocation changes only a universal constant and monotonicity gives the advertised global budget. The inherited \(k=1\) lower conclusion is the universal-constant maximum of nonconstancy/VC hardness and ALMM log-star hardness under the same candidate delta contract. Overflow is then zero, so no stopped, conditional, or remainder-only surrogate replaces either baseline.
- Scope-accumulation compatibility: All aggregation is finite and fixed-sample. Privacy accumulates over a support of at most two affected factors: \((\varepsilon/2,\delta/2)+(\varepsilon/2,\delta/2)=(\varepsilon,\delta)\). Upper errors accumulate as a nonnegative weighted mass with total weight one. Lower overflow is subtracted once per local marginal and then averaged, not union-bounded; low-factor mass is discarded once. No persistent, adversarial-sign, all-time, uniform, limiting, or cross-candidate defect is present.
- Scope and dependence consistency: The upper clause remains uniform over all \(0<\delta<1\); the lower clause is asserted separately for each candidate satisfying both displayed delta conjuncts. The proof uses the factor-budget conjunct and deliberately retains the \(1/[n\log(n+1)]\) source-regime conjunct. All hidden constants are universal, and no dependence on \(k,d_i,|C_i|,D,n,\varepsilon,\delta\) is hidden outside the declared expressions.
- Generated-condition provenance: Occupancy, success, priors, hard-set membership, overflow, simulated learners, and expected-risk certificates are all derived outputs. None is moved into a theorem-facing assumption or admissibility package.
- Citation and tool applicability: Yan v2's exact labels and source roles match the sketch; the theorem does not supply product privacy, padded utility, cardinality, or the lower clause, and the sketch supplies those bridges separately. ALMM's formal corollary supplies only PAC hardness, but its active proof obtains the needed jump from expected empirical loss \(\le1/8\); the sketch correctly treats the expected-risk statement as a current-notation proof wrapper. The finite threshold restriction makes randomized DP kernels a compact finite-dimensional polytope on all finite labeled databases, including nonrealizable ones, so finite minimax is applicable to arbitrary improper outputs after restriction to their finite label vector.
- Same-setting repair plausibility: No material obstruction requiring repair was found. The remaining work is local derivation and source instantiation under the already fixed setting.
- Target-preserving bridge-repair gate: Not triggered. All theorem-critical bridges are already present and preserve the exact target, including both \(k=1\) baseline conclusions.
- High-risk obligation classes: Structural identities, source wrappers, perturbation/coupling loss, mode conversion from PAC to expectation and back, finite-prior generation, arbitrary-mass aggregation, explicit-rate simplification, and candidate-wise scope were all checked. None requires a new primitive condition or theorem-contract change.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Product VC, LD, and finite-cardinality identities | Exact structural interface for both clauses | Disjoint full Cartesian product and factor VC/LD | Primitive `assump:canonical-product`, `assump:vc-one-factors`; direct derivation | Projection caps VC per block, product witnesses give the lower directions, and factorwise online learners give the LD upper direction | Exact branch labels, disjoint domain, binary representation, and whole-domain partition | \(\operatorname{VC}(C)=k\), \(\operatorname{LD}(C)=\sum_i d_i\); finite products multiply cardinalities | Raw factor classes export exact global quantities, plus \(s_i\ge2\), \(k\le M/2\), \(s_i\le r_i+2\) | Exact same class object; no surrogate residual | Finite nonnegative algebraic sum; no limiting defect | Primitive assumptions -> `step_001` -> all consumers | `step_001`; consumers `step_006`, `step_007`, `step_010`, `step_015` | \(k=1\), \(d_i=1\), and infinite-cardinality boundary pass; cardinality identity is conditional on finiteness | `step-local` | None |
| Yan factor learner and exact quota \(q_i\) | Cited wrapper supplying factor DP and PAC utility | Yan v2 `alg:improperlearner`, `cor: threshold dimension and littlestone dimension`, `thm:improper sample complexity`; current-notation endpoint wrapper | Cited theorem plus direct \(TD+1\) and symmetrization bridge | \(TD(C_{i,f})+1\le2^{d_i+1}+1\) and a universal \(K\) make the source threshold at most \(q_i\); utility is invoked only unpadded | Source and branch use fixed-size replacement adjacency, binary labels, arbitrary distributions, improper output; random permutation aligns multiset and sequence conventions | Source threshold \(\le q_i\) after \((\alpha,\beta,\varepsilon,\delta)=(1/64,1/4096,\varepsilon/2,\delta/2)\) | Raw VC-one, finite LD, and positive parameters export exact factor DP and \(R_{D_i}\le1/64\) with failure \(1/4096\) | Same \(c_i,D_i,R_{D_i}\); padding is excluded and separately charged | One fixed call per factor; implicit constant and log-star shift absorbed by \(K,s_i\) | Source + `step_001` -> `step_002` before routing or utility | `step_002`; consumers `step_003`, `step_005`, `step_006` | \(d_i=1\), \(\delta\uparrow1\), tiny positive delta, infinite \(C_i\), and nonrealizable padded DP inputs pass | `step-local` | None |
| Routed two-affected-factor privacy | Exact global replacement-DP certificate | Multiset routing, coupled permutations, basic composition, postprocessing | Direct derivation from `step_002` | A replacement changes zero, one, or two factor multisets, each by at most one replacement | Permutation wrapper turns each changed multiset pair into source sequences differing in one entry; DP holds on nonrealizable data | At most \(2(\varepsilon/2,\delta/2)=(\varepsilon,\delta)\) | Exact factor DP outputs export the tuple, then deterministic piecewise hypothesis | Piecewise learner is exact postprocessing; no residual | Finite support two, independent of \(k\); no repeated accumulation | `step_002` -> `step_003` -> `step_015` | `step_003`; consumer `step_015` | Same-block, cross-block, selected/unselected, padded/full, and \(k=1\) transitions pass | `step-local` | None |
| Weighted arbitrary-mass occupancy | Generated padded-block mass bound for upper utility | Binomial marginals and light/heavy split at \(128q_i\) | Primitive i.i.d. sampling plus exact quotas | Light blocks are charged by mass; heavy blocks have a multiplicative lower-tail bound | Exact branch block masses and conditional distributions; no balance or independence among counts assumed | \(\mathbb E\sum_i\rho_i1\{N_i<q_i\}\le128Q/n+e^{-16}\) | Raw \(N_i\sim\mathrm{Bin}(n,\rho_i)\), \(\sum_i\rho_i=1\), \(q_i\ge1\) export exact padded risk weight | Produced variable is exactly the padded-factor contribution weight | Finite weighted nonnegative sum; dependence among counts is irrelevant and no union bound is used | Primitive sample -> `step_004` -> `step_005` | `step_004`; consumer `step_005` | \(\rho_i=0\) contributes exactly zero; arbitrarily many tiny blocks and one full-mass block pass | `step-local` | None |
| Weighted PAC conversion | Global high-probability upper utility | Yan conditional utility, occupancy bound, Markov inequality | `step_002`, `step_004`; direct risk algebra | Conditional first quota records are i.i.d. from \(D_i\), and every bad factor is charged by its true \(\rho_i\) | Same distributional-risk convention; no factor-output independence is assumed | \(R_D\le1/64+W\), \(\mathbb EW<3/1024\), and Markov at \(3/64\) gives failure below \(1/16\) | Exact shortage and factor-failure controls export exact \(R_D\le1/16\) with probability at least \(15/16\) | \(W\) contains every padding and factor-utility residual with its exact weight | One finite weighted aggregation at fixed \(n\); no horizon accumulation | `{step_002, step_004}` -> `step_005` -> `step_015` | `step_005`; consumer `step_015` | Zero-mass padded blocks, arbitrary wrong bad hypotheses, and \(k=1\) pass | `step-local` | None |
| Quota-sum specialization | Public explicit upper rate | Exact quota formula and structural inequalities | `step_001`, `step_002`; direct arithmetic | Fixed factor terms, logarithms, and all ceilings are retained and dominated | Natural-log public rate and source log-star convention are separated; only universal shifts are absorbed | \(Q\le C(M/\varepsilon)\log^2(16384M/(\varepsilon\delta))+k\le C_{\mathrm{quota}}(M/\varepsilon)\log^2(eM/(\varepsilon\delta))\) | Raw \(s_i\le M\), \(\sum_i s_i=M\), \(k\le M/2\), \(\varepsilon\le1/10\) export exact \(Q\) | Exact consumed sample threshold; no dropped term | Finite nonnegative sum; ceiling defect adds at most \(k\) | `{step_001, step_002}` -> `step_006` -> `step_015` | `step_006`; consumer `step_015` | \(k=1,d_i=1,\delta\uparrow1\), and tiny delta pass because the public logarithm has a universal lower bound | `step-local` | None |
| Global VC lower certificate | Additive one-per-factor lower source | Independent unseen labels on one shattered point per block | `step_001`; direct random-target argument | Full product supplies all \(2^k\) labelings and arbitrary improper output cannot predict unseen fair labels | Same global sample, risk, and unrestricted-output convention; privacy is not needed | If \(n<k/2\), expected unseen mass is at least \(1/2\), so expected risk is at least \(1/4>31/256\) | Raw shattered set and uniform distribution export \(n\ge a_{\mathrm{VC}}k\) | Exact global risk; no surrogate | One finite expectation; collisions only increase unseen mass | `step_001` -> `step_007` -> `step_010`, `step_014` | `step_007`; consumers `step_010`, `step_014` | \(k=1,n\ge1\) is covered by the candidate convention; point-function factors pass | `step-local` | None |
| ALMM expected-risk wrapper at \(1/8\) | Cited current-notation unrestricted factor lower interface | ALMM `lem:bun`, `lem:reduction`, `thm:main`, `thm:shelah`, `thm:ADPimpliesLD`; Bun et al. Lemma 5.9 | Cited proof path plus explicit expectation/resampling wrapper | Expected risk at most \(1/8\) yields expected empirical accuracy at least \(7/8\), exactly the active `lem:reduction` input; threshold embedding yields log-star hardness | Replacement adjacency, unrestricted outputs, binary relabeling, same distributional loss; factor-nine and base shifts enter universal constants | \(m<a_{\mathrm L}\log_2^*d\) and \(\delta\le c_\delta/[m^2\log(m+1)]\) imply \(\sup_{c,D}\mathbb ER_D>1/8\) | Raw expected-risk hypothesis on an \(m\)-sample exports the exact factor risk floor after 9-to-1 resampling | Uniform-on-database population risk equals empirical risk; no target residual | One fixed budget; factor-nine, delta denominator, and additive log-star shifts are constant losses | Cited proof -> `step_008` -> `step_009`; independent of product conclusion | `step_008`; consumer `step_009` | \(d=1\) is deliberately inactive, \(m=8\) is defined, smaller epsilon transfers by monotonicity, and improper outputs remain allowed | `step-local` | None |
| Finite hard priors | Algorithm-independent factor task distributions | Finite ALMM threshold restriction and finite zero-sum minimax | `step_008` plus finite-dimensional direct argument | The compact polytope contains all randomized DP kernels on finite labeled databases, and loss is bilinear; output label vectors include improper hypotheses | DP constraints include nonrealizable finite datasets and exact replacement adjacency; tasks remain realizable inside \(C_i\) | \(\min_B\max_\theta L(B,\theta)>1/8\) implies a finite \(\nu_i\) with \(\min_B\mathbb E_{\nu_i}L(B,\theta)>1/8\) | Raw finite threshold tasks export a prior for each subcritical factor; fixed realizable point masses complete other factors | Loss is exact \(R_{D_i}\); finite support is a subproblem, not a surrogate target | One compact game per factor; finite product of priors exists | `step_008` -> `step_009` before any hidden-factor learner is defined | `step_009`; consumers `step_012`, `step_013` | Infinite ambient \(C_i,X_i\), arbitrary outputs, and absence of hard low-factor priors pass by finite restriction and baseline completion | `step-local` | None |
| High-factor and low-mass certificate | Structural membership and subcritical-budget bridge | VC lower, ALMM constant, and explicit \(s_i,r_i,m_{n,i}\) arithmetic | `step_007`, `step_008`; direct derivation | Separate sources pay low-factor count, floor eight, ceiling one, and log-star shift | Exact theorem weights \(\pi_i=s_i/M\) and candidate budgets are used | With \(R_0=\lceil32/a_{\mathrm L}\rceil\) and \(c\le\min\{a_{\mathrm L}/64,a_{\mathrm{VC}}/[128(R_0+2)]\}\), \(w_L<1/128\) and \(m_{n,i}<a_{\mathrm L}r_i\) on \(H\) | Raw \(n\ge a_{\mathrm{VC}}k\), \(n<cM\), \(s_i\le r_i+2\) export exact hard-set interface | Exact weights and budgets; no surrogate | Finite deterministic partition; all discarded mass is explicit | `{step_007, step_008}` -> `step_010` -> `step_013`, `step_014` | `step_010`; consumers `step_013`, `step_014` | All-low factors contradict the assumed regime through VC; one high factor and floor/ceiling transitions pass | `step-local` | None |
| Binomial overflow certificate | Generated coupling-loss control | Optimized binomial Chernoff bound | Primitive \(m_{n,i}\) definition; direct derivation | Factor-four buffer and floor eight cover both mean regimes | Exact \(L_i\sim\mathrm{Bin}(n,\pi_i)\), integer overflow event \(L_i>m_{n,i}\) | \(\Pr[O_i]\le\eta_0=e^7(2/9)^9<3/2048\) via threshold at least \(\max\{9,4\mu+1\}\) | Raw mean and quota export the exact ideal/truncated disagreement probability | A bounded local risk loses at most \(\eta_0\); no other residual | One local subtraction per factor marginal; no union over factors | Block-index sequence -> `step_011` -> `step_012`, `step_013`, `step_014` | `step_011`; consumers `step_012` to `step_014` | \(p=0,np\le2,np=2,np\ge2,n=1\), and ceiling transitions pass | `step-local` | None |
| Hidden-factor learner and one-use adjacency | Exact factor-DP reduction and same-target coupling | Sequential one-use embedding, constant overflow branch, one global call to \(A\) | `step_009`, `step_010`, `step_011`; direct construction | Each input row changes at most one global row; arbitrary global output restricts to an allowed improper factor output | Input rows stay in \(X_i\), global replacement adjacency is exact, and DP is required on nonrealizable inputs | Conditional on auxiliary randomness, one input replacement maps to zero or one global replacement; overflow output is input-independent | Priors, block labels, and streams export an eligible \((\varepsilon,\delta)\)-DP \(B_i\) and ideal experiment | Off overflow the same \((c_i,D_i)\) and output agree; residual is at most \(1_{O_i}\) in \(R_{D_i}\) | No privacy composition across input rows or factors; one use and one call | `{step_009, step_010, step_011}` -> `step_012` -> `step_013` | `step_012`; consumer `step_013` | Zero requested rows, used/unused replacement, overflow, \(k=1\), arbitrary labels, and improper \(A\) pass | `step-local` | None |
| Independent-prior tensorization and weighted lower risk | Direct-sum expected-risk certificate | Product of finite priors, same ideal experiment, exact risk identity | `step_009` to `step_012`; direct Tonelli/linearity | Each \(B_i\) marginal matches the same product-prior ideal experiment; no factor is selected after observing \(A\) | Targets combine by full Cartesian equality and rows are i.i.d. from \(D=\sum_i\pi_iD_i\) | \(\mathbb ER_{D_i}(A\text{ restricted to }X_i,c_i)>1/8-\eta_0\) on \(H\), hence \(\mathbb ER_D>(1-w_L)(1/8-\eta_0)\) | Raw hard priors, mass, and couplings export exact global expected risk despite joint output dependence | Local overflow residual is weighted once; low mass is explicit; all targets are unchanged | Finite nonnegative weighted sum; no union or independence of output coordinates needed | `{step_009, step_010, step_011, step_012}` -> `step_013` -> `step_014` | `step_013`; consumer `step_014` | One hard factor, zero-risk low factors, correlated joint output, and infinite ambient factors with finite priors pass | `step-local` | None |
| Numerical PAC contradiction and witness extraction | Candidate-wise lower theorem | PAC-to-expectation inequality plus hard-mass lower certificate | `step_007`, `step_010`, `step_013`; direct arithmetic and logic | Exact expected-risk upper and lower use the same global object and metric | Same high-probability PAC convention and exact failure threshold | \((127/128)(1/8-3/2048)=32131/262144>31744/262144=31/256\) | Raw \(w_L<1/128\), \(\eta_0<3/2048\), and PAC upper export \(n\ge c_{\mathrm{low}}M\) and a deterministic failure witness | Every lower residual is present and numerically dominated | One fixed-candidate comparison; strictness supports witness extraction | `step_013` -> `step_014` -> `step_015` | `step_014`; consumer `step_015` | \(n=1\), all-low, one-high, and equality in the delta threshold pass | `step-local` | None |
| Candidate-wise final assembly | Exact theorem closure and scope guard | Accepted earlier interfaces and sample-complexity definition | `step_003`, `step_005`, `step_006`, `step_014`; direct logical assembly | Each clause is invoked only on its proved parameter scope | Upper arbitrary-delta mode and lower candidate-wise mode remain distinct; risk and privacy conventions are unchanged | Lower uses both-conjunct assumption at the current \(n\); sandwich uses it only at \(n_*\) | Raw upper and lower outputs export exactly the setting's two clauses | No expectation-only or uniform-delta surrogate is exported | No cross-candidate accumulation or inference | Earlier theorem outputs -> `step_015` -> final theorem | `step_015`; final theorem consumer | If the condition fails at \(n_*\), the lower and sandwich are inactive while the unconditional upper remains | `step-local` | None |
| One-factor upper baseline | Baseline invariance obligation | Single Yan factor call with routing/padding and privacy monotonicity | `step_002` to `step_006` | Same VC-one PAC conclusion and displayed dependence, up to universal allocation constants | Produced and consumed class, target, distribution, and risk are identical | Only one factor changes; \((\varepsilon/2,\delta/2)\) implies advertised \((\varepsilon,\delta)\) | Raw Yan interface exports the one-factor theorem without cardinality or properness | Padding is absent once \(n\ge2^{17}q_1\); no baseline residual | No factor aggregation | `step_002` -> `step_003`, `step_005`, `step_006` -> `step_015` | Upper baseline produced by `step_015`; final theorem consumes it | \(k=1,\rho_1=1\): full occupancy is deterministic once \(n\ge q_1\) | `step-local` | None |
| One-factor lower baseline | Baseline invariance obligation | VC nonconstancy plus ALMM log-star hardness | `step_007` to `step_014` | The two sources cover small and large \(r_1\) and yield \(\Omega(s_1)\) under the same candidate condition | Same factor class, target, distributional risk, unrestricted output, and candidate privacy mode | High branch has \(m_{n,1}\ge4n\) and zero overflow; low branch is paid by VC with the chosen universal \(c\) | Raw VC and ALMM interfaces export the exact one-factor lower conclusion | Overflow residual is zero; no stopped or expectation-only theorem replaces PAC impossibility | One candidate and one factor | `step_007`, `step_008` -> `step_010` to `step_014` -> `step_015` | Lower baseline produced by `step_014`; final theorem consumes it | \(r_1=0\) uses VC; \(r_1\ge R_0\) uses ALMM; both boundary branches close | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is exact-goal aligned, source-adequate at the two nonstandard cited wrappers, structurally complete, and ready for local proof work. Every theorem-critical hard obligation has an existing mechanism source and a fixed current-notation interface, every generated output has a noncircular producer-consumer path, and every residual is compared with the exact consumed risk scale. The lower direct sum does not assume factorwise behavior of the global learner: finite minimax fixes each prior before the common experiment, the one-use reduction preserves unrestricted replacement DP, and linearity handles arbitrary dependence among output coordinates. Therefore no sketch or theorem-contract change is presently required, and all 15 steps are accepted for downstream proof-stage allocation.
