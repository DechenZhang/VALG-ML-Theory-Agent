# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 4
- Review attempt: 4
- Producer unit attempt: 1
- Reviewed artifact: `perspective_2/idea_3/proof_sketch.md`
- Reviewed sketch SHA-256: `774fc8092a34259ff9232366f5f67a9240539ac7ef7e7aaee261620698e24d01`
- Binding setting: `perspective_2/idea_3/setting.md`
- Binding setting SHA-256: `13cb7296ea48e286ba553a4a153b84c00bd51c97f43c74d6b74f62321258d327`
- Current idea SHA-256: `5fb501c41e664dc96aff86222e7f484a7546c914354be1e625378d7185289531`
- Proof tracker SHA-256: `365c3b4b4c502be30ef5caa0831383277242a0272e3919164f35864f9b17f6bc`
- Technical-survey SHA-256: `bdd87666f3bee8bc2b342b904b65b24113a713a53578a6bf8ae756b9d25f1b7d`
- Prior live attempt-3 review SHA-256: `6cd0370c57c1bc5ee93035f76072e2bc538438942ce162ed1ed8a2e2b13a5bf2`; used only as failure context.
- Evidence boundary: no artifact under `proof_history/` was used as evidence.
- Goal mode: exact-goal mode.
- Progress type: `material_partial`.
- Reviewed roadmap: a zero-step, pre-proof obstruction diagnostic asserting that the unchanged arbitrary-class Lyu procedure cannot be made into the measurable learner, marked kernel, and event interfaces required by the binding theorem contract.

## Sketch Viability Score

3

## Sketch Review Status

IDEA_FAIL

## Smallest Retry Target

/subagent-idea-generator

## Retry Mode

new_idea

## Audit Summary

- Goal alignment: The sketch preserves the exact learner, arbitrary possibly infinite class scope, unconditional probability mode, output identity, metric, rate, and `material_partial` label. It does not prove that statistical rate false for every learner. It instead gives an allowed `v=d=1` class on which the particular exact learner required by the goal has no all-input measurable kernel under the conventional Borel sample and evaluation-output interfaces. Because the exact learner is itself part of the binding goal, that is a goal-level obstruction rather than target drift.
- Dependency audit: There is no step graph. That would be invalid for a viable proof roadmap, but here the obstruction precedes every legal producer: restrictions and finite laws exist pointwise while the measurable state/output kernel needed by privacy, marking, Fubini, PAC conversion, and rate export does not. Relabeling this obligation as a future step would not supply a source.
- High-risk coverage: The review independently checked the non-universally-measurable point-class construction, both dimension-one claims, the diagonal restriction and empty/singleton list split, the full-codomain cylinder and risk identity, the occurrence projection, the direct marked bad event, the exact `E_good` witness and integer interval, the old-Lyu propagation, and the locality route. The decisive kernel witness is valid. The direct-bad diagonal is nonrealizable for the fixed `(D,c)` away from `t_0`, so it is not by itself a realizable-PAC lower bound; it remains decisive for the all-input learner-kernel and marked-event interface.
- Explicit-rate coverage: The exposed variables, hidden-constant restriction, fixed-sample mode, `d=0` branch, trace fixed point, holdout inequality, lower-tail exponent, finite multiplicity, and new/old scalar rates remain coherent conditional calculations. No public rate can be exported without a learner kernel and an unconditional measurable PAC event.
- Assumption and citation plausibility: The point-class dimension and measurability facts are direct. The listed Lyu ingredients provide fixed-input combinatorial lists, conditional finite laws, structural descent, privacy arithmetic, and scalar bounds, but no cited result supplies measurable dependence of restrictions, lists, atom weights, or output laws on the sample. No standard-Borel, permissibility, measurable-graph, measurable-enumeration, or universally measurable envelope source appears in the setting.

## Early Obstruction Audit

- Limiting-case stress: The no-data `d=0` singleton branch remains valid. For a non-universally-measurable `A` with at least two points, `C_A={h_t:t in A}` shatters one point and cannot shatter two; after a label-one root branch its version class is a singleton, so no depth-two Littlestone tree exists. Hence `VC(C_A)=LD(C_A)=1`. Removing `t_0` does not make `A` Borel or universally measurable. The positive-dimensional boundary therefore lies inside the binding scope.
- Theorem-critical bridge support: On the all-positive diagonal sample, empirical errors are exactly zero for `h_x` when `x in A` and one for every available alternative; when `x notin A` all point functions have error one. Under the displayed source cutoff below one, the restriction is respectively `{h_x}` or empty. The empty/nonempty state coordinate therefore pulls back to `1_A`. Pointwise finite lists and finite mechanism masses cannot bridge that coordinate to a measurable state or kernel.
- Exported-interface feasibility: The exact fixed-input state, list supports, Sparse Sample masses, occurrence sets, empirical error, trace tails, and scalar margins are available. The required exports are sample-measurable lists, `s -> K_s(E)`, marked coordinates, a jointly measurable direct bad event, a measurable `E_good` route, and the final DP/PAC law. For the cylinder `E={f:f(t_0)=0}`, the exact diagonal law is zero off `A\{t_0}` and strictly positive on `A\{t_0}`; the sketch's stronger constant-`p` formula is consistent with the singleton symmetric state, but constancy is not needed because measurability would already make the positivity set Borel. Thus the raw controls cannot support the exported interface.
- Theorem-critical mechanism witness gate: Failed at theorem-contract level. The mechanism source is only a family of finite laws indexed set-theoretically by datasets. Finite support supplies cardinality, normalization, and pointwise privacy ratios, not measurable variation. The nonmeasurable positivity set is an exact qualitative obstruction that no margin, concentration exponent, dependency change, or later local proof can absorb.
- Entry-state trace stress: The first positive diagonal input already activates the obstruction: `x in A` produces singleton restrictions and a positive-probability actual `h_x` output, whereas `x notin A` produces empty restrictions and only the fixed default. The output cylinder separates these branches. For the marked coordinate, every actual occurrence set is `[k]`, yielding the same nonmeasurable positivity set in each mark coordinate. The `k=2`, empirical-error-zero, early-stage, empty-list, and default paths do not remove it.
- Obligation locality classification: The point-class dimension calculation, fixed-input restriction calculation, positivity of the singleton actual-output path, pointwise occurrence projection, risk-cylinder identity, integer-spacing calculation, holdout inequality, one-sided Chernoff bound, finite multiplicity, finite DDim recurrence, privacy arithmetic, and scalar rate algebra are `step-local`. The measurable sample-to-state/output kernel, occurrence kernel, direct marked event, exact unconditional `E_good` export, common DP/PAC interface, and old-Lyu baseline propagation are `idea/theorem-contract defect`. There is no remaining `sketch/interface defect` whose repair would preserve all binding primitives and the exact procedure.
- Noncircular closure gate: The pointwise finite laws are noncircular as set-theoretic objects, but they do not close measurability. Declaring a sigma-field or completion specifically to contain every preimage needed by the exact learner changes the distribution/sample interface and supplies the missing theorem-facing source by fiat. Likewise, assuming a measurable final event in order to integrate its sections is the output that the repair must produce.
- Mechanism-source and boundary stress: The full-codomain event `E` is a cylinder in the evaluation sigma-field, not an artificial subset of `C_A`. With `D=delta_{t_0}` and `c=h_{t_0}`, it is exactly `{f:err_D(f,c)>alpha}` for every binding `alpha<1/4`. Thus neither a smaller proper-output view nor omission of the risk event from the output sigma-field is compatible with the goal. A richer sigma-field only adds kernel obligations.
- Generated-output flow: Restrictions, lists, fixed-input Sparse Sample laws, and marked formulas have only sectionwise producers. There is no legal measurable producer before privacy, direct-event formation, Fubini, PAC utility, confidence accounting, or rate specialization. The pointwise projection of the marked law to `K_s` is exact, but projection does not create coordinate measurability.
- Source-to-claim adequacy: The direct formulas match their fixed-input claims. They do not match the claim class "measurable kernel": on diagonal inputs `K_s(E)` has positivity set `A\{t_0}`, and each actual mark coordinate has the same positivity set. The standard finite-sum fact applies only after measurable support graphs, atom weights, and membership coordinates exist. None is primitive or cited. The exact output and branch conventions used by the witness match the sketch's fixed-default totalization and actual-function lists.
- Residual-to-target adequacy: There is no surrogate-function or metric residual: the same actual `h_x` is used for empirical and population errors, and the occurrence projection has zero distributional residual pointwise. The missing residual is total at the interface level: a nonmeasurable coordinate cannot be transferred into a Markov kernel or a jointly measurable event at any quantitative scale.
- Baseline invariance audit: The `d=0`, finite-class, `v=d`, scalar `d^5`, and small-`delta` calculations survive. The infinite `v=d=1` witness is itself an allowed baseline boundary. The old-Lyu arm uses the same exact restriction/list/Sparse Sample maps, so changing only its block exponent does not alter the nonmeasurable positivity set. Its scalar rate is preserved, but its common unconditional DP/PAC interface is not; the public minimum therefore cannot be assembled as stated.
- Scope-accumulation compatibility: The obstruction occurs in one state transition and one kernel coordinate, before accumulation. Conditional modules have finite unions, the DDim recurrence spends one unit from a budget at most `d`, and the marked PAC calculation has a single finite occurrence union. No persistent forcing defect remains there. Those finite budgets cannot control the qualitative kernel defect.
- Scope and dependence consistency: The witness fixes admissible public parameters and uses `d=v=1`; it does not alter `alpha,beta,epsilon,delta`, the rate, the output metric, or the fixed-sample mode. The output event is required both by the all-input randomized-learner interface and by a legitimate risk specialization. Using outer probability, a `D`-dependent completion, a countable/permissible class restriction, or an encoded replacement learner would change a binding mode, scope, primitive assumption, or procedure.
- Generated-condition provenance: `E_good`, actual selection, the occurrence mark, empirical success, and population failure are correctly treated as derived. The fixed-section trace inequality and marked finite sums give only sectionwise derivations. The exact `E_good` witness with `C_A^0={0} union C_A` pulls its failure event back to `A`: choose `z_0 notin A`, a fixed partition, and integer `q` with `gamma m/2<q<=2 gamma m/3`. The interval length is `gamma m/6>1` under the displayed `C_blk=2^20` bound, so such an integer exists; `k>=2` gives full error at most `gamma/3`, while the selected block error exceeds `gamma/2`. This validates nonmeasurability of the exact joint event. It does not alone prove that every conceivable measurable envelope fails, but no envelope source is present, and the independent final-event route is already blocked by the kernel witness.
- Citation and tool applicability: Sauer-Shelah and fixed-input without-replacement tails remain adequate for their conditional roles. The Lyu list, AboveThreshold, Sparse Sample, SOA, and privacy results remain adequate only at the set-theoretic or fixed-input interfaces stated in the sketch. They are not measurability or permissibility theorems. The local one-sided Chernoff proof avoids the known factor issue in the cited two-sided display. No unresolved citation is being used to turn the kernel obstruction into a future step.
- Same-setting repair plausibility: None survives. An added standard-Borel/countability/permissibility/measurable-graph assumption changes primitive scope; a measurable encoding, decoder, fallback, quotient, or alternative list mechanism changes the exact procedure or output identity; outer probability or `D`-dependent completion changes the probability mode; restricting to finite classes changes class scope. Merely adding bridge steps cannot change the exact preimage `A\{t_0}`.
- Target-preserving bridge-repair gate: Failed. Attempt 3's requested bridge would have been target-preserving only if the unchanged setting already supported a measurable kernel. The explicit `v=d=1` witness shows it does not. Every viable repair adds an unsupported theorem-facing mechanism source or changes the procedure, scope, or success/probability criterion, so `/proof-sketch` is no longer a sound retry target.
- High-risk obligation classes: Structural dimension, finite restriction support, sample-to-state measurability, generated-output flow, occurrence projection, conditional-to-unconditional conversion, nonpermissible-class behavior, exact risk-event formation, fixed-point and tail arithmetic, DP scope, explicit-rate specialization, and baseline propagation were all checked. The structural and scalar classes pass conditionally; the measurable-kernel/event class fails at theorem-contract depth.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Non-universally-measurable point-class witness and `VC=LD=1` | Structural obstruction setup inside the positive-dimensional theorem scope | Direct point-function construction and direct VC/Littlestone argument | Primitive arbitrary-class scope; standard existence of a non-universally-measurable subset of `[0,1]` | Exact: one point is shattered; two points are not; a label-one root branch leaves a singleton | Every `h_t` is Borel under the diagnostic Borel instance; no nonmeasurable concept is used | Version class after root label one is `{h_x}`, so depth two is impossible | Exports an allowed infinite class with `v=d=1` for all downstream stress tests | No object-transfer residual; this is the exact class consumed | No accumulated defect; removing one Borel singleton preserves nonmeasurability | Direct construction precedes every kernel claim and assumes no target conclusion | Sketch construction produces the class for restriction, kernel, mark, and risk witnesses | `d=1`, at least two indices, and `t_0` removal all pass | step-local | None |
| Diagonal restriction and empty/singleton list state | Generated state and first learner-entry interface | Exact empirical restriction cutoff and actual singleton list construction | Fixed-input Lyu definitions under the binding exact procedure | Error zero versus error one supplies exactly `{h_x}` versus empty when the cutoff is below one | Borel sample diagonal and a state sigma-field exposing empty/nonempty are the minimal conventions; the pullback fails there | `H_i^r(Delta(x))={h_x}` on `A` and is empty off `A`; empty/nonempty equals `1_A` | Finite cardinality and actual identity are raw controls; measurable state/list export is infeasible | Exact state is consumed, so there is no surrogate residual to absorb | One-step qualitative defect; no scalar forcing or finite budget controls nonmeasurability | Set-theoretic restriction is a producer, but no measurable producer exists before list consumption | Intended producer is the restriction/list map; Sparse Sample, privacy, marking, and PAC consume it | First all-positive `v=d=1` input already separates `A`; empty and singleton boundaries are both allowed | idea/theorem-contract defect | /subagent-idea-generator |
| Full-codomain cylinder learner kernel and risk event | All-input randomized-learner kernel for DP and a legitimate PAC-risk output event | Fixed-input exact learner laws, fixed default `h_{t_0}`, and positive singleton actual-output probability | Exact AboveThreshold/Sparse Sample path on the diagonal point class | Positivity of `K_{Delta(x)}(E)` is equivalent to `x in A\{t_0}`; this alone contradicts Borel measurability | `E={f:f(t_0)=0}` is an evaluation cylinder on the full improper codomain and equals the `D=delta_{t_0}`, `c=h_{t_0}` risk event | The sketch gives `K_{Delta(x)}(E)=p 1_{A\{t_0}}` with public `p>0`; constancy is stronger than needed | Pointwise laws and privacy ratios exist; `s -> K_s(E)` is not a measurable exported kernel coordinate | Same exact output and risk cylinder; zero target residual | One coordinate fails before any composition or confidence accumulation | Fixed-input laws do not noncircularly produce a kernel; adding tailored measurable structure supplies a new primitive | Exact learner law would produce for DP, marking, PAC, and final assembly; producer is absent | Nonrealizable diagonal inputs are allowed by the all-input learner/DP interface; `d=1` and default paths pass | idea/theorem-contract defect | /subagent-idea-generator |
| Uniform occurrence-mark refinement and projection | Generated marked kernel for producer-block localization | Pointwise finite occurrence formula with uniform weight over `I_s(f)` | Fixed-input Sparse Sample atom masses and actual lists | Projection matches the original law pointwise, but each actual mark coordinate has the same nonmeasurable positivity set | Exact actual-function and block-index conventions match attempt 3; measurable graph/enumeration conventions are absent | Sum over marks `0,...,k` equals `K_s(E)`; on the diagonal each actual coordinate is `(p/k)1_{A\{t_0}}` | Nonempty finite occurrence sets make the formula normalized; they do not make coordinate maps measurable | Zero projection residual sectionwise; total measurability residual at the kernel interface | One finite mark, no accumulated forcing | Pointwise projection is noncircular but is not a measurable producer | Intended marked-law producer feeds the direct bad event and finite reconstruction | Every occurrence set is `[k]`; `k=2`, singleton support, and default mark all remain covered | idea/theorem-contract defect | /subagent-idea-generator |
| Direct marked actual-output bad event | Joint event needed for Fubini and unconditional PAC conversion | Marked actual output, exact empirical loss, and the `delta_{t_0}` risk cylinder | Pointwise learner and marked-law formulas | Its section probability is `(p/k)1_{A\{t_0}}`, so a jointly measurable all-input event cannot produce it | Same actual `h_x`, full-sample loss, and population zero-one metric as the theorem | On `Delta(x)`, empirical error is zero and population error is one for `x!=t_0` | Quantitative predicates are exact; the marked learner event itself is not exportable | No function or metric residual; event measurability is wholly missing | Single event, no accumulation | Assuming joint measurability to integrate sections is circular | Intended producer is the marked kernel; Fubini, confidence ledger, PAC, and rate consume it | The diagonal away from `t_0` is nonrealizable for fixed `(D,c)`, so this is a kernel/event obstruction, not alone a realizable-PAC lower bound | idea/theorem-contract defect | /subagent-idea-generator |
| Exact `E_good` joint event and conditional trace charge | Generated concentration event for structural inclusions and confidence accounting | Fixed-sample finite partition analysis, trace count, and low-error threshold clause | Sauer-Shelah and fixed-input without-replacement tails; direct `C_A^0` witness | Conditional inequalities match fixed sections, but the exact joint event pulls back to nonmeasurable `A` | Threshold and partition conventions match the sketch; joint Borel sample-event convention fails | Choose `gamma m/2<q<=2 gamma m/3`; then full error is at most `gamma/3` for `k>=2`, while one block exceeds `gamma/2` | Fixed-section probability is controlled; exact unconditional event or useful envelope has no legal source | Exact source thresholds are consumed, so there is no scalar residual; joint-event measurability is wholly missing | Finite partition union only; joint sample dependence is uncontrolled | Conditional producer is noncircular, but tower use requires the missing joint event or an independent final event | Conditional trace producer feeds structural steps and total confidence; unconditional producer is absent | Interval length `gamma m/6>1` under `C_blk=2^20`; `z_0 notin A`, zero concept, and `v=d=1` pass | idea/theorem-contract defect | /subagent-idea-generator |
| Finite reconstruction, holdout, lower tail, and multiplicity | Conditional PAC comparison and quantitative specialization | Producer-block finite union, iid complement, direct Chernoff proof, and block constant | Attempt-4 displayed derivations and primitive iid sampling | Adequate once a legal marked event exists | Same actual function, zero-one loss, complement sample, and fixed-sample mode | Complement loss is at most `k alpha/(8(k-1))<=alpha/4`; tail exponent is `9 alpha(k-1)m/32`; `C_blk` dominates multiplicity | Raw finite candidates and scalar margins support the conditional exported bound | No surrogate residual; only the upstream event interface is missing | Finite union and one confidence charge; no persistent forcing | The arithmetic depends on, but does not assume, its own quantitative conclusion | Conditional module would consume marked kernel and output PAC/rate bounds | Exact `k=2`, empirical-zero, and finite-candidate boundaries pass | step-local, but blocked by idea-level producer | /subagent-idea-generator |
| End-to-end DP conclusion | All-input neighboring-dataset privacy for the exact randomized learner | Raw one-list replacement, fixed-input likelihood inequalities, and privacy composition | Lyu fixed-input mechanisms plus current totalization arithmetic | Privacy ratios match pointwise output laws but do not create a Markov kernel | Exact raw-record, actual-output, and cylinder-event conventions are used | Privacy-budget split composes only after measurable output laws form a kernel | Pointwise controls are present; the required all-input kernel export fails | No privacy-parameter residual; learner existence is missing | Finite transcript and one optional call; no accumulation issue beyond accepted composition | Composition cannot serve as its own kernel producer | Kernel must precede privacy and final theorem; no legal producer exists | Nonrealizable neighboring inputs are in scope; the diagonal witness is therefore relevant | idea/theorem-contract defect | /subagent-idea-generator |
| Old-Lyu arm and public frontier baseline | Baseline common-interface recovery and final quantitative minimum | Old block calibration plus the same exact restriction/list/Sparse Sample procedure | Current idea, setting, and technical-survey lineage | Scalar `d^5` source matches its rate role; it does not match the missing measurable-interface role | Same state, output, and probability conventions as the new arm | Changing only the trace exponent leaves the diagonal kernel and mark coordinates unchanged | Scalar raw controls are feasible; common DP/PAC export for the two Lyu arms is not | Rate residual is zero algebraically; interface residual is total | Finite specialization; no forcing accumulation | Old scalar theorem cannot noncircularly be treated as a measurability theorem | Old arm should produce for the public minimum; the same kernel blocker intervenes | `d=0` and finite-class arms survive, but infinite `v=d=1` disables the finite arm and blocks both Lyu arms | idea/theorem-contract defect | /subagent-idea-generator |

## Blocking Issues

1. Location: `Sigma-field preflight`, `Explicit finite-Littlestone kernel obstruction`, `Pre-step kernel producer`, and the first two exported-interface rows. Defect: the binding arbitrary-class setting supplies only pointwise restrictions and finite laws. For the allowed Borel point class `C_A`, the exact empty/singleton state and the full-codomain cylinder coordinate have non-Borel pullbacks `A` and `A\{t_0}`. Downstream effect: the exact randomized learner is not an all-input measurable kernel, so DP event quantification, marked refinement, PAC mixing, and theorem assembly have no legal producer. Smallest repair direction: `/subagent-idea-generator`; add explicit measurable/permissible structure or replace the exact procedure with a measurably encoded learner.

2. Location: `Sparse Sample and occurrence-coordinate formulas`, `Direct marked actual-output event`, and the associated generated-output rows. Defect: the occurrence formula and projection are correct only pointwise. Each actual mark coordinate and the direct empirical/population bad event retain the nonmeasurable positivity set. Downstream effect: producer-block localization and Fubini cannot be invoked. Smallest repair direction: `/subagent-idea-generator`; a measurable graph/enumeration/decoder assumption or a changed output/list mechanism is required.

3. Location: `Exact E_good obstruction` and the exact-event exported interface. Defect: the fixed-sample partition tail does not produce a joint event; the displayed `C_A^0` construction, including the valid integer spacing, makes the exact event nonmeasurable. The construction does not by itself exclude every measurable majorant, but neither the setting nor a cited tool supplies a majorant with the required conditional charge, and the alternate direct-event route is already blocked. Downstream effect: the structural inclusions and confidence ledger cannot be converted to the binding unconditional probability mode. Smallest repair direction: `/subagent-idea-generator`; add a permissibility/universal-measurability source or change the success mode/procedure.

4. Location: `Old-Lyu comparison arm`, Rate Objectives, and the final public frontier. Defect: the old arm changes scalar calibration only and inherits the same exact state and output maps. Downstream effect: its `d^5` algebra cannot provide the common unconditional DP/PAC baseline needed for the stated minimum. Smallest repair direction: `/subagent-idea-generator`; use a separately established measurable learner under a compatible contract or revise the frontier claim.

## Required Repair Bundle

1. Linked issue: Blocking Issue 1. Required change: revise the idea and formalized theorem contract to state the measurable instance, sample, state, and output interfaces and to supply a genuine theorem-facing mechanism for measurable restrictions, finite-list graphs or enumerations, atom weights, and the sample-to-output kernel. This necessarily adds primitive standard-Borel/countability/permissibility/measurable-selection structure or narrows the class/distribution scope. Affected assumptions and interfaces: `assump:finite-littlestone`, `assump:realizable-iid`, the definition of `A_C^{VC-Lyu}`, DP, and PAC probability. Smallest repair target: `/subagent-idea-generator`.

2. Linked issue: Blocking Issues 1-2. Required change: alternatively replace or totalize the learner with a measurably encoded list/output mechanism and prove projection or decoding to the consumed risk object. This changes the exact Lyu procedure, fallback, actual-function identity, or forbidden quotient/encoding interface and must be presented as a new idea rather than a sketch bridge. Affected procedure and outputs: restrictions, essential lists, Sparse Sample, occurrence mark, and final hypothesis. Smallest repair target: `/subagent-idea-generator`.

3. Linked issue: Blocking Issues 2-3. Required change: under the revised contract, construct the marked kernel and the direct empirical-and-population bad event as genuinely jointly measurable objects, then either construct a measurable `E_good` envelope with the same conditional charge or replace `E_good` by a measurable proof route. If outer probability, a `D`-dependent completion, or a conditional success statement is used, expose that as a changed probability mode/success criterion. Affected obligations: occurrence projection, Fubini, trace tower, PAC utility, and confidence allocation. Smallest repair target: `/subagent-idea-generator`.

4. Linked issue: Blocking Issue 4. Required change: re-establish every comparison arm under the same revised measurable DP/PAC interface before taking the public minimum. Preserve the separately valid `d=0` and finite-class calculations, but do not cite the old Lyu scalar rate as a measurable-kernel theorem. Affected theorem output: old arm, infinite-class case, `v=d` baseline, and frontier. Smallest repair target: `/subagent-idea-generator`.

5. Target-preserving repair check: no repair confined to proof steps, dependencies, event bookkeeping, or quantitative loss can preserve the current setting and exact goal. The exact diagonal cylinder coordinate has nonmeasurable positivity set `A\{t_0}`. Making it measurable requires changed primitive assumptions, changed class/distribution scope, changed algorithm/procedure or output encoding, or changed probability/success mode. Therefore `/proof-sketch` cannot supply the missing mechanism under the unchanged theorem contract.

## Review Rationale

The attempt-4 diagnostic supplies the concrete obstruction that attempt 3 lacked. Its point-class witness lies in the exact `v=d=1` arbitrary-class scope, uses only individually Borel concepts, reaches both the state and full-codomain risk-cylinder interfaces, and survives occurrence marking. The `E_good` integer construction is also internally sound, while the finite-reconstruction and rate calculations remain valid only conditionally. The deepest failure is not a missing roadmap edge: the exact learner-kernel coordinate itself is nonmeasurable, and every viable remedy changes a primitive assumption, procedure, scope, or probability mode. The aligned route is therefore `IDEA_FAIL` with a new-idea retry.
