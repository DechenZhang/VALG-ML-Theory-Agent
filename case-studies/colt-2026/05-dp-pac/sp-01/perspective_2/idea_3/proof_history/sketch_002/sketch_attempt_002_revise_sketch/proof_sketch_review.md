# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 2
- Reviewed artifact: `perspective_2/idea_3/proof_sketch.md`
- Reviewed sketch SHA-256: `51d7799b7e2a6d5eda6a6f8487c6e96c5fdfc73c949a55e0a38bf2508b0635a8`
- Binding setting: `perspective_2/idea_3/setting.md`
- Binding setting SHA-256: `13cb7296ea48e286ba553a4a153b84c00bd51c97f43c74d6b74f62321258d327`
- Technical survey: `perspective_2/idea_3/technical_survey.md`
- Technical-survey SHA-256: `97814dc307d7ec269ac4475dc92e399ee96c7a6b318e155031fca9c39928fadb`
- Triggering diagnostic review: `perspective_2/idea_3/global_proof_review.md`
- Triggering-review SHA-256: `4973283abebd7795891ed4f01d02faa0c96ac233bc53b0e9bb0fa949619f3c01`
- Triggering status: `REVISE_SKETCH`
- Primary source checked independently: Xin Lyu, *Private Learning of Littlestone Classes, Revisited*, arXiv:2510.00076v1; downloaded v1 `submission/main.tex` SHA-256 `b9f6d947f66f6223889f50c8eaa0c3275155a76fbaaecf1020eb8821389d6f87`.
- Formalized target: exact-goal, universal `material_partial` VC-sensitive refinement of Lyu's fixed-block approximate-DP learner.
- Reviewed roadmap: twelve forward-only steps, with an exact public teacher definition, a current-notation repair of Lyu's stage endpoint and factor-two scale convention, a total all-input learner, the exact improper SOA output family, and normalized public comparison arms.

## Sketch Viability Score

8

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch preserves the exact setting, arbitrary possibly infinite finite-Littlestone class scope, realizable distribution-free utility, replacement-adjacency privacy on every labeled input, potentially improper output, fixed-sample population-error metric, and the displayed dependence on \(d,v,\alpha,\beta,\varepsilon,\delta\). It remains labeled `material_partial`, states that the full open problem remains unresolved, and makes no full-result claim.
- Dependency audit: All twelve rows have stable IDs, exact intended claims, assumptions, proof tools, output targets, rate roles, and `PENDING` status. Every edge points to an earlier step. The parameter and total-procedure package is produced before use; trace outputs precede source-stage utility; exact lists precede structural descent and selection; privacy is separated from utility; exact SOA membership precedes fixed-class generalization; and public specialization consumes completed DP/PAC outputs.
- High-risk coverage: The sketch exposes the source's undefined \(H_i^{d+1}\) endpoint instead of concealing it, records the nontrivial factor-two \(p\)-translation, pins the teacher/list/noise fixed point, totalizes all failure paths before privacy, separates list-level and raw-record adjacency, fixes the exact Lemma 4.2 output family, and retains every boundary/comparison obligation. These are difficult local derivations but no longer missing interfaces.
- Explicit-rate coverage: All exposed variables and permitted hidden logarithms are declared. The candidate \(\bar k\), the inequalities `LogClosure` and `MarginBound`, the ceiling-aware trace fixed point, the relative-VC threshold, and the three public arms give legal producer paths for eliminating \(k,m,n_0,p_r,L,B\), all mechanism losses, and all confidence allocations. No positive power is hidden in \(\Lambda\).
- Assumption and citation plausibility: The only theorem-facing assumptions are the three stable setting assumptions. Every event, restriction, list, common item, selected-output witness, adjacency relation, privacy statement, and generalization event is derived. Every theorem-critical Lyu statement has a v1 label, current-object mapping, exact role, and non-output boundary. Direct wrappers are expressly identified where v1 itself is incomplete or only asymptotic.

## Early Obstruction Audit

- Limiting-case stress: The \(d=0\) branch returns the unique concept with no data. For \(d\ge1\), \(1\le v\le d\), \(k\ge2\), and \(n_0d\ge\max\{n_0,d+1\}\). The construction remains legal at \(d=v=1\), under repeated sample points, for a zero error trace, for arbitrarily small \(\alpha\), for arbitrarily small \(\delta\), and for infinite \(C\). At \(v=d\) it returns to the \(d^5\) scale. Fixed positive \(\delta\) is not falsely claimed to satisfy \(N\delta\to0\).
- Theorem-critical bridge support: Conditional on the indexed master sample, xor with the fixed label vector gives exactly the error-trace family, and each random block is marginally a uniform sample without replacement. Lyu Proposition 2 supplies the high-error relative clause; a current-notation hypergeometric/KL upper tail supplies the low-error clause, including mean zero. The resulting finite union is the exact event consumed by the threshold algebra. Downstream source bridges are fixed before their consumers rather than delegated as unidentified future work.
- Exported-interface feasibility: Every exported interface lists raw controls, defect classes, a dominance or transfer relation, and consumers. The trace event separates multiplicity from concentration; teacher selection separates AboveThreshold noise, Sparse Sample effective-domain loss, and the failure-symbol score; privacy separates sensitivity-one queries from one-list replacement; and PAC transfer separates exact fixed-family membership, empirical error, and uniform population conversion.
- Theorem-critical mechanism witness gate: Passed. The trace exponent dominates the finite trace/block multiplicity; the public candidate \(\bar k\) dominates the exact list and noise losses; the DDim potential has a one-unit finite-budget drop; a score-\(k\) item activates AboveThreshold and survives Sparse Sample; block locality supplies all-input privacy; leaf irreducibility supplies the empirical contradiction; and the fixed SOA family supplies uniform generalization.
- Entry-state trace stress: At \(d=0\), the conclusion is already true and no positive-branch mechanism activates. At \(r=0\), \(p_0=n_0d\) already supplies both required irreducibility scales. If \(M_0=0\), Corollary 4.1 Item 4 and the realizable target immediately give score \(k\); otherwise an accurately unsuccessful first stage activates Lemma 4.3 before \(M_1\) is consumed. On arbitrary nonrealizable data, the first query, empty lists, every `Below` transcript, \(\perp\), and the final default are already defined.
- Obligation locality classification: Every theorem-critical obligation is `step-local`. `step_001` is a finite scalar/parameter-package lemma, `step_006` is a source-wrapper lemma, and `step_012` is a finite public-specialization proposition with explicit cases; each may contain locally audited sublemmas without changing its exported interface. No later proof step is being used as the first source of a stage convention, margin, support fact, privacy map, output family, or rate comparison.
- Noncircular closure gate: The candidate quantities are defined for every integer \(t\) before feasibility is tested, and explicit \(\bar k\) proves the feasible set nonempty. \(Q(t)\) is defined without \(m(t)\) or \(n(t)\), so the trace fixed point is not self-assuming. The DDim recurrence does not assume termination, privacy uses no utility event, and generalization ranges over the fixed class \(\widehat C_{d+1}\), not a data-dependent family invented after selection.
- Mechanism-source and boundary stress: The source v1 defines \(H_i^j\) only for \(j\in[d]\) but immediately runs stages \(1,\ldots,d+1\). The sketch does not cite a nonexistent endpoint theorem: it defines \(H_{i,\mathrm{src}}^{d+1}\) by the same displayed formula as a current-notation wrapper, then proves the only consumed cross-stage inclusion directly. Reindexing gives \(H_i^r=H_{i,\mathrm{src}}^{r+1}\) while retaining the binding \(p_r=2^rn_0d\); the residual factor two is explicitly discharged by \((p_{r+1},p_r)=(2p_r,p_r)\), exactly Lemma 4.3's interface.
- Generated-output flow: Passed. The parameter package, trace family, per-trace tails, \(E_{\rm good}\), stage/list wrapper, score-\(k\) stage, actual selected item, raw-record DP, exact SOA witness, PAC event, and public rates each have named producers, consumers, final uses, and forward paths.
- Source-to-claim adequacy: Lyu v1 Proposition 2 matches the high-error without-replacement claim but not the near-zero upper tail; the sketch correctly supplies the latter directly. Lemma 4.3 compares an optimal \((2p,d)\)-decomposition of \(\mathcal G\) with an optimal \((p,d)\)-decomposition of \(\mathcal H\), and its equality conclusion is equality of SOA functions on \(X\), exactly what the sketch consumes. Corollary 4.1's four cases were checked: Item 1 is the \(p^d2^{d^2}\) list bound; Item 2 transfers essential functions under inclusion and equal same-\(p\) DDim; Item 3 gives nonempty \(p\)-essential support only when \(\DDim_{2p,d}(H)=\DDim_{p,d}(H)\); Item 4 gives finite \(H\) and \(\operatorname{Ess}_{p,d}(H)=H\) when the DDim is zero. The roadmap makes no generic nonempty-support inference from Item 3; its common item comes from the exact Lemma 4.3 equality case, while its zero case uses Item 4 plus the common target.
- Residual-to-target adequacy: The source and branch use the same threshold classes after the explicit endpoint definition. The only stage residual is the factor-two \(p\)-shift, and every maximal \((p_r,d)\)-leaf is \(p_r2^{d-t}\)-irreducible with \(p_r2^{d-t}\ge n_0d\ge\max\{n_0,d+1\}\). Selected list objects and consumed outputs are the same actual functions, not sample traces. One raw record changes the same single list coordinate at every fixed-partition stage. Empirical and population losses concern the same selected function and target.
- Baseline invariance audit: Exact zero-sample recovery at \(d=0\), return to the \(d^5\) scale at \(v=d\), the separately normalized coarse Lyu arm, the finite-class arm, disabling that arm for infinite \(C\), and the declared \(N\delta\to0\) schedule are all retained. Each comparison is made only after the corresponding learner has the same \((\alpha,\beta,\varepsilon,\delta)\) interface.
- Scope-accumulation compatibility: Trace and confidence losses are finite unions. Structural accumulation is controlled by \(0\le M_r\le d\) and the one-step implication that absence of a current common actual item forces \(M_{r+1}\le M_r-1\). AboveThreshold is one process stopped after the first `Above`; Sparse Sample is called at most once. Privacy therefore does not accumulate once per stage. No persistent, unknown-sign defect remains.
- Scope and dependence consistency: Utility is high probability jointly over sample, partition, and mechanism randomness; privacy is the all-input output-distribution inequality. The theorem is fixed-sample, not conditional or all-time. Hidden constants are universal, and \(\Lambda\) contains only logarithms of displayed parameters. The finite-class comparison is the only place \(|C|\) appears.
- Generated-condition provenance: \(E_{\rm good}\), inclusion, bounded lists, potential descent, common support, mechanism-good events, actual selection, raw adjacency, DP, exact output-family membership, empirical utility, and uniform population utility are all derived. None is promoted to a final theorem assumption.
- Citation and tool applicability: Lyu Algorithms 1 and 2 and Lemmas 3.1 and 3.2 were checked in v1. Algorithm 1 outputs an actual union-list item or \(\perp\); Lemma 3.1 requires \(B\ge10\log(L/\delta_s)/\varepsilon_s\) and gives \((2\varepsilon_s,\delta_s)\)-DP for one-list replacement. Algorithm 2 adds independent \(\operatorname{Lap}(1/\eta)\) noise; with one positive-report budget its lemma gives the stated approximate-DP prefix after the explicit constant wrapper. Lemma 4.2 controls exactly \(\widehat C_{d+1}=\{\operatorname{SOA}_{\mathcal G}:\mathcal G\subseteq C\text{ is }(d+1)\text{-irreducible}\}\). Theorem 3's informal success amplification and tilde teacher formula are not used as missing interfaces; the sketch supplies its own finite failure allocation and explicit teacher dictionary.
- Teacher/list/noise closure: The closure is noncircular and quantitatively feasible. For \(\bar k=\lceil C_{\rm teach}d^2G\lambda^2/\varepsilon\rceil\), the definition of \(\lambda\) directly gives \(\log\bar k=O(\lambda)\), \(a(\bar k)\le O(\lambda)(v+\log(1/\beta))\), \(\log Q(\bar k)=O(\lambda)\), and hence \(\log n(\bar k)=O(\lambda)\). Since \(\log L=d\log(2^dn d)+d^2\log2\), it is at most \(O(d^2\lambda)\). Therefore \(B+\tau_{\rm SS}+\tau_{\rm AT}\le O(d^2G\lambda^2/\varepsilon)\), including the ceilings, \(\log(1/\alpha)\), \(\log n_0\), \(\log L\), tiny \(\delta\), and \(d=1\). A universal \(C_{\rm teach}\) makes \(\bar k/2\) dominate this sum by the displayed positive margin, proving existence and \(k\le\bar k\).
- Trace fixed point: From \(m\le C d^2a\log Q/\alpha+1\) and \(Q=e+ekd^2a/(\alpha v)\), with \(d,v,k,a,\alpha\) in the declared range, \(en_0/v\le C'Q\log Q\). Thus \(\log(en_0/v)\le C_{\rm fp}\log Q\), including the ceiling. Since \(a\) dominates both \(v\) and \(\log(4k/\beta)\), sufficiently large universal \(C_{\rm blk}\) makes the trace exponent dominate \(v\log(en_0/v)+\log(16k/\beta)\). No unresolved \(\log n_0\) or wrong-direction big-O remains.
- Same-setting repair plausibility: No present obstruction requires repair. The prior global-review issues are resolved within the same setting: the source mismatch is an explicit bridge, the teacher is an exact least feasible integer with a public witness, the algorithm is total before privacy, and the output family is exact. Later failure of one of these fixed interfaces would route back to `/proof-sketch`, but the obstruction-level preflight finds no such failure now.
- Target-preserving bridge-repair gate: No current blocker triggers this gate. The repair preserved the primitive assumptions, algorithmic scope, metric, probability mode, exposed powers, success criterion, and all inherited baselines.
- High-risk obligation classes: The near-zero finite-population tail, fixed points, source endpoint, factor-two decomposition map, all four Corollary 4.1 cases, recursive potential, private-selection margin, arbitrary-input adjacency, improper-output identity, relative-VC conversion, explicit public rates, and boundary specializations all pass at sketch granularity.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Zero-dimensional branch and positive-branch entry | Baseline recovery and legal entry | Direct LD/VC argument; fixed \(h_0\in C\) | `assump:finite-littlestone` and nonemptiness | \(d=0\) forces one actual function; \(d\ge1\) gives \(1\le v\le d\) | Branch convention is internal; no source translation needed | Singleton output uses \(N=0\); positive formulas are bypassed at \(d=0\) | Primitive \(C,d,v\) export an explicit boundary learner and legal positive domain | Output equals the unique target; no surrogate residual | No repeated forcing | `step_001` precedes every positive consumer | `step_001` to final theorem and parameter consumers | Stationary \(d=0\) state already satisfies utility and privacy; \(d=v=1\) enters legally | step-local | None |
| Exact teacher integer, list/noise margin, and public envelope | Quantitative existence and selection-margin closure | Direct scalar dictionary; Lyu Cor. 4.1 Item 1 and Lemma 3.1 threshold | Current definitions (Alloc)-(Kbar); source list and privacy statements | Linear score \(t/2\) dominates logarithmic list, failure-symbol, effective-domain, and AT losses | \(L(t)=p_d(t)^d2^{d^2}\) and \(B(t)=\lceil10\log(L/\delta_s)/\varepsilon_s\rceil\) exactly match source conventions | \(t/2-\tau_{\rm AT}\ge B(t)+\tau_{\rm SS}(t)+2\); \(\bar k\) is feasible; \(k\le\bar k\) | Raw public parameters define \(a,Q,m,n,L,B\); export is the exact tuple used by the learner | Same tuple is consumed by blocks, lists, privacy, utility, and rate; ceilings are controlled | One finite scalar check; logarithmic defects have no iterative accumulation | Candidate tuple exists before feasibility; explicit \(\bar k\) proves nonempty feasible set | `step_001` produces for `step_002`-`step_012` as applicable | \(d=1\), small \(\alpha\), tiny \(\delta\), and ceiling-dominated cases remain feasible | step-local | None |
| VC error-trace count | Structural finite reduction for possibly infinite \(C\) | Xor bijection and Sauer--Shelah | Standard direct combinatorial facts | Gives the exact exponent \(v\), not class cardinality or \(d\) | Same indexed master-sample prediction/error vectors | \(\lvert\mathcal E_C(S)\rvert\le\Pi_C(n_0)\le(en_0/v)^v\) | Fixed \(S\), labels, and \(n_0\ge v\) export the finite trace family | The vectors counted are exactly those entering block deviations | Duplicate functions/traces collapse; no stochastic accumulation | `step_001` then `step_002` | `step_002` to `step_004`,`step_005` and final trace rate | Repeated points and infinite \(C\) are harmless; \(v=1\) is legal | step-local | None |
| Exact \(E_{\rm good}\) and trace failure budget | Generated concentration event feeding all utility wrappers | Lyu Prop. 2; current finite-population KL tail; finite union | arXiv:2510.00076v1 plus direct derivation | Covers both exact source clauses; KL branch remains active at zero/small mean | Same block marginal, thresholds \(\gamma/3,\gamma/2\), and relative tolerance \(1/(5d)\) | \(4k\Pi_C(n_0)e^{-c_{\rm tr}m\alpha/d^2}\le\beta/4\) after \(\log(en_0/v)\le C\log Q\) | Per-trace tails and finite trace count export the exact simultaneous event | No object transform; source consumes the same block errors | Finite union only; block dependence is not multiplied | `step_002`,`step_003` to `step_004`,`step_005` before consumers | Produced for `step_006`,`step_007`,`step_010`,`step_011` | Mean zero makes the low-tail bad event impossible; boundary \(e_S=\gamma/3\) is covered | step-local | None |
| Source endpoint, factor-two stage map, lists, and leaf scales | Source-convention and generated-object wrapper | Direct endpoint definition; Lyu Def. 4.2, Lemmas 4.1/4.3, Def. 4.3, Cor. 4.1 | Exact v1 labels; endpoint gap explicitly identified | Supplies the exact classes, actual essential functions, list bounds, and irreducible leaf witnesses | Source stages are \(1,\ldots,d+1\); current stages are \(0,\ldots,d\); \(H_i^r=H_{i,\rm src}^{r+1}\); \(p_{r+1}=2p_r\) | `Incl-low`, `Incl-high`, Pair, and \(p_r2^{d-t}\ge n_0d\ge\max\{n_0,d+1\}\) | \(E_{\rm good}\), thresholds, and public \(p_r\) export inclusion, finite actual-function lists, and leaf witnesses | Factor-two is fully transferred; selected functions are identical on \(X\), not trace representatives | Exact finite transition; no error accumulation | `step_004`,`step_005` precede `step_006`; no source identity is assumed | `step_006` to `step_007`-`step_011` | Undefined v1 endpoint is directly totalized; empty arbitrary-input classes map to empty lists; \(d=1\) retains both scales | step-local | None |
| DDim descent and common actual item | Signed structural recurrence and finite termination | Lyu Lemma 4.3 equality case; Cor. 4.1 Items 1-4; realizable target | Exact source statements and `step_006` inclusion/list outputs | If next/current maxima are equal, one max-leaf SOA appears in every current optimal decomposition; at zero, Item 4 gives lists equal to classes | Lemma uses \((2p_r,d)\) for next and \((p_r,d)\) for current; equality is actual-function equality | If no current score-\(k\) item, \(M_{r+1}\le M_r-1\); \(0\le M_0\le d\) | Inclusion, exact lists, and common target export a score-\(k\) stage by \(r=d\) | Same actual function is consumed by frequency query; no trace residual | One-unit monotone drop with budget at most \(d\); no additive forcing | `step_006` then `step_007`, without assuming termination | `step_007` produces for `step_008`,`step_010` | If \(M_0=0\), support is immediate; if \(M_d=0\), target \(c\) lies in every list | step-local | None |
| AboveThreshold detection and Sparse Sample actual output | Positive-margin generated selection | Lyu Algorithms 1-2/Lemma 3.1; Laplace and exponential-weight tails | Exact v1 mechanisms plus direct utility inequalities | Detects score \(k\), lower-bounds any reported-stage score, and excludes \(\perp\) | List size, \(B\), exponent \(\varepsilon_s\), threshold \(k/2\), and one-positive prefix are fixed in source-compatible form | \(q\ge k/2-\tau_{\rm AT}\) and returned score \(\ge q-\tau_{\rm SS}>B\) | Score-\(k\) stage, \(L,B\), and finite effective domain export an actual list function | Returned object is an exact union-list member; \(\perp\) occurs only off the allocated good event | \(d+1\) finite Laplace tails and one Sparse Sample tail are union-bounded | `step_001`,`step_005`,`step_007` then `step_008` | `step_008` to `step_009`-`step_011` | Empty stage cannot be a good-noise false positive; first or last stage success is detected | step-local | None |
| Total all-input replacement DP | Scope upgrade from list-level tools to raw records | Fixed total procedure; RawList; Lyu Lemmas 3.1-3.2; adaptive composition and postprocessing | Exact source privacy statements plus direct block-locality wrapper | Sensitivity-one AT prefix and one-list SS replacement give public output privacy | For each fixed data-independent partition, one raw replacement changes only its block/list coordinate at every stage and selected call | \((\varepsilon/4,\delta/2)\) AT plus \((\varepsilon/4,\delta/2)\) SS gives \((\varepsilon/2,\delta)\), hence \((\varepsilon,\delta)\) | Total lists, queries, stopping, \(\perp\), and defaults are fixed before release | Raw and consumed adjacency are exactly coupled; mixture over common partition randomness adds no residual | One finite transcript and at most one optional call; no per-stage privacy accumulation | Roadmap totalization and `step_006` list map precede `step_009`; utility is never assumed | `step_009` to `step_012` and final theorem | Arbitrary labels, empty restrictions, immediate `Above`, all `Below`, and \(\perp\) all terminate with defined outputs | step-local | None |
| Exact SOA witness and empirical error | Object-target bridge for improper empirical utility | Essential-function definition; leaf irreducibility; Lyu Theorem 3 proof pattern | Exact source definitions; empirical contradiction derived in current notation | Every actual list item equals \(\operatorname{SOA}_{\mathcal G}\) for an actual maximal leaf \(\mathcal G\subseteq C\) | Leaf scale is the current \(p_r2^{d-t}\), already shown at least \(n_0\) and \(d+1\) | All \(g\in\mathcal G\) have master error at most \(2\gamma\); if the SOA had larger error, the \(n_0\)-point SOA restriction would be empty, contradicting irreducibility | Actual selected item and \(E_{\rm good}\) export exact membership and empirical error \(\le\alpha/8\) | Produced and consumed functions coincide on \(X\); master error uses the same target labels | One deterministic contradiction; stochastic defects were budgeted upstream | `step_006`,`step_008` then `step_010` | `step_010` to `step_011` | Infinite \(C\), improper output, DDim-zero leaves, duplicate points, and empirical zero remain valid | step-local | None |
| Fixed improper family and relative-VC PAC transfer | Structural class control and empirical-to-population conversion | Lyu Lemma 4.2; VC less than or equal to LD; standard one-sided relative VC inequality | Exact v1 fixed family and direct uniform-convergence wrapper | Controls exactly \(\widehat C_{d+1}\), the family containing every produced output | Same binary function class and population-error metric; no undefined eligible family | \(\operatorname{VC}(\widehat C_{d+1})\le d\) and uniformly \(R(f)\le2\widehat R(f)+\alpha/2\) at the stated sample threshold | Exact membership, empirical \(\alpha/8\), and i.i.d. realizability export population error below \(\alpha\) | Same \(f,c\) in empirical and population losses; no class-cardinality residual | One fixed-sample uniform event with failure \(\beta/4\) | `step_010` precedes `step_011`; family is fixed before data | `step_011` to `step_012` and final theorem | Possibly infinite \(C\) and output outside \(C\) are handled by growth dimension, not finite enumeration | step-local | None |
| New VC-sensitive public rate | Quantitative specialization and theorem closure | Teacher envelope, trace fixed point, DP/PAC outputs, direct algebra | `step_001`,`step_005`,`step_009`,`step_011` | Eliminates all auxiliaries and exposes \(d^4(v+\log(1/\beta))G/(\varepsilon\alpha)\) | Same public parameters and fixed-sample mode as the binding goal | \(k\le Kd^2G\lambda^2/\varepsilon\), \(a\le K\lambda(v+\log(1/\beta))\), \(\log Q\le K\lambda\), then multiply with ceiling control | Exact \(k,m,n_0\) used by the learner export its public sample bound | Only powers of \(\lambda\) remain hidden; no positive structural power or \(\log n_0\) remains | One finite algebraic specialization | All mechanism and utility producers precede `step_012` | `step_012` to final theorem | \(v=1\), \(v=d\), small \(\alpha\), and tiny \(\delta\) preserve the claimed direction | step-local | None |
| Coarse Lyu comparison arm | Baseline universal-arm normalization | Same explicit mechanism dictionary with exponent \(d\); Lyu structural/PAC wrappers | Source Theorem 3/Cor. 5.1 plus current exact calibration | Produces an independently DP/PAC \(\widetilde O(d^5)\) learner, not an undefined tilde parameter | Same allocations, total procedure, SOA family, and accuracy/confidence interface as new arm | Coarse trace count and \(\bar k_{\rm old}\) give (OldArm) | Explicit old \(a,Q,m,k,L,B\) export a completed comparison arm | Separate learner; no residual is transferred into the new arm | One finite specialization | `step_012` derives arm independently before minimization | `step_012` to final frontier | Full-cube rate is restored; fixed \(\delta\) is not treated as a vanishing schedule | step-local | None |
| Finite-class arm and infinite-class disabling | Baseline comparison and domain split | Exponential mechanism and finite-class relative deviation | Standard direct mechanisms | Gives pure-DP empirical selection and PAC utility only when \(\lvert C\rvert<\infty\) | Same replacement adjacency and population metric; infinite class is not approximated | Displayed empirical and population sample thresholds in (FiniteArm) | Finite \(M=\lvert C\rvert\), sensitivity-one error score, and realizable zero-score target export the finite arm | Same selected \(h\in C\) and target; infinite \(C\) maps cost to \(+\infty\) | Two finite confidence events | Independent derivation inside `step_012` before comparison | `step_012` to final frontier | Singleton is already handled by \(d=0\); infinite class disables only this arm | step-local | None |
| \(v=d\), \(N\delta\to0\), and common-interface minimum | Baseline invariance and asymptotic comparison | Direct substitution and setting's declared schedule | Binding rate definitions and completed arms | Preserves the exact inherited comparisons without weakening | All arms first use common \((\alpha,\beta,\varepsilon,\delta)\) normalization | Substitute \(v=d\); and \(0\le N\delta\le\delta K\Lambda^qR_{\rm VC}\to0\) only under the declared schedule | Completed public rates export the minimum and limit statement | Scalar comparisons only; no generated-object residual | One finite comparison and one declared-sequence implication | `step_012` consumes only accepted prior outputs | Final theorem/frontier | Full cube has no false saving; fixed positive \(\delta\) is expressly excluded from the limit claim | step-local | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

`ACCEPTED` is the correct early-gate status. Attempt 2 repairs every issue identified by the triggering global review without changing the formalized theorem contract. The primary-source recheck confirms both the v1 endpoint defect and the exact decomposition/private-mechanism/output-family statements; the sketch now distinguishes direct wrappers from actual source outputs and provides the needed factor-two, parameter, totality, adjacency, and same-function bridges. The scalar fixed points are directionally and quantitatively closed under all allowed boundary regimes, the twelve-step graph is locally auditable and forward-only, and every theorem-critical generated output has a legal source and consumer path. Substantial proof work remains, which is why the viability score is 8 rather than 9 or 10, but no theorem-level obstruction or sketch/interface defect remains before `/global-proof` and step-local proof work.
