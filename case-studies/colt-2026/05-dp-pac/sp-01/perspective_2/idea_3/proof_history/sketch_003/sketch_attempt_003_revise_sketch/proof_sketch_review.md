# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 3
- Review attempt: 3
- Producer unit attempt: 1
- Reviewed artifact: `perspective_2/idea_3/proof_sketch.md`
- Reviewed sketch SHA-256: `36cc233d6584e181b899ae39a50035a539802e9c435db49c44ce52c9ed63523b`
- Binding setting: `perspective_2/idea_3/setting.md`
- Binding setting SHA-256: `13cb7296ea48e286ba553a4a153b84c00bd51c97f43c74d6b74f62321258d327`
- Technical survey: `perspective_2/idea_3/technical_survey.md`
- Technical-survey SHA-256: `bdd87666f3bee8bc2b342b904b65b24113a713a53578a6bf8ae756b9d25f1b7d`
- Triggering diagnostic: `perspective_2/idea_3/proof_steps/step_011/review.md`
- Triggering-review SHA-256: `832fde03cc549542bc701a1e7b653ff8c0a6b38a7c1dd16d488b856f26f87751`
- Goal mode: exact-goal mode.
- Progress type: `material_partial`.
- Reviewed roadmap: twelve forward-only steps retaining Lyu's exact fixed-block procedure and replacing the invalid generic relative-VC bridge by an occurrence-marked, producer-block finite-reconstruction argument.

## Sketch Viability Score

6

## Sketch Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Retry Mode

revise_sketch

## Audit Summary

- Goal alignment: The target remains the exact unconditional arbitrary-class theorem in `setting.md`, with the same potentially improper Lyu procedure, population (0)-(1) metric, approximate-DP scope, exposed variables, and `material_partial` label. The remaining gap to the source open problem is stated honestly. No theorem-facing assumption, procedure change, or success-criterion weakening was introduced.
- Dependency audit: The twelve step rows are stable, complete, and acyclic. `step_011` directly depends on `step_006`, so the set-theoretic block-local list producer is present. It does not depend on `step_009`, and neither `step_008` nor `step_009` currently exports the measurable sample-to-state/output kernel needed by the marked lift. The producer and dependency path for that interface are therefore missing.
- High-risk coverage: The finite-reconstruction idea correctly isolates one producer block, takes an all-stage union of at most ((d+1)L) actual functions, and evaluates it on an independent complement. The sketch also removes every active use of the old generic relative-VC/outer-probability theorem. The central measurability assertions, however, are stated as conclusions without a legal mechanism source.
- Explicit-rate coverage: The exposed variables, hidden-constant restrictions, fixed-sample probability mode, and public specialization obligations are present. The full-sample-to-complement inequality is correct, including (k=2). The one-sided Chernoff exponent (9\alpha(k-1)m/32) is correct. The multiplicity bound and (C_{\rm blk}=2^{20}) domination are quantitatively sufficient. These calculations do not repair the missing measurable-event interface.
- Assumption and citation plausibility: Lyu Algorithm 1 supplies a finite conditional distribution after a list tuple is fixed, and the cited decomposition results plausibly supply the set-theoretic list and SOA objects. None of those cited statements, as mapped in the sketch, supplies measurability of the block-to-list map, the sample-to-Sparse-Sample kernel, the occurrence-mark refinement, or (f\mapsto\operatorname{err}_D(f,c)) on a declared output sigma-field. Lemma 4.2 is correctly retained only for structural dimension and is not misused as a permissibility result.

## Early Obstruction Audit

- Limiting-case stress: At (d=0), the no-data singleton branch is sound. At (d\ge1), (k\ge2) leaves ((k-1)m\ge m) complement observations. For (k=2), (k\alpha/[8(k-1)]=\alpha/4), so the holdout inequality is exact. For (p>\alpha), the event (\widehat p\le\alpha/4) is contained in (\widehat p\le p/4), and the multiplicative lower-tail inequality with deviation (3/4) gives (\exp[-9p(k-1)m/32]\le\exp[-9\alpha(k-1)m/32]). Infinite (C), improper outputs, and Durst--Dudley-type families do not break this sectionwise finite-union calculation once a measurable marked bad event exists; they do break any attempt to infer that event's measurability from individual candidate measurability alone.
- Theorem-critical bridge support: The set-theoretic bridge is coherent: on mark (J=i), the selected output belongs to (\mathcal U_i(S_i)), and (\mathcal U_i(S_i)) is fixed before the complement. The probability bridge is not yet supported. The sketch labels the occurrence refinement a "measurable finite-state lift" because Algorithm 1 uses occurrence scores, but using those scores for each fixed input does not prove that the refined probabilities form a measurable kernel as the sample varies.
- Exported-interface feasibility: The raw controls for the finite reconstruction are adequate: an actual selected item, its stage, block-local lists, (k\ge2), empirical error at most (\alpha/8), and iid complement data. The exported measurable pair ((\widehat f,J)) and the measurable event to which Fubini is applied are not produced. Thus the raw-control-to-exported-interface path fails before the otherwise valid finite union and exponential domination can be consumed.
- Theorem-critical mechanism witness gate: The list-size, independence, holdout, Chernoff, and scalar-dominance witnesses pass. The measurability witness fails: "the exact mechanism already evaluates the predicates" is a category assertion, not a source-to-claim implication on a declared state/output sigma-field. This omission is repairable at sketch level only if the unchanged exact procedure supports an explicit measurable-kernel construction.
- Entry-state trace stress: The finite-stage DDim trace remains viable: (M_0=0) gives immediate common support, and otherwise each accurately `Below` transition spends one unit of the finite (d)-budget. For the repaired PAC block, (k=2), a data-dependent selected stage, an early false-positive stage, and empirical error zero all retain a nonempty producer occurrence and valid holdout inequality. The failed point is not activation but measurability of the generated pair and bad event before they are consumed.
- Obligation locality classification: The teacher fixed point, finite trace count and conditional tail arithmetic, factor-two source bridge, DDim descent, selection margin, set-theoretic list support, raw replacement arithmetic, exact SOA identity, holdout inequality, one-sided Chernoff proof, and scalar rate specialization are `step-local`. Joint measurability of (E_{\rm good}), the sample-to-list/Sparse-Sample kernel, the marked lift, the population-error event, the Fubini interface, and the old-arm reuse of that interface are `sketch/interface defect`. No `idea/theorem-contract defect` is established at this review stage.
- Noncircular closure gate: The scalar and DDim closures are noncircular. The PAC measurability closure is circular: Section "Measurable occurrence mark" defines (\mathcal H_i) using "the measurable final learner-error event" even though producing a measurable event for the fixed learner is part of the unconditional PAC theorem. Likewise, calling the exact Sparse Sample state measurable does not establish that state as a measurable producer.
- Mechanism-source and boundary stress: For each fixed finite list tuple, Algorithm 1's exponential weights define a valid discrete law and the uniform occurrence mark preserves its output marginal. The missing source is variation with the dataset. A finite support on each section does not imply measurability of the support map or of sums over data-dependent supports. This remains true for an arbitrary possibly infinite class and arbitrary underlying measurable space.
- Generated-output flow: Set-theoretic flow from `step_006` lists and the `step_008` selected item into `step_011` is present. Measurable flow is absent. If `step_009` is repaired to produce the exact stochastic-kernel interface, `step_011` must depend on it; alternatively `step_008` must explicitly export that interface. The current Generated Output Flow table incorrectly records the marked lift and population event with no blocker.
- Source-to-claim adequacy: Lyu's finite conditional output law matches the score and support claims, but it does not, as cited, imply measurability of (s\mapsto K_s(E)) for each measurable output event (E), nor of the refined maps (s\mapsto K_s(E\cap\{J=i\})). Individual measurability of each fixed candidate's loss is enough for each fixed producer-block section; it is not enough to create the required joint marked event.
- Residual-to-target adequacy: Conditional on a legal marked event, the same selected function is used in full empirical, complement empirical, and population losses. The exact residual relation is
  \[
  e_{S_{-i}}(f)\le \frac{k}{k-1}e_S(f)\le\alpha/4,
  \]
  and the population/complement comparison has no surrogate residual. The missing target-side component is event measurability, not a quantitative residual.
- Baseline invariance audit: The (d=0) and (v=d) algebraic baselines are preserved. The finite-class arm is separate and finite, so its ordinary finite union is plausible. The old-Lyu comparison arm explicitly reuses the same occurrence-marked PAC bridge and therefore inherits the unresolved measurable-kernel/event defect; its common DP/PAC interface is not yet established.
- Scope-accumulation compatibility: The trace multiplicity, mark multiplicity, and confidence ledger are finite unions. The structural recurrence has one-step relation (M_{r+1}\le M_r-1) and budget (M_0\le d). The marked PAC term is a single finite section union with no persistent forcing. There is no accumulation obstruction once the measurable interface is supplied.
- Scope and dependence consistency: The claimed bound retains all exposed structural, accuracy, confidence, privacy, and logarithmic dependencies. The final probability mode is unconditional joint probability, not pointwise conditional or outer probability. That makes the missing measurable-kernel/event producer theorem-critical rather than a notation concern.
- Generated-condition provenance: (E_{\rm good}), actual selection, empirical error, and the occurrence mark are all meant to be derived. The current sketch gives valid set-theoretic producers for the first three properties on fixed inputs, but it does not give a measurable producer for (E_{\rm good}) across samples or for the marked final bad event.
- Citation and tool applicability: Every active occurrence of the old relative-VC tool has been removed or identified only as a nonconsumed boundary. The needed lower-tail Chernoff inequality is correctly assigned to a local exponential-Markov proof rather than the false factor-one two-sided source display. The Lyu source labels used for decomposition, lists, selection, and privacy remain convention-compatible at the set-theoretic level. No cited source is identified as supplying the missing measurable state/output interface.
- Same-setting repair plausibility: A target-preserving repair remains plausible: expose a measurable state/output sigma-field and prove the exact list, Sparse Sample, and occurrence-mark kernels measurable; define the Fubini event directly through actual marked output, empirical error at most (\alpha/8), and population error above (\alpha); and handle the trace event through a measurable envelope or by conditioning the already measurable final event. If such a kernel cannot be constructed for the arbitrary-space exact procedure, the next review must escalate with a concrete theorem-contract-change rationale.
- Target-preserving bridge-repair gate: The quantitative target, procedure, metric, and success criterion need not change merely to add the missing bridge and dependency interfaces. Therefore the smallest current repair is the sketch, not the idea.
- High-risk obligation classes: Structural descent, finite list support, adaptive selection, all-input privacy, improper-output identity, mode conversion, explicit-rate specialization, and baseline comparison were all scanned. Only the joint measurability/generated-output-flow class remains blocking; the concentration and rate mathematics behind the proposed repair survives.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Boundary branch and teacher/list/noise fixed point | Baseline recovery and quantitative learner entry | Direct LD/VC facts; explicit `(Teacher)` through `(MarginBound)` dictionary | Primitive setting assumptions and current scalar definitions; Lyu list envelope and privacy threshold only for their stated roles | Linear (t/2) margin is the right source for logarithmic list/noise losses | The exact (L(t)), (B(t)), and privacy parameters match the branch convention | (t/2-\tau_{\rm AT}\ge B(t)+\tau_{\rm SS}(t)+2); explicit (\bar k) is feasible | Public (d,v,\alpha,\beta,\varepsilon,\delta) produce the exact tuple used downstream | No surrogate tuple; ceilings remain in the bridge | One finite scalar check; no accumulated forcing | Candidate tuple is defined before feasibility and before all consumers | `step_001` produces for every later parameter consumer | (d=0) bypasses all positive formulas; (d=v=1), tiny (\delta), and ceiling-dominated cases remain covered | step-local | None |
| Conditional trace deviations and exact (E_{\rm good}) export | Generated concentration condition for source threshold algebra | Fixed-trace without-replacement tails, xor/Sauer count, finite partition union | Lyu Proposition 2 plus direct KL upper tail and step_002 count | Correct for each fixed indexed sample and finite partition section | Error-vector and threshold conventions match Lyu; joint sample-event convention is not supplied | For fixed (S), (\Pr_P(E_{\rm good}^c\mid S)\le4k\Pi_C(n_0)e^{-c_{\rm tr}m\alpha/d^2}) | Fixed-section raw controls support the conditional bound; no measurable joint event/envelope is exported | The produced and consumed trace/block errors coincide; there is no surrogate residual | Finite union only, but cross-sample measurability is uncontrolled | Conditional producer is noncircular; unconditional tower use presupposes the missing joint measurability | Intended producer step_004/step_005; consumers step_006, step_010, and the final ledger | Zero trace and repeated indices pass sectionwise; arbitrary-space joint event remains unresolved | sketch/interface defect | /proof-sketch |
| Factor-two stage bridge, lists, and irreducible leaf witnesses | Source-convention and generated structural-object bridge | (S-H) through (Irred); Lyu Definitions 4.2/4.3, Lemmas 4.1/4.3, Corollary 4.1 | Exact source labels and current wrappers | Produces actual functions and both (n_0)- and (d+1)-irreducibility | (H_i^r=H_{i,\rm src}^{r+1}), (p_{r+1}=2p_r), and equality is on all (X) | Inclusion plus (p_r2^{d-t}\ge n_0d\ge\max\{n_0,d+1\}) | Set-theoretically, each (\mathcal L_i^r) depends only on block (S_i) and has size at most (L) | Factor-two residual is exactly discharged; no trace representative remains | Finite exact transitions; no additive defect | step_004 and step_005 precede step_006; no target membership is assumed | step_006 produces lists and leaf witnesses for all later structural and PAC consumers | Empty classes are totalized; DDim zero, (d=1), and infinite (C) are covered set-theoretically | step-local | None |
| DDim descent and score-(k) actual item | Signed finite recurrence and support production | Lyu Lemma 4.3 equality case, Corollary 4.1 Item 4, realizable target | Source structural results and `step_006` outputs | Absence of common support forces a strict integer drop; zero DDim gives the common target | Exact ((2p_r,p_r)) comparison and actual-function identity | (0\le M_0\le d); no score-(k) item implies (M_{r+1}\le M_r-1) | Inclusion and lists export a score-(k) stage by stage (d) | Same actual function is scored and later selected | One-unit charge with finite budget (d); no forcing | `step_006` then `step_007`, without assuming termination | `step_007` produces for `step_008` and `step_010` | (M_0=0) is active immediately; final zero state contains target (c) | step-local | None |
| Actual Sparse Sample output on the good mechanism event | Finite adaptive selection and output-support condition | Lyu Algorithms 1-2; exact score weights; direct Laplace/exponential-weight tails | Source conditional finite mechanisms and current teacher margin | For a fixed list tuple, the support and score statements are exact | Score, failure-symbol, threshold, and list-size conventions match | Reported (q_r\ge k/2-\tau_{\rm AT}); returned score (\ge q_r-\tau_{\rm SS}>B) | Set-theoretic finite tuple exports an actual list member with allocated failure | Output is the same actual function consumed by `step_010`; no surrogate | (d+1) finite noises and one optional call | `step_001`,`step_007` then `step_008` | `step_008` produces selected item/state for later steps | Early false positive, first/last stage, empty list, and (\perp) are handled | step-local | None |
| All-input learner kernel and replacement privacy | Public stochastic-kernel and DP scope upgrade | Total procedure, RawList, source privacy lemmas, adaptive composition | Lyu list-level DP statements plus current raw-record map | Pointwise list-level likelihood inequalities match raw replacement after block localization | The sketch does not declare/prove the sample-to-output kernel on the output sigma-field | Privacy arithmetic is ((\varepsilon/4,\delta/2)+(\varepsilon/4,\delta/2)\preceq(\varepsilon,\delta)) | Raw adjacency controls are present; measurable kernel feasibility is not | No metric residual; the unresolved term is the kernel interface itself | Finite transcript and one call; no stage accumulation | Totalization is prior, but measurability is not produced by `step_008` or `step_009` | Intended `step_009` producer; `step_011`, `step_012`, and final theorem consume the learner law | Arbitrary labels and terminal paths are set-theoretically total; arbitrary-space measurability remains untested | sketch/interface defect | /proof-sketch |
| Exact SOA identity and empirical error | Improper object-target and empirical-utility bridge | Essential leaf witness, `(Irred)`, Lyu Theorem 3 contradiction | `step_006` and `step_008` outputs plus exact source proof pattern | Produces the same selected (f=\operatorname{SOA}_{\mathcal G}) and (e_S(f)\le\alpha/8) | Function equality is on all (X); no trace quotient | (n_0)-irreducibility forbids the all-sample SOA restriction from emptying | Actual output and generated trace event support the exact empirical claim | Same function and labels; zero quantitative residual | One deterministic contradiction after finite upstream failures | `step_006`,`step_008` then `step_010` | `step_010` produces for PAC transfer | Improper output, infinite (C), repeated points, and empirical zero pass | step-local | None |
| Occurrence-marked Sparse Sample lift | Generated marked kernel for producer-block localization | Proposed uniform mark on (I_r(f)) after exact Sparse Sample output | Only a fixed-input finite conditional law is identified | Projection preserves the fixed-input output marginal, but source does not prove a measurable joint kernel | Output/state sigma-field, list-map measurability, and marked-event measurability are absent | Proposed mass is (\kappa_s(f)/\lvert I_r^s(f)\rvert) on ((f,i)) for (i\in I_r^s(f)) | Nonempty occurrences and finite score are raw controls; they do not imply (s\mapsto K_s(E\times\{i\})) measurable | Projection has zero distributional residual if the lift exists; existence as measurable kernel is unsupported | One finite mark; no accumulation | The claimed producer assumes exactly the measurability it must export | Intended producer is step_008 or repaired step_009; consumer is step_011 | (k=2), adaptive stage, and infinite (C) preserve set-theoretic occurrences but do not resolve measurability | sketch/interface defect | /proof-sketch |
| Fixed-section PAC transfer and (\beta_{\rm gen}) charge | Mode conversion from empirical to population error | Producer-block union, iid complement, holdout inequality, local Chernoff | Direct probability derivation after a legal measurable marked event | Sectionwise source matches the claim and avoids uncountable uniform convergence | Each fixed candidate loss may be measurable, but joint marked learner-error event is not produced | (e_{S_{-i}}\le k\alpha/[8(k-1)]\le\alpha/4); tail (\le e^{-9\alpha(k-1)m/32}) | Raw finite section has at most ((d+1)L) candidates; Fubini needs a jointly measurable (\mathcal H_i) | Same (f,c) and (0)-(1) metric; no residual beyond event measurability | One finite section union and one confidence charge | Current (\mathcal H_i) includes potentially nonmeasurable (E_{\rm good}) and assumes a measurable risk event | Intended `step_011` producer; `step_012` and final theorem consume PAC utility | (k=2), (d=v=1), empirical zero, and Durst--Dudley classes pass the arithmetic, not the interface | sketch/interface defect | /proof-sketch |
| Public rate, old-Lyu arm, and baseline frontier | Quantitative specialization and baseline invariance | Teacher envelope, trace/PAC/DP outputs, direct algebra, finite-class exponential mechanism | Earlier planned outputs and comparison-arm dictionaries | New-arm and old-arm algebra match their stated powers; finite arm is genuinely finite | Common probability interface is missing for both marked Lyu arms | `CompressionLog` gives (\log[4k(d+1)L/\beta]\le22d^2a\log Q), dominated by ((9/32)2^{20}d^2a\log Q) | Scalar raw controls are adequate; completed DP/PAC interfaces are not yet available | Only allowed logarithms remain algebraically; measurable PAC defect persists | Finite specialization; no asymptotic accumulation beyond declared (N\delta) schedule | `step_012` depends on the unresolved PAC producer | Final theorem and class-complexity minimum | (d=0), (v=d), finite/infinite (C), and (N\delta\to0) schedule are algebraically preserved; old arm inherits the interface defect | sketch/interface defect | /proof-sketch |

## Blocking Issues

1. Location: "Measurable occurrence mark and block-local reconstruction bridge", the `step_011` mechanism witness, Exported Interface Feasibility, Generated Output Flow, and the `step_011` row. Defect: the exact fixed-input Sparse Sample law and its occurrence scores do not by themselves supply a measurable kernel from datasets to marked pairs. The sketch declares the lift measurable without specifying the state/output sigma-fields or proving measurability of the data-dependent list support, the output kernel, the membership events, or the refined probabilities. Downstream effect: ({J=i}) and hence the Fubini integrand have no legal measurable producer. Smallest repair direction: add a source-compatible measurable-kernel bridge for the exact procedure, with projection to the original learner law and a producer-consumer dependency path. If `step_009` is the producer, make it export this interface and add it as a direct dependency of `step_011`.

2. Location: equations `(Good)`, `(Section)`, `(Compression)`, the `step_004`/`step_005` event flow, and the sentence defining (\mathcal H_i). Defect: (\mathcal H_i) is defined by intersecting (E_{\rm core}), which contains the uncountable-class event (E_{\rm good}), with a population-error event that is merely called measurable. Neither joint event is shown measurable under the declared arbitrary-space/output convention. A pointwise finite-section cover bounds sections only after an already measurable left-hand event exists; it cannot create that measurability. Downstream effect: Fubini, the unconditional trace charge, and the four-part failure ledger are not justified. Smallest repair direction: define the PAC Fubini event directly through actual marked output, (e_S(\widehat f)\le\alpha/8), and (\operatorname{err}_D(\widehat f,c)>\alpha), prove that event jointly measurable, and either produce a measurable (E_{\rm good}) envelope or route its pointwise partition bound through sections of the already measurable final event.

3. Location: the old-Lyu paragraph under "Exact normalized comparison wrappers" and `step_012`. Defect: the old comparison arm reuses the same unproved marked-kernel/Fubini interface. Downstream effect: the common ((\alpha,\beta,\varepsilon,\delta)) baseline required before taking the public minimum is not established, even though the old-arm rate algebra is plausible. Smallest repair direction: propagate the repaired measurable interface to the old arm and repeat the common-interface audit; do not cite Corollary 5.1 as a measurability theorem.

## Required Repair Bundle

1. Linked issue: Blocking Issue 1. Required change: declare the exact analysis/output state and sigma-fields and give an obstruction-level proof that the sample-to-list/Sparse-Sample law is a measurable kernel and that the uniform occurrence refinement is a measurable kernel. Record the formula for every marked-coordinate event, its source-to-claim implication, and projection equality. Affected steps: `step_006`, `step_008`, `step_009`, `step_011`; affected flow/interface tables: Mechanism-Source And Boundary Stress, Exported Interface Feasibility, Generated Output Flow, Dependency Notes. Smallest repair target: `/proof-sketch`.

2. Linked issue: Blocking Issue 2. Required change: replace (\mathcal H_i=E_{\rm core}\cap\{\operatorname{err}_D(\widehat f,c)>\alpha\}\cap\{J=i\}) by a directly measurable marked actual-output event carrying (e_S(\widehat f)\le\alpha/8) and bad population error. State exactly how the population-error event is measurable on the declared output interface; do not infer it from the PAC objective. Then rederive the fixed-section inclusion and Fubini identity. Affected step: `step_011`. Smallest repair target: `/proof-sketch`.

3. Linked issue: Blocking Issue 2. Required change: repair the unconditional trace-event route. Either construct a measurable joint/envelope event with the same fixed-(S) bound, or avoid exporting (E_{\rm good}) as a standalone random event and integrate sections of an independently measurable final learner event. Remove every hidden "for laws for which the event is measurable" restriction. Affected steps: `step_004`, `step_005`, `step_006`, `step_010`, `step_011`; affected generated-output flow: (E_{\rm good}) and total confidence. Smallest repair target: `/proof-sketch`.

4. Linked issue: Blocking Issues 1-2. Required change: make the measurable producer explicit in the step graph. The current direct `step_006` dependency is correct for block locality. Add `step_009` to `step_011` if `step_009` proves the learner/marked kernel, or strengthen `step_008` to produce that kernel and state why `step_009` is not needed. Affected step: `step_011` and Dependency Notes. Smallest repair target: `/proof-sketch`.

5. Linked issue: Blocking Issue 3. Required change: instantiate the repaired measurable marked-section bridge for the old-Lyu arm and reverify its PAC probability mode before the frontier is exported. Preserve the finite arm, (d=0), (v=d), infinite-class disabling, and small-(\delta) schedule unchanged. Affected step: `step_012`. Smallest repair target: `/proof-sketch`.

6. Target-preserving repair check: Preserve the setting, exact Lyu procedure, arbitrary-class scope, output hypothesis, metric, exposed rate, confidence ledger, and `material_partial` conclusion. The finite-reconstruction, holdout, Chernoff, multiplicity, privacy-budget, and rate calculations need no weakening. If the exact procedure cannot support the required measurable kernels/events without an added standard-Borel, countability, permissibility, measurable-selection, or output-sigma assumption, the next artifact must identify that concrete theorem-contract obstruction rather than silently adding the assumption.

## Review Rationale

The finite-reconstruction idea is materially stronger than the failed generic relative-VC route, and its set-theoretic probability and rate calculations are viable. It is not ready for global or step proof because the repair's defining premise, a jointly measurable marked learner-error event, has no legal producer. The same omission also exposes an older joint-measurability gap in the standalone (E_{\rm good}) ledger and propagates to the old-Lyu comparison arm. These are roadmap, interface, and dependency defects that can still plausibly be repaired without changing the theorem contract, so the sketch is routed back for the smallest target-preserving revision.
