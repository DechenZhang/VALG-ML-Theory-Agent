# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Reviewed artifact: `perspective_2/idea_3/proof_sketch.md`
- Reviewed sketch SHA-256: `4bfc2da8c0aebf4c01c543c53a3b2e7a0303cc7a91df63c46a7ddb86c19ffada`
- Binding setting: `perspective_2/idea_3/setting.md`
- Binding setting SHA-256: `13cb7296ea48e286ba553a4a153b84c00bd51c97f43c74d6b74f62321258d327`
- Optional source-plausibility input: `perspective_2/idea_3/technical_survey.md`, SHA-256 `3fcf55b8fad2143c0b965f57016fbe7452c945cdfdbd716dcf33850884dd7b48`
- Formalized target: exact-goal, universal `material_partial` VC-sensitive refinement of Lyu's fixed-block learner.
- Reviewed roadmap: twelve dependency-ordered steps, from the zero-dimensional split and conditional VC trace count through Lyu's unchanged decomposition/private-selection interfaces and the public rate/baseline bridge.

## Sketch Viability Score

8

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The roadmap targets the exact theorem in `setting.md`: the same arbitrary possibly infinite finite-Littlestone class scope, realizable distribution-free data model, all-input replacement-adjacency approximate DP, unrestricted improper learner, displayed (d,v,\alpha,\beta,\varepsilon,\delta) dependence, and zero-sample (d=0) branch. It changes only the fixed-block trace calibration. The result is consistently labeled `material_partial`; it does not claim a characterization or a general polynomial dependence on (v) and (\log d) or (\log^*d), and it states the remaining source gap.
- Dependency audit: All twelve stable step IDs have exact theorem roles and only backward dependencies. `step_002` and `step_003` independently produce the trace-count and per-trace-tail inputs; `step_004`-`step_005` close the generated event before any source stage consumes it; the source utility and privacy branches then separate cleanly before `step_012`. No theorem-facing output is consumed before its producer.
- High-risk coverage: The sketch localizes the near-zero without-replacement upper tail, ceiling-aware fixed point, source stage/list compatibility, noisy common-item margin, raw-record-to-one-list privacy map, improper-output generalization, and no-hidden-positive-power specialization. Each has a nonvacuous mechanism source and an obstruction-level control relation. These remain substantial local proof obligations, but none requires a changed assumption, procedure, metric, scope, or success criterion.
- Explicit-rate coverage: The rate objective records all exposed variables, universal-only hidden constants, fixed quantities, privacy/probability/horizon/norm modes, auxiliary quantities to eliminate, and the required baseline reductions. The raw relations (m\asymp d^2a\log Q/\alpha), (n_0=km), (\log(en_0/v)=O(\log Q)), and the source-valid (k=\widetilde O(d^2\log(1/(\delta\beta))/\varepsilon)) give the claimed (d^4(v+\log(1/\beta))) power profile; all remaining occurrences of (k,p_j,L_j,a,Q) are logarithmic and are assigned to `step_008` and `step_012` for explicit discharge.
- Assumption and citation plausibility: The three primitive assumptions are cited by stable IDs, while concentration events, lists, support, common items, sensitivity, privacy, and generalization remain derived. Xin Lyu, arXiv:2510.00076v1, provides the exact cited labels and source roles: `prop:chernoff-sample-without-replacement`, the displayed (E_{\mathrm{good}}) and (H_i^j) interfaces in the proof of `thm: DP-ERM-littlestone`, the decomposition and essential-hypothesis results, the two private mechanisms, `lemma: Ldim-of-SOA`, and `coro: PAC learning`. The sketch records their non-output boundaries. The one low-error tail not stated by Lyu's multiplicative proposition is honestly assigned to a direct current-notation hypergeometric/KL derivation in `step_003`.

## Early Obstruction Audit

- Limiting-case stress: For (d=0), nonemptiness and (\operatorname{LD}(C)=0) force a singleton class, so deterministic zero-sample output is exactly private and correct. In the positive branch (d\ge1) implies (1\le v\le d). Repeated sample points and the zero error trace do not affect the indexed-partition argument. For a fixed trace with mean (\mu\le\alpha/3), the one-sided hypergeometric/KL bound at threshold (\alpha/2) has exponent at least (c m\alpha), including (\mu=0), so weakening it to (c m\alpha/d^2) is valid. At (v=d), the block scale returns to (\widetilde O(d^3/\alpha)) and the total to the source (\widetilde O(d^5)) scale. Nonrealizable neighboring inputs affect privacy but are not used in the utility proof; `step_009` explicitly totalizes that branch.
- Theorem-critical bridge support: The only new bridge is supported by fixed-sample trace counting and without-replacement concentration. Conditional on the indexed labeled sample, xor with the fixed label vector bijects prediction and error traces, so there are at most (\Pi_C(n_0)\) relevant vectors. A union bound over (k\Pi_C(n_0)) block/trace pairs gives exactly the event consumed by Lyu. The later bridges are source-identical current-notation wrappers with explicit producer steps.
- Exported-interface feasibility: The sketch fixes the raw controls and consumers for the trace count, per-trace tail, (E_{\mathrm{good}}), list envelope, common-item score, private selected output, empirical-risk object, output-class complexity, and public rate. In particular, the selection interface separates the list-size/privacy threshold, failure-symbol score, Laplace error, and effective-domain utility loss; the privacy interface separates AboveThreshold sensitivity from one-list Sparse Sample adjacency. No single threshold is asked to control an unrelated defect class.
- Theorem-critical mechanism witness gate: Passed for every applicable block. The finite trace union has exponent versus multiplicity; the scalar fixed point has (a\log Q) versus (v\log(en_0/v)+\log(k/\beta)); the source stage closure has an integer DDim potential with one-unit drop; selection has a (k/2) common-item margin versus threshold/noise terms; privacy has sensitivity one and one-list replacement; empirical-to-population conversion has a fixed output class of VC dimension at most (d); and specialization has explicit substitution inequalities. Each witness names its opposing terms, boundary behavior, and consumers.
- Entry-state trace stress: The (d=0) state exits before positive-branch formulas. For (d\ge1), `step_001` supplies legal positive parameters; `step_004` produces (E_{\mathrm{good}}) before the first source inclusion; the first accurately unsuccessful stage activates the DDim drop before the next stage; the first frequency query is sensitivity one on arbitrary input; and an output with excessive empirical error contradicts the already-produced irreducibility interface. No tested entry leaves a required mechanism inactive while its theorem conclusion is false.
- Obligation locality classification: `step_001` through `step_012` are all `step-local`. The mechanism sources already occur in primitive assumptions, direct finite-sample algebra, standard facts assigned to direct derivations, or identified source statements with current-object mappings. Later proof steps must instantiate those sources but are not being used as the first source of any theorem-critical mechanism.
- Noncircular closure gate: The event, inclusion, list, common-item, selected-output, privacy, empirical-risk, and generalization interfaces all have earlier producers. The DDim descent does not assume termination, the fixed point uses (Q) independent of (m,n_0), privacy is not conditioned on a utility event, and uniform convergence is over a fixed source-defined output class rather than the realized output alone.
- Mechanism-source and boundary stress: The high-error trace branch follows Lyu's relative without-replacement proposition; the low-error branch uses the separately identified one-sided KL wrapper, so the mechanism does not vanish as the trace mean tends to zero. The current (p_j) sequence doubles stage by stage and begins at (n_0d), which is sufficient for the (n_0)-point irreducibility contradiction; the harmless source indexing/common-factor normalization only decreases list envelopes. At (d=1), all sources remain active, while (d=0) is excluded from their denominators.
- Generated-output flow: Passed. Every generated output in the sketch's flow table has a legal producer, explicit consumers, final theorem use, and forward dependency path. The final assembly consumes DP only from `step_009`, utility only from `step_011`, and the quantitative/baseline bridge only from `step_012`.
- Source-to-claim adequacy: Sauer--Shelah supplies a trace cardinality bound of the required type, not merely regularity. The hypergeometric/KL derivation supplies the missing positive exponent at the low-error boundary. Lyu's decomposition results supply finite essential lists and strict DDim descent; his sparse-sampling lemma supplies replacement-list privacy under the displayed (L,B,\varepsilon_s,\delta_s) interface; his AboveThreshold lemma supplies privacy for sensitivity-one queries; and `lemma: Ldim-of-SOA` supplies a fixed improper output class of Littlestone, hence VC, dimension at most (d). The finite-class source is used only for the separately normalized comparison arm. No upper-bound-only source is used as a lower-margin or support source.
- Residual-to-target adequacy: The produced error traces and source-consumed traces are identical; the produced (E_{\mathrm{good}}) is the exact source event; fixed blocks, (H_i^j), essential lists, and the selected SOA hypothesis are not transformed surrogates; one raw replacement maps to one block and hence one list; and empirical and population errors concern the same selected hypothesis and target. The only numerical residuals are the trace multiplicity, rounding, private-mechanism noise, confidence allocations, uniform-convergence term, and auxiliary logarithms, each with a named domination relation and target scale.
- Baseline invariance audit: The inherited conclusions are preserved: exact zero-sample learning for (d=0); the original universal Lyu arm; the finite-cardinality arm; and return to (\widetilde O(d^5)) when (v=d). The (d=0) stationary state already satisfies utility and privacy. In the full-cube specialization the new trace exponent equals (d), so the sketch does not manufacture a saving. The three-arm minimum is delayed until the arms share the public parameter interface.
- Scope-accumulation compatibility: The trace and confidence closures are finite unions. The only repeated structural scope is at most (d+1) stages, controlled by (M_j\in\{0,\ldots,d\}) and the one-step relation (M_{j+1}\le M_j-1) after an accurately failed stage. Utility noise failures are union-bounded over the finite stages; privacy uses one AboveThreshold process and one Sparse Sample call, not additive stage composition. No persistent adversarial-sign defect is left unbudgeted.
- Scope and dependence consistency: Quantifiers, arbitrary-class scope, all-input privacy, realizable utility, fixed-sample mode, error metric, and hidden-dependence restrictions match `setting.md`. The (\delta\ll1/N) convention is used only for the source-facing asymptotic comparison; the theorem keeps explicit (\delta)-dependence. Neither (\log d) nor (\log^*d) is conflated with (d), and no class/list/support cardinality is hidden in a universal constant.
- Generated-condition provenance: Passed. (E_{\mathrm{good}}), inclusion, bounded lists, potential descent, common support, mechanism-good events, raw-neighbor sensitivity, DP, improper-output membership, empirical error, and generalization are all derived outputs with named producers. None is moved into the unconditional theorem assumptions.
- Citation and tool applicability: The current arXiv source identity and exact labels were independently checked. The without-replacement proposition has the same indexed finite-population convention as a fixed teacher block. The proof of `thm: DP-ERM-littlestone` displays the same (E_{\mathrm{good}}), fixed blocks, (H_i^j), geometric (p_j), essential lists, AboveThreshold/Sparse Sample flow, empirical-risk contradiction, and PAC corollary used here. The sketch correctly records that the multiplicative proposition alone does not prove the near-zero clause and that the private-mechanism lemmas require sensitivity/list-envelope/threshold discharge. Ghazi et al.'s Lemma 4.4 is consumed through Lyu's exact `lemma: Ldim-of-SOA` restatement. The remaining tools are direct current-notation derivations or standard inequalities with their raw assumptions exposed.
- Same-setting repair plausibility: No current material obstruction needs repair. If a later local derivation fails, the sketch already identifies the narrow candidate step; a newly discovered mismatch in the exact source event, list identity, privacy map, or positive-power specialization would instead require sketch review at that time. No such mismatch appears at obstruction-level preflight.
- Target-preserving bridge-repair gate: Not triggered by a present blocker. The reviewed roadmap preserves the binding setting and goal without adding assumptions or weakening a baseline. Local constants, ceilings, confidence splits, and current-notation source wrappers can be discharged inside the assigned steps under the unchanged theorem contract.
- High-risk obligation classes: Structural trace counting, near-zero concentration, generated-event closure, recursive DDim descent, list support and private selection, raw-input privacy, improper-output generalization, explicit-rate specialization, source convention, and baseline recovery were all tested. Each is localized and source-adequate at sketch granularity.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `step_001`: zero-dimensional recovery and positive-branch legality | Baseline invariance and formula-entry certificate | Direct VC/LD definitions and deterministic output | Primitive `assump:finite-littlestone`, `assump:approximate-dp-regime` | \(d=0\Rightarrow C\) singleton and \(v=0\); \(d\ge1\Rightarrow1\le v\le d\) | Same binary-class, LD, VC, and replacement-DP conventions as the setting | Zero data give zero error and \(0\)-DP; positive ranges make \(a,Q,m,n_0\) legal and \(n_0\ge v\) | Raw class/parameter ranges export the branch split and allocation package | No residual: the singleton output is the target; positive formulas are bypassed at \(d=0\) | One-time branch split; no accumulated defect | Primitive conditions are available before `step_001`; no generated premise is assumed | `step_001` produces the branch certificate and parameter package for all later steps and `step_012` | \(d=0\) is stationary and correct; \(d=v=1\) enters legally | step-local | None |
| `step_002`: VC error-trace count | Structural finite-cardinality producer for concentration | Xor bijection and Sauer--Shelah | Direct standard derivation from `assump:finite-littlestone` | Supplies \(\lvert\mathcal E_C(S)\rvert\le\Pi_C(n_0)\le(en_0/v)^v\) | Indexed repeated points and arbitrary fixed labels preserve trace cardinality; \(n_0\ge v\ge1\) | Restriction traces mapped by xor with the fixed label vector are in bijection | Fixed \(S\), VC dimension \(v\), and \(n_0\) export the exact error-trace family used in `step_004` | Zero object-transfer residual; duplicates collapse rather than add terms | Static finite count; no forcing or accumulated defect | `step_001` proves legality before the trace count is used | `step_002` produces the trace bound for `step_004` and `step_005` | Repeated points and \(v=1\) remain valid | step-local | None |
| `step_003`: two-branch per-trace block deviation | Approximation/concentration interface, including null-source boundary | Lyu `prop:chernoff-sample-without-replacement` plus direct one-sided hypergeometric/KL derivation | arXiv:2510.00076v1 and current-notation direct derivation | Relative source gives the high-error clause; KL upper tail gives the low-error clause at positive \(m\alpha\) scale | A fixed binary population on indexed \(S\) and a uniform size-\(m\) subset exactly match one source teacher block | For \(\mu>\alpha/3\), failure is at most \(2e^{-m\alpha/(225d^2)}\); for \(\mu\le\alpha/3\), \(\Pr(\widehat\mu>\alpha/2)\le e^{-mD(\alpha/2\mathbin{\Vert}\mu)}\le e^{-cm\alpha}\) | One trace, exact source thresholds, and \(m\) export total failure at most \(4e^{-c_{\rm tr}m\alpha/d^2}\) | No wrong-scale residual: the low-error exponent is stronger than the consumed (m\alpha/d^2) scale | One block event followed only by the finite union in `step_004` | `step_001` supplies the legal parameter range before the direct tail derivation | `step_003` produces the per-trace tail for `step_004` | At \(\mu=0\) failure is impossible; at \(\mu=\alpha/3\) KL is \(\Omega(\alpha)\) | step-local | None |
| `step_004`: exact simultaneous \(E_{\mathrm{good}}\) | Generated event and finite-scope closure | Outputs of `step_002`, `step_003`, finite union bound, tower property | Direct derivation with Lyu's displayed event definition | Produces the exact high/low event consumed by the source inclusion proof | Same master sample, blocks, hypotheses, empirical errors, and thresholds; no independence convention is imported | \(\Pr(E_{\rm good}^c\mid S)\le4k\Pi_C(n_0)e^{-c_{\rm tr}m\alpha/d^2}\) | Trace count and per-block tail export the conditional event; integration exports its unconditional budget | The produced event is exactly the source-consumed event; no object or threshold residual remains | Finite multiplicity \(k\Pi_C(n_0)\) is explicit; no sign or iterative defect | `step_002` and `step_003` precede every use of the event | `step_004` produces \(E_{\rm good}\) for `step_005`-`step_007`, `step_010`, and `step_011` | Mutually dependent partition blocks are harmless under a union bound | step-local | None |
| `step_005`: fixed point and trace-failure budget | Explicit-rate bridge and generated-event probability closure | Direct scalar algebra from \(a,Q,m,n_0\) | Primitive parameters plus `step_004` | Supplies the exact logarithmic domination needed by the trace exponent | Uses the binding ceiling and \(Q\) convention; \(Q\) is independent of \(m,n_0\) | \(en_0/v\le CQ\log Q\), so \(\log(en_0/v)\le C_{\rm fp}\log Q\); universal \(C_{\rm blk}\) makes the exponent dominate \(vC_{\rm fp}\log Q+\log(4k/\beta_{\rm tr})\) | Raw \(k,d,v,\alpha,\beta\), ceiling, and `step_004` export the failure budget and \(n_0\le Ckd^2a\log Q/\alpha\) | Ceilings and additive constants are dominated at the same logarithmic target scale | One scalar closure; rounding is absorbed at the same logarithmic scale | \(Q\) is fixed before \(m,n_0\), so the fixed point is noncircular | `step_005` produces the probability/rate certificate for `step_006`, `step_008`, `step_011`, and `step_012` | \(d=v=1\) has positive denominators; \(v=d\) recovers the source block power | step-local | None |
| `step_006`: fixed-block inclusion and bounded essential lists | Generated membership/interface wrapper | Lyu proof of `thm: DP-ERM-littlestone`, `lemma: number of leaves`, `coro: essential hypotheses` | arXiv:2510.00076v1 exact labels | \(E_{\rm good}\) and geometric thresholds give \(H_i^{j+1}\subseteq\cap_{i'}H_{i'}^j\); decomposition results give list size at most \(p_j^d2^{d^2}\) | Same fixed blocks and \(H_i^j\); current stages double \(p_j\), begin at \(n_0d\), and use the recorded harmless index/common-factor normalization | The source threshold ratio times \(1-1/(2d)\) is at most one; \(p_{j+1}=2p_j\); \(L_j\le p_j^d2^{d^2}\) | Exact event, source thresholds, \(p_j\), and restrictions export inclusion and finite lists | Produced and consumed blocks, restrictions, and lists are the same; no fresh-slice or surrogate residual exists | Finite \(d+1\)-stage interface; no fresh-slice or transformed-object residual | `step_004`-`step_005` produce the event and calibration before source objects consume them | `step_006` produces inclusion/list envelopes for `step_007`-`step_010` | Empty/singleton restrictions are totalized; \(d=1\) retains the threshold gap | step-local | None |
| `step_007`: DDim descent and common essential item | Structural progress witness, support, and finite-stage closure | Lyu `lemma: p-decomposition`, `def: essential hypothesis`, `coro: essential hypotheses` | arXiv:2510.00076v1 exact statements | Inclusion plus equal decomposition dimension produces a common essential SOA; accurate absence forces strict dimension loss | Same \(H_i^j\), optimal decompositions, SOA identities, and doubling-\(p\) convention | \(M_j=\max_i\DDim_{p_j,d}(H_i^j)\in\{0,\ldots,d\}\); an accurately unsuccessful stage gives \(M_{j+1}\le M_j-1\) | Inclusion and finite lists export an actual score-\(k\) item at a plateau and success within \(d+1\) stages | The common item is an actual essential SOA in the produced lists; no support or identity residual remains | One-unit monotone potential drop with budget at most \(d\); mechanism noise is budgeted separately | `step_006` produces inclusion and lists before descent/support is consumed | `step_007` produces potential closure and the common item for `step_008` and `step_010` | At \(M_0=0\), the source corollary gives a finite actual item; at \(d=1\), at most one failed drop occurs | step-local | None |
| `step_008`: accurate detection and actual-list Sparse Sample output | Generated private-output utility, membership, and margin closure | Lyu `algo: abovethreshold`, `algo:private sample`, their utility calculation, and direct exponential-mechanism tail | arXiv:2510.00076v1 exact algorithms; standard direct utility derivation | A score-\(k\) common item and bounded lists supply the positive margin against the failure symbol and noise | Same list tuple, score, failure symbol, one AboveThreshold process, and one successful-stage Sparse Sample call | With \(L_{\max}\le(2^dn_0d)^d2^{d^2}\), take \(B\ge10\log(L_{\max}/\delta_s)/\varepsilon_s\), \(k/2>B+c\log(kL_{\max}/\beta_s)/\varepsilon_s\), and \(k/10>c\log((d+1)/\beta_t)/\varepsilon_t\) | Source-valid \(k\), list envelope, privacy split, and common score export an actual list item with allocated failure | The output is sampled from the exact produced list union; membership and support transfer have zero residual | Laplace, effective-domain, failure-symbol, and confidence defects are separated and finitely union-bounded | `step_007` produces support before selection; membership is not assumed | `step_008` produces the selected output/good event for `step_009`-`step_011` | Empty lists create no false utility conclusion; a score-\(k\) plateau activates selection | step-local | None |
| `step_009`: all-input end-to-end approximate DP | Privacy closure and raw-to-list interface | Lyu `lemma: privacy of abovethreshold`, `lemma: privacy of sparse sampling`, adaptive/concurrent composition, postprocessing | arXiv:2510.00076v1 exact privacy statements | Sensitivity-one queries and one bounded-list replacement exactly match the cited claim classes | Couple neighbors with the same data-independent partition: one record changes one block, hence one list per stage; utility and realizability are unused | Choose internal parameters so AboveThreshold costs at most \((\varepsilon_t',\delta_t)\), Sparse Sample costs \((2\varepsilon_s,\delta_s)\), and the two budgets sum within \((\varepsilon,\delta)\) | Raw-record coupling, totalized queries/lists, \(L,B\), and one-call structure export public replacement-adjacency DP | One raw replacement maps exactly to one replaced list in each conditional interface; no adjacency residual remains | No stage-count privacy defect: one SVT process and one Sparse Sample call | Totalized source mechanisms are DP for every transcript; the proof does not condition on utility events | `step_009` produces end-to-end DP for `step_012` from `step_006`-`step_008` interfaces | The first query is sensitivity one on every labeled input; no-success paths use a fixed totalized output | step-local | None |
| `step_010`: improper SOA representation and low empirical error | Object-target compatibility and empirical-risk bridge | Lyu utility proof of `thm: DP-ERM-littlestone` and decomposition irreducibility | arXiv:2510.00076v1 exact proof role | An actual essential item is \(\SOA_{\mathcal G}\) for \(\mathcal G\subseteq H_i^j\subseteq C\); excessive master error makes its SOA-label restriction empty | Produced and consumed hypothesis are identical; current \(p_0=n_0d\) supplies at least \(n_0\)-point irreducibility | \(E_{\rm good}\) gives each \(g\in\mathcal G\) constant-fraction empirical error; excessive error of \(\widehat h\) contradicts irreducibility | Selected-list membership, leaf object, and fixed-block event export representation and low full-sample error | The produced and consumed hypothesis are identical; no transformed-object or target residual remains | One finite utility event; no transformed-target or repeated-scope residual | `step_006`-`step_008` produce every premise before the empirical bridge | `step_010` produces the SOA representation and empirical guarantee for `step_011` | At empirical error zero the bridge is immediate; \(d=0\) was already discharged | step-local | None |
| `step_011`: improper-output PAC conversion | Fixed-class complexity, empirical-to-population transfer, and confidence closure | Lyu `lemma: Ldim-of-SOA`, `coro: PAC learning`, relative VC inequality | arXiv:2510.00076v1; Lyu restates Ghazi et al. Lemma 4.4 | The cited lemma bounds the exact SOA-output class by LD, hence VC, at most \(d\) | Same output function and binary zero-one metric; the output class is fixed from \(C\), not the realized sample | Require \(n_0\ge c(d\log(1/\alpha)+\log(1/\beta_g))/\alpha\); combine empirical error with relative deviation and the allocated failure union | `step_010` output, fixed class, iid sample, and confidence allocations export population error at most \(\alpha\) | Empirical and population errors use the same output and target; the uniform event controls adaptive selection | Finite confidence union; uniformity controls adaptive output choice | The fixed output class exists independently and `step_010` produces the realized member before conversion | `step_011` produces final PAC utility for `step_012` | Infinite \(C\) and improper output are covered by the fixed SOA-output class; \(d=1\) remains finite VC | step-local | None |
| `step_012`: public rate, \(d=0\), \(v=d\), and three-arm frontier | Quantitative specialization and baseline invariance | `step_001`, `step_005`, `step_009`, `step_011`; Lyu `coro: PAC learning`; finite-class private ERM from literature D1 | Accepted earlier outputs and identified sources | Substitution gives the exact new theorem; old sources give only their separately stated baseline arms | Every arm is normalized to the same \(\alpha,\beta,\varepsilon,\delta\) interface before minimization | \(n_0\le Ckd^2a\log Q/\alpha\), \(a\le\Lambda^{O(1)}(v+\log(1/\beta))\), \(\log Q\le\Lambda^{O(1)}\); all auxiliary logs and rounding are bounded explicitly | Technical rate, DP, PAC utility, and source arms export \(K\Lambda^qR_{\rm VC}\) and the fixed-parameter minimum | Every auxiliary is eliminated at the public scale; no positive power or generated-object residual remains | One finite specialization; no positive power or generated-object defect may remain | All theorem-facing inputs are earlier outputs before the final substitution | `step_012` produces final theorem data and the baseline/frontier statement | \(d=0\) gives \(N=0\); \(v=d\) returns \(d^5\); \(v=o(d)\) gives the tilde-scale saving; infinite \(C\) disables only the finite arm | step-local | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch isolates a genuine raw-control refinement rather than renaming a source factor: Lyu's displayed (E_{\mathrm{good}}) already quantifies over hypotheses in (C), and conditional on the full indexed sample the relevant error vectors have Sauer exponent (v), not merely the upper bound (d). The high-error and near-zero block deviations both have the required exponent, the ceiling-aware fixed point closes the resulting trace multiplicity, and the source's later fixed-block objects remain unchanged. The difficult selection, privacy, improper-output, and specialization calculations have adequate sources and explicit current-notation interfaces; they are hard local proofs, not missing theorem mechanisms. The exact (d=0) recovery, (v=d) source scale, old universal arm, finite-class arm, and remaining open gap are all preserved. Therefore the branch is ready for the next proof stage under the unchanged setting, with status `ACCEPTED`, score 8, retry target `None`, and retry mode `none`.
