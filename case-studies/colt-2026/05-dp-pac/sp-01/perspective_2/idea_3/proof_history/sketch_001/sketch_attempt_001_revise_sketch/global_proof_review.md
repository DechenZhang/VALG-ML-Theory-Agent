# Global Proof Review

## Global-Proof Review Identity

- Branch: `perspective_2/idea_3`
- Sketch attempt: 1
- Global-proof attempt: 1
- Binding setting: `perspective_2/idea_3/setting.md`
- Binding setting SHA-256: `13cb7296ea48e286ba553a4a153b84c00bd51c97f43c74d6b74f62321258d327`
- Reviewed sketch: `perspective_2/idea_3/proof_sketch.md`
- Reviewed sketch SHA-256: `4bfc2da8c0aebf4c01c543c53a3b2e7a0303cc7a91df63c46a7ddb86c19ffada`
- Reviewed sketch review: `perspective_2/idea_3/proof_sketch_review.md`
- Reviewed sketch review SHA-256: `2a61598895571092e9f1762f97a87d17b3f38ff5999b52836f5550fc92c509f1`
- Sketch review status: `ACCEPTED`
- Reviewed global proof: `perspective_2/idea_3/global_proof.md`
- Reviewed global proof SHA-256: `0f30efe85c65737b22a4b6de4babb51426678c6f5fd955ae7a8c99b132fced4c`
- Reviewed global proof status: `PARTIAL_BLOCKED`
- Reviewed global proof suggested routing: `None`

## Global-Proof Review Status

REVISE_SKETCH

## Smallest Retry Target

/proof-sketch

## Blocking Issues

1. The claimed source-convention identity is false as written. Lyu's arXiv:2510.00076v1 algorithm uses stages (1\le j\le d+1) and (p_j=2^jnd), whereas the setting and sketch use (0\le j\le d) and (p_j=2^jn_0d). Reindexing source stage (j+1) gives (2^{j+1}n_0d), not the branch value (2^jn_0d). This changes the decomposition dimensions, essential lists, and the exact ((2p,d))-versus-((p,d)) interface in Lemma 4.3. Blocks C and D therefore cannot call the objects source-identical or treat the discrepancy as a relabeling.
2. The teacher/list/noise interface is not closed. The learner is defined using only a tilde upper envelope for (k_{\mathrm{Lyu}}(d,\beta,\varepsilon,\delta)), while Lyu Algorithm 1 and Lemma 3.1 require an explicit (B\ge 10\log(L/\delta_s)/\varepsilon_s), and utility additionally requires the common-item margin to dominate the failure-symbol and effective-domain losses. Here \(\log L=O(d\log n_0+d^2)\), so the exact lower bound for (k), its possible logarithmic (\alpha) or (n_0) dependence, its upper envelope, and its coupling with (n_0=km) must be fixed together. Equations (E.4), (E.5), and (H.1) merely defer both directions of this theorem-critical comparison.
3. The exact decomposition/common-item wrapper is deferred past the review gate. The sketch and diagnostic do not state the current-notation hypotheses and conclusions of Lyu Lemma 4.3 and Corollary 4.1, including the ((2p,d))/((p,d)) pairing, actual-function SOA equality, and the zero-DDim support case. Consequently the recurrence (M_{j+1}\le M_j-1), the existence of an actual common list item, and the list envelope do not yet have a source-convention-compatible producer.
4. The all-input learner is not fixed as a total procedure. Blocks E and F leave empty restrictions, empty lists, no-success transcripts, the failure symbol, and the final default output to later totalization, and leave the fixed-transcript raw-record-to-one-list relation (F.1) unproved at the interface level. Privacy must apply to every labeled input before any realizable utility event is invoked. Choosing these behaviors in a proof step would define or change the procedure rather than merely prove a fixed step claim.
5. The improper-output PAC wrapper does not match its cited source statement. Lyu Lemma 4.2 controls the fixed class
   \[
   \widehat{\mathcal H}_{d+1}
   =\{\operatorname{SOA}_{\mathcal G}:\mathcal G\subseteq C
     \text{ is }(d+1)\text{-irreducible}\},
   \]
   but Block G2 uses an undefined family of "eligible" or "sufficiently irreducible" outputs. The sketch must prove that the selected actual list item has exactly this representation, with at least ((d+1))-irreducibility, before treating the output family as fixed and applying relative VC generalization.
6. Because Issues 1--5 are `sketch/interface defect`s, the diagnostic's blanket `step-local` classification and `PARTIAL_BLOCKED` status are contract-invalid. Under the global-proof contract these obligations should have been exposed as sketch blockers, but rewriting only `global_proof.md` cannot repair the accepted sketch's missing or incorrect interfaces.

## Required Repair Bundle

1. **Affected artifact/section:** `proof_sketch.md`, `step_006`-`step_007`, source map, mechanism witness, exported-interface table, and generated-output flow. **Smallest target:** `/proof-sketch`. Replace the current accepted sketch with a new attempt that states the exact (H_i^j) definitions and source stage range, then either uses Lyu's exact (p_j=2^jnd) convention or proves a complete current-notation bridge for the branch's factor-two-shifted (p_j). Instantiate Lemma 4.3 and Corollary 4.1 with the exact paired parameters and identify the same actual SOA function in all lists, including the DDim-zero boundary.
2. **Affected artifact/section:** `proof_sketch.md`, `step_008` and `step_012`, plus their rate and exported-interface entries. **Smallest target:** `/proof-sketch`. In the new sketch attempt, fix an explicit integer teacher count and privacy/confidence allocation, state (B,L), AboveThreshold accuracy, Sparse Sample failure-symbol and effective-domain losses, and expose inequalities giving both the utility lower bound on (k) and the public upper envelope. Resolve any (k\leftrightarrow n_0\leftrightarrow L) fixed point and show exactly where logarithmic (\alpha,\beta,\delta) dependence is allowed.
3. **Affected artifact/section:** `proof_sketch.md`, learner flow and `step_009`. **Smallest target:** `/proof-sketch`. Fix the total algorithm on every labeled input before privacy is proved: define all empty-class/list cases, stage exhaustion, `\perp`, no-success behavior, and a data-independent default output. State the transcript-conditioned list tuple and prove at interface granularity that one raw replacement changes only one list coordinate at every query and at the selected Sparse Sample call.
4. **Affected artifact/section:** `proof_sketch.md`, `step_010`-`step_011`, source map, and PAC exported interface. **Smallest target:** `/proof-sketch`. Replace the vague output-family wrapper by the exact Lemma 4.2 class, allocate a producer for ((d+1))-irreducibility and exact SOA identity, and state the empirical-error and relative-VC conclusions with the same selected function and fixed class.
5. **Affected artifact/section:** `proof_sketch.md`, `step_012` and baseline bridge. **Smallest target:** `/proof-sketch`. Preserve the current (d=0), (v=d), old-Lyu, finite-class, infinite-class, and (N\delta\to0) conclusions, but make each comparison conditional only on already-produced common-interface DP/PAC bounds. Restate the exact Lyu Theorem 3/Corollary 5.1 parameter interface rather than using a tilde source formula as both an algorithm definition and a proved rate bound.

Rerunning `/global-proof` is insufficient: it could document the missing material, but the accepted sketch currently fixes the wrong source-convention claim and leaves the algorithm, cited wrappers, and rate-bearing exported interfaces underdetermined. All repairs above require a new sketch attempt; none presently requires changing the formalized assumptions, theorem scope, metric, success criterion, or claimed material-partial conclusion.

## Contract And Status Audit

The required input gate passes: the reviewed `proof_sketch_review.md` records `ACCEPTED` and binds the current sketch hash. The global diagnostic contains all required named sections, a diagnostic-boundary note, a controlled status, and surface-valid alignment `PARTIAL_BLOCKED`/`None`.

The substantive status gate fails. `PARTIAL_BLOCKED` is valid only when every unresolved theorem-critical obligation is already source-adequate and `step-local` under unchanged sketch interfaces. Blocks C--G and the rate part of H instead defer source conventions, wrapper conclusions, algorithm totalization, and raw-control-to-exported-interface relations. The scope certificate repeatedly calls these obligations `step-local` while saying that a future step must restate the source definition, verify normalization, select defaults, or prove the source envelope. A future proof step cannot be the first source of those interfaces. The stronger sketch-level obstruction is independently visible, so the contract-defect tie-breaker requires `REVISE_SKETCH`, not `REVISE_GLOBAL`.

## Claim And Sketch Fidelity Audit

The attempted theorem otherwise preserves the setting's arbitrary finite-Littlestone class scope, realizable distribution-free utility, all-input approximate DP, improper output, fixed-sample mode, population (0)-(1) metric, visible (d,v,\alpha,\beta,\varepsilon,\delta) dependence, and `material_partial` label. It does not silently weaken the goal or overclaim the remaining open problem.

Fidelity fails at the procedure and source-object level. The setting and accepted sketch say that every downstream object is Lyu-source-identical and that the stage normalization is only a relabeling. The exact source stage and (p_j) formulas disprove that assertion without an additional bridge. Similarly, the accepted sketch exports a source-valid teacher count, a total all-input learner, and a fixed improper output class without fixing their exact interfaces. Repair must preserve the theorem and baselines but replace the current sketch attempt.

The baseline invariance obligations themselves remain plausible: the direct (d=0) branch is sound; substituting (v=d) into the proposed new rate returns the (d^5) scale; the old Lyu and finite-class arms are separate comparisons; and the finite-class arm is disabled for infinite (C). They cannot yet be assembled because the new arm and old-arm normalization are not closed.

## Theorem-Level Structure Audit

Blocks A and B form a coherent theorem-level contribution. Conditioning on the indexed master sample, xoring prediction traces with fixed labels, applying Sauer--Shelah with exponent (v), using a high-error relative without-replacement tail and a low-error KL tail, and then taking a finite union bound do not require block independence. The ceiling-aware scalar fixed point (B.9)--(B.13) is also a viable noncircular interface.

The twelve-step coverage table is complete as a map, but Blocks C--H do not yet compose. Block C consumes an unproved convention identity; Block D consumes the corresponding uninstantiated DDim comparison; Block E consumes an unspecified exact (k,B,L) margin; Block F consumes a not-yet-fixed total algorithm; Blocks G1--G2 consume an undefined irreducibility/output-family wrapper; and Block H treats all of these as already normalized. These are roadmap/interface defects, not merely long local derivations.

## Dependency And Assumption Audit

No forbidden generated condition is listed as a primitive final-theorem assumption. The defect is instead that several named producers do not have fixed legal outputs:

1. `step_006` does not yet produce source-compatible (H_i^j,p_j), inclusion, and list objects because the accepted convention bridge is wrong.
2. `step_007` therefore cannot yet export the signed DDim recurrence or actual common-item support.
3. `step_008` lacks an exact teacher/margin producer and a total selected-output interface.
4. `step_009` depends on a "totalized algorithm interface" that is not an accepted output of an earlier fixed procedure.
5. `step_010`-`step_011` do not produce and consume the exact ((d+1))-irreducible SOA family.
6. `step_012` lacks a proved exact teacher upper envelope and common-interface source-arm normalization.

Thus the graph is syntactically acyclic but semantically incomplete. The missing derived-condition and generated-output bridges must be assigned in the new sketch rather than assumed by downstream local steps.

## Citation And Tool Audit

- **Trace tools:** Lyu, *Private Learning of Littlestone Classes, Revisited*, arXiv:2510.00076v1, Proposition 2 supplies the high-error without-replacement relative Chernoff statement. The low-error hypergeometric/KL tail is a current-notation direct derivation. Their fixed binary population, uniform block, and exact error thresholds match Block B. This part passes obstruction-level source and convention review.
- **Sparse Sample:** Source Algorithm 1 outputs either an actual element of (\bigcup_i\mathcal L_i) or (\perp). Lemma 3.1 gives ((2\varepsilon_s,\delta_s))-DP for addition, removal, or replacement of one list only when each list has size at most (L) and (B\ge10\log(L/\delta_s)/\varepsilon_s). This supports actual-list membership and list-level privacy, but it does not supply the branch's list envelope, score margin, raw-record adjacency, or no-failure utility.
- **AboveThreshold:** Source Algorithm 2 and Lemma 3.2 require sensitivity-one queries and have their own counter/privacy convention. The review does not accept the diagnostic's simplified one-process cost until the exact query sequence, stopping convention, allocation, and concurrent-composition wrapper are stated for this learner.
- **Decomposition:** Source Lemma 4.3 compares an optimal ((2p,d))-decomposition of (\mathcal G\subseteq\mathcal H) with an optimal ((p,d))-decomposition of (\mathcal H). Corollary 4.1 gives at most (p^d2^{d^2}) essential hypotheses and separate equal-DDim and zero-DDim conclusions. The diagnostic's same-(p_j) potential shorthand does not discharge these hypotheses, and the factor-two stage mismatch is material.
- **Improper outputs:** Source Lemma 4.2 controls exactly (\widehat{\mathcal H}_{d+1}), the SOA functions of ((d+1))-irreducible subclasses. Theorem 3 states realizable empirical utility and this output form; Corollary 5.1 performs the PAC conversion. None of these labels justifies an undefined "eligible" family or totalizes the branch's arbitrary-input implementation by citation alone.
- **Rate comparisons:** Theorem 3 and Corollary 5.1 provide a tilde-order source rate, while Algorithm 1/Lemma 3.1 expose a concrete list-size condition. The branch must supply an exact current-parameter wrapper before using the source expression as both the definition of (k) and the public upper bound. The finite-class exponential-mechanism arm has a plausible direct derivation and is not the source of the present blocker.

## Quantitative Dependence Audit

The diagnostic correctly exposes (d,v,\alpha,\beta,\varepsilon,\delta), keeps (|C|) confined to the finite-class comparison, uses joint high-probability utility, and distinguishes it from all-input privacy. The trace failure relation and its scalar fixed point preserve the desired exponent (v) and appear quantitatively viable.

The unresolved quantitative interface is theorem-critical. The list bound contains (d\log n_0+d^2); Sparse Sample imposes a lower bound through (B); selection utility imposes further threshold and effective-domain bounds; and the public theorem needs an upper bound on the same (k). The notation (k_{\mathrm{Lyu}}(d,\beta,\varepsilon,\delta)=\widetilde O(d^2\log(1/(\delta\beta))/\varepsilon)) neither fixes those inequalities nor explains any logarithmic dependence on (\alpha) or the self-referential (n_0=km). Hence (E.4) cannot be inferred from (E.5), and (H.1) cannot be treated as an already-discharged source statement.

Conditional on a repaired public rate, (0\le N\delta\le\delta K\Lambda^qR_{\mathrm{VC}}\to0) is a valid direct implication under the setting's declared schedule. Likewise, (v=d) algebraically restores the (d^5) scale. Those checks do not repair the missing teacher/list/noise bridge.

## Scope And Closure Review

There is no hidden all-time or infinite-horizon upgrade. The trace event uses a finite union; the proposed DDim argument has a finite integer potential; mechanism failures use a finite confidence budget; and the theorem is fixed-sample. The trace and scalar fixed-point rows are noncircular and scope-compatible.

The downstream certificate is nevertheless too shallow for acceptance. The DDim recurrence has the right sign only after the exact Lemma 4.3 ((2p,d)/(p,d)) hypotheses and source stage pairing are established. The selection row names a structural margin but lacks a fixed teacher count that dominates every listed defect. The privacy row says that empty/no-success behavior is handled while the prose explicitly leaves it for a future step. The PAC row names a fixed output class without stating the exact Lemma 4.2 class or producing its membership. These rows are `sketch/interface defect`s with `/proof-sketch` as their smallest route.

The relevant entry and boundary stresses reinforce that route. At source stage 1, the branch's (j=0) objects have no identity bridge. At zero DDim, nonempty common support must come from the exact Corollary 4.1 conclusion and the realizable intersection, not from nonnegativity of the potential alone. On arbitrary nonrealizable input, the first query, empty lists, stage exhaustion, and default output must already be total. For improper generalization, the selected object must enter (\widehat{\mathcal H}_{d+1}) before the sample is observed. None of these failures currently forces a new assumption or weaker theorem.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| VC trace and exact (E_{\rm good}) | Generated concentration event feeding source utility | Xor trace identity, Sauer--Shelah, relative Chernoff, hypergeometric/KL tail | Lyu Prop. 2 plus current-notation direct derivation | Exact high-error relative and low-error absolute clauses are addressed | Same indexed finite-population block convention; labels removed by xor | (B.3)--(B.8) with exponent (m\alpha/d^2) and finite union over (k\Pi_C(n_0)) | Fixed (S), (n_0\ge v), one marginally uniform block; exported event is the source threshold event | Same error vector and thresholds; no surrogate residual | Finite multiplicity only; block dependence is not accumulated | `step_002`,`step_003` precede `step_004` | `step_004` produces the event for `step_006`,`step_007`,`step_010`,`step_011` | (\mu=0) makes the bad upper tail impossible; repeated points and dependent blocks are harmless | step-local | None |
| Ceiling-aware trace fixed point | Quantitative event-budget closure | Direct scalar inequalities | Primitive parameters and outputs of trace row | Controls the exact trace multiplicity and sample definition | Current branch convention; no external translation | (B.9)--(B.13), with (Q) independent of (m,n_0) | (a,Q,m,n_0=km) give the exported failure budget and technical sample bound | Same (n_0) in trace count and learner; ceiling explicitly bounded | One scalar closure; no repeated forcing | `step_001`,`step_004` precede `step_005` | `step_005` feeds all downstream quantitative consumers | (d=v=1) is legal; (d=0) bypasses (1/v) | step-local | None |
| Exact (H_i^j,p_j) stage bridge and inclusion | Source-wrapper membership and generated list interface | Lyu Theorem 3 proof and Lemma 4.3 | arXiv:2510.00076v1, source (H_i^j), stages, and (p_j) | Inclusion is a source conclusion, but the branch object map is not established | Fails: source uses (j=1,\ldots,d+1), (p_j=2^jnd); branch uses (j=0,\ldots,d), (2^jn_0d) | Desired inclusion (H_i^{j+1}\subseteq\cap_{i'}H_{i'}^j); no valid paired-(p) instantiation is fixed | Exact definitions and threshold algebra are deferred, so the exported lists are underdetermined | A factor-two decomposition parameter remains between produced and consumed objects | Finite stages, but no legal first producer under the accepted convention | No valid source wrapper precedes consumption under the accepted convention | Intended `step_006` would produce restrictions and lists for `step_007`-`step_010` | Branch (j=0) has no source-identical stage-1 object | sketch/interface defect | /proof-sketch |
| DDim descent and actual common item | Signed structural descent, finite termination, and support | Lyu Lemma 4.3 and Corollary 4.1 | Exact ((2p,d)/(p,d)) comparison and essential-hypothesis conclusions | Could supply the claim after exact equality hypotheses and functional identity are mapped | Not established because the (p_j) pairing and list objects are mismatched | Claimed (M_{j+1}\le M_j-1), (0\le M_j\le d); source hypotheses for this recurrence are missing | Inclusion, optimal decompositions, and essential lists are not yet valid exported controls | Common item must be the same function, not only an equal trace; identity bridge is deferred | At most (d) signed drops would be finite-budgeted if the recurrence were produced | Exact inclusion and the DDim parameter bridge must precede the recurrence | Intended `step_007` would produce support for selection and empirical utility | At DDim zero, common support needs Cor. 4.1's exact boundary conclusion and nonempty realizable intersection | sketch/interface defect | /proof-sketch |
| Sparse Sample actual-list output and utility margin | Generated selected-output support and finite failure closure | Lyu Algorithm 1, Lemma 3.1, plus source stage utility | Exact union-list output or (\perp); (B\ge10\log(L/\delta_s)/\varepsilon_s) | Actual-list support matches, but nonfailure and score-margin claims need additional inequalities | List-level convention matches only after exact lists and privacy allocation are fixed | Need $B+\varepsilon_s^{-1}\log(kL/\beta_s)<$ common-item slack and AboveThreshold accuracy | List bound exists only symbolically; exact (k,B,L) lower/upper closure is absent | If nonfailure holds, output is an exact list item; (\perp) remains an uncontrolled outcome | Finite noise events; no repeated-scope issue, but multiple defect classes lack a common dominance proof | Intended `step_008` depends on `step_007` support and `step_005` calibration | Producer would feed `step_009`-`step_011`; it is not yet interface-complete | Empty lists and stage exhaustion can leave only (\perp); default behavior is not fixed | sketch/interface defect | /proof-sketch |
| All-input ((\varepsilon,\delta))-DP | Scope closure from raw replacement to public privacy | Lyu Algorithms 1--2, Lemmas 3.1--3.2, concurrent composition | List replacement and sensitivity-one source interfaces | Source tools match only after a total query/list procedure and conditional adjacency map exist | Raw-record-to-one-list convention and exact AboveThreshold accounting are deferred | Desired (F.1) plus the exact source privacy sum in (F.2) | Data-independent partition is available; total lists, queries, stage exhaustion, and default output are not fixed | Residual from one raw record to one changed list coordinate is asserted but not yet defined for every transcript | One AboveThreshold process and one Sparse Sample call would avoid stage accumulation | No legal producer precedes release until the total algorithm is specified | Intended `step_009` feeds `step_012` and the final theorem | Arbitrary nonrealizable input, empty restriction/list, no success, and first query are unresolved | sketch/interface defect | /proof-sketch |
| Exact SOA identity and empirical error | Object-target bridge for improper empirical utility | Lyu Theorem 3 proof and essential-hypothesis construction | Realizable empirical guarantee for an SOA of an irreducible subclass | Source could match, but selected-list membership and exact irreducibility level are not produced | Depends on the unresolved stage/list convention | Excess empirical error should contradict the exact irreducibility restriction path | Actual selected essential SOA and explicit irreducibility are missing raw outputs | Produced selected item has not been shown identical to the SOA object measured by source empirical error | One finite contradiction; event failures separately budgeted | Intended `step_010` follows `step_006`-`step_008` | Output would feed fixed-class generalization in `step_011` | Improper output and (d=1) are allowed; trace equality alone is insufficient | sketch/interface defect | /proof-sketch |
| Fixed improper SOA class and PAC transfer | Structural class-complexity and empirical-to-population bridge | Lyu Lemma 4.2, Theorem 3, Corollary 5.1, relative VC bound | Exact class (\widehat{\mathcal H}_{d+1}) has LDim at most (d) | Diagnostic's undefined eligible family is not the cited conclusion | Fails until every selected output is shown to be (\operatorname{SOA}_{\mathcal G}) for ((d+1))-irreducible (\mathcal G\subseteq C) | LDim/VC bound plus a relative VC inequality at empirical margin (\alpha-\gamma_{\rm emp}) | Fixedness, membership, empirical error, and exact sample threshold must all be exported | Same selected function and target would give zero object residual once membership is proved | Finite confidence union; adaptive selection controlled only by fixed-class uniformity | `step_010` must produce exact membership before `step_011` consumes it | Intended `step_011` produces PAC utility for `step_012` | Infinite (C), output outside (C), and zero empirical error are valid only for the exact fixed family | sketch/interface defect | /proof-sketch |
| New public VC-sensitive rate | Quantitative theorem closure | Trace fixed point, exact teacher calibration, DP/PAC outputs | Direct algebra plus exact Lyu current-parameter wrapper | Trace factor supports (vd^4), but teacher and mechanism envelopes are not discharged | Exact source (k) convention, confidence split, and possible (\alpha/n_0) logs are unspecified | Need simultaneous lower utility inequalities and upper (k\le K\Lambda^q d^2\log(1/(\delta\beta))/\varepsilon) | (k,B,L,p_j), allocations, and all source logs must be eliminated; this path is incomplete | Technical (n_0) is the public sample size, but unresolved teacher terms may alter the envelope | One specialization, conditional on all prior producers | Completed DP/PAC and exact teacher bounds must precede specialization | Intended `step_012` would produce the public rate for the final theorem | Small (\alpha), large (n_0), and arbitrary allowed (\delta) expose the missing list-log comparison | sketch/interface defect | /proof-sketch |
| (N\delta\to0) source-compatible schedule | Asymptotic quantitative specialization | Setting's explicit schedule and completed public rate | Primitive declared schedule | Exact implication is adequate once the rate exists | Same public parameters and fixed-sequence mode | (0\le N\delta\le\delta K\Lambda^qR_{\rm VC}\to0) | Requires only the completed public rate; no new hidden control | No object transfer; scalar domination is exact | One asymptotic implication, not a privacy-mode upgrade | Repaired rate producer precedes comparison | `step_012` and final comparison | Does not assert convergence for fixed positive (\delta) | step-local | None |
| Exact (d=0) recovery | Baseline invariance and boundary branch | Nonemptiness plus LDim zero | Direct argument | Two distinct functions would shatter a depth-one tree | Pure branch convention, no source translation | Singleton output, (N=0), data-independent (0)-DP | All raw controls are primitive and the exported learner is explicit | Output equals the unique target | No update or accumulated defect | `step_001` directly produces final branch | Final theorem consumes it directly | Stationary entry already satisfies the conclusion | step-local | None |
| (v=d) return to (d^5) | Baseline rate invariance | Proposed new-rate algebra | Direct specialization | Substitution gives (d^4(d+\log(1/\beta))) | Same rate convention | Set (v=d) in the completed rate | Feasible only after the new public rate is repaired | No residual beyond unresolved rate producer | One specialization | The completed new-rate producer must precede specialization | `step_012` produces the baseline for the final frontier | Full-cube regime shows no false power saving | step-local | None |
| Old Lyu comparison arm | Baseline comparison with common DP/PAC interface | Lyu Theorem 3 and Corollary 5.1 | arXiv:2510.00076v1 | Source gives an old (\widetilde O(d^5)) PAC arm, but exact public normalization is deferred | Confidence, accuracy, privacy, and small-(\delta) conventions are not mapped in the sketch | Need explicit source parameter substitution before taking a minimum | Only a tilde summary is supplied; common-interface feasibility is not yet shown | Separate learner is appropriate, but parameter residuals are unclassified | No accumulation; comparison only | `step_012` is named but lacks a fixed wrapper | Final frontier consumes the normalized arm | Must not be used to prove the new arm or claim fixed-(\delta) asymptotics | sketch/interface defect | /proof-sketch |
| Finite-class arm and infinite-class disabling | Baseline comparison and domain split | Direct exponential-mechanism/private-ERM derivation | Standard sensitivity-one score argument | Finite (C) supports the claimed arm; infinite (C) sets (\log^+\lvert C\rvert=+\infty) | Same replacement adjacency and PAC metric | Exponential-mechanism utility plus one-sided finite union bound | Score sensitivity, realizable zero-score target, and finite cardinality are explicit | Same hypothesis and target in empirical/population errors | Two finite failure events; no repeated scope | `step_012` can derive this arm independently | Final minimum consumes it only for finite (C) | Infinite (C) cleanly disables rather than approximates the arm | step-local | None |

## Hard-Step Localization Audit

- `step_001`, `step_002`, `step_003`, `step_004`, and `step_005`: `step-local`. Their sources, conventions, raw controls, boundary cases, and exported interfaces are already fixed at obstruction-level granularity.
- `step_006` and `step_007`: `sketch/interface defect`. The factor-two source convention, exact Lemma 4.3 pairing, list identity, and zero-DDim support source must be fixed in the roadmap.
- `step_008`: `sketch/interface defect`. Exact (k,B,L), the positive margin, every defect class, and the coupled public upper envelope must be part of the accepted interface before local proof.
- `step_009`: `sketch/interface defect`. Totalization and the fixed-transcript raw-to-list mapping must describe the fixed procedure, not be chosen for the first time by a local proof worker.
- `step_010` and `step_011`: `sketch/interface defect`. The exact irreducibility level, SOA identity, fixed Lemma 4.2 output family, and generalization wrapper must be producer-consumer interfaces.
- `step_012`: mixed. The direct (N\delta), (d=0), (v=d), and finite-class calculations are `step-local`; the new-arm teacher specialization and old-Lyu common-interface wrapper are `sketch/interface defect`s.

No reviewed obligation is presently an `idea/theorem-contract defect`. The same assumptions and conclusion plausibly permit repair by changing the roadmap and wrappers.

## Early Idea-Failure Screen

- **Contract contradiction:** No counterexample to the formalized sample-complexity target was found. The current contradiction is between the claimed source-identical procedure and the exact source stage convention, which is a repairable interface issue unless the revised sketch proves otherwise.
- **Missing mechanism:** The trace and fixed-point mechanisms exist. The downstream missing items are exact source bridges, a teacher/margin inequality package, total procedure semantics, and an exact output-family wrapper. These are not supplied by naming future proof steps.
- **Source-convention stress:** Reindexing the source's stages (1,\ldots,d+1) to (0,\ldots,d) leaves a factor two in (p_j). Lemma 4.3's own ((2p,d)/(p,d)) structure makes that difference theorem-facing.
- **Residual-to-target stress:** Actual-list support from Algorithm 1 is adequate only after nonfailure; raw-record adjacency is adequate only after a total one-list map; and Lemma 4.2 applies only after exact membership in (\widehat{\mathcal H}_{d+1}). Each residual is presently unresolved at the consumed target interface.
- **Persistent-defect stress:** No infinite accumulation obstruction appears. The proposed finite DDim potential would close if its exact source recurrence were produced. Noise and confidence defects are finite but not yet dominated by an exact teacher margin.
- **Limiting/boundary stress:** The (d=0) and (v=d) baselines pass. The (j=0) stage, DDim-zero support, empty-list/no-success input, infinite-class improper output, and small-(\alpha) list-log regime expose the sketch defects above.
- **Entry-state trace:** The zero-dimensional entry is complete. For the positive branch, the first arbitrary-input query and no-success/default path are not fixed, so all-input DP cannot yet be localized to a proof step.
- **Generated-condition check:** (E_{\rm good}) has a legal producer. Exact lists, common support, selected SOA membership, total adjacency, and the fixed improper class do not yet have source-compatible producer interfaces.
- **Dependence/mode check:** No unsupported horizon or probability-mode upgrade is claimed. The exact logarithmic dependence of (k) on (L,n_0,\alpha,\beta,\delta) is unresolved and may not be hidden by the tilde notation.
- **Baseline invariance:** The intended (d=0), (v=d), old-Lyu, finite/infinite-class, and (N\delta) conclusions are retained rather than weakened. They must be reattached to a repaired new-arm and common-interface rate proof.
- **Same-setting repair plausibility:** A new sketch can plausibly fix the convention map, parameter package, total procedure, and exact SOA family without changing primitive assumptions, algorithmic scope, metric, success criterion, or the material-partial conclusion. This rules out `IDEA_FAIL` on the present evidence.

## Review Rationale

`REVISE_SKETCH` is the smallest sound status. The global diagnostic contains useful and apparently viable theorem-level work for the conditional trace event and scalar fixed point, and no idea-level impossibility has been established. However, acceptance would send proof-step workers interfaces that are either source-incompatible or not yet defined. `/global-proof` alone cannot alter the accepted stage convention, step outputs, dependencies, total procedure, teacher calibration, or cited-wrapper conclusions. The controller should open a new `/proof-sketch` attempt that preserves the viable Blocks A--B and repairs Blocks C--H before any step proof begins.
