# Global Proof Review

## Global-Proof Review Identity

- Branch: `perspective_2/idea_4`
- Sketch attempt: 1
- Global-proof producer attempt: 1
- Global-proof review attempt: 1
- Binding setting: `perspective_2/idea_4/setting.md`
- Binding setting SHA-256: `a33149a79b6aa9bac978e69ec3b87d7eb3ccba3d5e93abc8daf897a9a122fbf4`
- Reviewed proof sketch: `perspective_2/idea_4/proof_sketch.md`
- Reviewed proof-sketch SHA-256: `cc6d2becc9c22c43494d32351fc387ad87c6bb5b8fc0667517a12d42fd9bb8e7`
- Reviewed proof-sketch review: `perspective_2/idea_4/proof_sketch_review.md`
- Reviewed proof-sketch-review SHA-256: `302cf83a2270c12b59ba079b9a4423e67062238b2f8dbfbc4ca900d0c19c2789`
- Upstream idea SHA-256: `e5b309c3a9041d18c73672d91b4914bb3d17f871e2f7dedad34e6a7b5a0d05de`
- Upstream technical-survey SHA-256: `d88d9bcc66bb9decf204074c572bf7135451d0d7ac36047218810f624a0c0f63`
- Reviewed global proof: `perspective_2/idea_4/global_proof.md`
- Reviewed global-proof SHA-256: `b618e30577a9d959a753e32587c391b7a98abfe594249655cb8682cebe63705e`
- Reviewed global-proof status: `PARTIAL_BLOCKED`
- Reviewed suggested routing: `None`
- Upstream sketch-review status: `ACCEPTED`
- Independently checked primary source: Xin Lyu, *Private Learning of Littlestone Classes, Revisited*, arXiv:2510.00076v1, official source archive; `submission/main.tex` SHA-256 `b9f6d947f66f6223889f50c8eaa0c3275155a76fbaaecf1020eb8821389d6f87`

## Global-Proof Review Status

REVISE_GLOBAL

## Smallest Retry Target

/global-proof

## Blocking Issues

1. Block K is not the required independent theorem-level diagnostic for the old-Lyu arm. It defines part of an old scalar dictionary, but then delegates the teacher witness to "the same public-witness calculation," declares the source map, inclusion, DDim recurrence, and mechanism margin to hold "verbatim," and summarizes kernel, privacy, empirical utility, marking, reconstruction, and rate closure rather than exposing their old-arm equations and hypothesis discharges. This is precisely the category-only or future-step shortcut that cannot certify a theorem-critical baseline arm as `step-local`.
2. The displayed old marked inclusion (K.4) omits intersection with the old core event even though the preceding prose says "On its own core event." Without that intersection the inclusion is false on paths where old trace, mechanism, or empirical success fails. Consequently (K.5) and the claimed four-part confidence ledger do not follow from the displayed relation as written.
3. The old-arm rows in `Scope And Closure Certificate`, `Exported Interface Feasibility`, and `Generated Output Flow` overstate what Block K establishes. They call the path valid and residual-free without an exact old source/current endpoint map, old score-margin and actual-output calculation, old all-input adaptive privacy discharge, old SOA empirical contradiction, core-qualified marked producer inclusion, explicit old multiplicity domination, and ceiling-aware old rate and small-delta audit.

## Required Repair Bundle

1. Affected artifact and section: `global_proof.md`, Block K. Smallest repair target: `/global-proof`. Preserve the current accepted sketch. Replace every "same," "verbatim," and "repeat" shortcut by a self-contained old-arm diagnostic: define all old confidence/privacy shares, `a_old,Q_old,m_old,n_old,p_old,L_old,B_old,tau_AT,old,tau_SS,old`, exhibit a noncircular feasible `bar k_old`, and state the exact old `E_good` high and near-zero clauses and fixed-point charge.
2. Affected artifact and section: `global_proof.md`, Block K source/structure/privacy chain. Smallest repair target: `/global-proof`. Preserve the current accepted sketch. Give the exact old endpoint and current/source index map, the exact `(2p,p)` half-scale pairing, the equality-case common essential item and DDim-zero support argument, the AboveThreshold selected-score inequality, the direct Sparse Sample actual-output/failure-symbol bound, and all-input one-block/list replacement privacy for every transcript and fallback path.
3. Affected artifact and section: `global_proof.md`, (K.4)-(K.6). Smallest repair target: `/global-proof`. Preserve the current accepted sketch. Put the old core event explicitly on the left of the marked pathwise inclusion, prove the old marked projection and producer-block dependence before conditioning, derive the old empirical SOA contradiction, and display the inequality by which `k_old(d+1)L_old` is dominated by the complement-tail exponent. Then eliminate all old auxiliaries with ceilings and state separately: theorem validity for arbitrary allowed `delta`, the schedule needed for any `N_old delta -> 0` source-facing baseline claim, and the fixed-versus-scheduled-`delta` minimum.
4. Affected sections: `Citation And Tool Audit`, `Quantitative Dependence Audit`, `Scope And Closure Certificate`, `Exported Interface Feasibility`, `Generated Output Flow`, `Early Obstruction And Repair Plausibility`, and `Global Gaps And Hard Steps`. Smallest repair target: `/global-proof`. Preserve the current accepted sketch. Update each old-arm entry to point to the repaired concrete equations and producer paths. Record explicitly that Lyu Theorem 3/Corollary 5.1 do not output raw-space measurability, a marked high-confidence PAC conversion, the local near-zero tail, or the current all-input quotient wrapper, so those outputs must come from the repaired current-notation diagnostic rather than from the citation.

No step claim, dependency, primitive assumption, output target, generated-output allocation, theorem scope, metric, success criterion, or conclusion needs to change on the present evidence. A new sketch attempt is therefore not the smallest repair.

## Contract And Status Audit

The reviewed file has all required top-level sections, uses the controlled status `PARTIAL_BLOCKED`, uses the aligned `Suggested Routing = None`, covers `step_001` through `step_016`, and contains the diagnostic-boundary note. The exact conditional claim is preserved.

The artifact nevertheless fails the detailed-diagnostic and Gate Evidence contracts. `PARTIAL_BLOCKED` is acceptable only when every unresolved theorem-critical obligation is already shown to have a concrete, source-compatible, raw-control-to-exported-interface path. Block K instead assigns construction of most of one complete theorem arm to future `step_015` work through analogy. Its dependent certificate and flow rows cannot make that missing evidence true. The producer status/routing tokens are individually legal, but the file is not safe for downstream step consumption until the global diagnostic is rewritten.

## Claim And Sketch Fidelity Audit

The attempted claim matches `setting.md`: it is explicitly conditional on the finite-or-countable measurable evaluation quotient; retains arbitrary realizable distributions, all-input raw replace-one approximate DP, improper output in `H_C`, exact decoded zero-one risk, and the displayed `R_VC`; and does not claim the unrestricted open problem. The `d=0`, `v=d`, finite/infinite-class, old-arm, finite-arm, and scheduled-`delta` statements are retained. The quantifier order and all four primitive assumptions are unchanged.

Blocks A-J and L follow the accepted step claims and forward dependency order. Block K also aims at the accepted `step_015` target, but its lack of detail is not a silent change of that target. The smallest correction is to diagnose the already-fixed step under its own old parameters, not to alter the sketch. The label "no exposed sketch/interface defect" is supportable only after that diagnostic is supplied; it is premature in the current file.

## Theorem-Level Structure Audit

All twelve named blocks have coherent theorem roles, and the coverage tables account for all sixteen sketch steps. Blocks A-C produce parameters, quotient objects, kernels, and the mark; D-I produce the VC trace event, source structure, actual output, privacy, empirical control, and population reconstruction; J exports the VC arm; K is intended to export the old arm; and L exports the finite arm and minimum.

The dependency graph is forward and the final minimum is conceptually correct only after each arm is complete. The current composition fails at that last premise: Block L consumes an old-arm tuple that Block K has summarized rather than diagnostically derived. The finite arm itself is not blocked, but the three-arm assembly is. Rewriting K and its dependent audit rows closes this structural defect without adding a block or changing dependencies.

## Dependency And Assumption Audit

The primitive conditions are correctly limited to finite Littlestone dimension, the measurable countable evaluation quotient, realizable iid sampling for utility, and the declared approximate-DP parameter regime. Quotient identities, kernels, events, restrictions, lists, common support, mechanism success, privacy, empirical utility, marks, PAC events, and rate tuples are treated as derived outputs rather than theorem assumptions. Privacy does not consume realizability or `E_good`.

The producer-consumer paths through the VC-sensitive arm are legal. In the old arm, `step_015` is allowed to instantiate a separate dictionary internally, and reuse of the static quotient lemmas from `step_002`-`step_004` is legal. What is missing is not a new dependency but the diagnostic derivation from those dependencies to the old generated outputs. Until repaired, the old kernel/DP/PAC/rate tuple is an asserted derived output, and the final minimum cannot consume it.

## Citation And Tool Audit

The official arXiv v1 source identity and claimed `submission/main.tex` hash were independently confirmed. The following exact active labels were checked: `prop:chernoff-sample-without-replacement`; `def: p-decomposition`; `claim: exist decomposition`; `lemma: number of leaves`; `lemma: Ldim-of-SOA`; `lemma: p-decomposition`; `def: essential hypothesis`; `coro: essential hypotheses`; `algo:private sample`; `lemma: privacy of sparse sampling`; `algo: abovethreshold`; `lemma: privacy of abovethreshold`; `thm: DP-ERM-littlestone`; and `coro: PAC learning`.

The source-to-current mappings used in Blocks D-I pass obstruction-level review. Proposition 2 has the printed `k <= N` versus subset-size `t` typo, but its factor 2 and exponent denominator 3 support only the high-mean relative branch. Lemma 4.3 has the required direction `G subset H`, compares optimal `(2p,d)` and `(p,d)` decompositions, and gives exact SOA equality in the equality case. Corollary 4.1 gives the list cap and DDim-zero support. Lemmas 3.1-3.2 have the claimed one-list replacement and sensitivity-one AboveThreshold privacy roles.

Known non-outputs are material and correctly limit citation use: the source does not prove the branch's near-zero one-sided tail; its pre-algorithm display omits the `d+1` restriction endpoint; the branch's half-scale current lists are not literal source-stage lists; Sparse Sample utility is commented out and must be derived directly from its active law; Theorem 3 prints malformed `SOA_G(h)` and its proof supports only `hat h = SOA_G`; the proof establishes high constant success and merely asserts an unproved probability-one conversion; and neither Theorem 3 nor Corollary 5.1 supplies raw quotient-kernel measurability, the marked projection, the current all-input raw-neighbor bridge, or the branch's explicit high-confidence marked reconstruction. Blocks E-I avoid these non-outputs. Block K does not yet show the corresponding independent avoidance and discharge, which is why its cited-wrapper classification cannot currently be accepted as `step-local` evidence.

The countable-product standard-Borel fact, Sauer--Shelah, the finite-population mgf comparison, the Bernoulli lower tail, and the finite exponential-mechanism calculation are used in compatible objects and modes. No separate Ghazi theorem is consumed beyond Lyu's active Lemma 4.2 restatement.

## Quantitative Dependence Audit

The VC arm exposes `d,v,alpha,beta,epsilon,delta`, uses fixed-sample unconditional PAC probability and binary risk, and restricts hidden constants to universal constants and powers of the declared logarithmic envelope. The displayed relations in A, D, I, and J expose the correct `d^2` teacher scale, `d^2(v+log(1/beta))/alpha` block scale, finite multiplicities, and `d^4(v+log(1/beta))/(epsilon alpha)` product. The remaining ceiling and logarithmic estimates are substantial but plausibly local under the fixed interfaces. The new-arm `N delta -> 0` statement is correctly conditional on its declared schedule and is not asserted for fixed positive `delta`.

The finite arm's normalization, score sensitivity, normalizer lower bound from the realizable comparator, and union bound give the stated pure-DP/PAC scale at diagnostic granularity. The `d=0`, `v=d`, finite/infinite `C`, and `log^+|C|` boundaries are coherent.

The old arm does not pass the rate gate as written. K.1-K.3 expose only part of the raw controls; the feasible `k_old` envelope is referred to by analogy, the old candidate and confidence exponent comparison is not displayed, and K.6 is concluded from scale prose. The diagnostic must show every old logarithm and ceiling is absorbed without an extra positive power of `d`, and must distinguish an ordinary valid approximate-DP bound from any source-facing `N_old delta -> 0` schedule. Those repairs remain within the accepted `step_015` claim.

## Scope And Closure Review

There is no all-time, uniform, or infinite-horizon closure. The VC trace event is one finite union; the DDim recurrence has the concrete one-unit potential drop and reserve at most `d`; AboveThreshold and Sparse Sample form one finite adaptive composition; and marked reconstruction is one finite candidate union. The quotient, output projection, adjacency, and risk bridges are exact. Entry tests at `d=0`, `d>=1`, `M_0=0`, mean zero, `k=2`, empty restrictions, fallback, and finite/infinite `C` are compatible with the claimed mechanisms.

The old-arm certificate row fails the scope-and-closure review. Its "old analogues of A-I" and "same finite stages/mark/ledger" are categories, not the required concrete control relations. In particular, the current file lacks an old core-qualified pathwise inclusion and therefore lacks a valid noncircular producer for the old PAC event. The row's `step-local` verdict is supportable after a global rewrite because the accepted sketch has already fixed the old arm, its sources, and its output interface; no changed closure mechanism or new theorem source is presently indicated.

The generated-output-flow table is forward in form, but its old-arm row is not substantively discharged. The flow `old dictionary/source wrappers -> K -> L` becomes legal only once K explicitly produces the old kernel, all-input DP, actual SOA/empirical certificate, marked PAC event, and rate tuple from its old inputs.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Feasible positive-branch tuple and `d=0` law | Activation, totality, and null baseline | Direct scalar construction and singleton consequence of `LD=0` | Primitive parameter assumptions; active Lyu list/privacy formulas | Linear teacher score can dominate the explicitly logarithmic list and noise costs | Same list-size and mechanism conventions; current scalar dictionary is explicit | (A.1)-(A.3), `k` least feasible, `n_0=km` | Primitive parameters export every VC-arm scalar and fallback before sampling | Exact tuple; no transformed target | One deterministic fixed point; ceilings are local, not accumulated | Public witness precedes least-feasible choice and sampling | A/`step_001` -> C-G,J | `d=0` already has zero risk; `d=v=1` and `k=2` activate legally | step-local | None |
| Quotient dimensions, raw neighbor map, and decoded risk | Structural and object-target bridge | Evaluation equivalence, representatives, and pushforward | Primitive measurable countable quotient | Bijection preserves VC/LD witnesses; pushforward preserves labels and risk | Exact binary class, metric, adjacency, and decoder conventions | (B.1)-(B.4) | Measurable cells, `T_N`, `bar C`, and `bar D` export the exact source and raw objects | Zero quotient-to-raw residual | Finite products and exact equalities; no forcing | Primitive quotient -> B before every source/final consumer | B/`step_002` -> C-D,I-L | Repeated fibers, improper outputs, finite/countable quotient, and `d=0` pass | step-local | None |
| Quotient/raw kernel and marked projection | Generated measurable output and analysis coordinate | Countable discrete input, standard-Borel output, finite mass split | Direct standard-Borel facts and C.1-C.3 | Countability supplies sections; finite weights preserve mass | Exact `H_C` output and raw `T_N` pullback; mark is unreleased | `A_N=K_C o T_N`; `sum_i tilde K(E x {i})=K(E)` | Total pointwise laws and internal state export both kernels | Zero output/privacy residual after projection | Finite path/mark split; repeated occurrences only alter weights | Total quotient law precedes mark and all probability use | C/`step_003`-`step_004` -> G,I,K-L | Empty/fallback path has mark 0; countably infinite `Q_C` passes | step-local | None |
| Exact VC trace event and fixed point | Generated simultaneous concentration control | Sauer--Shelah, active Lyu Proposition 2 high branch, direct hypergeometric mgf low branch | Verified v1 proposition plus current derivation | High relative and near-zero one-sided sources match their separate claim classes | One marginal uniform block is used; no conditional block independence | (D.1)-(D.6) and `4k(en_0/v)^v exp(-c m alpha/d^2)` | `v,a,Q,m,n_0` and exact error traces export `E_good` | Same empirical losses and thresholds; no trace representative | One finite trace/block union, with exponent dominating multiplicity | Trace family and block size precede event; tower removes conditioning | D/`step_005`-`step_007` -> E-F,H-I | Mean 0, `gamma/3`, repeated records, `v=1`, and `k=2` pass | step-local | None |
| Source endpoint, `(2p,p)` pairing, lists, and irreducible leaves | Cited structural wrapper | Active Definitions 4.2/4.3, Lemmas 4.1/4.3, Corollary 4.1 | Verified official v1 labels and statements | Exact DDim inequality, exact SOA equality, leaf scale, and list cap match the consumed claims | The omitted endpoint is defined locally; `p_r=p_{r+1,src}/2` is used only through uniform-in-`p` statements | (E.1)-(E.6) | `E_good`, nonempty restrictions, and fixed optimal decompositions export actual function lists | Exact function and scale identities; no surrogate residual | `d+1` finite transitions; empty privacy states are totalized separately | D -> E before structural/mechanism consumers | E/`step_008` -> F-I | First/last stage, empty restriction, `d=1`, and both irreducibility scales pass | step-local | None |
| DDim descent and score-`k` support | Finite termination and common-item invariant | Lemma 4.3 equality case and Corollary 4.1 DDim-zero item | Verified active source statements plus realizable target membership | Equality forces one actual SOA into every current essential list; zero DDim puts the target in every list | Same quotient function and occurrence-score convention | `q_r<k => M_{r+1}<=M_r-1`, `M_0<=d`; `M_r=0 => q_r=k` | Current lists, exact inclusion, integer DDim, and target membership export support | Zero common-function residual | Unit potential drop, zero additive forcing, reserve at most `d` | E precedes F; termination is derived, not assumed | F/`step_009` -> G | `M_0=0`, first drop, `M_d=0`, and `d=1` pass | step-local | None |
| Actual AboveThreshold/Sparse Sample output | Generated mechanism output | Active Algorithms 1-2 and Lemmas 3.1-3.2 plus direct noise/weight tails | Verified official v1; commented Sparse Sample utility is not used | Score-`k` and teacher margin supply an actual list member and exclude `perp` with allocated failure | Fixed `H_C`, actual lists, sensitivity-one score, source exponential weight | (G.1)-(G.3) | Score support, list cap, finite effective range, `B`, and noise scales export actual output | Literal list membership; fallback is separately charged | One finite transcript and one optional sample call | A,E,F precede mechanisms | G/`step_010` -> H-I and release | Immediate/last success, empty lists, false stage, `perp`, and `k=2` pass | step-local | None |
| All-input raw replacement privacy | Theorem-facing privacy closure | One-list Sparse Sample privacy, sensitivity-one AboveThreshold privacy, adaptive composition | Verified active v1 lemmas plus exact quotient neighbor bridge | Each source DP claim is used only after raw-to-one-list locality is established | Replace-one raw/quotient adjacency and arbitrary labels match; realizability is absent | Two finite budgets compose as in (G.4) | Total kernels, fixed partition, local lists, and every transcript export raw DP | Exact pullback; no event or utility residual | One two-component composition, not per-stage accumulation | B,C,E precede privacy; no `E_good` input | G/`step_011` -> J,L,final | Same-cell replacement, empty/fallback, different stopping stages pass | step-local | None |
| Exact SOA empirical certificate | Structural same-target empirical bridge | Essential leaf identity and irreducibility; active source proof pattern | Definition 4.3 and Theorem 3 proof, with malformed theorem text excluded | Every leaf member has low master loss; larger SOA loss forces empty SOA-labeled restriction | Same quotient function and empirical metric; no trace proxy | (H.1)-(H.3) | Actual output, leaf witness, `E_good`, and `n_0` scale export empirical error | Zero function residual | One deterministic contradiction; fallback excluded upstream | E,G precede H | H/`step_012` -> I | Empirical zero, improper output, repeated points, DDim-zero leaf pass | step-local | None |
| Marked producer holdout and quotient/raw PAC | Generated population-success closure | Block-local finite candidates, iid complement, direct Bernoulli lower tail | Current marked construction and direct probability derivation | Pathwise inclusion precedes conditioning; no independence is claimed after conditioning on the mark | Full, complement, and population risks use the same quotient function/target | (I.2)-(I.5) | `k>=2`, `(d+1)L` cap, core empirical bound, and iid complement export PAC | Only `k/(k-1)<=2`, dominated at `alpha/4`; decoded risk is exact | One finite union and four explicit confidence shares | C,E,G,H precede I; mark is not a premise for independence | I/`step_013` -> J,L,final | `k=2`, immediate/late stage, mark 0, countably infinite quotient pass | step-local | None |
| VC public rate and new-arm small-`delta` mode | Quantitative specialization | A,D,G,I outputs and direct envelope algebra | Current direct derivations; no source tilde used as proof | Explicit teacher/block products have the required power profile | Same fixed-sample DP/PAC/risk modes and displayed variables | (J.1)-(J.3) and `delta N <= delta K Lambda^q R_VC` | All auxiliaries have earlier producers; output is a complete learner tuple | Only declared logarithmic factors remain | One scalar specialization; no time accumulation | Completed VC outputs -> J | J/`step_014` -> L,final | `d=0` bypass, `v=1`, `v=d`, scheduled versus fixed `delta` pass | step-local | None |
| Independent old-Lyu common-interface arm | Baseline theorem arm required before the minimum | Active Lyu Theorem 3/Corollary 5.1 plus old current-notation wrappers | Source identity is verified, but source does not output the required kernel/mark/high-confidence wrapper | The scalar lineage matches `d^5`; the current exported kernel/DP/PAC interface is not fully discharged | Endpoint, half-scale, actual-output, raw-neighbor, and marked conventions are asserted by analogy | K.1-K.3 and K.5-K.6 are partial; K.4 omits the core event | Old raw controls are named but the old teacher, mechanism, empirical, privacy, and multiplicity paths are not displayed | Common-interface equality is claimed, not proved pathwise in K | Finite old stages are claimed; exact defect-to-budget inequalities are missing | No VC-success circularity, but the old PAC producer is incomplete | Intended K/`step_015` -> L; current old tuple is not certified | Boundary labels are listed, but `d=1`, fallback, and scheduled `delta` lack old equations | step-local after diagnostic repair; current evidence-contract failure | /global-proof |
| Finite exponential mechanism and three-arm minimum | Finite theorem arm and baseline assembly | Direct finite exponential mechanism; completed Lyu arms | Current direct calculation and quotient bridge | Sensitivity/normalizer comparison gives pure DP; realizable comparator and finite union give PAC | Same `bar C`, `H_C`, decoder, adjacency, and risk | (L.1)-(L.3) | Finite class and score gap export `R_fin`; minimum requires completed J and K | Zero finite quotient/raw residual; infinite arm is explicitly disabled | One finite selection and deterministic comparison | B,C,J,K must precede L | L/`step_016` -> final | Singleton, `d=0`, finite/infinite `C`, and `v=d` pass; minimum remains blocked by K | finite arm step-local; final minimum blocked by global K diagnostic | /global-proof |

## Hard-Step Localization Audit

- `step_001` through `step_014`: the diagnostic fixes unchanged claims, dependencies, sources, control relations, exported interfaces, and boundary cases. Their remaining ceiling, source-instantiation, measure-theoretic, and probability derivations are substantial but genuinely `step-local`.
- `step_015`: the accepted claim and dependencies already fix a plausible same-setting old-arm mechanism, so no sketch change is currently justified. The reviewed diagnostic, however, does not provide enough old-arm evidence to certify that obligation for downstream work. This is a `/global-proof` omission, not permission for the step worker to invent the missing theorem-level interfaces.
- `step_016`: the finite-law proof is `step-local`. Its final minimum is downstream-blocked until the global diagnostic actually certifies the old tuple consumed from `step_015`.

No hard obligation currently requires a changed primitive assumption, algorithm, theorem scope, metric, exposed dependence, success criterion, or conclusion. No `idea/theorem-contract defect` is visible. No current step claim or dependency must change, so `REVISE_SKETCH` would be deeper than necessary.

## Early Idea-Failure Screen

- Contract contradiction: none. The countable-quotient restriction is explicit, and the theorem does not claim the unrestricted open problem.
- Missing mechanism: none for Blocks A-J or the finite arm. The old arm has an available same-setting mechanism family, but the current diagnostic fails to instantiate it; this is document-level missing evidence, not evidence that the mechanism cannot exist.
- Source-convention stress: the official v1 endpoint omission, half-scale pairing, high-tail-only proposition role, active versus commented Sparse Sample statements, malformed theorem output, and unsupported probability-one sentence are all survivable through the current wrappers. K must independently show those wrappers rather than cite the source as if it output them.
- Residual-to-target stress: quotient/raw risk, raw/quotient adjacency, and marked/released output are exact in the VC arm. The producer-holdout residual is explicitly dominated. K has not yet re-established these facts for its own old generated law.
- Persistent-defect and accumulation stress: the VC arm has only finite multiplicities and a finite DDim reserve. No persistent defect is hidden. The old arm lists finite categories but must expose their actual budget inequalities.
- Limiting and boundary stress: `d=0`, `d=v=1`, `v=d`, mean zero, `M_0=0`, `k=2`, empty/fallback paths, finite/infinite `C`, and fixed versus scheduled `delta` reveal no current counterexample. The old schedule and core-event boundary remain undocumented, not falsified.
- Entry-state trace: the no-data state already satisfies the theorem; positive dimension activates the first source scale; initial zero DDim gives immediate support. No inactive mechanism leaves a false target conclusion.
- Generated-condition stress: all VC generated outputs have legal producers. The old PAC/rate tuple is the only producer gap in the reviewed diagnostic.
- Dependence/mode and baseline stress: the conditional probability, fixed-sample horizon, binary-risk metric, and new/finite arm dependences are preserved. The exact old baseline remains plausible but is not yet independently certified.
- Same-setting repair plausibility: a global rewrite can supply every missing old equation while preserving all accepted steps and the theorem contract. `/proof-sketch` and `/subagent-idea-generator` are therefore not justified by current evidence.

## Review Rationale

`REVISE_GLOBAL` is the smallest sound status. The quotient-first VC-sensitive architecture survives independent source, object, boundary, privacy, concentration, reconstruction, and rate stress, and the accepted sixteen-step roadmap need not change. However, a complete old-Lyu arm is theorem-facing because the final claim explicitly takes a three-arm minimum and preserves the `d^5` baseline. The present Block K is an analogy plus a scalar envelope, not an independent diagnostic, and its displayed marked inclusion is missing the event on which it is valid. A focused rewrite of Block K and every audit row that consumes it is sufficient; accepting the current `PARTIAL_BLOCKED` artifact would improperly defer theorem-level interfaces to `step_015`.
