# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Reviewed artifact: `perspective_2/idea_1/proof_sketch.md`
- Formalized target: target-spec-mode universal material-partial upper bound from `perspective_2/idea_1/setting.md`
- Reviewed roadmap: no legal step decomposition; the proposed dyadic SOA chain-or-rank-drop producer is rejected on the infinite point-function class before privacy or rate steps begin.

## Sketch Viability Score

2

## Sketch Review Status

IDEA_FAIL

## Smallest Retry Target

/subagent-idea-generator

## Retry Mode

new_idea

## Audit Summary

- Goal alignment: The attempted (Q(v,d)^5) upper bound, quantifiers, arbitrary possibly infinite class scope, approximate-DP mode, improper/unbounded learner convention, and separation of (log d) from (log^*d) agree with `setting.md` and the source direction. The branch is correctly labeled `material_partial` and states the remaining characterization gap. The sketch does not prove that goal: it instead gives an obstruction to the particular two-node certificate and learner required by the goal.
- Dependency audit: The sketch has zero steps and hence no cyclic or backward dependency. This is honest rather than a structural acceptance candidate: the first required generated output, a finite valid certificate (T_V), has no legal producer, so every putative privacy, utility, or specialization step would consume an unproduced object. A step DAG cannot be opened under the current theorem contract.
- High-risk coverage: The sketch localizes the universal certificate, termination/charge recurrence, rank-node and chain-node source wrappers, low-error coverage, stable mass, neighboring-sample support, generated membership, privacy composition, and public-rate bridge. The point-function boundary test directly breaks the first two obligations and exposes the remaining obligations as downstream interface defects.
- Explicit-rate coverage: The rate objective correctly declares (v, d, Q, alpha, beta, epsilon, delta, k), allowed hidden constants, probability mode, fixed-sample mode, risk metric, and the finite-class/generic-Littlestone baseline arms. No technical rate is produced: (q(T)) is undefined on the counterexample, and neither the raw-(d) Lyu interfaces nor the finite-order Cohen et al. interfaces provide a valid (q)-to-(Q) specialization or an explicit (Phi_{\mathrm{inh}}).
- Assumption and citation plausibility: The three setting assumptions are cited by stable ids, and certificate, support, stability, membership, and privacy facts remain classified as derived rather than theorem-facing assumptions. The cited sources have identifiable locators and quoted statement roles, but their conclusion interfaces are inadequate for the branch: Lyu's decomposition/learner does not output dyadic rank drops or a modular (q^5) wrapper, and Cohen et al.'s threshold utility requires a genuine fixed finite ordered threshold instance. No cited result supplies the missing universal certificate producer.

## Early Obstruction Audit

- Limiting-case stress: Let (X) be infinite and (C_\star=\{h_0\}\cup\{h_x:x\in X\}), where (h_0\equiv0) and (h_x(z)=\mathbf 1\{z=x\}). One point is shattered but no two points realize label (11), so (operatorname{VC}(C_\star)=1). A depth-one Littlestone tree is realized, while the label-1 child after any root query is the singleton ({h_x}), so (operatorname{LD}(C_\star)=1). For every finite (F\subset X), the allowed restriction (V_F=\{h_0\}\cup\{h_x:x\notin F\}) again has ((v,d,r)=(1,1,1)). At any (z\notin F), SOA chooses label (0) strictly, its positive child is a singleton, and its continuing negative child is (V_{F\cup\{z\}}) with unchanged VC, LD, and dyadic rank. Thus the proposed progress source is inactive on every finite continuing trace. A single-cut order cannot contain three distinct point states, because the label column for (x) has a singleton (1), forcing (h_x) to be an endpoint, and a total order has at most two endpoints. A finite chain leaves infinitely many point states; grouping two of them leaves a VC-one fiber, while separating all of them requires infinitely many fibers. Hence the required finite certificate fails on an allowed class with (Q(1,1)=4).
- Theorem-critical bridge support: The earliest bridge from `assump:finite-littlestone` to a finite certificate is false for the specified node semantics. Consequently there is no bridge to (q(T)), low-error candidates, stable support, generated membership, a finite privacy ledger, or the public theorem. These are not merely unproved local lemmas.
- Exported-interface feasibility: The raw controls (v=d=1), the SOA convention, and finite restrictions export neither a lower-rank child cover nor a finite single-cut-chain cover. Lyu's cited source exports its own raw-(d) decompositions, list bounds, and end-to-end learner, not the branch's rank-node list/support interface. Cohen et al. consumes a fixed finite ordered threshold domain rather than producing such an order for arbitrary SOA states. Certificate size, list size, chain size, node count, raw (d), and event/privacy losses remain uncontrolled, so none can be exported into (Phi_{\mathrm{inh}}) or the (Q^5) theorem.
- Theorem-critical mechanism witness gate: Failed. For certificate existence and charge closure, the named control terms are dyadic-rank decrease and VC decrease, but both are exactly zero on (V_F\to V_{F\cup\{z\}}). The persistent same-band residual has no absorption, cancellation, stopping, or finite-budget relation. The rank-node and chain-node witnesses also fail source-to-claim matching at the root. Supplying a star/tree mechanism would change the specified procedure and mechanism source, so this is not same-setting sketch repair.
- Entry-state trace stress: At the allowed entry (V_\varnothing=C_\star), the first query (z) produces a rank-zero positive singleton and the continuing state (V_{\{z\}}) with the original ((1,1,1)). The rank/VC progress mechanism is inactive before certificate termination is consumed. Trying the chain alternative at the same entry either includes at least three point states and violates the single-cut premise, or omits infinitely many point states and has no finite lower-VC routing. The theorem-facing certificate conclusion is false at this entry.
- Obligation locality classification: Universal finite-certificate production and finite charge/termination are `idea/theorem-contract defect`; universal coverage by the Cohen chain routine is also `idea/theorem-contract defect`. A modular Lyu rank-node wrapper, low-error transfer, stable mass/support, generated membership, finite composition ledger, and quantitative specialization are `sketch/interface defect` obligations only after a legal revised idea supplies the missing structural producer. They cannot be spawned as steps in the current branch because their first dependency is false.
- Noncircular closure gate: Failed. Certifying the star root would require assuming a finite cover of infinitely many point states, assuming a global single-cut order that does not exist, or treating the infinite family of singleton fibers as a new symbolic node. Each option assumes or changes the missing generated structure rather than deriving it from the current primitives.
- Mechanism-source and boundary stress: Singleton classes at VC or LD zero are valid terminal cases, but the immediately adjacent allowed case (v=d=1) already breaks the mechanism. The decisive SOA comparisons are strict, so tie breaking cannot repair the trace. Finite truncations give arbitrarily many zero-drop continuations while (Q=4), and the infinite class removes every finite termination budget. Upper bounds, irreducibility, and finite list thresholds do not supply the missing lower-rank, lower-VC, or single-cut source.
- Generated-output flow: Failed at the root. There is no producer for (T_V); therefore no producer for (q(T_V)), node lists/orders, low-error coverage, common mass, neighboring support, membership, privacy allocation, joint good event, (Phi_{\mathrm{inh}}), or the frontier bridge. All listed consumers occur strictly downstream of this missing producer.
- Source-to-claim adequacy: The direct structural source fails on (C_\star). Under the branch convention, Lyu's irreducible root is not equal or bridged to the required lower-rank restriction or finite chain; its raw-(d) list/stage bounds do not imply a (q)-parameterized hybrid theorem. Cohen et al.'s produced cut/interior point on a fixed finite ordered domain is not equal or bridged to a low-error hypothesis in the arbitrary class; the needed fixed order, threshold realization, adjacency-preserving map, and cut-to-risk transfer are absent. These are source/interface mismatches, not difficult instantiations of adequate sources.
- Residual-to-target adequacy: Failed. For the structural bridge, the residual target is the continuing (V_F) itself, with no decrease in the theorem's rank/VC control. For the Lyu bridge, raw (d), essential-list size, source stage count, and source-specific interleaving remain after any attempted transfer to (q). For the Cohen bridge, generated-order dependence, infinite order size, non-threshold label columns, membership, adjacency, and population-risk transfer remain. None is dominated at the required (Q)-only scale.
- Baseline invariance audit: The inherited unrestricted generic (widetilde O(d^5)) learner still applies to (C_\star), and the finite-cardinality arm remains a separate baseline when (C) is finite. The proposed certificate learner is undefined on the infinite baseline class, so the new arm does not preserve the target's arbitrary-class coverage and cannot be combined into the claimed minimum frontier. Merely retaining the old external arm does not establish the missing new-arm conclusion.
- Scope-accumulation compatibility: Failed. The accumulated defect is the persistent same-band/same-VC continuation, with one-sided nontermination and zero potential drop per negative update. There is no recurrence charging its number to (Q), no finite potential budget, and no permitted stopping or conditioning clause. Candidate changes, privacy calls, and failure probabilities would therefore accumulate over an unbounded or infinite number of nodes.
- Scope and dependence consistency: The sketch faithfully tests the universal possibly infinite-class scope rather than narrowing it. The failure occurs at fixed (Q=4), so class/cardinality, certificate-length, or raw-(d) dependence cannot be hidden in a universal constant, a polylogarithm of (Q), or (Phi_{\mathrm{inh}}(\alpha,\beta,\varepsilon,\delta)).
- Generated-condition provenance: The sketch correctly refuses to assume certificate validity, low-error coverage, support, stability, membership, or privacy. Their absence is blocking because the target is unconditional; moving them into admissibility would violate the theorem contract.
- Citation and tool applicability: For Lyu arXiv:2510.00076, the reviewed source interfaces are `def: p-decomposition`, `claim: exist decomposition`, `lemma: p-decomposition`, `coro: essential hypotheses`, `lemma: privacy of sparse sampling`, `thm: DP-ERM-littlestone`, and `coro: PAC learning`. The model hypotheses are compatible with `assump:finite-littlestone`, `assump:realizable-iid`, and `assump:approximate-dp-regime`, but the conclusions do not guarantee a dyadic rank drop, threshold chain, external-certificate support interface, or certificate-parameterized (q^5) theorem. For Cohen et al., DOI:10.1145/3564246.3585148 / arXiv:2211.06387, the reviewed interfaces are `algo:partition`, `theo:partition-private`, `theo:partition-privatek`, `theo:threshold-utility`, `theo:threshold-privacy`, and `theo:learning-threshold`. Their required fixed finite order, genuine threshold labels, and adjacency-preserving maps are not produced by the branch; the conclusions do not manufacture the order or an arbitrary-class cut-to-risk bridge. Even granting every quoted source statement, the required conclusions do not follow, and no uncertain citation is treated as proof authority.
- Same-setting repair plausibility: No roadmap, dependency split, conditional local lemma, or conservative loss allowed by the current target can repair a false root certificate statement. Handling (C_\star) requires adding a star/general-VC-one tree node and private routine, permitting a new infinite symbolic certificate plus a new routing mechanism, or restricting to finite classes. Those respectively change the algorithm/procedure and theorem-critical mechanism source, change certificate semantics/procedure, or change theorem scope.
- Target-preserving bridge-repair gate: Failed at idea level. The formalized goal requires the learner of the stated Lyu-rank/Cohen-chain form and a finite certificate for every reached restriction. Reorganizing later bridges cannot create (T_{C_\star}). Any repair capable of doing so changes a component of the theorem contract named above; therefore `/proof-sketch` is not a sound first retry.
- High-risk obligation classes: Structural lower/progress claims, recursive termination, generated membership and stability, finite-order threshold transfer, neighboring-dataset support, approximate-DP composition, confidence accumulation, explicit (Q^5) dependence, public quantitative specialization, and arbitrary-class baseline coverage were all tested. The structural and recursive classes fail directly; the remaining classes lack legal inputs.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Finite valid certificate (T_V) for every reached restriction | Generated structural certificate; root producer for the learner | Proposed SOA rank-drop-or-single-cut dichotomy | Novel direct derivation from `assump:finite-littlestone`, with Lyu decomposition as one proposed ingredient | Fails: (C_\star) has no finite cover by lower-rank children or a finite single-cut chain with lower-VC residual fibers | Counterexample uses the exact LD-empty, SOA tie, restriction, rank, and arbitrary-infinite-class conventions; Lyu does not add the missing dyadic-drop conclusion | Intended relation is strict rank drop or strict VC drop on every continuation; on (V_F\to V_{F\cup\{z\}}), both drops are zero | Raw (v=d=1) and all finite restrictions are known, but they do not export any finite (T_V) | The produced irreducible same-rank root is not the consumed lower-rank or finite-chain object; no transfer relation exists | Persistent one-sided same-band continuation; infinitely many unresolved point states | None; finiteness or a global order would assume the missing output | Producer absent; consumers are (q(T)), all node routines, aggregation, and the final theorem | At (C_\star), the first negative update returns the same ((1,1,1)) state type while the certificate is unfinished | idea/theorem-contract defect | /subagent-idea-generator |
| Termination and charge bound (q(T)\le c_0Q(v,d)) | Recurrence, accumulated-scope closure, and class-complexity budget | Potential based on dyadic-rank crossings and chain-to-lower-VC transitions | Proposed direct bookkeeping, conditional on the missing certificate | Fails: the proposed charge ignores arbitrarily many and infinitely many zero-drop continuations | Exact branch potential variables are used; no cited source supplies a replacement recurrence | Required one-step relation would decrease rank or VC; actual relation is (P(1,1)-P(1,1)=0) on each continuing update | (Q(1,1)=4) is fixed, but no finite path length or charge object is exported | (q(T)) is undefined because (T) is absent; zero-charge steps are not dominated by (Q) | Persistent nondecaying forcing with no sign cancellation, stopping rule, or finite budget | None; termination cannot be assumed to define the charge used to prove termination | Certificate should produce (q); rate, privacy, and confidence consumers receive nothing | Finite truncations have arbitrarily many same-band steps at fixed (Q); infinite (X) has no finite trace | idea/theorem-contract defect | /subagent-idea-generator |
| Rank-node low-error candidate list and common item | Low-error coverage and generated candidate interface | Lyu decomposition, essential-hypothesis, sparse-sampling, DP-ERM, and PAC results quoted in the sketch | Cited source arXiv:2510.00076 with stable statement labels recorded in the sketch | Fails for the consumed interface: the source supplies its own raw-(d) construction, not an external rank-node wrapper parameterized by (q) | Model convention matches arbitrary finite-LD classes, but the source object is an irreducible decomposition/list rather than the branch's lower-dyadic-rank certificate node | Source list-size/privacy conditions are not a relation producing branch low-error coverage from a certificate node | Available raw controls include source list bounds and (d)-stage structure; no branch list, neighbor-common item, or (q)-bound is exported | Raw (d), list size, source interleaving, and hybrid membership residuals are uncontrolled at the (Q)-only target scale | No accumulation relation charges repeated wrapper calls or list changes to (q) | No branch node producer; treating source end-to-end output as the node output would change objects and dependencies | Intended consumers are sparse selection, aggregation, utility, and rate specialization | At (C_\star), Lyu may expose the irreducible root, so the proposed rank-drop node never activates | sketch/interface defect downstream of the idea defect | /proof-sketch |
| Fixed finite single-cut chain and low-error chain hypothesis | Structural support, membership, threshold utility, and arbitrary-class risk transfer | Cohen et al. Reorder-Slice-Compute and threshold-learning routines | Cited DOI/arXiv source with algorithms and theorem labels recorded in the sketch | Fails: the source assumes a true fixed finite threshold order; it does not order arbitrary SOA states or map an arbitrary cut to a low-error class hypothesis | Source finite ordered-domain/threshold convention differs from the branch's generated arbitrary-class state family; no compatibility wrapper exists | Required single-cut relation fails because each (h_x) is a singleton-positive column and at most two such states can be order endpoints | No finite order, finite size (L), adjacency-preserving map, membership certificate, or cut-to-hypothesis map is available | Infinite/generated order, two-change columns, adjacency, membership, and population-risk residuals are undominated | Repeated one-point peeling has unbounded or infinite slice count; its defects are persistent and have no finite charge or absorption relation | None; the order and threshold property would be assumed before the source can consume them | No chain producer; intended consumers are RSC utility/privacy, aggregation, and final risk | At the root, including three point states breaks single-cut; omitting them leaves an infinite unresolved residual | idea/theorem-contract defect | /subagent-idea-generator |
| Stable candidate mass and neighboring-sample support | Generated stability invariant needed by sparse/frequency selection and privacy | Proposed independent-block aggregation plus parent routines | Novel hybrid bridge; neither cited source exports it for this certificate | Fails: independent blocks alone do not imply a common low-error item or overlap under one-record replacement | No source-supplied common candidate object exists under the branch convention, so compatibility cannot be established | Needed relations are a positive frequency lower bound and a neighboring-list overlap/support inequality; neither is stated or sourced | Raw blocks are available only after node outputs, which are absent; no stable-mass interface can be exported | Candidate-list changes, generated-order changes, and missing list-size control remain at full scale | Persistent across nodes and neighboring samples; no contraction or finite ledger | None; stability cannot be an admissibility premise for the selection that is supposed to prove it | Producer absent; consumers are sparse selection, frequency selection, utility, and privacy | No legal first candidate is generated at (C_\star), so support is inactive at initialization | sketch/interface defect downstream of the idea defect | /proof-sketch |
| Generated restriction, order, slice, state, and hypothesis membership | Derived membership/local-validity flow needed to make every routine well defined | Intended certificate induction | Novel derived invariant from the proposed structural producer | Fails because the producer certificate and both node interfaces are absent | Exact branch objects are identified, but there is no source object mapping for an arbitrary generated order or slice | Needed implication is producer membership plus dependency-preserving routing to every consumer; no such implication starts | Primitive restrictions exist, but no certified order, slice, candidate list, or returned-hypothesis interface is exported | All generated-object residuals are uncontrolled because no same-object producer exists | Membership defects persist through every downstream call; no closure relation | None; assuming membership would be circular | Missing root certificate should produce all later objects; every node routine and final risk argument consumes them | At (C_\star), a finite chain cannot contain or route all targets, so membership already fails at the first structural split | sketch/interface defect downstream of the idea defect | /proof-sketch |
| End-to-end ((\varepsilon,\delta))-DP and joint good event | Finite composition, adjacency, confidence, and theorem closure | Sparse selection, RSC, block aggregation, and approximate-DP composition | Cited parent privacy results plus a novel hybrid ledger | Fails: parent privacy theorems apply to their own lists/maps, not the unproduced hybrid generated procedure | Source adjacency conventions require fixed list/map hypotheses that the branch has not established | Needed finite sum/composition and union-bound relations require a bounded call/node count and valid per-call interfaces; none exists | No finite node count, privacy allocation, confidence allocation, adjacency-preserving generated maps, or failure-event budget is exported | List/order replacement, unbounded node count, and event failures are uncontrolled and cannot be hidden in (Phi_{\mathrm{inh}}) | Privacy and failure losses accumulate monotonically over an unbounded/infinite traversal | None; a finite ledger cannot assume finite certificate termination | No certificate/node producer; final privacy and PAC theorem are the consumers | The procedure has no legal first node on (C_\star), so no ledger can be initialized | sketch/interface defect downstream of the idea defect | /proof-sketch |
| Explicit (Phi_{\mathrm{inh}}), technical (q^5) bound, (q\)-to-(Q) substitution, and public frontier | Quantitative specialization and public theorem bridge | Proposed modularization of Lyu and Cohen rates plus (q(T)\le c_0Q) | Novel rate bridge using cited parent rates | Fails: no parent theorem is parameterized by this certificate charge, and the charge object is absent | Source rates concern different procedures and expose raw (d) or finite order size (L); no convention-preserving translation is supplied | Needed inequalities must bound all raw (d,L), list, node, privacy, and confidence terms by (Q) and the four allowed parameters; no such inequalities exist | Parent rate statements are raw controls, but no common technical theorem or admissible specialization is exported | Raw (d), (L), certificate size, call count, and hidden polylogarithms remain; none is dominated at the target scale | Unbounded structural traversal makes sample/privacy/confidence terms non-summable under the proposed budget | Producer path breaks at (T_V), then (q), then node outputs, then the technical theorem | Missing rate producer; public sample bound and minimum frontier consume it | At (Q=4) the new learner is undefined, so no universal-rate specialization is possible | sketch/interface defect downstream of the idea defect | /proof-sketch |
| Arbitrary-class baseline coverage and three-arm minimum | Baseline invariance and source-facing materiality | Existing generic (d^5) and finite-cardinality learners plus the proposed new arm | Literature baselines and the novel certificate learner | Fails for the new arm: retaining old external arms does not make the specified new learner exist on every class | Infinite-class convention makes the cardinality arm unavailable; the generic arm remains valid but is a different procedure | Required relation is a valid common-parameter bound for each participating arm before taking the minimum | Existing arms are available in their own regimes, but the (Q^5) arm has no exported privacy/utility bound | Missing new-arm result and unnormalized parameter factors prevent the consumed minimum statement | Not an iterative defect; it is a universal-scope and specialization failure at an allowed baseline class | Existing baselines are noncircular, but they do not produce the new arm | Generic learner covers (C_\star); proposed certificate learner does not; final frontier consumes all valid arms | Infinite (C_\star) removes the finite-cardinality arm and directly tests arbitrary-class coverage | idea/theorem-contract defect for the specified new arm | /subagent-idea-generator |

## Blocking Issues

1. **Location:** `Proof Roadmap`, `Mechanism-Source And Boundary Stress / blocker_001`, and `Blockers / 1`. **Defect:** the universal finite two-node certificate is false on the allowed infinite point-function class. **Downstream effect:** there is no legal first generated object, no step DAG, and no learner of the specified form. **Smallest repair direction:** revise the idea by changing the algorithm/procedure or certificate semantics to include a source-adequate star/general-VC-one mechanism; alternatively change theorem scope, which would no longer be the current target.
2. **Location:** `Rate Objectives`, `blocker_001`, and `Blockers / 2`. **Defect:** the charge sees only rank-band crossings and chain-to-fiber VC drops, while the star continuation has zero decrease for arbitrarily many or infinitely many updates. **Downstream effect:** termination, sample use, privacy calls, confidence loss, and (q(T)\le c_0Q) have no finite accumulated-scope control. **Smallest repair direction:** the next idea must supply a new well-founded structural control and a concrete one-step relation that charges the star continuation under the unchanged universal scope.
3. **Location:** `Mechanism-Source And Boundary Stress / blocker_002` and the rank-node exported-interface row. **Defect:** the quoted Lyu results do not export a certificate-local, neighboring-support, low-error, or (q^5) wrapper; raw-(d) and source-specific list/stage terms remain. **Downstream effect:** rank-node utility, stability, sparse selection, and the (Q)-rate bridge are unsupported. **Smallest repair direction:** after idea revision supplies a valid structure, a new sketch must state an exact current-notation wrapper, source-hypothesis discharge, defect split, list/support interface, and all raw-(d)-to-new-budget inequalities.
4. **Location:** `Mechanism-Source And Boundary Stress / blocker_003` and the chain-node exported-interface row. **Defect:** Cohen et al.'s finite threshold routine is consumed without a universal producer for a fixed finite single-cut order, adjacency-preserving maps, membership, or a cut-to-risk bridge. The star class proves universal coverage by this node type false. **Downstream effect:** no chain-node privacy or utility result applies. **Smallest repair direction:** revise the idea to use a different source-compatible mechanism on star/tree pieces; any retained threshold node must be limited to genuinely produced finite threshold pieces with a complete wrapper interface.
5. **Location:** `Assumption Provenance Objectives`, `blocker_004`, `Exported Interface Feasibility`, and `Generated Output Flow`. **Defect:** low-error coverage, stable mass, neighboring support, membership, finite composition, explicit (Phi_{\mathrm{inh}}), and the public frontier have no producers or adequate residual bounds. **Downstream effect:** even a hypothetical certificate could not yet be converted into the unconditional DP-PAC theorem. **Smallest repair direction:** after the structural idea is replaced, the next sketch must allocate separate lemma-sized producers and legal dependency paths for each generated output and an explicit technical-to-public rate bridge.

## Required Repair Bundle

1. **Linked issue:** Blocking Issues 1-2; `blocker_001`; no current Step ID exists. **Required change:** replace the rank-drop/single-cut-only certificate idea with a procedure and finite representation that handles (C_\star) and every reached restriction without a theorem-facing certificate assumption. The new mechanism must expose a source-adequate terminal/routing interface, a noncircular producer path, the first-update trace, and a concrete well-founded recurrence controlling every continuation. **Affected assumptions/contract:** `assump:finite-littlestone` and the universal arbitrary-class quantifier remain the primitive test; changing the node set, private routine, or certificate semantics is an algorithm/procedure theorem-contract change. **Smallest repair target:** `/subagent-idea-generator`.
2. **Linked issue:** Blocking Issue 3; `blocker_002`; no current Step ID exists. **Required change:** if Lyu machinery remains in the revised idea, preflight an exact current-notation modular conclusion. Record every source hypothesis, object mapping, convention match, list-size/stage term, low-error interface, neighbor-support interface, controlled/uncontrolled residual, and the inequalities connecting those raw controls to the new global budget. Do not replace (d) by a new charge syntactically. **Smallest repair target:** `/subagent-idea-generator`. A subsequent `/proof-sketch` run must supply the wrapper decomposition.
3. **Linked issue:** Blocking Issue 4; `blocker_003`; no current Step ID exists. **Required change:** retain Cohen et al. only on finite pieces for which a legal earlier producer proves the fixed total order, true single-cut property for all covered targets, finite size, object membership, adjacency preservation, and cut-to-hypothesis population-risk transfer. Supply a separate source-compatible routine for the star/tree residual rather than grouping it into a false threshold interface. **Smallest repair target:** `/subagent-idea-generator`.
4. **Linked issue:** Blocking Issue 5; `blocker_004`; no current Step ID exists. **Required change:** once a viable structural idea exists, give stable step IDs and separate producer steps for certificate validity, global budget, nodewise low-error coverage, stable mass, neighboring support, generated membership, privacy accounting, confidence accounting, technical sample complexity, explicit (Phi_{\mathrm{inh}}), and the common-parameter minimum frontier. Each exported interface must state raw controls, defect classes, residual-to-target comparison, consumers, and boundary handling. **Smallest repair target:** `/proof-sketch`; this obligation is sequenced after the revised idea supplies a legal structural producer.
5. **Target-preserving repair check:** no current sketch-level bridge can preserve the formalized goal, because the goal itself requires a finite certificate and learner of the stated two-node form on (C_\star), and that structural output does not exist. A new star/VC-one node, an infinite symbolic certificate with a new private routing routine, or a finite-class restriction changes respectively the algorithm/procedure and mechanism source, certificate semantics/procedure, or theorem scope. This is the concrete reason `REVISE_SKETCH` is insufficient.

## Review Rationale

The deepest defect precedes proof decomposition: an allowed class with (v=d=1) falsifies the universal certificate producer required by the specified learner. The same example also defeats the proposed finite charge and the universal threshold-chain fallback at their exact entry state. Later source-wrapper, generated-flow, privacy, and rate gaps are substantial, but they cannot be meaningfully repaired while the root producer is false. Because every repair that handles the counterexample changes the algorithm/procedure, certificate semantics, theorem-critical mechanism source, or universal class scope, the correct controlled route is `IDEA_FAIL` with score 2, retry target `/subagent-idea-generator`, and retry mode `new_idea`.
