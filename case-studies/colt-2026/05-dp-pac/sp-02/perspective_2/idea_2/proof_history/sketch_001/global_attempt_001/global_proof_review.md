# Global Proof Review

## Global-Proof Review Identity

- Branch: `perspective_2/idea_2`
- Sketch attempt: `1`
- Global producer attempt: `1`
- Reviewed `setting.md` SHA-256: `49c9a6758e675f6463a84f244605d0dbe06aecc05a592956cf24dfdfc0efa0e4`
- Reviewed `proof_sketch.md` SHA-256: `14e8f3e085477bf19dc0a37aaef61cb41f3aba8bfbe3df30ec62cc02cdd1177b`
- Reviewed `proof_sketch_review.md` SHA-256: `d8dacfddfc6a11a9dcaf6ad34d6599cac0b0b5f65a7a01d3f274b06b004caf7c`
- Reviewed `global_proof.md` SHA-256: `692a72cd3e4e6fdfaac65063b7515523508697cade48d8354dd25f138773426e`
- Accepted sketch-review status: `ACCEPTED`
- Reviewed global-proof status: `PARTIAL_BLOCKED`
- Reviewed global suggested routing: `None`
- Budget check: run mode `interactive`; idea index `2 <= 30`, sketch attempt `1 <= 10`, and global attempt `1 <= 100`.
- Primary-source preflight: Alon et al., arXiv:1806.00949, active source `<TEMP_ROOT>/d3-src/ADP.tex` SHA-256 `5eaeb54287aa8aa89a00ad4b05f9158b56e01450a18e635afeecc4ed4e97128f`; its ignored duplicate begins at line 876 and `<TEMP_ROOT>/d3-src/header.tex` SHA-256 `99af402478d02a1e4e4b8650102e4feb5699779a2ad9f8fd06992cd5e612d801` defines `\ignore` to erase it. Bun et al., arXiv:1504.07553, corrected December 19, 2024 source `<TEMP_ROOT>/bun-thresholds-main.tex` SHA-256 `ae85c2352f6373357d60166aa88626edcb11337d2a5f4efe5d1c83edf355649e`.

## Global-Proof Review Status

REVISE_GLOBAL

## Smallest Retry Target

/global-proof

## Blocking Issues

1. **G7 does not actually define the off-overflow shared-record coupling that G7, G8, and the closure certificates consume.** The diagnostic first declares the first `n` records of hidden pool `J` to be the external sample and then says that the ideal sample uses the "next unused" record from each selected pool. With a single ordinary notion of pool consumption, the first ideal hidden-arm occurrence is then record `n+1`, whereas the constructed input uses external record `z_1`, which is pool record `1`. Thus the asserted record-for-record equality on `R <= n` does not follow. This also conflicts with the Scope And Closure Certificate, which says that the first hidden occurrence uses the first pool record. The accepted sketch already fixes the intended interface by coupling the first `R` hidden-pool records to the first `R` external records, so this is a diagnostic-artifact defect rather than a sketch-interface or theorem-contract defect. Until G7 explicitly defines separate ideal counters that start at `1` and are not advanced by designating the external prefix, the overflow-only residual, `(UT)`, and the `k=1` pathwise baseline certificate are not established by the reviewed diagnostic.

## Required Repair Bundle

1. **Affected artifact/section:** `global_proof.md`, Block G7 and every G7-dependent statement in G8 and G9. **Smallest repair target:** `/global-proof`. Replace the ambiguous shared-pool wording by an explicit indexed construction. For pool records `Z_{j,1},Z_{j,2},...`, set external record `z_l=Z_{J,l}` for `l in [n]`; at ideal position `r`, use `Z_{I_r,C_{I_r}(r)}`, where `C_j(r)=|{s <= r:I_s=j}|`; and couple the constructed nonhidden draw at position `r` to that same record. Then the `l`-th hidden occurrence uses `z_l=Z_{J,l}` in the constructed input and `Z_{J,l}` in the ideal input, so equality holds exactly when `R <= n`. State explicitly that choosing the external prefix does not advance the ideal counters. Preserve sketch attempt `1`, all dependencies, all output targets, and the theorem contract.
2. **Affected artifact/section:** `global_proof.md` Theorem-Level Block Map, Scope And Closure Certificate, Exported Interface Feasibility, Generated Output Flow, Early Obstruction And Repair Plausibility, Global Gaps And Hard Steps, and the `k=1` baseline discussion. **Smallest repair target:** `/global-proof`. Propagate the corrected record identity through the producer-consumer and entry-state fields. In particular, remove the current unsupported certification that G7 has an overflow-only residual until the displayed first-`R` identity is present, and make G8's coupling charge and the baseline trace cite that identity. Preserve the current accepted sketch; no new sketch attempt is required.

## Contract And Status Audit

The artifact contains all required producer sections, uses the controlled status `PARTIAL_BLOCKED`, aligns it with `Suggested Routing = None`, records the accepted sketch identities, and includes the diagnostic-boundary note. The theorem-level draft is detailed rather than a restatement of sketch rows. Its status would otherwise be admissible because the remaining mathematical derivations are localized to the nine accepted steps.

The artifact is nevertheless not contract-valid for downstream consumption. G7's prose, G7's first-transition certificate, and the claimed off-overflow equality are mutually inconsistent. The Exported Interface Feasibility and Residual-To-Target Adequacy gates require the exact raw-record-to-ideal-record relation, not merely the category label "next-unused iid pools." Because the accepted sketch already contains the required first-`R` relation, the defect is repairable by rewriting the global diagnostic under the same sketch. This supports `REVISE_GLOBAL`, not `REVISE_SKETCH` or `IDEA_FAIL`.

## Claim And Sketch Fidelity Audit

The attempted theorem preserves the binding setting: the disjoint-threshold minor is primitive, the learner remains arbitrary-output and computationally unrestricted, the PAC antecedent is distribution-free and realizable, privacy is replacement ((\varepsilon_0,\delta_m))-DP, and the conclusion is the conditional fixed-sample lower bound `m >= c k log^* N`. The quantifier order, fixed `alpha_0,beta_0`, exposed `m,k,N,epsilon_0,delta_m`, universal hidden constants, and `k=1` baseline all match `setting.md` and sketch attempt `1`.

G1--G6 and G9 preserve the accepted sketch interfaces. G7 intends the accepted first-`R` grand-pool coupling but does not state it faithfully: the accepted sketch says to couple the first `R` hidden-pool records to the real input, while the global wording can reserve the first `n` records and then consume a disjoint suffix. Restoring the accepted wording and exact counters requires no changed step, dependency, assumption, output target, scope, mode, metric, or conclusion.

## Theorem-Level Structure Audit

The block order G1 -> G2 -> G3 -> G4 -> G5 -> G6 -> G7 -> G8 -> G9 is acyclic and covers every accepted sketch step. G1--G4 supply learner-wise finite-game hardness, G5 is the sole prior producer, G6 supplies exact privacy, G7 is intended to supply the ideal-mixture coupling, G8 supplies exchangeability and utility, and G9 specializes constants.

The only failed theorem-level transition is the written G7-to-G8 coupling edge. Under the literal suffix reading, G7 does not produce recordwise equality on `R <= n`, so G8 cannot charge only `p_ov`. The accepted sketch already provides the missing exact relation, and all other block dependencies remain unchanged. This is the smallest global rewrite point.

## Dependency And Assumption Audit

The three theorem-facing assumptions are correctly classified as primitive. Candidate one-arm privacy and the universal-small-risk premise are legitimate local conditional hypotheses. The empirical wrapper, homogeneous rise, moving family, algorithm-wise hardness, hard prior, exact-private hidden kernel, ideal mixture, exchangeability identity, utility bound, and rate inequalities are all assigned to earlier named producers. The prior is not assumed before minimax, and no generated invariant is promoted to a public assumption.

The G7 problem is not a missing primitive source. It is a missing exact equality inside an already-approved generated-output interface. The iid pools, arm indices, external sample, and constructed records are available, but the diagnostic must specify which indexed pool record each consumer receives. The first-`R` identity is already available under the accepted sketch, so no dependency or assumption change is needed.

## Citation And Tool Audit

- **Bun et al. secrecy of the sample:** The corrected source's `lem:secrecy-of-the-sample` states, for mechanism sample size `n`, database size `M >= 2n`, and `epsilon <= 1`, the with-replacement parameters `6 epsilon n/M` and `exp(6 epsilon n/M)(4n/M) delta`. At `M=9n` these are exactly `2epsilon/3` and `(4/9)e^(2epsilon/3)delta`, both dominated by `(epsilon,delta)` for `epsilon <= 1`. The source and branch use the same replacement-adjacency and with-replacement database convention. G1's exact empirical-risk identity is a direct current-object calculation. This interface passes.
- **Alon homogeneity and endpoint mechanism:** The active source labels and statement roles are correctly identified; the ignored duplicate is not authority. The source does not verbatim supply expected loss `1/20`, odd `M`, or all `epsilon <= 1`, so the global diagnostic appropriately treats them as direct current-notation obligations. Independently, expected total error `M/20` gives side error at most `1/10` for even `M` and at most `9/80` for odd `M >= 9`; the endpoint gaps `9/(10e)-e/10` and `71/(80e)-9e/80` are positive. When `|H|>2M+1`, alternating selected points with unused successors supplies a same-rank replacement after every selected point; the removed point has one-sided off-sample rank `j-1`. When `|H|<=2M+1`, the required homogeneous-set upper bound is immediate. The finite coloring scale is `exp(O(M log(eM)))`. G2 passes at obstruction-level granularity.
- **Alon moving-family mechanism:** With `i-1` fixed extremes on the left and `M-i` on the right, every moving sample has the moving point at ordered position `i`, any pair differs in one record, and an off-sample coordinate has rank `i-1` to the left and `i` to the right. Prefix and suffix choices cover `i=1` and `i=M`. The branch consumes the same restricted bit coordinates and the same replacement metric. G3 passes.
- **Alon product/binary mechanism:** For a representative `x` left unqueried in a depth-`T` leaf interval, its correct binary path probes only coordinates `z != x`; Hoeffding applies to the `D` iid vectors at those fixed correct-path coordinates, and a union bound handles adaptivity. Product indistinguishability is exactly `(D epsilon,D delta)`. The lower event mass `(2/3-Ddelta)e^{-D epsilon}` is at least `(1/2)e^{-D}` under `Ddelta <= 1/6` and `epsilon <= 1`; `2^T` disjoint leaves contradict unit mass when `T log 2 > D + log 2`. Combining the active Ramsey lower bound with the leaf upper bound gives `log^(M) N <= exp(O(M^2 log^2(eM)))` and hence `log^* N <= M + log^* M + O(1)`. G4 passes at diagnostic granularity.
- **Finite minimax:** After finite restriction, the full sample space and output cube are finite. Nonnegativity, row sums, and all two-sided eventwise DP inequalities form a nonempty compact convex polytope; each experiment risk is affine. Applying finite minimax to this polytope and the experiment simplex gives the displayed `min max = max min` orientation and attainment. The arbitrary-output restriction is lossless for risk on `[N]`. G5 passes.
- **Direct hidden-arm tools:** G6's transcriptwise equal-or-one-replacement privacy map is exact. The Bernstein exponent is `3(mu+12)^2/[8(mu+3)]`, minimized at `mu=6` with value `27/2`. Conditional iid-arm averaging gives `(EX)` once the ideal sample is defined independently of the hidden designation. These direct tools pass. They do not repair the missing G7 record identity automatically.

## Quantitative Dependence Audit

The diagnostic exposes the main variables `m,k,N,epsilon_0,delta_m`, one-arm variables `n,N,epsilon,delta`, and auxiliary `M,D,T,gamma,eta`. Fixed quantities, probability mode, fixed-sample scope, zero-one risk, and replacement adjacency are preserved. The constants in G2--G4 are uniform at `epsilon=1`; no hidden `1/epsilon` dependence is introduced. The product defect is explicitly `Ddelta`, the PAC failure and overflow are separate fixed defects, and the final strict inequality `5/128+e^(-27/2)<1/20` is correct.

The deterministic specialization also passes: `n <= 15m`, `n^2 log(en) <= C_Delta m^2 log(em)`, `c_delta=a_delta/C_Delta`, and the `N_0` choice absorb the additive `13`. The same inherited `delta_m` is supplied to `(HP)`. Quantitative acceptance remains blocked only because the current G7 text has not justified that overflow is the sole coupling residual.

## Scope And Closure Review

G1--G6 use finite, noncircular producer paths. G2's lower/sign source is the explicit positive endpoint gap, not generic geometry. G3 exports exact one-record laws. G4 budgets every repeated defect through the finite relations `T exp(-2Deta^2) <= 1/3`, `Ddelta <= 1/6`, and `T log 2 > D + log 2`. G5's prior is produced only after learner-wise hardness. G6's changed-record use count is at most one. These certificate rows are source-adequate and step-local.

G7 fails the Exported Interface Feasibility and Residual-To-Target Adequacy gates as written. The target is recordwise equality of the constructed and ideal learner inputs on `R <= n`. The raw objects are the external prefix and the per-arm iid pools. The required relation is that the `l`-th hidden occurrence in both inputs is the same record `Z_{J,l}`. The reviewed prose does not define this relation and can instead produce `Z_{J,n+l}` for the ideal input. Thus a non-overflow mismatch is left uncontrolled. The Scope And Closure Certificate's assertion that the first hidden occurrence uses the first pool record conflicts with the Whole-Proof Draft and cannot itself serve as proof or repair.

Once G7 is rewritten with separate ideal counters, its finite closure is noncircular: exact equality holds on `R <= n`, bounded loss charges the single overflow event, and Bernstein supplies a finite uniform budget. G8's `(EX)` is independently noncircular, but `(UT)` and the `k=1` pathwise baseline currently consume the failed G7 residual statement. The accepted sketch already contains the needed relation, so `/global-proof` is sufficient.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G1 private empirical wrapper | Probability-mode and finite-output bridge | Corrected Bun secrecy lemma; measurable restriction | arXiv:1504.07553, `lem:secrecy-of-the-sample`, corrected 2024 source | Exact with-replacement wrapper and exact empirical-risk equality | Same replacement adjacency; sample/database sizes map as `n` and `M=9n`; order/label bijection preserves loss | `tilde epsilon=2epsilon/3`; `tilde delta=(4/9)e^(2epsilon/3)delta`; `E L_D=R_n(B,e)` | Candidate DP kernel, finite coordinates, iid uniform indices export the private wrapper | Zero risk residual; privacy parameters improve | Repeated row selections are integrated in the source formula; no composition defect | Candidate kernel -> G1 wrapper -> G2--G5 | G1 produces wrapper; G2--G5 consume it | `epsilon=1`, `delta=0`, repeated indices, arbitrary outputs pass | step-local | None |
| G2 endpoint gap, odd parity, and homogeneous rise | Structural positive lower source for G3 | Expected loss, one-record DP, active Alon homogeneity/Ramsey, direct parity derivation | arXiv:1806.00949, active `def:homog`, `lem:reduction`, `lem:finiteramsey`/`lem:ramsey`, `thm:ramsey` | Side marginals plus one replacement give a positive off-sample list gap; finite homogeneity gives an adjacent rise | Branch order reversal and fixed near-balanced rank template preserve threshold realizability and ranks | `g_even=9/(10e)-e/10>0`; `g_odd=71/(80e)-9e/80>0`; subtract DP/mesh defects, then telescope | Expected error `M/20`, unused successors when `card(H)>2M+1`, mesh `gamma`, and finite coloring export `Omega(1/M)` rise | Same prediction marginals; DP and mesh residuals are below the required gap scale | Finite endpoint comparisons and finite telescoping; small-`H` branch is already bounded | G1 -> G2, with no prior or hardness conclusion assumed | G2 produces gap/homogeneous set; G3--G4 consume them | Worst `epsilon=1`, odd `M>=9`, first/last selected points, and `delta=0` pass | step-local | None |
| G3 one-sided moving family | Exact adjacency and left/right separation | Fixed-extremes construction using the G2 rise | Active Alon `lem:AtoP`/`lem:reduction` plus direct branch indexing | Off-sample ranks are exactly `i-1` left of `x` and `i` right of `x` | Same ordered sample, rank-label template, replacement metric, and output coordinates | Half the adjacent rise minus one mesh error exceeds `a_0/M` | `i-1` fixed left records, `M-i` fixed right records, and moving `x` export pairwise DP laws on `J` | Produced laws are the exact binary-search laws; no surrogate target | No accumulated defect; each pair differs in one record | G2 rise -> G3 samples/laws -> G4 | G3 produces `P_x`; G4 consumes them | Prefix `i=1`, suffix `i=M`, and diagonal coordinate exclusion pass | step-local | None |
| G4 product DP and adaptive binary leaves | Finite accumulated privacy/counting closure | Active `lem:prod`, `lem:binary`; Hoeffding and finite tree counting | arXiv:1806.00949 active block, current constants rederived | Pairwise laws plus margin give high-probability correct paths and positive reference-law leaf mass | Same finite output cube and event interface; endpoint uses `epsilon<=1` directly | `D=ceil(2eta^-2 log(6T))`; `T exp(-2Deta^2)<=1/3`; `Ddelta<=1/6`; `2^(T-1)e^-D>1` | G3 margin, `D` iid vectors, a full depth-`T` tree, and one representative per leaf export the contradiction | Events and products are on the same `P_x^D`; no transformed-object residual | Hoeffding defects union-bound over the correct path; additive DP defects grow linearly only to `1/6` | G3 family exists before G4 products and events | G4 produces finite homogeneous-set upper bound; G5 consumes hardness | First probe has positive margin; `delta=0` removes leakage; `epsilon=1` retains mass | step-local | None |
| G4 Ramsey inversion and one-arm constants | Finite scope conversion to `Omega(log^* N)` | Active Erdos-Rado theorem and G4 leaf upper bound | arXiv:1806.00949 active `thm:ramsey` and `lem:ramsey` | The same `M`-homogeneous object has source lower and binary upper bounds | Same ordered domain, color count, and homogeneous object | `log^(M)N <= exp(C M^2 log^2(eM))`, hence `log^*N <= M+log^*M+O(1)`; translate `M=9n` | Finite color count and leaf upper bound export `a_th,a_delta,N_th` | Exact cardinality comparison; finite-entry branches are absorbed into universal choices | No stochastic or all-time accumulation | Ramsey lower and G4 upper are independent prior inputs to G5 | G4 produces constants; G5/G9 consume them | Fixed `M`, large `N`, small-`N` exclusion, and `M<log^*N` contradiction regime pass | step-local | None |
| G5 hard prior `(HP)` | Generated prior and quantifier reversal | Finite minimax on compact DP-kernel polytope | Direct finite-dimensional theorem; no uncertain external wrapper | `min_K max_e r_e = max_Pi min_K E_Pi r_e`, with attainment | Input space, output cube, adjacency, DP constraints, and risk are exactly branch objects | Algorithm-wise value `>=1/20` transfers without loss | Finite row-stochastic kernels plus all eventwise DP inequalities export a prior on exactly `E_{n,N}` | Zero support, risk, privacy, or discretization residual | One finite game; no accumulation | G4 hardness -> G5 prior -> G6--G9 | G5 is sole producer; hidden-arm blocks consume it | `delta=0`, `epsilon=1`, deterministic and randomized kernels pass | step-local | None |
| G6 exact privacy of `B_Pi` | Inherited privacy interface for `(HP)` | Primitive learner DP; one-use transcript map; postprocessing | `assump:unrestricted-private-pac` and explicit construction | One external replacement changes zero or one learner-input record | Same ordered replacement adjacency and exact hidden-arm restriction | Equal inputs if unused; one replacement if used; common constant on overflow | Prior, data-independent coins, distinct real records, and simulations export exact `(epsilon_0,delta_m)` DP | Zero privacy residual; no composition or group loss | Changed-record use count is at most one across all `m` positions | G5 prior and primitive DP -> G6 certificate -> G9 | G6 produces kernel/privacy; G7--G9 consume it | `R=0`, `R=n`, `R>n`, unused change, `k=1`, `delta_m=0` pass | step-local | None |
| G7 ideal-mixture coupling and overflow | Generated same-sample bridge and sole claimed utility residual | Accepted first-`R` shared-pool interface; minor table; Bernstein | Accepted sketch `step_007` plus direct iid construction; global diagnostic misstates the interface | The intended source matches only if hidden ideal occurrence `l` and external record `z_l` are both `Z_{J,l}` | Current branch notation; no external convention issue | Required: `z_l=Z_{J,l}` and ideal record at occurrence `l` equals `Z_{J,l}`; this relation is absent from G7's "next unused" wording | Raw pools and external prefix exist, but the global diagnostic does not define separate ideal counters; exported off-overflow equality is therefore infeasible as written | Not established: a suffix reading leaves a non-overflow record mismatch in addition to overflow | Intended defect is one overflow event; written construction introduces an uncontrolled mismatch on `R<=n` | Intended G6 -> G7 -> G8 path is acyclic, but G7 has not produced the consumed equality | G7 should produce equality/`(OF)`; G8 utility and G9 consume them | First hidden occurrence should use `Z_{J,1}` in both inputs; current prose can use `Z_{J,n+1}` ideally | step-local under accepted sketch; diagnostic-artifact defect | /global-proof |
| G8 exchangeability `(EX)` | Exact arm-to-mixture risk identity | Iid experiment vector independent of uniform `J` | Direct conditional expectation from G6/G7 latent objects | Conditional ideal law does not use `J`; arm average equals mixture risk pointwise | Same arm distributions, pushforwards, zero-one risk, and hidden restriction | `(1/k) sum_j L_{Q_ej}(h o phi_j)=L_barQ(h)` | Iid vector and ideal sample export exact symmetry | Zero target residual for the ideal run | One finite arm average; no `k` multiplier | Iid vector and ideal output precede `(EX)` | G8 produces `(EX)`; utility and auxiliary statement consume it | `k=1`, deterministic `h`, and nonuniform arm risks pass | step-local | None |
| G8 utility `(UT)` and auxiliary statement | PAC-to-expectation conversion and upper side of contradiction | PAC guarantee, bounded loss, `(EX)`, corrected G7 coupling, Markov | Primitive PAC plus generated G7/G8 controls | Mechanisms match the finite-game risk only after constructed-to-ideal transfer | Same population-risk and experiment-risk objects | `E L_bar <= alpha_0+beta_0`; then add only `p_ov`; Markov factor `8` | PAC and `(EX)` are available, but the reviewed G7 diagnostic has not exported the required overflow-only transfer | Current `UT` residual is unsupported until first-`R` equality is stated | PAC failure and overflow are separately finite; current text leaves an additional unbudgeted coupling mismatch | `(EX)` precedes utility; G7 must precede constructed-risk transfer | G8 produces `(UT)`; G9 consumes it | PAC failure charge and `k=1` pass after G7 correction | step-local, blocked by upstream diagnostic defect | /global-proof |
| G9 rate specialization and contradiction | Quantitative public bridge and theorem closure | G5 hard value, G6 privacy, G8 utility, primitive delta scale | Direct arithmetic in current notation | The same `n` and `delta_m` satisfy `(HP)` under the negated public bound | Same probability, risk, adjacency, and parameter conventions | `n<=15m`; delta comparison; `m<ck log^*N` implies `n<a_th log^*N`; strict risk contradiction | All scalar controls are available; utility input remains contingent on repaired G7 | No scalar residual is missing; only upstream G7-to-G8 transfer is unresolved | Fixed arithmetic, no accumulated term | G5/G6/G8 precede G9 | G9 produces final theorem | `m=1`, `N=N_0`, `epsilon_0=1`, `delta_m=0` pass once G8 is valid | step-local, blocked by upstream diagnostic defect | /global-proof |
| `k=1` baseline invariance | Exact recovery of one-chain `Omega(log^*N)` | G5 prior, exact G6 privacy, first-`R` G7 coupling, `(EX)`, G9 arithmetic | Same active Alon mechanism and accepted branch specialization | At `k=1`, `R=m<n`, and constructed and ideal inputs must use the same first `m` pool records | Same one-chain threshold, privacy, and risk convention | Required pathwise relation: hidden occurrence `l` uses external `z_l=Z_{1,l}` and ideal `Z_{1,l}` | Sole-arm objects and constants are available; reviewed G7 wording does not currently expose the equality | Overflow is zero, but suffix mismatch remains under the literal reviewed wording | No arm accumulation; only fixed wrapper/additive losses | G5 -> G6 -> corrected G7 -> G8 -> G9 | G6--G9 produce and consume baseline interfaces | First position must be pool record `1` in both inputs; current prose is inconsistent on this trace | step-local under accepted sketch; diagnostic-artifact defect | /global-proof |

## Hard-Step Localization Audit

- `step_001`: `step-local`. The corrected secrecy statement, current size map, privacy domination, output restriction, and exact loss identity are fixed.
- `step_002`: `step-local`. The expected-loss side marginals, positive even/odd endpoint constants, unused-successor layout, finite coloring, and adjacent-rise interface are exposed without a new theorem assumption.
- `step_003`: `step-local`. Fixed extremes give exact one-record adjacency and the one-sided rank identities for all accepted endpoint branches.
- `step_004`: `step-local`. The margin, `D,T`, adaptive correct-path argument, product leakage budget, disjoint leaves, Ramsey comparison, and delta translation are fixed under the accepted sketch.
- `step_005`: `step-local`. The finite strategy sets, affine payoff, minimax orientation, attainment, and arbitrary-output equivalence are fixed.
- `step_006`: `step-local`. The one-use transcript map supplies exact privacy under unchanged parameters.
- `step_007`: the mathematical obligation remains `step-local` under the accepted sketch, but the reviewed global diagnostic has a `/global-proof` artifact defect. A future step cannot be asked to choose between prefix-sharing and suffix consumption because that exact producer-consumer identity must already be fixed by the diagnostic.
- `step_008`: `(EX)` is `step-local`; `(UT)` is `step-local` only after the global diagnostic correctly exports G7's first-`R` identity. No new sketch bridge is needed.
- `step_009`: `step-local` after the repaired G7/G8 interface. Its scalar specialization is already fixed.

No theorem-critical obligation requires a changed sketch step, dependency, assumption, mechanism source, theorem scope, mode, metric, exposed dependence, success criterion, or conclusion. No `idea/theorem-contract defect` is present.

## Early Idea-Failure Screen

- **Contract contradiction:** None. The conditional minor theorem is permitted by the setting and remains explicitly partial relative to Open Question 2.
- **Missing mechanism:** No theorem-level mechanism source is absent from the accepted sketch. The reviewed diagnostic fails to instantiate the available G7 mechanism exactly.
- **Source-convention stress:** Bun's with-replacement convention and Alon's order/label convention match after the stated translations. No properness, pure-DP, or computational restriction is imported.
- **Residual-to-target stress:** G1--G6 and `(EX)` have exact target-side relations. G7 currently fails because its prefix/suffix record residual is not exposed or controlled; the accepted first-`R` equality repairs it without changing the target.
- **Persistent-defect stress:** G4's additive DP defect is finitely capped by `Ddelta`; PAC failure and overflow are each charged once. No all-time or nondecaying defect exists. The G7 mismatch is not an admissible extra defect because the theorem budget permits only overflow there.
- **Limiting and boundary stress:** The even/odd endpoint gaps remain positive at `epsilon=1`; G3's one-sided ranks are valid; G4 remains active at `delta=0`; minimax remains nonempty; privacy handles `R=0,R=n,R>n`; and scalar constants cover `m=1,N=N_0`. The current G7 first hidden occurrence fails the written trace because it can be pool record `n+1` ideally rather than record `1` in both inputs.
- **Generated-condition and flow stress:** The prior, privacy certificate, latent vector, mixture, and scalar conditions have legal producers. The off-overflow equality is not legally produced by the current G7 wording, but its producer interface is already present in the accepted sketch.
- **Dependence and mode stress:** No hidden horizon, confidence, `epsilon`, `delta`, or class dependence is required. The theorem remains fixed-sample and high-probability PAC in its antecedent.
- **Baseline invariance:** The target `k=1` conclusion is supportable without weakening, but the current diagnostic must restore the exact first-`m` shared records before claiming pathwise no-overflow equality.
- **Same-setting repair plausibility:** A `/global-proof` rewrite with explicit pool indices and counters fully repairs the issue. `/proof-sketch` would be unnecessarily deep because the accepted sketch already states the correct first-`R` coupling. `/subagent-idea-generator` is not justified because no theorem-contract change is required.

## Review Rationale

`ACCEPTED` is unavailable because the reviewed G7 block fails a theorem-critical exported-interface and residual-to-target gate, and its own certificate contradicts its construction prose. The failure propagates to `(UT)` and the baseline trace. The source adaptations in G1--G5, exact privacy in G6, exchangeability identity, constants, minimax orientation, and theorem contract survive independent review.

The smallest sound route is `/global-proof`: rewrite the pool construction so that external record `l` and ideal hidden occurrence `l` are exactly the same pool record, then update every dependent audit row. This preserves accepted sketch attempt `1` and all theorem-facing interfaces, so `REVISE_SKETCH` and `IDEA_FAIL` would be too deep.
