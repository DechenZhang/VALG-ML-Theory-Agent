# Global Proof Review

## Global-Proof Review Identity

- Branch: `perspective_2/idea_2`
- Sketch attempt: `1`
- Global producer attempt: `2`
- Reviewed `setting.md` SHA-256: `49c9a6758e675f6463a84f244605d0dbe06aecc05a592956cf24dfdfc0efa0e4`
- Reviewed `proof_sketch.md` SHA-256: `14e8f3e085477bf19dc0a37aaef61cb41f3aba8bfbe3df30ec62cc02cdd1177b`
- Reviewed `proof_sketch_review.md` SHA-256: `d8dacfddfc6a11a9dcaf6ad34d6599cac0b0b5f65a7a01d3f274b06b004caf7c`
- Reviewed `global_proof.md` SHA-256: `bbd8a4f3a7d579085fbfcbea87beb2dce14d00f99922eb44b694e690460b29b6`
- Accepted sketch-review status: `ACCEPTED`
- Reviewed global-proof status: `PARTIAL_BLOCKED`
- Reviewed global suggested routing: `None`
- Attempt identity: the current sketch and sketch-review digests match the producer identity, and `proof_tracker.md` contains the running row for sketch attempt `1`, global attempt `2`.
- Diagnostic history: archived global attempt `1` and its `REVISE_GLOBAL` review were consulted only to identify the required G7 repair; they were not used as current proof evidence.
- Primary-source preflight: Alon et al., arXiv:1806.00949, active source `<TEMP_ROOT>/d3-src/ADP.tex` SHA-256 `5eaeb54287aa8aa89a00ad4b05f9158b56e01450a18e635afeecc4ed4e97128f`; `<TEMP_ROOT>/d3-src/header.tex` SHA-256 `99af402478d02a1e4e4b8650102e4feb5699779a2ad9f8fd06992cd5e612d801` defines `\ignore` to erase the duplicate beginning at line 876. Bun et al., arXiv:1504.07553, corrected December 19, 2024 source `<TEMP_ROOT>/bun-thresholds-main.tex` SHA-256 `ae85c2352f6373357d60166aa88626edcb11337d2a5f4efe5d1c83edf355649e`.

## Global-Proof Review Status

ACCEPTED

## Smallest Retry Target

None

## Blocking Issues

None

## Required Repair Bundle

None

## Contract And Status Audit

The reviewed artifact contains every required producer section, uses the controlled status `PARTIAL_BLOCKED`, aligns it with `Suggested Routing = None`, records the current accepted-sketch identities, and includes the diagnostic-boundary note. The whole-proof draft is dependency ordered and exposes the mathematical interface for every theorem-critical transition rather than merely repeating sketch rows. Its block map, sketch coverage, assumption audit, citation audit, quantitative audit, scope-and-closure certificate, exported-interface table, generated-output flow, obstruction screen, and gap localization are all present.

`PARTIAL_BLOCKED` is valid here: no step proof is yet accepted, but every unresolved theorem-critical obligation is classified `step-local` and has a concrete source, same-target interface, finite-scope control relation, boundary trace, and producer-consumer path under the unchanged accepted sketch. Attempt 2 also propagates the repaired G7 indexed identity through G8, G9, the certificate, exported interfaces, generated-output flow, obstruction audit, and baseline discussion. The diagnostic remains explicitly non-authoritative as proof evidence.

## Claim And Sketch Fidelity Audit

The attempted theorem matches `setting.md` and accepted sketch attempt `1`: the disjoint-threshold minor is primitive; the learner remains arbitrary-output, improper, and computationally unrestricted; the antecedent is distribution-free realizable PAC and replacement-adjacency `(epsilon_0,delta_m)`-DP; and the conclusion is the conditional fixed-sample lower bound `m >= c k log^* N`. The quantifier order, fixed `alpha_0=1/128` and `beta_0=1/32`, exposed `m,k,N,epsilon_0,delta_m`, universal hidden constants, and source-progress label are preserved.

The G7 repair is faithful to the accepted `step_007` output target. It does not change the construction or add an assumption: it makes the accepted first-`R` pool sharing explicit by setting `z_l=Z_{J,l}`, using occurrence counters `C_j(r)`, and declaring that external-prefix designation does not advance ideal counters. The theorem-facing `k=1` baseline remains the full unrestricted one-chain `Omega(log^* N)` conclusion, not a stopped, conditional, or remainder-only surrogate.

## Theorem-Level Structure Audit

The order G1 -> G2 -> G3 -> G4 -> G5 -> G6 -> G7 -> G8 -> G9 is acyclic and covers all nine accepted sketch steps. G1--G4 produce learner-wise finite-game hardness; G5 is the sole producer of a chosen attaining hard prior; G6 produces the exact-private competitor; G7 produces the realizable iid mixture, indexed off-overflow equality, and overflow transfer; G8 produces `(EX)`, `(UT)`, and the auxiliary statement; and G9 performs only deterministic admissibility and contradiction assembly. No uniqueness of the minimax optimizer is needed or consumed.

The former G7-to-G8 gap is closed before any consumer uses it. For hidden occurrence `l`, both learner inputs use `T_J(Z_{J,l})=T_J(z_l)`; every nonhidden position uses the same `T_j(Z_{j,C_j(r)})`; hence the full inputs agree on `R<=n`. Equation `(G7.4)` then exports exactly one residual, `p_ov`, to G8. At `k=1`, `C_1(r)=r` gives pathwise equality on the first `m` pool records.

## Dependency And Assumption Audit

The minor table, learner PAC/DP properties, and fixed parameter scale are correctly classified as primitive conditions. Candidate one-arm privacy and the universal-small-risk premise are legitimate local conditional hypotheses inside the finite-game contradiction. The empirical wrapper, homogeneous rise, moving family, algorithm-wise hardness constants, hard prior, exact-private hidden kernel, iid mixture, indexed coupling certificate, `(EX)`, `(UT)`, and rate inequalities are derived outputs of named earlier blocks.

No generated condition is promoted to a theorem-facing assumption. In particular, G5 produces the prior before G6 consumes it; G6 produces the iid latent vector and kernel before G7; G7 defines its ideal sample without using `J` and proves record equality before G8 consumes `(G7.4)`; and G9 checks the same inherited `delta_m` and the same `n` before applying `(HP)`.

## Citation And Tool Audit

- **Bun et al. secrecy of the sample:** The corrected 2024 `lem:secrecy-of-the-sample` applies to replacement adjacency, mechanism sample size `n`, database size `M=9n>=2n`, with-replacement sampling, and `epsilon<=1`. It gives exactly `tilde epsilon=2epsilon/3` and `tilde delta=(4/9)e^(2epsilon/3)delta`, both dominated by `(epsilon,delta)`. The wrapper input law is exactly `Q_e^n`, so the empirical-risk identity has zero residual. The source does not supply threshold hardness or `(HP)`, and the diagnostic does not claim that it does.
- **Alon et al. homogeneity and endpoint source:** The active labels `def:homog`, `lem:finiteramsey`/`lem:ramsey`, `thm:ramsey`, `lem:lbhomog`, and `lem:reduction` have the stated roles; the ignored duplicate is not used. The source does not verbatim cover expected loss `1/20`, odd `M`, or every `epsilon<=1`, so G2 correctly rederives them. Expected total error `M/20` yields side errors at most `1/10` in the even case and `9/80` for odd `M>=9`; the endpoint gaps `9/(10e)-e/10` and `71/(80e)-9e/80` are positive. Alternating selected points with unused successors supplies the required one-record off-sample comparisons when the homogeneous set is large, while the small-set branch is already bounded.
- **Alon moving-family source:** Active `lem:AtoP` and `lem:reduction` map to the same ordered replacement metric and restricted output coordinates. With `i-1` fixed records on the left and `M-i` on the right, off-sample ranks are exactly `i-1` for `z<x` and `i` for `z>x`; prefix and suffix intervals cover the endpoint cases. The G2 adjacent rise dominates the homogeneity residual at scale `a_0/M`.
- **Alon product/binary/Ramsey source:** Active `lem:prod` and `lem:binary` supply the correct finite product and disjoint-event mechanism. Along the deterministic correct path for a representative left unqueried in its leaf, Hoeffding applies to fixed off-diagonal coordinates; a union bound handles the adaptive routing. Product privacy is `(D epsilon,D delta)`, `Ddelta<=1/6` leaves event mass at least `(1/2)e^-D`, and `T log 2>D+log 2` yields the disjoint-leaf contradiction. Combining the active Ramsey lower bound with the binary upper bound gives `log^(M) N <= exp(O(M^2 log^2(eM)))` and hence `log^* N <= M+log^* M+O(1)`.
- **Finite minimax and direct tools:** The restricted DP kernels form a nonempty compact convex finite polytope, each experiment risk is affine, and finite minimax gives the displayed `min max = max min` orientation with attainment. The G6 one-use transcript map preserves exact privacy. Bernstein gives exponent `3(mu+12)^2/(8(mu+3))`, minimized at `mu=6` with value `27/2`. Conditional iid-arm averaging gives `(EX)` because the ideal formula and learner output law do not depend on `J`.

All theorem-critical sources have stable identities, current-object mappings, convention checks, hypothesis discharge paths, conclusion interfaces, and explicit non-output boundaries. The order reversal `q -> N+1-q` and label map preserve threshold orientation, zero-one risk, and ordered replacement adjacency.

## Quantitative Dependence Audit

The diagnostic exposes `m,k,N,epsilon_0,delta_m`, the one-arm variables `n,N,epsilon,delta`, and auxiliary `M,D,T,gamma,eta`. Fixed PAC constants, zero-one risk, replacement adjacency, expectation/high-probability conversions, and fixed-sample scope are explicit. Hidden constants are universal and do not depend on the learner, class, embeddings, prior, or exposed variables; using the worst endpoint `epsilon=1` prevents hidden `1/epsilon` dependence.

Every defect has a displayed source and budget: wrapper multiplicities are included in the secrecy formula; G2 uses the positive even/odd gaps; G4 separately controls Hoeffding error and `Ddelta`; G7 charges only overflow, with `p_ov<=e^(-27/2)`; and G8 has `5/128+e^(-27/2)<1/20`. G9 verifies `n<=15m`, `n^2 log(en)<=C_Delta m^2 log(em)`, uses the same `delta_m`, and absorbs the additive `13` through the explicit `N_0` choice. The `k=1` specialization removes overflow and preserves the original scale.

## Scope And Closure Review

All theorem-critical scopes are finite. G1 integrates repeated sampling in a closed-form privacy bound. G2 uses a finite coloring and telescoping of a fixed positive gap. G4 exposes the accumulated defects and the concrete controls `T exp(-2Deta^2)<=1/3`, `Ddelta<=1/6`, and `2^(T-1)e^-D>1`. G5 is one finite game. G6 has deterministic changed-record use count at most one. G7 advances exactly one occurrence counter per sampled position, advances no counter when designating the external prefix, preserves equality positionwise on `R<=n`, and charges the single event `R>n`. G8 uses one PAC failure event, one overflow event, and one finite arm average. G9 is fixed arithmetic.

The producer paths are noncircular, all source-supplied interfaces are convention compatible, and every produced object is either identical to the consumed target or linked by the explicit overflow residual in the same zero-one-risk metric. Entry and boundary traces cover `epsilon=1`, `delta=0`, odd `M`, one-sided moving intervals, the first binary probe, `R=0`, `R=n`, `R>n`, an unused changed record, PAC failure, `m=1`, `N=N_0`, and `k=1`. Every certificate obligation below is therefore `step-local` under the unchanged accepted sketch.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G1 private empirical wrapper | Probability-mode and finite-output bridge | Corrected Bun secrecy lemma; measurable restriction | arXiv:1504.07553, corrected 2024 `lem:secrecy-of-the-sample` | Exact with-replacement wrapper and exact empirical-risk identity | Same replacement adjacency; source sizes map to branch `n` and `M=9n`; order/label bijection preserves loss | `tilde epsilon=2epsilon/3`; `tilde delta=(4/9)e^(2epsilon/3)delta`; `E L_D=R_n(B,e)` | Candidate DP kernel, finite coordinates, iid uniform indices export the private wrapper | Zero risk residual; privacy parameters improve | Repeated row selections are integrated in one finite source formula; no composition term | Candidate kernel -> G1 wrapper -> G2--G5 | G1 produces wrapper; G2--G5 consume it | `epsilon=1`, `delta=0`, repeated indices, odd `M`, arbitrary outputs pass | step-local | None |
| G2 endpoint gap, parity, and homogeneous rise | Structural positive lower source for G3 | Expected loss, one-record DP, active Alon homogeneity/Ramsey, direct parity derivation | arXiv:1806.00949 active `def:homog`, `lem:reduction`, `lem:finiteramsey`/`lem:ramsey`, `thm:ramsey` | Side marginals plus one replacement give a positive off-sample list gap; finite homogeneity gives an adjacent rise | Branch order reversal and fixed near-balanced template preserve realizability and ranks | `g_even=9/(10e)-e/10>0`; `g_odd=71/(80e)-9e/80>0`; subtract DP/mesh defects and telescope | Expected error `M/20`, unused successors when the set is large, mesh `gamma`, finite coloring export `Omega(1/M)` rise | Same prediction marginals; DP and mesh residuals are below the required margin | Finite endpoint comparisons and telescoping; the small-set branch is already bounded | G1 -> G2 without any prior or hardness conclusion assumed | G2 produces gap/homogeneous set; G3--G4 consume them | Worst `epsilon=1`, odd `M>=9`, first/last selected ranks, `delta=0` pass | step-local | None |
| G3 one-record moving family | Exact adjacency and left/right separation | Fixed-extremes construction using the G2 rise | Active Alon `lem:AtoP`/`lem:reduction` plus current-notation indexing | Off-sample ranks are exactly `i-1` left of the moving point and `i` right of it | Same ordered sample, rank-label template, replacement metric, and output coordinates | Half the adjacent rise minus one mesh error exceeds `a_0/M` | `i-1` fixed left records, `M-i` fixed right records, one moving record export pairwise DP laws on the same coordinate interval | Produced laws are exactly the binary-search laws; no surrogate target | No accumulated defect; each sample pair differs in one record | G2 rise -> G3 samples/laws -> G4 | G3 produces `P_x`; G4 consumes them | Prefix `i=1`, suffix `i=M`, diagonal-coordinate exclusion pass | step-local | None |
| G4 product DP and adaptive binary leaves | Finite accumulated privacy/counting closure | Active `lem:prod`, `lem:binary`; Hoeffding and finite tree counting | arXiv:1806.00949 active binary/product block, constants rederived | Pairwise laws plus margin give correct paths and positive reference-law leaf mass | Same finite output cube and event laws; `epsilon<=1` used at the exact likelihood interface | `D=ceil(2eta^-2 log(6T))`; `T exp(-2Deta^2)<=1/3`; `Ddelta<=1/6`; `2^(T-1)e^-D>1` | G3 margin, `D` iid vectors, depth-`T` tree, one unqueried representative per leaf export the contradiction | Events and products use the same `P_x^D`; no transformed-object residual | Hoeffding failures union-bound along the correct path; additive DP defects grow only to `1/6` | G3 family exists before G4 products and events | G4 produces the homogeneous-set upper bound; G5 consumes hardness | First probe has positive margin; `delta=0` removes leakage; `epsilon=1` retains mass | step-local | None |
| G4 Ramsey inversion and one-arm constants | Finite scope conversion to `Omega(log^* N)` | Active Erdos-Rado theorem and G4 leaf upper bound | arXiv:1806.00949 active `thm:ramsey` and `lem:ramsey` | The same `M`-homogeneous object has source lower and binary upper bounds | Same ordered domain, color count, and homogeneous object | `log^(M)N<=exp(C M^2 log^2(eM))`, hence `log^*N<=M+log^*M+O(1)`; translate `M=9n` | Finite color denominator and leaf upper bound export universal `a_th,a_delta,N_th` | Exact cardinality comparison; finite-entry cases are absorbed into universal thresholds | No stochastic or all-time accumulation | Ramsey lower and G4 upper are independent inputs before G5 | G4 produces constants; G5 and G9 consume them | Fixed `M`, large `N`, allowed small-`N` exclusion pass | step-local | None |
| G5 hard prior `(HP)` | Generated prior and quantifier reversal | Finite minimax on compact DP-kernel polytope | Direct finite-dimensional theorem | `min_K max_e r_e=max_Pi min_K E_Pi r_e`, with attainment | Input space, output cube, adjacency, DP constraints, and risk are exactly branch objects | Algorithm-wise value at least `1/20` transfers without loss | Finite stochastic kernels and all eventwise DP inequalities export a prior on exactly `E_{n,N}` | Zero support, risk, privacy, or discretization residual | One finite game; no accumulation | G4 hardness -> G5 prior -> G6--G9 | G5 is the sole producer of the selected prior; hidden-arm blocks consume it | `delta=0`, `epsilon=1`, deterministic and randomized kernels pass | step-local | None |
| G6 exact privacy of `B_Pi` | Inherited privacy interface for `(HP)` | Primitive learner DP; one-use transcript map; postprocessing | `assump:unrestricted-private-pac` and explicit construction | One external replacement changes zero or one learner-input record | Same ordered replacement adjacency and exact hidden-arm restriction | Equal inputs if unused; one replacement if used; common constant on overflow | Prior, input-independent coins, distinct real records, and simulations export exact `(epsilon_0,delta_m)` DP | Zero privacy residual; no composition or group loss | Changed-record use count is deterministically at most one across all `m` positions | G5 prior and primitive DP -> G6 certificate -> G9 | G6 produces kernel/privacy; G7--G9 consume it | `R=0`, `R=n`, `R>n`, unused change, `k=1`, `delta_m=0` pass | step-local | None |
| G7 indexed ideal-mixture coupling and overflow | Generated same-sample bridge, realizability, and sole utility residual | Indexed iid pools, occurrence counters, minor table, Bernstein | Accepted `step_007` interface and direct finite coupling | Constructed and ideal records agree at every position on `R<=n`; mixture is pointwise realizable | Current branch objects and zero-one-risk metric; no external convention translation | `z_l=Z_{J,l}`; ideal position `r` uses `Z_{I_r,C_{I_r}(r)}`; external designation advances no counter; nonhidden draws use the same index | Pools, counters, uniform arm indices, one-use external records, and disjoint embeddings export iid mixture plus exact first-`R` equality | Hidden occurrence `l` uses `T_J(Z_{J,l})` in both inputs; nonhidden records also coincide; `(G7.4)` leaves residual only `p_ov` | Equality is preserved over finite `m` positions; the single event `R>n` is charged once and bounded by `e^(-27/2)` | G6 vector/kernel -> G7 indexed coupling -> G8/G9 | G7 produces mixture, `(G7.2c)`, `(OF)`, `(G7.4)`; G8, G9, baseline consume them | `R=0`, first hidden occurrence, `R=n`, `R>n`, `mu=6`, `k=1` all pass | step-local | None |
| G8 exchangeability `(EX)` | Exact arm-to-mixture risk identity | Iid experiment vector and `J`-independent ideal construction | Direct conditional expectation from G6--G7 objects | Conditional ideal output law does not use `J`; the finite arm average equals mixture risk pointwise | Same arm distributions, pushforwards, zero-one risk, and hidden restriction | `(1/k) sum_j L_{Q_ej}(h o phi_j)=L_barQ(h)` | Iid vector, indexed pools, ideal sample, and independent uniform `J` export exact symmetry | Zero target residual for the ideal run | One finite arm average; no multiplier or accumulated defect | Vector, pools, counters, and ideal output are produced before `(EX)` | G8 produces `(EX)`; utility and auxiliary statement consume it | `k=1`, deterministic output, highly nonuniform arm risks pass | step-local | None |
| G8 utility `(UT)` and auxiliary statement | PAC-to-expectation conversion and upper side of contradiction | PAC guarantee, bounded loss, `(EX)`, indexed G7 transfer, Markov | Primitive PAC plus derived G7--G8 controls | Ideal mixture utility controls the same constructed finite-game risk through `(G7.4)` | Same population-risk and experiment-risk objects; no surrogate loss | `E L_bar<=alpha_0+beta_0`; add only `p_ov`; `5/128+e^(-27/2)<1/20`; Markov factor `8` | PAC event, `(EX)`, exact off-overflow equality, and overflow bound export `(UT)` and the `7/8` certificate | PAC failure and overflow are the only residuals, each with its own source | One PAC event and one overflow event; no persistent or per-arm accumulation | G7 identity/transfer and `(EX)` precede `(UT)` | G8 produces `(UT)` and auxiliary output; G9 consumes `(UT)` | PAC failure, `R=n`, zero overflow, `k=1`, deterministic output pass | step-local | None |
| G9 rate specialization and contradiction | Quantitative public bridge and theorem closure | G5 hard value, G6 privacy, G7 transfer, G8 utility, primitive delta scale | Direct arithmetic in current notation | The same `n`, `delta_m`, and constructed kernel satisfy both `(HP)` and `(UT)` | Same probability, risk, adjacency, and parameter conventions | `n<=15m`; delta comparison; `m<ck log^*N` implies `n<a_th log^*N`; strict risk contradiction | Scalar controls, exact privacy, and overflow-only utility transfer export the public constants and lower bound | No scalar residual; all upstream residuals are already budgeted in `(UT)` | Fixed arithmetic; no accumulated term | G5--G8 precede G9 | G9 produces the final theorem | `m=1`, `N=N_0`, `epsilon_0=1`, `delta_m=0` pass | step-local | None |
| `k=1` baseline invariance | Exact recovery of the unrestricted one-chain `Omega(log^* N)` result | G5 prior, exact G6 privacy, indexed G7 coupling, `(EX)`, G9 arithmetic | Same active Alon mechanism and exact branch specialization | At `k=1`, `R=m<n`, and both inputs use the same first `m` pool records | Same one-chain threshold, privacy, risk, and learner interface | `J=1`; `C_1(r)=r`; `z_r=Z_{1,r}`; `bar S_r=T_1(Z_{1,r})=T_1(z_r)` | Sole-arm objects, prior, and constants export the full baseline lower bound | Overflow is zero and no record residual remains; only fixed universal wrapper/additive losses remain | No arm accumulation; every position is pathwise coupled | G5 -> G6 -> G7 -> G8 -> G9 | G6--G9 produce and consume the baseline interfaces | First position and every later position use the same indexed record; exact/no-overflow boundary passes | step-local | None |

## Hard-Step Localization Audit

- `step_001`: `step-local`. The corrected secrecy statement, exact size map, privacy domination, finite restriction, convention map, and zero-residual risk identity are fixed.
- `step_002`: `step-local`. The expected-loss side marginals, positive even/odd endpoint sources, sparse successor layout, finite coloring, Ramsey interface, and adjacent-rise scale are exposed under current assumptions.
- `step_003`: `step-local`. Fixed extremes give exact one-record adjacency and the required rank identities, including one-sided endpoint intervals.
- `step_004`: `step-local`. The margin, `D,T`, correct-path concentration, product leakage, disjoint leaves, Ramsey comparison, iterated-log extraction, and delta translation are fixed and finite-scope compatible.
- `step_005`: `step-local`. Compactness, convexity, affine risk, minimax orientation, attainment, and arbitrary-output restriction are fixed; only existence of an attaining prior is needed.
- `step_006`: `step-local`. The one-use transcript map gives exact inherited privacy across every occupancy branch.
- `step_007`: `step-local`. Attempt 2 now fixes the raw indexed pools, counters, no-counter-advance convention, same-index nonhidden coupling, hidden first-`R` identity, iid law, realizability, transfer inequality, and overflow budget before local proof begins.
- `step_008`: `step-local`. The `J`-independent ideal law, exact `(EX)`, bounded-loss conversion, indexed transfer to `(UT)`, and Markov auxiliary interface are all produced by earlier blocks.
- `step_009`: `step-local`. The delta/sample comparisons, strict contradiction, and exact `k=1` trace are deterministic consequences of accepted outputs.

No unresolved obligation requires a changed sketch step, dependency, primitive assumption, generated-output flow, mechanism source, convention bridge, theorem scope, mode, metric, exposed dependence, success criterion, or conclusion.

## Early Idea-Failure Screen

- **Contract contradiction:** None. The explicitly conditional minor theorem is permitted by the formalized setting and does not claim to close the parent open problem.
- **Missing mechanism:** None. Every positive, lower, privacy, coupling, symmetry, and quantitative closure claim has a concrete source under the current theorem contract.
- **Source-convention stress:** The Bun sampling convention and Alon order/label convention match after the stated mappings; no properness, pure-DP, or computational restriction is imported.
- **Residual-to-target stress:** G1 and G5 have zero residual; G2--G4 use the same restricted coordinates; G6 preserves exact privacy; G7 proves recordwise equality on `R<=n`; `(EX)` compares the exact arm and mixture risks; G9 uses the same `delta_m` and kernel.
- **Persistent-defect stress:** Every repeated defect is finite and explicitly budgeted. `Ddelta` is capped, PAC failure and overflow are charged once, and no all-time or nondecaying term is present.
- **Limiting and boundary stress:** The endpoint gaps remain positive at `epsilon=1`; odd parity and one-sided intervals are covered; the binary mechanism remains active at `delta=0`; privacy covers `R=0,R=n,R>n`; the first hidden occurrence uses `Z_{J,1}=z_1`; and scalar boundaries `m=1,N=N_0` are explicit.
- **Entry-state trace:** At `k=1`, `J=1`, `C_1(r)=r`, `R=m<n`, and every constructed and ideal position is the same record. No allowed entry leaves a required mechanism inactive while the target conclusion is false.
- **Generated-condition and flow stress:** Every generated output has an earlier legal producer and dependency path; no closure, specialization, or assembly label exports an unsupported object.
- **Dependence and mode stress:** The theorem remains fixed-sample with a high-probability PAC antecedent; only the contradiction is expressed in expected risk. All exposed dependence is retained.
- **Baseline invariance:** The full one-chain scale is preserved, with exact privacy, zero overflow, and pathwise sample equality, rather than a weaker conditional or remainder statement.
- **Same-setting repair plausibility:** No repair is currently indicated. The attempt-1 diagnostic defect was repaired entirely within `/global-proof`; no sketch or theorem-contract change is needed.

## Review Rationale

`ACCEPTED` is the smallest sound status. The reviewed diagnostic is contract-complete, faithful to the accepted sketch and formalized goal, and independently viable at theorem level. Its cited sources and direct tools support the stated local interfaces, every finite accumulation has a concrete budget, every generated output has an acyclic producer-consumer path, and every unresolved hard obligation is genuinely `step-local`.

Most importantly, attempt 2 closes the sole prior blocker at the raw-record level: external record `l` and ideal hidden occurrence `l` are both `Z_{J,l}`, external designation does not advance the ideal counters, nonhidden positions use the same indexed draws, and overflow is therefore the only transfer residual. That identity is propagated through `(G7.4)`, `(EX)`, `(UT)`, the final contradiction, and the exact `k=1` baseline. The artifact is safe to consume as diagnostic context for the nine proof steps, while remaining non-authoritative as proof evidence.
