# Structural Proof Review

## Reviewer Verdict

PASS

## Blocking Issues

None

## Nonblocking Concerns

1. The accepted `step_006/proof.md` unit_001 quantifies an arbitrary sequence of binary classes and then uses `X_\kappa` without separately spelling out the accompanying sequence of domains. The public theorem and appendix bind `\{X_\kappa\}` before `\{C_\kappa\}` and require `C_\kappa\subseteq\{0,1\}^{X_\kappa}` before any use of `X_\kappa`. This is a notation and scope clarification of the setting's existing class-on-domain quantifier, not a changed step claim or theorem scope.
2. No successful dynamic compilation trace is available because the known Tectonic failure occurs before it reads the TeX sources. The independent source checks reported below found balanced environments, complete support-file wiring, unique labels, resolved internal references, and BibTeX coverage for every citation.

## Suggested Repair Depth

None

## Smallest Candidate Repair Target

None

## Structural Audit

- Goal alignment: PASS. This is exact-goal mode. The theorem in `latex_template/3_main.tex` preserves the sole nonstandard primitive hypothesis `assump:polynomial-global-stability`, the fixed values `alpha_0=beta_0=1/8`, arbitrary fixed `epsilon_0 in (0,1)`, the universal sequence of domains and nonempty finite classes, size divergence, the exact `(SP)` quantifier order, arbitrary-output and computationally unrestricted distribution-free realizable PAC learning, symmetric ordered replacement privacy, the exact `delta_kappa` and `N_kappa`, every-fixed-`rho` privacy negligibility, privacy relaxation only to a larger additive allowance, the exclusion of an eventual linear unrestricted lower bound, and the conditional progress label. It neither strengthens assumptions nor narrows a boundary regime.
- Dependency closure: PASS. The current proofs for `step_001` through `step_006` are paired with accepted reviews whose recorded sketch attempt, step ID, unit attempt, proof path, and SHA-256 match the current artifacts. The accepted dependency graph is acyclic: Step 1 produces the source learner, Step 2 specializes and pads it, Step 3 identifies the schedule, Step 4 proves the sample asymptotics, Step 5 proves privacy negligibility, and Step 6 combines the earlier outputs. The repaired Step 5 retains Step 4 as an available accepted dependency but proves `(PN)` without consuming `(AS)` or any other Step 4 conclusion; using fewer derived inputs does not alter its accepted claim, and Step 6 consumes `(AS)` and `(PN)` through separate producer paths.
- Sketch-step coverage: PASS. Every required accepted sketch step has a visible appendix subsection and is used in the main-theorem proof. Step 1 maps to stable selection under binary recoding; Step 2 to explicit sample domination and exact padding; Step 3 to the exact privacy schedule; Step 4 to subpower consequences of superpolynomial separation; Step 5 to uniform privacy negligibility; and Step 6 to sequence assembly and the lower-bound contradiction. The appendix contains full translations of the accepted non-atomic units in counts `4 + 2 + 3 + 4 + 5 + 4 = 22`, followed by a separate proof of the main theorem.
- Assembly discipline: PASS. The public results preserve the accepted step assumptions, dependency meaning, objects, probability modes, privacy direction, and conclusions. Assembly-only material fixes notation, states theorem-style interfaces, combines accepted conclusions, and proves direct implications such as ceiling expansion, privacy relaxation, and the final `o(L)` versus `Omega(L)` contradiction. The Step 6 domain binder noted above makes an implicit source binder explicit without adding a domain restriction or changing quantifier order. No independent unreviewed lemma, convergence-mode upgrade, assumption discharge, stronger claim, or new external result is introduced.
- Global-diagnostic reconciliation: PASS. The accepted review pairs with `global_proof.md` SHA-256 `29fe55b5c731f40c454af7ca83b67a9effb5a84705964c7d6fe5c2b04e851105`. Its six step-local hard obligations are all discharged by the current accepted step artifacts. In particular, the high-risk exact-`(SP)` quantifier conversion is fully proved in Step 4, and the ceiling-aware uniform factorization over bounded, unbounded, and oscillatory `q_kappa` is fully proved in repaired Step 5. No global gap is bypassed by assembly.
- LaTeX bundle structural synchronization: PASS. `main.tex`, `arxiv.sty`, `ims.bst`, and `reference.bib` exist; `main.tex` inputs all five content files and selects the declared bibliography style and database. The content files expose the required sections in order: Theoretical Setup, Preliminaries, Main Theorem, Proof Sketch, and the appendix Proof Details with six mathematical step subsections and a final Proof of the Main Theorem subsection. The public sources contain no branch-artifact dependency, workflow/audit prose, placeholder text, mechanical debris, or public `claim`, `hypothesis`, or `invariant` authority.
- Main theorem self-containedness: PASS. All theorem notation is defined in the setup and preliminaries or inline before use. The theorem states the primitive assumption basis, class and domain quantifiers, parameter ranges, exact learner interface, exact rate formulas, allowed constant dependence, PAC probability mode, privacy mode, fixed-sample and sequence-asymptotic modes, privacy comparison direction, lower-bound conclusion, and conditional status. Although detailed, it is one coherent reader-facing interface rather than an admissibility or helper-constant dictionary, so no separate simplified corollary is structurally required.
- Theorem-style statement shape: PASS. Public lemmas, propositions, and theorems state their assumption basis or named prior results, local conditions, and conclusions. Mathematical dependencies use numbered assumptions and public lemma, proposition, or theorem references, not appendix subsection names, proof-step IDs, local unit IDs, or reviewer decisions. Step-local claims and target assemblies are translated into allowed theorem-style statements and substantive proof text.
- Explicit-rate and bridge structure: PASS. The public theorem states the exact rate rather than a simplified proxy, so no separate Rate Specialization Bridge is needed. The appendix displays the full BLM substitution, all five sample-bound term dominations, fixed-constant provenance, ceiling controls, exact privacy logarithm, every-exponent allocations, product and sum absorptions, fixed-`rho` exponential-polynomial bound, `rho=3` allowance specialization, probability/privacy modes, and the final comparison. No exposed cardinality, confidence, privacy, or fixed-parameter dependence is silently dropped or hidden.
- Derivation-over-notation: PASS. The preliminaries contain only stable setting-derived notation used to state and read the theorem. Proof-local quantities such as `B_0`, `D_a`, exponent-allocation constants, `H`, `x_kappa`, factor functions, and tail witnesses remain inside the relevant appendix proofs. Displayed derivations, rather than helper dictionaries, verify boundedness, constants, term absorption, ceiling effects, and limiting claims.
- Appendix local-unit coverage: PASS. Direct source-to-appendix comparison checked every accepted non-atomic unit and target-step assembly. The appendix preserves recoding and source-admissibility checks; both Step 2 units and equations corresponding to source (16)--(34); all three Step 3 units and source (35)--(56); all four Step 4 units and source (57)--(93); the repaired five-unit Step 5 argument, including pointwise ceiling control and the independent `(PN)` route; and all four Step 6 units, including two-sided privacy relaxation and the identical-learner contradiction. Boundary cases `q=1`, `L=0`, `d=0`, `ell=0`, unused-record replacement, bounded/constant/oscillatory/unbounded `q_kappa`, finite prefixes, and noninteger fixed `rho>0` remain visible.
- Internal label/ref structure: PASS. An independent scan over all public TeX found 166 labels with no duplicates, 198 internal references with no missing targets, balanced begin/end environments, no malformed bare `ref` or `cite` fragments, and no forbidden public proof-authority environments. The sole citation key `bun2021equivalence` is present in `reference.bib`, and there are no unused bibliography entries. The stable assumption-ID sets in `setting.md` and `1_intro.tex` both equal `assump:polynomial-global-stability`.

## Reviewed Artifact Identity

- Assembly attempt: sketch attempt 1, assembly attempt 5.
- `assembly_report.md` SHA-256: `7f5933460cac08505ead073f2f7d38d4d061271c7bba1431b9567981107af6b3`.
- `latex_template/5_appendix.tex` SHA-256: `9c3b3148057f6634920f8699d470fbd35949618c1745f44ffc7074a63292b4f9`.
- Accepted step proof SHA-256 values, in Step 1 through Step 6 order: `7004c0871be76af5f501ac7c0774f1b7aaf859c7c79fa0382f96e10e705ba782`, `e8d3f9f1387486e5530a08ac208bf2351296402b3aa057618b6a9aedbffccda6`, `961ca80cea1a6f1fd8851a7d10d1e3b1e83c682484d4469850fb5e4c1c26c63d`, `ddf6b5106fb93a642f0c6cebcef1834d8fec4e911411448c5f42431d0b49bc45`, `9529468202e72f43873b2cd2e66289127bab027ae12a62ae0a9d7c65870c8b63`, and `905333c2504633c99317786edbd5d44a6c19decab6cf0bfda79147423afc1eee`.
- Accepted step review SHA-256 values, in Step 1 through Step 6 order: `23ea8071d8ef32deaa5ca90f353081c3f756ac726e27a3fc4a8c7fe25628476c`, `c63fe051b9cefb5d95e6fea15205e1c1f6a5b1f7feea2e44122699c3476d542f`, `12003a92f06a646c2c62fcbae378258d07bab2d3ebec103dc4fe0ff1d5d222a6`, `041c5ea10c61035a597a0c12d5fd0a4f1c380c2e1c708cd4e8607c3bf2a58028`, `0243dce779785be72f5a55f001a33a1173ff19eaffb80f211e7fdf261fcfeea3`, and `e4930f5c85092a58b3fed6b5e5d36a8fdb9ea605b1852f9c768f51cb5cedef64`.
