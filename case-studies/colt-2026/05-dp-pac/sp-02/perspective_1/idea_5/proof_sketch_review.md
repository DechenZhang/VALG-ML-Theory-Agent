# Proof Sketch Review

## Reviewed Sketch Identity

- Sketch attempt: 1
- Reviewed branch: `perspective_1/idea_5`
- Goal mode: exact-goal mode for the conditional theorem in `setting.md`
- Roadmap reviewed: Bun--Livni--Moran stable selection, quantitative specialization `(SC)`, exact privacy logarithm `(LD)`, Item 2 asymptotics `(AS)`, privacy negligibility `(PN)`, and final conditional nonexistence assembly.

## Sketch Viability Score

9

## Sketch Review Status

ACCEPTED

## Smallest Retry Target

None

## Retry Mode

none

## Audit Summary

- Goal alignment: The sketch targets every clause of the exact formalized goal: one arbitrary-output distribution-free realizable learner per class, the displayed `N_a` and `delta_a`, exact `(SC)` and `(LD)`, sequence-level `(AS)`, every-fixed-`rho` `(PN)`, the standard source-allowance comparison, and the resulting Item 3 contradiction. It does not claim the primitive global-stability profile and repeatedly preserves the `conditional` progress label and the remaining source gap.
- Dependency audit: `step_001` through `step_006` form an acyclic graph whose edges point only to earlier steps. Each step has a stable ID, exact intended claim, assumptions, proof tool or challenge, output target, rate objective, and `PENDING` review status. Every generated output has a producer and a dependency path to its listed consumers.
- High-risk coverage: The sketch localizes the cited-source wrapper, universal constant extraction, label and adjacency conventions, fixed-distribution-to-distribution-free quantifier wrapper, padding, bounded-`q` privacy activation, exact Item 2 quantifier extraction, `d=0`, ceiling control, every fixed `rho>0`, privacy monotonicity, and final asymptotic contradiction. No theorem-critical bridge is left as generic future work.
- Explicit-rate coverage: Objectives R1--R3 declare exposed variables, allowed and forbidden hidden-constant dependence, fixed quantities, probability mode, horizon or limiting mode, norm or metric mode, and baseline obligations. The specialization keeps the cardinality correction `u(L)` explicit and gives a term-by-term route from the source bound to `N_a`.
- Assumption and citation plausibility: `assump:polynomial-global-stability` is the only nonstandard primitive theorem assumption; all private learners, schedules, and asymptotics are assigned to derived steps. The exact arXiv:2003.00563v3 source was checked: the shared theorem counter makes `thm:selection` Theorem 17, its statement has exactly the two displayed big-O terms, its range is all of `{-1,+1}^X`, and its DP definition is symmetric on fixed-length samples disagreeing in one example. Its construction uses `G` and public parameters, not the realizable distribution, so one fixed `G_C` yields the claimed distribution-free wrapper.

## Early Obstruction Audit

- Limiting-case stress: The roadmap survives `q=1`, `d=0`, bounded `q_kappa`, bounded Littlestone dimension, `L=0` for isolated per-class identities, and `L_kappa -> infinity` for sequence claims. At bounded `q_kappa`, `u(L_kappa) -> infinity` simultaneously gives `N_kappa=O(u(L_kappa))=o(L_kappa)` and activates negligible privacy; no step assumes `q_kappa -> infinity`.
- Theorem-critical bridge support: All six bridges have existing sources under the unchanged theorem contract. `step_001` wraps the cited theorem; `step_002` proves the sample ceiling and padding; `step_003` proves the exact schedule; `step_004` extracts subpower growth; `step_005` proves uniform privacy negligibility; and `step_006` performs only dependency-complete assembly.
- Exported-interface feasibility: Each exported interface lists its raw controls, controlled defect classes, transfer or dominance relation, margin or slack source, consumers, and missing-interface status. The BLM output is recoded bijectively, padding is an exact suffix projection, and `(AS)` and `(PN)` concern the exact `N_a` and `delta_a`, not surrogate quantities.
- Theorem-critical mechanism witness gate: Passed for `step_001`--`step_006`. The positive sources are respectively the accurate atom plus BLM selection, exponent slack plus `1+T_a`, the explicit `u(L)` exponent, the full `(SP)` quantifier plus finite-class counting, exponential decay in `x+u`, and `N/L -> 0` plus valid privacy at the required allowance. Each witness identifies opposing terms, a concrete control relation, boundary behavior, and producer-consumer provenance.
- Entry-state trace stress: No recursive algorithm, trajectory, or all-time invariant is used, so no first-update recurrence is applicable. The relevant exact and boundary entries are nevertheless traced: binary recoding has zero residual, padded samples are ignored before the source kernel runs, `q=1` gives `eta=1`, and bounded `q_kappa` leaves the `u(L_kappa)` source active before `(AS)`, `(PN)`, and final assembly consume it.
- Obligation locality classification: `step_001` BLM wrapper, `step_002` `(SC)` and padding, `step_003` `(LD)`, `step_004` `(AS)`, `step_005` `(PN)` and source comparison, and `step_006` final assembly are all `step-local`. No hard obligation requires a changed assumption, algorithm, scope, privacy mode, metric, exposed dependence, or conclusion.
- Noncircular closure gate: Passed. The primitive atom certificate precedes BLM selection; the schedule precedes `(PN)`; `(SP)` and counting precede `(AS)`; and the learner and both limiting certificates precede the Item 3 contradiction. Neither private learnability nor the desired rate is used to produce itself.
- Mechanism-source and boundary stress: The atom source remains nonzero at `q=1`; exponent slack controls every source term; `u(L)` excludes the bounded-`q` privacy failure; the full every-`p`, eventually-all-`kappa` premise supplies the sequence upgrade; and the `x`-factor in `(PN)` is uniformly controlled even when it neither converges nor stays bounded.
- Generated-output flow: Passed. The primitive producer feeds `step_001`; the raw learner feeds `step_002`; `(SC)`, `(LD)`, `(AS)`, and `(PN)` are generated before `step_006`; and only `step_006` exports the final conditional theorem. There are no consumers before producers or missing dependency edges.
- Source-to-claim adequacy: arXiv:2003.00563v3, Section 5.2, Theorem 17 (`thm:selection`) assumes one randomized `G` with an atom of mass at least `eta` and loss at most `alpha/2` and concludes an `(epsilon,delta)`-DP map into the full hypothesis space with loss at most `alpha`, failure at most `beta`, and sample size `O(m log(1/(eta beta delta))/(eta epsilon) + log(1/(eta beta))/(alpha epsilon))`. The source uses symmetric replacement of one record. The pointwise `{0,1} <-> {-1,+1}` bijection preserves realizability, atom equality, zero-one loss, i.i.d. sampling, and adjacency, and reverse recoding is postprocessing. Although the source statement fixes a realizable distribution, the displayed algorithm partitions the input and uses only `G` and public parameters; hence a single distribution-independent `G_C` gives one learner valid for every realizable `Q`. The source does not supply the primitive profile or any of `(SC)`--`(PN)`, and the sketch does not attribute those outputs to it.
- Residual-to-target adequacy: The source and branch hypotheses differ only by exact pointwise recoding, so the loss and atom residuals are zero. The padded and unpadded learners are the same kernel on the used prefix, so the PAC and DP residuals are zero. The scalar bridges use the exact displayed `q`, `L`, `T_a`, `N_a`, and `delta_a`; every ceiling or constant residual is explicitly dominated at the consumed target scale.
- Baseline invariance audit: Passed. Recoding preserves the original binary arbitrary-output risk exactly; padding preserves the same PAC and privacy parameters; and bounded-`q` sequences retain both `N_kappa=o(L_kappa)` and `delta_kappa N_kappa^rho -> 0` rather than being excluded or replaced by a fixed-`delta` surrogate. The final theorem retains all admissible finite-class sequences and remains conditional on the original primitive profile.
- Scope-accumulation compatibility: The only accumulated scope is an eventual sequence limit, not privacy composition across `kappa`. `(AS)` uses pointwise eventual inequalities. `(PN)` has the concrete control `delta N^rho <= C e^{-x}x^rho(1+x)^rho e^{-u}(1+u)^rho`; the first factor has a finite supremum for `x>=1`, while the second tends to zero as `u->infinity`. Thus no persistent additive or adversarial-sign defect accumulates.
- Scope and dependence consistency: `K_a` depends only on `a`, fixed `alpha_0,beta_0`, and the universal BLM constant, and not on the class, cardinality, sequence index, distribution, or `epsilon_0`. Sequence asymptotics may hide the fixed `epsilon_0` and requested exponent, as declared. Probability, adjacency, risk, and limiting modes agree with `setting.md`.
- Generated-condition provenance: No realized event, stability trajectory, boundedness invariant, or recurrence is promoted to a theorem assumption. The BLM learner, padded learner, asymptotic estimates, and privacy certificate are all derived outputs with named producers.
- Citation and tool applicability: The source identity, v3 locator, theorem label and number, role, source objects, hypotheses, output range, adjacency convention, quantitative conclusion, distribution-free wrapper, and non-outputs are all fixed. The theorem proof contains informal wording about identifying the frequent hypothesis, but the sketch consumes only the theorem's valid accurate-output conclusion.
- Same-setting repair plausibility: No repair is required. The remaining global-stability-profile gap is the explicit primitive condition defining the conditional theorem, not a missing sketch bridge.
- Target-preserving bridge-repair gate: Passed without repair. Every needed bridge preserves the current target exactly; none weakens an inherited baseline conclusion or changes the theorem contract.
- High-risk obligation classes: Structural source applicability, quantitative specialization, exact recoding and padding, sequence mode conversion, explicit dependence, bounded-parameter boundaries, privacy-scale comparison, and conditional source-direction fidelity were all checked and pass.

## Gate Evidence Table

| Obligation / generated condition | Claim class / theorem role | Mechanism source | Source provenance | Source-to-claim match | Source-convention compatibility | Concrete control relation | Raw controls / exported interface feasibility | Residual-to-target adequacy | Defect / forcing / accumulation behavior | Noncircular producer path | Generated-output producer and consumers | Entry / boundary stress result | Locality verdict | Smallest retry target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BLM wrapper, arbitrary-output learner, and binary-recoding baseline (`step_001`) | Cited-tool conversion producing the only private learner and raw sample bound | BLM Theorem 17 plus the accurate atom from `assump:polynomial-global-stability` | Bun--Livni--Moran, arXiv:2003.00563v3, Section 5.2, Theorem 17, label `thm:selection`; exact primary TeX checked | Source premise is mass `>=eta` and loss `<=alpha/2`; source conclusion is arbitrary-output `(epsilon,delta)`-DP with loss `<=alpha`, failure `<=beta`, and the exact two-term big-O dependence required by `(BLM)` | Source labels are `{-1,+1}` and branch labels are `{0,1}`; `y -> 2y-1` and its inverse preserve exact hypothesis equality, loss, iid samples, and fixed-length one-record replacement; output recoding is postprocessing | `n <= C_BLM[m log(1/(eta beta delta))/(eta epsilon) + log(1/(eta beta))/(alpha epsilon)]`; one fixed `G_C` works for all realizable `Q`, and the source construction uses `G_C` and public parameters rather than `Q` | Raw controls are fixed `G_C`, `m_C<=q^a`, atom mass `>=q^{-a}`, loss `<=alpha_0/2`, and public parameters; exported interface is one distribution-free arbitrary-output learner plus `(BLM)` | Source hypothesis and branch hypothesis are pointwise recodings, so risk and atom residuals are exactly zero; output scope and adjacency also transfer exactly | Finite disjoint producer batches and one fresh selection sample are already accounted for in the source theorem; no iterative or cross-`kappa` accumulation is added | Primitive profile -> recoded `G_C` -> Theorem 17 -> raw learner; neither private learnability nor the target rate is assumed | Producer: primitive profile and `step_001`; consumers: `step_002` and `step_006` | At `q=1`, `eta=1` and `m_C=1`, so the source remains active; randomized/improper outputs and replacement of either coordinate of one example remain within the source convention | `step-local` | None |
| `(SC)`, universal `K_a`, ceiling, and exact-size padding (`step_002`) | Quantitative specialization and baseline-preserving exported sample interface | Direct algebra from `(BLM)` and the primitive profile bounds | Derived `(BLM)` output of `step_001`; branch definitions `b=2a+2`, `T_a>=q^b`, `q>=1`, and fixed `alpha_0,beta_0` | The source terms are retained until each is dominated by the exact `q^b(1+T_a)` target; no cardinality or privacy term is dropped | No external convention change applies: the derivation uses the exact branch scalars and the same source learner; prefix padding uses the branch's ordered replacement adjacency | With `B_0=log(1/beta_0)`, the bracket is `q^{2a}(T_a+a log q+B_0)+alpha_0^{-1}(a log q+B_0) <= D_a q^b(1+T_a)` using `a log q<=a q^b`, `q^{2a}<=q^b<=T_a`; then `epsilon_0^{-1}<=epsilon_0^{-2}` and `K_a>=C_BLM D_a` | Raw controls are `(BLM)`, `m<=q^a`, `eta=q^{-a}`, and the scalar inequalities; export is `n_BLM<=N_a` and an exactly `N_a`-record learner, with `K_a` independent of class, cardinality, index, distribution, and `epsilon_0` | Ignoring the unused suffix gives the identical source kernel on the used prefix; iid utility is unchanged, and adjacency in an ignored coordinate gives identical outputs, so the PAC/DP residual is zero | Fixed per-class arithmetic only; logarithmic, confidence, alpha, epsilon, and rounding defects are separately absorbed, with no accumulated residual | `step_001` -> `(SC)` and prefix projection in `step_002` -> `step_004`/`step_006`; `(AS)` is not assumed | Producer: `step_002`; consumers: `step_004` and `step_006` | At `q=1`, `log q=0` and `1+T_a` still dominates; as `epsilon_0` approaches 1 from below, `epsilon_0^{-2}` still dominates `epsilon_0^{-1}`; the outer ceiling covers integer sample size | `step-local` | None |
| Exact `(LD)` schedule and bounded-`q` privacy activation (`step_003`) | Exact privacy specialization and baseline invariance for the critical bounded-complexity regime | Direct evaluation of the declared `u`, `T_a`, and `delta_a` | Primitive scalar definitions in `setting.md` | The direct source supplies the exact privacy parameter consumed by BLM and final assembly, not a proxy or asymptotic replacement | No external convention applies: all logarithms here are the declared natural logarithms and the same `delta_a` is passed downstream | For `L>=0`, `u(L)=log log(e^e+L)>=1`, `e^{-u(L)}=1/log(e^e+L)`, `T_a=q^b+u(L)>=2`, and `log(1/delta_a)=T_a=q^{2a+2}+log log(e^e+L)` exactly | Raw controls are `q>=1`, `L>=0`, and the definitions; export is exact `delta_a in (0,1)` and `(LD)` to `step_005` and `step_006` | Produced and consumed privacy parameters are identical, so residual is zero; no cardinality dependence is hidden | There is no privacy composition across classes; pointwise monotone exponent growth controls the limiting regime. When `q` is bounded, `u(L)->infinity` is the active source | Definitions -> `step_003` -> `step_005`/`step_006`; privacy negligibility is not assumed in defining the schedule | Producer: `step_003`; consumers: `step_005` and `step_006` | At `L=0`, `u=1`; at `q=1`, `T_a>=2`; along bounded `q_kappa` and `L_kappa->infinity`, `u(L_kappa)` alone forces `delta_kappa->0` | `step-local` | None |
| `(AS)` from the exact Item 2 quantifiers, including `d=0` and bounded `q` (`step_004`) | Sequence mode conversion and sublogarithmic sample certificate | Full `(SP)` quantifier, `L_kappa->infinity`, `ell_kappa<=L_kappa/log 2`, and elementary log-star growth | Primitive sequence premises and standard finite-class counting stated in `setting.md` | These sources control the exact target variables `d,s,q,T_a,N_a`; no proxy dimension or altered cardinality benchmark is introduced | Direct current-notation derivation; natural-log `L` and base-two `log*` are related only through the explicit counting inequality and an upper growth comparison | For each `t>0`, choose integer `p` with `1/p<t/2`; eventually `d<L^{1/p}<=L^{t/2}` while `s=O(1+log(e+L))<=L^{t/2}`, so `q<=2L^{t/2}<=L^t`. Fixed powers, sums, products, `u(L)=L^{o(1)}`, and the ceiling then give `T_a,N_a=L^{o(1)}` and hence `N_a=o(L)` | Raw controls are the exact every-`p`, eventually-all-`kappa` premise, size divergence, counting, and the explicit formula; export is exact `(AS)` for the target `N_a` | All comparisons concern the exact target variables. Fixed factors and the ceiling are controlled by exponent allocation; no residual changes the `L^{o(1)}` scale | Eventual inequalities are pointwise in `kappa`, not summed. Arbitrarily small exponent budgets absorb every fixed factor, so there is no persistent additive defect | `(SP)` plus counting and formulas from `step_002`/`step_003` -> `step_004` -> `step_006`; the final sublogarithmic conclusion is not a premise | Producer: `step_004`; consumer: `step_006` | If `d=0`, the root inequality remains valid and no division by `d` occurs; if `q` is bounded, `N=O(u(L))=o(L)`; a single-`p` or infinitely-often premise would fail, but the accepted setting supplies the full quantifier | `step-local` | None |
| `(PN)`, ceiling control, and every fixed `rho>0` (`step_005`) | Limiting privacy closure and exact target-scale residual domination | Exact `(LD)`, explicit `N_a`, `x=q^b>=1`, `u(L)->infinity`, and exponential-versus-polynomial domination | Derived schedule from `step_003` and exact size formula from `step_002`; only primitive size divergence is additionally used | The mechanism controls the exact product `delta_kappa N_kappa^rho` for each fixed `rho`, uniformly over every possible behavior of `x_kappa` | Direct current-notation scalar derivation; the same exact `N_a` and `delta_a` are consumed by the theorem | If `A=K_a epsilon_0^{-2}x(1+x+u)`, then `A>=1` and `N=ceil A<=2A`; hence `delta N^rho <= C e^{-x}x^rho(1+x)^rho e^{-u}(1+u)^rho`. The `x` factor has finite supremum on `[1,infinity)`, and the `u` factor tends to zero for every fixed `rho>0` | Raw controls are exact `x,u,N,delta` and the ceiling; export is `(PN)` plus the source-allowance certificate. Constants may depend on fixed `a,rho,epsilon_0,K_a` but not on `kappa,q,L,C` | The ceiling produces only the displayed constant factor; every remaining residual is dominated at the exact `N^{-rho}` target scale | No DP defect is composed over `kappa`. Arbitrary or adversarial variation of `x_kappa` is neutralized by a uniform supremum, while the independent `u_kappa` factor vanishes | `step_003` plus exact `N_a` and `L->infinity` -> `step_005` -> `step_006`; `(PN)` is not an admissibility premise | Producer: `step_005`; consumer: `step_006` | For constant `q_kappa` (including `q=1`), the `x` factor is fixed and the `u` factor still vanishes; for unbounded `q`, exponential decay only helps; the proof is valid for every fixed noninteger or integer `rho>0` | `step-local` | None |
| `rho=3` source comparison, DP monotonicity, theorem closure, and conditional label (`step_006`) | Final theorem closure, privacy-mode comparison, asymptotic contradiction, and source-direction fidelity | Padded learners, `(AS)`, `(PN)`, the defining DP inequality, and the definition of an eventual `Omega(L)` lower bound | Derived outputs of `step_002`--`step_005`; source direction and `conditional` progress metadata from `idea.md`, `setting.md`, and the research brief | The same unrestricted learner on the same class and risk metric has `N/L->0` at a privacy parameter no larger than the allowed source parameter; the conclusion is stated only under the primitive profile | Branch and source use the same symmetric replacement-DP convention after recoding. Relaxing from `delta` to `bar delta` changes no object or metric and is valid only when `bar delta>=delta` | `(PN)` with `rho=3` gives `delta N^3->0`; since `log(eN)<=N` for `N>=1`, eventually `delta<=c/N^3<=c/[N^2 log(eN)]` for every fixed `c>0`. If `bar delta(N)>=delta`, then `P<=e^epsilon P'+delta<=e^epsilon P'+bar delta` in both directions. Finally `N/L->0` contradicts every positive eventual `Omega(L)` constant | Raw controls are the exact padded learner and all four bridge outputs; export is the exact conditional universal-nonexistence theorem at the stated or weaker privacy allowance | Same class, sample count, risk, adjacency, and learner are consumed; privacy relaxation has zero object residual and only increases the allowed additive parameter. The unresolved profile is retained as an explicit assumption, not hidden as a residual | Pointwise learners are compared eventually; there is no probability union or privacy composition across the sequence. Unknown positive lower-bound constants are handled by `N/L->0` | All prior producers -> `step_006` -> final assembly; Item 3's negation and the universal profile's truth are never used as producers | Producer: `step_006`; consumer: final theorem assembly | On bounded `q`, preceding rows still supply both required rates. Equality `bar delta=delta` and every larger allowance have the correct monotonicity direction. Removing the profile would leave the theorem unproved, and the sketch explicitly keeps the result `conditional` | `step-local` | None |

## Blocking Issues

None

## Required Repair Bundle

None

## Review Rationale

The sketch is ready for step-level proof work. Its only unresolved mathematical content is partitioned into lemma-sized local derivations whose mechanism sources, conventions, quantitative interfaces, boundary cases, and producer-consumer paths are already fixed under the accepted setting. Independent primary-source and algebraic checks support the BLM wrapper, `(SC)`, `(LD)`, `(AS)`, `(PN)`, the `rho=3` comparison, and privacy monotonicity. Because no roadmap, assumption, dependency, generated-output interface, theorem scope, or progress label must change, `ACCEPTED` is the smallest and correct status.
